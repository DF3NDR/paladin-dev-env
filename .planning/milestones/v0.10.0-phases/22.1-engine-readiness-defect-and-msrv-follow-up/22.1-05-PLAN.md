---
phase: 22.1-engine-readiness-defect-and-msrv-follow-up
plan: 05
type: execute
wave: 4
depends_on: [22.1-02, 22.1-03, 22.1-04]
files_modified:
  - .planning/phases/22.1-engine-readiness-defect-and-msrv-follow-up/22.1-CI-EVIDENCE.md
  - .planning/phases/22-battlefield-state-superstep-engine/22-UAT.md
autonomous: false
requirements: [ENG-05]

must_haves:
  truths:
    # --- Phase goal truths (D-12/D-13/D-14) ---
    - "G-22-1's closing evidence is a ci.yml workflow run on the branch head whose OVERALL conclusion is success — not a green job inside a run whose overall conclusion is cancelled or failure (D-12)."
    - "The cited run's head SHA equals the branch head SHA at the time of capture, and that head contains the commits from plans 22.1-01 through 22.1-04 (D-14)."
    - "All four Postgres log facts are quoted verbatim from the job log with their timestamps, not paraphrased: container healthy, pg_isready accepting connections, the test-result line, and the skip-detection line (D-12)."
    - "The msrv, Security Audit and License & Dependency Policy jobs — the three that were failure in the previously-cited run 33688238662 — are success in the cited run."
    - "22-UAT.md's G-22-1 resolution field carries a pointer to 22.1-CI-EVIDENCE.md and to the new run id (D-13)."
    # --- Edge coverage lifted from the spec-less probe fallback report (ENG-05) ---
    - "adjacency (ENG-05): the quoted test-result line's passed count equals EXACTLY the number of #[tokio::test] functions the waypoint::postgres module declares — the CI job's own declared-count step is the authority, and a count greater or fewer than the declared number is not evidence."
    - "empty (ENG-05): a run in which zero waypoint::postgres tests actually executed against the live server (the env-gated suite taking its SKIP path) is NOT evidence, regardless of a green job status — the skip-detection step's line is what distinguishes exercised from skipped, and its absence invalidates the capture."
    - "ordering (ENG-05): when more than one ci.yml run exists for the branch head, the evidence names exactly one run — the one whose headSha equals the branch head and whose createdAt is the latest among runs on that SHA — and if two runs tie on both, both run ids are recorded rather than one silently chosen."
  artifacts:
    - ".planning/phases/22.1-engine-readiness-defect-and-msrv-follow-up/22.1-CI-EVIDENCE.md — run id, job id, commit SHA, timestamps and verbatim quoted log facts, following the 18-CODEQL-EVIDENCE.md format."
    - ".planning/phases/22-battlefield-state-superstep-engine/22-UAT.md — G-22-1 resolution field extended with the pointer to the new evidence."
  key_links:
    - "gh run view <id> --json conclusion (run level) -> gh run view --json jobs (job level): both are required — a job-level query alone cannot see that the run was superseded and cancelled, which is exactly how run 33688238662 was mis-read."
    - "ci.yml's concurrency group -> a superseded run's cancelled conclusion: the mechanism that makes a later push invalidate an earlier run's evidence, and the reason D-14 makes this the phase's last plan."
    - "ADR-0044's required-check set -> postgres-integration and msrv: both already gate PR #52's merge, so no ruleset edit is needed (D-13)."
  prohibitions:
    - statement: "A green job inside a workflow run whose overall conclusion is not success MUST NOT be recorded as closing evidence for a gap, and a paraphrase of a log line MUST NOT stand in for the quoted line — evidence that reads as assurance while proving less than it claims is worse than recording no evidence at all."
      status: unverified
      verification: flagged-unverified
---

<objective>
Capture G-22-1's closing evidence: a `ci.yml` workflow run on the branch head, after every other
Phase 22.1 change has landed, whose OVERALL conclusion is `success` and whose
`postgres-integration` job carries the four required log facts.

Purpose: 22-17 closed G-22-1 on job `100440861780` inside run `33688238662` — a green Postgres job
inside a run whose overall conclusion is `cancelled` (superseded by the next push under the CI
concurrency group) and whose `MSRV`, `Security Audit` and `License & Dependency Policy` jobs all
failed. The gap's record names evidence that does not meet its own bar (D-12).

Output: `22.1-CI-EVIDENCE.md` in the 18-CODEQL-EVIDENCE.md format, and a pointer appended to
22-UAT.md's G-22-1 resolution.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22.1-engine-readiness-defect-and-msrv-follow-up/22.1-CONTEXT.md
@.planning/phases/22.1-engine-readiness-defect-and-msrv-follow-up/22.1-02-SUMMARY.md
@.planning/phases/22.1-engine-readiness-defect-and-msrv-follow-up/22.1-03-SUMMARY.md
@.planning/phases/22.1-engine-readiness-defect-and-msrv-follow-up/22.1-04-SUMMARY.md
@.planning/phases/22-battlefield-state-superstep-engine/22-17-SUMMARY.md
@.planning/decisions/0044-branch-protection-posture.md
</context>

## Artifacts this phase produces

| Kind | Symbol / path | Plan |
|------|---------------|------|
| new file | `.planning/phases/22.1-engine-readiness-defect-and-msrv-follow-up/22.1-CI-EVIDENCE.md` | 22.1-05 |
| doc field | the extended `resolution` value on `22-UAT.md`'s `G-22-1` entry | 22.1-05 |

<tasks>

<task type="auto">
  <name>Task 1: Capture the whole-run evidence and author 22.1-CI-EVIDENCE.md</name>
  <precondition>`gh auth status` exits 0 in this shell (GH_TOKEN reaches non-interactive shells via BASH_ENV in this devcontainer) and `git rev-parse --abbrev-ref HEAD` reports the phase branch.</precondition>
  <files>.planning/phases/22.1-engine-readiness-defect-and-msrv-follow-up/22.1-CI-EVIDENCE.md</files>
  <read_first>
    - `.planning/milestones/v0.9.0-phases/18-rust-sast-evaluate-and-adopt-codeql/18-CODEQL-EVIDENCE.md` — the evidence-file format this file must follow: run id, job id, commit SHA, timestamps, and log facts quoted verbatim rather than summarized. Read it before writing a single line.
    - `.planning/phases/22-battlefield-state-superstep-engine/22-17-SUMMARY.md` — the four log facts as 22-17 defined them, and the quoting style (verbatim line with its timestamp).
    - `.github/workflows/ci.yml` — the `postgres-integration` job (~lines 811-886): the container-health step, the `pg_isready` assertion step, the contract-suite step, the "Fail if the suite took the SKIP path" step, and the "Fail if the suite selected fewer tests than it declares" step. These five steps produce the log lines to quote, and the last one is the authority for the declared test count.
    - `.github/workflows/ci.yml` line 26 — the `concurrency` group that causes a superseded run to conclude `cancelled`, the mechanism that invalidated the previously-cited evidence.
    - `.planning/decisions/0044-branch-protection-posture.md` — confirms `postgres-integration` and `msrv` are already in the required-check set, so no ruleset edit is in scope.
  </read_first>
  <action>
1. Push the branch head so CI runs against a head that contains every Phase 22.1 commit (D-14).
   Confirm first that the working tree is clean and that `git log --oneline` shows the commits from
   plans 22.1-01 through 22.1-04.
2. Wait for the run to finish, then resolve the run at the run level, not the job level:
   `gh run list --workflow ci.yml --branch <branch> --json databaseId,headSha,createdAt,conclusion`
   and select the run whose `headSha` equals `git rev-parse HEAD` with the latest `createdAt`. If
   two runs tie on both SHA and timestamp, record both ids rather than choosing one silently.
3. Read the run-level verdict: `gh run view <run-id> --json conclusion,status,headSha,jobs`. The
   overall `conclusion` MUST be `success`. If it is `cancelled` (a later push superseded it) or
   `failure`, do not record it — re-run or re-push and repeat. A green `postgres-integration` job
   inside a non-`success` run is explicitly not evidence (D-12).
4. Confirm at the job level that `postgres-integration` is `success`, and that the three jobs which
   were `failure` in the previously-cited run — `msrv`, `Security Audit`, `License & Dependency
   Policy` — are all `success` in this run.
5. Extract the four log facts from the Postgres job with `gh run view --job <job-id> --log`, and
   quote each verbatim with its timestamp — never paraphrased:
   (a) the container-healthy line from the "Start postgres-test and wait for healthy" step;
   (b) the `pg_isready` line reporting that the server accepts connections;
   (c) the `test result: ok. N passed; 0 failed` line, where N must equal the count the job's own
       "Fail if the suite selected fewer tests than it declares" step computes for the
       `waypoint::postgres` module — record that step's output alongside it so the equality is
       auditable rather than asserted;
   (d) the skip-detection step's line confirming every `waypoint::postgres` test exercised the live
       server. Its absence means the env-gated suite took its SKIP path and the capture is invalid.
6. Write `.planning/phases/22.1-engine-readiness-defect-and-msrv-follow-up/22.1-CI-EVIDENCE.md` in
   the 18-CODEQL-EVIDENCE.md format: run id and URL, the run-level `conclusion`, head SHA,
   workflow-run created/updated timestamps, the Postgres job id and its `conclusion`, the
   per-job conclusion table for at least the four named jobs, the four quoted log facts in fenced
   blocks, and a short "why the previously-cited evidence did not meet the bar" paragraph naming
   run `33688238662`, its `cancelled` overall conclusion and its three failed jobs. State plainly
   what the evidence proves (the suite executed against a live server inside a fully green run) and
   what it does not.
Do not edit any ruleset or branch-protection configuration — ADR-0044 already places both jobs in
the required-check set (D-13).
  </action>
  <verify>
    <automated>test -s .planning/phases/22.1-engine-readiness-defect-and-msrv-follow-up/22.1-CI-EVIDENCE.md && RUN=$(grep -oE '[0-9]{8,}' .planning/phases/22.1-engine-readiness-defect-and-msrv-follow-up/22.1-CI-EVIDENCE.md | head -1) && gh run view "$RUN" --json conclusion,headSha --jq '.conclusion' | grep -qx success && gh run view "$RUN" --json jobs --jq '[.jobs[] | select(.name | test("Postgres|MSRV|Security Audit|License")) | .conclusion] | unique | .[]' | grep -qx success</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/22.1-engine-readiness-defect-and-msrv-follow-up/22.1-CI-EVIDENCE.md` exists and is non-empty.
    - The run id recorded in the file resolves, and `gh run view <run-id> --json conclusion --jq .conclusion` prints exactly `success`.
    - `gh run view <run-id> --json headSha --jq .headSha` equals the SHA recorded in the evidence file, and that SHA is an ancestor-or-equal of the branch head containing plans 22.1-01 through 22.1-04.
    - `gh run view <run-id> --json jobs --jq '[.jobs[] | select(.name | test("Postgres Waypoint Contract Suite")) | .conclusion] | .[]'` prints `success`.
    - `gh run view <run-id> --json jobs --jq '[.jobs[] | select(.name | test("MSRV|Security Audit|License & Dependency Policy")) | .conclusion] | unique | .[]'` prints only `success`.
    - The evidence file contains all four quoted log facts (paths below are `.planning/phases/22.1-engine-readiness-defect-and-msrv-follow-up/22.1-CI-EVIDENCE.md`): `grep -c 'accepting connections'` returns at least 1; `grep -cE 'test result: ok\. [0-9]+ passed; 0 failed'` returns at least 1; `grep -c 'exercised the live server'` returns at least 1; and a container-health line from the "wait for healthy" step is present.
    - The evidence file names the superseded prior run: `grep -c '33688238662'` returns at least 1, and `grep -c 'cancelled'` returns at least 1.
    - `git status --porcelain` lists no change to any file under `.github/` — no ruleset or workflow edit was made during evidence capture.
  </acceptance_criteria>
  <done>A fully green `ci.yml` run on the branch head is recorded by id, SHA and timestamp, with the four Postgres log facts quoted verbatim and the previously-cited run's shortfall named explicitly.</done>
</task>

<task type="auto">
  <name>Task 2: Point G-22-1's UAT record at the closing evidence</name>
  <files>.planning/phases/22-battlefield-state-superstep-engine/22-UAT.md</files>
  <read_first>
    - `.planning/phases/22-battlefield-state-superstep-engine/22-UAT.md` — the `G-22-1` entry in full, especially its existing `resolution` field (which currently cites run `33688238662`, commit `2b2bc1d5` and job `100440861780`) and the YAML style of the surrounding gap entries.
    - `.planning/phases/22.1-engine-readiness-defect-and-msrv-follow-up/22.1-CI-EVIDENCE.md` — the file written by Task 1, for the exact run id and path to cite.
  </read_first>
  <action>
Append a single sentence to `G-22-1`'s `resolution` field — do not rewrite or delete the existing
text, which is the historical record of what 22-17 believed at the time. The appended sentence must
state: that the 22-17 evidence was a green job inside a run whose overall conclusion was `cancelled`
and which contained three failed required jobs; that Phase 22.1 re-captured the evidence against a
fully green run; and the new run id plus the relative path to `22.1-CI-EVIDENCE.md`.

Leave every other field of the entry (`truth`, `status`, `reason`, `severity`, `test`,
`root_cause`, `artifacts`) untouched, and change no other gap entry in the file. Keep the field a
valid single YAML scalar — match the quoting style already used by the surrounding entries so the
file still parses.
  </action>
  <verify>
    <automated>grep -c '22.1-CI-EVIDENCE.md' .planning/phases/22-battlefield-state-superstep-engine/22-UAT.md && python3 -c "import sys,yaml" 2>/dev/null; grep -A3 'gap_id: G-22-1' .planning/phases/22-battlefield-state-superstep-engine/22-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '22.1-CI-EVIDENCE.md' .planning/phases/22-battlefield-state-superstep-engine/22-UAT.md` returns 1.
    - The `G-22-1` `resolution` field still contains its original text: `grep -c '100440861780' .planning/phases/22-battlefield-state-superstep-engine/22-UAT.md` returns at least 1.
    - `git diff --stat .planning/phases/22-battlefield-state-superstep-engine/22-UAT.md` shows changes confined to the `G-22-1` entry, and `git diff -U0 .planning/phases/22-battlefield-state-superstep-engine/22-UAT.md | grep -c '^-[^-]'` returns at most 1 (only the resolution line was replaced, nothing deleted outright).
    - `grep -c 'gap_id:' .planning/phases/22-battlefield-state-superstep-engine/22-UAT.md` returns the same value as before the edit.
  </acceptance_criteria>
  <done>G-22-1's record names the run that finally satisfied it and the evidence file that documents it, with the historical record preserved rather than overwritten.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm the closing evidence meets its own bar</name>
  <what-built>
    A fully green `ci.yml` workflow run was captured on the Phase 22.1 branch head, recorded in
    `.planning/phases/22.1-engine-readiness-defect-and-msrv-follow-up/22.1-CI-EVIDENCE.md`, and
    pointed at from `22-UAT.md`'s `G-22-1` resolution field. The engine readiness fix, the truthful-
    outcome guards, the fingerprint fix and the MSRV bump are all on the head that run tested.
  </what-built>
  <how-to-verify>
    1. Read `.planning/phases/22.1-engine-readiness-defect-and-msrv-follow-up/22.1-CI-EVIDENCE.md`.
    2. Run `gh run view <run-id> --json conclusion,headSha,createdAt --jq '{conclusion, headSha, createdAt}'`
       and confirm `conclusion` is `success` and `headSha` matches the SHA in the file.
    3. Run `gh run view <run-id> --json jobs --jq '[.jobs[] | {name, conclusion}]'` and confirm the
       Postgres, MSRV, Security Audit and License & Dependency Policy jobs are all `success`.
    4. Confirm the four quoted log facts in the file are verbatim lines (with timestamps), not
       paraphrases, by spot-checking one against
       `gh run view --job <job-id> --log | grep -F '<quoted fragment>'`.
    5. Confirm `git log --oneline` on the branch head shows the commits from plans 22.1-01 through
       22.1-04 at or before the recorded SHA.
    6. Confirm no branch-protection or ruleset change was made: `git status --porcelain` shows only
       the two planning files.
  </how-to-verify>
  <resume-signal>Type "approved" to seal G-22-1, or describe what fails the bar</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions -> the evidence record | CI output crosses into a durable project claim. An incorrectly-scoped query (job level instead of run level) produces a record that reads as assurance while proving less than it claims. |
| local shell -> GitHub API (`gh`, GH_TOKEN) | An authenticated credential is used for read-only queries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22.1-16 | Repudiation (a gap recorded closed on evidence that does not meet its bar) | `22.1-CI-EVIDENCE.md` / `22-UAT.md` G-22-1 | high | mitigate | Run-level `conclusion` must be `success` before capture, verified independently at the job level and again by a blocking human checkpoint; the previously-cited run's shortfall is named in the file itself (Task 1, D-12). |
| T-22.1-17 | Spoofing (a skipped suite presenting as an executed one) | the env-gated `waypoint::postgres` suite | high | mitigate | The skip-detection line and the declared-test-count step are both required facts, and the `empty` edge truth makes a zero-executed run explicitly non-evidence — the same failure shape that made G-22-1 a gap in the first place. |
| T-22.1-18 | Information Disclosure | `gh` log extraction and the evidence file | medium | mitigate | Only workflow log lines about container health, connection readiness and test counts are quoted; no secret, token or environment value is copied into the file. `GH_TOKEN` is read from the environment and never echoed into a command line, log or committed file. |
| T-22.1-19 | Tampering (silently choosing a favourable run among several) | run selection | medium | mitigate | The `ordering` edge truth pins selection to head-SHA equality plus latest `createdAt`, and requires recording both ids on a tie rather than silently choosing (Task 1 step 2). |
| T-22.1-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no packages and touches no manifest or lockfile; it writes two planning documents. `22.1-RESEARCH.md` §Package Legitimacy Audit records no `[ASSUMED]`/`[SUS]`/`[SLOP]` package for the phase. |
</threat_model>

<verification>
- `gh run view <run-id> --json conclusion --jq .conclusion` prints `success`.
- Postgres, MSRV, Security Audit and License & Dependency Policy jobs all `success` in that run.
- All four log facts present verbatim in `22.1-CI-EVIDENCE.md`.
- `22-UAT.md` G-22-1 resolution points at the new run and evidence file, with its original text
  preserved.
- Human checkpoint approved.
</verification>

<success_criteria>
G-22-1 is closed on a whole-workflow-run `success` captured after every other Phase 22.1 change
landed, with the four Postgres log facts quoted verbatim, the previously-cited run's shortfall named
explicitly, and a human confirmation that the evidence meets its own bar.
</success_criteria>

<output>
Create `.planning/phases/22.1-engine-readiness-defect-and-msrv-follow-up/22.1-05-SUMMARY.md` when done
</output>
