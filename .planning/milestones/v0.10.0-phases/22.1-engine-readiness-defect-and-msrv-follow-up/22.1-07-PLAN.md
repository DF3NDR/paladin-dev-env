---
phase: 22.1-engine-readiness-defect-and-msrv-follow-up
plan: 07
type: execute
wave: 6
depends_on: [22.1-06]
files_modified:
  - .planning/phases/22.1-engine-readiness-defect-and-msrv-follow-up/22.1-CI-EVIDENCE.md
autonomous: true
requirements: [ENG-05]

must_haves:
  truths:
    # --- Phase goal truths (D-25, restating D-12's bar on the final head) ---
    - "D-25: the re-captured evidence is a ci.yml workflow run whose OVERALL conclusion is success — not a green job inside a run whose overall conclusion is cancelled or failure — on the branch head that CONTAINS Plan 22.1-06's BUG-04 fix."
    - "D-25: the Postgres, MSRV, Security Audit and License & Dependency Policy jobs are all success in that same run, and the four Postgres log facts are quoted verbatim with their timestamps rather than paraphrased."
    - "D-25: the original run's section stays in 22.1-CI-EVIDENCE.md intact as G-22-1's closing evidence; the new material is a dated section appended below it, never a replacement — an evidence file that overwrites its own history cannot show that the bar was met twice, on two different heads, for two different reasons."
    - "D-25: verification is automated only — the developer approved the evidence format and bar at the 22.1-05 checkpoint, so this plan carries no blocking human gate."
    - "D-20/D-25: this run is the evidence that the program gates are green on the phase's FINAL commit — the msrv job at the 1.88 floor, cargo semver-checks vs 0.9.0, the api-surface job, Security Audit and License & Dependency Policy all conclude success on the same head."
    - "The push is performed by the orchestrator; this plan waits for and reads the resulting run. If the branch head at capture time does not contain Plan 22.1-06's commits, the capture is invalid regardless of the run's colour (D-14's rule, restated for the second capture)."
    # --- Edge coverage for ENG-05 (carried from Plan 22.1-05's probe-derived truths; the same
    #     requirement, the same evidence bar, one head later — no second probe is run) ---
    - "adjacency (ENG-05): the quoted test-result line's passed count equals EXACTLY the number of #[tokio::test] functions the waypoint::postgres module declares at the tested commit — the CI job's own declared-count step is the authority. After Plan 22.1-06 adds its two contract wrappers that number is 28, up from the 26 the first capture recorded; a count that does not match the job's own computed EXPECTED is not evidence."
    - "empty (ENG-05): a run in which zero waypoint::postgres tests actually executed against the live server (the env-gated suite taking its SKIP path) is NOT evidence, regardless of a green job status — the skip-detection step's line is what distinguishes exercised from skipped, and its absence invalidates the capture."
    - "ordering (ENG-05): when more than one ci.yml run exists for the branch head, the evidence names exactly one run — the one whose headSha equals the branch head and whose createdAt is the latest among runs on that SHA — and if two runs tie on both, both run ids are recorded rather than one silently chosen."
    - "boundary (ENG-05): the recorded head SHA must be an ancestor-or-equal of the branch head AND contain Plan 22.1-06's fix commit; `git merge-base --is-ancestor <recorded-sha> HEAD` exiting 0 plus `git log --oneline <recorded-sha> | grep 'fix(22.1-06)'` matching are the two checks that pin it."
  artifacts:
    - ".planning/phases/22.1-engine-readiness-defect-and-msrv-follow-up/22.1-CI-EVIDENCE.md — a new dated `## Re-capture after BUG-04 (<date>)` section carrying the new run id, head SHA, run-level conclusion, per-job table, the four quoted Postgres log facts, and the D-20 program-gate confirmation; the existing sections unmodified."
  key_links:
    - "gh run view <id> --json conclusion (run level) -> gh run view --json jobs (job level): both are required — a job-level query alone cannot see that the run was superseded and cancelled, which is exactly how run 33688238662 was mis-read at checkpoint 22-17."
    - "Plan 22.1-06's two new postgres.rs #[tokio::test] wrappers -> the postgres-integration job's `grep -cE '^\\s*#\\[tokio::test\\]'` declared-count step -> the quoted `test result: ok. N passed` line: adding contract clauses is exactly what moves N, so the declared-vs-passed equality is the check that proves the new clauses actually ran against the live server."
    - "ci.yml's concurrency group -> a superseded run's cancelled conclusion: the mechanism that makes a later push invalidate an earlier run's evidence, and the reason this capture is the phase's last plan."
  prohibitions:
    - statement: "A green job inside a workflow run whose overall conclusion is not success MUST NOT be recorded as closing evidence, and a paraphrase of a log line MUST NOT stand in for the quoted line — evidence that reads as assurance while proving less than it claims is worse than recording no evidence at all."
      status: unverified
      verification: flagged-unverified
    - statement: "The first capture's section MUST NOT be edited, rewritten or deleted to make the file read as one clean result. The historical record of what was true on the earlier head is part of the evidence, not clutter."
      status: unverified
      verification: flagged-unverified
---

<objective>
Re-capture the whole-workflow-run CI evidence on the branch head that contains Plan 22.1-06's BUG-04
fix, and append it to `22.1-CI-EVIDENCE.md` as a dated section, leaving the original run's section in
place as G-22-1's closing evidence.

Purpose: D-14 made the first capture the phase's last plan precisely because a run that is green only
because a later push cancelled it does not count. Promoting BUG-04 into scope moved the phase's final
commit, so the first capture (run `33706948313`, head `7c77b0e2`) no longer sits on the head the
phase ships. D-25 requires the same bar re-met on the new head, and states that this also satisfies
D-20's requirement that the program gates are green on the phase's final commit.

Output: a `## Re-capture after BUG-04` section in `22.1-CI-EVIDENCE.md` with the new run id, head
SHA, run-level and per-job conclusions, the four Postgres log facts quoted verbatim, and the D-20
gate confirmation.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22.1-engine-readiness-defect-and-msrv-follow-up/22.1-CONTEXT.md
@.planning/phases/22.1-engine-readiness-defect-and-msrv-follow-up/22.1-CI-EVIDENCE.md
@.planning/phases/22.1-engine-readiness-defect-and-msrv-follow-up/22.1-05-SUMMARY.md
@.planning/phases/22.1-engine-readiness-defect-and-msrv-follow-up/22.1-06-SUMMARY.md
@.planning/decisions/0044-branch-protection-posture.md
</context>

## Artifacts this phase produces

| Kind | Symbol / path | Plan |
|------|---------------|------|
| doc section | `## Re-capture after BUG-04 (<date>)` in `.planning/phases/22.1-engine-readiness-defect-and-msrv-follow-up/22.1-CI-EVIDENCE.md` | 22.1-07 |
| doc subsection | `### D-20 program-gate confirmation on the phase's final commit` (nested under the re-capture section) | 22.1-07 |

No source symbol, test or public API is produced by this plan. It carries no `type="tracer"` task
for the same reason Plan 22.1-05 did not: there is no code path to slice — the plan reads a CI run
and writes one planning document. The phase's tracer is Plan 22.1-01's readiness slice, extended by
Plan 22.1-06's resume slice.

<tasks>

<task type="auto">
  <name>Task 1: Resolve and verify the re-capture run on the final head</name>
  <precondition>`gh auth status` exits 0 in this shell (GH_TOKEN reaches non-interactive shells via BASH_ENV in this devcontainer), `git status --porcelain` is clean of source changes, and the orchestrator has already pushed the branch head containing Plan 22.1-06's commits — this plan does not push.</precondition>
  <files>.planning/phases/22.1-engine-readiness-defect-and-msrv-follow-up/22.1-CI-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/22.1-engine-readiness-defect-and-msrv-follow-up/22.1-CI-EVIDENCE.md` in full — the first capture's section layout (Selected run / Why the previously-cited evidence did not meet the bar / Run selection (ordering) / Per-job conclusion / The four required Postgres log facts / What this evidence proves, and what it does not / No ruleset or workflow edit). The new section mirrors this layout; do not restructure the existing one.
    - `.planning/milestones/v0.9.0-phases/18-rust-sast-evaluate-and-adopt-codeql/18-CODEQL-EVIDENCE.md` — the evidence-file precedent the whole file follows: run id, job id, commit SHA, timestamps, log facts quoted verbatim rather than summarized.
    - `.github/workflows/ci.yml` — the `postgres-integration` job (~lines 811-886): the container-health step with its anchored `grep -qx healthy` loop, the `pg_isready` step, the contract-suite step, the "Fail if the suite took the SKIP path" step, and the "Fail if the suite selected fewer tests than it declares" step whose `EXPECTED` is `grep -cE '^\s*#\[tokio::test\]' crates/paladin-storage/src/waypoint/postgres.rs`. Also line 26, the `concurrency` group that makes a superseded run conclude `cancelled`.
    - `.planning/phases/22.1-engine-readiness-defect-and-msrv-follow-up/22.1-06-SUMMARY.md` — the fix commit SHAs this run's head must contain, and the new declared `#[tokio::test]` count for `waypoint::postgres`.
    - `.planning/decisions/0044-branch-protection-posture.md` — confirms `postgres-integration` and `msrv` are already in the required-check set, so no ruleset edit is in scope.
  </read_first>
  <action>
1. Confirm the head under test. Run `git rev-parse HEAD` and `git log --oneline` and verify the head
   contains Plan 22.1-06's `test(22.1-06)` and `fix(22.1-06)` commits. If the working tree still
   holds uncommitted source changes, or the head predates 22.1-06, stop and report — a capture on a
   head that does not carry the fix is invalid regardless of colour (D-14, restated by D-25).
2. Wait for the `ci.yml` run on that head to finish, then resolve it at the RUN level, not the job
   level: `gh run list --workflow ci.yml --branch <branch> --json databaseId,headSha,createdAt,conclusion`
   and select the run whose `headSha` equals `git rev-parse HEAD` with the latest `createdAt`. If two
   runs tie on both SHA and timestamp, record both ids rather than choosing one silently. If no run
   exists yet for the head, wait and re-poll; do not fall back to a run on an earlier SHA.
3. Read the run-level verdict: `gh run view <run-id> --json conclusion,status,headSha,jobs`. The
   overall `conclusion` MUST be `success`. If it is `cancelled` (a later push superseded it) or
   `failure`, do not record it — report the failing job and stop; a green `postgres-integration` job
   inside a non-`success` run is explicitly not evidence (D-12, carried into D-25).
4. Confirm at the job level that `Postgres Waypoint Contract Suite (live server)`, the `MSRV` job at
   the 1.88 floor, `Security Audit` and `License & Dependency Policy` are all `success`, and record
   each job id. Record the conclusion of every other job in the run too, so a `skipped` job is named
   as skipped rather than silently folded into "all green".
5. Extract the four log facts from the Postgres job with `gh run view --job <job-id> --log`, quoting
   each verbatim WITH its timestamp — never paraphrased: (a) the container-healthy line from the
   "Start postgres-test and wait for healthy" step, together with that step's recorded `conclusion`;
   (b) the `pg_isready` line reporting that the server accepts connections; (c) the
   `test result: ok. N passed; 0 failed` line together with the "Declared tests: E, passed: A" line
   the job's own declared-count step prints, so the equality is auditable rather than asserted, and
   independently re-run `grep -cE '^\s*#\[tokio::test\]' crates/paladin-storage/src/waypoint/postgres.rs`
   in this worktree at the same head and quote its output; (d) the skip-detection step's line
   confirming every `waypoint::postgres` test exercised the live server. Its absence means the
   env-gated suite took its SKIP path and the capture is invalid.
6. State explicitly, in the section you write in Task 2, that `N` moved from the first capture's 26
   to the new declared count because Plan 22.1-06 added two contract clauses to the shared suite —
   and that the increase is itself the proof those two clauses ran against the live Postgres server,
   which is the only place they can run.
Do not edit any ruleset, branch-protection configuration or workflow file — ADR-0044 already places
both jobs in the required-check set, and a workflow edit during evidence capture would invalidate
the capture's own premise.
  </action>
  <verify>
    <automated>RUN=$(gh run list --workflow ci.yml --branch "$(git rev-parse --abbrev-ref HEAD)" --json databaseId,headSha,createdAt,conclusion --jq "[.[] | select(.headSha==\"$(git rev-parse HEAD)\")] | sort_by(.createdAt) | last | .databaseId") && test -n "$RUN" && gh run view "$RUN" --json conclusion --jq .conclusion | grep -qx success && gh run view "$RUN" --json jobs --jq '[.jobs[] | select(.name | test("Postgres Waypoint Contract Suite|MSRV|Security Audit|License & Dependency Policy")) | .conclusion] | unique | .[]' | grep -qx success</automated>
  </verify>
  <acceptance_criteria>
    - A run id is resolved whose `headSha` equals `git rev-parse HEAD`, and `gh run view <run-id> --json conclusion --jq .conclusion` prints exactly `success`.
    - `git log --oneline <recorded-sha> | grep -c 'fix(22.1-06)'` returns at least 1, and `git merge-base --is-ancestor <recorded-sha> HEAD` exits 0.
    - `gh run view <run-id> --json jobs --jq '[.jobs[] | select(.name | test("Postgres Waypoint Contract Suite")) | .conclusion] | .[]'` prints `success`.
    - `gh run view <run-id> --json jobs --jq '[.jobs[] | select(.name | test("MSRV|Security Audit|License & Dependency Policy")) | .conclusion] | unique | .[]'` prints only `success`.
    - The Postgres job log yields all four facts: a container-health line from the "wait for healthy" step, a line matching `accepting connections`, a line matching `test result: ok\. [0-9]+ passed; 0 failed`, and a line matching `exercised the live server`.
    - The `Declared tests: E, passed: A` line from the job's own declared-count step has `E == A`, and `E` equals the value `grep -cE '^\s*#\[tokio::test\]' crates/paladin-storage/src/waypoint/postgres.rs` returns in this worktree at the recorded SHA (expected 28 after Plan 22.1-06; a mismatch means the run tested a different tree).
    - `git status --porcelain` lists no change under `.github/` — no ruleset or workflow edit was made during capture.
  </acceptance_criteria>
  <done>Exactly one ci.yml run is identified on the head carrying Plan 22.1-06's fix, its overall conclusion is `success`, the four named jobs are `success`, and the four Postgres log facts plus the declared-vs-passed equality are extracted verbatim.</done>
</task>

<task type="auto">
  <name>Task 2: Append the dated re-capture section to 22.1-CI-EVIDENCE.md</name>
  <files>.planning/phases/22.1-engine-readiness-defect-and-msrv-follow-up/22.1-CI-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/22.1-engine-readiness-defect-and-msrv-follow-up/22.1-CI-EVIDENCE.md` — again, immediately before editing, so the append lands after the final `## No ruleset or workflow edit` section and touches nothing above it.
    - `.planning/phases/22.1-engine-readiness-defect-and-msrv-follow-up/22.1-CONTEXT.md` — D-25 (the re-capture bar and the "leave the earlier run in place" instruction) and D-20 (the program-gate list this section confirms).
    - `.planning/phases/22-battlefield-state-superstep-engine/22-UAT.md` — the `G-22-1` entry as Plan 22.1-05 left it, to confirm its pointer still resolves to this file (it names the file, not a section, so no edit is needed here).
  </read_first>
  <action>
Append a single new top-level section, `## Re-capture after BUG-04 (<ISO date>)`, to the END of
`22.1-CI-EVIDENCE.md`. Change no byte above it: the first capture's sections stay verbatim as
G-22-1's original closing evidence (D-25).

The section opens with two or three sentences stating WHY a second capture exists — BUG-04 was
promoted into Phase 22.1 at the 22.1-05 checkpoint, which moved the phase's final commit past the
head the first capture recorded (`7c77b0e2`, run `33706948313`), and D-14's rule that the evidence
must sit on the head the phase ships applies to the new head exactly as it applied to the old one.
State plainly that the first capture is not superseded as G-22-1's closing evidence; it is
supplemented, because G-22-1's truth was proven on the head that closed it.

Then mirror the first capture's layout, in this order:
1. **Selected run** — run id, URL, workflow, trigger ref, head SHA (with the `git merge-base
   --is-ancestor` and `git log ... | grep 'fix(22.1-06)'` confirmations quoted as commands and
   results), overall conclusion, created and updated timestamps.
2. **Run selection (ordering)** — the `gh run list` table of every run on this head SHA with its
   `createdAt` and `conclusion`, and one sentence naming which was selected and why. If two tie on
   SHA and `createdAt`, record both ids.
3. **Per-job conclusion** — a table of at least the four named jobs with their conclusions and job
   ids, plus a parenthetical naming any `skipped` job explicitly rather than folding it into a
   summary count.
4. **The four required Postgres log facts** — each in its own fenced block, quoted verbatim with its
   timestamp, in the same (a)-(d) order the first capture used, with the `Declared tests: E, passed:
   A` line quoted alongside fact (c) and the independently re-run `grep -cE` output beneath it. Add
   one sentence noting the declared count rose from the first capture's 26 because Plan 22.1-06
   added two shared contract clauses, and that this increase is the proof those clauses executed
   against the live server.
5. **`### D-20 program-gate confirmation on the phase's final commit`** — a short table of the
   program gates D-20 names, each with its job name and conclusion from THIS run: the `msrv` job at
   the 1.88 floor, `cargo semver-checks` vs 0.9.0, the api-surface job, `Security Audit` and
   `License & Dependency Policy`. One sentence stating that D-25 designates this run as the evidence
   for D-20's "gates green on the phase's final commit" requirement.
6. **What this evidence proves, and what it does not** — in the first capture's voice. It proves the
   `waypoint::postgres` contract suite (N of N declared tests, including the two frontier clauses)
   executed against a live Postgres server inside a run whose overall conclusion was `success`, on a
   commit containing the BUG-04 fix. It does not prove anything about any other commit, or about
   behaviour this suite does not exercise.
7. **No ruleset or workflow edit** — one sentence with the `git status --porcelain` evidence.

Do not edit `22-UAT.md`: Plan 22.1-05 already pointed G-22-1's `resolution` at this file by path, and
the pointer resolves to the file rather than to a section, so appending a section keeps it correct
without a second edit to a prior phase's record.
  </action>
  <verify>
    <automated>EV=.planning/phases/22.1-engine-readiness-defect-and-msrv-follow-up/22.1-CI-EVIDENCE.md; test -s "$EV" && grep -q 'Re-capture after BUG-04' "$EV" && grep -q '33706948313' "$EV" && grep -qE 'test result: ok\. [0-9]+ passed; 0 failed' "$EV" && grep -q 'exercised the live server' "$EV" && grep -q 'accepting connections' "$EV" && grep -q 'D-20 program-gate confirmation' "$EV"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Re-capture after BUG-04' .planning/phases/22.1-engine-readiness-defect-and-msrv-follow-up/22.1-CI-EVIDENCE.md` returns 1, and that heading is the file's last top-level `##` section.
    - The original capture is intact: `grep -c '33706948313' <evidence file>` returns at least 1, `grep -c '7c77b0e2' <evidence file>` returns at least 1, and `git diff -U0 <evidence file> | grep -c '^-[^-]'` returns 0 (the edit is pure append — no line above the new section was removed or rewritten).
    - The new section names a run id different from `33706948313`, and `gh run view <new-run-id> --json conclusion --jq .conclusion` prints `success`.
    - All four log facts appear inside the new section: `grep -c 'accepting connections'`, `grep -cE 'test result: ok\. [0-9]+ passed; 0 failed'`, `grep -c 'exercised the live server'` each return at least 2 across the whole file (one occurrence from the first capture, one from the re-capture), and a container-health line from the "wait for healthy" step is present in the new section.
    - `grep -c 'Declared tests:' <evidence file>` returns at least 2, and the re-capture's value has `E == A` with `E` equal to the `#[tokio::test]` count in `crates/paladin-storage/src/waypoint/postgres.rs` at the recorded SHA.
    - `grep -c 'D-20 program-gate confirmation' <evidence file>` returns 1, and that subsection lists the msrv, semver-checks, api-surface, Security Audit and License & Dependency Policy jobs with their conclusions from the new run.
    - `git status --porcelain` lists only `.planning/` files — no source, workflow or ruleset file changed during this plan.
    - `git diff --stat .planning/phases/22-battlefield-state-superstep-engine/22-UAT.md` is empty (no second edit to a prior phase's record).
  </acceptance_criteria>
  <done>`22.1-CI-EVIDENCE.md` carries a dated re-capture section proving the same D-12 bar on the head that contains the BUG-04 fix, with the four Postgres log facts quoted verbatim, the declared-vs-passed equality at the new count, and the D-20 program-gate confirmation — with the first capture preserved byte-for-byte as G-22-1's original closing evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions -> the evidence record | CI output crosses into a durable project claim. An incorrectly-scoped query (job level instead of run level, or a run on an earlier head) produces a record that reads as assurance while proving less than it claims. |
| local shell -> GitHub API (`gh`, GH_TOKEN) | An authenticated credential is used for read-only queries. |
| prior evidence -> new evidence | The existing file is itself a record under revision; an append that rewrites history destroys the audit trail it exists to provide. |

*API-coverage detector note:* `bin/lib/api-coverage.cjs` reports `detected: true` over this plan on
exactly one signal — the `local shell -> GitHub API (`gh`, GH_TOKEN)` row above, wording carried
verbatim from Plan 22.1-05's threat model. No API coverage matrix is authored: `gh` is an existing,
read-only, already-authenticated CLI used to READ a workflow run, not an external API surface this
phase integrates, wraps or exposes. Disposition recorded here rather than left implicit.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22.1-25 | Repudiation (a phase sealed on evidence that does not sit on its final commit) | `22.1-CI-EVIDENCE.md` re-capture section | high | mitigate | The recorded head SHA must contain the `fix(22.1-06)` commit (`git log ... | grep`) and be an ancestor-or-equal of the branch head (`git merge-base --is-ancestor`), and the run's overall conclusion must be `success` before anything is written (Task 1, D-25). |
| T-22.1-26 | Spoofing (a skipped suite presenting as an executed one) | the env-gated `waypoint::postgres` suite | high | mitigate | The skip-detection line and the declared-vs-passed equality are both required facts; the `empty` edge truth makes a zero-executed run explicitly non-evidence, and the declared count rising from 26 to 28 is the independent signal that Plan 22.1-06's two new clauses actually ran against the live server. |
| T-22.1-27 | Tampering (rewriting the earlier record so the file reads as one clean result) | the existing sections of `22.1-CI-EVIDENCE.md` | medium | mitigate | The edit is append-only, asserted by `git diff -U0 ... | grep -c '^-[^-]'` returning 0 and by the first run id and head SHA still being present (Task 2 acceptance criteria, D-25). |
| T-22.1-28 | Information Disclosure | `gh` log extraction and the evidence file | medium | mitigate | Only workflow log lines about container health, connection readiness and test counts are quoted; no secret, token or environment value is copied into the file. `GH_TOKEN` is read from the environment and never echoed into a command line, log or committed file. |
| T-22.1-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no packages and touches no manifest or lockfile; it writes one planning document. `22.1-RESEARCH.md` §Package Legitimacy Audit records no `[ASSUMED]`/`[SUS]`/`[SLOP]` package for this phase. |
</threat_model>

<verification>
- The selected run's `headSha` equals `git rev-parse HEAD` and contains the `fix(22.1-06)` commit.
- `gh run view <run-id> --json conclusion --jq .conclusion` prints `success`.
- Postgres, MSRV, Security Audit and License & Dependency Policy jobs all `success` in that run.
- All four Postgres log facts present verbatim in the new section, with `Declared tests: E, passed: A`
  showing `E == A` at the post-22.1-06 count.
- The first capture's section is byte-for-byte unchanged; the edit is a pure append.
- `git status --porcelain` shows only `.planning/` changes.
</verification>

<success_criteria>
`22.1-CI-EVIDENCE.md` records a second, dated capture meeting D-12's bar — whole-run `success`, the
four named jobs `success`, the four Postgres log facts quoted verbatim, the declared-vs-passed
equality at the new test count — on the branch head that carries the BUG-04 fix, together with the
D-20 program-gate confirmation, and with the original run's section preserved intact as G-22-1's
closing evidence. No human gate is required (D-25).
</success_criteria>

<output>
Create `.planning/phases/22.1-engine-readiness-defect-and-msrv-follow-up/22.1-07-SUMMARY.md` when done
</output>
