# Phase 22.1: Engine readiness defect and MSRV follow-up - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-02
**Phase:** 22.1-engine-readiness-defect-and-msrv-follow-up
**Mode:** `--auto` — every question resolved on its recommended option without prompting; each
selection is logged with the evidence that made it the recommendation.
**Areas discussed:** Readiness fix semantics (BUG-03), MSRV position (X-11.2), G-22-1 closing-evidence bar, Fingerprint coverage (CR-01) and the resume-safety claim

---

## Readiness fix semantics (BUG-03 scope and contract)

| Option | Description | Selected |
|--------|-------------|----------|
| General cycle bootstrap | Fix the mechanism: any non-entry node inside a cycle fed from outside can never take a first turn; self-loop is the degenerate case | ✓ |
| Self-edge exemption only | Seed a node's own self-edge as resolved until its first run; leaves `entry -> a -> b -> a` starving `a` | |

`[auto] Readiness — Q: "Scope of the fix?" → Selected: "General cycle bootstrap" (recommended default)`

| Option | Description | Selected |
|--------|-------------|----------|
| Starvation release | When the Vanguard would be empty, release nodes with a fresh fired edge blocked only by `Pending` edges from live never-executed sources; no new persisted state; DAG joins untouched | ✓ (preferred; research validates) |
| Dominator back-edge classification | Static: an edge whose source the target dominates is non-gating before first execution; fails multi-entry cycles | |
| Self-edge seeding | Symptom-only | |
| Reject entry-less cycles at validate() | Truthful but forbids legitimate shapes | ✓ as guard only |

`[auto] Readiness — Q: "Preferred mechanism?" → Selected: "Starvation release, rejection as the guard" (recommended default)`

| Option | Description | Selected |
|--------|-------------|----------|
| Typed error guard at run end | Never report Completed while an eligible node holds an unconsumed fired edge | ✓ |
| Rely on the fix alone | | |

`[auto] Readiness — Q: "Truthful-outcome guard?" → Selected: "Typed error guard" (recommended default)`

| Option | Description | Selected |
|--------|-------------|----------|
| Keep fixtures, correct comments, add RED fixtures, un-ignore repro unchanged | Entry self-loops stay legal; comments must not describe fixed behaviour as current | ✓ |
| Rewrite all nine readiness-dodge fixtures | Churn without added coverage | |

`[auto] Readiness — Q: "Existing fixtures?" → Selected: "Keep + correct + add" (recommended default)`

| Option | Description | Selected |
|--------|-------------|----------|
| Register as BUG-03 / ENG-FR-06a, pre-release classification | Developer decided at 22-17 that registration happens when 22.1 is planned | ✓ |
| Leave unregistered | Contradicts the 22-17 disposition | |

`[auto] Readiness — Q: "Registration?" → Selected: "Register as BUG-03" (recommended default)`

**Notes:** The general-case claim was reached by reading `Frontier::is_ready`, `edge_resolution` and
`propagate_dead`: a back-edge source with its own `Pending` incoming edge is never marked dead, so
its edge blocks the target forever. `validate_accepts_two_node_cycle` is validate-only and makes
`a` the entry for exactly this reason.

---

## MSRV position (X-11.2 stop-and-flag resolution)

| Option | Description | Selected |
|--------|-------------|----------|
| Raise the floor | No rmcp release (0.15.0 … 3.2.0) builds STDIO transport below process-wrap ^9.0; no 9.x below 1.86 | ✓ |
| Keep 1.85 | Only by dropping `transport-child-process` — removes a shipped Arsenal capability (X-03) | |

`[auto] MSRV — Q: "Keep 1.85 or raise?" → Selected: "Raise" (recommended default)`

| Option | Description | Selected |
|--------|-------------|----------|
| 1.88 | Lowest floor clearing process-wrap (1.87) and the RUSTSEC-2026-0009 fix (`time` ≥ 0.3.47, rust-version 1.88) with no new exception; all 22-17 pin-backs have a ≤ 1.88 current release | ✓ (subject to `cargo +1.88 check` measurement) |
| 1.87 + advisory exception | Adds to SEC-01 exception sprawl for one release lower | |
| 1.86 + `process-wrap = 9.0.0` | Transitive `.0` pin plus the same exception | |

`[auto] MSRV — Q: "Target floor?" → Selected: "1.88" (recommended default)`

| Option | Description | Selected |
|--------|-------------|----------|
| Revert 44e13fbd's 20-crate pin-back | Restores `time 0.3.47`; fixes the red Security Audit and cargo-deny jobs | ✓ |
| Keep pin-backs, add exception | | |

`[auto] MSRV — Q: "Lockfile handling?" → Selected: "Revert pin-backs" (recommended default)`

| Option | Description | Selected |
|--------|-------------|----------|
| `resolver = "3"` | MSRV-aware resolution; lockfile-neutral at adoption | ✓ |
| Leave resolver 2 | Every `cargo update` can silently break the floor again | |

`[auto] MSRV — Q: "Recurrence guard?" → Selected: "resolver 3" (recommended default)`

| Option | Description | Selected |
|--------|-------------|----------|
| Workspace inheritance + full doc sweep in one commit | X-11.2 "applied in one place"; README, CI job, MIGRATION §9.3, CHANGELOG, three mdBook pages | ✓ |
| Edit 11 literals | | |

`[auto] MSRV — Q: "Where the floor lives?" → Selected: "Workspace inheritance" (recommended default)`

| Option | Description | Selected |
|--------|-------------|----------|
| Keep `rmcp = "=2.1.0"` | Moving the pin cannot restore any lower floor; rmcp 3.x declares 1.88 itself | ✓ |
| Move to rmcp 3.x | SDK upgrade, out of scope | |

`[auto] MSRV — Q: "rmcp pin?" → Selected: "Keep" (recommended default)`

**Notes:** crates.io index queried 2026-09-02 for `process-wrap`, `rmcp`, `time`, `darling`,
`home`, `idna_adapter`, `serde_with`, `tonic`, `icu_*`. The CI log for run 33688238662 shows the
MSRV job failing on `process-wrap@9.1.0 requires rustc 1.87.0` and both audit jobs failing on
`RUSTSEC-2026-0009` (`time 0.3.45`). `docs/src/api-reference/stable-api.md` claims "Current MSRV:
Rust 1.93.1" — reconciled by the doc sweep.

---

## G-22-1 closing-evidence bar

| Option | Description | Selected |
|--------|-------------|----------|
| Whole-workflow-run success on the post-MSRV branch head, four log facts re-extracted | Run 33688238662 is `cancelled` overall with MSRV/audit/deny failures | ✓ |
| Job-level success inside a cancelled run | What 22-17 recorded; superseded by the next push | |

`[auto] Evidence — Q: "Evidence bar?" → Selected: "Whole-run green" (recommended default)`

| Option | Description | Selected |
|--------|-------------|----------|
| `22.1-CI-EVIDENCE.md` + pointer in 22-UAT.md G-22-1 | Phase 18 evidence-file precedent | ✓ |
| SUMMARY only | | |

`[auto] Evidence — Q: "Where recorded?" → Selected: "Evidence file + UAT pointer" (recommended default)`

| Option | Description | Selected |
|--------|-------------|----------|
| Last plan | Depends on every other change being on the branch head | ✓ |
| First plan | Would certify a run the later changes invalidate | |

`[auto] Evidence — Q: "Ordering?" → Selected: "Last plan" (recommended default)`

**Notes:** ADR-0044 already places `msrv` and `postgres-integration` in the required-check set;
no ruleset change.

---

## Fingerprint coverage (CR-01) and the resume-safety claim

| Option | Description | Selected |
|--------|-------------|----------|
| Fix the hash pre-release and reword the claim | Phase 22 D-04's one-way rating applies after release; now is the only cheap window | ✓ |
| Reword only | Leaves resume unable to detect defer/entry changes | |

`[auto] Fingerprint — Q: "Fix or narrow?" → Selected: "Fix + reword" (recommended default)`

| Option | Description | Selected |
|--------|-------------|----------|
| Every scheduling/merge-semantics property (entry set, defer, dynamic targets, dispatch rules, output_field, serde-canonical conditions) | The entry-set omission was found during this discussion | ✓ |
| defer_flags + dynamic_targets only | Review's minimal fix; leaves the entry set unhashed | |

`[auto] Fingerprint — Q: "What the hash covers?" → Selected: "Scheduling/merge structure" (recommended default)`

| Option | Description | Selected |
|--------|-------------|----------|
| Golden hex test + difference tests | Makes future canonicalization drift fail CI | ✓ |
| Difference tests only | | |

`[auto] Fingerprint — Q: "Golden test?" → Selected: "Golden + difference tests" (recommended default)`

| Option | Description | Selected |
|--------|-------------|----------|
| Reword to the measured guarantee, cite the deferred resume finding | | ✓ |
| Keep "structurally impossible" | False on two counts | |

`[auto] Fingerprint — Q: "Doc claim?" → Selected: "Reword" (recommended default)`

---

## Claude's Discretion

- D-03 algorithm details and release ordering relative to the `defer` release.
- `EngineError` variant naming for the guard and the run-end check.
- BUG-03 / ENG-FR-06a wording in the three `.project/` files.
- Lockfile details beyond "no advisory, no > 1.88 requirement".
- Plan/wave decomposition (suggested order in CONTEXT.md).

## Deferred Ideas

- BUG-04 candidate: resume rebuilds the Frontier from scratch, losing pre-crash edge resolutions
  (code-reading finding; repro shape and recommended disposition in CONTEXT.md).
- 22-REVIEW.md WR-01 / WR-02; 22-deferred-items.md item 1 (qdrant `--all-features` rustdoc).
- rmcp 3.x upgrade (reachable at 1.88, not 22.1's).
- Reviewed todo, not folded: local `make coverage` reproduction check (score 0.2).
