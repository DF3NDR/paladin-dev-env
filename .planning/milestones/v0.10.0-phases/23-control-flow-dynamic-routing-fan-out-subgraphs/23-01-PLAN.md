---
phase: 23-control-flow-dynamic-routing-fan-out-subgraphs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/paladin-battalion/src/edge_evaluator.rs
  - crates/paladin-battalion/src/lib.rs
  - crates/paladin-battalion/src/campaign_service.rs
  - crates/paladin-battalion/src/engine/graph.rs
  - crates/paladin-battalion/src/engine/superstep.rs
  - crates/paladin-battalion/src/engine/mod.rs
  - MIGRATION.md
  - .project/v0.10.0/08-traceability-matrix.md
autonomous: true
requirements: [CF-01]

must_haves:
  truths:
    # --- Phase-goal truths (goal-backward, D-01..D-06) ---
    - "A graph or Campaign carrying an EdgeCondition::Custom(name) with no registered evaluator fails validation BEFORE any node executes, on BOTH the legacy CampaignExecutionService path and the WarEngine path, with a recording port proving zero Paladin executions occurred (CF-FR-02, D-03)."
    - "A registered evaluator returning Ok(true) routes the edge and Ok(false) does not, on both paths (CF-FR-04, D-01)."
    - "A registered evaluator returning Err at runtime fails the run with a typed error naming the edge and the evaluator name, never defaulting either branch (CF-FR-03, D-02/D-04)."
    - "No configuration, environment variable, cargo feature, or builder option exists that restores the pre-fix always-true behavior for an unregistered Custom condition (CF-FR-02, D-05)."
    - "MIGRATION.md §9.1 carries a worked before/after example for M-B-01 showing the exact v0.9 line that silently always-routed, the exact v0.10 registration line, and the validation error text a user sees if they skip it (D-06)."
    # --- Edge coverage lifted from the spec-less probe fallback report (CF-01) ---
    - "adjacency (CF-01): two edges carrying the identical unregistered EdgeCondition::Custom(name) produce exactly ONE occurrence of that name in the validation error, deduped and sorted — the offender list merges equal names rather than repeating them."
    - "empty (CF-01): a graph with zero EdgeCondition::Custom edges validates successfully against an empty EdgeEvaluatorRegistry (no false rejection); a graph with exactly one unregistered Custom(name) names exactly that one; and an engine-path evaluator whose source Paladin node has not yet written its output_field receives the empty string, never a null or the whole Battlefield JSON."
    - "encoding (CF-01): evaluator-name lookup is exact byte equality on the String key — case-sensitive, with no Unicode normalization and no trimming — so Custom(\"isUrgent\") and Custom(\"isurgent\") are two distinct names, and the offender list is ordered by Rust str byte order rather than any locale collation."
    - "ordering (CF-01): the unregistered-condition list is sorted and deduplicated, so two runs over the same graph whose edges were declared in different orders produce byte-identical error text on both paths."
  artifacts:
    - "crates/paladin-battalion/src/edge_evaluator.rs — EdgeConditionEvaluator trait, EdgeContext, EdgeEvaluatorRegistry, EdgeEvaluatorError."
    - "crates/paladin-battalion/src/campaign_service.rs — with_evaluator builder method, private evaluators field, async evaluate_edge_condition consulting the registry, fail-closed pre-check after campaign.validate(), placeholder arm removed."
    - "crates/paladin-battalion/src/engine/mod.rs — WarEngine::with_edge_evaluator, EngineError::UnregisteredEdgeCondition, EngineError::EdgeEvaluatorFailed."
    - "crates/paladin-battalion/src/engine/graph.rs — WarGraph::validate gains the evaluator-registry parameter and the fail-closed clause."
    - "crates/paladin-battalion/src/engine/superstep.rs — evaluate_edge_condition's Custom arm consults the registry and awaits the evaluator."
    - "MIGRATION.md — §9.1 M-B-01 worked example; §9.2 EdgeCondition and CampaignExecutionService rows resolved."
    - ".project/v0.10.0/08-traceability-matrix.md — BUG-01 row carrying the RED and GREEN commit SHAs."
  key_links:
    - "WarGraph::validate -> EdgeEvaluatorRegistry: the fail-closed check must run inside the SAME pre-execution validate call that already holds the CustomDispatchResolver clause, never a parallel validation entry point."
    - "CampaignExecutionService::execute -> campaign.validate()? -> the new fail-closed pre-check: the check runs immediately after the existing structural validation and before the first paladin_port.execute call."
    - "superstep::evaluate_edge_condition -> EdgeEvaluatorRegistry::get: by the time the runtime arm runs, validate() has already guaranteed the name is registered, so a miss there is a typed internal error, never a silent default."
    - "EdgeContext.thread/superstep -> the CF-05 LlmDecisionEvaluator memo key (D-24): EdgeContext must carry the thread id and superstep index so a later plan can memoize one LLM call per decision per superstep without touching superstep.rs again."
  prohibitions:
    - statement: "The fix MUST NOT ship any configuration flag, environment variable, cargo feature, builder option, or documented workaround that restores the pre-fix always-true behavior for an unregistered EdgeCondition::Custom — the program authorised exactly one behavioral break, and an escape hatch converts a loud, sanctioned break back into the silent routing corruption it replaced."
      status: unverified
      verification: flagged-unverified
---

<objective>
Fix BUG-01 fail-closed and test-first on both the legacy `CampaignExecutionService` and the
`WarEngine`: a registered-`EdgeConditionEvaluator` mechanism, validation that fails naming every
unregistered `EdgeCondition::Custom(name)` before any node executes, and runtime evaluator errors
that fail the run — then land M-B-01's owed worked example in `MIGRATION.md`.

Purpose: `EdgeCondition::Custom` currently warns and evaluates true on both paths, silently
corrupting conditional routing. This is the program's single sanctioned behavioral break
(overview §7, `MIGRATION.md` M-B-01), and the mechanism it introduces is what CF-05's
`LlmDecision` evaluator plugs into later in this phase.

Output: a new `edge_evaluator` module in `paladin-battalion`, both placeholder sites deleted, the
four CF-FR-04 cases green on both paths with failing-then-passing order visible in git history,
and `MIGRATION.md` §9.1/§9.2 resolved for CF-01.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-control-flow-dynamic-routing-fan-out-subgraphs/23-CONTEXT.md
@.planning/phases/23-control-flow-dynamic-routing-fan-out-subgraphs/23-RESEARCH.md
@.planning/phases/23-control-flow-dynamic-routing-fan-out-subgraphs/23-PATTERNS.md
@.project/v0.10.0/02-control-flow-routing-fanout-subgraphs.md
@crates/paladin-battalion/src/engine/dispatch_registry.rs
@crates/paladin-battalion/src/campaign_service.rs
</context>

## Artifacts this phase produces

Symbols and files created by Phase 23 (this plan's share). The plan-review source-grounding pass
must treat these as new, not as drift against existing code:

| Kind | Symbol / path | Plan |
|------|---------------|------|
| module | `crates/paladin-battalion/src/edge_evaluator.rs` | 23-01 |
| trait | `EdgeConditionEvaluator` | 23-01 |
| struct | `EdgeContext<'a>` | 23-01 |
| struct | `EdgeEvaluatorRegistry` | 23-01 |
| enum | `EdgeEvaluatorError` | 23-01 |
| method | `CampaignExecutionService::with_evaluator` | 23-01 |
| method | `WarEngine::with_edge_evaluator` | 23-01 |
| enum variant | `EngineError::UnregisteredEdgeCondition { names }` | 23-01 |
| enum variant | `EngineError::EdgeEvaluatorFailed { from, to, evaluator, source }` | 23-01 |
| doc id | `M-B-01` worked example (`MIGRATION.md` §9.1) | 23-01 |

## Flagged assumptions (spec-less probe fallback)

None for CF-01 — all four probe rows for this requirement (`adjacency`, `empty`, `encoding`,
`ordering`) were classified and are authored as `must_haves.truths` above. No row was dismissed.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Registered-evaluator edge conditions, end-to-end on both paths (RED then GREEN)</name>
  <reversibility rating="costly">Per D-01, `EdgeConditionEvaluator` becomes a published `paladin-battalion` trait; moving it between crates later touches facade re-exports and every downstream import. The placement decision itself is already made in CONTEXT.md, so no gate is inserted.</reversibility>
  <files>crates/paladin-battalion/src/edge_evaluator.rs, crates/paladin-battalion/src/lib.rs, crates/paladin-battalion/src/campaign_service.rs, crates/paladin-battalion/src/engine/graph.rs, crates/paladin-battalion/src/engine/superstep.rs, crates/paladin-battalion/src/engine/mod.rs</files>
  <read_first>
    - `crates/paladin-battalion/src/engine/dispatch_registry.rs` (107 lines, read in full) — `DispatchRegistry`'s `new`/`register`/`resolver` shape and its in-module `#[cfg(test)]` test style. This is the house pattern D-01/D-03 say to mirror.
    - `crates/paladin-battalion/src/campaign_service.rs` — `CampaignExecutionService`'s struct fields, `new(paladin_port)` (unchanged), `with_herald` (~line 100, the additive-builder precedent), `execute` (~line 170) and its `campaign.validate()?` line (~175), `execute_internal`'s per-edge loop (~line 308) and `evaluate_edge_condition` (~lines 378-399, the legacy defect site).
    - `crates/paladin-battalion/src/engine/graph.rs` — `pub fn validate(&self, custom_dispatch: &CustomDispatchResolver)` at line 290 and its five inline clauses; the `DispatchRule::Custom` registered check at lines 317-325 (the clause shape to mirror); `validate_eligible_set` (~334) and `validate_schedulable` for the "collect every offender, report once" discipline; the 24 in-file `validate(&...)` test call sites that must be updated.
    - `crates/paladin-battalion/src/engine/superstep.rs` — `fn evaluate_edge_condition(condition, battlefield)` (~lines 1189-1213, the engine defect site), its call site inside `Frontier::record_execution` (~lines 824-847), and `NodeDispatch::Paladin`'s `output_field` write (~line 111) which defines what "the source node's output" means on the engine path.
    - `crates/paladin-battalion/src/engine/mod.rs` — `#[non_exhaustive] pub enum EngineError` (~line 137), `with_dispatch_rule` (line 438, the exact builder shape), and the two `graph.validate(registry)?` call sites at lines 488 and 608.
    - `crates/paladin-core/src/platform/container/battalion/mod.rs` — `BattalionError` (not `#[non_exhaustive]`) and its `InvalidGraph(String)` variant (~line 755), the variant D-04 requires reusing.
    - `crates/paladin-core/src/platform/container/battalion/campaign.rs:34` — `EdgeCondition` (not `#[non_exhaustive]`) — read-only; this enum is not modified by this phase.
    - `.project/v0.10.0/02-control-flow-routing-fanout-subgraphs.md` §2.1 — CF-FR-01…04 verbatim, and §4 item 1 (RED tests first).
  </read_first>
  <behavior>
    Eight regression tests, four per path, all committed FAILING in one commit before any
    production change lands (D-05; traceability protocol step 4 and SHIP-03 check history order):

    Legacy path (`campaign_service.rs`, in-module `#[cfg(test)]`):
    - `unregistered_custom_condition_is_rejected_before_any_paladin_executes` — a Campaign with
      `EdgeCondition::Custom("is_urgent")` and no registered evaluator returns
      `Err(BattalionError::InvalidGraph(_))` whose message contains `is_urgent`, and the recording
      Paladin port reports zero executions.
    - `registered_true_evaluator_routes_the_custom_edge` — the same Campaign with an evaluator
      registered under `is_urgent` returning `Ok(true)` executes the downstream Paladin.
    - `registered_false_evaluator_does_not_route_the_custom_edge` — the same graph with
      `Ok(false)` leaves the downstream Paladin unexecuted.
    - `evaluator_error_fails_the_legacy_run_naming_the_edge` — an evaluator returning `Err` makes
      `execute` return `Err`, with the message naming the source node, target node and the
      evaluator name; assert it is NOT the success path and NOT the false branch.

    Engine path (`engine/graph.rs` for validation, `engine/superstep.rs` for runtime):
    - `unregistered_custom_edge_condition_fails_graph_validation` — `WarGraph::validate` returns
      `Err(EngineError::UnregisteredEdgeCondition { names })` with `names == vec!["is_urgent"]`.
    - `every_unregistered_custom_name_is_listed_sorted_and_deduped` — a graph with three edges
      carrying `Custom("zeta")`, `Custom("alpha")`, `Custom("alpha")` yields
      `names == vec!["alpha", "zeta"]` (deduped, byte-sorted) — this is the `adjacency` and
      `ordering` edge truth.
    - `registered_engine_evaluator_true_and_false_route_correctly` — one run per verdict through
      `WarEngine::start`, asserting the downstream node ran / did not run.
    - `engine_evaluator_error_fails_the_run_naming_edge_and_evaluator` — the run returns
      `Err(EngineError::EdgeEvaluatorFailed { from, to, evaluator, .. })` with all three fields
      populated; assert the run does not complete.

    Plus two registry unit tests in `edge_evaluator.rs`: name lookup is exact byte equality
    (`"isUrgent"` and `"isurgent"` are distinct keys — the `encoding` edge truth), and
    `registered_names()` returns a byte-sorted list.

    GREEN: after the RED commit, the mechanism makes all ten pass with every pre-existing
    `paladin-battalion` test still green and unmodified.
  </behavior>
  <action>
Work in exactly two commits so failing-then-passing order is visible in git history.

COMMIT 1 (RED) — tests only, no production change. Write the ten tests described in `<behavior>`
against the API the GREEN commit will introduce, so the crate does not compile until GREEN lands;
that is the intended red state for a mechanism that does not exist yet. Commit as
`test(23-01): reproduce BUG-01 on both custom-edge-condition paths (red)`.

COMMIT 2 (GREEN) — the mechanism, per D-01…D-05:

1. New module `crates/paladin-battalion/src/edge_evaluator.rs`, registered in `lib.rs` beside the
   existing service modules and re-exported at crate root. It declares:
   - `#[async_trait] pub trait EdgeConditionEvaluator: Send + Sync` with the single method
     `async fn evaluate(&self, output: &str, ctx: &EdgeContext<'_>) -> Result<bool, EdgeEvaluatorError>`
     (D-02 — async is a deliberate deviation from PRD 02's sync sketch because CF-05's
     `LlmDecision` must await an `LlmPort` call; the trait is new in v0.10 so X-10 does not apply).
   - `pub struct EdgeContext<'a>` with public fields `source: &'a NodeId`, `target: &'a NodeId`,
     `battlefield: Option<&'a Battlefield>` (`Some` on the engine path, `None` on the legacy
     path), plus `thread: Option<&'a ThreadId>` and `superstep: Option<u64>`. The last two are
     required so CF-05's `LlmDecisionEvaluator` can key its one-call-per-decision memo on
     `(thread, superstep, source)` without a later change to `superstep.rs`.
   - `pub enum EdgeEvaluatorError` via `thiserror`, `#[non_exhaustive]`, carrying at minimum a
     structured `Evaluation { evaluator: String, reason: String }` variant (X-06 — no bare String
     variant).
   - `#[derive(Default)] pub struct EdgeEvaluatorRegistry` wrapping
     `HashMap<String, Arc<dyn EdgeConditionEvaluator>>` with `new()`, `register(name, evaluator)`,
     `get(&str) -> Option<&Arc<dyn EdgeConditionEvaluator>>`, `contains(&str) -> bool` and
     `registered_names() -> Vec<&str>` (sorted). There is NO reserved-name guard (D-01: unlike
     `DispatchRule::Custom`, `EdgeCondition::Custom` names collide with no built-in variant name).
     Duplicate registration under the same name REPLACES the prior evaluator and returns `Self`
     rather than erroring — mirror `ReservedDispatchName`'s stance by documenting the choice in
     rustdoc (CONTEXT.md discretion). Keys are compared by exact `String` equality: no trimming,
     no case folding, no Unicode normalization — state this in the `register` rustdoc.

2. Legacy path (`campaign_service.rs`), per D-03/D-04:
   - Add a private `evaluators: EdgeEvaluatorRegistry` field initialised empty in `new`, and the
     additive builder `pub fn with_evaluator(mut self, name: impl Into<String>, evaluator: Arc<dyn EdgeConditionEvaluator>) -> Self`
     shaped exactly like `with_herald`. `CampaignExecutionService::new(paladin_port)` keeps its
     signature (CF-FR-02 compatibility constraint).
   - In `execute`, immediately after the existing `campaign.validate()?` line and before any node
     executes, run the fail-closed pre-check: walk every edge's condition, collect the `name` of
     each `EdgeCondition::Custom(name)` not present in `self.evaluators`, sort and dedup the
     collected names, and if non-empty return
     `Err(BattalionError::InvalidGraph(...))` whose message lists every name. Reuse the EXISTING
     `InvalidGraph(String)` variant — `BattalionError` is a pre-existing public enum without
     `#[non_exhaustive]`, so adding a variant would be an unsanctioned X-10 break, and PRD
     CF-FR-02 plus the published M-B-01 row both name this exact variant (D-04). X-06's
     "no new call sites on String variants" is consciously overridden here by the explicit FR —
     record that one-line justification in a code comment at the call site.
   - Make `evaluate_edge_condition` an `async fn`, add the `.await` at its call site (~line 308,
     already inside `async fn execute_internal`), and replace the `EdgeCondition::Custom(_)` arm's
     `warn!` call and `Ok(true)` return with: look the name up in `self.evaluators`, build an
     `EdgeContext` with `battlefield: None`, `thread: None`, `superstep: None` and the source and
     target `NodeId`s, `await` `evaluate`, and map an `Err` into
     `BattalionError::ExecutionError`-family failure whose message names the source node, the
     target node and the evaluator name. A registry miss at this point is unreachable because
     `execute`'s pre-check already ran; return the same fail-closed error rather than any default
     branch (CF-FR-03). Delete the placeholder comment lines with the arm.

3. Engine path, per D-03/D-04:
   - `WarGraph::validate` gains a second parameter `evaluators: &EdgeEvaluatorRegistry`, placed
     after `custom_dispatch` (pre-release signature, free to change). Add ONE new clause after the
     existing `DispatchRule::Custom` clause and before `validate_eligible_set()`: collect every
     `EdgeCondition::Custom(name)` on `self.edges` whose name is absent from `evaluators`, sort
     with `sort_unstable`, `dedup`, and on a non-empty result return
     `EngineError::UnregisteredEdgeCondition { names: Vec<String> }`. Collect every offender — do
     not fail fast on the first, matching `validate_eligible_set`'s discipline. Update all 24
     in-file test call sites and the two `graph.validate(registry)?` call sites in `engine/mod.rs`
     (lines 488 and 608) plus the three in `superstep.rs`'s test module.
   - `engine/mod.rs`: add `pub fn with_edge_evaluator(mut self, name: impl Into<String>, evaluator: Arc<dyn EdgeConditionEvaluator>) -> Self`
     shaped like `with_dispatch_rule` (but infallible — no reserved-name failure mode), storing
     into a new private `edge_evaluators: EdgeEvaluatorRegistry` field on `WarEngine`; pass a
     borrow of it into both `graph.validate` call sites and forward it into `superstep::run`.
     Add the two new `EngineError` variants — `UnregisteredEdgeCondition { names: Vec<String> }`
     and `EdgeEvaluatorFailed { from: NodeId, to: NodeId, evaluator: String, source: EdgeEvaluatorError }`
     — with `thiserror` messages naming their fields. `EngineError` is already `#[non_exhaustive]`,
     so both additions carry zero X-10 register burden.
   - `superstep.rs`: `evaluate_edge_condition` becomes `async`, gains the registry, the source and
     target `NodeId`s, the `ThreadId` and the superstep index as parameters, and its
     `EdgeCondition::Custom(_)` arm is replaced by a registry lookup plus an awaited `evaluate`
     call. Delete the placeholder comment lines with the arm. The `output` argument follows D-02:
     when the source node is a `NodeSpec::Paladin`, pass the string value of its `output_field`
     read from the Battlefield, using the empty string when that field has no value yet; for every
     other node kind pass the canonical Battlefield JSON the existing `Contains`/`Regex` arms
     already render. Document that rule in the function's rustdoc. Convert an evaluator `Err` into
     `EngineError::EdgeEvaluatorFailed`, propagated so the run fails — never a default branch.
   - Its caller inside `Frontier::record_execution` must now await; thread the async through the
     minimum surface needed and keep `Frontier`'s other methods synchronous.

4. Confirm both defect sites are gone: after the change no `warn!` or unconditional-true arm
   remains for `EdgeCondition::Custom` in either file, and no new configuration, env var, cargo
   feature or builder option exists that restores the previous behavior (the prohibition above).

Commit as `fix(23-01): fail closed on unregistered custom edge conditions (green)`.
  </action>
  <verify>
    <automated>cargo test -p paladin-battalion --lib</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p paladin-battalion --lib edge_evaluator` exits 0 with `0 failed`.
    - `cargo test -p paladin-battalion --lib campaign_service::tests::unregistered_custom_condition_is_rejected_before_any_paladin_executes` exits 0 and reports `1 passed`.
    - `cargo test -p paladin-battalion --lib` exits 0 with `0 failed` and `0 ignored`.
    - `grep -rn "defaulting to true" crates/` returns no matches (exit status 1).
    - `grep -v '^\s*//' crates/paladin-battalion/src/engine/superstep.rs | grep -A3 'EdgeCondition::Custom' | grep -c 'Ok(true)'` returns 0.
    - `grep -c 'pub fn with_evaluator' crates/paladin-battalion/src/campaign_service.rs` returns 1, and `grep -c 'pub fn new(paladin_port' crates/paladin-battalion/src/campaign_service.rs` returns 1 (constructor signature unchanged).
    - `grep -c 'pub fn with_edge_evaluator' crates/paladin-battalion/src/engine/mod.rs` returns 1.
    - `grep -c 'UnregisteredEdgeCondition\|EdgeEvaluatorFailed' crates/paladin-battalion/src/engine/mod.rs` returns at least 2.
    - `grep -rn 'InvalidGraph' crates/paladin-core/src/platform/container/battalion/mod.rs | wc -l` is unchanged from HEAD, and `git diff HEAD~2 -- crates/paladin-core/` is empty (no `paladin-core` change in this plan).
    - `git log --oneline -2` shows a `test(23-01)` commit strictly preceding the `fix(23-01)` commit, and `git show <test-commit> --stat` lists only test-bearing source files.
    - `cargo fmt --check` exits 0 and `cargo clippy -p paladin-battalion --all-targets -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>Unregistered custom conditions fail validation before execution on both paths, registered evaluators route true/false correctly, evaluator errors fail the run with a typed error naming edge and evaluator, both placeholder sites are gone, and the red-then-green order is in git history.</done>
</task>

<task type="auto">
  <name>Task 2: Land M-B-01's worked example and resolve CF-01's §9.2 register rows</name>
  <files>MIGRATION.md, .project/v0.10.0/08-traceability-matrix.md</files>
  <read_first>
    - `MIGRATION.md` §9.1 — the M-B-01 row (line 15) and the "Worked examples" bullet list (lines 20-25) where M-B-01 is recorded `TBD — owner CF-01, Phase 23`; M-B-04's bullet is the "no example owed" contrast.
    - `MIGRATION.md` §9.2 — the `EdgeCondition` row (line 40) and the `CampaignExecutionService` row (line 42), both `TBD — owner CF-01, Phase 23`; line 45's Plan 22-01 "deliberate zero" note as the tone to match; the column contract at line 29.
    - `MIGRATION.md` §9.8 — the upgrade-checklist paragraph naming "custom evaluator registration lands with CF-01, Phase 23".
    - `.project/v0.10.0/08-traceability-matrix.md` — the BUG-01 row and protocol step 4 (test-first, grep-absence), plus the BUG-02/03/04 rows as the row shape.
    - `.project/v0.10.0/00-program-overview.md` §7 BUG-01 and §9.1's pre-populated M-B-01 requirement ("Include a worked before/after example in this section").
    - `crates/paladin-battalion/src/campaign_service.rs` and `crates/paladin-battalion/src/edge_evaluator.rs` as they stand after Task 1 — the example must quote the API that actually shipped, and the error text must be the string the code actually produces.
  </read_first>
  <action>
Resolve every CF-01-owned `TBD` in `MIGRATION.md` and record the fix in the traceability matrix.

1. `MIGRATION.md` §9.1, "Worked examples" — replace the M-B-01 `TBD` bullet with a worked
   before/after example. Per CONTEXT.md's Specific Ideas it must read like a migration, not a
   spec, and must contain exactly three things: (a) the v0.9 construction line that silently
   always-routed — a `Campaign` edge built with `EdgeCondition::Custom("is_urgent")` and the
   one-sentence statement that this edge fired on every run regardless of the Paladin's output;
   (b) the v0.10 line that fixes it — the same construction plus
   `.with_evaluator("is_urgent", Arc::new(...))` on the `CampaignExecutionService`, with a
   minimal `EdgeConditionEvaluator` impl; and (c) the alternative route for users who do not want
   an evaluator — replacing the condition with `Contains`/`Regex`/`Always`. Close with the exact
   validation error text a user sees if they skip both, copied verbatim from the message Task 1's
   code produces (run the failing case and transcribe, do not paraphrase).

2. `MIGRATION.md` §9.2 — fill the two CF-01 rows. `paladin-core` / `EdgeCondition`: Mitigation
   `none required — no signature or variant change; the semantic change is M-B-01`,
   Deliberate-breaking `N`. `paladin-battalion` / `CampaignExecutionService`: Mitigation
   `additive builder method (with_evaluator); new(paladin_port) unchanged`, Deliberate-breaking
   `N`. Leave the `Commander`/`CommanderBuilder` row's `TBD` alone — Plan 23-03 owns it (D-27).

3. `MIGRATION.md` §9.8 — leave the section `TBD` (owner SHIP-01, Phase 29) but update the
   parenthetical so it records custom-evaluator registration as landed in this phase rather than
   as pending, naming `with_evaluator` and `with_edge_evaluator` as the two registration surfaces.

4. `.project/v0.10.0/08-traceability-matrix.md` — update the BUG-01 row per protocol step 4 with
   the two commit SHAs from Task 1 (the `test(23-01)` RED SHA and the `fix(23-01)` GREEN SHA, in
   that order) and the grep-absence evidence, matching the column shape the BUG-02/03/04 rows use.

Do not edit any other `.project/` file in this task (D-29 restricts this phase's `.project/` edits
to the ENG-FR-11 note, owned by Plan 23-06, and this BUG-01 row).
  </action>
  <verify>
    <automated>grep -c 'with_evaluator' MIGRATION.md && grep -c 'TBD — owner CF-01' MIGRATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'TBD — owner CF-01, Phase 23' MIGRATION.md` returns 0.
    - `grep -c 'with_evaluator' MIGRATION.md` returns at least 1, and `grep -c 'is_urgent' MIGRATION.md` returns at least 2 (the before line and the after line).
    - The M-B-01 worked-example block in `MIGRATION.md` contains all four of the substrings `EdgeCondition::Custom`, `with_evaluator`, `InvalidGraph`, and `Contains`.
    - `grep -c 'TBD — owner CF-05, Phase 23' MIGRATION.md` returns at least 1 (the Commander row is deliberately left for Plan 23-03).
    - `grep -n 'BUG-01' .project/v0.10.0/08-traceability-matrix.md` matches a row containing both commit SHAs produced by Task 1, verifiable with `git cat-file -e <sha>` for each.
    - `git diff --stat` for this task lists exactly `MIGRATION.md` and `.project/v0.10.0/08-traceability-matrix.md`.
  </acceptance_criteria>
  <done>M-B-01 carries a worked before/after example quoting the shipped API and the real error text, both CF-01 §9.2 rows are resolved, and the BUG-01 traceability row names the RED and GREEN commits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow author -> `EdgeConditionEvaluator` impl | Third-party code registered by name and invoked inside the scheduler; its `Err` and its verdict both steer routing. |
| caller -> `WarGraph::validate` / `CampaignExecutionService::execute` | The validation verdict is the trust surface: an accepted graph that then routes on an unevaluated condition is the boundary violation this plan closes. |
| in-process only | No network, filesystem, credential or external-input surface is added by this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-01 | Tampering (of the routing contract) | `evaluate_edge_condition` on both paths | high | mitigate | The `EdgeCondition::Custom` arm no longer returns a default verdict on either path; every custom name must resolve to a registered evaluator at validation time (Task 1 clauses 2 and 3), and the prohibition in `must_haves` forbids any restoring configuration. |
| T-23-02 | Denial of Service | third-party `EdgeConditionEvaluator::evaluate` awaited inside the superstep loop | medium | accept | A hostile or hanging evaluator is author-supplied in-process code with the same trust as a `StateNode`; per-node timeouts are Phase 25 (`FT-03`) scope. `EngineLimits::max_supersteps` remains the run-level bound. Recorded, not mitigated here. |
| T-23-03 | Information Disclosure | `EdgeEvaluatorFailed`'s `source` field and the legacy error message | medium | mitigate | The typed error carries the evaluator NAME, the source and target `NodeId`s, and the evaluator's own structured reason — never the rendered Battlefield JSON or the evaluated `output` string, so a state field holding secret-like data cannot reach a log line through an error path. Asserted by the acceptance criterion that the error's fields are exactly `from`, `to`, `evaluator`, `source`. |
| T-23-04 | Repudiation | git history of the BUG-01 fix | low | mitigate | Two-commit red-then-green order (Task 1 acceptance criteria) plus the traceability-matrix SHAs (Task 2) make the defect and its closure independently auditable, per protocol step 4. |
| T-23-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no packages and does not modify any `Cargo.toml` or `Cargo.lock`. `23-RESEARCH.md` §Package Legitimacy Audit records zero external package additions for the whole phase and no `[ASSUMED]`/`[SUS]`/`[SLOP]` verdict; the only manifest edit in Phase 23 is the workspace-local `paladin-llm` dev-dependency in Plan 23-03. |
</threat_model>

<verification>
- `cargo test -p paladin-battalion --lib` — 0 failed, 0 ignored.
- `cargo test -p paladin-battalion` — full crate green including integration targets.
- `cargo test --workspace` — no regression outside the crate.
- `grep -rn "defaulting to true" crates/` — no matches.
- `git log --oneline` shows `test(23-01)` strictly before `fix(23-01)`.
- `MIGRATION.md` has no `TBD — owner CF-01, Phase 23` remaining.
- `cargo fmt --check` and `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean.
</verification>

<success_criteria>
An unregistered `EdgeCondition::Custom` fails validation before any node executes on both the
legacy and engine paths, naming every offender sorted and deduped; registered evaluators route
true and false correctly; an evaluator error fails the run with a typed error naming the edge and
the evaluator; both placeholder sites are deleted with no restoring configuration; and M-B-01
carries a worked before/after example with the two CF-01 §9.2 rows resolved.
</success_criteria>

<output>
Create `.planning/phases/23-control-flow-dynamic-routing-fan-out-subgraphs/23-01-SUMMARY.md` when done
</output>
