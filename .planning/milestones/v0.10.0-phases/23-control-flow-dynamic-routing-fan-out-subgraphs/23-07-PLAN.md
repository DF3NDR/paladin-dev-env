---
phase: 23-control-flow-dynamic-routing-fan-out-subgraphs
plan: 07
type: execute
wave: 5
depends_on: [23-05]
files_modified:
  - src/config/engine.rs
  - src/config/mod.rs
  - MIGRATION.md
autonomous: true
requirements: [CF-03]

must_haves:
  truths:
    - "EngineConfig exists at src/config/engine.rs with Default, validate() -> Result<(), String> and EnvOverridable, mirroring CitadelConfig and WaypointRetentionConfig field-for-field (X-09, D-16)."
    - "EngineConfig carries the four fields MIGRATION.md §9.5 already documents — max_supersteps (50), max_node_visits (25), run_timeout_secs (None), waypoint_durability (Strict) — plus max_muster_tasks (100), each with the APP_ENGINE_* environment override §9.5 names (D-16)."
    - "APP_ENGINE_MAX_MUSTER_TASKS set in the environment reaches a running engine's effective limit end-to-end: apply_env_overrides -> validate -> conversion into EngineLimits -> a muster exceeding it fails with the typed limit error (D-16, CF-FR-13)."
    - "validate() rejects a zero value for max_supersteps, max_node_visits and max_muster_tasks, and rejects Some(0) for run_timeout_secs, in the same style WaypointRetentionConfig rejects Some(0) bounds."
    - "A v0.9 configuration file with no engine: section resolves to EngineConfig::default(), whose values equal today's EngineLimits::default() and WaypointDurability::Strict — so a v0.9 configuration boots v0.10 with identical engine behavior (X-09, §9.5's default-off claim)."
    - "MIGRATION.md §9.5 no longer describes EngineConfig as planned-but-absent: the entry records it as landed, at its real path, with max_muster_tasks and APP_ENGINE_MAX_MUSTER_TASKS added, and records that LlmDecision and StrategySelection::Semantic deliberately add no config struct and no env var (D-16, D-26)."
  artifacts:
    - "src/config/engine.rs — EngineConfig with Default, validate(), EnvOverridable, and the conversions into EngineLimits and WaypointDurability."
    - "src/config/mod.rs — the module declaration and the EngineConfig re-export, matching the WaypointRetentionConfig entries."
    - "MIGRATION.md — §9.5's EngineConfig bullet rewritten as landed, plus the code-configured-only note for CF-05."
  key_links:
    - "EngineConfig -> EngineLimits: the conversion is `impl From<EngineConfig> for EngineLimits` in src/config/engine.rs — legal under the orphan rule because EngineConfig is local to this crate, so no paladin-battalion edit is needed."
    - "APP_ENGINE_MAX_MUSTER_TASKS -> EngineLimits.max_muster_tasks -> the Directive-receipt limit check Plan 23-05 added: this is the whole reason X-09 requires the struct for CF-FR-13."
    - "MIGRATION.md §9.5's 'not yet in the tree' claim -> this plan: §9.5 is the single place that claim lives, and closing it is part of the phase's program-gate obligation (D-16)."
---

<objective>
Bring `EngineConfig` into the tree — the X-09 config struct `max_muster_tasks` requires — carrying
the four engine tunables `MIGRATION.md` §9.5 already documents plus the new one, and close §9.5's
standing "not yet in the tree" claim.

Purpose: X-09 requires a config struct for every new runtime tunable, and CF-FR-13 adds one.
`EngineConfig` never landed in Phase 22, so covering the four pre-existing fields alongside is
incidental and cheap — and it is what makes the "a v0.9 configuration boots v0.10 identically"
claim checkable rather than asserted.

Output: `src/config/engine.rs` mirroring the house config shape, an `APP_ENGINE_*` override path
proven end-to-end into a running engine's limits, and §9.5 updated.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-control-flow-dynamic-routing-fan-out-subgraphs/23-CONTEXT.md
@.planning/phases/23-control-flow-dynamic-routing-fan-out-subgraphs/23-RESEARCH.md
@.planning/phases/23-control-flow-dynamic-routing-fan-out-subgraphs/23-PATTERNS.md
@src/config/waypoint_retention.rs
@src/config/citadel.rs
</context>

## Artifacts this phase produces

| Kind | Symbol / path | Plan |
|------|---------------|------|
| module | `src/config/engine.rs` | 23-07 |
| struct | `EngineConfig` | 23-07 |
| env vars | `APP_ENGINE_MAX_SUPERSTEPS`, `APP_ENGINE_MAX_NODE_VISITS`, `APP_ENGINE_RUN_TIMEOUT_SECS`, `APP_ENGINE_WAYPOINT_DURABILITY`, `APP_ENGINE_MAX_MUSTER_TASKS` | 23-07 |
| trait impl | `From<EngineConfig> for EngineLimits` | 23-07 |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: EngineConfig from environment to a running engine's effective limits</name>
  <files>src/config/engine.rs, src/config/mod.rs</files>
  <read_first>
    - `src/config/waypoint_retention.rs` (read in full) — the exact shape to mirror: module rustdoc naming the FR, `#[derive(Debug, Clone, Serialize, Deserialize)]`, the MANUAL `Default` impl with its `#[allow(clippy::derivable_impls)]` and the comment explaining why it is manual, `validate() -> Result<(), String>` colocated with the zero-checks, and the `EnvOverridable` impl.
    - `src/config/citadel.rs` (lines 1-90) — the `read_env`/`EnvOverridable` idiom and how a non-numeric field is parsed from its environment variable.
    - `src/config/env_utils.rs` — `EnvOverridable`'s trait definition and `read_env`'s contract, including what happens to an unparseable value.
    - `src/config/mod.rs` — the `pub mod waypoint_retention;` declaration (line 27) and the `pub use crate::config::waypoint_retention::WaypointRetentionConfig;` re-export (line 49); add the `engine` entries in exactly those two places.
    - `crates/paladin-battalion/src/engine/graph.rs` — `EngineLimits`'s four fields after Plan 23-05 (`max_supersteps: u64`, `max_node_visits: u32`, `run_timeout: Option<Duration>`, `max_muster_tasks: u32`) and its `Default` impl, so the conversion and the default-equality test target the real values.
    - `crates/paladin-battalion/src/engine/mod.rs` — `WaypointDurability`'s variants and any existing string parsing for it.
    - `MIGRATION.md` §9.5 — the `EngineConfig` bullet (lines 71-75), which names all four field defaults and env vars verbatim; those are the target values, not a starting point for judgment.
  </read_first>
  <behavior>
    - `default_engine_config_matches_todays_engine_defaults` — `EngineLimits::from(EngineConfig::default())`
      equals `EngineLimits::default()`, and the default durability is `Strict`. This is the
      mechanical form of §9.5's "a v0.9 configuration boots identically" claim.
    - `validate_rejects_zero_limits` — zero for `max_supersteps`, `max_node_visits` or
      `max_muster_tasks` each returns `Err` naming the offending field; `Some(0)` for
      `run_timeout_secs` likewise.
    - `env_overrides_apply_for_every_field` — each of the five `APP_ENGINE_*` variables set in the
      environment changes exactly its own field and no other.
    - `unparseable_env_value_leaves_the_field_at_its_prior_value` — matching the existing
      `read_env` idiom's behavior in `citadel.rs`; assert against that behavior, not a new one.
    - `waypoint_durability_parses_both_variants_case_insensitively` — `Strict` and `BestEffort`.
    - `app_engine_max_muster_tasks_reaches_a_running_engines_limit` — the end-to-end tracer
      assertion: set the environment variable to a small value, build an `EngineConfig`, apply
      overrides, validate, convert into `EngineLimits`, run a graph whose planner musters more than
      that many tasks, and assert the run fails with the typed muster-limit error naming that
      number. Use a serialised environment guard so the test does not race sibling tests that read
      the process environment.
  </behavior>
  <action>
1. Create `src/config/engine.rs` as a field-for-field mirror of `src/config/waypoint_retention.rs`,
   including its module rustdoc convention (name the FR — X-09 and CF-FR-13 — and state the
   defaults-to-today's-behavior contract explicitly in prose, as `WaypointRetentionConfig`'s
   rustdoc does):
   - `#[derive(Debug, Clone, Serialize, Deserialize)] pub struct EngineConfig` with
     `max_supersteps: u64` (default `50`), `max_node_visits: u32` (default `25`),
     `run_timeout_secs: Option<u64>` (default `None`), `waypoint_durability: WaypointDurability`
     (default `Strict`) and `max_muster_tasks: u32` (default `100`). These five values and the five
     env-var names below are taken verbatim from `MIGRATION.md` §9.5 plus D-16 — do not re-derive
     them.
   - A MANUAL `Default` impl (not a derive), with the same `#[allow(clippy::derivable_impls)]`
     treatment and an equivalent comment explaining why it is manual and colocated with
     `validate()`.
   - `validate(&self) -> Result<(), String>` rejecting `0` for `max_supersteps`,
     `max_node_visits` and `max_muster_tasks`, and `Some(0)` for `run_timeout_secs`, with messages
     in the same voice as `WaypointRetentionConfig`'s. Return `Result<(), String>` — do NOT
     introduce a `thiserror` enum here; both existing config structs use the `String` form and X-06
     governs new failure modes in library code, not this established config convention.
   - `impl EnvOverridable for EngineConfig` reading `APP_ENGINE_MAX_SUPERSTEPS`,
     `APP_ENGINE_MAX_NODE_VISITS`, `APP_ENGINE_RUN_TIMEOUT_SECS`,
     `APP_ENGINE_WAYPOINT_DURABILITY` and `APP_ENGINE_MAX_MUSTER_TASKS`, using the same
     `read_env`/parse idiom both existing configs use — do not invent a second idiom.
   - `impl From<EngineConfig> for EngineLimits` (legal under the orphan rule because `EngineConfig`
     is local to this crate — no `paladin-battalion` edit is required) mapping `run_timeout_secs`
     into `Option<Duration>`, plus an accessor or `From` for the `WaypointDurability` half.

2. `src/config/mod.rs`: add `pub mod engine;` beside the other module declarations and
   `pub use crate::config::engine::EngineConfig;` beside the `WaypointRetentionConfig` re-export,
   in the same alphabetical/ordering convention the file already uses.

3. Every public item gets rustdoc with a doc test (X-02). The doc test must not mutate the process
   environment.

4. Add nothing to `EngineLimits` in this plan — Plan 23-05 already added `max_muster_tasks`; this
   plan only supplies the configuration surface for it.
  </action>
  <verify>
    <automated>cargo test -p paladin-ai --lib config::engine</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p paladin-ai --lib config::engine` exits 0 with `0 failed`.
    - `cargo test -p paladin-ai --lib config::engine::tests::default_engine_config_matches_todays_engine_defaults` exits 0 and reports `1 passed`.
    - `cargo test -p paladin-ai --lib config::engine::tests::app_engine_max_muster_tasks_reaches_a_running_engines_limit` exits 0 and reports `1 passed`.
    - `src/config/engine.rs` contains all five substrings `APP_ENGINE_MAX_SUPERSTEPS`, `APP_ENGINE_MAX_NODE_VISITS`, `APP_ENGINE_RUN_TIMEOUT_SECS`, `APP_ENGINE_WAYPOINT_DURABILITY`, `APP_ENGINE_MAX_MUSTER_TASKS`.
    - `grep -c 'impl Default for EngineConfig' src/config/engine.rs` returns 1 and `grep -c '#\[derive(.*Default' src/config/engine.rs` returns 0 (manual impl, matching the house convention).
    - `grep -c 'fn validate(&self) -> Result<(), String>' src/config/engine.rs` returns 1.
    - `grep -c 'pub mod engine;' src/config/mod.rs` returns 1 and `grep -c 'config::engine::EngineConfig' src/config/mod.rs` returns 1.
    - `git diff HEAD -- crates/paladin-battalion/` is empty (the conversion lives in the root crate; no battalion edit).
    - `cargo test -p paladin-ai --doc config::engine` exits 0.
    - `cargo fmt --check` exits 0 and `cargo clippy --workspace --all-targets --all-features -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>`EngineConfig` exists in the house shape, defaults to today's engine behavior, rejects invalid values, and an `APP_ENGINE_MAX_MUSTER_TASKS` override is proven to reach a running engine's effective muster limit.</done>
</task>

<task type="auto">
  <name>Task 2: Close MIGRATION.md §9.5's EngineConfig claim and record CF-05's no-config decision</name>
  <files>MIGRATION.md</files>
  <read_first>
    - `MIGRATION.md` §9.5 in full (lines 65-82) — the `EngineConfig` bullet (71-75) with its "**not yet in the tree**; lands in a later engine-wiring plan of this phase" wording, the `WaypointRetentionConfig` bullet (76-80) as the format for a landed entry, and the closing paragraph (82) claiming a v0.9 configuration resolves to identical behavior.
    - `src/config/engine.rs` as Task 1 left it — the real field names, defaults and env vars, so the entry describes what shipped rather than what was planned.
    - `.planning/phases/23-control-flow-dynamic-routing-fan-out-subgraphs/23-CONTEXT.md` D-16 and D-26 — the two facts this section must now record.
    - `crates/paladin-battalion/src/llm_decision.rs` and `crates/paladin-battalion/src/commander.rs` as Plan 23-03 left them — confirm by reading that neither reads an `APP_*` variable, so the D-26 claim is written from the code rather than from intent.
  </read_first>
  <action>
1. Rewrite `MIGRATION.md` §9.5's `EngineConfig` bullet as a LANDED entry in the same format the
   `WaypointRetentionConfig` bullet uses: name the real path `src/config/engine.rs`, mark it landed
   in this plan, list all five fields with their defaults and `APP_ENGINE_*` variables (adding
   `max_muster_tasks` / `APP_ENGINE_MAX_MUSTER_TASKS`), and state the conversion into
   `EngineLimits`/`WaypointDurability`. Delete the bullet's planned-but-absent claim (the exact
   phrase this task's acceptance criterion negative-greps for) and its "lands in a later
   engine-wiring plan" pointer — both are now false, and neither may be quoted back into the
   rewritten entry.

2. Add a bullet to §9.5 recording D-26 explicitly: `LlmDecision` evaluators and Commander
   `StrategySelection::Semantic` hold trait objects configured in code, add no `APP_*` environment
   variable and no config struct, and their `on_ambiguous`/`on_parse_error` settings are
   per-evaluator and per-node enums rather than runtime-global tunables — so X-09 adds no config
   struct for either. If Plan 23-03 already added this bullet, verify its text against the code and
   leave it rather than duplicating it.

3. Update §9.5's closing paragraph so its "a v0.9 configuration file — which has no
   `engine:`/`waypoint_retention:` section at all — resolves to identical runtime behavior" claim
   names the now-checkable proof: `EngineConfig::default()` converts to `EngineLimits::default()`
   and `WaypointDurability::Strict`, asserted by the named test from Task 1. Leave the
   integration-test TBD owned by SHIP-02 / Phase 29 untouched.

4. Edit no other `MIGRATION.md` section in this task.
  </action>
  <verify>
    <automated>grep -c 'not yet in the tree' MIGRATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'not yet in the tree' MIGRATION.md` returns 0.
    - `grep -c 'APP_ENGINE_MAX_MUSTER_TASKS' MIGRATION.md` returns at least 1.
    - `grep -c 'src/config/engine.rs' MIGRATION.md` returns at least 1.
    - The §9.5 `EngineConfig` entry contains all five of `max_supersteps`, `max_node_visits`, `run_timeout_secs`, `waypoint_durability`, `max_muster_tasks`.
    - `grep -c 'StrategySelection::Semantic' MIGRATION.md` returns at least 1 and the matched bullet also contains `no `APP_` variable`-equivalent wording plus `X-09`.
    - `git diff --stat` for this task lists exactly `MIGRATION.md`.
    - No `MIGRATION.md` section other than §9.5 differs from its pre-task content — verified by `git diff MIGRATION.md` showing changed hunks only within the §9.5 range.
  </acceptance_criteria>
  <done>§9.5 records `EngineConfig` as landed at its real path with all five fields and env vars, records that CF-05's LLM routing adds no config surface, and its identical-boot claim points at a named passing test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process environment -> `EngineConfig` | `APP_ENGINE_*` variables are operator-controlled input that sets execution and resource limits for every engine run. |
| config file -> `EngineConfig` | A deserialized `engine:` section reaches the same fields; `validate()` is the single gate both paths pass through. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-28 | Denial of Service | an operator setting a limit to zero or an absurd value | medium | mitigate | `validate()` rejects zero for all three count limits and `Some(0)` for the timeout, in the same style `WaypointRetentionConfig` rejects zero bounds; the defaults are the bounded values `MIGRATION.md` §9.5 already documents. |
| T-23-29 | Tampering (silent behavior change on upgrade) | `EngineConfig::default()` | high | mitigate | `default_engine_config_matches_todays_engine_defaults` asserts the default config converts to exactly `EngineLimits::default()` and `WaypointDurability::Strict`, so a v0.9 configuration with no `engine:` section cannot resolve to different runtime behavior. |
| T-23-30 | Information Disclosure | config values in logs or `Debug` output | low | accept | `EngineConfig` carries only numeric limits and a durability enum — no credential, path, or connection string — so its `Debug` output is not a disclosure surface. Recorded for completeness. |
| T-23-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no packages and modifies no manifest; `23-RESEARCH.md` §Package Legitimacy Audit records zero external package additions for the phase. |
</threat_model>

<verification>
- `cargo test -p paladin-ai --lib config::engine` — 0 failed.
- `cargo test -p paladin-ai --doc config::engine` — doc tests pass.
- `cargo test --workspace` — no regression.
- `grep -c 'not yet in the tree' MIGRATION.md` — 0.
- `git diff HEAD -- crates/paladin-battalion/` — empty.
- `cargo fmt --check` and `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean.
</verification>

<success_criteria>
`EngineConfig` exists at `src/config/engine.rs` in the house shape with all five fields, their
documented defaults and their `APP_ENGINE_*` overrides; the override path is proven end-to-end into
a running engine's muster limit; the default config is proven equal to today's engine behavior; and
`MIGRATION.md` §9.5 records it as landed with CF-05's deliberate no-config decision alongside.
</success_criteria>

<output>
Create `.planning/phases/23-control-flow-dynamic-routing-fan-out-subgraphs/23-07-SUMMARY.md` when done
</output>
