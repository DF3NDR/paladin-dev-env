---
phase: 23-control-flow-dynamic-routing-fan-out-subgraphs
plan: 12
type: execute
wave: 9
depends_on: [23-10, 23-11]
files_modified:
  - docs/src/user-guides/control-flow.md
  - docs/src/SUMMARY.md
  - MIGRATION.md
  - CHANGELOG.md
  - .project/current-exports.txt
autonomous: true
requirements: [CF-01, CF-02, CF-03, CF-04, CF-05]

must_haves:
  truths:
    - "A new mdBook page documents control flow — Directives with Goto and End, Muster, subgraphs, LlmDecision and Commander Semantic — each with a minimal example, behind a short WarGraph preamble, and is wired into docs/src/SUMMARY.md so it is reachable from the built book (X-08, D-28)."
    - "Every new public API added by this phase has rustdoc with a doc test, and cargo doc builds with no new broken intra-doc links (X-08, X-02)."
    - "MIGRATION.md §9.2 carries a deliberate-zero note for the new-in-0.10 engine and Waypoint types this phase reshaped — StateNode, NodeSpec, NodeContext, EngineLimits, EngineError, WarGraph, Waypoint — recording that their absence from the register is deliberate, in the same voice as the Plan 22-01 note (D-07, D-27)."
    - "All three CF-owned §9.2 rows are resolved with no TBD remaining: EdgeCondition, CampaignExecutionService (Plan 23-01) and Commander/CommanderBuilder (Plan 23-03) (D-27)."
    - "cargo semver-checks against the v0.9.0 published crates and the MSRV 1.88 job are both green on the phase's final commit, with the semver allowlist containing only entries present in MIGRATION.md §9.2 (X-10.5, X-11.1, D-27)."
    - "make security and cargo clippy --workspace --all-targets --all-features -- -D warnings are clean, and workspace line coverage stays at or above 82% (ADR-0006, D-30)."
    - ".project/current-exports.txt is regenerated if and only if the public surface it tracks moved, using the existing scripts/extract-public-api.sh path (D-27)."
    - "CHANGELOG.md's [Unreleased] section records this phase's user-visible changes, including the single sanctioned behavioral break M-B-01 with a pointer to MIGRATION.md."
  artifacts:
    - "docs/src/user-guides/control-flow.md — the new mdBook page."
    - "docs/src/SUMMARY.md — the page's entry under User Guides."
    - "MIGRATION.md — the §9.2 deliberate-zero note and confirmation that no CF-owned TBD remains."
    - "CHANGELOG.md — the [Unreleased] entries for CF-01…CF-05."
    - ".project/current-exports.txt — regenerated if the tracked surface moved."
  key_links:
    - "docs/src/SUMMARY.md -> the new page: an mdBook page not listed in SUMMARY.md is not in the book at all, so the wiring is the deliverable, not an afterthought."
    - "MIGRATION.md §9.2 -> the cargo semver-checks allowlist: X-10.5 requires the allowlist to contain only entries present in the register, so the register must be final before the gate is evidenced."
    - "the phase's rustdoc doc tests -> cargo doc + cargo test --doc: X-08's no-new-broken-intra-doc-links requirement and X-02's doc-test requirement are checked by two different commands; both must run."
---

<objective>
Close the phase's program-gate obligations: the X-08 mdBook control-flow page, the §9.2 register's
final state including the deliberate-zero note, the CHANGELOG entries, and the recorded evidence
that `cargo semver-checks`, the MSRV job, `make security`, clippy and the coverage floor are all
green on the phase's final commit.

This plan produces documentation and gate evidence rather than runtime behavior, so it has no
vertical slice to trace and therefore leads with an `auto` task rather than a `tracer` one.

Purpose: PRD 02 §3 item 7 makes the versioning gate an acceptance criterion, X-08 makes the docs
one, and `MIGRATION.md` is a living document the verification pass treats as a program-level
failure if it is stale. None of these close themselves.

Output: a reachable control-flow page with doc-tested examples, a §9.2 register with no CF-owned
TBD and an explicit deliberate-zero note, CHANGELOG entries, and recorded green gate evidence.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-control-flow-dynamic-routing-fan-out-subgraphs/23-CONTEXT.md
@.planning/phases/23-control-flow-dynamic-routing-fan-out-subgraphs/23-RESEARCH.md
@.project/v0.10.0/00-program-overview.md
@.project/v0.10.0/02-control-flow-routing-fanout-subgraphs.md
@MIGRATION.md
</context>

## Artifacts this phase produces

| Kind | Symbol / path | Plan |
|------|---------------|------|
| doc page | `docs/src/user-guides/control-flow.md` | 23-12 |
| doc entry | the `SUMMARY.md` User Guides line for that page | 23-12 |
| doc note | the `MIGRATION.md` §9.2 deliberate-zero note for this phase | 23-12 |

<tasks>

<task type="auto">
  <name>Task 1: The control-flow mdBook page, wired into the book</name>
  <files>docs/src/user-guides/control-flow.md, docs/src/SUMMARY.md</files>
  <read_first>
    - `docs/src/user-guides/battalion-patterns.md` and `docs/src/user-guides/maneuver-flow-dsl.md` — the house voice, heading depth, and how runnable examples are presented in this book; copy that shape rather than inventing one.
    - `docs/src/SUMMARY.md` — the `# User Guides` block (lines ~11-27) where the new entry belongs, and the ordering convention used there.
    - `docs/book.toml` — `src = "src"`, so the page path is relative to `docs/src/`.
    - The five surfaces this page documents, as their plans left them: `crates/paladin-battalion/src/edge_evaluator.rs` and `llm_decision.rs`, `crates/paladin-core/src/platform/container/directive.rs`, `crates/paladin-battalion/src/engine/directive_parser.rs`, and `crates/paladin-battalion/src/engine/graph.rs`'s `add_worker_template` / `NodeSpec::Battalion` / `StateMap`. Every example on the page must compile against these as shipped.
    - `.planning/phases/23-control-flow-dynamic-routing-fan-out-subgraphs/23-CONTEXT.md` D-28 and its Deferred Ideas entry — the page carries only a SHORT WarGraph preamble; the full engine page is a Phase 29 / SHIP-01 item and must not be absorbed here.
    - `.project/v0.10.0/00-program-overview.md` §4 — the ubiquitous-language table, so the page uses Directive, Muster, Vanguard, Parley, Battlefield, Waypoint, Thread, WarGraph, WarEngine and Commander consistently.
  </read_first>
  <action>
1. Create `docs/src/user-guides/control-flow.md` with a short WarGraph preamble (enough context that
   a reader who has never met `WarGraph` can follow the page — no more) followed by one section per
   capability, each with a minimal example:
   - Directives: `NextStep::Edges` as the default, `Goto` with its validation and
     `max_node_visits` bound, `End` with its precedence over `Goto`, and a note that `Parley` is
     declared but lands in Phase 24.
   - `DirectiveParser`: `PlainOutput` as the backward-compatible default and `StructuredDirective`
     with the documented JSON envelope and both `on_parse_error` modes. Reference the rustdoc for
     the envelope's authoritative shape rather than restating it, so the two cannot drift.
   - Muster: worker templates, the `task_key` ordering guarantee, payload isolation with the
     `{muster.payload}` / `{muster.task_key}` template references, `max_muster_tasks`, and
     mid-muster resume.
   - Subgraphs: `NodeSpec::Battalion`, `StateMap`'s in and out directions, private child fields,
     namespaced child checkpoints, and `restart_on_resume`.
   - LLM routing: registering an `LlmDecisionEvaluator` under an `EdgeCondition::Custom` name, the
     `on_ambiguous` modes, and Commander `StrategySelection::Semantic` with its heuristic fallback.
     Include the egress-boundary warning: the rendered prompt is what leaves the process, and the
     workflow author controls it.
   - A short migration callout for M-B-01 linking to `MIGRATION.md` §9.1, since a reader arriving
     from a v0.9 workflow needs it here.

2. Wire the page into `docs/src/SUMMARY.md` under `# User Guides`, in the existing entry style and
   ordering convention. A page absent from `SUMMARY.md` is absent from the book.

3. Keep every example minimal and correct against the shipped API. Where an example duplicates a
   rustdoc doc test, link to the item instead of copying it.

4. Do NOT write a general WarEngine/Battlefield/Waypoint page here — that residual is recorded under
   Deferred Ideas and belongs to a docs pass or SHIP-01 (Phase 29). The preamble is the whole of
   this plan's engine coverage.
  </action>
  <verify>
    <automated>cargo doc --workspace --no-deps 2>&1 | grep -c 'warning: unresolved link' ; grep -c 'control-flow.md' docs/src/SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/src/user-guides/control-flow.md` exists and contains all six substrings `NextStep`, `DirectiveParser`, `add_worker_template`, `StateMap`, `LlmDecisionEvaluator`, `StrategySelection::Semantic`.
    - `grep -c 'control-flow.md' docs/src/SUMMARY.md` returns 1, and the matched line sits inside the `# User Guides` block (verified by `awk '/^# User Guides/{f=1} /^# Architecture/{f=0} f && /control-flow.md/' docs/src/SUMMARY.md` printing the line).
    - `grep -c 'M-B-01\|MIGRATION.md' docs/src/user-guides/control-flow.md` returns at least 1.
    - `cargo doc --workspace --no-deps` exits 0 and emits no `unresolved link` warning that is absent from a HEAD-baseline run of the same command.
    - `cargo test --workspace --doc` exits 0 with `0 failed`.
    - `grep -c 'Battlefield' docs/src/user-guides/control-flow.md` returns at least 1 and the page's engine coverage stays a preamble — `wc -l docs/src/user-guides/control-flow.md` is under 400 lines.
  </acceptance_criteria>
  <done>A control-flow page exists, is reachable from the built book, documents all five capabilities with minimal correct examples and the M-B-01 callout, and `cargo doc` gains no broken intra-doc link.</done>
</task>

<task type="auto">
  <name>Task 2: Finalise the §9.2 register and the CHANGELOG</name>
  <files>MIGRATION.md, CHANGELOG.md</files>
  <read_first>
    - `MIGRATION.md` §9.2 in full — the column contract (line 29), all eleven pre-populated rows, the three CF-owned rows resolved by Plans 23-01 and 23-03, and the Plan 22-01 deliberate-zero note (line 45) whose voice and structure this phase's note must match.
    - `MIGRATION.md` §9.1 — the M-B-01 row and its worked example as Plan 23-01 landed it, so the CHANGELOG's behavioral-break entry points at the right anchor.
    - `CHANGELOG.md` — the `## [Unreleased]` section and the MSRV entry beneath it (the house entry style: a bolded lead sentence, the mechanism, then a `MIGRATION.md` anchor link).
    - `.project/v0.10.0/00-program-overview.md` X-10 rules 1-3 and §9.2's instruction that each row's final state must match the `cargo semver-checks` allowlist exactly.
    - The types this phase reshaped that are absent at the `v0.9.0` tag, so the deliberate-zero note enumerates them accurately: `StateNode`, `NodeSpec`, `NodeContext`, `NodeError`, `EngineLimits`, `EngineError`, `WarGraph`, `Waypoint`, `ThreadId`, `FrontierSnapshot`.
  </read_first>
  <action>
1. `MIGRATION.md` §9.2 — add a deliberate-zero note in the same voice and structure as the Plan
   22-01 note at line 45, covering the new-in-0.10 engine and Waypoint types this phase reshaped:
   `StateNode` (return type changed), `NodeSpec` (a variant and a field added), `NodeContext` (a
   field added), `EngineLimits` (a field added), `EngineError` (variants added to an already
   `#[non_exhaustive]` enum), `WarGraph` (`validate`'s signature changed, a marker set added) and
   `Waypoint` (two additive `#[serde(default)]` fields). State plainly that every one of these is
   absent at the `v0.9.0` tag, so X-10 — which governs only pre-existing public types — does not
   apply, and that their absence from the register is deliberate rather than an omission.

2. Verify no CF-owned `TBD` remains anywhere in `MIGRATION.md` — the three rows (`EdgeCondition`,
   `CampaignExecutionService`, `Commander`/`CommanderBuilder`) plus §9.1's M-B-01 worked example and
   §9.5's `EngineConfig` entry. If any is still pending, that is a finding to report rather than a
   gap to fill from this plan: the owning plan's work did not land.

3. `CHANGELOG.md` `## [Unreleased]` — add entries for this phase in the house style. At minimum:
   a `### Changed` entry for M-B-01 (custom edge conditions now fail closed) with a link to
   `MIGRATION.md` §9.1 and the one-line "why" (the previous behavior silently always-routed), and
   `### Added` entries for node-driven `Directive` routing, Muster dynamic fan-out, `NodeSpec::Battalion`
   subgraph composition, LLM-evaluated edge routing and Commander `Semantic` strategy selection, and
   `EngineConfig` with its `APP_ENGINE_*` variables. Each entry states what a user can now do or
   must now do — not which plan produced it.

4. Do not touch any other `MIGRATION.md` section, and do not create a released-version heading in
   `CHANGELOG.md`; the release cut is Phase 29 / SHIP-01's.
  </action>
  <verify>
    <automated>grep -c 'TBD — owner CF-' MIGRATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'TBD — owner CF-' MIGRATION.md` returns 0.
    - `MIGRATION.md` §9.2 contains a note whose text includes all four substrings `deliberate zero`, `v0.9.0`, `StateNode`, and `Waypoint`.
    - `grep -c 'M-B-01' CHANGELOG.md` returns at least 1, and the matched `## [Unreleased]` entry also contains `MIGRATION.md`.
    - `CHANGELOG.md`'s `## [Unreleased]` section contains all five substrings `Directive`, `Muster`, `Battalion`, `LlmDecision`, `EngineConfig`.
    - `grep -c '^## \[0\.10\.0\]' CHANGELOG.md` returns 0 (no release heading cut by this plan).
    - `git diff --stat` for this task lists exactly `MIGRATION.md` and `CHANGELOG.md`.
    - `./scripts/check-changelogs.sh` exits 0 if that script gates `CHANGELOG.md`'s structure; if it does not apply to the root changelog, record that in the SUMMARY rather than skipping silently.
  </acceptance_criteria>
  <done>§9.2 carries an explicit deliberate-zero note for this phase's new-in-0.10 types, no CF-owned TBD remains anywhere in `MIGRATION.md`, and `[Unreleased]` records every user-visible change including the sanctioned break.</done>
</task>

<task type="auto">
  <name>Task 3: Record the program-gate evidence on the phase's final commit</name>
  <precondition>`cargo semver-checks --version` succeeds — the tool must be installed locally, or the evidence must come from the repository's existing `semver` CI job (`.github/workflows/ci.yml`, job `semver`) run on the phase's final commit. If neither is available, record the gate as not-run with that reason in the SUMMARY and do not mark the plan complete on the reduced evidence.</precondition>
  <files>.project/current-exports.txt, MIGRATION.md</files>
  <read_first>
    - `.github/workflows/ci.yml` — the `semver` job (line ~303) and the `msrv` job (line ~251): their exact commands, their toolchain pins, and what they compare against, so the recorded evidence names the same commands CI runs rather than a local approximation.
    - `scripts/extract-public-api.sh` and `scripts/check-api-surface.sh` — how `.project/current-exports.txt` is generated and checked, and which crates it covers.
    - `Makefile` — the `audit`, `deny` and `security` targets (lines ~348-380), and any coverage target, so the recorded commands are the project's own rather than invented ones.
    - `.planning/decisions/0006-coverage-gate.md` — the 82% workspace line-coverage floor and the `cargo llvm-cov --fail-under-lines` form CI's `coverage` job uses.
    - `MIGRATION.md` §9.2 as Task 2 left it — the register whose final state the semver allowlist must match exactly (X-10.5).
    - `.project/v0.10.0/02-control-flow-routing-fanout-subgraphs.md` §3 item 7 — the versioning-gate acceptance criterion this task evidences.
  </read_first>
  <action>
1. Run each gate on the phase's final commit and record the exact command and its verbatim result in
   the plan SUMMARY — a paraphrase does not count as evidence, and a green job inside a run whose
   overall conclusion is not success is not closing evidence:
   - `cargo semver-checks` against the v0.9.0 published crates (or the CI `semver` job on this
     commit). Confirm the allowlist contains ONLY entries present in `MIGRATION.md` §9.2 (X-10.5) —
     if it contains anything else, that is a finding, not something to add a register row for after
     the fact.
   - The MSRV job at 1.88 (`cargo +1.88 check --workspace --all-features --all-targets --locked` or
     the CI `msrv` job's exact command).
   - `make security` (cargo-audit + cargo-deny).
   - `cargo clippy --workspace --all-targets --all-features -- -D warnings`.
   - `cargo fmt --check`.
   - Workspace line coverage against the 82% floor (ADR-0006), using the same `cargo llvm-cov
     --fail-under-lines` form CI's `coverage` job uses. Record the measured figure, not just the
     pass verdict.

2. Regenerate `.project/current-exports.txt` if and only if this phase moved the public surface that
   file tracks, using the existing `scripts/extract-public-api.sh` path — do not hand-edit it and do
   not regenerate it unconditionally (a spurious regeneration hides a real surface change in noise).
   If `scripts/check-api-surface.sh` passes unchanged, record that and leave the file alone.

3. If any gate fails, STOP and report it rather than working around it. A new advisory is a blocker
   unless it is explicitly allowlisted in `.cargo/audit.toml` with a reason, and adding such an entry
   is not this plan's authority.

4. Record every result in the SUMMARY with the command, the commit SHA it ran against, and the
   verbatim key output line — this is what the verification pass and Phase 29's release readiness
   read.
  </action>
  <verify>
    <automated>cargo fmt --check && cargo clippy --workspace --all-targets --all-features -- -D warnings && make security</automated>
  </verify>
  <acceptance_criteria>
    - `cargo fmt --check` exits 0.
    - `cargo clippy --workspace --all-targets --all-features -- -D warnings` exits 0.
    - `make security` exits 0 (cargo-audit and cargo-deny both clean), with no new entry added to `.cargo/audit.toml` or `deny.toml` by this plan (`git diff HEAD -- .cargo/audit.toml deny.toml` is empty).
    - `cargo semver-checks` (or the CI `semver` job on the phase's final commit) reports success, and the SUMMARY quotes its verdict line plus the commit SHA.
    - The MSRV check at 1.88 exits 0, and the SUMMARY quotes the exact command and the commit SHA.
    - Workspace line coverage is at or above 82.00%, with the measured figure recorded in the SUMMARY.
    - `./scripts/check-api-surface.sh` exits 0; `git status --porcelain .project/current-exports.txt` is either empty (surface unchanged) or shows a regeneration produced by `scripts/extract-public-api.sh` with the SUMMARY recording which symbols moved.
    - `cargo test --workspace` exits 0 with `0 failed`.
  </acceptance_criteria>
  <done>Every program gate — semver-checks, MSRV 1.88, make security, clippy, fmt, the 82% coverage floor and the API-surface check — is green on the phase's final commit with its command, SHA and verbatim result recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation -> workflow author | The control-flow page tells an author what leaves the process when `LlmDecision` renders a template; a page that omits that turns a documented boundary into an undocumented one. |
| dependency graph -> `make security` | cargo-audit and cargo-deny gate the phase on the RustSec advisory database and the licence policy. |
| public API surface -> downstream crates | `cargo semver-checks` and `.project/current-exports.txt` are what detect an unregistered compile-breaking change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-48 | Information Disclosure | undocumented LLM egress in the control-flow page | medium | mitigate | The LLM-routing section carries the egress-boundary warning explicitly — the rendered prompt is what leaves the process and the author controls it — matching the rustdoc obligation from Plan 23-03 so the warning exists at both the API and the guide level. |
| T-23-49 | Tampering (an unregistered breaking API change shipping) | the §9.2 register and the semver allowlist | high | mitigate | `cargo semver-checks` runs against the v0.9.0 published crates and its allowlist is confirmed to contain only entries present in §9.2 (X-10.5); the deliberate-zero note makes the new-in-0.10 exclusions explicit rather than implicit, so a missing row is distinguishable from a deliberate absence. |
| T-23-50 | Tampering (supply chain) | dependency advisories at phase close | high | mitigate | `make security` runs cargo-audit and cargo-deny and must exit 0, with an acceptance criterion asserting no new suppression was added to `.cargo/audit.toml` or `deny.toml` by this plan — closing the "silence the advisory instead of fixing it" path. |
| T-23-51 | Repudiation (evidence that reads as assurance while proving less) | the recorded gate evidence | high | mitigate | Each gate's command, commit SHA and verbatim key output line are recorded rather than paraphrased, and a green job inside a run whose overall conclusion is not success is explicitly not accepted — the 22.1 evidence discipline applied to this phase. |
| T-23-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no packages and modifies no manifest; `23-RESEARCH.md` §Package Legitimacy Audit records zero external package additions for the phase, and this plan's own criteria assert the audit and deny configs are unchanged. |
</threat_model>

<verification>
- `cargo doc --workspace --no-deps` — no new unresolved-link warning versus a HEAD baseline.
- `cargo test --workspace --doc` — 0 failed.
- `cargo test --workspace` — 0 failed.
- `cargo fmt --check`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `make security` — all clean.
- `cargo semver-checks` and the MSRV 1.88 check — green on the final commit, quoted in the SUMMARY.
- Workspace line coverage at or above 82%, figure recorded.
- `grep -c 'TBD — owner CF-' MIGRATION.md` — 0.
- `./scripts/check-api-surface.sh` — exits 0.
</verification>

<success_criteria>
The control-flow page is written, wired into the book and doc-test clean; `MIGRATION.md` has no
CF-owned TBD and carries the deliberate-zero note; `CHANGELOG.md` `[Unreleased]` records every
user-visible change including M-B-01; and semver-checks, MSRV 1.88, `make security`, clippy, fmt,
the 82% coverage floor and the API-surface check are all green on the phase's final commit with
their commands, SHAs and verbatim results recorded.
</success_criteria>

<output>
Create `.planning/phases/23-control-flow-dynamic-routing-fan-out-subgraphs/23-12-SUMMARY.md` when done
</output>
