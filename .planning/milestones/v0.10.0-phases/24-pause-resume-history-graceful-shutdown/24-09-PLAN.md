---
phase: 24-pause-resume-history-graceful-shutdown
plan: 09
type: execute
wave: 8
depends_on: ["24-08"]
files_modified:
  - src/bin/paladin-server.rs
  - src/config/setup/service_runner.rs
  - k8s/server/deployment.yaml
  - k8s/deployment.yaml
  - k8s/README.md
  - docs/src/deployment/kubernetes.md
  - docs/src/deployment/production.md
  - MIGRATION.md
autonomous: true
requirements: [HITL-04]

must_haves:
  truths:
    - "Both process entry points wire the ShutdownCoordinator: paladin-server's shutdown_signal and ServiceRunner::wait_for_shutdown each cancel the coordinator on SIGTERM or SIGINT, then wait at most shutdown_grace before axum's with_graceful_shutdown completes or the runner exits (D-22)."
    - "With graceful_shutdown set to false the process skips the wait entirely and exits immediately — the documented M-B-02 disable switch for legacy-only deployments (D-20, D-22)."
    - "Every in-flight engine run the facade starts registers with the coordinator, so it observes cancellation at its mid-superstep grace point (D-22)."
    - "resume continues a Halted thread, asserted explicitly at the process-wiring level and not only inside the engine (HITL-FR-14)."
    - "k8s/server/deployment.yaml and k8s/deployment.yaml both declare terminationGracePeriodSeconds of 60, twice the 30 second default grace (D-23)."
    - "k8s/README.md, docs/src/deployment/kubernetes.md and docs/src/deployment/production.md document the 2x rule and both env switches; production.md's existing 30 second figure is reconciled with the new rule rather than left contradicting it (D-23)."
    - "MIGRATION.md section 9.1's M-B-02 line 70 TBD is replaced with a concrete before-and-after worked example, and section 9.5 lists both new EngineConfig fields with their env vars and defaults (D-23, D-29)."
    - "MIGRATION.md section 9.8's 'termination grace lands with HITL-04' bullet is made concrete with the value operators should set."
  artifacts:
    - "src/bin/paladin-server.rs (coordinator construction, shutdown_signal wiring)"
    - "src/config/setup/service_runner.rs (wait_for_shutdown wiring)"
    - "k8s/server/deployment.yaml and k8s/deployment.yaml (terminationGracePeriodSeconds)"
    - "MIGRATION.md (M-B-02 worked example, 9.5 config entries, 9.8 bullet)"
  key_links:
    - "SIGTERM/SIGINT -> shutdown_signal / wait_for_shutdown -> ShutdownCoordinator::cancel_and_wait(shutdown_grace) -> registered runs observe cancellation mid-superstep"
    - "EngineConfig.graceful_shutdown false -> the wait is skipped -> the process exits without lingering"
    - "terminationGracePeriodSeconds 60 -> at least 2x shutdown_grace 30 -> the kubelet does not SIGKILL mid-grace"
  prohibitions:
    - statement: "Settings is not modified; the shutdown configuration stays on the standalone EngineConfig under src/config/ (X-10 avoidance, Phase 22/23 precedent)."
      status: recalled
      verification: flagged-unverified
    - statement: "The k8s termination grace is documented as at least twice the configured shutdown grace; a manifest that sets it lower than the configured grace is a misconfiguration the docs must warn against."
      status: recalled
      verification: flagged-unverified
---

<objective>
Connect the coordinator to the outside world: SIGTERM and SIGINT now drain in-flight engine runs
inside the grace window, the disable switch is real, and every operator-facing surface — manifests,
deployment docs and the migration guide — tells the same story.

Purpose: HITL-04's process-wiring and operator-surface clauses (HITL-FR-15) and the `MIGRATION.md`
M-B-02 worked example this phase owes the program.

Output: wired entry points, `terminationGracePeriodSeconds` in both manifests, three updated
deployment docs, and three filled `MIGRATION.md` sections.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/24-pause-resume-history-graceful-shutdown/24-CONTEXT.md
@.planning/phases/24-pause-resume-history-graceful-shutdown/24-08-SUMMARY.md
@.project/v0.10.0/00-program-overview.md
@.project/v0.10.0/03-pause-resume-history-shutdown.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire the coordinator into both process entry points</name>
  <files>src/bin/paladin-server.rs, src/config/setup/service_runner.rs</files>
  <read_first>
    - src/bin/paladin-server.rs (read in full — shutdown_signal() and the axum::serve(...).with_graceful_shutdown call at ~line 132, and the state composition at ~lines 60-75 where the coordinator must be constructed)
    - src/config/setup/service_runner.rs (wait_for_shutdown at ~line 253 and the surrounding runner lifecycle)
    - crates/paladin-battalion/src/engine/shutdown.rs (ShutdownCoordinator::register/cancel_and_wait from plan 24-08)
    - src/config/engine.rs (shutdown_grace_secs and graceful_shutdown from plan 24-08 Task 3)
    - crates/paladin-battalion/src/engine/mod.rs (WarEngine::with_shutdown_grace)
  </read_first>
  <behavior>
    - Test 1 (`shutdown_signal_cancels_the_coordinator`): a simulated signal cancels the coordinator's root token, so a registered run's child token reports cancellation.
    - Test 2 (`process_waits_up_to_grace_for_in_flight_runs`): with a registered run that finishes inside the window, the wait returns as soon as it drains rather than at the deadline.
    - Test 3 (`process_stops_waiting_at_the_grace_deadline`): with a run that never drains, the wait returns after approximately the configured grace.
    - Test 4 (`graceful_shutdown_disabled_skips_the_wait`): with `graceful_shutdown` false, the wait is skipped and the entry point proceeds immediately even with a registered run in flight.
    - Test 5 (`service_runner_wait_for_shutdown_cancels_the_coordinator`): the same behaviour through `ServiceRunner::wait_for_shutdown`.
    - Test 6 (`resume_continues_a_halted_thread_after_process_shutdown`): a run halted by a coordinator-driven shutdown is resumed by a fresh engine and completes — the explicit HITL-FR-14 assertion at the wiring level.
  </behavior>
  <action>
Write the failing tests first (RED commit), then implement (GREEN commit).

Construct a single `ShutdownCoordinator` in `src/bin/paladin-server.rs` alongside the existing state
composition (~lines 60-75) and thread it to every component that starts an engine run. Today the
only such component is the background continuation the resume port spawns (plan 24-10), so the
coordinator is constructed here and handed forward; nothing else registers yet.

In `shutdown_signal()`, on SIGTERM or SIGINT, call `ShutdownCoordinator::cancel_and_wait` with the
`Duration` derived from `EngineConfig.shutdown_grace_secs` before allowing
`axum::serve(...).with_graceful_shutdown` (~line 132) to complete. When
`EngineConfig.graceful_shutdown` is false, cancel the root token but skip the wait entirely — that
is the M-B-02 disable switch for legacy-only deployments.

Apply the same pattern to `ServiceRunner::wait_for_shutdown` in `src/config/setup/service_runner.rs`
(~line 253): cancel the coordinator, then wait at most `shutdown_grace` (skipped when disabled)
before the runner exits.

Do not add fields to `Settings`. The configuration is read from the standalone `EngineConfig` that
plan 24-08 extended, following the Phase 22/23 precedent for every X-09 config struct.

Add Test 6 explicitly at this level even though the engine-level `Halted` resume behaviour is
already pinned in plan 24-01 — D-22 requires the process-wiring path to be tested, not only the
engine path.
  </action>
  <verify>
    <automated>cargo test shutdown_signal_cancels_the_coordinator &amp;&amp; cargo test graceful_shutdown_disabled_skips_the_wait &amp;&amp; cargo test resume_continues_a_halted_thread_after_process_shutdown</automated>
  </verify>
  <acceptance_criteria>
    - `src/bin/paladin-server.rs` constructs a `ShutdownCoordinator` and calls `cancel_and_wait` from its shutdown path
    - `src/config/setup/service_runner.rs` calls `cancel_and_wait` from `wait_for_shutdown`
    - `cargo test shutdown_signal_cancels_the_coordinator` exits 0
    - `cargo test process_waits_up_to_grace_for_in_flight_runs` exits 0
    - `cargo test process_stops_waiting_at_the_grace_deadline` exits 0
    - `cargo test graceful_shutdown_disabled_skips_the_wait` exits 0
    - `cargo test service_runner_wait_for_shutdown_cancels_the_coordinator` exits 0
    - `cargo test resume_continues_a_halted_thread_after_process_shutdown` exits 0
    - `git diff` for this task shows no change to `src/config/settings.rs`
    - `cargo clippy --workspace -- -D warnings` exits 0
  </acceptance_criteria>
  <done>Both entry points drain in-flight runs within the configured grace, the disable switch short-circuits the wait, and a shutdown-halted thread is provably resumable.</done>
</task>

<task type="auto">
  <name>Task 2: Operator surface — k8s manifests and deployment docs</name>
  <files>k8s/server/deployment.yaml, k8s/deployment.yaml, k8s/README.md, docs/src/deployment/kubernetes.md, docs/src/deployment/production.md</files>
  <read_first>
    - k8s/server/deployment.yaml (the real paladin-server Deployment; note it declares no termination grace today)
    - k8s/deployment.yaml (the second Deployment manifest)
    - k8s/README.md (the operator-facing manifest guide)
    - docs/src/deployment/kubernetes.md (the mdBook Kubernetes page)
    - docs/src/deployment/production.md (specifically the passage around line 351 that currently shows a 30 second termination grace — the figure D-23 reconciles)
    - src/config/engine.rs (the two env-var names and defaults as landed in plan 24-08)
  </read_first>
  <action>
Add `terminationGracePeriodSeconds: 60` to the pod spec of both `k8s/server/deployment.yaml` and
`k8s/deployment.yaml` (D-23). Sixty is twice the 30 second default `shutdown_grace`; state that
derivation in a YAML comment beside the field so a later reader can recompute it rather than
copying a magic number.

Update `k8s/README.md`, `docs/src/deployment/kubernetes.md` and `docs/src/deployment/production.md`
with: the rule that `terminationGracePeriodSeconds` should be at least twice the configured
`shutdown_grace`, so the kubelet does not send SIGKILL while the process is still inside its grace
window; the two env vars `APP_ENGINE_SHUTDOWN_GRACE_SECS` (default 30) and
`APP_ENGINE_GRACEFUL_SHUTDOWN` (default true, set false to exit without waiting); and what an
operator observes on SIGTERM — in-flight supersteps finish or their stragglers are recorded
`Skipped` and re-listed for resume.

`docs/src/deployment/production.md` currently shows a 30 second termination grace around line 351.
Reconcile it: change the shown value to 60 and add the 2x derivation, so the page does not
contradict the manifests.

Do not renumber, reformat or reorder unrelated sections of these files; make scoped edits only.
  </action>
  <verify>
    <automated>grep -c 'terminationGracePeriodSeconds: 60' k8s/server/deployment.yaml k8s/deployment.yaml &amp;&amp; grep -c 'APP_ENGINE_SHUTDOWN_GRACE_SECS' k8s/README.md docs/src/deployment/kubernetes.md docs/src/deployment/production.md &amp;&amp; mdbook build docs 2>/dev/null || cargo doc --no-deps --workspace</automated>
  </verify>
  <acceptance_criteria>
    - `k8s/server/deployment.yaml` contains `terminationGracePeriodSeconds: 60`
    - `k8s/deployment.yaml` contains `terminationGracePeriodSeconds: 60`
    - `k8s/README.md` contains `APP_ENGINE_SHUTDOWN_GRACE_SECS` and `APP_ENGINE_GRACEFUL_SHUTDOWN`
    - `docs/src/deployment/kubernetes.md` contains `APP_ENGINE_SHUTDOWN_GRACE_SECS` and `terminationGracePeriodSeconds`
    - `docs/src/deployment/production.md` contains `terminationGracePeriodSeconds: 60` and no longer presents 30 as the recommended termination grace
    - All three docs state the rule that termination grace should be at least twice the configured shutdown grace
    - `kubectl apply --dry-run=client -f k8s/server/deployment.yaml` succeeds if a cluster context is available; otherwise the YAML parses under any available YAML linter
  </acceptance_criteria>
  <done>Both manifests declare a termination grace derived from the engine default, and all three operator docs teach the same 2x rule and the same two env vars.</done>
</task>

<task type="auto">
  <name>Task 3: MIGRATION.md M-B-02 worked example, section 9.5 and section 9.8</name>
  <files>MIGRATION.md</files>
  <read_first>
    - MIGRATION.md (read in full — the M-B-02 row at line 16, its "TBD — owner HITL-04, Phase 24" placeholder at line 70, the section 9.5 configuration bullet list at lines 114-136, and the section 9.8 upgrade-checklist bullet at line 147 mentioning that termination grace lands with HITL-04)
    - src/config/engine.rs (the exact field names, env-var names and defaults to document)
    - k8s/server/deployment.yaml (the after-state the example describes)
    - .planning/phases/24-pause-resume-history-graceful-shutdown/24-CONTEXT.md (D-23's specified before/after content)
  </read_first>
  <action>
Replace `MIGRATION.md` line 70's `M-B-02: TBD — owner HITL-04, Phase 24` placeholder with the
concrete worked example D-23 specifies. Before: the process exits on SIGTERM without waiting for
in-flight engine runs, and `terminationGracePeriodSeconds` is unset in the shipped manifests. After:
the process waits up to `shutdown_grace` for in-flight runs to halt, the manifests declare
`terminationGracePeriodSeconds: 60`, `APP_ENGINE_SHUTDOWN_GRACE_SECS` defaults to 30, and
`APP_ENGINE_GRACEFUL_SHUTDOWN` set to false restores the old no-wait behaviour for legacy-only
deployments. Show both states concretely enough that an operator can diff their own manifest against
it. Update the M-B-02 row at line 16 so its "A documented disable switch will be provided" future
tense becomes the shipped present tense naming the env var.

Add both new `EngineConfig` fields to section 9.5's configuration bullet list, each with its field
name, env var, default and one-line operator meaning, following the formatting of the bullets
already there (lines 121-126). Note explicitly that both default to today's behaviour so a v0.9
configuration boots v0.10 unchanged, which is the X-09 claim section 9.5 opens with.

Make section 9.8's "termination grace lands with HITL-04, Phase 24" bullet concrete: state the value
to set and the 2x derivation. Leave the rest of section 9.8 as the SHIP-01/Phase 29 `TBD` it is —
finalising the checklist is not this phase's scope.

Section 9.2's type register and section 9.6's endpoint list are filled in plan 24-12; do not touch
them here.
  </action>
  <verify>
    <automated>grep -c 'APP_ENGINE_SHUTDOWN_GRACE_SECS' MIGRATION.md &amp;&amp; grep -c 'terminationGracePeriodSeconds' MIGRATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `MIGRATION.md` contains `APP_ENGINE_SHUTDOWN_GRACE_SECS` and `APP_ENGINE_GRACEFUL_SHUTDOWN`
    - `MIGRATION.md` contains `terminationGracePeriodSeconds: 60` inside the M-B-02 worked example
    - `MIGRATION.md`'s M-B-02 example entry no longer reads as an unowned placeholder for Phase 24
    - `MIGRATION.md` section 9.5 lists `shutdown_grace_secs` and `graceful_shutdown` with their defaults
    - `MIGRATION.md` section 9.8's termination-grace bullet names a concrete value and the 2x derivation
    - `MIGRATION.md` sections 9.2 and 9.6 are unchanged by this task
    - `cargo test --workspace` exits 0 and `cargo fmt --check` exits 0
  </acceptance_criteria>
  <done>The program's migration guide carries a copy-pasteable M-B-02 before-and-after, both new config knobs, and a concrete upgrade-checklist value.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| container orchestrator -> process signals | The kubelet's SIGKILL deadline bounds how long the process may drain |
| operator env -> process lifetime | Two env vars change whether and how long the process waits before exiting |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-35 | Denial of Service | A pod SIGKILLed mid-grace, losing the Halted Waypoint write | high | mitigate | Both manifests declare `terminationGracePeriodSeconds: 60`, twice the default grace, and all three operator docs teach the 2x rule so a tuned grace is matched by a tuned termination period |
| T-24-36 | Denial of Service | A hung run holding the process open indefinitely on shutdown | medium | mitigate | The wait is bounded by `cancel_and_wait(grace)`, which returns at the deadline; `graceful_shutdown` false removes the wait entirely |
| T-24-37 | Repudiation | An operator unable to tell whether a restart lost work | medium | mitigate | The M-B-02 worked example states what an operator observes on SIGTERM, and over-grace nodes are recorded `Skipped` and re-listed on the Halted Waypoint rather than vanishing |
| T-24-38 | Information Disclosure | Deployment docs or manifests carrying a credential | medium | mitigate | Only env-var names and numeric defaults are documented; no secret value is added to any manifest, and the existing `secret.yaml.example` pattern is untouched |
| T-24-SC | Tampering | package-manager installs | low | accept | No new package is installed by this plan |
</threat_model>

<verification>
- `cargo test --workspace` green
- `cargo fmt --check` and `cargo clippy --workspace -- -D warnings` green
- Both k8s manifests parse and declare the termination grace
- `MIGRATION.md` carries no remaining Phase 24 / HITL-04 placeholder in sections 9.1, 9.5 or 9.8
</verification>

<success_criteria>
SIGTERM drains in-flight runs within the configured grace on both entry points, the disable switch
works, the manifests and all three deployment docs teach the same 2x rule, and `MIGRATION.md`'s
M-B-02 debt to the program is paid.
</success_criteria>

## Artifacts this phase produces

New surfaces created by this plan (excluded from drift verification): the coordinator wiring in
`src/bin/paladin-server.rs` and `src/config/setup/service_runner.rs`, `terminationGracePeriodSeconds`
in both k8s Deployments, and the `MIGRATION.md` M-B-02 worked example. The full phase-wide artifact
list is in `24-01-PLAN.md`.

<output>
Create `.planning/phases/24-pause-resume-history-graceful-shutdown/24-09-SUMMARY.md` when done
</output>
