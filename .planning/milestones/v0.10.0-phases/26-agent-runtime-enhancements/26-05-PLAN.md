---
phase: 26-agent-runtime-enhancements
plan: 05
type: execute
wave: 3
depends_on: ["26-01", "26-02"]
files_modified:
  - crates/paladin-core/src/platform/container/execution_result.rs
  - crates/paladin-core/Cargo.toml
  - src/application/cli/formatters/output.rs
  - crates/paladin-web/src/agent_controller.rs
  - src/application/services/paladin/middleware/limits.rs
  - src/application/services/paladin/middleware/mod.rs
  - .cargo/semver-checks-allowlist.toml
  - MIGRATION.md
autonomous: true
requirements: [RT-02]

must_haves:
  truths:
    - "StopReason gains CallLimit and TokenBudget in ONE change, is marked non-exhaustive, every in-tree exhaustive match gains a _ arm, the doc comment lists the variants added in this release, and the MIGRATION 9.2 row is resolved Y (enum_marked_non_exhaustive) with the allowlist entry and the crates/paladin-core/Cargo.toml lint suppression in the same commit (D-07, D-37, X-10.1, X-10.2, X-10.5)."
    - "The X-10.2 exception is deliberately NOT taken even though overview section 3 names StopReason as its example: nothing in the tree or the examples relies on exhaustive matching, and one mechanism across four enums beats a bespoke exception -- the reasoning is written into the enum's rustdoc (D-07)."
    - "is_successful() returns true for BOTH new variants (the run ended with the model's last answer intact) and is_limit() also returns true for both; MaxLoops and Timeout keep today's answers and the asymmetry is stated on the enum (D-08, X-03)."
    - "ModelCallLimit counts the reasoning loop's model calls -- one per before_model, post-retry -- and explicitly does NOT count the service's buffered retry attempts or a summarizer's own call; the exclusion is documented (D-08)."
    - "EDGE(RT-02/adjacency): the limit fires at exactly calls >= max_calls -- a run with max_calls: 3 makes exactly 3 model calls, not 2 and not 4 -- and TokenBudget keeps the response that crossed the budget, so the documented overshoot is at most one response (D-08)."
    - "TokenBudget accumulates response.usage.total_tokens in after_model and finishes with the crossing response kept, a truncation notice appended and StopReason::TokenBudget (D-08)."
    - "ToolCallLimit { max_calls, per_tool } denies through ToolFlow::Deny with a fixed model-facing message naming the tool, never fails the run, and applies to handoff calls as well as Armament calls (D-08, D-04, RT-FR-05)."
    - "EDGE(RT-02/idempotency): every limit counter is per-run scratch on ModelCallContext, so two sequential runs through one service instance each start from zero and a second identical run behaves identically to the first (D-03, D-08)."
    - "EDGE(RT-02/concurrency): ten concurrent runs through one service carrying one ModelCallLimit { max_calls: 3 } each make exactly 3 model calls with StopReason::CallLimit and exactly 30 port calls in total, under a multi-thread flavor with a 10 s guard (D-03, D-39, RT-FR-02)."
    - "Each built-in's constructor takes its own AgentRuntimeConfig sub-struct (ModelCallLimitConfig, TokenBudgetConfig, ToolCallLimitConfig) rather than loose scalars (D-10)."
  artifacts:
    - "crates/paladin-core/src/platform/container/execution_result.rs (StopReason::CallLimit, StopReason::TokenBudget, non-exhaustive)"
    - src/application/services/paladin/middleware/limits.rs
    - ".cargo/semver-checks-allowlist.toml (the StopReason entry)"
    - "MIGRATION.md section 9.2 (the StopReason row resolved Y)"
  key_links:
    - "ModelCallContext.loop_index / cumulative_tokens (per-run scratch) -> ModelCallLimit / TokenBudget -> MiddlewareFlow::Finish(FinalResult { output, stop_reason }) -> PaladinResult.stop_reason"
    - "ToolCallContext.call.name -> ToolCallLimit per-tool counter -> ToolFlow::Deny { reason } -> the accumulated output where a tool error is injected today"
    - "MIGRATION.md 9.2 StopReason row (Y) <-> allowlist entry <-> crates/paladin-core/Cargo.toml lint suppression"
  prohibitions:
    - statement: "A limit breach never fails the run: ModelCallLimit and TokenBudget Finish, ToolCallLimit Denies, and none of the three returns MiddlewareFlow::Fail."
      status: recalled
      verification: flagged-unverified
    - statement: "is_successful() and is_limit() answers for MaxLoops, Timeout, Completed and StopWord are not changed by this plan."
      status: recalled
      verification: flagged-unverified
    - statement: "A StopReason::Guardrail variant is not added here -- it is a Deferred Idea, free under the new attribute but not an RT FR."
      status: recalled
      verification: flagged-unverified
    - statement: "No limit counter is stored on the middleware struct; every counter lives in per-run context state."
      status: recalled
      verification: flagged-unverified
---

<objective>
Add the two `StopReason` variants RT-FR-04 and RT-FR-06 need, apply the X-10 treatment to the enum
in one change, and ship the three limit middlewares — `ModelCallLimit`, `TokenBudget` and
`ToolCallLimit` — each constructed from its own `AgentRuntimeConfig` sub-struct and each proving that
a breached budget ends a run gracefully rather than failing it.

Purpose: these are the first real consumers of the chain from plan 26-01 and the first proof that
D-03's "state on the context, not on the middleware" gives per-run isolation for free. They are also
where the phase's second X-10 register row is discharged.

Output: `StopReason::CallLimit` and `StopReason::TokenBudget` plus the non-exhaustive attribute and
`_` arms in three first-party matchers; `middleware/limits.rs`; the resolved §9.2 row, allowlist
entry and per-crate lint suppression.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-agent-runtime-enhancements/26-CONTEXT.md
@.planning/phases/26-agent-runtime-enhancements/26-RESEARCH.md
@.planning/phases/26-agent-runtime-enhancements/26-PATTERNS.md
@.project/v0.10.0/05-agent-runtime-enhancements.md
@.project/v0.10.0/00-program-overview.md
@MIGRATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: StopReason gains CallLimit and TokenBudget under the full X-10 treatment</name>
  <reversibility rating="costly">D-07: marking a published enum non-exhaustive is a one-release decision — the `_` arms it forces on downstream matchers stay forever, and removing the attribute later is itself breaking.</reversibility>
  <files>crates/paladin-core/src/platform/container/execution_result.rs, crates/paladin-core/Cargo.toml, src/application/cli/formatters/output.rs, crates/paladin-web/src/agent_controller.rs, .cargo/semver-checks-allowlist.toml, MIGRATION.md</files>
  <read_first>
    - crates/paladin-core/src/platform/container/execution_result.rs — `StopReason` :102-111 (the four current variants), `is_successful` :118, `is_limit` :123, and the `PaladinResult` doc comment at lines 38-46 explaining why `PaladinResult` itself stays exhaustive and constructible; do not conflate the two enums' semver treatment
    - src/application/cli/formatters/output.rs:276-292 and :495-498 — the two exhaustive matches that need a `_` arm
    - crates/paladin-web/src/agent_controller.rs:151-154 — `stop_reason_label`'s exhaustive match, plus its tests at :1640-1657 which must keep passing and should gain coverage for the two new labels
    - .cargo/semver-checks-allowlist.toml — entry schema and the set-equality rule; note whether plan 26-03 has already added the `paladin-ports` entry so this one is appended rather than replacing it
    - MIGRATION.md §9.2 — the `StopReason` row at line 130 currently TBD, and the row format used by Phase 23/24/25
    - crates/paladin-core/Cargo.toml — the `[package.metadata.cargo_semver_checks.lints]` section (add it if absent, matching how plan 26-03 wrote the `paladin-ports` one)
    - .project/v0.10.0/00-program-overview.md §3 X-10.2 — the paragraph that names `StopReason` as the *example* of a legitimate exception, so the rustdoc can state accurately why the exception is declined here
  </read_first>
  <behavior>
    - Test 1 (`new_stop_reasons_are_successful_and_limited`): `StopReason::CallLimit.is_successful()` and `StopReason::TokenBudget.is_successful()` are both `true`; `is_limit()` is `true` for both.
    - Test 2 (`existing_stop_reason_answers_are_unchanged`): `MaxLoops`, `Timeout`, `Completed` and `StopWord("x")` return exactly the `is_successful()` / `is_limit()` answers they return today — assert all eight booleans explicitly.
    - Test 3 (`stop_reason_requires_a_wildcard_arm`): a test-local `match` over `StopReason` without a `_` arm does not compile — expressed as a compile-fail assertion in prose plus a positive test that a `_`-arm match handles a `CallLimit` value.
    - Test 4 (`cli_formatter_labels_the_new_variants`): the CLI output formatter produces a distinct, non-empty rendering for `CallLimit` and for `TokenBudget` rather than falling into a generic wildcard string.
    - Test 5 (`agent_controller_labels_the_new_variants`): `stop_reason_label(&StopReason::CallLimit)` and `stop_reason_label(&StopReason::TokenBudget)` return stable snake-case labels (`call_limit`, `token_budget`), asserted alongside the existing four.
    - Test 6 (`stop_reason_round_trips_through_serde_with_the_new_variants`): both new variants serialize and deserialize equal.
  </behavior>
  <action>
Write the failing tests first, commit RED, then implement and commit GREEN.

**`crates/paladin-core/src/platform/container/execution_result.rs`.** Add `CallLimit` and
`TokenBudget` to `StopReason` and mark the enum `#[non_exhaustive]` in the same edit. Extend
`is_successful()` (:118) so both new variants return `true` — PRD RT-FR-04's "true-with-warning": the
run ended with the model's last answer intact — and extend `is_limit()` (:123) so both return `true`.
Do not change any existing variant's answer (X-03). Add to the enum's rustdoc: (a) the list of
variants added in this release, which X-10.2 requires; (b) one paragraph stating that the X-10.2
exception is declined — overview §3 names this very enum as the example where exhaustive matching
might be legitimate, but nothing in the tree or in `examples/` relies on it, and one mechanism across
`PaladinError`, `LlmError`, `BattalionError` and `StopReason` beats a bespoke exception (D-07); and
(c) an explicit note on the `is_successful()` asymmetry — `MaxLoops` and `Timeout` are not
successful, `CallLimit` and `TokenBudget` are, because the former two mean the answer may be
incomplete and the latter two mean a budget stopped a run whose last answer stands (D-08).

**`src/application/cli/formatters/output.rs`.** Add explicit arms for both new variants at :276-292
and :495-498 with renderings in the same register as the neighbours, plus a `_` arm for future
variants. A wildcard alone is not acceptable here: the point of the two new variants is that a user
can tell *why* their run stopped.

**`crates/paladin-web/src/agent_controller.rs`.** Add `call_limit` and `token_budget` arms to
`stop_reason_label` (:151-154) plus a `_` arm, and extend the existing label tests at :1651-1657.

**`crates/paladin-core/Cargo.toml`.** Add the `[package.metadata.cargo_semver_checks.lints]`
suppression for `enum_marked_non_exhaustive`, matching the shape plan 26-03 used for
`paladin-ports`.

**`.cargo/semver-checks-allowlist.toml`.** Append one `[[entry]]`: `crate = "paladin-ai-core"` (the
published package name, per the Plan 25-14 correction — the directory is `paladin-core`),
`lint = "enum_marked_non_exhaustive"`, `migration_row = "paladin-ai-core | StopReason"`,
`requirement_id = "RT-02"`, justification naming the two variants and the declined X-10.2 exception.

**`MIGRATION.md` §9.2.** Resolve the `StopReason` row at line 130: Mitigation records the
non-exhaustive attribute plus the release-note variant list; Deliberate-breaking `Y` with the same
justification; Requirement `RT-FR-04, RT-FR-06`. Touch no other row.
  </action>
  <verify>
    <automated>cargo check --workspace --all-targets --all-features &amp;&amp; cargo test -p paladin-ai-core --lib new_stop_reasons_are_successful_and_limited &amp;&amp; cargo test -p paladin-ai-core --lib existing_stop_reason_answers_are_unchanged &amp;&amp; cargo test -p paladin-web --lib agent_controller &amp;&amp; cargo test -p paladin-ai --lib formatters</automated>
  </verify>
  <acceptance_criteria>
    - `crates/paladin-core/src/platform/container/execution_result.rs` contains `CallLimit` and `TokenBudget`
    - The enum carries the attribute: `grep -B4 'pub enum StopReason' crates/paladin-core/src/platform/container/execution_result.rs | grep -q non_exhaustive`
    - Both new variants are labelled explicitly rather than swallowed by a wildcard: `grep -c 'StopReason::CallLimit' src/application/cli/formatters/output.rs` is at least `2` and `grep -c 'StopReason::CallLimit' crates/paladin-web/src/agent_controller.rs` is at least `1`
    - `.cargo/semver-checks-allowlist.toml` contains `enum_marked_non_exhaustive` and `paladin-ai-core`
    - The MIGRATION row is resolved: `grep 'StopReason' MIGRATION.md | grep -c TBD` is `0`
    - `crates/paladin-core/Cargo.toml` contains `cargo_semver_checks`
    - `cargo test -p paladin-ai-core --lib existing_stop_reason_answers_are_unchanged` exits 0 and asserts all eight pre-existing booleans
    - `cargo test -p paladin-web --lib agent_controller` exits 0 with the two new label assertions present
    - `cargo check --workspace --all-targets --all-features` exits 0 — no matcher left without a `_` arm
    - `cargo test -p paladin-ai-core --doc` exits 0
  </acceptance_criteria>
  <done>`StopReason` carries the two new variants and the non-exhaustive attribute, all three first-party matchers render them explicitly, existing variants' `is_successful`/`is_limit` answers are unchanged, and the §9.2 row, allowlist entry and lint suppression agree — in one commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ModelCallLimit and TokenBudget finish gracefully at exactly the budget</name>
  <files>src/application/services/paladin/middleware/limits.rs, src/application/services/paladin/middleware/mod.rs</files>
  <read_first>
    - src/application/services/paladin/middleware/mod.rs and context.rs and chain.rs (the trait, contexts and driver from plan 26-01 — in particular `ModelCallContext.loop_index`, `cumulative_tokens`, `scratch` and the typed per-run bag, and `FinalResult`)
    - src/application/services/paladin/paladin_execution_service.rs — the loop at :840-1022, especially the model call at :862-871 and the existing `total_tokens` running sum, and the `MaxLoops` return at :987-1022 (the shape a `Finish` must produce an equivalent of)
    - src/config/agent_runtime.rs — `ModelCallLimitConfig` and `TokenBudgetConfig` from plan 26-02 (the constructor arguments)
    - crates/paladin-ports/src/output/llm_port.rs — `LlmResponse` :705 and its `usage` / `total_tokens` shape, so `TokenBudget` reads the right field
    - crates/paladin-llm/src/mock.rs — `MockLlmAdapter` :79, `with_responses` :92, `call_count` :196 (the scripted mock these tests drive)
    - .planning/phases/26-agent-runtime-enhancements/26-CONTEXT.md `<specifics>` — the isolation test and the `TokenBudget { max_tokens: 250 }` / 100-tokens-per-call arithmetic verbatim
    - src/application/services/orchestration/listener.rs — the X-05 multi-thread stress idiom with exact counts and a timeout guard
  </read_first>
  <behavior>
    - Test 1 (`model_call_limit_finishes_at_exactly_max_calls`): `ModelCallLimit` from a config with `max_calls: 3` over a mock scripted to keep producing non-final answers yields exactly 3 `LlmPort` calls, `StopReason::CallLimit`, and an output equal to the accumulated output plus the truncation notice.
    - Test 2 (`model_call_limit_does_not_count_buffered_retries`): a mock that errors transiently twice before succeeding on the first loop iteration still consumes exactly one of the three budgeted calls — the service's own retry attempts are invisible to the limit.
    - Test 3 (`token_budget_keeps_the_crossing_response_and_finishes`): `TokenBudget { max_tokens: 250 }` with a mock reporting 100 tokens per call yields exactly 3 calls (the third crosses), the run's `token_count` is 300, `StopReason::TokenBudget`, and `is_successful()` is true.
    - Test 4 (`token_budget_overshoot_is_at_most_one_response`): with a mock reporting 400 tokens on its first call against `max_tokens: 250`, the run makes exactly 1 call and finishes with `TokenBudget`.
    - Test 5 (`limits_never_fail_the_run`): neither middleware ever returns `MiddlewareFlow::Fail` — assert the returned `Result` is `Ok` and the `PaladinResult` is present in both Test 1 and Test 3 scenarios.
    - Test 6 (`disabled_limit_config_installs_nothing_and_changes_nothing`): with `enabled: false`, the run's port call count and `PaladinResult` are identical to a run with no middleware at all.
    - Test 7 (`sequential_runs_reset_the_counters`): two sequential runs through one service instance carrying one `ModelCallLimit { max_calls: 3 }` each make exactly 3 calls — 6 total, not 3.
    - Test 8 (`concurrent_runs_keep_independent_limit_counters`): ten concurrent runs through one service, `#[tokio::test(flavor = "multi_thread")]` under a 10 s timeout guard, each make exactly 3 model calls with `StopReason::CallLimit` and 30 port calls in total.
  </behavior>
  <action>
Write the failing tests first, commit RED, then implement and commit GREEN.

Create `src/application/services/paladin/middleware/limits.rs` and register it in `middleware/mod.rs`
with its public re-exports.

`pub struct ModelCallLimit` constructed as `ModelCallLimit::new(ModelCallLimitConfig)` (D-10). Its
`before_model` reads the per-run call count from `ModelCallContext`'s typed state (D-03 — never a
field on the struct), increments it, and returns `MiddlewareFlow::Finish(FinalResult { output,
stop_reason: StopReason::CallLimit })` when the count would exceed `max_calls`, with the accumulated
output plus an appended truncation notice as the output. The exact notice wording is at your
discretion; write it once as a private `const` so the test and the implementation cannot drift.
Rustdoc states precisely what is counted: one per `before_model`, post-retry — the service's
buffered retries inside `execute_with_retry_and_temperature` and a summarizer's own model call are
**not** counted (D-08).

`pub struct TokenBudget` constructed as `TokenBudget::new(TokenBudgetConfig)`. Its `after_model`
adds `resp.usage.total_tokens` to the per-run total and, once the total crosses `max_tokens`,
returns `Finish` with the crossing response's content kept, the notice appended and
`StopReason::TokenBudget`. Rustdoc states the documented consequence: the overshoot is at most one
response, because the budget is checked after the response that crossed it has already arrived
(D-08).

Both are stateless `Arc<dyn>` values (D-03): the only mutable state is on the context. Say so in the
module doc, and say why — RT-FR-02's isolation requirement is satisfied by construction, with no
factory trait, which is what Test 8 proves.

For Test 8, follow `listener.rs`'s X-05 pattern exactly: `flavor = "multi_thread"`, a
`tokio::time::timeout` guard, exact-count assertions, and one shared `Arc` of the middleware across
all ten runs so the test would fail if isolation were achieved by cloning instead.
  </action>
  <verify>
    <automated>cargo test -p paladin-ai --lib model_call_limit_finishes_at_exactly_max_calls &amp;&amp; cargo test -p paladin-ai --lib model_call_limit_does_not_count_buffered_retries &amp;&amp; cargo test -p paladin-ai --lib token_budget_keeps_the_crossing_response_and_finishes &amp;&amp; cargo test -p paladin-ai --lib concurrent_runs_keep_independent_limit_counters &amp;&amp; cargo test -p paladin-ai --lib sequential_runs_reset_the_counters</automated>
  </verify>
  <acceptance_criteria>
    - `src/application/services/paladin/middleware/limits.rs` exists and contains `pub struct ModelCallLimit` and `pub struct TokenBudget`
    - Both take their config sub-struct: `grep -c 'ModelCallLimitConfig\|TokenBudgetConfig' src/application/services/paladin/middleware/limits.rs` is at least `2`
    - Neither keeps a counter as a struct field: `grep -A6 'pub struct ModelCallLimit' src/application/services/paladin/middleware/limits.rs | grep -c 'AtomicU32\|Mutex\|Cell'` is `0`
    - `cargo test -p paladin-ai --lib model_call_limit_finishes_at_exactly_max_calls` exits 0 and asserts exactly 3 port calls
    - `cargo test -p paladin-ai --lib model_call_limit_does_not_count_buffered_retries` exits 0
    - `cargo test -p paladin-ai --lib token_budget_keeps_the_crossing_response_and_finishes` exits 0 and asserts `token_count == 300`
    - `cargo test -p paladin-ai --lib token_budget_overshoot_is_at_most_one_response` exits 0
    - `cargo test -p paladin-ai --lib concurrent_runs_keep_independent_limit_counters` exits 0 and the test carries `flavor = "multi_thread"` and a timeout guard
    - `cargo test -p paladin-ai --lib disabled_limit_config_installs_nothing_and_changes_nothing` exits 0
    - `cargo test -p paladin-ai --doc` exits 0
  </acceptance_criteria>
  <done>Both budget middlewares finish a run gracefully at exactly their configured boundary with the right `StopReason`, never fail the run, reset between sequential runs, and hold independent counts across ten concurrent runs through one shared instance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ToolCallLimit denies a call without failing the run</name>
  <files>src/application/services/paladin/middleware/limits.rs, src/application/services/paladin/middleware/mod.rs</files>
  <read_first>
    - src/application/services/paladin/middleware/limits.rs (the two middlewares from Task 2 — same file, same state idiom)
    - src/application/services/paladin/middleware/context.rs — `ToolCallContext { call, kind, loop_index, .. }` and its scratch access
    - src/application/services/paladin/paladin_execution_service.rs — the Arsenal branch :922-968 with the tool-error arm :955-967 (where a `Deny` reason lands) and the handoff branch :891-921 (which `around_tool` also wraps, D-04)
    - src/config/agent_runtime.rs — `ToolCallLimitConfig { enabled, max_calls, per_tool }` from plan 26-02
    - crates/paladin-core/src/platform/container/arsenal/core.rs — `ArmamentCall` :36, for the `name` a per-tool counter is keyed on
    - .planning/phases/26-agent-runtime-enhancements/26-CONTEXT.md `<specifics>` — `ToolCallLimit { max_calls: 2 }` with a scripted tool-calling mock, the third call denied, the model sees "tool budget exhausted", the run completes
  </read_first>
  <behavior>
    - Test 1 (`tool_call_limit_denies_the_call_past_the_budget`): with `max_calls: 2` and a scripted tool-calling mock, the first two tool invocations reach the Arsenal (call-count mock is 2) and the third is denied; the run reaches a normal completion rather than an error.
    - Test 2 (`denied_tool_call_reason_reaches_the_model`): the denial text names the tool and appears in the accumulated output at the position a tool error appears today, so the model sees it on the next iteration.
    - Test 3 (`per_tool_caps_are_independent_of_the_global_cap`): with `max_calls: 10` and `per_tool: {"search": 1}`, the second `search` call is denied while a `calculate` call still succeeds.
    - Test 4 (`tool_call_limit_applies_to_handoff_calls`): a handoff dispatch counts against the same budget and is denied past it, since a handoff is a tool call the model made (D-04).
    - Test 5 (`tool_call_limit_never_fails_the_run`): in every scenario above the run returns `Ok(PaladinResult)`; no scenario produces a `PaladinError`.
    - Test 6 (`per_tool_counters_are_per_run`): two sequential runs each get a fresh per-tool map; a tool exhausted in run 1 is available again in run 2.
  </behavior>
  <action>
Write the failing tests first, commit RED, then implement and commit GREEN.

Add `pub struct ToolCallLimit` to `middleware/limits.rs`, constructed as
`ToolCallLimit::new(ToolCallLimitConfig)` where the config carries `max_calls: u32` and
`per_tool: HashMap<String, u32>` (D-08). Implement `around_tool` only: read the per-run global count
and the per-run per-tool map from `ToolCallContext`'s scratch, increment for
`cx.call.name`, and return `ToolFlow::Deny { reason }` when either the global cap or that tool's cap
is exceeded. The reason is a **fixed** model-facing message naming the tool — write it once as a
private `const` formatter (`"tool budget exhausted for `{tool}`"`) so the test asserts the same
string the implementation produces. Never return `Fail`; never return `Rewrite`.

The middleware does not distinguish `ToolCallKind::Armament` from `ToolCallKind::Handoff` — both
count, per D-04's rule that a handoff is a tool call the model made. State that on the struct's
rustdoc so it reads as a decision rather than an oversight, and cover it with Test 4.

Add a short module-doc paragraph tying the three limits together: all three end a run or a call
*gracefully*; none of them is an error path; the config sub-structs are all inert by default so
installing the chain on a v0.9 deployment changes nothing (D-10).
  </action>
  <verify>
    <automated>cargo test -p paladin-ai --lib tool_call_limit_denies_the_call_past_the_budget &amp;&amp; cargo test -p paladin-ai --lib denied_tool_call_reason_reaches_the_model &amp;&amp; cargo test -p paladin-ai --lib per_tool_caps_are_independent_of_the_global_cap &amp;&amp; cargo test -p paladin-ai --lib tool_call_limit_applies_to_handoff_calls &amp;&amp; cargo test -p paladin-ai --lib per_tool_counters_are_per_run</automated>
  </verify>
  <acceptance_criteria>
    - `src/application/services/paladin/middleware/limits.rs` contains `pub struct ToolCallLimit`
    - The denial message is a single named constant, not a duplicated literal: `grep -c 'tool budget exhausted' src/application/services/paladin/middleware/limits.rs` is `1`
    - `ToolCallLimit` returns no failure path: `grep -A40 'impl ExecutionMiddleware for ToolCallLimit' src/application/services/paladin/middleware/limits.rs | grep -v '^\s*//' | grep -c 'MiddlewareFlow::Fail'` is `0`
    - `cargo test -p paladin-ai --lib tool_call_limit_denies_the_call_past_the_budget` exits 0 and asserts the Arsenal call count is exactly 2
    - `cargo test -p paladin-ai --lib per_tool_caps_are_independent_of_the_global_cap` exits 0
    - `cargo test -p paladin-ai --lib tool_call_limit_applies_to_handoff_calls` exits 0
    - `cargo test -p paladin-ai --lib tool_call_limit_never_fails_the_run` exits 0
    - `cargo test -p paladin-ai --lib per_tool_counters_are_per_run` exits 0
    - `cargo clippy --workspace --all-targets --all-features -- -D warnings` exits 0
    - `cargo fmt --check` exits 0
  </acceptance_criteria>
  <done>A tool budget breach denies the individual call with a fixed message the model sees, leaves the run healthy, applies per-tool and to handoffs alike, and resets between runs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator budget config → run termination | An operator-set number decides when a run stops producing work |
| denial message → model context | A framework-authored string is inserted into the prompt the model reads next |
| `StopReason` → downstream matchers | A published enum other crates match on |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-22 | Denial of Service | an unbounded reasoning loop or token spend | medium | mitigate | This plan is itself the mitigation: `ModelCallLimit` and `TokenBudget` bound calls and tokens per run and finish gracefully at exactly the boundary; `ToolCallLimit` bounds tool invocations globally and per tool. All three are inert by default (D-10) so they must be opted into. |
| T-26-23 | Tampering | the denial message read as an instruction by the model | low | mitigate | The message is a fixed framework constant naming only the tool, contains no model-controlled substring, and lands where a tool error already lands — it is data in the tool-result position, not a system instruction (D-08, D-41) |
| T-26-24 | Repudiation | a downstream matcher silently mis-classifying a new `StopReason` | medium | mitigate | Both new variants are matched **explicitly** in all three first-party matchers rather than swallowed by the `_` arm, so a CLI or API consumer can tell why a run stopped; the acceptance criteria grep for the explicit arms |
| T-26-18 | Repudiation | an allowlist entry with no matching register row | medium | mitigate | The `semver` CI job checks set-equality in both directions; this plan writes the §9.2 row, the allowlist entry and the per-crate suppression in one commit |
| T-26-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package; 26-RESEARCH.md's Package Legitimacy Audit records two `OK` packages, neither added here |
</threat_model>

<verification>
- `cargo check --workspace --all-targets --all-features` green — no exhaustive `StopReason` match left without a `_` arm
- `cargo test -p paladin-ai-core --lib` and `--doc` green
- `cargo test -p paladin-web --lib agent_controller` green
- `cargo test -p paladin-ai --lib` green including the multi-thread isolation test
- `cargo semver-checks` reports only the allowlisted findings
- `cargo fmt --check` and `cargo clippy --workspace --all-targets --all-features -- -D warnings` green
</verification>

<success_criteria>
`StopReason` carries `CallLimit` and `TokenBudget` under the full X-10 treatment with the register,
allowlist and lint suppression in agreement; all three limit middlewares end a run or a call
gracefully at exactly their configured boundary, never fail the run, are constructed from their own
config sub-structs, and hold independent per-run counters across ten concurrent runs through one
shared service instance.
</success_criteria>

## Artifacts this phase produces

New symbols and files created by Phase 26 (excluded from drift verification — none exist in the tree
before this phase):

- **Traits:** `ExecutionMiddleware`, `TokenCounterPort`, `VaultPort`, `StructuredExecutorPort`,
  `StructuredExecutorExt`, `StructuredSchema`, `ConformanceFixture`
- **Types:** `MiddlewareFlow`, `ToolFlow`, `FinalResult`, `ModelCallContext`, `ToolCallContext`,
  `ToolCallKind`, `LlmResponseView`, `PromptAssembly`, `PromptSection`, `SectionPlacement`,
  `ModelCallLimit`, `TokenBudget`, `ToolCallLimit`, `Guardrail`, `GuardrailRule`, `HistoryTrimmer`,
  `SummarizationMiddleware`, `VaultRecallMiddleware`, `ModelRetryMiddleware`,
  `ModelFallbackMiddleware`, `ToolCallProtocolMiddleware`, `FinishOnPlainAnswerMiddleware`,
  `AgentRuntimeConfig` and its twelve sub-structs, `ToolErrorMode`, `HeuristicTokenCounter`,
  `Namespace`, `VaultRecord`, `ScoredVaultRecord`, `Page`, `VaultError`, `InMemoryVault`,
  `SqliteVault`, `SemanticVault`, `ConfinedVault`, `RunScope`, `InProcessArsenal`,
  `CompositeArsenalPort`, `VaultTools`, `Structured<T>`, `StructuredOptions`, `SchemaRef`,
  `ShapeError`, `TypedSchema<T>`, `ResponseFormat`, `ReasoningAgent`, `ReasoningAgentOptions`
- **Macros:** `llm_conformance_suite!`
- **Functions:** `extract_json`, `shape_check`, `render_instruction_block`, `run_structured`,
  `reasoning_agent`, `redact_secret_patterns`, `AgentRuntimeConfig::build_chain`,
  `ModelFallbackConfig::resolve_chain`, `RetryPredicate::admits`, `GarrisonEntry::summary`,
  `LlmRequest::new` and its four `with_*` builders, `ToolResultFormatter::format_error`,
  `Namespace::new`/`parse`/`is_prefix_of`
- **Enum variants:** `StopReason::CallLimit`, `StopReason::TokenBudget`,
  `PaladinError::StructuredOutputInvalid`, `PaladinError::GuardrailTripped`,
  `PaladinError::ArmamentFailed`, and the new structured-output `EngineError` variants
- **Struct fields:** `GarrisonEntry.is_summary`, `LlmRequest.response_format`,
  `NodeSpec::Paladin.output_schema`, `NodeContext.vault`, `ModelCallContext.llm_override`,
  `ModelCallContext.retry_policy`
- **Methods on pre-existing types:** the six new `PaladinExecutionService::with_*` /
  `execute_scoped` methods, `PaladinPort::execute_scoped`, `WarEngine::with_vault` /
  `with_structured_executor` / `with_output_schema`, `NodeContext::vault`,
  `NodeSpec::with_output_schema`
- **Constants:** `GRAPH_FINGERPRINT_VERSION` value `v6`, `GUARDRAIL_PATTERN_SIZE_LIMIT_BYTES`,
  `DEFAULT_MAX_VALUE_BYTES`
- **Cargo:** `schemars = "1.2"` as a direct facade dependency (no new lockfile package, no new feature)
- **Env vars:** the `APP_AGENT_RUNTIME_<SECTION>_<FIELD>` family
- **Migrations:** `002_add_garrison_is_summary.sql`, `003_create_vault_tables.sql` (both in
  `crates/paladin-memory/migrations/`)
- **New files:** the `src/application/services/paladin/middleware/` module tree,
  `src/application/services/paladin/{structured,vault_confined}.rs`,
  `src/application/services/arsenal/{in_process_arsenal,composite_arsenal,vault_tools}.rs`,
  `src/config/agent_runtime.rs`, `src/presets/mod.rs`,
  `crates/paladin-core/src/platform/container/{vault,structured,run_scope}.rs`,
  `crates/paladin-ports/src/output/{vault_port,token_counter_port,structured_executor_port}.rs`,
  `crates/paladin-memory/src/vault/`, `crates/paladin-memory/src/token_counter/`,
  `crates/paladin-memory/src/migrations.rs`, `crates/paladin-llm/src/conformance.rs`,
  `crates/doc-examples/src/agent_runtime.rs`, `docs/src/user-guides/agent-runtime.md`, and the four
  new `tests/integration/` files
- **Removed:** the root `migrations/` copy and its two `Dockerfile` `COPY` lines (D-17)

<output>
Create `.planning/phases/26-agent-runtime-enhancements/26-05-SUMMARY.md` when done
</output>
