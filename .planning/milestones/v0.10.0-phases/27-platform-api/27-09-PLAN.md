---
phase: 27-platform-api
plan: 09
type: execute
wave: 4
depends_on: [27-02]
files_modified:
  - crates/paladin-core/src/platform/container/assistant.rs
  - crates/paladin-core/src/platform/container/mod.rs
  - crates/paladin-ports/src/output/assistant_repository_port.rs
  - crates/paladin-ports/src/output/mod.rs
  - crates/paladin-ports/src/output/run_repository_port.rs
  - crates/paladin-storage/src/assistant/mod.rs
  - crates/paladin-storage/src/assistant/contract_tests.rs
  - crates/paladin-storage/src/assistant/in_memory.rs
  - crates/paladin-storage/src/assistant/sqlite.rs
  - crates/paladin-storage/src/assistant/postgres.rs
  - crates/paladin-storage/src/lib.rs
  - crates/paladin-storage/src/run/in_memory.rs
  - crates/paladin-storage/src/run/sqlite.rs
  - crates/paladin-storage/src/run/postgres.rs
  - crates/paladin-storage/src/run/contract_tests.rs
  - crates/paladin-storage/migrations/sqlite/003_create_assistants_tables.sql
  - crates/paladin-storage/migrations/postgres/003_create_assistants_tables.sql
  - MIGRATION.md
autonomous: false
requirements: [PLAT-04]

must_haves:
  truths:
    - "`AssistantDefinition { kind: AssistantKind (Agent | Workflow), body: serde_json::Value }` is a core type — a tagged envelope over opaque JSON that ports, storage and web carry without depending on `paladin-battalion` (D-28)."
    - "`assistant_versions` has primary key `(assistant_id, version)`; the repository exposes `create`, `append_version`, `get`, `get_version`, `list`, `list_versions`, `soft_delete` and NO update method — immutability holds because the code to violate it does not exist (D-29)."
    - "Two concurrent `POST /assistants/{id}/versions` both computing n+1: the `(assistant_id, version)` primary key admits exactly one; the loser gets `409 conflict` and may retry against the new `latest`."
    - "`GET /assistants/{id}/versions` returns versions in ascending `version` order; the order is total because `version` is unique per assistant."
    - "Publishing an identical definition body twice creates two distinct versions (n+1, n+2) — there is no content-based deduplication, and the audit trail records both with their own timestamps and notes."
    - "A run submitted without `version` while a new version is being published resolves `latest` inside the run-insert transaction: the run's frozen `(assistant_id, version)` is either the pre-publish or the post-publish version, never a torn or later-mutated value."
    - "Every version records `created_at`, `created_by` and `note`; a soft-deleted assistant's versions stay readable by `get_version` so historical runs remain reconstructable (PLAT-FR-10)."
  artifacts:
    - path: "crates/paladin-core/src/platform/container/assistant.rs"
      provides: "Assistant, AssistantVersion, NewAssistantVersion, AssistantDefinition, AssistantKind, AssistantSource"
      contains: "pub enum AssistantKind"
    - path: "crates/paladin-ports/src/output/assistant_repository_port.rs"
      provides: "AssistantRepositoryPort (no update method)"
      contains: "async fn append_version"
    - path: "crates/paladin-storage/migrations/sqlite/003_create_assistants_tables.sql"
      provides: "assistants + assistant_versions tables"
      contains: "PRIMARY KEY (assistant_id, version)"
    - path: "crates/paladin-storage/src/assistant/contract_tests.rs"
      provides: "assistant contract suite incl. concurrent-append and freeze-at-submit"
      contains: "assistant_version_freeze_at_submit"
  key_links:
    - from: "crates/paladin-storage/src/run/sqlite.rs"
      to: "crates/paladin-storage/migrations/sqlite/003_create_assistants_tables.sql"
      via: "insert_with_latest = INSERT INTO runs … SELECT latest FROM assistants (single statement)"
      pattern: "SELECT .*latest.* FROM assistants"
    - from: "crates/paladin-storage/src/assistant/sqlite.rs"
      to: "crates/paladin-ports/src/output/assistant_repository_port.rs"
      via: "impl AssistantRepositoryPort; unique violation on the PK → VersionConflict"
      pattern: "is_unique_violation"
---

<objective>
Persist assistants as append-only immutable versions: core types, the repository port with no update
method, migrations, three adapters, a contract suite that proves the concurrency edges, and the
run-repository extension that freezes `latest` inside the run-insert statement.

Purpose: PLAT-FR-08 (immutability + freeze-at-submit), PLAT-FR-10 (audit trail) and the persisted
half of PLAT-04. D-28 and D-29 are one-way doors — both a persisted column and a published body — so
this plan opens with a decision checkpoint before the core type is written.

Output: `assistant.rs` (core), `assistant_repository_port.rs`, `003_create_assistants_tables.sql`
(both backends), `assistant/{mod,in_memory,sqlite,postgres,contract_tests}.rs`,
`RunRepositoryPort::insert_with_latest` on all three run adapters, and `MIGRATION.md` §9.3/§9.4 rows.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-platform-api/27-CONTEXT.md
@.planning/phases/27-platform-api/27-PATTERNS.md
@.planning/phases/27-platform-api/27-02-SUMMARY.md
@.project/v0.10.0/06-platform-api.md
@CLAUDE.md

<interfaces>
PRD 06 §2.3 shapes (binding): `Assistant { assistant_id: String /* slug, unique */, versions:
Vec<AssistantVersion> /* append-only */, latest: u32 }`; `AssistantVersion { version: u32 /* 1-based */,
definition: AssistantDefinition, created_at, created_by: Option<String>, note: Option<String> }`.
D-28 replaces the PRD's typed `Agent(PaladinConfigDoc) | Workflow(WarGraphDoc)` arms with
`AssistantDefinition { kind: AssistantKind, body: serde_json::Value }`.
From 27-02: `SqliteRunRepository`/`PostgresRunRepository` error mapping (`is_unique_violation()` →
`ThreadBusy`) — `insert_with_latest` must keep that mapping and add `UnknownAssistant`.
From `crates/paladin-core/src/platform/container/waypoint.rs:36-73`: the `ThreadId` newtype +
validation-error shape to copy for the assistant-id slug validation.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm the one-way assistant shape and immutability contract (D-28, D-29)</name>
  <decision>D-28 fixes `AssistantDefinition` as a tagged envelope `{ kind: Agent | Workflow, body:
  opaque JSON }` persisted as two columns and echoed verbatim over HTTP; D-29 fixes immutability as a
  schema primary key `(assistant_id, version)` plus the absence of any update method or `PUT` route.</decision>
  <context>Both are rated **one-way** in 27-CONTEXT.md and were auto-selected. `AssistantDefinition`
  is simultaneously a persisted column pair (`kind`, `body`) and the request/response body of
  `POST /assistants`, `POST /assistants/{id}/versions` and every `GET` — changing it after v0.10.0
  means a data migration for every stored version and a wire break for generated SDK clients.
  "No PUT, ever" is an explicit promise clients will build on; adding an update path later would be a
  semantic break even if additive in code. Every later plan in PLAT-04 (validation, resolution,
  controller, worker execution) reads this shape.</context>
  <options>
    <option id="option-a">
      <name>Proceed as decided (D-28 + D-29 verbatim)</name>
      <pros>Keeps `paladin-core`/`paladin-ports`/`paladin-storage`/`paladin-web` free of engine types
      (X-01, ADR-0031); one validation seam (the facade) and one persistence shape for both kinds;
      immutability enforced by schema and router, not discipline.</pros>
      <cons>The body is opaque to storage — no SQL-level querying of definition contents; a future
      typed column would be an additive migration.</cons>
    </option>
    <option id="option-b">
      <name>Typed arms in core (`Agent(PaladinConfigDoc) | Workflow(WarGraphDoc)`) as PRD 06 §2.3 literally writes</name>
      <pros>Stronger typing at the storage layer.</pros>
      <cons>Drags engine vocabulary (`WarGraphDoc`) into `paladin-core`, which has no engine — an
      ADR-0031/X-01 violation; two validation seams; rejected in 27-CONTEXT.md D-28.</cons>
    </option>
  </options>
  <resume-signal>Select: option-a or option-b</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Core assistant types, the repository port (no update), migrations `003`, and the contract suite on InMemory</name>
  <reversibility rating="one-way">D-28/D-29: `AssistantDefinition` is a persisted column pair and a published body; the `(assistant_id, version)` primary key and the absent update path are promises clients build on.</reversibility>
  <files>crates/paladin-core/src/platform/container/assistant.rs, crates/paladin-core/src/platform/container/mod.rs, crates/paladin-ports/src/output/assistant_repository_port.rs, crates/paladin-ports/src/output/mod.rs, crates/paladin-ports/src/output/run_repository_port.rs, crates/paladin-storage/src/assistant/mod.rs, crates/paladin-storage/src/assistant/contract_tests.rs, crates/paladin-storage/src/assistant/in_memory.rs, crates/paladin-storage/src/lib.rs, crates/paladin-storage/src/run/in_memory.rs, crates/paladin-storage/src/run/contract_tests.rs, crates/paladin-storage/migrations/sqlite/003_create_assistants_tables.sql, crates/paladin-storage/migrations/postgres/003_create_assistants_tables.sql</files>
  <read_first>
    - `crates/paladin-core/src/platform/container/waypoint.rs` 36-73 (newtype + error shape) and
      `container/mod.rs`.
    - `crates/paladin-core/src/platform/container/run.rs` (27-01) — the `schema_version` /
      `#[non_exhaustive]` / builder discipline to repeat.
    - `crates/paladin-ports/src/output/waypoint_port.rs` 160-260 — trait doc style and the mock module.
    - `crates/paladin-storage/migrations/{sqlite,postgres}/002_create_runs_table.sql` (27-02) — the
      migration style to continue.
    - `crates/paladin-storage/src/run/contract_tests.rs` (27-02) — the suite shape.
    - `.planning/phases/27-platform-api/27-CONTEXT.md` D-28, D-29, D-30, D-32, D-52.
  </read_first>
  <behavior>
    - `AssistantId::new` accepts a slug (`[a-z0-9][a-z0-9_-]{0,63}`), rejects empty/whitespace/uppercase
      with a structured `AssistantIdError`.
    - `AssistantDefinition { kind, body }` round-trips through serde with `kind` serialised as
      `"agent"` / `"workflow"`; `Assistant`, `AssistantVersion` carry `schema_version` and are
      `#[non_exhaustive]` with `new` + `with_*` construction; `AssistantSource { Stored, Code }`.
    - Repository contract (InMemory first): `create(id, NewAssistantVersion { definition, created_by,
      note })` creates the assistant with `latest == 1` and returns version 1; a second `create` for the
      same id → `AlreadyExists`. `append_version(id, new)` returns version `latest + 1` and advances
      `latest`; on an unknown or soft-deleted id → `NotFound`. `get_version(id, 0)` and
      `get_version(id, latest + 1)` → `Ok(None)`. `list_versions(id, limit, cursor)` is ascending by
      `version` with keyset pagination. `list(limit, cursor, include_deleted)` is ascending by
      `assistant_id`. `soft_delete(id)` sets `deleted_at`, `get(id)` then returns the assistant with
      `deleted_at: Some`, `get_version` still works, and `append_version` → `NotFound`. Publishing the
      identical body twice yields versions n+1 and n+2 with distinct `created_at`/`note`.
    - `concurrent_append_admits_exactly_one_per_version` (`multi_thread`, timeout guard): ten tasks
      calling `append_version` concurrently on one assistant produce versions 2..=11 with no gaps and
      no duplicates (every call is `Ok` because the adapter retries on `VersionConflict` at most 20
      times); a variant with retries disabled (`append_version_once`) sees at least one
      `VersionConflict` when two callers pre-compute the same `n+1` (SQL adapters).
    - Run-repository extension: `insert_with_latest(run)` (run's `assistant.version` ignored)
      inserts with `assistant_version = assistants.latest` in ONE statement and returns the resolved
      version; unknown/soft-deleted assistant → `UnknownAssistant { assistant_id }`; busy thread →
      `ThreadBusy`. `assistant_version_freeze_at_submit` (`multi_thread`): 20 alternating
      `append_version` and `insert_with_latest` calls on distinct threads — every run's version is a
      version that exists and is ≤ the `latest` observed after the loop; no run has version 0 or a
      version greater than `latest`.
  </behavior>
  <action>
    Create `crates/paladin-core/src/platform/container/assistant.rs` (declare in `container/mod.rs`):
    `AssistantId` newtype + `AssistantIdError`, `AssistantKind { Agent, Workflow }`
    (`#[serde(rename_all = "snake_case")]`), `AssistantDefinition { kind, body: serde_json::Value }`,
    `AssistantSource { Stored, Code }`, `AssistantVersion { assistant_id, version: u32, definition,
    created_at, created_by: Option<String>, note: Option<String>, schema_version }`,
    `NewAssistantVersion { definition, created_by, note }`, `Assistant { assistant_id, latest: u32,
    source, created_at, deleted_at: Option<DateTime<Utc>>, schema_version }`,
    `ASSISTANT_SCHEMA_VERSION`. Rustdoc + doc tests (X-02).
    Create `crates/paladin-ports/src/output/assistant_repository_port.rs`: `AssistantRepositoryPort`
    with exactly `create`, `append_version`, `get`, `get_version`, `list`, `list_versions`,
    `soft_delete` — the module docs state that no update method exists by design (D-29) — and
    `AssistantRepositoryError { NotFound { assistant_id }, AlreadyExists { assistant_id },
    VersionConflict { assistant_id, version }, Backend { source }, Serialization { message },
    UnknownSchemaVersion { found } }`, `AssistantPage`, `AssistantVersionPage`; mock module. Declare
    in `output/mod.rs`. Add `insert_with_latest(&self, run: &Run) -> Result<u32, RunRepositoryError>`
    and `RunRepositoryError::UnknownAssistant { assistant_id }` to `run_repository_port.rs`.
    Write `003_create_assistants_tables.sql` for both backends: `assistants(assistant_id TEXT PRIMARY
    KEY, latest INTEGER NOT NULL, created_at, deleted_at NULL, schema_version TEXT NOT NULL)` and
    `assistant_versions(assistant_id TEXT NOT NULL, version INTEGER NOT NULL, kind TEXT NOT NULL,
    body TEXT/JSONB NOT NULL, created_at, created_by TEXT NULL, note TEXT NULL, schema_version TEXT NOT
    NULL, PRIMARY KEY (assistant_id, version))`. Create `assistant/{mod,contract_tests,in_memory}.rs`
    (declared in `storage/src/lib.rs` beside `run`), with `InMemoryAssistantRepository` and
    `InMemoryRunRepository::with_assistants(Arc<InMemoryAssistantRepository>)` implementing
    `insert_with_latest` by reading `latest` then inserting under the run lock (lock order: assistants
    read → runs write, documented). Extend `run/contract_tests.rs` with `insert_with_latest_*` clauses
    and `assistant_version_freeze_at_submit`, and the InMemory test wiring.
  </action>
  <acceptance_criteria>
    - `cargo test -p paladin-ai-core --lib assistant 2>&1 | grep -qE 'test result: ok\. [1-9][0-9]* passed'`.
    - `cargo test -p paladin-storage --lib assistant::in_memory 2>&1 | grep -E 'test result: ok\. [1-9][0-9]* passed'` — at least 12 tests.
    - `cargo test -p paladin-storage --lib assistant_version_freeze_at_submit 2>&1 | grep -qE 'test result: ok\. [1-9] passed'`.
    - `grep -c 'async fn update' crates/paladin-ports/src/output/assistant_repository_port.rs` is 0 and `grep -c 'async fn append_version' crates/paladin-ports/src/output/assistant_repository_port.rs` is 1.
    - `grep -c 'PRIMARY KEY (assistant_id, version)' crates/paladin-storage/migrations/sqlite/003_create_assistants_tables.sql crates/paladin-storage/migrations/postgres/003_create_assistants_tables.sql` reports 1 in each.
    - `grep -c 'async fn insert_with_latest' crates/paladin-ports/src/output/run_repository_port.rs` is 1.
    - `grep -c 'paladin-battalion\|paladin_battalion' crates/paladin-core/src/platform/container/assistant.rs` is 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p paladin-storage --lib assistant::in_memory 2>&amp;1 | grep -qE 'test result: ok\. (1[2-9]|[2-9][0-9]) passed' &amp;&amp; cargo test -p paladin-storage --lib assistant_version_freeze_at_submit 2>&amp;1 | grep -qE 'test result: ok\. [1-9] passed'</automated>
  </verify>
  <done>Core assistant types, an update-less port, the migrations and the contract suite exist; the InMemory
  adapters pass every clause including the concurrency edges.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SQLite + Postgres assistant repositories, `insert_with_latest` on the SQL run adapters, `MIGRATION.md` §9.3/§9.4</name>
  <files>crates/paladin-storage/src/assistant/mod.rs, crates/paladin-storage/src/assistant/sqlite.rs, crates/paladin-storage/src/assistant/postgres.rs, crates/paladin-storage/src/run/sqlite.rs, crates/paladin-storage/src/run/postgres.rs, MIGRATION.md</files>
  <read_first>
    - `crates/paladin-storage/src/run/{sqlite,postgres}.rs` (27-02) — error mapping, migrator, test
      wiring and the `STORAGE_POSTGRES_TEST_URL` skip helper to reuse.
    - `crates/paladin-storage/src/assistant/contract_tests.rs` (Task 2).
    - `MIGRATION.md` §9.3 and §9.4.
    - `.planning/phases/27-platform-api/27-05-SUMMARY.md` (if present) or `27-05-PLAN.md` — the
      `schemars` dependency edge that 27-05 added to `paladin-battalion`, which this plan registers in
      §9.3 on its behalf (same-wave file-ownership rule).
  </read_first>
  <behavior>
    - `SqliteAssistantRepository` and `PostgresAssistantRepository` pass every contract clause;
      `append_version` is insert-then-CAS: `INSERT INTO assistant_versions (.., version = ?n+1)` (PK
      violation → `VersionConflict`) then `UPDATE assistants SET latest = ?n+1 WHERE assistant_id = ?
      AND latest = ?n`; the public method retries on `VersionConflict` up to 20 times;
      `append_version_once` does not.
    - `SqliteRunRepository::insert_with_latest` and the Postgres twin execute `INSERT INTO runs (…,
      assistant_version, …) SELECT …, a.latest, … FROM assistants a WHERE a.assistant_id = ? AND
      a.deleted_at IS NULL` and read back the resolved version; `rows_affected() == 0` →
      `UnknownAssistant`; unique violation → `ThreadBusy`.
    - Postgres modules self-skip with `SKIP:` locally.
  </behavior>
  <action>
    Create `assistant/sqlite.rs` and `assistant/postgres.rs` behind the existing features, wired
    one `#[tokio::test]` per contract clause (Postgres via `STORAGE_POSTGRES_TEST_URL`, `SKIP:`
    line). Add `insert_with_latest` to `run/sqlite.rs` and `run/postgres.rs`, plus the new contract
    clauses in their test modules. In `MIGRATION.md` §9.4 add the `003_create_assistants_tables.sql`
    row (tables, PK, soft-delete column, "no update statement exists in any adapter", owner PLAT-04);
    in §9.3 add the row "`schemars 1.2` — new dependency edge from `paladin-battalion` (already a
    workspace dependency since Phase 26; no new package in `Cargo.lock`); `cargo msrv verify` at
    1.88 run by plan 27-05".
  </action>
  <acceptance_criteria>
    - `cargo test -p paladin-storage --features sqlite --lib assistant::sqlite 2>&1 | grep -E 'test result: ok\. [1-9][0-9]* passed'` — at least 12 tests.
    - `cargo test -p paladin-storage --features sqlite --lib run::sqlite 2>&1 | grep -qE 'test result: ok\. (1[8-9]|[2-9][0-9]) passed'` (27-02's count plus the new clauses).
    - `cargo test -p paladin-storage --features postgres --lib assistant::postgres -- --nocapture 2>&1 | grep -c 'SKIP:'` is at least 1 locally.
    - `grep -cE '^\s*#\[tokio::test\]' crates/paladin-storage/src/assistant/postgres.rs` equals `grep -c 'pub async fn' crates/paladin-storage/src/assistant/contract_tests.rs`.
    - `grep -c 'FROM assistants' crates/paladin-storage/src/run/sqlite.rs` is at least 1 and the same for `run/postgres.rs`.
    - `awk '/^## 9.4/,/^## 9.5/' MIGRATION.md | grep -c '003_create_assistants_tables'` is 1 and `awk '/^## 9.3/,/^## 9.4/' MIGRATION.md | grep -c schemars` is at least 1.
    - `cargo clippy -p paladin-storage --all-features --all-targets -- -D warnings` passes.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p paladin-storage --features sqlite --lib assistant::sqlite 2>&amp;1 | grep -qE 'test result: ok\. (1[2-9]|[2-9][0-9]) passed' &amp;&amp; cargo test -p paladin-storage --features postgres --lib assistant::postgres -- --nocapture 2>&amp;1 | grep -q 'SKIP:'</automated>
  </verify>
  <done>Three assistant adapters pass one suite; `latest` is frozen inside the run-insert statement on both
  SQL backends; the migration and dependency edge are registered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin HTTP caller → stored definitions | Arbitrary JSON bodies become persisted assistant versions (validated in 27-12 before reaching this layer). |
| Concurrent publishers / submitters → `assistants`/`runs` | Version numbering and freeze-at-submit are contended. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-09-01 | Tampering | version mutation | high | mitigate | No update method on the port, no UPDATE statement on `assistant_versions` in any adapter (grep gate), PK `(assistant_id, version)`. |
| T-27-09-02 | Tampering | torn `latest` at submit | high | mitigate | Single-statement `INSERT … SELECT latest` on SQL backends; `assistant_version_freeze_at_submit` concurrency clause. |
| T-27-09-03 | Repudiation | who published what | medium | mitigate | `created_by`/`created_at`/`note` mandatory columns; `list_versions` is the changelog (PLAT-FR-10). |
| T-27-09-04 | Information disclosure | definition bodies (prompts) | low | accept | Admin-authored configuration read only through authenticated routes; no credential field exists in either kind's body (27-12 prohibition). |
</threat_model>

<verification>
- `cargo test -p paladin-storage --features sqlite --lib assistant` and `--lib run` report non-zero counts; Postgres modules print `SKIP:`.
- `cargo clippy -p paladin-storage --all-features --all-targets -- -D warnings`, `cargo fmt --check`.
- `grep -rc 'UPDATE assistant_versions' crates/paladin-storage/src/assistant/` is 0 in every file.
</verification>

<success_criteria>
- Assistants are append-only immutable versions at the schema and port level, with an audit trail, and `latest` freezes at submit as a database property.
- The one-way door was confirmed by a human before the core type was written.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

See the identical section in `27-02-PLAN.md` — the canonical list of every symbol, route, table,
migration, CI job, env var, dependency promotion and doc asset Phase 27 creates. This plan's own
contribution: `crates/paladin-core/src/platform/container/assistant.rs` (`AssistantId`, `AssistantIdError`,
`AssistantKind`, `AssistantDefinition`, `AssistantSource`, `AssistantVersion`, `NewAssistantVersion`,
`Assistant`, `ASSISTANT_SCHEMA_VERSION`), `crates/paladin-ports/src/output/assistant_repository_port.rs`
(`AssistantRepositoryPort`, `AssistantRepositoryError`, `AssistantPage`, `AssistantVersionPage`),
`RunRepositoryPort::insert_with_latest`, `RunRepositoryError::UnknownAssistant`,
`crates/paladin-storage/src/assistant/*` (`InMemoryAssistantRepository`, `SqliteAssistantRepository`,
`PostgresAssistantRepository`, `contract_tests`), migrations `003_create_assistants_tables.sql`, tables
`assistants` and `assistant_versions`.
</artifacts_produced>

<output>
Create `.planning/phases/27-platform-api/27-09-SUMMARY.md` when done.
</output>
