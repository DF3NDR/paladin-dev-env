---
phase: 27-platform-api
plan: 26
type: execute
wave: 1
depends_on: [27-19]
files_modified:
  - crates/paladin-storage/src/run_queue/contract_tests.rs
  - crates/paladin-storage/src/run_queue/redis.rs
  - crates/paladin-storage/src/run_queue/in_memory.rs
autonomous: true
gap_closure: true
requirements: [PLAT-02]

must_haves:
  truths:
    - "`contract_tests::run_all` gives EVERY clause a fresh, empty queue: it takes a factory (a closure returning a future that yields a new `RunQueuePort` implementation) and calls it once per clause, so no clause can observe messages, leases or tokens left behind by an earlier clause (the defect CI run 34238527001 exposed at contract_tests.rs:188 — `ack_removes_message_permanently` saw depth 6, the five leases left by the fifo, lease-expiry and extend-lease clauses, instead of 1)."
    - "The eight clause bodies are byte-identical to before: no assertion is weakened, reordered, skipped or made tolerant; only `run_all`'s signature and body change, plus the two backend tests that call it (D-06)."
    - "`run_all` is exercised on BOTH backends: `redis_run_queue_full_contract_suite_via_run_all` builds a fresh randomized-prefix `RedisRunQueue` per clause, and a new `in_memory_run_queue_full_contract_suite_via_run_all` builds a fresh `InMemoryRunQueue` per clause and passes locally — so the sequence can never again pass on one backend by accident of statelessness while failing on the other."
    - "The CI `redis-queue` job's declared-vs-passed guard stays consistent: the number of `#[test]`/`#[tokio::test]` attributes in `redis.rs` is unchanged (one `run_all` test remains one test), so `Declared tests: N, passed: N` still holds when the live suite is green."
    - "PLAT-02 `idempotency` probe: running `run_all` twice against the in-memory factory passes both times — a fresh queue per clause means the second run inherits nothing from the first."
  prohibitions:
    - statement: "No contract clause body (`fifo_order_and_distinct_lease_tokens` … `concurrent_workers_each_message_exactly_once`) MAY be edited; the fix lives in `run_all` and its callers only."
      category: correctness
    - statement: "The Redis Tier-2 result MUST NOT be recorded as passed from a local run — the live-server proof is the CI `redis-queue`, `coverage` and `integration-tests` jobs at the pushed SHA (D-51)."
      category: process
    - statement: "No new public API may be added to `paladin-storage`'s tracked surface beyond the changed `run_all` signature in the already-public `contract_tests` module."
      category: process
  artifacts:
    - path: "crates/paladin-storage/src/run_queue/contract_tests.rs"
      provides: "factory-based run_all that yields a fresh queue per clause"
      contains: "pub async fn run_all"
    - path: "crates/paladin-storage/src/run_queue/in_memory.rs"
      provides: "in_memory_run_queue_full_contract_suite_via_run_all"
      contains: "full_contract_suite_via_run_all"
  key_links:
    - from: "crates/paladin-storage/src/run_queue/redis.rs"
      to: "crates/paladin-storage/src/run_queue/contract_tests.rs"
      via: "the redis run_all test hands run_all a factory built on queue_or_skip"
      pattern: "contract_tests::run_all\\("
---

<objective>
Close the single remaining red cause in CI run 34238527001 at `a1dbe74e` (the SHA carrying gap-closure plans
27-19 … 27-24). Three jobs — `Redis Run Queue Contract Suite (live server)`, `Coverage` and `Integration Tests` —
all fail on ONE test, `run_queue::redis::tests::redis_run_queue_full_contract_suite_via_run_all`:

```
panicked at crates/paladin-storage/src/run_queue/contract_tests.rs:188:5
assertion `left == right` failed
  left: 6
 right: 1
```

`contract_tests::run_all` runs the eight shared clauses back-to-back on ONE queue instance. The fifo clause
leaves three leased (unacked, 30 s lease) messages, the lease-expiry clause one, the extend-lease clause one;
`ack_removes_message_permanently` then enqueues one message and asserts `depth() == 1` but sees 6. The
baseline run (34222317640) never reached line 188 because `run_all` aborted earlier at line 105 on the
attempt off-by-one that 27-19 fixed — fixing the first failing assertion made the later, latent ordering
defect reachable. Only the Redis backend calls `run_all`; the in-memory backend never did. The eight
per-clause Redis tests (fresh key prefix each) all pass, so this is a suite-isolation defect in the
convenience runner, not an adapter regression.

Purpose: PLAT-02 — the phase's re-verification needs `redis-queue`, `coverage` (which currently exits 101
before computing a percentage) and `integration-tests` green at the closing SHA.

Output: a `run_all` that provisions a fresh queue per clause, exercised on both backends.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
Read first:
- `crates/paladin-storage/src/run_queue/contract_tests.rs` — the clauses and `run_all` (line ~436).
- `crates/paladin-storage/src/run_queue/redis.rs` tests module (~line 690 onward): `queue_or_skip()` returns
  `Option<RedisRunQueue>` under a fresh `test-run-queue-<uuid>` prefix and prints a named `SKIP:` line when
  `redis-test` is unreachable; `redis_run_queue_full_contract_suite_via_run_all` (~line 847) is the caller.
- `crates/paladin-storage/src/run_queue/in_memory.rs` tests module (~line 220 onward): one `#[tokio::test]` per
  clause built on `InMemoryRunQueue::new()`; no `run_all` test exists yet.
- `.github/workflows/ci.yml` `redis-queue` job: the declared-count guard counts `#[test]`/`#[tokio::test]`
  attributes in `redis.rs` and compares with the `passed` count — keep that count unchanged.
- `.planning/phases/27-platform-api/27-19-SUMMARY.md` — what the attempt fix changed (do not touch the Lua).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make run_all provision a fresh queue per clause, exercised on both backends</name>
  <files>crates/paladin-storage/src/run_queue/contract_tests.rs, crates/paladin-storage/src/run_queue/redis.rs, crates/paladin-storage/src/run_queue/in_memory.rs</files>
  <behavior>
    - RED first: add `in_memory_run_queue_full_contract_suite_via_run_all` to `in_memory.rs` calling the
      CURRENT `run_all(&InMemoryRunQueue::new())` and run it — expect it to FAIL at contract_tests.rs:188 with
      `left: 6 right: 1` (the same failure CI showed), proving the defect is reproducible without Redis.
      Record the failing output verbatim in the SUMMARY.
    - GREEN: change `run_all` to take a factory, e.g.
      `pub async fn run_all<F, Fut, Q>(fresh_queue: F) where F: Fn() -> Fut, Fut: Future<Output = Q>, Q: RunQueuePort`
      and call `fresh_queue().await` once per clause, passing `&queue` to each clause exactly as before. Update
      the module docs (the "identical suite across backends" paragraph) to state the fresh-queue-per-clause
      contract and why (leases left by earlier clauses are not the later clause's fixture).
    - Redis caller: probe once with `queue_or_skip()` (keep the SKIP path and its message exactly as is), then
      `contract_tests::run_all(|| async { queue_or_skip().await.expect("redis-test was reachable a moment ago") }).await`
      so each clause runs under its own fresh randomized prefix. Do not add or remove any `#[tokio::test]` in
      `redis.rs`.
    - In-memory caller: `contract_tests::run_all(|| async { InMemoryRunQueue::new() }).await`, and a second
      test (or a second call in the same test) that runs it twice to pin the idempotency probe.
    - Do NOT edit any clause body. Do NOT touch the Lua scripts or the claim marker from 27-19.
  </behavior>
  <acceptance_criteria>
    - `cargo test -p paladin-storage --lib run_queue::in_memory` passes, including `in_memory_run_queue_full_contract_suite_via_run_all`.
    - `cargo test -p paladin-storage --features redis-queue --lib run_queue::redis` compiles and every test either passes or takes the named `SKIP:` path locally (Docker is unavailable here; the live proof is CI).
    - `git diff --stat` of `contract_tests.rs` shows changes only in the module docs and in `run_all` (verify with `git diff -U0 crates/paladin-storage/src/run_queue/contract_tests.rs` — no hunk inside any `pub async fn <clause>` body).
    - `grep -cE '^\s*#\[test\]|^\s*#\[tokio::test\]' crates/paladin-storage/src/run_queue/redis.rs` is unchanged from before the edit (record both numbers).
    - `cargo fmt --all -- --check` exits 0; `cargo clippy -p paladin-storage --all-features --all-targets -- -D warnings` exits 0; `cargo check --workspace --all-targets --all-features` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p paladin-storage --lib run_queue::in_memory 2>&1 | grep -qE 'test result: ok\. [1-9][0-9]* passed' &amp;&amp; cargo test -p paladin-storage --features redis-queue --lib run_queue::redis 2>&1 | grep -qE 'test result: ok\.'</automated>
  </verify>
  <done>run_all hands every clause a fresh queue, the in-memory backend proves it locally, the Redis caller keeps its SKIP path and test count, and no clause body changed.</done>
</task>

</tasks>

<verification>
- Local: the RED in-memory failure reproduces CI's `left: 6 right: 1`, then passes after the fix.
- CI (plan 27-25's checkpoint, at the SHA containing this plan): `redis-queue` 0 failed with the live-server line, `coverage` computes a percentage ≥ 82 %, `integration-tests` green.
</verification>

<success_criteria>
- `run_all` cannot leak state between clauses on any backend.
- Clause bodies unchanged; redis.rs test count unchanged; both backends exercise `run_all`.
</success_criteria>
