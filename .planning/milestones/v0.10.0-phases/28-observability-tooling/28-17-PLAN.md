---
phase: 28-observability-tooling
plan: 17
type: execute
wave: 8
depends_on: [28-09, 28-13, 28-15, 28-16]
files_modified:
  - docs/src/operations/observability.md
  - docs/src/user-guides/graph-visualization.md
  - docs/src/user-guides/eval-harness.md
  - docs/src/SUMMARY.md
  - .planning/decisions/0048-paladin-eval-composition-crate.md
  - .planning/decisions/PROMOTION.md
  - MIGRATION.md
  - scripts/publish-crates.sh
  - Makefile
  - .github/workflows/ci.yml
  - .project/current-exports.txt
  - .planning/phases/28-observability-tooling/28-CI-EVIDENCE.md
autonomous: true
requirements: [OBS-01, OBS-02, OBS-03, OBS-04]

must_haves:
  truths:
    - "Three new mdBook pages exist and are wired into the table of contents: an operations page covering the trace model's twelve variants and envelope, the sinks, OTLP setup with a collector example, persistence and retention, drop accounting and the correlation story; a graph-visualization user guide covering both export commands, the badge and colour legend, the inspector page with its admin gating and the configurable Mermaid URL for air-gapped hosts; and an eval-harness user guide covering the file format with its schema link, every assertion with an example, the harness setup, repeat, bless and live modes, and the promotion path (D-38)."
    - "ADR-0048 records the `paladin-eval` composition-crate classification with its correct number — the next free number is 0048, not 0047, per the promotion register — so a future reader of the extracted-crate dependency rule does not read this crate's dependency edges as a violation (D-27, corrected by research)."
    - "`MIGRATION.md` §9.2 carries a deliberate-zero note in the established form for every new-in-0.10 type this phase touched, and an explicit row for every pre-existing-at-0.9.0 public type touched — with the inventory taken FIRST and the expected count being zero (D-39, X-10.1)."
    - "`MIGRATION.md` §9.3 lists the five new dependencies with their MSRV verification results, §9.4 lists the `run_traces` migrations for both backends with the statement that the persistence-file format is unchanged, §9.5 lists the new configuration keys and environment variables, §9.6 lists the additive stream payload field, the new stream mode and the dev-ui page as a non-API route, and §9.7 records no deprecations (D-39)."
    - "`paladin-eval` is registered in every place the publishable crates are enumerated: the publish script's dependency-ordered array and the publish-dry-run make target — and is deliberately EXCLUDED from the semver job's package list with an inline comment recording why (it has no 0.9.0 baseline to diff against), rather than silently omitted (D-40)."
    - "`.project/current-exports.txt` is regenerated so the api-surface CI job is green on the phase's final commit — the carried concern from Phases 25 and 26 is not left red again."
    - "Every close-out gate in the locked order is run and its result recorded: the workspace test suite including the evals target, formatting, lints with and without each new feature, semver checks against the published 0.9.0 crates for the eleven pre-existing publishable crates, the MSRV job at 1.88 with all features, the security scan, coverage at or above the floor, the api-surface check with the regenerated baseline, the OpenAPI re-bless and the SDK-clients job, a default-build dependency-tree check showing no new heavy dependency, and the feature-flags job covering each new feature individually (D-40)."
    - "Docker-dependent tiers — the Postgres `run_traces` contract suite — are read green from a named CI run and recorded in the phase's CI-evidence file, never marked passed locally (24-CONTEXT D-28)."
    - "`cargo doc` adds no new broken intra-doc links; the roughly sixty pre-existing rustdoc warnings carried from Phase 26 are not this phase's to fix and are not made worse."
  prohibitions: []
  artifacts:
    - path: "docs/src/operations/observability.md"
      provides: "the trace model, sinks, OTLP setup, persistence, retention and drop accounting"
      contains: "trace_seq"
    - path: "docs/src/user-guides/eval-harness.md"
      provides: "the scenario format, every assertion, the harness setup, repeat/bless/live and the promotion path"
      contains: "--repeat"
    - path: ".planning/decisions/0048-paladin-eval-composition-crate.md"
      provides: "ADR-0048 classifying paladin-eval as a composition crate"
      contains: "composition"
    - path: ".planning/phases/28-observability-tooling/28-CI-EVIDENCE.md"
      provides: "named CI run identifiers and results for every Docker-dependent and CI-only gate"
      contains: "postgres-integration"
  key_links:
    - from: "docs/src/SUMMARY.md"
      to: "docs/src/operations/observability.md"
      via: "the table of contents lists all three new pages so mdBook builds them"
      pattern: "observability|graph-visualization|eval-harness"
    - from: "scripts/publish-crates.sh"
      to: "crates/paladin-eval/Cargo.toml"
      via: "the dependency-ordered publish array gains the twelfth crate"
      pattern: "paladin-eval"
---

<objective>
Close the phase honestly: three documentation pages, the ADR that classifies the new crate, every
`MIGRATION.md` row this phase owes, the release-pipeline registrations the twelfth crate needs, and a
recorded pass through the full close-out gate list with CI evidence for the tiers that cannot run
locally.

Purpose: X-08, X-10, X-11 and D-38 through D-40. Phase 29's SHIP gates check that this phase left
these rows complete, and Phase 25's and 26's carried concern — a red api-surface job left behind —
must not recur.

Output: three mdBook pages, ADR-0048, filled `MIGRATION.md` sections, updated crate lists, a
regenerated public-API baseline, and `28-CI-EVIDENCE.md`.

Note on task shape: this plan is bookkeeping and documentation over already-shipped behaviour, so it
deliberately carries no end-to-end tracer slice — there is no new architecture to prove. Its tasks are
verified by the gates they run and the artifacts they produce.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-observability-tooling/28-CONTEXT.md
@.planning/phases/28-observability-tooling/28-RESEARCH.md
@.planning/phases/28-observability-tooling/28-PATTERNS.md
@.planning/phases/28-observability-tooling/28-BENCH-EVIDENCE.md
@.project/v0.10.0/07-observability-tooling.md
@.project/v0.10.0/00-program-overview.md
@CLAUDE.md

<interfaces>
Corrections that are load-bearing here (28-RESEARCH.md Metadata):
1. The next free ADR number is **0048**, not 0047 — `.planning/decisions/PROMOTION.md:69` reads
   "Next free ADR number: 0048" because Phase 16 consumed 0047. The file must be named
   `0048-paladin-eval-composition-crate.md` and the register's next-free line advanced afterwards.
2. `crates/doc-examples`'s package is `paladin-doc-examples` with publishing disabled — it is
   precedent for a composition crate's dependency SHAPE only, not for the publishing decision. The ADR
   must not cite it as a publishing precedent.

Crate-list enumeration points (28-RESEARCH.md Q16, each verified by direct read):
- `scripts/publish-crates.sh:145-155` — a hardcoded eleven-crate dependency-ordered array; insert the
  new crate after the storage crate and before the umbrella crate.
- `.github/workflows/ci.yml:330-343` — the semver job's hardcoded eleven-package list fed to
  `cargo semver-checks check-release --package <pkg> --baseline-version 0.9.0`. Do NOT add the new
  crate: it has no 0.9.0 baseline and the tool would error. Record the omission's reason as an inline
  comment in the job rather than leaving it silent.
- `Makefile:547-557` — the publish-dry-run target's per-crate lines; add one for the new crate after
  the storage crate. Note that the herald crate is already missing from this target — a pre-existing
  gap, not introduced by this phase and not this phase's to fix; mention it in the summary rather than
  fixing it silently.
- `scripts/extract-public-api.sh` / `scripts/check-api-surface.sh` — no per-crate list; the tool runs
  at workspace level and the members glob already covers a new directory under the crates directory,
  so only a regeneration of the baseline file is needed.
- `.github/workflows/ci.yml:1077` — the SDK-clients job reads the committed OpenAPI document off disk
  and needs no edit.

`MIGRATION.md` §9.2's deliberate-zero note form is set by the Phase 24 and Phase 25 notes at lines
196 and 198 — follow that form exactly.

New dependencies to record in §9.3, with their verified MSRV results: the three OpenTelemetry crates
(behind the `otel` feature), the custom test harness crate and the glob crate, plus the new consumer
of the already-present YAML crate.

Docker is unavailable locally (24-CONTEXT D-28); the Postgres contract suite is Tier 2, proven only by
CI's Postgres integration job, and read green from a named run — never marked passed locally. Phase 27
recorded its equivalent evidence in its own CI-evidence file; follow that precedent.

The bench evidence for the overhead acceptance criterion already exists at
`.planning/phases/28-observability-tooling/28-BENCH-EVIDENCE.md` (28-06) and is cited, not recomputed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Three mdBook pages and ADR-0048</name>
  <files>docs/src/operations/observability.md, docs/src/user-guides/graph-visualization.md, docs/src/user-guides/eval-harness.md, docs/src/SUMMARY.md, .planning/decisions/0048-paladin-eval-composition-crate.md, .planning/decisions/PROMOTION.md</files>
  <read_first>
    - `docs/src/SUMMARY.md` — the table-of-contents structure and where operations and user-guide pages sit.
    - Two existing pages, one from each of those sections, for the house voice, heading depth and code-block conventions.
    - `.planning/decisions/PROMOTION.md` around line 69 — the next-free-ADR-number line and the promotion procedure.
    - Two recent ADRs in `.planning/decisions/` — the front-matter fields, the status vocabulary, and the single-physical-line bullet convention the ADR parser requires for code-locations and considered-options lists.
    - `.planning/decisions/0031-extracted-crate-dependency-rule.md` — the invariant ADR-0048 clarifies the scope of.
    - `docs/schemas/eval-scenario.schema.json` — the schema the eval page links to.
    - `.planning/phases/28-observability-tooling/28-CONTEXT.md` D-27, D-38.
  </read_first>
  <behavior>
    - `mdbook build` succeeds and each of the three pages is reachable from the table of contents.
    - The operations page documents all twelve trace variants and the record envelope, the four sinks,
      an OTLP collector configuration example, the persistence table and its shared retention policy,
      the drop-accounting story, and how the stream correlation field ties an event to a log line, a
      span and a stored row.
    - The graph-visualization page documents both export commands with runnable invocations, the badge
      and outcome-colour legend matching the frozen goldens, the inspector page with its feature gate
      and admin scope, and how an air-gapped operator repoints the diagram library URL.
    - The eval-harness page documents the file format with a link to its schema, every assertion with a
      worked example, the test-target setup, the repeat, bless and live flags, and the promotion path
      from scripted CI runs to pre-release live smoke.
    - ADR-0048 exists with the correct number, records the composition-crate classification and the
      publishing decision on its own reasoning, cites the extracted-crate rule as the invariant it
      clarifies rather than contradicts, and does not cite the documentation-examples crate as a
      publishing precedent.
    - The promotion register's next-free-ADR-number line is advanced past 0048.
  </behavior>
  <action>
    Write the three pages and add them to `docs/src/SUMMARY.md` under the operations and user-guides
    sections. Keep every example runnable and every value consistent with what shipped — read the
    relevant summaries rather than restating the plans. Write
    `.planning/decisions/0048-paladin-eval-composition-crate.md` in the house ADR form, taking care
    with the single-physical-line bullet convention the ADR parser needs, and advance the promotion
    register's next-free-number line. Where a documented behaviour was resolved at implementation time
    differently from the plan, document what shipped and note the divergence rather than the intent.
  </action>
  <acceptance_criteria>
    - `mdbook build docs 2>&1 | grep -qiv 'error'` and the command exits 0.
    - `for p in operations/observability user-guides/graph-visualization user-guides/eval-harness; do grep -q "$p" docs/src/SUMMARY.md || exit 1; done`.
    - `grep -c 'trace_seq' docs/src/operations/observability.md` is at least 1 and `grep -c 'run_traces' docs/src/operations/observability.md` is at least 1.
    - `grep -c 'graph export' docs/src/user-guides/graph-visualization.md` is at least 1 and `grep -c 'run export' docs/src/user-guides/graph-visualization.md` is at least 1 and `grep -ci 'air-gapped\|local mirror' docs/src/user-guides/graph-visualization.md` is at least 1.
    - All twelve assertion names appear in the eval page: `for a in final_state_field_equals final_state_field_matches field_json_path_equals node_executed node_not_executed edge_fired route_taken run_status total_tokens_max supersteps_max parley_raised final_state_snapshot; do grep -q "$a" docs/src/user-guides/eval-harness.md || exit 1; done`.
    - `test -f .planning/decisions/0048-paladin-eval-composition-crate.md` and `test ! -f .planning/decisions/0047-paladin-eval-composition-crate.md`.
    - `grep -c '0049' .planning/decisions/PROMOTION.md` is at least 1 — the next-free line is advanced.
    - `grep -c 'doc-examples\|paladin-doc-examples' .planning/decisions/0048-paladin-eval-composition-crate.md` is 0, or, if mentioned, only as a dependency-shape precedent — verify by reading the sentence.
  </acceptance_criteria>
  <verify>
    <automated>mdbook build docs &amp;&amp; test -f .planning/decisions/0048-paladin-eval-composition-crate.md &amp;&amp; grep -q 'eval-harness' docs/src/SUMMARY.md</automated>
  </verify>
  <done>An operator can set up OTLP export, a developer can export and read a graph, and a team can write their first eval scenario — all from the shipped documentation — and the new crate's classification is on the record with the right number.</done>
</task>

<task type="auto">
  <name>Task 2: `MIGRATION.md` rows and the twelfth crate's registrations</name>
  <files>MIGRATION.md, scripts/publish-crates.sh, Makefile, .github/workflows/ci.yml</files>
  <read_first>
    - `MIGRATION.md` §9.2 around lines 190-205 — the Phase 24 and Phase 25 deliberate-zero notes whose form this follows — and the current contents of §9.3 through §9.7.
    - `.cargo/semver-checks-allowlist.toml` — the entry schema, expected untouched by this phase.
    - `scripts/publish-crates.sh` lines 140-160 — the dependency-ordered array and the invariant its comment states.
    - `.github/workflows/ci.yml` lines 325-350 — the semver job's package list.
    - `Makefile` lines 540-565 — the publish-dry-run target.
    - Every `28-*-SUMMARY.md` produced so far — the authoritative record of which public types were actually touched.
    - `.project/v0.10.0/00-program-overview.md` X-10 in full; `.planning/phases/28-observability-tooling/28-CONTEXT.md` D-39, D-40.
  </read_first>
  <behavior>
    - The inventory is taken FIRST: before writing any row, enumerate every public type this phase
      touched and classify each as new-in-0.10 or pre-existing-at-0.9.0 by checking the published
      baseline, and record that enumeration in the summary. The expected pre-existing count is zero;
      if it is not zero, each one gets an explicit register row with its mitigation.
    - §9.2 carries deliberate-zero notes for the trace event enum, the trace sink trait's signature
      change, the fallback adapter's renamed setter, the stream mode enum's new variant and marker, the
      stream event payload's new field, and the node-context type if it gained a trace accessor.
    - §9.3 lists all five new dependencies with their MSRV verification results and states whether any
      MSRV bump was needed — if one was, it is a stop-and-flag item, recorded rather than applied
      unilaterally.
    - §9.4 lists both `run_traces` migrations and states that the file-based persistence format is
      unchanged.
    - §9.5 lists the new configuration section, the diagram-URL key, the trace environment variable
      family and the eval live-mode variable.
    - §9.6 lists the additive stream payload field, the new stream mode value and the dev-ui page as a
      non-API route.
    - §9.7 records no deprecations.
    - The publish script's array and the publish-dry-run target gain the new crate in dependency order;
      the semver job does NOT, and carries an inline comment saying why.
  </behavior>
  <action>
    Take the public-type inventory first and record it. Fill `MIGRATION.md` §9.2 through §9.7 with the
    rows listed in `<behavior>`, following the established note form exactly and leaving no placeholder
    text — Phase 29's SHIP-01 gate fails on any unfilled marker. Insert the new crate into
    `scripts/publish-crates.sh`'s dependency-ordered array after the storage crate and add its line to
    the `Makefile`'s publish-dry-run target after the storage crate's; note in the summary that the
    herald crate's pre-existing absence from that target was observed and deliberately not fixed here.
    Add an inline comment to the semver job's package list recording that the new crate is excluded
    because it has no published 0.9.0 baseline to diff against. Leave the semver allowlist untouched —
    this phase produces no deliberate-breaking entries.
  </action>
  <acceptance_criteria>
    - `awk '/^## 9.2/,/^## 9.3/' MIGRATION.md | grep -ci 'TraceEvent'` is at least 1 and `awk '/^## 9.2/,/^## 9.3/' MIGRATION.md | grep -ci 'RunStreamMode'` is at least 1.
    - `awk '/^## 9.3/,/^## 9.4/' MIGRATION.md | grep -ci 'msrv'` is at least 1 and that section names all five new dependencies.
    - `awk '/^## 9.4/,/^## 9.5/' MIGRATION.md | grep -c 'run_traces'` is at least 1.
    - `awk '/^## 9.5/,/^## 9.6/' MIGRATION.md | grep -c 'PALADIN_TRACE_'` is at least 1 and `grep -c 'PALADIN_EVAL_LIVE'` in the same range is at least 1.
    - `awk '/^## 9.6/,/^## 9.7/' MIGRATION.md | grep -c 'trace_seq'` is at least 1 and the same range mentions the replay mode and the dev-ui route.
    - `grep -c 'TBD' MIGRATION.md` is 0 in the sections this phase owns — verify by `awk '/^## 9.2/,/^## 9.8/' MIGRATION.md | grep -c 'TBD'` being 0.
    - `grep -c 'paladin-eval' scripts/publish-crates.sh` is 1 and `grep -c 'paladin-eval' Makefile` is 1.
    - `awk '/semver/,/^  [a-z]/' .github/workflows/ci.yml | grep -c 'paladin-eval'` is at most 1 and, if 1, it is inside a comment explaining the exclusion — verify by reading the line.
    - `git diff --exit-code .cargo/semver-checks-allowlist.toml` is clean.
  </acceptance_criteria>
  <verify>
    <automated>test "$(awk '/^## 9.2/,/^## 9.8/' MIGRATION.md | grep -c 'TBD')" -eq 0 &amp;&amp; grep -q 'paladin-eval' scripts/publish-crates.sh &amp;&amp; grep -q 'paladin-eval' Makefile</automated>
  </verify>
  <done>Every migration row this phase owes is written from an inventory rather than from memory, and the twelfth crate is registered everywhere it must be and deliberately absent where it must not be.</done>
</task>

<task type="auto">
  <name>Task 3: Run the close-out gates in order, regenerate the public-API baseline, record CI evidence</name>
  <precondition>The Postgres `run_traces` contract suite and the workspace coverage measurement require services unavailable in this devcontainer; both are read green from named CI runs on the phase's final commit and recorded in the CI-evidence file rather than executed locally.</precondition>
  <files>.project/current-exports.txt, .planning/phases/28-observability-tooling/28-CI-EVIDENCE.md</files>
  <read_first>
    - `scripts/check-api-surface.sh` and `scripts/extract-public-api.sh` — the regeneration invocation and the baseline path.
    - `scripts/coverage.sh` — the exact invocation CI's coverage job runs and the floor it enforces.
    - `.github/workflows/ci.yml` — the semver, MSRV, api-surface, SDK-clients and Postgres integration jobs, and their names as they appear in a run listing.
    - `.github/workflows/feature-flags.yml` — the legs, including the two added by this phase.
    - `.planning/phases/27-platform-api/27-CI-EVIDENCE.md` — the precedent for how CI-only evidence is recorded.
    - `.planning/phases/28-observability-tooling/28-BENCH-EVIDENCE.md` — the overhead measurement to cite.
    - `.planning/phases/28-observability-tooling/28-CONTEXT.md` D-40 for the gate order.
  </read_first>
  <behavior>
    - The gates run in the locked order and each result is recorded: the workspace test suite including
      the evals target; formatting; lints across the workspace and all targets, run separately with and
      without each new feature; semver checks against the published 0.9.0 crates for the eleven
      pre-existing publishable crates; the MSRV job at 1.88 with all features; the security scan;
      coverage at or above the floor; the api-surface check with the regenerated baseline; the OpenAPI
      re-bless and the SDK-clients job; a default-build dependency-tree check confirming no new heavy
      dependency entered; and the feature-flags legs for each new feature individually.
    - `.project/current-exports.txt` is regenerated and the api-surface check passes against it, so the
      job is green on the phase's final commit.
    - The default-build dependency tree contains none of this phase's feature-gated or test-only new
      dependencies.
    - `28-CI-EVIDENCE.md` records, for every gate that cannot run locally, the workflow name, the run
      identifier, the commit and the result — with the Postgres contract suite explicitly among them
      and explicitly not claimed as locally passed.
    - Any gate that fails is recorded as failed with its output, not softened; a failing gate blocks the
      phase rather than being renegotiated here.
  </behavior>
  <action>
    Regenerate `.project/current-exports.txt` with the extraction script and confirm the api-surface
    check passes against it — this is the carried concern from Phases 25 and 26 and must not be left
    red again. Run every locally-runnable gate in the locked order, capturing each command and its
    result. Run the dependency-tree check on a default build and confirm none of the feature-gated or
    test-only dependencies this phase added are present. <!-- planner-discipline-allow: opentelemetry, libtest-mimic -->
    Push the branch, wait for the CI jobs that cannot run locally, and write
    `.planning/phases/28-observability-tooling/28-CI-EVIDENCE.md` recording for each: the workflow, the
    job name, the run identifier, the commit SHA and the outcome — following the Phase 27 evidence
    file's structure. Cite the bench-evidence file for the overhead criterion rather than recomputing
    it. If any gate fails, record the failure verbatim and stop rather than adjusting the gate.
  </action>
  <acceptance_criteria>
    - `./scripts/check-api-surface.sh` exits 0 against the regenerated baseline.
    - `git diff --stat .project/current-exports.txt` shows the baseline changed in this commit.
    - `cargo test --workspace 2>&1 | grep -qE 'test result: ok'` and `cargo test --test evals 2>&1 | grep -qE 'test result: ok'`.
    - `cargo fmt --check` exits 0; `cargo clippy --workspace --all-targets -- -D warnings` exits 0; the same with `--features otel` and with `--features dev-ui` each exit 0.
    - `make security` exits 0.
    - `! cargo tree -e features --no-default-features --features default 2>/dev/null | grep -qiE 'opentelemetry|libtest-mimic'` — a default build gained neither. <!-- planner-discipline-allow: opentelemetry, libtest-mimic -->
    - `test -s .planning/phases/28-observability-tooling/28-CI-EVIDENCE.md` and `grep -c 'postgres-integration' .planning/phases/28-observability-tooling/28-CI-EVIDENCE.md` is at least 1 and `grep -cE 'run [0-9]{6,}|runs/[0-9]{6,}' .planning/phases/28-observability-tooling/28-CI-EVIDENCE.md` is at least 1 — real run identifiers, not placeholders.
    - `grep -ci 'coverage' .planning/phases/28-observability-tooling/28-CI-EVIDENCE.md` is at least 1 with the measured figure recorded.
  </acceptance_criteria>
  <verify>
    <automated>./scripts/check-api-surface.sh &amp;&amp; cargo fmt --check &amp;&amp; cargo clippy --workspace --all-targets -- -D warnings &amp;&amp; test -s .planning/phases/28-observability-tooling/28-CI-EVIDENCE.md</automated>
  </verify>
  <done>Every gate in the locked order has a recorded result, the public-API baseline is green rather than carried forward red, and the tiers that need Docker are proven by named CI runs rather than asserted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → operator configuration decisions | A documented example an operator copies becomes their production configuration. |
| Release pipeline crate lists → crates.io | A registration error publishes or omits a crate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-17-01 | Information disclosure | A documented OTLP example showing a real-looking credential header | high | mitigate | The collector example uses an obvious placeholder and the page states that header values are secrets, are redacted from debug output and must not be committed. |
| T-28-17-02 | Information disclosure | Documenting the trace value-capture flag without its consequence | high | mitigate | The operations page states that enabling it puts redacted, capped field values into logs and stored rows, that it never reaches the stream wire, and that the default is off. |
| T-28-17-03 | Tampering | Publishing the new crate in the wrong dependency order | medium | mitigate | Insertion follows the script's stated invariant that every dependency is on the registry before its dependent publishes; the dry-run target exercises it. |
| T-28-17-04 | Repudiation | Claiming a Docker-dependent gate passed without evidence | high | mitigate | The CI-evidence file records workflow, job, run identifier, commit and outcome for every non-local gate, and the acceptance criteria require real run identifiers rather than placeholders. |
| T-28-17-05 | Tampering | An unregistered public-API change slipping past review | medium | mitigate | The inventory is taken before any row is written, the baseline is regenerated, and the api-surface check is required to pass on the final commit. |
</threat_model>

<verification>
- `mdbook build docs` green with all three pages in the table of contents.
- `MIGRATION.md` sections 9.2 through 9.7 filled with no placeholder markers.
- The full close-out gate list run in the locked order with every result recorded; Docker-dependent tiers read green from named CI runs and recorded in `28-CI-EVIDENCE.md`.
- `./scripts/check-api-surface.sh` green against the regenerated baseline.
</verification>

<success_criteria>
- PRD 07 acceptance 7 holds: every pre-existing public type touched is registered, semver checks and the MSRV job pass, new dependencies are listed, new migrations are listed, and new configuration and environment variables are listed.
- Phase 29's SHIP-01 gate will find no unfilled section this phase owed, and the api-surface job is green rather than carried forward red.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

See the identical section in `28-01-PLAN.md` — the canonical list for Phase 28. This plan's own
contribution: `docs/src/operations/observability.md`,
`docs/src/user-guides/graph-visualization.md`, `docs/src/user-guides/eval-harness.md` and their
table-of-contents entries, `.planning/decisions/0048-paladin-eval-composition-crate.md` (ADR-0048)
with the advanced next-free-number line, the filled `MIGRATION.md` sections 9.2 through 9.7, the
`paladin-eval` registrations in `scripts/publish-crates.sh` and the `Makefile` publish-dry-run target
plus the documented exclusion comment in the semver job, the regenerated
`.project/current-exports.txt`, and `.planning/phases/28-observability-tooling/28-CI-EVIDENCE.md`.
</artifacts_produced>

<output>
Create `.planning/phases/28-observability-tooling/28-17-SUMMARY.md` when done.
</output>
