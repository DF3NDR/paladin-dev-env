---
phase: 29-program-gates-release
plan: 04
type: execute
wave: 2
depends_on: [29-01, 29-02]
files_modified:
  - .project/v0.10.0/09-program-acceptance-audit.md
  - .planning/phases/29-program-gates-release/29-ACCEPTANCE-AUDIT.md
autonomous: true
requirements: [SHIP-03]

must_haves:
  truths:
    - "`.project/v0.10.0/09-program-acceptance-audit.md` exists with exactly ten `##` sections, one per doc-08 verification-protocol step, each ending in a `Verdict:` line reading `PASS`, `PASS with findings` or `FAIL` (D-10)."
    - "The ten sections appear in doc-08's protocol order 1 through 10, so a reader can map section N to protocol step N positionally without reading the prose (edge: ordering)."
    - "Every FR in docs 01-07 has a row in the per-FR evidence table with columns `FR | owning phase/plan | test anchor(s) | CI status`; an FR whose anchor cell is empty is recorded as a finding and is never rendered as covered (D-11, edge: adjacency)."
    - "A section whose findings list is empty says so explicitly with an empty-list marker rather than omitting the findings heading, so \"no findings\" and \"not checked\" are distinguishable (edge: empty)."
    - "E2E-1/2/3 are recorded green by name with the exact commands run and the observed pass counts, plus the four `paladin-eval` dogfood scenarios from `tests/evals.rs` (doc-08 step 3)."
    - "BUG-01 is recorded with its RED commit `b2d05045` and GREEN commit `8d5ef333`, a re-run grep showing the old warn-and-default-true branch absent at Phase 29 HEAD, and the four living tests named (D-15)."
    - "BUG-02's fix is recorded: `WarGraph::validate()` rejects a stranded self-loop-only node, the regression test is named and re-run, and the audit states whether any test fixture still works around strandedness by wiring stranded nodes to entry (doc-08 step 4)."
    - "Orphan-behavior scope is the measured set of integration/E2E `[[test]]` targets and test files added since `v0.9.0` plus `tests/evals.rs`'s registered scenarios; each traces to at least one FR, X-rule or BUG-0x, and unit tests inside `#[cfg(test)]` modules are explicitly out of scope with the reason stated (D-13)."
    - "The ubiquitous-language table covers all twelve overview §4 terms mapped to the canonical Rust type or module that embodies each, with sanctioned aliases recorded as aliases and the `Frontier`/`Vanguard` split recorded with disposition \"accepted alias, rename deferred\" (D-14)."
    - statement: "Ubiquitous-language conformance is checked by grep over public rustdoc and `docs/src/`, which compares byte-wise over ASCII Rust identifiers — a Unicode look-alike or a case variant of a §4 term would not be matched, so absence of a grep hit is not proof that no competing synonym exists."
      verification: backstop
  prohibitions:
    - statement: "A finding MUST NOT be fixed by changing production Rust behavior under the banner of an audit correction. Allowed fixes are documentation, test additions, FR-citation doc comments, `MIGRATION.md`/allowlist corrections, CI gate text and changelog text; anything else halts for a recorded human decision (D-12, X-03)."
      category: scope-integrity
      status: resolved
      verification: judgment
    - statement: "A public type MUST NOT be renamed to satisfy the ubiquitous-language table. A rename is an X-10 break in a release-gate phase; a naming divergence is FILED with a disposition, never corrected here (D-14)."
      category: release-integrity
      status: resolved
      verification: judgment
  artifacts:
    - path: ".project/v0.10.0/09-program-acceptance-audit.md"
      provides: "The program acceptance audit: ten protocol sections, the per-FR evidence table, orphan-behavior scope, the ubiquitous-language table, BUG-01..04 evidence"
      contains: "Verdict:"
    - path: ".planning/phases/29-program-gates-release/29-ACCEPTANCE-AUDIT.md"
      provides: "Phase-directory pointer to the corpus audit document plus the single overall verdict line, so the GSD verifier finds it"
      contains: "09-program-acceptance-audit.md"
  key_links:
    - from: ".project/v0.10.0/09-program-acceptance-audit.md"
      to: ".project/v0.10.0/08-traceability-matrix.md"
      via: "each ## section executes the correspondingly numbered step of doc-08's ten-step verification protocol"
      pattern: "Verdict:"
    - from: ".planning/phases/29-program-gates-release/29-ACCEPTANCE-AUDIT.md"
      to: ".project/v0.10.0/09-program-acceptance-audit.md"
      via: "the phase-directory pointer file links the corpus document and carries the verdict line"
      pattern: "09-program-acceptance-audit"
---

<objective>
Run doc-08's ten-step verification protocol and write down what it found. This plan creates the audit
document with all ten sections and fills the first five: per-row implementation/test confirmation,
cross-cutting X-rule spot checks, the three program E2E scenarios plus their eval dogfood copies,
BUG-01 and BUG-02 evidence, and the findings pass — every FR that has no passing test, every test that
traces to no FR, and every divergence from the twelve ubiquitous-language terms.

Purpose: SHIP-03, per D-10 through D-15. The corpus is the program's source of truth and is not
archived per milestone the way `.planning/phases/` is, so the audit lives there and the phase
directory gets a pointer.

Output: `.project/v0.10.0/09-program-acceptance-audit.md` with sections 1-5 filled and 6-10 present as
headed placeholders that plan 29-07 (steps 6-9) and plan 29-09 (step 10) complete, plus the phase-dir
pointer file.

This plan has no `type="tracer"` task: it produces an evidence document over already-shipped code and
touches no layered execution path, so a synthetic thin slice would add ceremony without information.
The tracer for this phase is plan 29-01's boot test.

**D-12 is a hard boundary on this plan.** Every step below can produce a finding. Findings are
RECORDED with a proposed disposition. The only fixes this plan may make are documentation text, a test
addition, or a doc-comment citing an FR on an existing test. If a finding needs a production code
change to close, the executor stops and returns a checkpoint naming the finding and the proposed
disposition — it does not choose between fixing in v0.10.0 and deferring.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-program-gates-release/29-CONTEXT.md
@.planning/phases/29-program-gates-release/29-RESEARCH.md
@.planning/phases/29-program-gates-release/29-PATTERNS.md
@.project/v0.10.0/08-traceability-matrix.md
@.project/v0.10.0/00-program-overview.md
@CLAUDE.md

<interfaces>
Measured against HEAD in this planning session:

- The ten-step protocol is `.project/v0.10.0/08-traceability-matrix.md` lines 98-108. Steps 1-5 are
  this plan's scope: (1) per-row implementing code + PRD Test Plan tests + CI-passing acceptance
  criteria; (2) cross-cutting X-01…X-09 per epic (dependency directions via `cargo tree` / import
  review, coverage ≥ 82%, clippy clean); (3) the three program E2E scenarios (overview §6) plus the
  eval dogfood copies (OBS-FR-15); (4) BUG-01 grep-absence + test-first order AND BUG-02's
  `WarGraph::validate()` stranded-self-loop rejection, test-first order, and no fixture working around
  strandedness; (5) file findings — FR with no passing test, test with no FR, ubiquitous-language
  divergence. Steps 6-10 are headed placeholders here.
- FR inventory, by unique `\b(ENG|CF|HITL|FT|RT|PLAT|OBS)-FR-\d+[a-z]?\b` match per corpus doc:
  doc 01 = 28, doc 02 = 21, doc 03 = 20, doc 04 = 24, doc 05 = 27, doc 06 = 19, doc 07 = 20 —
  **159 total**. RESEARCH assumption A2 notes some may be lettered variants of a base FR (e.g.
  `ENG-FR-12a`) that the table may reasonably collapse to one row with its parent; record whichever
  choice is made and its row count.
- Orphan-behavior scope, measured: `git diff --diff-filter=A --name-only v0.9.0 HEAD -- tests/
  'crates/*/tests/'` lists **22** added `.rs` files; `git diff v0.9.0 HEAD -- Cargo.toml
  'crates/*/Cargo.toml' | grep '^+name = '` shows **15** added target-name lines. Re-measure at
  execution time (this plan's own dependencies 29-01 and 29-02 add two more: `v0_9_config_boot` and
  `crates/paladin-web/tests/openapi_golden_v0_9.rs`, both of which must trace to SHIP-02).
- The twelve overview §4 terms, from `.project/v0.10.0/00-program-overview.md:80-93`: **Battlefield,
  Dispatch, Superstep, Waypoint, Thread, Directive, Muster, Parley, Vanguard, Chronicle, Aegis,
  Vault**. §4's own wording sanctions "dispatch rule"/"reducer" as interchangeable for Dispatch and
  describes Vanguard as "the frontier" — both are recorded as sanctioned aliases, not deviations.
  Known candidate deviation: the `Frontier` type in `paladin-battalion::engine` alongside
  `compute_next_vanguard`.
- Existing per-phase mappings to seed D-11's table from, read frontmatter and tables only:
  `.planning/phases/2{2,3,4,5,6,7,8}-*/2x-VERIFICATION.md` (must-have truths to tests) and
  `2x-VALIDATION.md` (requirement to test id to command), plus doc-08's own rows (the G-08 row lists
  seven anchors as `file#test_name` — that is the anchor format to reuse).
- BUG-01 evidence: `.planning/phases/23-control-flow-dynamic-routing-fan-out-subgraphs/23-01-SUMMARY.md`
  records RED `b2d05045` then GREEN `8d5ef333` and the grep-absence statement at line 118. The four
  living tests are `unregistered_custom_condition_is_rejected_before_any_paladin_executes`,
  `unregistered_custom_edge_condition_fails_graph_validation`,
  `every_unregistered_custom_name_is_listed_sorted_and_deduped`,
  `registered_engine_evaluator_true_and_false_route_correctly`.
- E2E targets that already exist: `tests/integration/e2e_crash_resume_test.rs`,
  `tests/integration/e2e_approval_gate_test.rs`, `tests/integration/e2e_muster_defer_order_test.rs`,
  shared fixtures in `tests/helpers/e2e_fixtures.rs`, and `tests/evals.rs` whose registered scenarios
  are `e2e-1-crash-resume`, `e2e-2-approval-gate` (approve and deny) and
  `e2e-3-map-reduce-fault-tolerance` — `cargo test --test evals` reported `4 passed` at Phase 28
  close (`28-CI-EVIDENCE.md`).
- BUG-01..04 classification: overview §7. BUG-02/03/04 are pre-release engine fixes needing no
  §9.1/§9.2 entry by §7's own classification (D-15) — the audit states that and cites §7 rather than
  re-deriving it.
- Evidence-document conventions to mirror: `.planning/phases/27-platform-api/27-CI-EVIDENCE.md` and
  `28-CI-EVIDENCE.md` for the pointer-file shape and the "record the gap, do not omit it" habit
  (`28-17-SUMMARY.md` lines 205-218 is the list of gaps a Phase 29 SHIP reviewer was told to expect).
- The throwaway FR grep is a scratchpad command, not a committed script (D-11 and Claude's
  Discretion: keep it as a fenced command inside the audit document unless it would need zero
  maintenance as `scripts/audit-fr-coverage.sh`; default is the fenced command).
</interfaces>
</context>

<tasks>

<!-- planner-discipline-allow: Verdict: pending -->

<task type="auto">
  <name>Task 1: Create the audit document's ten-section skeleton and the per-FR evidence table</name>
  <files>.project/v0.10.0/09-program-acceptance-audit.md, .planning/phases/29-program-gates-release/29-ACCEPTANCE-AUDIT.md</files>
  <read_first>
    - `.project/v0.10.0/08-traceability-matrix.md` — the whole file: the row format, the G-08 row's
      seven `file#test_name` anchors, and lines 98-108's ten protocol steps verbatim.
    - `.project/v0.10.0/00-program-overview.md` §3 (X-01…X-11), §5 (DoD items 1-7), §6 (the three E2E
      scenario texts) and §7 (BUG-01…04 classifications).
    - `.planning/phases/28-observability-tooling/28-VERIFICATION.md` frontmatter (`human_verification`,
      two items) and `28-17-SUMMARY.md` lines 205-218 ("Open acceptance gaps a Phase 29 SHIP reviewer
      should see").
    - `.planning/phases/27-platform-api/27-CI-EVIDENCE.md` first 40 lines — the phase-dir evidence-doc
      shape the pointer file follows.
    - `.planning/phases/22-*/22-VALIDATION.md` and one other phase's `2x-VALIDATION.md` — the
      requirement-to-test-id-to-command table shape the per-FR table seeds from.
  </read_first>
  <action>
    Create `.project/v0.10.0/09-program-acceptance-audit.md` with a short header stating what it is
    (the post-implementation audit doc-08 reserves as number 09), the date, the HEAD SHA it was run
    at, and the branch. Then create exactly ten `##` sections, numbered and titled to match doc-08's
    protocol steps 1 through 10 in that order. Every section ends with a `Verdict:` line whose value is
    one of `PASS`, `PASS with findings`, `FAIL`, plus a `Findings:` sub-list. When a section has no
    findings write an explicit empty-list marker (a single `- none` item) rather than dropping the
    `Findings:` heading, so a reader can tell "checked, nothing found" from "not checked". Sections 6
    through 10 get their heading, a one-line statement of which plan completes them (29-07 for 6-9,
    29-09 for 10) and `Verdict: pending` — they are placeholders in this plan, not omissions.

    Fill section 1 (per-row confirmation) and section 5's per-FR half with the evidence table. Build
    the seed by grepping `\b(ENG|CF|HITL|FT|RT|PLAT|OBS)-FR-[0-9]+[a-z]?\b` across `tests/`,
    `crates/*/src`, `crates/*/tests`, `src/` and the seven `2x-VALIDATION.md` files from the
    scratchpad, then curate by hand against doc-08's own rows and each phase's
    `2x-VERIFICATION.md`. Table columns are exactly `FR | owning phase/plan | test anchor(s) | CI
    status`, with anchors in doc-08's `file#test_name` form. Record the FR row count and state
    explicitly whether lettered variants were collapsed into their parent row (RESEARCH assumption
    A2). An FR with an empty anchor cell is a finding in section 5's list with a proposed
    disposition of either "anchor added" (a doc comment on an existing test citing the FR — a comment,
    not a behavior change) or "test written"; the latter only where a genuine gap exists, and recorded
    as a deviation. Keep the grep as a fenced command inside the audit document rather than committing
    a script, unless it turns out to need zero maintenance.

    Also fill section 2 (cross-cutting X-rules): record the dependency-direction spot check per epic
    with the command used, the workspace coverage figure and its source, and the clippy result. Where
    a figure can only come from CI, name the workflow and job and mark it as owed to
    `29-CI-EVIDENCE.md` (plan 29-09) rather than inventing a local number.

    Create `.planning/phases/29-program-gates-release/29-ACCEPTANCE-AUDIT.md` containing only a link
    to the corpus document and one overall verdict line, so the GSD verifier finds an artefact in the
    phase directory. Mark the verdict `pending` until plan 29-07 and plan 29-09 complete their
    sections.

    If any step in this task surfaces a finding that needs a production code change, stop and return a
    checkpoint naming the finding, the file, and the proposed disposition (fix in v0.10.0 with a
    recorded decision, or defer to v0.10.1/v0.11 with a `WINDOWS.md` row). Do not make the change and
    do not choose the disposition (D-12, X-03).
  </action>
  <acceptance_criteria>
    - `grep -c '^## ' .project/v0.10.0/09-program-acceptance-audit.md` is 10.
    - `grep -c '^Verdict:' .project/v0.10.0/09-program-acceptance-audit.md` is 10 and every value matches `PASS|PASS with findings|FAIL|pending`.
    - `grep -c 'Findings:' .project/v0.10.0/09-program-acceptance-audit.md` is 10.
    - The ten `##` headings appear in ascending protocol order: `grep -oE '^## [0-9]+' .project/v0.10.0/09-program-acceptance-audit.md | grep -oE '[0-9]+' | sort -c -n` exits 0.
    - The per-FR table has a row per FR: `grep -cE '^\| *`?(ENG|CF|HITL|FT|RT|PLAT|OBS)-FR-[0-9]+' .project/v0.10.0/09-program-acceptance-audit.md` is at least 150, and the document states the exact expected count and the lettered-variant decision.
    - `grep -c 'CI status' .project/v0.10.0/09-program-acceptance-audit.md` is at least 1 and `grep -c 'test anchor' .project/v0.10.0/09-program-acceptance-audit.md` is at least 1.
    - `grep -c '09-program-acceptance-audit.md' .planning/phases/29-program-gates-release/29-ACCEPTANCE-AUDIT.md` is at least 1 and the pointer file is under 30 lines: `test "$(wc -l < .planning/phases/29-program-gates-release/29-ACCEPTANCE-AUDIT.md)" -lt 30`.
    - No `.rs` file changed by this task's commit: `git diff --name-only HEAD~1 | grep -c '\.rs$'` is 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -euo pipefail; A=.project/v0.10.0/09-program-acceptance-audit.md; test "$(grep -c "^## " "$A")" -eq 10; test "$(grep -c "^Verdict:" "$A")" -eq 10; test "$(grep -c "Findings:" "$A")" -eq 10; grep -oE "^## [0-9]+" "$A" | grep -oE "[0-9]+" | sort -c -n; test "$(grep -cE "^\| *.?(ENG|CF|HITL|FT|RT|PLAT|OBS)-FR-[0-9]+" "$A")" -ge 150; grep -q "09-program-acceptance-audit.md" .planning/phases/29-program-gates-release/29-ACCEPTANCE-AUDIT.md'</automated>
  </verify>
  <done>The audit document exists with all ten protocol sections in order, and every one of the program's FRs has a row naming its owning plan and its test anchor — or is a recorded finding.</done>
</task>

<task type="auto">
  <name>Task 2: Fill steps 3 and 4 — the three E2E scenarios green, and BUG-01/BUG-02 evidence at Phase 29 HEAD</name>
  <files>.project/v0.10.0/09-program-acceptance-audit.md</files>
  <read_first>
    - `.project/v0.10.0/09-program-acceptance-audit.md` as written by Task 1.
    - `.project/v0.10.0/00-program-overview.md` §6 (E2E-1/2/3 scenario texts) and §7 (BUG-01…04
      classification, the source for "BUG-02/03/04 need no §9.1/§9.2 entry").
    - `tests/integration/e2e_crash_resume_test.rs`, `tests/integration/e2e_approval_gate_test.rs`,
      `tests/integration/e2e_muster_defer_order_test.rs` — the test function names to cite as anchors.
    - `tests/evals.rs` and root `Cargo.toml` lines 379-432 — the registered eval scenarios and their
      `[[test]]` blocks.
    - `.planning/phases/23-control-flow-dynamic-routing-fan-out-subgraphs/23-01-SUMMARY.md` — BUG-01's
      RED/GREEN SHAs and the grep-absence statement at line 118.
    - The BUG-02 owner: search `crates/paladin-battalion/src/engine/graph.rs` for `WarGraph::validate`
      and its stranded-node rejection, plus the regression test name, and the phase SUMMARY that
      recorded the fix.
  </read_first>
  <action>
    Fill section 3 by actually running the three E2E targets and the eval harness, then recording the
    exact commands and the observed pass counts verbatim — not a summary. Cite each scenario's own test
    function names as `file#test_name` anchors, and map each to its overview §6 scenario text so a
    reader can see E2E-1, E2E-2 and E2E-3 are all represented. Record the four `tests/evals.rs`
    scenario names as the OBS-FR-15 dogfood copies and note that they exercise the same fixtures
    (`tests/helpers/e2e_fixtures.rs`) rather than a parallel implementation.

    Fill section 4 with BUG-01's and BUG-02's evidence. For BUG-01: cite RED `b2d05045` and GREEN
    `8d5ef333` in that order as the test-first proof, re-run the grep for the old warn-and-default-true
    branch across `crates/` and `src/` at this HEAD and record the command with its exact match count,
    and name the four living tests listed in `<interfaces>`, each with the file it lives in. For
    BUG-02: run the regression test that proves `WarGraph::validate()` rejects a stranded
    self-loop-only node, record its name and result, confirm the fix landed test-first by citing the
    commit pair, and state whether any test fixture still wires a stranded node to entry to work
    around strandedness — WINDOWS.md rows 24 and 25 record exactly that pattern for `loop_gate` and
    `self_loop_graph`, so cross-reference them and record the answer rather than leaving it implied.
    Close the section by citing overview §7 for BUG-02/03/04 needing no §9.1/§9.2 entry — cite it, do
    not re-derive it.

    Set each filled section's `Verdict:` line from what was actually observed. A grep that returns a
    match, an E2E target that fails, or a fixture workaround still present makes the verdict
    `PASS with findings` or `FAIL` with the finding listed — never a `PASS` with the detail buried in
    prose. If closing any finding would need a production code change, stop and return a checkpoint per
    D-12.
  </action>
  <acceptance_criteria>
    - `cargo test --test e2e_crash_resume --test e2e_approval_gate --test e2e_muster_defer_order 2>&1 | grep -cE 'test result: ok'` is 3, and the audit records the same three commands with their pass counts.
    - `cargo test --test evals 2>&1 | grep -qE 'test result: ok\. 4 passed'`, and the audit names all four scenarios.
    - `grep -c 'b2d05045' .project/v0.10.0/09-program-acceptance-audit.md` is at least 1 and `grep -c '8d5ef333' .project/v0.10.0/09-program-acceptance-audit.md` is at least 1.
    - The old BUG-01 branch is absent at HEAD and the audit records the command and the count: running the recorded grep over `crates/` and `src/` returns 0 matches. <!-- planner-discipline-allow: defaulting to true -->
    - `grep -c 'unregistered_custom_condition_is_rejected_before_any_paladin_executes' .project/v0.10.0/09-program-acceptance-audit.md` is at least 1 and all four BUG-01 test names appear: `grep -cE 'unregistered_custom_edge_condition_fails_graph_validation|every_unregistered_custom_name_is_listed_sorted_and_deduped|registered_engine_evaluator_true_and_false_route_correctly' .project/v0.10.0/09-program-acceptance-audit.md` is at least 3.
    - `grep -c 'WarGraph::validate' .project/v0.10.0/09-program-acceptance-audit.md` is at least 1 and the section cites WINDOWS.md rows 24 and 25 by number.
    - Sections 3 and 4 no longer read `Verdict: pending`: `awk '/^## 3/,/^## 5/' .project/v0.10.0/09-program-acceptance-audit.md | grep -c 'Verdict: pending'` is 0.
    - No `.rs` file changed by this task's commit: `git diff --name-only HEAD~1 | grep -c '\.rs$'` is 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -euo pipefail; A=.project/v0.10.0/09-program-acceptance-audit.md; cargo test --test evals 2>&amp;1 | grep -qE "test result: ok\. 4 passed"; test "$(cargo test --test e2e_crash_resume --test e2e_approval_gate --test e2e_muster_defer_order 2>&amp;1 | grep -cE "test result: ok")" -eq 3; grep -q b2d05045 "$A"; grep -q 8d5ef333 "$A"; grep -q "WarGraph::validate" "$A"; test "$(awk "/^## 3/,/^## 5/" "$A" | grep -c "Verdict: pending")" -eq 0'</automated>
  </verify>
  <done>The three program acceptance scenarios and their eval dogfood copies are recorded green with real commands and counts, and BUG-01's and BUG-02's fixes are re-verified at this HEAD with SHAs, greps and named tests rather than restated from a prior phase's summary.</done>
</task>

<task type="auto">
  <name>Task 3: Fill step 5's findings pass — orphan behavior and the twelve-term ubiquitous-language table</name>
  <files>.project/v0.10.0/09-program-acceptance-audit.md</files>
  <read_first>
    - `.project/v0.10.0/09-program-acceptance-audit.md` as written by Tasks 1 and 2, especially the
      per-FR table.
    - `.project/v0.10.0/00-program-overview.md` lines 75-93 — the twelve §4 terms and the sanctioned
      aliases in their own wording.
    - The output of `git diff --diff-filter=A --name-only v0.9.0 HEAD -- tests/ 'crates/*/tests/'` and
      `git diff v0.9.0 HEAD -- Cargo.toml 'crates/*/Cargo.toml'` — the measured orphan-behavior scope
      at execution time, which now includes 29-01's and 29-02's two new targets.
    - `crates/paladin-battalion/src/engine/` — the `Frontier` type and `compute_next_vanguard`, the
      known naming split; and each §4 term's canonical type (`Battlefield`, `Waypoint`, `Directive`,
      `MusterTask`, `ParleyRequest`, `Chronicle`, `Aegis`, `Vault`) so the mapping column cites a real
      path.
    - `.planning/phases/29-program-gates-release/29-CONTEXT.md` D-13 and D-14.
  </read_first>
  <action>
    Fill section 5's orphan-behavior half. Measure the scope, do not assume it: list every `[[test]]`
    block present in HEAD's root `Cargo.toml` or a crate `Cargo.toml` but absent at `v0.9.0`, every
    file added under `tests/integration/` and `crates/*/tests/` since the tag, and `tests/evals.rs`'s
    registered scenarios. Record the measured counts alongside the planning-time figures (22 added
    `.rs` files, 15 added target-name lines) so a discrepancy is visible rather than silent. Map each
    to at least one FR, X-rule or BUG-0x owner — `v0_9_config_boot` and
    `crates/paladin-web/tests/openapi_golden_v0_9.rs` both own SHIP-02. Anything with no owner is a
    finding. State explicitly that unit tests inside `#[cfg(test)]` modules are out of scope and why
    (doc-08 step 5 targets behaviors, and integration tests are the witnesses for behaviors), so the
    exclusion is a recorded decision rather than an apparent omission.

    Fill section 5's ubiquitous-language half with a table of all twelve terms — Battlefield,
    Dispatch, Superstep, Waypoint, Thread, Directive, Muster, Parley, Vanguard, Chronicle, Aegis,
    Vault — each mapped to the canonical Rust type or module path that embodies it. Then grep public
    rustdoc and `docs/src/` for competing synonyms (`checkpoint` against Waypoint, `frontier` against
    Vanguard, `reducer` against Dispatch), record each hit count and classify it: `reducer` and
    `frontier` are sanctioned aliases by §4's own wording and are recorded as such, not as deviations.
    Record the `Frontier` type in `paladin-battalion::engine` alongside `compute_next_vanguard` as a
    naming split with disposition "accepted alias, rename deferred", citing that a public-type rename
    is an X-10 break in a release-gate phase, and note the deferred idea. Do not rename anything.

    State plainly, in the section, the limitation of the method: the greps compare byte-wise over ASCII
    Rust identifiers, so a Unicode look-alike or a case variant of a §4 term would not be matched and
    absence of a hit is not proof that no competing synonym exists. That sentence is what makes this
    section's claim honest about its own reach.

    Set section 5's `Verdict:` from the findings, and update the `29-ACCEPTANCE-AUDIT.md` pointer's
    verdict line to reflect sections 1-5 being complete while 6-10 remain pending. If any finding needs
    a production code change, stop and return a checkpoint per D-12.
  </action>
  <acceptance_criteria>
    - All twelve §4 terms appear in the audit's language table: `for t in Battlefield Dispatch Superstep Waypoint Thread Directive Muster Parley Vanguard Chronicle Aegis Vault; do grep -q "$t" .project/v0.10.0/09-program-acceptance-audit.md || exit 1; done` exits 0.
    - `grep -c 'Frontier' .project/v0.10.0/09-program-acceptance-audit.md` is at least 1 and `grep -c 'rename deferred' .project/v0.10.0/09-program-acceptance-audit.md` is at least 1.
    - `grep -c 'compute_next_vanguard' .project/v0.10.0/09-program-acceptance-audit.md` is at least 1.
    - `grep -c 'v0_9_config_boot' .project/v0.10.0/09-program-acceptance-audit.md` is at least 1 and `grep -c 'openapi_golden_v0_9' .project/v0.10.0/09-program-acceptance-audit.md` is at least 1 — this phase's own two new targets are inside the orphan-behavior scope, not exempt from it.
    - `grep -c 'cfg(test)' .project/v0.10.0/09-program-acceptance-audit.md` is at least 1 — the unit-test exclusion is stated, not implied.
    - `awk '/^## 5/,/^## 6/' .project/v0.10.0/09-program-acceptance-audit.md | grep -c 'Verdict: pending'` is 0.
    - `grep -ci 'byte-wise\|byte wise' .project/v0.10.0/09-program-acceptance-audit.md` is at least 1 — the grep method's own limitation is recorded.
    - No public Rust item renamed: `git diff --name-only HEAD~1 | grep -c '\.rs$'` is 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -euo pipefail; A=.project/v0.10.0/09-program-acceptance-audit.md; for t in Battlefield Dispatch Superstep Waypoint Thread Directive Muster Parley Vanguard Chronicle Aegis Vault; do grep -q "$t" "$A"; done; grep -q "rename deferred" "$A"; grep -q v0_9_config_boot "$A"; grep -q openapi_golden_v0_9 "$A"; test "$(awk "/^## 5/,/^## 6/" "$A" | grep -c "Verdict: pending")" -eq 0; test "$(git diff --name-only HEAD~1 | grep -c "\.rs\$")" -eq 0'</automated>
  </verify>
  <done>Every test target added since v0.9.0 traces to an owner or is a recorded finding, all twelve ubiquitous-language terms are mapped to real code with divergences filed rather than renamed, and the section states the limits of its own grep method.</done>
</task>

</tasks>

<threat_model>
> Assessed at **ASVS Level 1** (`workflow.security_asvs_level = 1`); **`high`-severity threats block the phase** (`workflow.security_block_on = high`); `medium`/`low` are dispositioned per the register below.
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prior phases' VERIFICATION/VALIDATION claims → this audit's verdicts | An audit that restates a prior summary instead of re-running the check launders an unverified claim into a release gate. |
| Audit finding → production code | The temptation to close a finding by changing behavior is exactly what X-03 forbids in a release-gate phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-04-01 | Repudiation | A `PASS` verdict recorded without re-run evidence | high | mitigate | Every filled section records the exact command and its observed output; the acceptance criteria re-run the E2E targets, the eval harness and the BUG-01 grep rather than reading the audit's own prose. |
| T-29-04-02 | Tampering | A production behavior change made to close an audit finding | high | mitigate | D-12 bounds the allowed fix set, the plan carries it as a prohibition, and each task's acceptance criteria assert no `.rs` file changed in that task's commit. |
| T-29-04-03 | Repudiation | A finding silently omitted because a section had no findings heading | medium | mitigate | All ten sections carry a `Findings:` heading with an explicit `- none` marker when empty, asserted by a criterion counting ten of each. |
| T-29-04-04 | Information disclosure | The audit quoting credentials or response bodies while citing evidence | low | mitigate | Evidence is commands, counts, SHAs and test names — no configuration values, no response bodies; the per-FR table's anchor column is `file#test_name` only. |
</threat_model>

<verification>
- The audit document has ten ordered sections, ten `Verdict:` lines and ten `Findings:` headings.
- `cargo test --test e2e_crash_resume --test e2e_approval_gate --test e2e_muster_defer_order` and
  `cargo test --test evals` both green, with the recorded counts matching the observed ones.
- The BUG-01 grep returns zero matches at this HEAD, recorded with its command.
- No `.rs` file is modified by any task in this plan.
</verification>

<success_criteria>
- SHIP-03's steps 1-5 are executed and written down, with every FR either anchored to a passing test
  or recorded as a finding, and every test target added since v0.9.0 either owned or recorded as an
  orphan.
- The phase directory carries `29-ACCEPTANCE-AUDIT.md` so the verifier finds the artefact, with a
  verdict line that honestly says which sections are still pending.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

See the identical section in `29-01-PLAN.md` — the canonical list for Phase 29. This plan's own
contribution: `.project/v0.10.0/09-program-acceptance-audit.md` (ten-section skeleton, the per-FR
evidence table, sections 1-5 filled) and
`.planning/phases/29-program-gates-release/29-ACCEPTANCE-AUDIT.md` (pointer + verdict line). No Rust
file, no new symbol, no dependency, no behavior change.
</artifacts_produced>

<output>
Create `.planning/phases/29-program-gates-release/29-04-SUMMARY.md` when done.
</output>
