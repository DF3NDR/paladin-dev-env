---
phase: 31-lossless-token-accounting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/paladin-core/src/platform/container/token_usage.rs
  - crates/paladin-core/src/platform/container/herald.rs
  - crates/paladin-battalion/src/grove_service.rs
  - crates/paladin-content/src/services/content_llm_analysis_service.rs
  - crates/paladin-eval/src/scripted_llm.rs
  - crates/paladin-herald/src/json_herald.rs
  - crates/paladin-herald/src/markdown_herald.rs
  - crates/paladin-herald/src/table_herald.rs
  - crates/paladin-llm/benches/llm_serialization_benchmarks.rs
  - crates/paladin-llm/src/anthropic/adapter.rs
  - crates/paladin-llm/src/compat/engine.rs
  - crates/paladin-llm/src/deepseek/adapter.rs
  - crates/paladin-llm/src/gemini/adapter.rs
  - crates/paladin-llm/src/llm_analysis_service.rs
  - crates/paladin-llm/src/mock.rs
  - crates/paladin-llm/src/openai/adapter.rs
  - crates/paladin-memory/src/services/memory_extraction_service.rs
  - crates/paladin-ports/src/output/llm_port.rs
  - examples/herald_custom_formatter.rs
  - examples/herald_json_output.rs
  - examples/herald_markdown_output.rs
  - examples/herald_streaming.rs
  - src/application/services/paladin/paladin_execution_service.rs
  - src/application/services/paladin/planning_service.rs
  - src/application/services/paladin/prompt_generation_service.rs
  - src/application/services/paladin/temperature_service.rs
  - tests/functional/content_llm_analysis_pipeline_test.rs
  - tests/functional/paladin_tool_invocation_test.rs
  - tests/helpers/mock_llm_adapter.rs
  - tests/integration/arsenal_bridge_regression_test.rs
  - tests/integration/autonomous_planning_test.rs
  - tests/integration/battalion/grove_integration_test.rs
  - tests/integration/battalion_herald_end_to_end_test.rs
  - tests/integration/context_injection_test.rs
  - tests/integration/herald_integration_test.rs
  - tests/integration/paladin_garrison_integration_test.rs
  - tests/unit/arsenal/handoff_tool_test.rs
  - tests/unit/herald_consolidation_test.rs
  - tests/unit/paladin_builder_arsenal_test.rs
  - tests/unit/paladin_builder_test.rs
  - tests/unit/paladin_execution_service_test.rs
autonomous: false
requirements: [ACCT-01]
must_haves:
  truths:
    - "TokenUsage carries cache_read_tokens, cache_write_tokens and reasoning_tokens as #[serde(default)] Option<u32> after the three existing counters, and its rustdoc states that total_tokens includes them plus the two inequalities cache_read_tokens + cache_write_tokens <= prompt_tokens and reasoning_tokens <= completion_tokens (D-01, D-02)"
    - "TokenUsage arithmetic saturates at u32::MAX and never panics or wraps: a unit test asserts TokenUsage::new(u32::MAX, 1) + TokenUsage::new(1, 0) yields prompt_tokens == u32::MAX and total_tokens == u32::MAX, and that TokenUsage::default() is the additive identity (x + default == x for a fully-populated x)"
    - "No rounding exists (integer counts); after every Add/AddAssign/Sum the total_tokens field is recomputed as prompt_tokens.saturating_add(completion_tokens) so the D-02 invariant total == prompt + completion holds; the Option merge rule is None+None=None, None+Some(x)=Some(x), Some(a)+Some(b)=Some(a.saturating_add(b)) — each case asserted by its own unit test in token_usage.rs"
    - "A JSON document written before this phase (prompt_tokens/completion_tokens/total_tokens only) deserialises into a TokenUsage whose three optionals are all None, and a document carrying all six keys round-trips byte-identically (ACCT-01, D-03)"
    - "TokenUsage::new(p, c) still leaves the three optionals None, and with_cache_read/with_cache_write/with_reasoning are the only way to set them — no constructor fabricates Some(0) (D-03, D-04)"
  artifacts:
    - "crates/paladin-core/src/platform/container/token_usage.rs — three new Option<u32> fields, three with_* builders, impl Add/AddAssign/Sum, expanded rustdoc, new serde and arithmetic tests"
  key_links:
    - "TokenUsage::new + with_* builders replace every in-tree full TokenUsage struct literal in the same commit as the field addition (D-04) — the workspace does not compile until every one migrates"
    - "impl Add/AddAssign/Sum on the type is the single accumulation implementation every later plan's accumulator (execution-service loop, Formation/Phalanx maps, TraceDispatcher, eval runner) defers to (D-06)"
---

<objective>
Give `TokenUsage` the three optional sub-counts and the saturating accumulation arithmetic every
later plan in this phase depends on, and migrate every in-tree full struct literal to the
constructor + builders in the same commit so the workspace stays green (ACCT-01; D-01, D-02, D-03,
D-04, D-06).

Purpose: `TokenUsage` is the one value type the whole phase carries. Until it can express
"the provider reported 100 cache-read tokens" and "these two usages sum to that usage", no carrier
above it can be lossless. `from_total` is NOT removed here — it is removed in plan 31-02 where
Formation/Phalanx get their real fix, so this plan never has to write the zeroing bug a second time.

Output: a richer, additive-tolerant `TokenUsage` with doc-tested builders and arithmetic, plus a
workspace that compiles and tests green on the new shape.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-lossless-token-accounting/31-CONTEXT.md
@.planning/phases/31-lossless-token-accounting/31-RESEARCH.md
@.planning/phases/31-lossless-token-accounting/31-PATTERNS.md
@CLAUDE.md
</context>

## Artifacts this phase produces (plan 31-01 slice)

New symbols created by this plan — newly-created, so excluded from drift verification against the
pre-phase tree:

- `TokenUsage.cache_read_tokens: Option<u32>`, `TokenUsage.cache_write_tokens: Option<u32>`,
  `TokenUsage.reasoning_tokens: Option<u32>` (new public fields)
- `TokenUsage::with_cache_read(u32) -> Self`, `TokenUsage::with_cache_write(u32) -> Self`,
  `TokenUsage::with_reasoning(u32) -> Self` (new chainable builders)
- `impl std::ops::Add for TokenUsage`, `impl std::ops::AddAssign for TokenUsage`,
  `impl std::iter::Sum for TokenUsage` (new trait impls)
- A private `TokenUsage` optional-merge helper (name at executor discretion; D-06 grants it)
- New unit tests in `token_usage.rs`: saturating-boundary, additive-identity, three Option-merge
  cases, legacy-JSON-without-optionals deserialisation, six-key round-trip

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Checkpoint: confirm the one-way wire and public-API shape for Phase 31</name>
  <action>Present the one-way door this phase walks through, once, covering the whole carrier break so no later plan needs its own stop. Do not start task 2 until the developer answers.</action>
  <decision>Land the Phase 31 serialized/public shape exactly as CONTEXT.md locks it: `TokenUsage` gains three `Option&lt;u32&gt;` sub-counts with `total_tokens` INCLUDING them (D-01, D-02); the carrier field is named `usage` on `PaladinResult`, `NodeExecutionRecord`, `TraceEvent::NodeFinished` and `TraceEvent::RunFinished` (D-07); `StreamingResponse` gains `usage` and is marked `#[non_exhaustive]` (D-13); the HTTP edge carries a `TokenUsageResponse` object instead of a bare count (D-24).</decision>
  <context>Each of these four is rated one-way in CONTEXT.md. `TokenUsage`'s field set and the meaning of `total_tokens` become the persisted Waypoint/trace JSON shape, the SSE payload shape and the input to Milestone 14's pricing function; the carrier field names are public fields on serialized types the downstream app and SSE clients already read; `StreamingResponse` is a port item type every provider adapter constructs; `ExecuteResponse` is a published HTTP response contract with a generated Python client. Undoing any of them after Phase 32/33 build on it is a migration of persisted rows plus a second published-contract break, not a refactor. ADR-0051 supersedes X-03 for Phases 31-33 so the break is sanctioned — this checkpoint confirms the shape, not the permission.</context>
  <options>
    <option id="proceed-as-decided">
      <name>Proceed with the CONTEXT.md shape (D-01, D-02, D-07, D-13, D-24)</name>
      <pros>Keystone shape lands once; Phase 32 and Milestone 14 build on it; total-based consumers (TokenBudget, total_tokens_max eval assertion, BattalionResult.total_tokens) keep identical semantics because total_tokens is inclusive.</pros>
      <cons>Every downstream full struct literal of the touched types breaks at 0.10.0; Anthropic's prompt_tokens becomes a larger, cache-inclusive figure for cached calls.</cons>
    </option>
    <option id="amend-before-landing">
      <name>Amend one or more of the four shapes before any code lands</name>
      <pros>Cheapest possible moment to change the field set, the inclusive-total contract, a field name, or the HTTP DTO.</pros>
      <cons>Requires re-running /gsd-discuss-phase 31 for the amended decision and re-planning the affected plans; blocks the phase.</cons>
    </option>
  </options>
  <resume-signal>Select: proceed-as-decided, or amend-before-landing and state which of D-01/D-02/D-07/D-13/D-24 changes</resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>Task 1: TokenUsage gains the three sub-counts, builders and saturating arithmetic</name>
  <reversibility rating="one-way">The six-field `TokenUsage` and the `total_tokens`-includes-sub-counts contract become the persisted Waypoint/trace JSON shape and the input to Milestone 14's pricing function; changing either later means migrating stored rows and re-breaking a published contract.</reversibility>
  <files>crates/paladin-core/src/platform/container/token_usage.rs</files>
  <read_first>
    - crates/paladin-core/src/platform/container/token_usage.rs (the whole 76-line file — current struct, both constructors, all four tests)
    - .planning/phases/31-lossless-token-accounting/31-PATTERNS.md, section "crates/paladin-core/src/platform/container/token_usage.rs" (the exact target shape and the Option-merge helper)
    - .planning/phases/31-lossless-token-accounting/31-CONTEXT.md, decisions D-01 through D-06
    - crates/paladin-core/src/platform/container/waypoint.rs lines 575-600 (the house rustdoc style for a `#[serde(default)]` field that must not break old rows)
  </read_first>
  <behavior>
    - `TokenUsage::new(10, 5)` still yields prompt 10, completion 5, total 15, and all three optionals `None`.
    - `TokenUsage::new(10, 5).with_cache_read(4).with_cache_write(2).with_reasoning(3)` yields `Some(4)`, `Some(2)`, `Some(3)` and leaves prompt/completion/total untouched.
    - `TokenUsage::new(u32::MAX, 1) + TokenUsage::new(1, 0)` yields `prompt_tokens == u32::MAX`, `completion_tokens == 1`, `total_tokens == u32::MAX` — saturating, no panic, no wrap.
    - `x + TokenUsage::default() == x` for a fully-populated `x` (additive identity).
    - Option merge: `None + None == None`; `None + Some(7) == Some(7)`; `Some(7) + Some(5) == Some(12)`; `Some(u32::MAX) + Some(1) == Some(u32::MAX)`.
    - After any `Add`/`AddAssign`/`Sum`, `total_tokens == prompt_tokens + completion_tokens` (saturating).
    - `[a, b, c].into_iter().sum::&lt;TokenUsage&gt;()` equals `a + b + c`.
    - Deserialising `{"prompt_tokens":10,"completion_tokens":5,"total_tokens":15}` yields all three optionals `None`.
    - Serialising then deserialising a six-field value returns an equal value, and the serialised JSON contains all six keys (the three optionals as `null` when `None` — no `skip_serializing_if`, D-21 depends on this).
  </behavior>
  <action>
Write the failing tests first, then the implementation (project TDD rule, red-green-refactor).

Add three public fields to `TokenUsage` in field order AFTER `total_tokens`, each
`Option&lt;u32&gt;` and each carrying `#[serde(default)]`: `cache_read_tokens`, `cache_write_tokens`,
`reasoning_tokens` (D-01). Do NOT add `skip_serializing_if` — the JSON key set must stay stable at
six keys so the herald in plan 31-05 can emit `null` for an unreported figure (D-21). Do NOT mark
the struct `#[non_exhaustive]`: it derives `Default`, and functional-update syntax is disallowed
cross-crate on a `#[non_exhaustive]` struct (D-04, X-10.3 option (b), the `PaladinResult`/`Settings`
precedent already recorded in MIGRATION.md §9.2).

Extend the struct rustdoc to state, in one sentence, that `total_tokens` INCLUDES cache and
reasoning tokens — `prompt_tokens` is every input token the provider billed (cache reads and cache
writes included), `completion_tokens` is every output token (reasoning/thinking included) — and to
state both inequalities explicitly: `cache_read_tokens + cache_write_tokens <= prompt_tokens` and
`reasoning_tokens <= completion_tokens` (D-02). State that `None` means the provider did not report
the figure and `Some(0)` means the provider reported zero (D-03). State that the counters are `u32`
and every accumulation saturates rather than wrapping (D-06). Exact wording is yours.

Add three chainable builders: `with_cache_read(self, u32) -> Self`, `with_cache_write(self, u32) -> Self`,
`with_reasoning(self, u32) -> Self`. Each sets its field to `Some(n)` and returns `self`. Each needs
a rustdoc example that compiles under `cargo test --doc` (project rule: all public items documented
with doc tests). Leave `TokenUsage::new(prompt, completion)`'s signature and body semantics
unchanged except that it now leaves the three optionals `None`.

Add `impl std::ops::Add for TokenUsage`, `impl std::ops::AddAssign for TokenUsage` and
`impl std::iter::Sum for TokenUsage` following the shape in 31-PATTERNS.md: every `u32` add is
`saturating_add`; `total_tokens` is RECOMPUTED as `prompt_tokens.saturating_add(completion_tokens)`
after the add so the D-02 invariant survives summation; the three optionals merge through one
private helper implementing `None + None = None`, `None + Some(x) = Some(x)`,
`Some(a) + Some(b) = Some(a.saturating_add(b))` (D-06). `Sum` folds from `TokenUsage::default()`.
`new` itself still uses plain `+` today — change it to `saturating_add` too so no constructor can
panic in a release-mode overflow (library code must not panic — CLAUDE.md).

Leave `TokenUsage::from_total` in place in this plan; plan 31-02 deletes it together with the
Formation/Phalanx call sites it exists to serve.

Add the new unit tests inside the existing `#[cfg(test)] mod tests` block, mirroring the literal-
assertion style of the four tests already there. Use distinct non-round figures (the house style —
`1_234`, `567`, `100`, `50`, `200`) so a swapped or zeroed field cannot pass by coincidence.
  </action>
  <verify>
    <automated>cargo test -p paladin-ai-core --lib token_usage &amp;&amp; cargo test -p paladin-ai-core --doc token_usage</automated>
  </verify>
  <acceptance_criteria>
    - `crates/paladin-core/src/platform/container/token_usage.rs` contains `pub cache_read_tokens: Option<u32>`, `pub cache_write_tokens: Option<u32>` and `pub reasoning_tokens: Option<u32>`, each immediately preceded by a `#[serde(default)]` attribute line
    - The same file contains `pub fn with_cache_read`, `pub fn with_cache_write` and `pub fn with_reasoning`
    - The same file contains `impl std::ops::Add for TokenUsage`, `impl std::ops::AddAssign for TokenUsage` and `impl std::iter::Sum for TokenUsage` (or the equivalent `use`-shortened paths)
    - `grep -c 'saturating_add' crates/paladin-core/src/platform/container/token_usage.rs` returns at least 6
    - The struct rustdoc contains both inequality statements (cache read plus cache write not exceeding prompt tokens; reasoning not exceeding completion tokens) and the sentence that the total includes them
    - `cargo test -p paladin-ai-core --lib token_usage` exits 0 with the nine new behaviors above all asserted
    - `cargo test -p paladin-ai-core --doc token_usage` exits 0 (the three builders' doc examples compile and run)
    - The file still contains no `unwrap(`, `expect(` or `panic!` outside the `#[cfg(test)]` module
  </acceptance_criteria>
  <done>`TokenUsage` carries the three optional sub-counts with documented inclusive-total semantics, saturating Add/AddAssign/Sum with the D-06 Option-merge rule, and doc-tested builders; `paladin-ai-core`'s own tests and doctests are green.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate every in-tree full TokenUsage struct literal to the constructor and builders</name>
  <files>crates/paladin-core/src/platform/container/herald.rs, crates/paladin-battalion/src/grove_service.rs, crates/paladin-content/src/services/content_llm_analysis_service.rs, crates/paladin-eval/src/scripted_llm.rs, crates/paladin-herald/src/json_herald.rs, crates/paladin-herald/src/markdown_herald.rs, crates/paladin-herald/src/table_herald.rs, crates/paladin-llm/benches/llm_serialization_benchmarks.rs, crates/paladin-llm/src/anthropic/adapter.rs, crates/paladin-llm/src/compat/engine.rs, crates/paladin-llm/src/deepseek/adapter.rs, crates/paladin-llm/src/gemini/adapter.rs, crates/paladin-llm/src/llm_analysis_service.rs, crates/paladin-llm/src/mock.rs, crates/paladin-llm/src/openai/adapter.rs, crates/paladin-memory/src/services/memory_extraction_service.rs, crates/paladin-ports/src/output/llm_port.rs, examples/herald_custom_formatter.rs, examples/herald_json_output.rs, examples/herald_markdown_output.rs, examples/herald_streaming.rs, src/application/services/paladin/paladin_execution_service.rs, src/application/services/paladin/planning_service.rs, src/application/services/paladin/prompt_generation_service.rs, src/application/services/paladin/temperature_service.rs, tests/functional/content_llm_analysis_pipeline_test.rs, tests/functional/paladin_tool_invocation_test.rs, tests/helpers/mock_llm_adapter.rs, tests/integration/arsenal_bridge_regression_test.rs, tests/integration/autonomous_planning_test.rs, tests/integration/battalion/grove_integration_test.rs, tests/integration/battalion_herald_end_to_end_test.rs, tests/integration/context_injection_test.rs, tests/integration/herald_integration_test.rs, tests/integration/paladin_garrison_integration_test.rs, tests/unit/arsenal/handoff_tool_test.rs, tests/unit/herald_consolidation_test.rs, tests/unit/paladin_builder_arsenal_test.rs, tests/unit/paladin_builder_test.rs, tests/unit/paladin_execution_service_test.rs (plus any further site `cargo check` reports)</files>
  <read_first>
    - crates/paladin-core/src/platform/container/token_usage.rs (as left by task 1 — the constructor and builder signatures to migrate to)
    - crates/paladin-ports/src/output/vision_port.rs (the `VisionTokenUsage` type — a DIFFERENT type that also matches a naive `TokenUsage {` grep; it is untouched by this phase per D-20)
    - crates/paladin-llm/src/mock.rs (`with_token_usage_struct` and the literals around it — the highest-traffic fixture site)
    - crates/paladin-core/src/platform/container/herald.rs (`ExecutionMetadata.token_usage` — already a full `TokenUsage`, keeps its name per D-07)
  </read_first>
  <action>
Adding public fields to a constructible struct breaks every full struct literal of it. Work
compiler-driven, not file-by-file: run `cargo check --workspace --all-targets --all-features`, and
fix exactly the sites it reports. There are roughly 96 literal sites across ~44 files
(`crates/`, `src/`, `tests/`, `examples/`, `benches/`).

For each reported site, apply whichever of these two migrations preserves the existing values
exactly (D-04):
  - when the literal's `total_tokens` equals its `prompt_tokens + completion_tokens`, replace the
    whole literal with `TokenUsage::new(prompt, completion)`, chaining `.with_cache_read(n)` /
    `.with_cache_write(n)` / `.with_reasoning(n)` only where the site genuinely means to set one
    (no site does today — leave them off);
  - when the literal's `total_tokens` deliberately differs from `prompt + completion` (a fixture
    testing an inconsistent provider figure), keep the struct literal and append
    `..Default::default()` so the three new fields default to `None`.

Never change a numeric value while migrating: a test asserting `total_tokens == 500` must still see
500. Never introduce `Some(0)` for a field the site does not set (D-03).

`VisionTokenUsage` (`crates/paladin-ports/src/output/vision_port.rs`, `openai/vision.rs`,
`anthropic/vision.rs`) is a different type and is explicitly out of scope (D-20) — leave its
literals alone. `ExecutionMetadata.token_usage` (`herald.rs`) keeps its field name (D-07); only its
literal construction migrates if it is a full literal.

Re-run `cargo check --workspace --all-targets --all-features` until it exits 0, then
`cargo test --workspace --all-features`, then `cargo fmt` and
`cargo clippy --workspace --all-targets --all-features -- -D warnings`.
  </action>
  <verify>
    <automated>cargo check --workspace --all-targets --all-features &amp;&amp; cargo test --workspace --all-features &amp;&amp; cargo fmt --check &amp;&amp; cargo clippy --workspace --all-targets --all-features -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `cargo check --workspace --all-targets --all-features` exits 0
    - `cargo test --workspace --all-features` exits 0 with no test's expected token figure changed from its pre-plan value
    - `cargo fmt --check` exits 0 and `cargo clippy --workspace --all-targets --all-features -- -D warnings` exits 0
    - `grep -rn 'cache_read_tokens: Some(0)\|cache_write_tokens: Some(0)\|reasoning_tokens: Some(0)' --include=*.rs crates src tests examples` returns no matches (no fabricated zero, D-03)
    - `crates/paladin-ports/src/output/vision_port.rs` is unchanged (`git diff --stat` lists no line change for it)
  </acceptance_criteria>
  <done>The whole workspace compiles, tests, formats and lints clean on the six-field `TokenUsage`, with every in-tree full literal expressed through `TokenUsage::new` plus builders or `..Default::default()`, and no numeric fixture value altered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider response body → adapter serde structs | untrusted numeric input crosses here; unchanged by this plan (adapters are only migrated to the constructor, not re-parsed) |
| persisted Waypoint/trace JSON → `TokenUsage` deserialisation | untrusted-at-rest JSON crosses here; the three new fields widen the accepted key set |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-01 | Tampering | `TokenUsage` arithmetic (`Add`/`AddAssign`/`Sum`, `new`) | medium | mitigate | Every `u32` add is `saturating_add`, including inside `new`, so a hostile or corrupt figure can produce a clamped total but never a wrapped (silently tiny) one nor a panic; the boundary unit test asserts `u32::MAX` clamping explicitly |
| T-31-02 | Denial of Service | `TokenUsage` deserialisation of persisted/provider JSON | low | mitigate | The three new fields are `Option<u32>` behind `#[serde(default)]`; `serde_json` rejects non-numeric, negative and out-of-range values into a `u32` and returns `Err`, never panics; no new allocation is introduced |
| T-31-03 | Information Disclosure | new rustdoc/doc-test examples in `token_usage.rs` | low | mitigate | Doc examples use only numeric token literals; no credential-shaped string is introduced (keeps `rust/hard-coded-cryptographic-value` from gaining a new false positive — see `CODEQL-DISMISSALS.md`) |
| T-31-SC | Tampering | package-manager installs | low | accept | Not applicable — this plan adds no dependency to any `Cargo.toml` (RESEARCH.md § Package Legitimacy Audit); `make security` (cargo-audit + cargo-deny) still runs at the phase gate in plan 31-07 |
</threat_model>

<verification>
- `cargo test -p paladin-ai-core --lib token_usage` and `cargo test -p paladin-ai-core --doc token_usage` green
- `cargo check --workspace --all-targets --all-features` exits 0
- `cargo test --workspace --all-features` green
- `make clean-code` (fmt + clippy + check) green
- No numeric token figure in any existing test changed value
</verification>

<success_criteria>
ACCT-01's type-level half is done: `TokenUsage` carries three `#[serde(default)]` optionals with a
rustdoc that states the inclusive-total contract and both inequalities; legacy JSON without the
fields deserialises to `None` and a six-key document round-trips; saturating `Add`/`AddAssign`/`Sum`
with the documented `Option`-merge rule exist and are tested; the workspace is green.
</success_criteria>

<output>
Create `.planning/phases/31-lossless-token-accounting/31-01-SUMMARY.md` when done
</output>
