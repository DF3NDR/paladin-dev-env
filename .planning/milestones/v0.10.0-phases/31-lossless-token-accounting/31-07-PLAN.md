---
phase: 31-lossless-token-accounting
plan: 07
type: execute
wave: 6
depends_on: ["31-05", "31-06"]
files_modified:
  - MIGRATION.md
  - .cargo/semver-checks-allowlist.toml
  - CHANGELOG.md
  - crates/paladin-core/Cargo.toml
  - crates/paladin-ports/Cargo.toml
  - crates/paladin-web/Cargo.toml
  - docs/src/api-reference/upgrading.md
  - docs/src/api-reference/migration-guide.md
  - .planning/phases/31-lossless-token-accounting/COVERAGE.md
autonomous: true
requirements: [ACCT-05]
must_haves:
  truths:
    - "The allowlist and the MIGRATION.md §9.2 register are set-equal in BOTH directions: one [[entry]] without its matching Y row, or one Y row without its entry, fails the semver job; this plan verifies it locally by running the ci.yml awk extraction and comparison and asserting an empty diff"
    - "Each new migration_row string equals the §9.2 row's first two cells exactly (crate | Type, with the type cell reduced to its first backtick identifier) — for example paladin-ai-core | TokenUsage and paladin-ports | StreamingResponse; a near-match such as paladin-core instead of paladin-ai-core is a gate failure and is caught by the same local comparison"
    - "A touched public type that did not exist at the 0.9.0 baseline (TraceEvent, NodeExecutionRecord, CompletedRow) gets a §9.2 row marked N/A — listed for completeness — and NO allowlist entry; the empty entry set for such rows is correct, not a gap (D-26)"
    - "New §9.2 rows are appended after the last existing row (the Phase 28 PaladinExecutionService row) in the order TokenUsage, PaladinResult, StreamingResponse, ChunkMetadata, ExecuteResponse, then the N/A completeness rows; the CI comparison is an order-independent deduplicated set compare, so the gate passes regardless of row order"
    - "Every allowlist lint id was derived empirically by running cargo semver-checks check-release against the landed code, never guessed ahead of the run, and any lint id not already present in the owning crate's [package.metadata.cargo-semver-checks.lints] table is added there in the same commit (D-27)"
    - "The coverage floor is a single workspace line-coverage number (cargo llvm-cov --fail-under-lines 82, ADR-0006); this plan records the measured figure to two decimals in its SUMMARY coverage block together with whether a local make coverage run reproduces the CI figure"
  artifacts:
    - "MIGRATION.md — new §9.2 rows plus a §9.4 persistence note and a §9.6 HTTP-response note"
    - ".cargo/semver-checks-allowlist.toml — one [[entry]] per (crate, lint, migration_row) that actually fired"
    - "CHANGELOG.md — the [0.10.0] Changed and Fixed bullets"
    - ".planning/phases/31-lossless-token-accounting/COVERAGE.md — the per-provider usage-reporting decision matrix, written at plan time and reconciled here against what the adapters actually landed"
  key_links:
    - "The row-level set-equality gate is (crate, type)-keyed and deduplicated, so the existing paladin-ai-core | PaladinResult entry can cover this phase's further change to the same type — extend that row's Change cell rather than adding a duplicate row, mirroring the paladin-ai | Settings precedent"
    - "cargo semver-checks must run AFTER all code has landed (waves 1-5) — writing allowlist entries before the tool has run is the failure mode RESEARCH.md Pitfall 1 names"
---

<objective>
Close ACCT-05: register every touched public type in `MIGRATION.md` §9.2 with a row-level-matched
`cargo semver-checks` allowlist entry derived empirically from the tool's own output, record the
carrier change and the two corrected under-reports in `CHANGELOG.md` `[0.10.0]`, and run the phase's
gate list green (D-26, D-27, D-28, D-31, plus the folded local-coverage verification note).

Purpose: ADR-0051 lets Phases 31-33 break public API, but every break still has to be *documented*
as a break — the §9.2 register plus the row-level allowlist is how a downstream consumer learns what
moved, and the set-equality CI gate is what stops the register from quietly falling behind the code.

Output: a complete compatibility register, an allowlist that is set-equal to it, a `[0.10.0]`
changelog entry a downstream consumer can act on, and recorded gate evidence.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-lossless-token-accounting/31-CONTEXT.md
@.planning/phases/31-lossless-token-accounting/31-RESEARCH.md
@.planning/phases/31-lossless-token-accounting/31-05-SUMMARY.md
@.planning/phases/31-lossless-token-accounting/31-06-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces (plan 31-07 slice)

New artifacts created by this plan — excluded from drift verification against the pre-phase tree:

- New `MIGRATION.md` §9.2 rows: `paladin-ai-core | TokenUsage`, an extended Change cell on the
  existing `paladin-ai-core | PaladinResult` row, `paladin-ports | StreamingResponse`,
  `paladin-ports | ChunkMetadata`, `paladin-web | ExecuteResponse`, plus N/A completeness rows for
  `paladin-ai-core | NodeExecutionRecord`, `paladin-ai-core | TraceEvent` and
  `paladin-ports | CompletedRow`
- New `.cargo/semver-checks-allowlist.toml` `[[entry]]` blocks, one per lint that actually fired
- New `CHANGELOG.md` `[0.10.0]` `### Changed` and `### Fixed` bullets
- New "Token usage carriers" subsections in `docs/src/api-reference/upgrading.md` and
  `docs/src/api-reference/migration-guide.md`
- Reconciled decisions in `.planning/phases/31-lossless-token-accounting/COVERAGE.md` (the file
  itself is created at plan time, not by this plan)

<tasks>

<task type="tracer">
  <name>Task 1: Discover the real semver lint ids, then write the §9.2 rows and matching allowlist entries</name>
  <files>MIGRATION.md, .cargo/semver-checks-allowlist.toml, crates/paladin-core/Cargo.toml, crates/paladin-ports/Cargo.toml, crates/paladin-web/Cargo.toml</files>
  <read_first>
    - MIGRATION.md §9.2 (lines ~157-200): the five-column row format, the `paladin-ai-core | PaladinResult` row (the constructible rationale to extend), the `paladin-ports | LlmRequest` row (the `#[non_exhaustive]` + builder template), the `paladin-ports | ResumeAccepted` row (the new-in-0.10 "N/A, no allowlist entry" wording to reuse verbatim), and the note at the top explaining that the Crate cell is the crates.io PACKAGE name (`paladin-ai-core`, never `paladin-core`)
    - .cargo/semver-checks-allowlist.toml (the header comment's five-key `[[entry]]` schema and the eleven existing entries)
    - .github/workflows/ci.yml lines ~303-400 (the `semver` job's per-package command and the awk-based set-equality step — read the awk script carefully; it is what you reproduce locally)
    - crates/paladin-core/Cargo.toml lines ~60-70, crates/paladin-ports/Cargo.toml lines ~42-50, crates/paladin-web/Cargo.toml lines ~76-84 (the three per-crate `[package.metadata.cargo-semver-checks.lints]` tables)
    - .planning/phases/31-lossless-token-accounting/31-RESEARCH.md § Common Pitfalls, Pitfall 1 and § Open Questions 1 (why the tool runs BEFORE the entries are written, and the recommendation to extend the existing `PaladinResult` row rather than duplicate it)
    - .planning/phases/31-lossless-token-accounting/31-CONTEXT.md, decisions D-26 and D-27
  </read_first>
  <action>
Run the tool FIRST. Install it locally if absent
(`cargo install cargo-semver-checks --locked --version 0.50.0`, the version `ci.yml` pins), then run
the `semver` job's own command for each of the three crates whose public API this phase touched:
`cargo semver-checks check-release --package paladin-ai-core --default-features --baseline-version 0.9.0`,
and the same for `paladin-ports` and `paladin-web`. Capture the exact lint ids it reports. Treat
every lint id named in CONTEXT.md D-27 and in RESEARCH.md as a hypothesis, not a fact — the whole
point of D-27 is that the register is written from observed output (RESEARCH.md Pitfall 1).

Then write the §9.2 rows, copying the file's own five-column format
(`Crate | Type / Trait | Change | Mitigation | Deliberate-breaking? | Req`) and the wording
conventions of the three template rows:

- `paladin-ai-core | TokenUsage` — three additive `Option<u32>` fields with `#[serde(default)]`,
  three chainable builders, `Add`/`AddAssign`/`Sum`, and the removal of the total-only constructor;
  the struct stays constructible and NOT `#[non_exhaustive]` because it derives `Default` (X-10.3
  option (b), the `PaladinResult`/`Settings` precedent). Note in the Change cell that
  `BattalionResult` needs no row of its own: its signature is unchanged and only the VALUES in
  `per_paladin_tokens` changed from zeroed to real. Mark `Y`; requirement `ACCT-01, ACCT-02`.
- `paladin-ai-core | PaladinResult` — EXTEND the existing row's Change cell (do not add a second
  row): the bare count field is replaced by `usage: TokenUsage` and `new`'s second parameter is
  retyped; the struct stays constructible. The set-equality gate is `(crate, type)`-keyed and
  deduplicated, so the existing allowlist entry for this pair can stay as-is, gaining any
  newly-observed lint id as a further entry only if the tool reports one the existing entry's `lint`
  key does not already name.
- `paladin-ports | StreamingResponse` — additive `usage: Option<TokenUsage>` with
  `#[serde(default)]`, struct marked `#[non_exhaustive]`, new `delta`/`terminal`/`with_usage`
  constructors replacing every in-tree literal in the same commit (X-10.3 option (a): the type has
  no `Default`). Mark `Y`; requirement `ACCT-03`.
- `paladin-ports | ChunkMetadata` — same treatment and same reasoning; state explicitly that the
  type derives no `Default` today, which is what makes option (a) the correct choice here (this
  resolves CONTEXT.md D-18's open question). Mark `Y`; requirement `ACCT-03`.
- `paladin-web | ExecuteResponse` — the bare count field replaced by `usage: TokenUsageResponse`
  (a new schema-annotated DTO in the same crate). Mark `Y`; requirement `ACCT-02`.
- N/A completeness rows, reusing the `ResumeAccepted` row's wording verbatim ("N/A — new type
  introduced in v0.10, not a pre-existing public API" / "N/A (new type; X-10 governs only
  pre-existing types)"): `paladin-ai-core | NodeExecutionRecord`, `paladin-ai-core | TraceEvent`
  and `paladin-ports | CompletedRow`. These get NO allowlist entry.

Append the new rows after the last existing row. Use the crates.io PACKAGE name in every Crate cell.

Write one `[[entry]]` in `.cargo/semver-checks-allowlist.toml` per `(crate, lint, migration_row)`
that ACTUALLY fired, following the file's five-key schema, with a one-line justification naming why
the change is deliberate-breaking rather than mitigable. Every `migration_row` value must equal its
§9.2 row's first two cells exactly, with the type cell reduced to its first backtick identifier —
`paladin-ai-core | TokenUsage`, not `paladin-core | TokenUsage`. Add any lint id the tool reported
that is not already in the owning crate's `[package.metadata.cargo-semver-checks.lints]` table to
that table with value `"allow"`, in the same commit.

Finally, reproduce the CI set-equality step locally: run the awk extraction and comparison exactly
as `ci.yml` performs it (register side scoped to the §9.2 section, allowlist side from the
`[[entry]]` blocks) and confirm the diff is empty in both directions.
  </action>
  <verify>
    <automated>cargo semver-checks check-release --package paladin-ai-core --default-features --baseline-version 0.9.0 &amp;&amp; cargo semver-checks check-release --package paladin-ports --default-features --baseline-version 0.9.0 &amp;&amp; cargo semver-checks check-release --package paladin-web --default-features --baseline-version 0.9.0</automated>
  </verify>
  <acceptance_criteria>
    - The three `cargo semver-checks check-release` commands above all exit 0 with the allowlist and per-crate lint tables in place
    - `grep -c 'migration_row = "paladin-ai-core | TokenUsage"' .cargo/semver-checks-allowlist.toml` returns at least 1
    - `.cargo/semver-checks-allowlist.toml` contains `migration_row = "paladin-ports | StreamingResponse"`, `migration_row = "paladin-ports | ChunkMetadata"` and `migration_row = "paladin-web | ExecuteResponse"`
    - `grep -c 'paladin-core |' MIGRATION.md` shows no NEW row using the directory name in a Crate cell for a row this plan added (existing historical rows are untouched)
    - MIGRATION.md §9.2 contains a row whose first two cells are `paladin-ai-core` and `TokenUsage`, and contains N/A completeness rows for `NodeExecutionRecord`, `TraceEvent` and `CompletedRow` that carry no allowlist entry
    - The local reproduction of the `ci.yml` awk set-equality comparison prints an empty diff in both directions
    - Every lint id written in an `[[entry]]` appears in the captured `cargo semver-checks` output recorded in the plan SUMMARY — no id was written that the tool did not report
  </acceptance_criteria>
  <done>Every touched public type has a §9.2 row, every deliberate break has an allowlist entry whose lint id came from the tool's own output, and the row-level set-equality gate passes locally in both directions.</done>
</task>

<task type="auto">
  <name>Task 2: CHANGELOG, upgrade guides, and the API coverage matrix</name>
  <files>CHANGELOG.md, docs/src/api-reference/upgrading.md, docs/src/api-reference/migration-guide.md, .planning/phases/31-lossless-token-accounting/COVERAGE.md</files>
  <read_first>
    - CHANGELOG.md (the `[Unreleased]` section and the `[0.10.0] - 2026-09-10` section — find the existing `### Changed` / `### Fixed` subsections and their bullet style)
    - docs/src/api-reference/upgrading.md and docs/src/api-reference/migration-guide.md (the existing per-change subsection shape to copy)
    - MIGRATION.md §9.2, §9.4 and §9.6 as left by task 1 (the upgrade-guide subsections point at §9.2)
    - .planning/phases/31-lossless-token-accounting/31-RESEARCH.md § Common Pitfalls, Pitfall 3 (why the Anthropic `prompt_tokens` correction needs the before/after formula spelled out)
    - .planning/phases/31-lossless-token-accounting/31-CONTEXT.md, decisions D-25 and D-28
  </read_first>
  <action>
Write the entries in the `[0.10.0]` section, not `[Unreleased]` — the dated section already exists
and the release is untagged (D-28). Leave the `[Unreleased]` Commissary re-export bullet alone.

Add a `### Changed` bullet covering the carrier change as one coherent item: `PaladinResult.usage`,
the `NodeFinished` / `RunFinished` / `NodeExecutionRecord` `usage` fields, the removal of the
total-only `TokenUsage` constructor, `StreamingResponse.usage` plus its `#[non_exhaustive]` marking
and new constructors, `ChunkMetadata.usage`, and `ExecuteResponse.usage`. Point at MIGRATION.md §9.2.

Add a `### Fixed` bullet naming BOTH corrected under-reports explicitly:
  1. battalion `per_paladin_tokens` previously recorded a zeroed prompt/completion split because
     the aggregation path built each entry from a total only — the real split is now preserved;
  2. the Anthropic adapter's `prompt_tokens` previously EXCLUDED cached input because the provider's
     own `input_tokens` does; it is now the billed figure
     `input_tokens + cache_read_input_tokens + cache_creation_input_tokens`. Spell out the
     before/after formula: a consumer computing its own cost estimate from `prompt_tokens` will see
     a LARGER number for any call that hits the prompt cache, and that number is the one Anthropic
     billed.

Add a "Token usage carriers" subsection to `docs/src/api-reference/upgrading.md` and
`docs/src/api-reference/migration-guide.md` summarising the carrier change in a few lines and
pointing at MIGRATION.md §9.2. Write "v0.10.0", never "v0.11.0".

Add the MIGRATION.md notes the serde policy needs if task 1 did not already place them: a §9.4
persistence note stating that Waypoint and persisted trace rows written before this phase report a
default (zero) usage for their history, that resume is unaffected because token history is not
resume-critical, and that NO legacy-shape deserializer maps the retired key into the new field
(D-25); and a §9.6 note recording the HTTP response-shape change (a changed response body, not a
new route).

Reconcile `.planning/phases/31-lossless-token-accounting/COVERAGE.md` — written at plan time from
the research findings — against what the adapters ACTUALLY landed in plans 31-03 and 31-04. Walk its
53 rows and correct any whose decision the implementation contradicts: a capability the research
predicted as `OPT-OUT` that an adapter in fact parses becomes `INTEGRATE`, and a capability the
research predicted as `INTEGRATE` whose field never materialised becomes `OPT-OUT` with a reason
citing what the adapter observed. Every `OPT-OUT` keeps a non-empty reason under 200 characters,
`INTEGRATE` stays the default, and no row may be deleted to avoid a decision. The DeepSeek reasoning
row and the Grok reasoning row are the two most likely to move, since their field names were flagged
as assumed rather than confirmed.
  </action>
  <verify>
    <automated>node /workspace/.claude/gsd-core/bin/gsd-tools.cjs query check api-coverage.verify-pre .planning/phases/31-lossless-token-accounting &amp;&amp; mdbook build docs/</automated>
  </verify>
  <acceptance_criteria>
    - `CHANGELOG.md`'s `[0.10.0]` section contains a `### Changed` bullet naming `PaladinResult`, `StreamingResponse` and `ExecuteResponse`, and a `### Fixed` bullet naming both the battalion split correction and the Anthropic billed-input correction with its before/after formula
    - `CHANGELOG.md`'s `[Unreleased]` section is unchanged in `git diff`
    - `docs/src/api-reference/upgrading.md` and `docs/src/api-reference/migration-guide.md` each contain a "Token usage carriers" subsection that references §9.2
    - `MIGRATION.md` §9.4 states that pre-phase Waypoint and trace rows report a default usage and that no legacy-shape deserializer exists; §9.6 records the HTTP response-shape change
    - `.planning/phases/31-lossless-token-accounting/COVERAGE.md` still has all 53 capability rows, every row carries a decision of `INTEGRATE` or `OPT-OUT`, and every `OPT-OUT` row carries a non-empty reason under 200 characters
    - The SUMMARY names every COVERAGE.md row whose decision this task changed, with the observed adapter behaviour that justified the change (or states explicitly that none changed)
    - The `api-coverage.verify-pre` check passes on the phase directory
    - `mdbook build docs/` exits 0 and `grep -rn 'v0\.11\.0' CHANGELOG.md docs/src/api-reference` returns no match
  </acceptance_criteria>
  <done>A downstream consumer reading `CHANGELOG.md` `[0.10.0]` learns what moved and what number changed value, the upgrade guides point at the register, and the per-provider usage-reporting coverage matrix records an explicit decision for every capability.</done>
</task>

<task type="auto">
  <name>Task 3: Run the phase gate list and record the evidence</name>
  <files>.planning/phases/31-lossless-token-accounting/31-07-SUMMARY.md</files>
  <read_first>
    - .planning/phases/31-lossless-token-accounting/31-CONTEXT.md, decision D-31 (the gate list) and the Folded Todos section (the local-coverage reproduction note)
    - .planning/phases/31-lossless-token-accounting/31-VALIDATION.md (the "Before /gsd-verify-work" sampling row — the same gate list, stated as commands)
    - .github/instructions/security.instructions.md (the manual credential-handling review checklist — the primary control for the new provider-response parsing this phase added)
    - Makefile (the `clean-code`, `security`, `coverage` and `openapi` targets)
  </read_first>
  <action>
Run the phase's full gate list and record each result, with the measured figure where there is one,
in the plan SUMMARY:

  - `make clean-code` (fmt + clippy + check)
  - `cargo test --workspace --all-features`
  - `cargo test --workspace --doc` (run separately — `cargo llvm-cov` skips doctests, so a
    doctest-only regression is invisible to the coverage job)
  - `cargo llvm-cov --workspace --fail-under-lines 82` (record the measured percentage to two
    decimal places, and record whether a local `make coverage` run reproduces the figure CI reports
    — this closes the folded todo `2026-08-13-verify-local-coverage-reproduction`)
  - `make security` (cargo-audit + cargo-deny)
  - `cargo doc --workspace --no-deps` with zero warnings
  - `mdbook build docs/`
  - the `semver` job's two steps: the per-package `cargo semver-checks check-release` runs from
    task 1, and the row-level allowlist ↔ §9.2 set-equality comparison
  - `make openapi` followed by `git diff --exit-code crates/paladin-web/openapi.json`
  - the CI Python-client generation job's inputs (confirm the regenerated `openapi.json` is what it
    consumes and that nothing else it reads changed)

Then perform the manual credential-handling review that
`.github/instructions/security.instructions.md` names as the primary control for code touching an
external response body, over the diff this phase produced in `crates/paladin-llm`. Confirm and
record in the SUMMARY: no log statement interpolates an API key; every deserialisation-failure path
in the touched adapters still redacts BEFORE truncating (`crate::redaction::diagnostic_excerpt`);
no adapter HTTP client gained redirect-following; and no new test fixture contains a
credential-shaped literal. Record the outcome as the mitigation evidence for threats T-31-08 and
T-31-13.

If any gate is red, fix it within this plan rather than deferring — ACCT-05 requires them green.
  </action>
  <verify>
    <automated>make clean-code &amp;&amp; cargo test --workspace --all-features &amp;&amp; cargo test --workspace --doc &amp;&amp; make security &amp;&amp; cargo doc --workspace --no-deps &amp;&amp; mdbook build docs/ &amp;&amp; cargo llvm-cov --workspace --fail-under-lines 82</automated>
  </verify>
  <acceptance_criteria>
    - `make clean-code` exits 0
    - `cargo test --workspace --all-features` exits 0 and `cargo test --workspace --doc` exits 0
    - `cargo llvm-cov --workspace --fail-under-lines 82` exits 0, and the SUMMARY records the measured line-coverage percentage to two decimals plus an explicit statement of whether local `make coverage` reproduces the CI figure
    - `make security` exits 0 with no un-allowlisted advisory
    - `cargo doc --workspace --no-deps` produces zero warnings and `mdbook build docs/` exits 0
    - `git diff --exit-code crates/paladin-web/openapi.json` is clean after `make openapi`
    - The SUMMARY records the four credential-handling review findings by name (no key in a log line; redact-before-truncate intact in every touched adapter; no redirect-following added; no credential-shaped fixture literal) as the evidence for T-31-08 and T-31-13
  </acceptance_criteria>
  <done>Every gate in the phase's gate list is green with its measured figure recorded, and the manual credential-handling review of the new provider-response parsing is documented as the mitigation evidence for this phase's two high-severity threats.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documented compatibility register → downstream consumer upgrading across the break | a missing or wrong row silently misleads an upgrade |
| regenerated `openapi.json` → CI Python-client generation job | a stale or malformed document breaks a downstream build |
| dependency advisory feed → `make security` | new advisories against already-vendored crates surface here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-24 | Repudiation | `MIGRATION.md` §9.2 register vs the allowlist | high | mitigate | The row-level set-equality gate is run locally in this plan (the same awk extraction `ci.yml` performs) and asserted to produce an empty diff in both directions, so a break cannot ship undocumented and a documented break cannot lack its suppression record |
| T-31-25 | Tampering | guessed rather than observed semver lint ids | medium | mitigate | Task 1 runs `cargo semver-checks check-release` against the landed code BEFORE any entry is written and an acceptance criterion requires every written lint id to appear in the captured output recorded in the SUMMARY (D-27, RESEARCH.md Pitfall 1) |
| T-31-08 / T-31-13 | Information Disclosure | the new provider-response parsing added in plans 31-03 and 31-04 | high | mitigate | Task 3 performs and records the manual credential-handling review `.github/instructions/security.instructions.md` designates as the primary control: no key in a log line, redact-before-truncate intact, no redirect-following added, no credential-shaped fixture literal. This is the closing evidence for both plans' high-severity threats |
| T-31-26 | Tampering | a new advisory against an already-vendored dependency | medium | mitigate | `make security` (cargo-audit + cargo-deny) runs as a gate; a new advisory is treated as a blocker unless explicitly allowlisted in `.cargo/audit.toml` with a stated reason |
| T-31-SC | Tampering | package-manager installs | low | accept | This phase adds no dependency to any `Cargo.toml` (RESEARCH.md § Package Legitimacy Audit, verified: no `Cargo.toml` dependency line changes). The one tool installation is `cargo-semver-checks` pinned to the exact version `ci.yml` pins (0.50.0, installed with `--locked`), used locally for lint discovery and never added to the dependency graph |
</threat_model>

<verification>
- `cargo semver-checks check-release` green for `paladin-ai-core`, `paladin-ports` and `paladin-web` against the 0.9.0 baseline
- The local reproduction of the `ci.yml` allowlist ↔ §9.2 set-equality comparison shows an empty diff both ways
- `make clean-code`, `cargo test --workspace --all-features`, `cargo test --workspace --doc`, `make security`, `cargo doc --workspace --no-deps`, `mdbook build docs/` all green
- `cargo llvm-cov --workspace --fail-under-lines 82` green with the measured figure recorded
- `git diff --exit-code crates/paladin-web/openapi.json` clean after `make openapi`
- `api-coverage.verify-pre` passes on the phase directory
</verification>

<success_criteria>
ACCT-05 is satisfied: every touched public type has a `MIGRATION.md` §9.2 row and a matching
`cargo semver-checks` allowlist row with the Phase 29 D-04 row-level gate green; the `CHANGELOG.md`
`[0.10.0]` section records the carrier change and both corrected under-reports; and `make clean-code`
plus the 82 % workspace line-coverage floor are green with the measured figure recorded.
</success_criteria>

<output>
Create `.planning/phases/31-lossless-token-accounting/31-07-SUMMARY.md` when done
</output>
