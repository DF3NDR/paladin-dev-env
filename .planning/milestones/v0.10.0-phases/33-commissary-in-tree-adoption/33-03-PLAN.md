---
phase: 33-commissary-in-tree-adoption
plan: 03
type: execute
wave: 3
depends_on: ["33-02"]
files_modified:
  - crates/paladin-memory/src/services/rag_retrieval_service.rs
autonomous: true
requirements: [COMM-01]
must_haves:
  truths:
    - "A property test over the `ration` seam proves, for random (content, score) vectors and random budgets, that the retained total never exceeds the budget, that no shed memory outscores a retained one, and that retained ∪ shed equals the input by id — nothing is lost (COMM-01, D-17)."
    - "One memory larger than the whole budget is retained truncated with the Commissary's per-item marker and an empty `shed`, not dropped — the exact opposite of the pre-phase behaviour (D-10a)."
    - "A `rag.max_tokens` beyond `u32::MAX` returns the typed budget-conversion error and never a wrapped, clamped or zero budget (D-05)."
    - "Memories whose scores are exactly equal keep insertion order through the rank sort and receive adjacent, distinct rank priorities — equal scores never merge into one item and never reorder run-to-run (D-08)."
    - "At the budget boundary the behaviour is one-sided and specified: a set that fits is retained whole with no shed and no cut; adding one memory past the allowance sheds the lowest-priority item rather than cutting every item (COMM-01 boundary edge)."
  artifacts:
    - path: "crates/paladin-memory/src/services/rag_retrieval_service.rs"
      provides: "the `ration_respects_budget_and_rank_order` proptest plus the four named edge tests"
      contains: "proptest!"
  key_links:
    - from: "crates/paladin-memory/src/services/rag_retrieval_service.rs"
      to: "crates/paladin-memory/Cargo.toml"
      via: "the proptest 1.4 dev-dependency added in 33-01 task 1 (D-17)"
      pattern: "proptest"
---

<objective>
Prove the rationing seam rather than assert it once. A `proptest` over the synchronous `ration`
seam (D-14, D-17) establishes the three COMM-01 invariants across random inputs, and four named
example-based tests pin the edges the phase's own decisions created: the oversized single memory
that is now retained-truncated instead of dropped (D-10a), the non-clamping budget conversion
(D-05), equal-score tie stability (D-08), and the budget boundary itself.

Purpose: COMM-01's success criterion names a property test explicitly — "the retained set's total
is ≤ the budget and the highest-scoring memories are the ones retained" is a property, not an
example.
Output: one property test and four named edge tests, all in the existing `#[cfg(test)]` module.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-commissary-in-tree-adoption/33-CONTEXT.md
@.planning/phases/33-commissary-in-tree-adoption/33-PATTERNS.md
@.planning/phases/33-commissary-in-tree-adoption/33-01-SUMMARY.md
</context>

## Artifacts this phase produces

New symbols and files created by Phase 33 (do not treat as drift): `RagRetrievalResult`,
`RagRetainedMemory`, `RagRetrievalError`, `RagRetrievalService::with_token_counter`,
`RagRetrievalService::ration`, `RagRetrievalService::commissary`, the `ShedItem` re-export
(33-01); `rag_omission_marker` (33-02); `ration_respects_budget_and_rank_order` (this plan);
`tests/integration/rag_commissary_test.rs` plus its `[[test]] name = "rag_commissary"` target
(33-04); `33-CI-EVIDENCE.md` and the audit `## 11.` section (33-06).

<tasks>

<task type="auto">
  <name>Task 1: Property-test the rationing seam</name>
  <precondition>`proptest = "1.4"` is present in `crates/paladin-memory/Cargo.toml` `[dev-dependencies]` (added by plan 33-01 task 1) and `cargo tree -p paladin-memory -e dev -i proptest` resolves</precondition>
  <read_first>
    - crates/paladin-memory/src/services/rag_retrieval_service.rs (the post-33-02 file: `ration`, `rank_by_relevance`, `RagRetrievalResult`, and the `#[cfg(test)]` helpers `MockEmbeddingPort`, `MockSanctumPort`, `create_test_entry`)
    - crates/paladin-memory/src/token_counter/heuristic.rs (`HeuristicTokenCounter` counts chars, not bytes — the reason the byte-allowance-to-token-tally direction is safe)
    - crates/paladin-llm/src/services/commissary.rs lines 431-470 (the dispense algorithm doc: allowance from `allotted_tokens * 1000 / pessimistic_tokens_per_1000_bytes`, stable sort by `(priority, insertion_index)`, shed-last-until-fits)
    - crates/paladin-memory/src/config/rag.rs (`RagConfig` construction for the per-case budget)
    - .planning/phases/33-commissary-in-tree-adoption/33-CONTEXT.md D-17 and the `<specifics>` strategy sketch
  </read_first>
  <files>crates/paladin-memory/src/services/rag_retrieval_service.rs</files>
  <action>
    Add `use proptest::prelude::*;` inside the existing `#[cfg(test)]` module and one
    `proptest! { #[test] fn ration_respects_budget_and_rank_order(...) }` case driving the SYNC
    `ration` seam directly — never `retrieve_context` — so no async runtime is needed inside a
    proptest closure (D-14, D-17).

    Strategy, per D-17 and CONTEXT `<specifics>`: `items in prop::collection::vec((".{1,600}",
    0.0f32..=1.0f32), 0..=20)` and `budget in 1u32..=2_000u32`. Build one `SanctumSearchResult` per
    generated pair via `create_test_entry`, keeping the generated order (deliberately unsorted),
    then call `rank_by_relevance` before `ration` so the rank-order property is exercised against
    unsorted input rather than against a vector that was already in the right order.

    Construct the service per case with the mock ports and a `RagConfig` whose `max_tokens` is the
    generated budget as `usize`, leaving the default `HeuristicTokenCounter` in place (D-07).

    Assert three properties: (i) `result.prompt_tokens <= budget`; (ii) for every `ShedItem`, its
    source memory's score is less than or equal to the score of every retained memory — resolve
    each shed label back to its input score through a UUID-keyed map built from the generated
    inputs; (iii) the multiset of ids across `result.memories` and `result.shed` equals the input
    id multiset exactly — nothing invented, nothing lost. Use `prop_assert!`/`prop_assert_eq!`, not
    bare `assert!`, so shrinking reports a minimal counterexample.

    Do not raise or lower the proptest case count from the default, and do not add a
    `ProptestConfig` with a fixed seed — a fixed seed would turn the property back into an example.

    Commit as `test(33): property-test the RAG rationing seam` (D-26 mechanics).
  </action>
  <verify>
    <automated>cargo test -p paladin-memory --lib ration_respects_budget_and_rank_order 2>&1 | grep -E 'test result: ok\. [1-9][0-9]* passed'</automated>
  </verify>
  <acceptance_criteria>
    - `crates/paladin-memory/src/services/rag_retrieval_service.rs` contains `proptest!` and `fn ration_respects_budget_and_rank_order`
    - `cargo test -p paladin-memory --lib ration_respects_budget_and_rank_order` reports `test result: ok. 1 passed` — a zero-selecting filter that exits 0 is NOT a pass
    - The test body calls `self.ration(` (or the seam via the service binding) and does NOT call `retrieve_context`, and contains no `#[tokio::test]` attribute
    - The test body contains three distinct `prop_assert` families corresponding to budget, rank order and id conservation
    - `grep -c 'ProptestConfig' crates/paladin-memory/src/services/rag_retrieval_service.rs` is 0
  </acceptance_criteria>
  <done>Random (content, score, budget) inputs cannot produce an over-budget tally, a higher-scoring shed memory, or a lost id.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the four named edges this phase's decisions created</name>
  <read_first>
    - crates/paladin-memory/src/services/rag_retrieval_service.rs (the seam, the result types, `rag_omission_marker`, and the existing test helpers)
    - crates/paladin-llm/src/services/commissary.rs lines 63-120 (`CommissaryPlan::truncation_marker` default `"\n... (truncated)"` — the per-item marker these tests assert on) and lines 431-470 (the `n == 1` guard and the share computation)
    - .planning/phases/33-commissary-in-tree-adoption/33-CONTEXT.md D-05, D-08, D-10 and the `<specifics>` note that the D-10a edge gets its own named test
  </read_first>
  <files>crates/paladin-memory/src/services/rag_retrieval_service.rs</files>
  <action>
    Add four named `#[test]` cases to the existing `#[cfg(test)]` module, all driving the sync
    `ration` seam with distinct non-round figures (Phase 31 house style — budget `1_234`, bodies of
    `987` / `654` / `321` bytes; never `1000`/`500`/`100`):

    (a) `single_memory_larger_than_budget_is_retained_truncated_not_dropped` — one memory whose body
    is far larger than the whole allowance. Assert `result.memories.len() == 1`,
    `result.memories[0].truncated == true`, the retained `body` ends with the Commissary's per-item
    truncation marker, `result.shed.is_empty()`, and the retained `body` is shorter than the input
    content. This is the D-10a edge and its expected output is the exact opposite of the
    pre-phase behaviour (which returned nothing at all), so name it plainly.

    (b) `budget_beyond_u32_returns_typed_error_and_never_clamps` — a `RagConfig` whose `max_tokens`
    is `usize::MAX` (or any value above `u32::MAX`, on a 64-bit target). Assert `ration` returns
    `Err` matching the budget-conversion variant, and assert the error's `to_string()` names the
    configured value. Assert explicitly that no `Ok` result with a reduced budget is produced —
    this is the D-05 no-clamp contract.

    (c) `equal_scores_keep_insertion_order_and_distinct_priorities` — three memories where two carry
    byte-identical scores. Assert the two equal-scoring memories appear in `result.memories` in
    their pre-sort insertion order, that all three ids are distinct in the output, and that nothing
    was merged (`result.memories.len() + result.shed.len() == 3`). This is the D-08 tie rule and
    the COMM-02 adjacency edge.

    (d) `at_the_budget_boundary_nothing_is_shed_and_one_past_it_sheds_the_lowest` — two runs over
    the same three memories: one budget large enough that the dispensed total fits, asserting
    `shed.is_empty()`, every `truncated == false`, and `prompt_tokens <= allotted_tokens`; and one
    budget small enough to force exactly one shed, asserting `result.shed.len() == 1`, that the shed
    label equals the UUID of the LOWEST-scoring memory, and that the two highest-scoring memories
    are still present. Both runs assert `prompt_tokens <= allotted_tokens`.

    Every test asserts on individual fields — `Stockpile`, `ShedItem` and `DispensedItem` derive
    only `Debug, Clone`, so struct-level `assert_eq!` does not compile (RESEARCH Pitfall 2).

    Commit as `test(33): pin the RAG rationing edge cases` (D-26).
  </action>
  <verify>
    <automated>cargo test -p paladin-memory --lib rag_retrieval_service 2>&1 | grep -E 'test result: ok\. [1-9][0-9]* passed' && cargo clippy -p paladin-memory --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - All four test function names exist verbatim in `crates/paladin-memory/src/services/rag_retrieval_service.rs`: `single_memory_larger_than_budget_is_retained_truncated_not_dropped`, `budget_beyond_u32_returns_typed_error_and_never_clamps`, `equal_scores_keep_insertion_order_and_distinct_priorities`, `at_the_budget_boundary_nothing_is_shed_and_one_past_it_sheds_the_lowest`
    - `cargo test -p paladin-memory --lib rag_retrieval_service` reports `test result: ok.` with a passed count of at least 16 and 0 failed
    - No test in this module uses `assert_eq!` with a whole `Stockpile`, `ShedItem` or `DispensedItem` value
    - `cargo clippy -p paladin-memory --all-targets -- -D warnings` exits 0
  </acceptance_criteria>
  <done>Each decision-created edge has a named, failing-if-regressed test, and the oversized-memory case documents the behaviour reversal the CHANGELOG will describe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator config → allocator budget | `rag.max_tokens` crosses `usize` → `u32` here and the test suite is what proves the conversion never clamps |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-02 | Denial of Service | the `usize` → `u32` budget conversion | medium | mitigate | Test (b) asserts the typed error and the absence of any reduced-budget `Ok` — the regression test for D-05's no-clamp contract |
| T-33-08 | Information Disclosure | proptest failure output | low | accept | Generated bodies are random strings, not real memory content; a shrink report prints generated data only |
| T-33-09 | Tampering | a future change quietly reordering equal-score memories | low | mitigate | Test (c) pins the stable-sort contract so a reorder fails CI rather than silently changing which memory the model reads first |
</threat_model>

<verification>
- `cargo test -p paladin-memory --lib ration_respects_budget_and_rank_order` — exactly 1 passed
- `cargo test -p paladin-memory --lib rag_retrieval_service` — at least 16 passed, 0 failed
- `cargo clippy -p paladin-memory --all-targets -- -D warnings`
</verification>

<success_criteria>
COMM-01's property obligation is discharged: retained total ≤ budget and highest-scoring retained
are proven over random inputs, not asserted once, and the four decision-created edges are pinned.
</success_criteria>

<output>
Create `.planning/phases/33-commissary-in-tree-adoption/33-03-SUMMARY.md` when done
</output>
