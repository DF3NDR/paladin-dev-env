---
phase: 33-commissary-in-tree-adoption
plan: 04
type: execute
wave: 3
depends_on: ["33-02"]
files_modified:
  - tests/integration/rag_commissary_test.rs
  - tests/integration/mod.rs
  - Cargo.toml
  - crates/paladin-llm/src/services/commissary.rs
  - docs/src/architecture/commissary.md
  - docs/src/getting-started/configuration.md
  - docs/src/api-reference/upgrading.md
  - docs/src/api-reference/migration-guide.md
autonomous: true
requirements: [COMM-03]
must_haves:
  truths:
    - "An ungated integration test drives `Commissary::dispense` through the real RAG path over `InMemorySanctum` and the facade re-export, and it runs in CI's `integration-tests` job with no Docker service (D-18) — the F4 production-caller evidence."
    - "The integration test asserts all three directions: a small budget produces a non-empty `shed` AND the omission marker in the rendered prompt; a large budget produces an empty `shed` AND no marker; one oversized memory produces `truncated == true` with the per-item marker and an empty `shed` (D-18, D-10a)."
    - "No silent token-based truncation remains in-tree: the retired helper's identifier appears nowhere under `crates`, `src`, `docs`, `examples` or `benches`, and no four-bytes-per-token byte-length estimate remains under `crates/paladin-memory/src` (D-19) — the Phase 26 D-13 deferral closes."
    - "The Commissary module doc names RAG as its first production caller in the past tense and no longer points at a live in-tree anti-pattern (D-19)."
  artifacts:
    - path: "tests/integration/rag_commissary_test.rs"
      provides: "the ungated F4 integration test with its three cases (D-18)"
      min_lines: 120
    - path: "Cargo.toml"
      provides: "the `[[test]] name = \"rag_commissary\"` target with no `required-features` (D-18)"
      contains: "rag_commissary"
    - path: "docs/src/architecture/commissary.md"
      provides: "an 'In-tree caller: RAG' section whose sketch mirrors the integration test (Phase 30 D-06)"
      contains: "In-tree caller"
  key_links:
    - from: "tests/integration/rag_commissary_test.rs"
      to: "crates/paladin-memory/src/services/rag_retrieval_service.rs"
      via: "the facade re-export path paladin::application::services::sanctum (D-18)"
      pattern: "application::services::sanctum"
    - from: "tests/integration/mod.rs"
      to: "tests/integration/rag_commissary_test.rs"
      via: "an ungated `pub mod` registration in the style of in_memory_sanctum_tests, not the qdrant-gated style"
      pattern: "pub mod rag_commissary_test"
---

<!-- planner-discipline-allow: truncate_to_token_budget -->

<objective>
Produce the F4 evidence and close F6. An ungated facade-level integration test (D-18) drives a real
`Commissary::dispense` through the real RAG path over `InMemorySanctum`, and the D-19 exit greps
prove the last silent token-truncation path is gone from the tree — which is what closes the
Phase 26 D-13 deferral. The doc sweep lands in the same plan because one of the two exit greps
cannot go green until the `paladin-llm` Commissary module doc stops naming the retired helper.

Purpose: COMM-03's two halves are "a production caller exercised by integration tests" and
"grep-provable absence"; they share one dependency (the retired identifier) so they ship together.
Output: a new ungated test target, a rewritten module doc, and four doc pages that describe the
new behaviour.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-commissary-in-tree-adoption/33-CONTEXT.md
@.planning/phases/33-commissary-in-tree-adoption/33-PATTERNS.md
@.planning/phases/33-commissary-in-tree-adoption/33-01-SUMMARY.md
@.planning/phases/33-commissary-in-tree-adoption/33-02-SUMMARY.md
</context>

## Artifacts this phase produces

New symbols and files created by Phase 33 (do not treat as drift): `RagRetrievalResult`,
`RagRetainedMemory`, `RagRetrievalError`, `RagRetrievalService::with_token_counter`,
`RagRetrievalService::ration`, `RagRetrievalService::commissary`, the `ShedItem` re-export
(33-01); `rag_omission_marker` (33-02); `ration_respects_budget_and_rank_order` (33-03);
`tests/integration/rag_commissary_test.rs` and its `[[test]] name = "rag_commissary"` target
(this plan); `33-CI-EVIDENCE.md` and the audit `## 11.` section (33-06).

## Flagged assumptions

- **COMM-03 (unclassified probe row, carried unresolved):** the exit grep sweep is the ONLY proof
  that no silent token-based truncation remains in-tree. It is an identifier-and-shape search, not
  a semantic analysis — a future silent truncation written with different words would not be
  caught by it. The scout's disposition of every other `truncat*` site as "marked, not silent"
  (Conclave's output truncation appends its own marker; the content summariser is a
  character-length summariser by design; the redaction and trace paths are redact-then-bound with
  explicit markers) is recorded in this plan's SUMMARY as a reviewed judgement, not as a machine
  check. Do not silently upgrade that judgement to a gate.

<tasks>

<task type="auto">
  <name>Task 1: The ungated F4 integration test through the real RAG path</name>
  <read_first>
    - tests/integration/rag_integration_tests.rs lines 140-260 (the deterministic mock `EmbeddingPort` pattern to copy) and 300-435 (the migrated call sites, for the post-33-01 result-type usage)
    - tests/integration/in_memory_sanctum_tests.rs (the ungated store-and-search pattern and `InMemorySanctum::new` usage)
    - tests/integration/mod.rs lines 50-80 (registration style: ungated like `in_memory_sanctum_tests`, NOT `#[cfg(feature = "qdrant")]` like `rag_integration_tests`)
    - Cargo.toml lines 320-400 (the `[[test]]` target block; `in_memory_sanctum_integration` is the no-`required-features` template)
    - crates/paladin-memory/src/sanctum/in_memory_adapter.rs (constructor and store/search semantics)
    - crates/paladin-memory/src/services/rag_retrieval_service.rs (the post-33-02 result types, `format_for_prompt` and `rag_omission_marker`)
    - src/application/services/sanctum/mod.rs (the facade re-export path the test must import through)
  </read_first>
  <files>tests/integration/rag_commissary_test.rs, tests/integration/mod.rs, Cargo.toml</files>
  <action>
    Create `tests/integration/rag_commissary_test.rs` importing ONLY through the facade re-export
    path — `use paladin::application::services::sanctum::{RagConfig, RagRetrievalService,
    RagRetrievalResult, rag_omission_marker};` and the `ShedItem` re-export as needed (D-18) — so
    the test proves the whole published path, not the crate-internal one.

    Build the fixture with `InMemorySanctum` plus a deterministic mock `EmbeddingPort` copied from
    `rag_integration_tests.rs`'s pattern (same `Embedding { vector, model, dimension, token_count }`
    construction; a fixed vector per input string so scores are reproducible). Store memories of
    distinct, non-round sizes (`987`, `654`, `321` bytes) with distinct scores, and assert three
    named cases:

    (a) `small_budget_sheds_and_marks` — `rag.max_tokens` small enough to force a shed. Assert
    `result.shed` is non-empty, `result.was_rationed()` is true, the shed label parses as a UUID,
    and `format_for_prompt(&result)` ends with exactly
    `rag_omission_marker(result.shed.len(), result.allotted_tokens)`.

    (b) `large_budget_sheds_nothing_and_emits_no_marker` — a budget comfortably above the total.
    Assert `result.shed.is_empty()`, every retained `truncated` is false, and the rendered prompt
    contains none of the marker text (compare against the helper's output for a hypothetical count
    of 1, asserting absence).

    (c) `single_oversized_memory_is_retained_truncated_not_shed` — one memory far larger than the
    whole budget. Assert `result.memories.len() == 1`, `result.memories[0].truncated == true`, the
    retained body carries the Commissary per-item truncation marker, and `result.shed.is_empty()`
    (the D-10a edge, at integration level this time).

    Register it in `tests/integration/mod.rs` as a plain `pub mod rag_commissary_test;` in
    alphabetical position, with NO `#[cfg(feature = ...)]` attribute — the existing
    `rag_integration_tests` line immediately after it keeps its `qdrant` gate (that suite needs
    Docker, which CI's `integration-tests` job and this devcontainer do not have).

    Add a `[[test]]` target to the root `Cargo.toml` next to `in_memory_sanctum_integration`:
    `name = "rag_commissary"`, `path = "tests/integration/rag_commissary_test.rs"`, and NO
    `required-features` key.

    Optionally also add a marker assertion to the `qdrant`-gated `rag_integration_tests.rs` (CONTEXT
    Claude's Discretion — harmless; the ungated test is the required evidence). If added, keep it
    to one assertion and do not restructure that suite.

    Commit as `test(33): add the ungated RAG-through-Commissary integration test` (D-26).
  </action>
  <verify>
    <automated>cargo test --test rag_commissary 2>&1 | grep -E 'test result: ok\. [1-9][0-9]* passed' && cargo test --test lib rag_commissary 2>&1 | grep -E 'test result: ok\.' && cargo clippy --workspace --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `tests/integration/rag_commissary_test.rs` exists and contains `application::services::sanctum` (the facade path), `InMemorySanctum`, and all three test function names
    - `Cargo.toml` contains a `[[test]]` block with `name = "rag_commissary"` and that block contains no `required-features` line
    - `tests/integration/mod.rs` contains `pub mod rag_commissary_test;` with no `#[cfg(` attribute on the preceding line
    - `cargo test --test rag_commissary` reports `test result: ok. 3 passed` (or more) with 0 failed, with NO `--features` flag on the command line — proving it is ungated and service-free
    - `cargo clippy --workspace --all-targets -- -D warnings` exits 0
  </acceptance_criteria>
  <done>A service-free, feature-free `cargo test --test rag_commissary` exercises a real `Commissary::dispense` through the published RAG path and asserts shed, marker and truncation in all three directions.</done>
</task>

<task type="auto">
  <name>Task 2: Retire the anti-pattern in prose and prove it with the exit greps</name>
  <read_first>
    - crates/paladin-llm/src/services/commissary.rs lines 14-30 (the module-doc bullet naming the live anti-pattern — the sentence being rewritten)
    - docs/src/architecture/commissary.md (whole file: `## Concept`, `## The model` with its `Stockpile`/`ShedItem` table rows to reuse as vocabulary, `## Usage sketch`, `## Honesty about exactness`, `## See also`)
    - docs/src/getting-started/configuration.md lines 278-290 (the `rag.max_tokens` YAML comment) and lines 408-416 (the four-meanings table row for the RAG injection cap)
    - docs/src/api-reference/upgrading.md line 80 (`## Token primitives`) and docs/src/api-reference/migration-guide.md line 29 (`### Token primitives`)
    - tests/integration/rag_commissary_test.rs (written in Task 1 — the usage sketch must mirror a real test, Phase 30 D-06)
    - .planning/phases/33-commissary-in-tree-adoption/33-CONTEXT.md D-19 and `<specifics>`
  </read_first>
  <files>crates/paladin-llm/src/services/commissary.rs, docs/src/architecture/commissary.md, docs/src/getting-started/configuration.md, docs/src/api-reference/upgrading.md, docs/src/api-reference/migration-guide.md</files>
  <action>
    Rewrite the `paladin-llm` Commissary module-doc sentence at lines 22-25 (D-19). It currently
    names a live in-tree anti-pattern by its function path; replace it with a past-tense sentence
    stating that the silent-drop path it was built to replace — the RAG retrieval service's inline
    byte-length budget estimate — was retired in v0.10.0 when RAG became this module's first
    production caller (Phase 33). **Deliberate deviation from CONTEXT `<specifics>`, recorded
    here:** the wording sketch in `<specifics>` spells out the retired helper's old function
    identifier, which would keep D-19's own exit grep permanently non-empty. D-19's grep is the
    locked requirement and the sketch is a suggestion, so the rewritten sentence names the
    behaviour and the crate, never the retired identifier. ADR-0049 is history and stays exactly as
    written — do not edit it.

    Add an `## In-tree caller: RAG` section to `docs/src/architecture/commissary.md`, placed after
    `## Usage sketch`, whose code sketch mirrors the Task 1 integration test rather than inventing a
    new API shape (Phase 30 D-06): the synthetic `ProviderCapabilities` with the RAG cap, the
    zero-reservation `CommissaryPlan`, one `ConsignmentItem` per ranked memory with the UUID label
    and the rank-order priority, and the `Stockpile` unpacked into the result struct. Reuse the
    page's existing `Stockpile`/`ShedItem` table vocabulary. State the two consequences plainly:
    an oversized single memory is retained truncated rather than dropped, and at a given
    `rag.max_tokens` the Commissary's pessimistic ratio plans fewer bytes than the old estimate
    allowed.

    In `docs/src/getting-started/configuration.md`, extend the `rag.max_tokens` YAML comment and the
    RAG-injection-cap row of the four-meanings table with one sentence: the cap is now enforced by
    the Commissary, shed memories are recorded and a marker naming the omitted count and the budget
    is appended to the injected context.

    In `docs/src/api-reference/upgrading.md` (`## Token primitives`) and
    `docs/src/api-reference/migration-guide.md` (`### Token primitives`), add one RAG line each
    pointing at the MIGRATION.md §9.2 rows plan 33-05 will write — name the changed return type of
    the retrieval call, the changed renderer parameter and the new builder, and tell the reader to
    read `.memories` / `.shed` off the result. Write "v0.10.0", never "v0.11.0" (D-26, Phase 30
    D-18).

    Then run both D-19 exit greps and paste their (empty) output into the commit body.

    Commit as `docs(33): retire the silent-truncation anti-pattern in prose` (D-26).
  </action>
  <verify>
    <automated>test -z "$(grep -rn 'truncate_to_token_budget' crates src docs examples benches 2>/dev/null)" && test -z "$(grep -rnE '\.len\(\) */ *4' crates/paladin-memory/src 2>/dev/null)" && mdbook build docs && cargo test -p paladin-llm --lib commissary 2>&1 | grep -E 'test result: ok\. [1-9][0-9]* passed'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'truncate_to_token_budget' crates src docs examples benches` produces no output (D-19; `CHANGELOG.md`, `MIGRATION.md`, `.planning/` and `.project/` are history and deliberately out of this grep's scope)
    - `grep -rnE '\.len\(\) */ *4' crates/paladin-memory/src` produces no output (D-19)
    - `crates/paladin-llm/src/services/commissary.rs` module doc contains the words `first production caller` and `v0.10.0`
    - `docs/src/architecture/commissary.md` contains a heading `## In-tree caller: RAG` and its sketch names `ConsignmentItem`, `priority` and `Stockpile`
    - `docs/src/getting-started/configuration.md` `rag.max_tokens` row mentions the marker and the shed record
    - Both `docs/src/api-reference/upgrading.md` and `docs/src/api-reference/migration-guide.md` contain a RAG line under their Token-primitives section, and `grep -c 'v0.11.0' docs/src/api-reference/upgrading.md docs/src/api-reference/migration-guide.md` is 0 for both
    - `mdbook build docs` exits 0 (`warning-policy = "error"`)
    - `cargo test -p paladin-llm --lib commissary` still reports `test result: ok.` with 0 failed (the 18 pre-existing Commissary unit tests are untouched — this is a doc-only edit to that crate)
  </acceptance_criteria>
  <done>Both exit greps are empty, the Commissary doc names RAG as its first production caller in the past tense, and the four user-facing doc pages describe the new behaviour.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored memory content → test assertions and CI logs | The integration test stores and renders memory-shaped text; assertions must not print bodies into CI output beyond the fixture strings it created |
| documentation claims → operator expectation | The configuration page is what an operator reads to decide `rag.max_tokens`; a wrong claim there is a silent misconfiguration |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-01 | Information Disclosure | integration-test assertions on shed labels | medium | mitigate | The test asserts labels parse as UUIDs rather than printing bodies; fixture content is synthetic |
| T-33-10 | Repudiation | a grep-only proof standing in for semantic absence | medium | accept | Recorded as a flagged assumption above and in the SUMMARY: the sweep is an identifier search, and the disposition of every other truncation site is a reviewed judgement, not a machine check |
| T-33-11 | Tampering | a doc page that overstates the enforcement | low | mitigate | The configuration page states the cap is planned at the Commissary's pessimistic ratio, matching the CHANGELOG behavioural note plan 33-05 writes |
</threat_model>

<verification>
- `cargo test --test rag_commissary` with no feature flags — at least 3 passed, 0 failed
- Both D-19 exit greps empty
- `mdbook build docs` exits 0
- `cargo clippy --workspace --all-targets -- -D warnings`
</verification>

<success_criteria>
COMM-03 is satisfied: a real production caller of `Commissary::dispense` is exercised by an
ungated integration test that runs without services, and no silent token-based truncation remains
in-tree by the two locked greps.
</success_criteria>

<output>
Create `.planning/phases/33-commissary-in-tree-adoption/33-04-SUMMARY.md` when done
</output>
