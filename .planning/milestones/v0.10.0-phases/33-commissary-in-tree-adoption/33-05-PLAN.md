---
phase: 33-commissary-in-tree-adoption
plan: 05
type: execute
wave: 4
depends_on: ["33-03", "33-04"]
files_modified:
  - MIGRATION.md
  - .cargo/semver-checks-allowlist.toml
  - CHANGELOG.md
  - .project/current-exports.txt
  - crates/paladin-memory/Cargo.toml
  - Cargo.toml
autonomous: true
requirements: [COMM-04]
must_haves:
  truths:
    - "Every lint that `cargo semver-checks` actually fires for this phase's breaks is discovered empirically — six runs with `--release-type minor`, `--default-features` and `--features content-processing` for both `paladin-memory` and `paladin-ai`, plus `paladin-llm` — never guessed, and each fired lint gets its allowlist entry and per-crate suppression in the same commit (D-22, Phase 32 D-14)."
    - "`MIGRATION.md` §9.2 carries one row per `crate | Type` pair — `paladin-memory | RagRetrievalService` covering the return-type and parameter breaks on both inherent methods, and `paladin-memory | retrieve_context_with_timeout` for the free function — and `make check-migration-allowlist` exits 0 with the register set-equal to the allowlist row for row (D-22)."
    - "`CHANGELOG.md` `[0.10.0]` carries the RAG behavioural note, the API `### Changed` bullet and the dependency-edge `### Added` line, the Phase 31 and Phase 32 entries are verified present by grep rather than rewritten, and no `[Unreleased]` section sits above `[0.10.0]` mid-cycle (D-21, RESEARCH Pitfall 4)."
    - "`.project/current-exports.txt` is regenerated in the same commit as the API change it records, so the pre-push baseline-drift gate stays green (D-23)."
  artifacts:
    - path: "MIGRATION.md"
      provides: "the two new §9.2 rows for this phase's published breaks (D-22)"
      contains: "RagRetrievalService"
    - path: ".cargo/semver-checks-allowlist.toml"
      provides: "one [[entry]] per fired lint, row-matched to §9.2 (D-22)"
    - path: "CHANGELOG.md"
      provides: "the [0.10.0] Behavioral changes / Changed / Added entries for the RAG rationing break (D-21)"
      contains: "RAG"
    - path: ".project/current-exports.txt"
      provides: "the regenerated public-surface baseline including the new RAG result/error re-exports (D-23)"
  key_links:
    - from: "MIGRATION.md"
      to: ".cargo/semver-checks-allowlist.toml"
      via: "scripts/check-migration-allowlist.sh row-level set equality in both directions"
      pattern: "check-migration-allowlist"
  prohibitions:
    - statement: "The reduced injected-context volume at an unchanged `rag.max_tokens` must not ship unannounced — it is a user-visible behavioural change (an oversized memory is now retained truncated instead of dropped, and the pessimistic ratio plans fewer bytes than the old estimate allowed) and is written down as one in the release notes, not left for a user to discover from changed model output."
      status: unverified
      verification: flagged
    - statement: "A §9.2 row must never be marked deliberate-breaking to make the set-equality gate pass, and an allowlist entry must never be added for a lint that did not actually fire — the register mirrors measured tool output, so a pair with zero fired lints keeps its row as migration guidance marked N/A with the tool-coverage note."
      status: unverified
      verification: flagged
---

<objective>
Register the break the way this program has registered every other one: discover the fired lints
empirically, write the §9.2 rows and their allowlist mirrors in the same commit, state the
behavioural change plainly in `CHANGELOG.md` `[0.10.0]`, and regenerate the public-surface
baseline. This is the bookkeeping half of COMM-04; plan 33-06 runs the gates on top of it.

Purpose: ADR-0051 grants the clean break, but every break still gets a §9.2 row and an allowlist
row — that is what makes the `0.10.0` tag cuttable.
Output: two §9.2 rows, their matching allowlist entries and per-crate suppressions, the
`[0.10.0]` entries, and a fresh `.project/current-exports.txt`.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-commissary-in-tree-adoption/33-CONTEXT.md
@.planning/phases/33-commissary-in-tree-adoption/33-01-SUMMARY.md
@.planning/phases/32-unified-token-primitives/32-05-SUMMARY.md
</context>

## Artifacts this phase produces

New symbols and files created by Phase 33 (do not treat as drift): `RagRetrievalResult`,
`RagRetainedMemory`, `RagRetrievalError`, `RagRetrievalService::with_token_counter`,
`RagRetrievalService::ration`, `RagRetrievalService::commissary`, the `ShedItem` re-export
(33-01); `rag_omission_marker` (33-02); `ration_respects_budget_and_rank_order` (33-03);
`tests/integration/rag_commissary_test.rs` and its `[[test]] name = "rag_commissary"` target
(33-04); the two new `MIGRATION.md` §9.2 rows and their allowlist entries (this plan);
`33-CI-EVIDENCE.md` and the audit `## 11.` section (33-06).

<!-- planner-discipline-allow: TBD -->

<tasks>

<task type="auto">
  <name>Task 1: Empirical semver-lint discovery — six runs, nothing guessed</name>
  <precondition>`cargo semver-checks --version` reports `0.50.0` (the version `ci.yml` pins and 33-RESEARCH verified installed in this devcontainer)</precondition>
  <read_first>
    - .planning/phases/32-unified-token-primitives/32-05-SUMMARY.md (Task 1: the six-run discovery record, the `qdrant-client` drift workaround, and the zero-lint-pair rule — the template this task repeats)
    - .planning/phases/33-commissary-in-tree-adoption/33-CONTEXT.md D-22 and `<specifics>` (the exact command shapes)
    - .github/workflows/ci.yml lines 340-365 (the `semver` job's per-package command — `--default-features --baseline-version 0.9.0`, no `--release-type`, which is why local discovery must ADD `--release-type minor` to see anything at all)
    - MIGRATION.md lines 195-210 (the `paladin-llm | Commissary` N/A template row and the `paladin-memory | TokenCounter` Y-with-allowlist rows)
  </read_first>
  <files>.planning/phases/33-commissary-in-tree-adoption/33-05-SUMMARY.md</files>
  <action>
    Run the six discovery commands and capture each run's verbatim summary line and every fired
    lint id (D-22, Phase 32 D-14 shape). All six use
    `cargo semver-checks check-release --baseline-version 0.9.0 --release-type minor`:
    `--package paladin-memory --default-features`; `--package paladin-memory --features
    content-processing`; `--package paladin-ai --default-features`; `--package paladin-ai
    --features content-processing`; `--package paladin-llm --default-features` (no change expected
    this phase — confirm empirically rather than asserting it); and one re-run of whichever
    `paladin-memory` invocation fired lints, to confirm the result is stable rather than a cache
    artefact.

    `--release-type minor` is mandatory on every local run: without it the tool compares against the
    already-bumped `0.10.0` manifest version and reports "no semver update required" for changes
    that will absolutely break a `0.9.0` consumer (Phase 32 D-14).

    If a run fails to build its baseline for an environment reason (the `qdrant-client` drift Phase
    32 hit is the known one), apply the same workaround 32-05-SUMMARY.md records and note it — do
    not silently skip a package.

    Record, in this plan's SUMMARY, a six-row table of `# | command | fired lints | verdict`. This
    task writes no register file; it produces the measured input Task 2 consumes. Make no code
    change and no commit (the SUMMARY is written at plan close).
  </action>
  <verify>
    <automated>cargo semver-checks check-release --package paladin-memory --default-features --baseline-version 0.9.0 --release-type minor 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All six commands were run and each one's verbatim final summary line is transcribed into the plan SUMMARY
    - Every fired lint is recorded with its lint id and the crate it fired for; a package with zero fired lints is recorded explicitly as zero, not omitted
    - Every recorded command line contains `--release-type minor`
    - No lint id appears in the record that was not printed by an actual run
  </acceptance_criteria>
  <done>The exact lint set this phase's breaks produce is measured, per package and per feature shape, with the zero results recorded as zeros.</done>
</task>

<task type="auto">
  <name>Task 2: Write the §9.2 rows, their allowlist mirrors and the per-crate suppressions</name>
  <read_first>
    - MIGRATION.md §9.2 (lines ~162-277): the row format, the `paladin-llm | Commissary` row at line 205 (the `N/A`-with-tool-gap template) and the `paladin-memory | TokenCounter` / `TokenCounterFactory` rows at 203-204 (the `Y`-with-allowlist template), plus the explanatory note beneath the table
    - .cargo/semver-checks-allowlist.toml (header comment block: the `[[entry]]` schema `crate` / `lint` / `migration_row` / `requirement_id` / `justification`, and the 18 existing entries for style)
    - scripts/check-migration-allowlist.sh (what the set-equality check actually parses — write rows it can read)
    - crates/paladin-memory/Cargo.toml and Cargo.toml (the `[package.metadata.cargo_semver_checks.lints]` tables, if any lint from Task 1 needs suppressing at tool level)
    - Task 1's measured lint list (this plan's SUMMARY draft)
  </read_first>
  <files>MIGRATION.md, .cargo/semver-checks-allowlist.toml, crates/paladin-memory/Cargo.toml, Cargo.toml</files>
  <action>
    Add exactly two §9.2 rows, keyed `crate | Type` per Phase 29 D-04 (D-22):

    Row 1 — `paladin-memory | RagRetrievalService`: the return-type break on `retrieve_context`
    (now a result struct carrying the retained memories, the shed record and the Stockpile
    accounting), the parameter-type break on `format_for_prompt` (it takes that struct), and the
    additive `with_token_counter` builder. `format_for_prompt` does NOT get its own row — Phase 29
    D-04's keying is `crate | Type`, not `crate | Type | method`, exactly as the `Commissary` row
    already covers both `new` and `from_port` (RESEARCH open question 3). Migration guidance in the
    row: read `.memories` and `.shed` off the result; the retained text to render is each item's
    `body`, never `memory.content`.

    Row 2 — `paladin-memory | retrieve_context_with_timeout`: the free function's return type
    changes the same way and still degrades to an EMPTY result on timeout. Add a `paladin-ai` row
    only if Task 1's discovery actually fired a lint for the facade re-export — an empirical
    condition, not a guess.

    For every lint Task 1 measured as FIRED: add one `[[entry]]` to
    `.cargo/semver-checks-allowlist.toml` naming the crate, the lint id, the matching
    `migration_row` string, `requirement_id = "COMM-04"` (add `COMM-01` where the row also records
    the behavioural break), and a one-line justification that says why the break is deliberate, not
    why it is mitigated — and add the corresponding `"allow"` line to that crate's own
    `[package.metadata.cargo_semver_checks.lints]` table in the SAME commit. For a pair where Task 1
    measured ZERO fired lints, mark the row `N/A` with the tool-coverage note and add NO allowlist
    entry — the `paladin-llm | Commissary` row at MIGRATION.md:205 is the template, and inventing
    an entry to make the gate look symmetrical would corrupt the register.

    Write "v0.10.0" in every new row (D-26, Phase 30 D-18). Confirm `grep -c TBD MIGRATION.md`
    is 0 before committing.

    Commit as `docs(33): register the RAG retrieval API break in MIGRATION.md §9.2` (D-26).
  </action>
  <verify>
    <automated>make check-migration-allowlist && make check-gates && test "$(grep -c TBD MIGRATION.md)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `MIGRATION.md` contains a §9.2 row whose first two cells are `paladin-memory` and `RagRetrievalService`, and one whose first two cells are `paladin-memory` and `retrieve_context_with_timeout`
    - `grep -c TBD MIGRATION.md` is 0
    - `make check-migration-allowlist` exits 0 (row-level set equality in both directions)
    - `make check-gates` exits 0
    - The number of new `[[entry]]` blocks in `.cargo/semver-checks-allowlist.toml` equals the number of distinct crate+lint pairs Task 1 measured as fired — no more, no fewer
    - Every new `[[entry]]` has a non-empty `justification`, and its `migration_row` string matches a §9.2 row verbatim
    - `grep -c 'v0.11.0' MIGRATION.md` is unchanged from before this task (this task introduces none)
  </acceptance_criteria>
  <done>The register, the allowlist and the per-crate suppressions agree row for row with what the tool actually reported.</done>
</task>

<task type="auto">
  <name>Task 3: CHANGELOG [0.10.0] and the regenerated public-surface baseline</name>
  <read_first>
    - CHANGELOG.md lines 1-120 (the `[Unreleased]` header at line 8 with its two Phase 32 facade re-export `### Added` bullets, and `[0.10.0] - 2026-09-10` at line 18 with its `### Behavioral changes` block and the Phase 32 Commissary entry at ~99-114)
    - CHANGELOG.md ~lines 70-77 (the Phase 31 `TokenUsage` bullets — to VERIFY present by grep, never to rewrite)
    - .project/current-exports.txt lines 3958-3975 (the RAG re-exports the regenerated baseline must now extend)
    - Makefile lines 377-383 (`api-surface` / `api-surface-update`)
    - .planning/phases/33-commissary-in-tree-adoption/33-CONTEXT.md D-21 and D-23; 33-RESEARCH.md Pitfall 4
  </read_first>
  <files>CHANGELOG.md, .project/current-exports.txt</files>
  <action>
    Add to `CHANGELOG.md`'s `[0.10.0]` section (D-21):

    - Under `### Behavioral changes`, one bullet in the style of the existing `M-B-0x` entries: RAG
      output now carries a truncation marker and a shed record where it previously dropped
      lower-scoring memories silently; a memory larger than the whole budget is now retained
      truncated rather than dropped entirely (the exact opposite of the old behaviour — the old
      path injected nothing for it); and at a given `rag.max_tokens` the injected volume is planned
      at the Commissary's pessimistic ratio, so roughly 30% fewer bytes are planned than the old
      byte-length estimate allowed. Name `RagRetrievalResult`'s `prompt_tokens` / `allotted_tokens`
      / `exact_tally` as where a caller can see the real usage.
    - Under `### Changed`, one bullet for the API break: the retrieval call's new return type, the
      renderer's new parameter type, the new error enum and the new `with_token_counter` builder,
      pointing at the MIGRATION.md §9.2 rows Task 2 wrote.
    - Under `### Added`, one line for the `paladin-memory` → `paladin-llm` production dependency
      edge.

    Then resolve the `[Unreleased]` discrepancy (RESEARCH Pitfall 4, orchestrator-approved): FOLD
    the two `[Unreleased]` `### Added` bullets (the Commissary facade re-export list and the window-
    resolver facade re-export list, both from Phase 32) into `[0.10.0]`'s `### Added` block, then
    DELETE the now-empty `## [Unreleased]` header. Phase 29 D-18/D-21 bumped `0.10.0` untagged on
    this branch, so nothing should sit above `[0.10.0]` mid-cycle. Record this in the plan SUMMARY
    as a deliberate choice, not an incidental cleanup — COMM-04's criterion reads "the `[0.10.0]`
    section carries the Phase 31/32 API entries", and this is what makes that literally true.

    VERIFY (do not rewrite) the Phase 31 and Phase 32 entries by grep: the `TokenUsage` bullets and
    the Commissary constructor-break entry must still be present under `[0.10.0]` after the edit.

    Regenerate the public-surface baseline with `make api-surface-update` and commit
    `.project/current-exports.txt` in the SAME commit as this CHANGELOG edit (D-23 — the pre-push
    gate from quick task 260916-h40 rejects drift). Confirm the regenerated file gains the new RAG
    result/error/ShedItem/marker re-exports around the existing RAG block.

    Write "v0.10.0" throughout, never "v0.11.0" (D-26).

    Commit as `docs(33): record the RAG rationing behaviour change in CHANGELOG [0.10.0]` (D-26).
  </action>
  <verify>
    <automated>make api-surface && test "$(grep -c '^## \[Unreleased\]' CHANGELOG.md)" = "0" && grep -q 'RagRetrievalResult' .project/current-exports.txt && awk '/^## \[0.10.0\]/,/^## \[0.9/' CHANGELOG.md | grep -qi 'rag' && awk '/^## \[0.10.0\]/,/^## \[0.9/' CHANGELOG.md | grep -q 'TokenUsage' && awk '/^## \[0.10.0\]/,/^## \[0.9/' CHANGELOG.md | grep -q 'Commissary'</automated>
  </verify>
  <acceptance_criteria>
    - `CHANGELOG.md` contains no `## [Unreleased]` header
    - The `[0.10.0]` section (from its header to the next `## [` header) contains: a behavioural bullet mentioning RAG and the truncation marker, a `### Changed` bullet naming the retrieval return type, an `### Added` line naming the `paladin-llm` dependency edge, the Phase 32 facade re-export bullets folded in, the Phase 31 `TokenUsage` entries and the Phase 32 `Commissary` entry
    - `grep -c 'v0.11.0' CHANGELOG.md` is 0
    - `.project/current-exports.txt` contains `RagRetrievalResult`, `RagRetrievalError` and `ShedItem` under the `application::services::sanctum` paths
    - `make api-surface` exits 0 (baseline matches the built surface — no drift)
    - The CHANGELOG edit and the regenerated baseline are in one commit (`git show --stat HEAD` lists both files)
  </acceptance_criteria>
  <done>The release notes state the behavioural change plainly, the `[0.10.0]` section literally carries the Phase 31/32 entries, and the API baseline matches the tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| measured tool output → release register | What `cargo semver-checks` printed is the only legitimate source for an allowlist entry |
| release register → downstream consumer | §9.2 and the CHANGELOG are what a downstream maintainer reads before upgrading |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-12 | Tampering | `.cargo/semver-checks-allowlist.toml` entries | high | mitigate | Entries are written only for lints Task 1 measured as fired; the acceptance criterion pins the entry count to the measured pair count, and `make check-migration-allowlist` enforces set equality both ways |
| T-33-13 | Repudiation | a behavioural change shipping without a release note | high | mitigate | D-21's three bullets plus the grep-scoped acceptance criteria over the `[0.10.0]` section only — a bullet anywhere else in the file does not satisfy them |
| T-33-14 | Tampering | public-surface baseline drift | medium | mitigate | `make api-surface-update` in the same commit as the API change; `make api-surface` is the verify |
</threat_model>

<verification>
- `make check-migration-allowlist`, `make check-gates`, `grep -c TBD MIGRATION.md` = 0
- `make api-surface` exits 0
- `[0.10.0]`-scoped CHANGELOG greps for the RAG note, `TokenUsage` and `Commissary`
</verification>

<success_criteria>
COMM-04's register half is done: every measured break has a §9.2 row and a matching allowlist
entry, the `[0.10.0]` section carries the RAG note plus the Phase 31/32 entries, and the API
baseline is current — so plan 33-06's gate sweep can run on a tree whose paperwork already agrees
with its code.
</success_criteria>

<output>
Create `.planning/phases/33-commissary-in-tree-adoption/33-05-SUMMARY.md` when done
</output>
