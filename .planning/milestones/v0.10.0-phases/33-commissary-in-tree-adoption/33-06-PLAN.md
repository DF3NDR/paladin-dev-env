---
phase: 33-commissary-in-tree-adoption
plan: 06
type: execute
wave: 5
depends_on: ["33-05"]
files_modified:
  - .planning/phases/33-commissary-in-tree-adoption/33-CI-EVIDENCE.md
  - .project/v0.10.0/09-program-acceptance-audit.md
  - .planning/phases/29-program-gates-release/29-ACCEPTANCE-AUDIT.md
autonomous: true
requirements: [COMM-04]
must_haves:
  truths:
    - "Every Phase 29 release gate named in D-24 is re-run on this phase's final commit and each result — command, verdict, head SHA, date — is recorded, including the ones that are not green (D-24, D-25)."
    - "The Phase 32 PRIM-04 regression check is green and recorded: the history-trimmer limit-resolution tests and the pre-resolver kept-set equivalence snapshot still pass, with no code change in this phase (D-20)."
    - "Evidence lands as a new `## 11.` section appended to the existing corpus acceptance audit plus a phase-local `33-CI-EVIDENCE.md`, and as a one-paragraph re-seal note on the Phase 29 pointer file — no new audit document is created (D-25)."
    - "The seven Phase 29 maintainer sign-off boxes are untouched and §11 adds exactly one more UNTICKED human-only box stating that the `0.10.0` tag may be cut; no agent ticks any of them (D-25, Phase 29 D-17)."
    - "Findings are recorded, never silently fixed: the pre-existing `cargo doc --workspace --no-deps` warning condition is carried as a recorded condition, explicitly not a gate (D-24, Phase 29 §8 and the Phase 32 32-05 precedent)."
    - statement: "A re-seal sweep interrupted partway is detectable rather than silently green: every §11 subsection records the head SHA and date its gate was run on, any gate whose recorded SHA differs from the phase's final commit is re-run before the section is considered complete, and the one gate this devcontainer cannot run at all (the 82% coverage floor, which needs Docker-backed Redis/MinIO) is labelled CI-attributed with the CI job named rather than reported as a local pass."
      verification: backstop
  artifacts:
    - path: ".planning/phases/33-commissary-in-tree-adoption/33-CI-EVIDENCE.md"
      provides: "the local sweep table and CI-run table in the 29-CI-EVIDENCE.md shape, with the D-19 exit grep as row 1 (D-25, CONTEXT <specifics>)"
      min_lines: 60
    - path: ".project/v0.10.0/09-program-acceptance-audit.md"
      provides: "the appended `## 11. Re-seal after Phases 30-33 (Phase 33, COMM-04)` section, one subsection per gate (D-25)"
      contains: "## 11. Re-seal after Phases 30-33"
  key_links:
    - from: ".planning/phases/29-program-gates-release/29-ACCEPTANCE-AUDIT.md"
      to: ".project/v0.10.0/09-program-acceptance-audit.md"
      via: "a one-paragraph 'Re-sealed on <SHA>' note appended to the existing pointer (D-25)"
      pattern: "Re-sealed on"
  prohibitions:
    - statement: "A release-gate finding must be recorded, never silently fixed or omitted from the evidence record, and no agent may tick a maintainer sign-off box — the judgment-tier sign-off is closed by a human reading the evidence, not by the process that produced it."
      status: unverified
      verification: flagged
    - statement: "A gate that could not be run locally must never be written up as a local pass — it is labelled CI-attributed with the CI job named, or it is recorded as not-run."
      status: unverified
      verification: flagged
---

<objective>
Re-seal the Phase 29 release gates on this phase's final commit, because Phases 31-33 changed
public API after Phase 29 sealed them. Run D-24's gate list, run the D-20 regression check, and
record the evidence where Phase 29 decided evidence lives — a new `## 11.` section in the corpus
acceptance audit, a phase-local CI-evidence file, and a pointer note — with one more unticked
human-only sign-off box for the tag.

Purpose: this is what makes `v0.10.0` releasable again (ADR-0051); the tag itself is cut on the
`main` merge by `release.yml`, outside this phase.
Output: a measured, honestly-labelled gate record on the final commit.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-commissary-in-tree-adoption/33-CONTEXT.md
@.planning/phases/33-commissary-in-tree-adoption/33-05-SUMMARY.md
@.planning/phases/29-program-gates-release/29-CI-EVIDENCE.md
</context>

## Artifacts this phase produces

New symbols and files created by Phase 33 (do not treat as drift): `RagRetrievalResult`,
`RagRetainedMemory`, `RagRetrievalError`, `RagRetrievalService::with_token_counter`,
`RagRetrievalService::ration`, `RagRetrievalService::commissary`, the `ShedItem` re-export
(33-01); `rag_omission_marker` (33-02); `ration_respects_budget_and_rank_order` (33-03);
`tests/integration/rag_commissary_test.rs` and its `[[test]] name = "rag_commissary"` target
(33-04); the two `MIGRATION.md` §9.2 rows and their allowlist entries (33-05);
`.planning/phases/33-commissary-in-tree-adoption/33-CI-EVIDENCE.md` and the
`## 11. Re-seal after Phases 30-33 (Phase 33, COMM-04)` audit section (this plan).

<!-- planner-discipline-allow: TBD -->

<tasks>

<task type="auto">
  <name>Task 1: Run the D-24 gate sweep and the D-20 regression check on the final commit</name>
  <precondition>`cargo semver-checks --version` reports 0.50.0, `rustup toolchain list` includes a `1.88` toolchain, and `docker info` FAILS (this devcontainer has no Docker — the coverage gate is therefore CI-attributed, per 33-RESEARCH Environment Availability)</precondition>
  <read_first>
    - .planning/phases/33-commissary-in-tree-adoption/33-CONTEXT.md D-20, D-24 and the Folded Todos note (the local-vs-CI coverage reproduction question)
    - .planning/phases/29-program-gates-release/29-CI-EVIDENCE.md (the `## Local sweep` table columns `# | Command | Result | Verdict` and the `## CI-run table` shape)
    - .project/v0.10.0/09-program-acceptance-audit.md §10 "Release readiness" (~line 1291) and its four subsections — the gate list being re-run — plus §8's `cargo doc` condition at ~line 1085 (carried, not a gate)
    - .github/workflows/ci.yml: `msrv` (251), `semver` (303, per-package command ~357), `integration-tests` (632), `coverage` (1344), `publish-dry-run` (1974 — `main`-push only), `crate-isolation` (583)
    - Makefile: `check-gates` (215), `coverage` (307), `openapi` (372), `api-surface` (378), `security` (401), `clean-code` (419), `publish-dry-run` (567)
    - src/application/services/paladin/middleware/history.rs (the three `limit_resolution_*` tests at 302-340 and `kept_set_equivalence_snapshot_pre_resolver` at 634 — the D-20 regression targets)
  </read_first>
  <files>.planning/phases/33-commissary-in-tree-adoption/33-CI-EVIDENCE.md</files>
  <action>
    Run every gate in D-24's list on the current HEAD (which must already contain plans 33-01
    through 33-05) and capture each one's verbatim final line plus its exit status. Record the head
    SHA once, at the start, and re-record it if any task in this plan produces a commit that moves
    it.

    The list, each with its exact command: `grep -c TBD MIGRATION.md` (expect 0);
    `make check-migration-allowlist` and `make check-gates`;
    `cargo test --features web-server --test v0_9_config_boot`;
    `cargo test -p paladin-web --test openapi_golden_v0_9`; the CI `semver` job's per-package
    command `cargo semver-checks check-release --package <pkg> --default-features --baseline-version
    0.9.0` for every publishable crate, plus the 33-05 Task 1 discovery runs referenced by pointer
    rather than repeated; `RUSTUP_TOOLCHAIN=1.88 cargo check --workspace --all-features --all-targets
    --locked`; `make publish-dry-run`; `make api-surface`; `make clean-code`; `make security`; and
    the exit grep from 33-04 re-run on the final tree.

    Coverage: attempt `make coverage` (CI's exact invocation is `cargo llvm-cov --workspace
    --features integration-tests,llm-all --lcov --output-path lcov.info --fail-under-lines 82 --
    --test-threads=1`). Docker is absent in this devcontainer, so Redis/MinIO-backed tests
    self-skip and the figure is not comparable — record the gate as CI-attributed, naming the CI
    `coverage` job, and do NOT write it up as a local pass. This is also where the folded todo
    (`2026-08-13-verify-local-coverage-reproduction`) is answered: state plainly whether the local
    run could reproduce CI's 82.39% figure and why not. The todo keeps its pending, no-
    `resolves_phase` status — record the answer, do not close the todo.

    D-20 regression check (no code change): `cargo test -p paladin-ai --lib limit_resolution` and
    `cargo test -p paladin-ai --lib kept_set_equivalence_snapshot_pre_resolver`, both of which must
    report a non-zero passed count — a filter that selects zero tests still exits 0, so the passed
    count is the evidence, not the exit status.

    Carry, do not fix: the pre-existing `cargo doc --workspace --no-deps` warning condition is
    recorded as a condition with its current warning count, explicitly marked "not a gate"
    (Phase 29 §8, Phase 32 32-05 precedent). Any NEW finding is recorded as a finding — do not
    silently fix it inside this plan (Phase 29 D-12).

    Write the results into a new `.planning/phases/33-commissary-in-tree-adoption/33-CI-EVIDENCE.md`
    copying `29-CI-EVIDENCE.md`'s shape exactly: a header block (phase, branch, head SHA, date), a
    `## Local sweep` table with columns `# | Command | Result | Verdict`, and a `## CI-run table`
    section for the workflow runs this branch has (or a plain statement that the pre-merge push has
    not happened yet, with the post-merge `publish-dry-run` run to be recorded afterwards per
    Phase 29 D-21's two-SHA rule). Row 1 of the local sweep is the D-19 exit grep, so the F6 closure
    is the first thing a reader sees (CONTEXT `<specifics>`).

    Commit as `docs(33): record the Phase 29 gate re-seal evidence` (D-26).
  </action>
  <verify>
    <automated>test "$(grep -c TBD MIGRATION.md)" = "0" && make check-gates && make api-surface && cargo test -p paladin-ai --lib limit_resolution 2>&1 | grep -E 'test result: ok\. [1-9][0-9]* passed' && cargo test -p paladin-ai --lib kept_set_equivalence_snapshot_pre_resolver 2>&1 | grep -E 'test result: ok\. [1-9][0-9]* passed' && test -s .planning/phases/33-commissary-in-tree-adoption/33-CI-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `33-CI-EVIDENCE.md` exists with a `## Local sweep` table whose first data row is the D-19 exit grep, and with a `## CI-run table` section
    - Every gate named in CONTEXT D-24 appears as its own row with its exact command and a verdict; a gate that was not run locally carries the verdict `CI-attributed` (with the CI job named) or `not run`, never a bare pass
    - `cargo test -p paladin-ai --lib limit_resolution` and `cargo test -p paladin-ai --lib kept_set_equivalence_snapshot_pre_resolver` each report `test result: ok.` with a passed count of at least 1
    - The evidence file names the head SHA it was produced on and today's date
    - The `cargo doc --workspace --no-deps` row is present, carries its measured warning count, and is labelled as a recorded condition rather than a gate
    - The coverage row states whether the local run reproduced CI's figure and why, and cites the CI `coverage` job as the evidence source
    - `make check-gates` and `make api-surface` exit 0
  </acceptance_criteria>
  <done>Every gate in D-24 has a measured, honestly-labelled result on a named SHA, and the Phase 32 regression check is green with a non-zero test count.</done>
</task>

<task type="auto">
  <name>Task 2: Append §11 to the corpus audit and the re-seal note to the Phase 29 pointer</name>
  <read_first>
    - .project/v0.10.0/09-program-acceptance-audit.md: §10 "Release readiness" (~1291-1400) as the structural template for §11, the `## Maintainer sign-off (unticked — closed by a human, not this audit)` block at ~1249 (the seven boxes that must remain exactly as they are), and §8's carried `cargo doc` condition at ~1085
    - .planning/phases/29-program-gates-release/29-ACCEPTANCE-AUDIT.md (the pointer file that receives the one-paragraph note)
    - .planning/phases/33-commissary-in-tree-adoption/33-CI-EVIDENCE.md (Task 1's measured results — §11 cites these, it does not re-derive them)
    - .planning/phases/33-commissary-in-tree-adoption/33-CONTEXT.md D-25
  </read_first>
  <files>.project/v0.10.0/09-program-acceptance-audit.md, .planning/phases/29-program-gates-release/29-ACCEPTANCE-AUDIT.md</files>
  <action>
    Append a new section `## 11. Re-seal after Phases 30-33 (Phase 33, COMM-04)` to
    `.project/v0.10.0/09-program-acceptance-audit.md` — appended, never a new audit document, and
    never an edit to §§1-10 (D-25, Phase 29 D-10). One subsection per gate from Task 1, each
    carrying the exact command, the verdict, the head SHA it was run on and the date. Open the
    section with two sentences stating why it exists: Phases 31, 32 and 33 changed public API after
    Phase 29 sealed the gates, so the gates are re-run on Phase 33's final commit, and this is the
    precondition for the `0.10.0` tag under ADR-0051.

    Include, verbatim from Task 1's record: the exit-grep closure of F6, the MIGRATION/allowlist
    set-equality result, both frozen compat tests, the per-package semver results, the MSRV check,
    the publish dry run, the API-surface check, `make clean-code`, `make security`, the
    CI-attributed coverage floor, the CHANGELOG greps, and the D-20 regression check. Record the
    `cargo doc` warning condition as a carried condition, explicitly not a gate.

    Add exactly ONE more sign-off box at the end of §11, unticked: a `- [ ]` line stating that the
    `0.10.0` tag may be cut. Do not tick it. Do not touch the seven existing Phase 29 boxes — not
    their text, not their state (D-25, Phase 29 D-17: judgment-tier sign-off is closed by a human).

    Append one paragraph to `.planning/phases/29-program-gates-release/29-ACCEPTANCE-AUDIT.md`'s
    pointer: "Re-sealed on `<head SHA>`, 2026-09-…" naming Phase 33 / COMM-04 and pointing at
    audit §11 and `33-CI-EVIDENCE.md`. Change nothing else in that file.

    Write "v0.10.0" throughout, never "v0.11.0" (D-26).

    Commit as `docs(33): append the Phase 33 re-seal section to the acceptance audit` (D-26).
  </action>
  <verify>
    <automated>grep -q '^## 11\. Re-seal after Phases 30-33' .project/v0.10.0/09-program-acceptance-audit.md && test "$(awk '/^## 11\. Re-seal after Phases 30-33/,0' .project/v0.10.0/09-program-acceptance-audit.md | grep -c '^- \[x\]')" = "0" && grep -q 'Re-sealed on' .planning/phases/29-program-gates-release/29-ACCEPTANCE-AUDIT.md && test "$(grep -c 'v0.11.0' .project/v0.10.0/09-program-acceptance-audit.md)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `.project/v0.10.0/09-program-acceptance-audit.md` contains `## 11. Re-seal after Phases 30-33 (Phase 33, COMM-04)` and the section contains one subsection per D-24 gate, each with a command, a verdict, a SHA and a date
    - Within §11 there is exactly one `- [ ]` sign-off line and zero `- [x]` lines
    - `git diff HEAD~1 -- .project/v0.10.0/09-program-acceptance-audit.md` shows additions only after the previous end of file — no line inside §§1-10 is modified, and the seven Phase 29 sign-off boxes are byte-identical to before
    - `.planning/phases/29-program-gates-release/29-ACCEPTANCE-AUDIT.md` gained exactly one paragraph containing `Re-sealed on` and a SHA, with no other change
    - `grep -c 'v0.11.0'` is 0 for both edited files
    - No new audit document was created anywhere under `.project/` or `.planning/`
  </acceptance_criteria>
  <done>The re-seal evidence lives in the corpus audit as §11 with a single unticked human-only box, the Phase 29 pointer records it, and nothing an agent must not touch was touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| measured gate output → release evidence | The audit is what a maintainer reads before cutting a tag; an unmeasured claim there is the highest-consequence falsehood in the phase |
| agent authority → human sign-off | The sign-off boxes are the explicit human-only control; the agent writes evidence beneath them and never closes them |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-15 | Spoofing | an agent ticking a maintainer sign-off box | high | mitigate | D-25/Phase 29 D-17: the acceptance criterion asserts zero `- [x]` lines inside §11 and byte-identical Phase 29 boxes; the diff check enforces append-only |
| T-33-16 | Repudiation | a gate written up as passing when it was not run | high | mitigate | Gates that cannot run locally carry `CI-attributed` (with the job named) or `not run`; the coverage row must state why the local run is not comparable |
| T-33-17 | Tampering | quietly fixing a finding instead of recording it | medium | mitigate | Phase 29 D-12 carried forward: new findings are recorded in §11 and the SUMMARY; the `cargo doc` condition is carried, explicitly not a gate |
| T-33-18 | Repudiation | an interrupted sweep read as complete | medium | accept | Each §11 subsection records the head SHA and date its gate ran on, so a mismatch against the final commit is visible — the backstop truth in this plan's `must_haves` carries the residual |
</threat_model>

<verification>
- `grep -c TBD MIGRATION.md` = 0, `make check-gates`, `make api-surface`
- Both D-20 regression commands report a non-zero passed count
- §11 present, append-only, with exactly one unticked box and zero ticked boxes
- `Re-sealed on` present in the Phase 29 pointer file
</verification>

<success_criteria>
COMM-04 is satisfied: the Phase 29 release gates are re-run on Phase 33's final commit with
per-gate evidence appended to the existing acceptance audit rather than a new one, the CHANGELOG
and MIGRATION register agree with the code, and the only thing standing between this branch and a
`0.10.0` tag is a human ticking one box.
</success_criteria>

<output>
Create `.planning/phases/33-commissary-in-tree-adoption/33-06-SUMMARY.md` when done
</output>
