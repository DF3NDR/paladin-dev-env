---
phase: 34-documentation-currency-audit
plan: 03
type: execute
wave: 3
depends_on: ["34-02"]
files_modified:
  - .planning/phases/34-documentation-currency-audit/34-AUDIT.md
  - .planning/phases/34-documentation-currency-audit/34-EVIDENCE.md
  - .planning/phases/34-documentation-currency-audit/34-evidence/
autonomous: true
requirements: [CURR-01, CURR-05]
must_haves:
  truths:
    - "The mdBook build itself is measured, not assumed: the exact docs.yml sequence is run and its result recorded, green or red (D-11)."
    - "The corpus-wide vocabulary sweep is recorded with its real hit count, including the architecture/commissary.md hit, rather than claimed clean (D-10, D-00f)."
    - "Every page in docs/src root, getting-started/, architecture/ and api-reference/ carries a settled verdict whose findings cell names the commands actually run (D-06, D-07)."
    - "upgrading.md and migration-guide.md are checked row-for-row against MIGRATION.md §9.1 and §9.8, each disagreement its own MB row (D-09)."
    - "Every orphan — a page present on disk and absent from SUMMARY.md — is itself recorded as a finding (D-05)."
    - "The folded MinIO todo is closed to its documentation slice: every docs/src and examples hit naming a retired image or download URL is recorded with file:line, and the RustFS evaluation stays deferred (CONTEXT.md Folded Todos)."
  artifacts:
    - ".planning/phases/34-documentation-currency-audit/34-AUDIT.md (§2 build baseline subsection + 18 settled rows)"
    - ".planning/phases/34-documentation-currency-audit/34-evidence/34-03-mdbook-build.txt"
  key_links:
    - "docs.yml pinned tool versions ↔ the audit header's toolchain block ↔ the build baseline subsection."
    - "§1 SS-nn rows ↔ the Cites cell of every stale/missing row this plan settles."
    - "34-signals.sh nine classes ↔ each row's findings cell."
  prohibitions:
    - statement: "No file outside .planning/ is created, modified or deleted (ROADMAP SC5, D-22)."
      status: required
      verification: "git status --porcelain -- . ':!.planning' prints nothing after the mdbook-mermaid install step, with docs/ restored via git checkout -- docs/ if the pin drifted."
    - statement: "No page verdict is settled by file existence, modification time or git log (D-00b)."
      status: required
      verification: "34-check.sh assertion (c) fails any settled row whose findings cell is empty or still the seeded placeholder."
---

<objective>
Measure the mdBook build baseline and settle the first 18 page verdicts — the root pages,
`getting-started/`, `architecture/` and `api-reference/`.

Purpose: these are the pages a reader hits first and the ones that carry the release-facing
contracts (`upgrading.md`, `migration-guide.md`, `stable-api.md`, `platform-api.md`,
`feature-flags.md`, `crate-map.md`). They are also where the Phase 30-33 vocabulary and token-split
changes bite hardest.
Output: `34-AUDIT.md` §2 build-baseline subsection plus 18 settled rows, and the raw build capture
under `34-evidence/`.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-documentation-currency-audit/34-CONTEXT.md
@.planning/phases/34-documentation-currency-audit/34-RESEARCH.md
@.planning/phases/34-documentation-currency-audit/34-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Phase 34 writes only inside `.planning/` — full list in `34-01-PLAN.md`. This plan creates the
`34-evidence/` subdirectory for raw captures (D-02 discretion) and mints `MB-nn` IDs continuing
from `MB-01`, which 34-01 assigned to `docs/src/appendix/doc-coverage-report.md`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: mdBook build baseline, link check, config/doc-example gates and the corpus-wide vocabulary sweep</name>
  <files>.planning/phases/34-documentation-currency-audit/34-AUDIT.md, .planning/phases/34-documentation-currency-audit/34-EVIDENCE.md, .planning/phases/34-documentation-currency-audit/34-evidence/34-03-mdbook-build.txt</files>
  <precondition>`mdbook`, `mdbook-linkcheck` and `mdbook-mermaid` are on PATH at the versions `.github/workflows/docs.yml` pins (0.4.40 / 0.7.7 / 0.13.0). Assert with `mdbook --version && mdbook-linkcheck --version && mdbook-mermaid --version` before running the sequence; if a binary is missing or its version differs from the pin, halt and record the deviation as a finding rather than installing anything.</precondition>
  <read_first>
    - `.github/workflows/docs.yml` — the exact build sequence and the three `--version` pins. The sequence this task reproduces is the one that workflow runs, in its order.
    - `docs/book.toml` — the `[output.linkcheck]` settings, specifically `warning-policy` and `follow-web-links`.
    - `scripts/check-doc-examples.sh` — its three layers, so the recorded result distinguishes Layer 1 (`cargo check` on the doc-examples crate), Layer 1b (README quick-example mirror) and Layer 2 (inline fenced-block scan).
    - `scripts/check-doc-config.sh` — what its pass means (YAML fenced blocks parse).
    - `docs/src/SUMMARY.md` — the nav, needed for the orphan check.
    - `.planning/phases/31-lossless-token-accounting/31-CONTEXT.md` D-29 — the `token_count` doc hit list this task re-checks.
    - `.planning/phases/34-documentation-currency-audit/34-AUDIT.md` — the §2 heading and column header written by 34-01.
  </read_first>
  <action>
Run the `docs.yml` sequence in its own order and record every step, whether green or red.

First `mdbook-mermaid install docs/`. Immediately afterwards run `git status --porcelain -- docs`.
If it is not clean, the generated `docs/mermaid.min.js` / `docs/mermaid-init.js` drifted from the
pin: restore them with `git checkout -- docs/` and record the drift as an `MB-nn` finding for
Phase 35 (a pinned generated asset that does not round-trip), not as a Phase 34 edit. If it is
clean, record that too — D-22 asks for the check, not just the remedy.

Then `mdbook build docs/` with the linkcheck backend active, teeing the whole run to
`.planning/phases/34-documentation-currency-audit/34-evidence/34-03-mdbook-build.txt`. Record the
exit code, the wall time and, verbatim in the evidence file, the linkcheck summary line. Then
`bash scripts/check-doc-examples.sh` and `bash scripts/check-doc-config.sh`, each teed into the
same evidence file under its own heading, each recorded with its exit code and its counts (for the
config gate, the number of YAML blocks checked and the number failed; for the doc-examples gate,
the per-layer result). Any red step is a finding: a build or link failure routes to Phase 35 as an
`MB-nn`; a `crates/doc-examples` compile failure routes to Phase 36 as an `EX-nn` and is recorded
as a pointer here for plan 34-08 to pick up in §4. Follow the `16-LINKCHECK-REPORT.md` shape from
34-PATTERNS.md: a timestamped run window, the tool-version block against the `docs.yml` pins, an
explicit statement of whether the mermaid install mutated `docs/`, and a labelled run heading per
attempt if a first run is red.

Then the orphan check (D-05). Build the set of pages linked from `docs/src/SUMMARY.md` by
extracting its markdown link targets, and `comm` it against `find docs/src -name '*.md' | sort`.
`SUMMARY.md` itself is not an orphan. Record the result as a subsection: the count of pages on
disk, the count reachable from the nav, and the list of any orphans. An orphan is classified
*stale content* with the note that it is orphaned from nav, gets its own `MB-nn` row and a proposed
nav position, and routes to Phase 35 to link or archive. If the orphan set is empty, say so with
the command that proved it — RESEARCH.md measured zero orphans, but D-00b forbids carrying that
forward as an assumption.

Then the vocabulary sweep (D-10, D-00f). Run `grep -rniE '\bQuartermaster\b' docs/src` and record
its full output verbatim in the evidence file with the hit count. RESEARCH.md P-07 found exactly
one hit, at `docs/src/architecture/commissary.md:7`, in a sentence pointing at the rename ADR — a
legitimate historical pointer, not a stray leftover, but a literal match for the grep D-10
specifies. Record it as its own `MB-nn` row with the classification *stale content*, the exact
line, and an explicit note that whether an ADR-pointer sentence is an intentional exception to the
must-be-empty rule is Phase 35's call, not this audit's (D-00c: recorded, never fixed). Do not
prejudge it and do not edit the page. Then re-check every page on the Phase 31 D-29 `token_count`
hit list for a bare token total sitting beside a prompt/completion `TokenUsage` split: for each
page on that list, run a grep for the bare-count token and for the split type, and record for each
page whether a bare total appears without its split. Each offending page becomes an `MB-nn` row;
pages on the list that are clean are recorded as clean with the command that proved it, so the
list's coverage is visible.

Then the object-store currency sweep (CONTEXT.md Folded Todos, the MinIO slice). Run a
corpus-wide `grep -rniE 'minio|dl\.min\.io|quay\.io' docs/src examples` and record the full hit
list verbatim in the evidence file. Every hit is checked against the pin quick task 260913-15w
introduced: the Docker Hub community images are retired (they 404 since 2026-09-12) and
`dl.min.io` returns 410, so the current form is a `quay.io` image pin plus the `mc` client fetched
from the archived GitHub release asset. A page or example still naming the retired Docker Hub image
or a `dl.min.io` download URL is `stale`, and each such page gets an `MB-nn` row here with the exact
`file:line` so the plan that settles that page's verdict can cite this row rather than re-deriving
it. The RustFS evaluation itself is infrastructure work and stays out of scope — record it as a
pointer for plan 34-09's deferred register, not as an `MB-nn` item.

Append every command above to `34-EVIDENCE.md` as numbered rows in the `33-CI-EVIDENCE.md` shape —
command, result, verdict — and mark each verdict cell as a baseline this phase carries rather than
a gate it enforces, since D-00c makes this phase's fix set empty by construction. Close with the
SC5 row: `git status --porcelain -- . ':!.planning'`, verbatim empty output.
  </action>
  <verify>
    <automated>cd /workspace && D=.planning/phases/34-documentation-currency-audit && test -s $D/34-evidence/34-03-mdbook-build.txt && grep -qi 'linkcheck' $D/34-evidence/34-03-mdbook-build.txt && grep -q 'check-doc-config' $D/34-EVIDENCE.md && grep -q 'check-doc-examples' $D/34-EVIDENCE.md && grep -q 'docs/src/architecture/commissary.md:7' $D/34-AUDIT.md && grep -qi 'orphan' $D/34-AUDIT.md && test -z "$(git status --porcelain -- . ':!.planning' && git status --porcelain -- docs)" && bash $D/34-check.sh --seed && echo BASELINE-OK</automated>
  </verify>
  <acceptance_criteria>
    - `34-evidence/34-03-mdbook-build.txt` exists, is non-empty, and contains the verbatim linkcheck summary line plus the `check-doc-examples.sh` and `check-doc-config.sh` captures under their own headings.
    - `34-EVIDENCE.md` gained one numbered row per command run — at minimum: `mdbook-mermaid install docs/`, the post-install `git status --porcelain -- docs`, `mdbook build docs/`, `bash scripts/check-doc-examples.sh`, `bash scripts/check-doc-config.sh`, the SUMMARY.md orphan `comm`, the vocabulary grep, and the SC5 `git status`.
    - Each of those rows carries an exit code or a count in its Result cell — no Result cell reads only pass or fail.
    - `34-AUDIT.md` §2 contains a build-baseline subsection with the timestamped run window, the three tool versions each annotated with its `docs.yml` pin, and an explicit sentence stating whether `mdbook-mermaid install docs/` mutated `docs/`.
    - The orphan subsection states the on-disk page count, the nav-reachable count and the orphan list (or states the list is empty and names the `comm` command that proved it).
    - `grep -c 'docs/src/architecture/commissary.md:7' 34-AUDIT.md` is at least 1, and that row's classification cell reads `stale content` and its note defers the verdict to Phase 35.
    - Every page on the Phase 31 D-29 hit list appears in the token-split subsection with either an offending finding or a clean result naming the grep that proved it.
    - `34-AUDIT.md` contains an object-store subsection holding the verbatim `grep -rniE 'minio|dl\.min\.io|quay\.io' docs/src examples` hit list with a per-hit `file:line`, and every hit naming a retired Docker Hub image or a `dl.min.io` URL carries an `MB-nn` ID; the RustFS evaluation is recorded as a deferred-register pointer, not an `MB-nn`.
    - `git status --porcelain -- docs` prints nothing at task end.
    - `bash 34-check.sh --seed` exits 0; `git status --porcelain -- . ':!.planning'` prints nothing.
  </acceptance_criteria>
  <done>The mdBook build, linkcheck, config gate, doc-examples gate, orphan set and vocabulary sweep are all measured at this HEAD and recorded with their producing commands; nothing outside .planning/ changed, including the generated mermaid assets.</done>
  <reversibility rating="reversible">Everything written is a `.planning/` record; the one tree-touching step (`mdbook-mermaid install docs/`) is explicitly checked and restored with `git checkout -- docs/`.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Settle verdicts for the 18 root, getting-started, architecture and api-reference pages</name>
  <files>.planning/phases/34-documentation-currency-audit/34-AUDIT.md, .planning/phases/34-documentation-currency-audit/34-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/34-documentation-currency-audit/34-AUDIT.md` — §1 (the shipped-surface checklist compiled by 34-02, the citation source for every stale/missing verdict) and the §2 column header and seeded rows.
    - `docs/src/SUMMARY.md` and `docs/src/introduction.md`.
    - `docs/src/getting-started/installation.md`, `configuration.md`, `quickstart.md`.
    - `docs/src/architecture/overview.md`, `hexagonal-design.md`, `domain-model.md`, `design-patterns.md`, `crate-map.md`, `commissary.md`.
    - `docs/src/api-reference/stable-api.md`, `platform-api.md`, `feature-flags.md`, `crate-map.md`, `migration-guide.md`, `upgrading.md`, `wargraph-doc-schema.md`.
    - `MIGRATION.md` §9.1 (behavioral changes) and §9.8 (operator checklist) — the two sections D-09 requires `upgrading.md` and `migration-guide.md` to agree with row-for-row.
    - `Cargo.toml` `[workspace.package]` and `ls crates/` — the live answers signal classes 1 and 3 compare against.
    - `.planning/phases/34-documentation-currency-audit/34-CONTEXT.md` D-06 (verdict definitions), D-07 (evidence per page), D-09, D-04 (sizing).
  </read_first>
  <action>
Settle a verdict for each of these eighteen pages, replacing its seeded row: `docs/src/SUMMARY.md`,
`docs/src/introduction.md`, the three `getting-started/` pages, the six `architecture/` pages and
the seven `api-reference/` pages.

Method per page, in order. Run `bash .planning/phases/34-documentation-currency-audit/34-signals.sh <page>`
and read its nine class lines. Then read the page. Then compare the page against §1: grep the page
for the §1 grep tokens whose phase the page's subject covers, and for each token that should be on
the page but is not, and each token that is on the page but is described contrary to the tree, you
have a finding. Then set the verdict per D-06: `current` means every applicable signal class was run
against the page and found to match the live tree; `stale` means the page exists but omits or
misdescribes a shipped item; `missing` is reserved for a capability with no page of its own in
`SUMMARY.md` and no section on any existing page — a capability that has a page whose content is
wrong is `stale`, never `missing`.

Write each row with a findings cell that names, per signal class, the command actually run and its
result, or the words checked, matches. A settled verdict with an empty findings cell is invalid and
`34-check.sh` will reject it. Where the verdict is `stale` or `missing`, the Cites cell must carry
the phase and the shipped item in the form `Phase 27 — POST /v1/runs (PLAT-02)`, copied from the
matching §1 row, and the row gets one or more `MB-nn` IDs with a size from the D-04 rubric.
Numbering continues from the highest `MB-nn` already in the file — `MB-01` was assigned by 34-01
and task 1 of this plan added more. Never reuse or renumber an ID.

Three pages need extra handling beyond the generic method.

`api-reference/upgrading.md` and `api-reference/migration-guide.md` (D-09): check them row-for-row
against `MIGRATION.md` §9.1 and §9.8. Build the list of §9.1 behavioral-change entries and the
§9.8 operator-checklist entries, and for each one record whether the page carries it, contradicts
it or omits it. Each disagreement is its own `MB-nn` row, not one lumped row for the page — Phase
35 SC1 requires these two pages to agree with `MIGRATION.md`, so the work list must be item-level.
Record the entry counts on both sides so the coverage is visible.

`architecture/commissary.md`: task 1 already minted a row for its line 7 vocabulary hit. Do not
mint a second row for the same line; settle the page's own currency verdict on its content — in
particular whether it describes the Phase 30 `Commissary` responsibilities and the Phase 32
`resolve_context_window` / `WindowSource` resolution path as the tree implements them — and
cross-reference the existing row rather than duplicating it.

`architecture/crate-map.md` and `api-reference/crate-map.md`: both must be checked against
`ls crates/` and against the Phase 33 D-03 crate-graph edge (`paladin-memory` now depends on
`paladin-llm`). A crate-graph diagram that predates that edge is `stale` with the edge named as
the missing item.

Append to `34-EVIDENCE.md` one numbered row per page recording the `34-signals.sh` invocation and
its headline result, plus the D-09 row-for-row comparison commands and the SC5 `git status`. Keep
the per-page evidence rows terse — the detail lives in the audit's findings cells.
  </action>
  <verify>
    <automated>cd /workspace && D=.planning/phases/34-documentation-currency-audit && for p in $(printf '%s\n' docs/src/SUMMARY.md docs/src/introduction.md docs/src/getting-started/*.md docs/src/architecture/*.md docs/src/api-reference/*.md); do grep "| $p " $D/34-AUDIT.md | grep -q 'not yet swept' && echo "UNSETTLED $p"; done | grep -q . && exit 1; bash $D/34-check.sh --seed && test -z "$(git status --porcelain -- . ':!.planning')" && echo GROUP-A-OK</automated>
  </verify>
  <acceptance_criteria>
    - All eighteen pages listed in `<read_first>` have a §2 row whose Verdict cell is one of `current`, `stale` or `missing` — none still carries the seeded placeholder. Prove with a loop over the eighteen paths grepping for the placeholder text and requiring no output.
    - Every settled row's findings cell names at least three signal classes with their producing command and result, or the words checked, matches per class — no findings cell is empty.
    - Every `stale` or `missing` row carries a Cites cell of the form `Phase N — <item> (<REQ-ID>)` matching a §1 `SS-nn` row, and at least one `MB-nn` ID with a size of `S`, `M` or `L`.
    - `34-AUDIT.md` contains a D-09 subsection listing the `MIGRATION.md` §9.1 and §9.8 entry counts and one line per entry recording carried, contradicted or omitted for both `upgrading.md` and `migration-guide.md`; each disagreement has its own `MB-nn` ID.
    - Both `crate-map.md` pages' rows name `ls crates/` as a producing command and state explicitly whether the Phase 33 `paladin-memory` → `paladin-llm` edge is present.
    - `grep -oE 'MB-[0-9]+' 34-AUDIT.md | sort | uniq -d` prints nothing (no duplicate IDs).
    - `bash 34-check.sh --seed` exits 0; `git status --porcelain -- . ':!.planning'` prints nothing.
  </acceptance_criteria>
  <done>Eighteen pages carry settled, command-backed verdicts; the two release-facing pages are reconciled entry-by-entry against MIGRATION.md; every finding has a stable MB ID and a size.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository working tree → git commit | `mdbook-mermaid install docs/` writes generated assets into `docs/`; a commit carrying them falsifies SC5 |
| build tool output → `34-evidence/` | The mdBook and script captures are committed verbatim |
| page reading → recorded verdict | A verdict inferred from a page's recency rather than its content is indistinguishable on disk from a measured one |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-01 | Tampering | `docs/mermaid.min.js`, `docs/mermaid-init.js`, phase commits | high | mitigate | `git status --porcelain -- docs` is run immediately after the mermaid install and again at task end; any drift is restored with `git checkout -- docs/` and recorded as a Phase 35 finding; the task verify requires both that status and `git status --porcelain -- . ':!.planning'` to be empty (D-22) |
| T-34-02 | Information Disclosure | `34-evidence/34-03-mdbook-build.txt` | high | mitigate | Only `mdbook`, `mdbook-linkcheck` and the two repo scripts are teed; none reads an environment secret. Before commit the capture is grepped for key-shaped tokens and any hit is redacted before surrounding text is truncated, per the house rule in `crates/paladin-llm/src/redaction.rs` |
| T-34-03 | Repudiation | §2 verdict rows | medium | mitigate | `34-check.sh` assertion (c) fails a settled row whose findings cell is empty or still the placeholder; acceptance criteria require at least three named signal-class commands per settled row (D-00b, D-07) |
| T-34-04 | Tampering (supply chain) | package installs | low | accept | Nothing is installed; the `mdbook` family is already present and its versions are asserted against the `docs.yml` pins by the task precondition, which halts rather than installing on a mismatch |
| T-34-05 | Denial of Service | devcontainer resources | low | accept | `check-doc-examples.sh` runs `cargo check` on one small crate (~39s measured); bounded and gitignored |
</threat_model>

<verification>
- `34-evidence/34-03-mdbook-build.txt` holds the verbatim build, linkcheck and gate-script output.
- `34-AUDIT.md` §2 carries the build-baseline subsection, the orphan subsection, the vocabulary subsection and 18 settled rows.
- No page among the eighteen still carries the seeded placeholder.
- `bash 34-check.sh --seed` exits 0; `git status --porcelain -- docs` and `git status --porcelain -- . ':!.planning'` are both empty.
</verification>

<success_criteria>
The mdBook toolchain result is a measurement, not an assumption, and the first eighteen pages —
including the two the Phase 35 release contract names — carry verdicts a reader can reproduce from
the commands written in their own findings cells.
</success_criteria>

<output>
Create `.planning/phases/34-documentation-currency-audit/34-03-SUMMARY.md` when done.
</output>
