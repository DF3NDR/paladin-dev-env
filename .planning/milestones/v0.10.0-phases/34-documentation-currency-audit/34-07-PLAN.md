---
phase: 34-documentation-currency-audit
plan: 07
type: execute
wave: 7
depends_on: ["34-06"]
files_modified:
  - .planning/phases/34-documentation-currency-audit/34-AUDIT.md
  - .planning/phases/34-documentation-currency-audit/34-EVIDENCE.md
  - .planning/phases/34-documentation-currency-audit/34-evidence/
  - .planning/phases/34-documentation-currency-audit/34-rustdoc-rows.sh
autonomous: true
requirements: [CURR-02, CURR-05]
must_haves:
  truths:
    - "Every one of the eleven library crates plus the facade is documented under RUSTDOCFLAGS=-D warnings with all features, and every error from every crate is enumerated as a row (D-14)."
    - "The known HeuristicTokenCounter link appears in the per-crate enumeration, proving the method (D-14, CONTEXT.md Specific Ideas)."
    - "The per-crate total is the floor Phase 36 sizes against, measured at this HEAD rather than carried from an earlier phase's figure (D-14, D-23, RESEARCH P-03)."
    - "Doctests are measured as their own baseline under default features, because the coverage and --tests gates skip them (D-15)."
    - "The public-API example-heading gate is run and recorded, and its scope drift is routed to the deferred register rather than fixed or absorbed (D-00e, D-15, RESEARCH P-06)."
  artifacts:
    - ".planning/phases/34-documentation-currency-audit/34-AUDIT.md (§3 complete — per-crate enumeration, doctest baseline, entry-point gate record)"
    - ".planning/phases/34-documentation-currency-audit/34-evidence/34-07-percrate/"
    - ".planning/phases/34-documentation-currency-audit/34-evidence/34-07-doctests.txt"
    - ".planning/phases/34-documentation-currency-audit/34-evidence/34-07-public-api-examples.txt"
  key_links:
    - "per-crate captures ↔ §3 rows, reconciled per crate by count."
    - "34-rustdoc-rows.sh (written by 34-06) ↔ the per-crate captures it parses."
    - "check-public-api-examples.sh output ↔ the deferred-register pointer plan 34-09 picks up."
  prohibitions:
    - statement: "No file outside .planning/ is created, modified or deleted (ROADMAP SC5, D-22)."
      status: required
      verification: "git status --porcelain -- . ':!.planning' prints nothing; target/ is a gitignored build product."
    - statement: "None of the entry-point gate's violations is fixed, and the # Examples rule is not extended beyond the set the project already enforces (D-00c, D-00e)."
      status: required
      verification: "git status --porcelain -- crates src scripts prints nothing; the violations appear only as recorded findings."
---

<objective>
Complete `§3`: run the per-crate `-D warnings --all-features` sweep that is the only accurate
enumeration of the all-features floor, measure the doctest baseline, and record the public-API
example-heading gate.

Purpose: the workspace invocation 34-06 recorded is a partial view by construction. Phase 36 must
size against the real floor, and RESEARCH.md measured that floor to be several times the figure
the roadmap, this phase's own context and the state file all still carry. Getting this number
right is the single highest-value measurement in the phase.
Output: `34-AUDIT.md` §3 per-crate enumeration with `RD-nn` rows for every crate, the doctest
baseline subsection, the entry-point gate record, and the per-crate raw captures.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-documentation-currency-audit/34-CONTEXT.md
@.planning/phases/34-documentation-currency-audit/34-RESEARCH.md
@.planning/phases/34-documentation-currency-audit/34-06-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Phase 34 writes only inside `.planning/` — full list in `34-01-PLAN.md`. This plan adds the
`34-evidence/34-07-percrate/` capture directory and continues the `RD-nn` numbering from the
highest already present in `34-AUDIT.md`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Per-crate all-features sweep across all twelve crates, every error enumerated</name>
  <files>.planning/phases/34-documentation-currency-audit/34-AUDIT.md, .planning/phases/34-documentation-currency-audit/34-evidence/34-07-percrate/, .planning/phases/34-documentation-currency-audit/34-EVIDENCE.md</files>
  <precondition>`34-rustdoc-rows.sh` exists from plan 34-06 and accepts an all-features capture with an error-shaped diagnostic label. Assert with a dry run against 34-06's default capture before the sweep; if the parser cannot handle an `error:`-prefixed block, extend it in this task rather than hand-transcribing rows.</precondition>
  <read_first>
    - `.planning/phases/34-documentation-currency-audit/34-rustdoc-rows.sh` — the parser written by 34-06, including its location-recovery branch and its reconciliation line.
    - `.planning/phases/34-documentation-currency-audit/34-AUDIT.md` §3 — the column header, the default-run enumeration, the workspace-run subsection, and the highest `RD-nn` assigned so far.
    - `.planning/phases/34-documentation-currency-audit/34-RESEARCH.md` — Pattern 2 (the twelve-crate invocation list and the measured per-crate result table) and Pitfall P-03 (the count has grown roughly fivefold against the carried figure). Treat that table as a prior expectation to compare against, never as the source of rows.
    - `crates/paladin-memory/src/token_counter/mod.rs` lines 1-10 — the known-answer case that must appear in this enumeration or the method is wrong.
    - `ls crates/` and the root `Cargo.toml` workspace members — the live crate list, so the twelve invocations are derived from the tree rather than copied.
  </read_first>
  <action>
Derive the crate list from the tree (`ls crates/` plus the facade package name from the root
`Cargo.toml`), and confirm it matches the twelve names RESEARCH.md swept: `paladin-battalion`,
`paladin-content`, `paladin-ai-core`, `paladin-eval`, `paladin-herald`, `paladin-llm`,
`paladin-memory`, `paladin-notifications`, `paladin-ports`, `paladin-storage`, `paladin-web` and
the facade `paladin-ai`. If the tree has more or fewer, sweep what the tree has and record the
difference — the list is derived, not copied.

For each crate run `RUSTDOCFLAGS="-D warnings" cargo doc -p <crate> --all-features --no-deps`,
teeing each to its own capture at
`.planning/phases/34-documentation-currency-audit/34-evidence/34-07-percrate/<crate>.txt`. Record
per crate the exit code, the content-error count and the wall time. Expect roughly five minutes
total across all twelve.

Parse each capture with `34-rustdoc-rows.sh`, passing the run label `all-features -p <crate>`, and
append the emitted rows to `34-AUDIT.md` §3 under a per-crate subheading. Every row keeps the same
schema as the default-run rows: `RD-nn` continuing the numbering, the run label, the crate, a real
`file:line`, the kind, the verbatim first line of the diagnostic, the location source, the evidence
anchor naming the per-crate capture, and a D-04 size. The location-recovery branch matters more
here than in the default run — the majority of these are unresolved links inside module-level doc
comments — so a row that ends without a real `file:line` is a parser failure to fix, not a row to
accept.

Verify the enumeration against its known answer: the `HeuristicTokenCounter` intra-doc link in
`paladin-memory` must appear as a row pointing at `crates/paladin-memory/src/token_counter/mod.rs`
line 3. If it does not, the sweep or the parser is wrong — stop and fix before continuing, because
CONTEXT.md names this case as the method's self-test.

Then record the floor in a summary subsection: a table of crate, exit code, content-error count and
wall time for all twelve, the total, and the count of crates that are red. Compare that total
explicitly against the figure the roadmap, `34-CONTEXT.md` D-14 and the state file all carry for
this bar, and state plainly by how much the work list Phase 36 would have been undersized had the
per-crate sweep not been run. Record the HEAD SHA and the D-23 note. Do not copy RESEARCH.md's
numbers — if this run's totals differ from its table, this run's totals are the record and the
difference is itself worth a sentence.

Append one numbered row per crate invocation to `34-EVIDENCE.md`, plus a total row and the SC5
`git status`.
  </action>
  <verify>
    <automated>cd /workspace && D=.planning/phases/34-documentation-currency-audit && test "$(ls $D/34-evidence/34-07-percrate/*.txt | wc -l)" -ge 12 && grep -q 'crates/paladin-memory/src/token_counter/mod.rs:3' $D/34-AUDIT.md && test "$(grep -c '^| RD-[0-9]' $D/34-AUDIT.md)" -ge 120 && test "$(grep -c '^| RD-[0-9]' $D/34-AUDIT.md)" = "$(grep -cE '^\| RD-[0-9]+ \|[^|]*\|[^|]*\| [A-Za-z0-9_./-]+\.rs:[0-9]+ ' $D/34-AUDIT.md)" && grep -oE 'RD-[0-9]+' $D/34-AUDIT.md | sort | uniq -d | grep -q . && exit 1; bash $D/34-check.sh --seed && test -z "$(git status --porcelain -- . ':!.planning')" && echo PERCRATE-OK</automated>
  </verify>
  <acceptance_criteria>
    - One capture file exists per crate under `34-evidence/34-07-percrate/`, at least twelve in total, each non-empty.
    - `34-AUDIT.md` §3 has a per-crate subheading for every crate swept, each naming the verbatim invocation with the crate substituted.
    - Every `RD-nn` row in the file ends its File:line cell with a real `<path>.rs:<line>` — the count of `RD` rows equals the count of `RD` rows matching a `.rs:<digits>` pattern.
    - A row exists carrying `crates/paladin-memory/src/token_counter/mod.rs:3` with the `HeuristicTokenCounter` link as its message.
    - `grep -oE 'RD-[0-9]+' 34-AUDIT.md | sort | uniq -d` prints nothing.
    - Per crate, the number of §3 rows under that crate's subheading equals the content-error count recorded for that crate in the summary table; the equality is stated per crate.
    - The summary subsection holds a twelve-row table (crate, exit code, error count, wall time), the total, the red-crate count, the measured HEAD SHA, and an explicit comparison against the figure the roadmap and D-14 carry, naming the undersizing in numbers.
    - `git status --porcelain -- . ':!.planning'` prints nothing; `bash 34-check.sh --seed` exits 0.
  </acceptance_criteria>
  <done>All twelve crates are swept individually, every error is a row with a real file:line, the known-answer case is present, and the true all-features floor is recorded with its gap against the previously carried figure stated in numbers.</done>
</task>

<task type="auto">
  <name>Task 2: Doctest baseline and the public-API example-heading gate record</name>
  <files>.planning/phases/34-documentation-currency-audit/34-AUDIT.md, .planning/phases/34-documentation-currency-audit/34-evidence/34-07-doctests.txt, .planning/phases/34-documentation-currency-audit/34-evidence/34-07-public-api-examples.txt, .planning/phases/34-documentation-currency-audit/34-EVIDENCE.md</files>
  <read_first>
    - `scripts/check-public-api-examples.sh` — its header comment explaining that it re-derives its target set from the live tree on every run, its gate mode and its list mode.
    - `.planning/milestones/v0.8.0-phases/16-documentation-currency-the-architecture-gap/16-DOCS-03-ENTRY-POINTS.md` — the frozen entry-point enumeration the script's set has drifted from. Read its count and its scoping statement, not the whole list.
    - `.planning/phases/34-documentation-currency-audit/34-CONTEXT.md` D-00e (the rule applies only to the enumerated entry-point set; the audit reports on that set and does not extend the rule) and D-15.
    - `.planning/phases/34-documentation-currency-audit/34-RESEARCH.md` — Pitfall P-06 (the script's set has grown, it is currently red, and no CI job or make target runs it) and Pitfall P-08 (the doctest command must stay on default features; adding all features trips an unrelated pre-existing test conflict).
    - `.planning/phases/34-documentation-currency-audit/34-AUDIT.md` §3 as written by task 1.
  </read_first>
  <action>
Run `cargo test --workspace --doc` under the default feature set — do not add `--all-features`,
which would trip a pre-existing, unrelated CLI-feature test conflict that has nothing to do with
doctests (RESEARCH P-08). Tee to
`.planning/phases/34-documentation-currency-audit/34-evidence/34-07-doctests.txt`. Record the
passed, failed and ignored counts and the wall time, and record why this baseline exists at all:
the coverage gate and the `--tests` selector both skip doctests, so a red doctest can sit unnoticed
behind two green gates, and Phase 36 requires them green. Every failing doctest is an `RD-nn` row
with its crate, its item path and the failure's first line. If the run is fully green, say so with
the counts and mint no rows.

Then run `bash scripts/check-public-api-examples.sh` in gate mode and in list mode, teeing both to
`.planning/phases/34-documentation-currency-audit/34-evidence/34-07-public-api-examples.txt`.
Record the derived entry-point count, the exit code, and the full list of violations with their
item names and violation kind. Compare the derived count against the count the frozen Phase 16
entry-point enumeration holds and state the drift in numbers. Also record that no CI job and no
make target invokes this script — prove it with a grep across `.github/workflows/*.yml` and the
`Makefile` and quote the empty result.

Route the disposition explicitly, and do not leave it for the executor of a later plan to infer.
The script's scope drift and its current violations are a finding about the rule's own apparatus,
not about any one page, crate file or example program, and the ID taxonomy has no slot for it.
Write it as a pointer in §3 stating that plan 34-09 files it in the deferred register with the
numbers recorded here. Do not mint `RD-nn` rows for the individual violations, do not fix any of
them, and do not extend the enumerated entry-point set — the rule's scope was never re-litigated
to the larger figure, and extending it silently would be exactly the kind of quiet scope change
this audit exists to surface.

Close §3 with a summary subsection: the default-run content-warning total, the per-crate
all-features total, the doctest result, the entry-point gate result, the total `RD-nn` count, and
the HEAD SHA — every figure counted from the rows and captures, not recalled.

Append the commands to `34-EVIDENCE.md` as numbered rows with carried-baseline verdicts, plus the
SC5 `git status`.
  </action>
  <verify>
    <automated>cd /workspace && D=.planning/phases/34-documentation-currency-audit && test -s $D/34-evidence/34-07-doctests.txt && test -s $D/34-evidence/34-07-public-api-examples.txt && grep -q 'cargo test --workspace --doc' $D/34-AUDIT.md && ! grep -q 'cargo test --workspace --all-features --doc' $D/34-AUDIT.md && grep -q 'check-public-api-examples.sh' $D/34-AUDIT.md && grep -qi 'deferred register' $D/34-AUDIT.md && test -z "$(git status --porcelain -- crates src scripts)" && test -z "$(git status --porcelain -- . ':!.planning')" && bash $D/34-check.sh --seed && echo DOCTEST-GATE-OK</automated>
  </verify>
  <acceptance_criteria>
    - `34-evidence/34-07-doctests.txt` and `34-evidence/34-07-public-api-examples.txt` both exist and are non-empty.
    - `34-AUDIT.md` quotes `cargo test --workspace --doc` verbatim and nowhere quotes an all-features variant of it; `grep -c 'cargo test --workspace --all-features --doc' 34-AUDIT.md` equals 0.
    - The doctest subsection records passed, failed and ignored counts plus wall time, and states why the baseline exists (the coverage gate and the `--tests` selector both skip doctests).
    - Every failing doctest, if any, has an `RD-nn` row with crate, item path and failure first line; if the run is green, the subsection says so with the counts and mints no row.
    - The entry-point subsection records the derived entry-point count, the gate exit code, the full violation list with item name and violation kind, and the numeric drift against the frozen Phase 16 enumeration's count.
    - The subsection quotes the empty result of a grep for the script name across `.github/workflows/*.yml` and the `Makefile`, proving no CI job or make target runs it.
    - The subsection states that plan 34-09 files this in the deferred register, that no `RD-nn` row is minted for an individual violation, and that the enumerated entry-point set is not extended.
    - `git status --porcelain -- crates src scripts` prints nothing — no violation was fixed and no script was edited.
    - §3 ends with a summary subsection whose every figure is counted from the rows and captures: default-run total, per-crate all-features total, doctest result, entry-point gate result, total `RD-nn` count, HEAD SHA.
    - `git status --porcelain -- . ':!.planning'` prints nothing; `bash 34-check.sh --seed` exits 0.
  </acceptance_criteria>
  <done>Doctests are measured on their own under default features, the entry-point gate's real state and scope drift are recorded with numbers, the disposition is routed to the deferred register rather than fixed or absorbed, and §3 closes with counted totals.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository working tree → git commit | `cargo doc` and `cargo test --doc` write into `target/` (gitignored); a commit carrying anything else outside `.planning/` falsifies SC5 |
| compiler and script output → `34-evidence/` | Twelve per-crate captures plus two script captures are committed verbatim |
| a red gate → the temptation to fix it | `check-public-api-examples.sh` is red today; fixing a violation here would silently extend a rule whose scope was never re-litigated |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-01 | Tampering | phase commits, `crates/`, `src/`, `scripts/` | high | mitigate | Task 2's verify requires `git status --porcelain -- crates src scripts` empty in addition to the `':!.planning'` check, so a fixed violation or an edited script fails the task rather than shipping as an unrecorded scope change (D-00c, D-00e, D-22) |
| T-34-02 | Information Disclosure | `34-evidence/34-07-percrate/*.txt`, `34-07-doctests.txt` | high | mitigate | Doctest output can echo example values; no provider key is exported for these runs and no live-vendor doctest is enabled by default. Before commit every capture is grepped for key-shaped tokens (`sk-`, `_API_KEY=`) and any hit is redacted before the surrounding text is truncated, per the house rule in `crates/paladin-llm/src/redaction.rs` |
| T-34-03 | Repudiation | `§3` per-crate rows | medium | mitigate | The per-crate row count must equal the per-crate error count and the equality is stated per crate; the known-answer row is a required acceptance criterion, so a sweep that silently missed a crate or a diagnostic shape fails the gate (D-14) |
| T-34-04 | Tampering (supply chain) | package installs | low | accept | Nothing is installed; `--all-features` enables only features the workspace already declares |
| T-34-05 | Denial of Service | devcontainer disk / wall clock | low | accept | The twelve-crate sweep costs roughly five minutes and the doctest run roughly one, all writing to the gitignored `target/` tree |
</threat_model>

<verification>
- Twelve per-crate captures exist; §3 carries a subheading and rows for each.
- Every `RD-nn` row ends with a real `.rs:<line>`; no `RD` ID is duplicated.
- The known-answer `paladin-memory` row is present.
- The doctest and entry-point subsections carry counted figures and the deferred-register routing.
- `git status --porcelain -- . ':!.planning'` and `git status --porcelain -- crates src scripts` are both empty.
</verification>

<success_criteria>
ROADMAP Success Criterion 2 is satisfiable in full from §3: both bars are quoted verbatim, every
warning and every error is enumerated with crate, file and line, and the all-features floor Phase 36
sizes against is this phase's own measurement rather than a figure carried from a phase that never
re-ran the sweep.
</success_criteria>

<output>
Create `.planning/phases/34-documentation-currency-audit/34-07-SUMMARY.md` when done.
</output>
