---
phase: 35-mdbook-currency
plan: 03
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - docs/src/getting-started/installation.md
  - docs/src/getting-started/quickstart.md
  - docs/src/user-guides/agent-orchestrator-bridge.md
  - docs/src/user-guides/content-processing.md
  - docs/src/user-guides/maneuver-flow-dsl.md
  - docs/src/user-guides/orchestration.md
  - docs/src/user-guides/control-flow.md
  - docs/src/user-guides/fault-tolerance.md
  - docs/src/user-guides/tool-integration.md
autonomous: true
requirements: [CURR-06, CURR-07, CURR-08]
must_haves:
  truths:
    - "D-20: every dependency pin in a snippet on these nine pages reads the literal 0.10.0, and no 0.5.x-0.9.x pin survives — `grep -rnE '\"0\\.[5-9]\\.[0-9]+\"'` over the nine files returns nothing"
    - "D-20: every MSRV statement on `docs/src/getting-started/installation.md` reads 1.88, matching `Cargo.toml` `rust-version`"
    - "D-20: every 'Every code example targets the current vX workspace' sentence on these pages reads v0.10.0"
    - "D-22: the Feature Flag Profiles table on `docs/src/getting-started/installation.md` is regenerated from the facade `Cargo.toml` `[features]` block and names every shipped flag the audit found undocumented, including `otel`, `dev-ui`, `redis-cache` and `storage-postgres`"
    - "D-10: `docs/src/user-guides/control-flow.md` links `user-guides/superstep-engine.md` and no longer defers the engine guide to a phase planning document"
    - "MB-22: `docs/src/user-guides/control-flow.md` describes Parley as shipped and names the `APP_ENGINE_MAX_MUSTER_TASKS` environment override beside `max_muster_tasks`"
    - "MB-23: `docs/src/user-guides/fault-tolerance.md` states the graph fingerprint version as v6, matching `GRAPH_FINGERPRINT_VERSION` in `crates/paladin-core/src/platform/container/waypoint.rs`"
    - "MB-29: `docs/src/user-guides/tool-integration.md` names the shipped prompt-level tool-call middleware pair and the `reasoning_agent` preset and links `agent-runtime.md`"
    - "D-00e: `mdbook build docs/` exits 0 with no broken links after all nine pages are edited"
    - statement: "D-11b: every Rust fence these nine pages keep is corrected against the live tree and marked illustrative, and each page carries the illustrative-fragment header note"
      verification: backstop
  artifacts:
    - docs/src/getting-started/installation.md
    - docs/src/user-guides/control-flow.md
  key_links:
    - "`docs/src/user-guides/control-flow.md` -> `user-guides/superstep-engine.md` (the D-10 forward link that replaces the deferred-documentation sentence; the target lands in plan 35-01)"
    - "`docs/src/user-guides/tool-integration.md` -> `user-guides/agent-runtime.md` (the shipped tool-call-protocol middleware the reachability note omits)"
    - "facade `Cargo.toml` `[features]` -> the installation-page Feature Flag Profiles table (D-22 regeneration source)"
  prohibitions:
    - statement: "D-01: no page under docs/src is deleted and no entry is removed from docs/src/SUMMARY.md"
      status: recalled
      verification: flagged-unverified
    - statement: "D-26: no existing crates/doc-examples module, support.rs, examples/ program or examples/README.md is edited"
      status: recalled
      verification: flagged-unverified
    - statement: "D-10: no shipped docs/src page cites a phase planning document as a reader-facing source"
      status: recalled
      verification: flagged-unverified
    - statement: "D-16: no new dated correction callout is added to any page"
      status: recalled
      verification: flagged-unverified
---

<objective>
Close the nine Getting Started and User Guides rows whose fix is prose and pin correction rather
than a new compile-verified module — MB-06, MB-07, MB-18, MB-21, MB-25, MB-26, MB-22, MB-23 and
MB-29.

Seven of the nine are the recurring D-20 sweep (version pins, MSRV, the "targets the current vX
workspace" sentence) plus D-22's feature-flag regeneration on the installation page. The remaining
three are content corrections the audit pinned to a specific line: MB-22 carries the D-10 forward
link to the engine guide plan 35-01 landed, MB-23 is a one-token fingerprint-version fix, and MB-29
adds the shipped tool-call middleware the page's reachability note omits.

D-11(b) governs the fences: these pages keep illustrative fragments, corrected against the tree and
marked, with the `orchestration.md` header note added where absent. D-16 forbids new dated
callouts. D-23 requires the closure table in the SUMMARY; D-24 requires one commit per page.

Purpose: make SC3's "no touched page names a type, function, config key, route or CLI flag the
v0.10.0 tree does not export" true across the guide chapter's long tail.
Output: nine corrected pages, one commit each.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-mdbook-currency/35-CONTEXT.md
@.planning/phases/35-mdbook-currency/35-RESEARCH.md
@.planning/phases/35-mdbook-currency/35-PATTERNS.md
@.planning/phases/35-mdbook-currency/35-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
New symbols created by Phase 35 — exclude from drift verification against the pre-phase tree. This
plan creates no new file; it links to `docs/src/user-guides/superstep-engine.md`, a page created by
plan 35-01 in wave 1.

Created by sibling plans and referenced here: `docs/src/user-guides/superstep-engine.md` and
`crates/doc-examples/src/superstep_engine.rs` (plan 35-01); `crates/doc-examples/src/`
`paladin_agents.rs`, `arsenal_tools.rs`, `herald_output.rs`, `battalion_patterns.rs`,
`sanctum_vector_memory.rs` plus their `pub mod` lines (plan 35-02);
`docs/src/contributing/adr-index.md` and the retitled Contributing nav entry (plan 35-05);
`CHANGELOG.md` `### Documentation` under `## [0.10.0]` (plan 35-10);
`.planning/phases/35-mdbook-currency/{deferred-items.md,35-EVIDENCE.md}`; requirement IDs
`CURR-06`…`CURR-10`.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: MB-06 — installation.md, the full D-20 and D-22 sweep on one page end to end</name>
  <files>docs/src/getting-started/installation.md</files>
  <read_first>
    - `docs/src/getting-started/installation.md` (whole page — notably line 12's toolchain table row, lines 17 and 19's MSRV prose, lines 48-78's dependency pins, lines 84-90's Feature Flag Profiles table)
    - `.planning/phases/34-documentation-currency-audit/34-AUDIT.md` §2 row 66 (MB-06's finding) and §5 row 4
    - `Cargo.toml` — `[workspace.package] version`, `rust-version`, and the whole `[features]` block (the D-22 regeneration source)
    - `rust-toolchain.toml` (the pinned toolchain)
    - `.planning/phases/35-mdbook-currency/35-RESEARCH.md` §"Facade and per-crate `[features]` tables" (the live flag inventory captured at research time — cross-check against `Cargo.toml`, do not substitute for reading it)
    - `docs/src/user-guides/orchestration.md` lines 14-17 (the illustrative-fragment header note wording)
  </read_first>
  <action>
    Correct every MSRV statement on the page to 1.88 — the toolchain-table row, the "Why Rust >= X?"
    heading and the "should print >= X" verification line — matching `Cargo.toml` `rust-version` and
    `rust-toolchain.toml`. Change every dependency pin in the installation snippets from the
    0.5.0-era string to the literal `0.10.0` per D-20; the facade crate is named `paladin-ai` and the
    core crate package name is `paladin-ai-core`, both already spelled correctly on the page.

    Regenerate the Feature Flag Profiles table from the facade `Cargo.toml` `[features]` block per
    D-22 so every shipped flag appears exactly once with the crate it belongs to and what it gates:
    the nine `llm-*` provider flags plus the `llm-all` aggregate, `vision`, `content-processing`,
    `web-server`, `notifications`, `storage-mysql`, `storage-postgres`, the `storage` aggregate,
    `redis-queue`, `redis-cache`, `s3-storage`, `openai-embeddings`, `qdrant`, `otel`, `dev-ui`,
    `cli`, `integration-tests`, `live-api-tests` and the `full` aggregate, with the `default` set
    stated as the three default LLM provider flags. Keep the profile framing the page already uses
    (a short recommended profile per use case) and add the full inventory below it rather than
    replacing the page's structure. Note that the `paladin-cli` binary carries
    `required-features = ["cli"]` and is therefore not built by a default `cargo build`.

    Per D-11(b) correct any remaining fence against the tree and mark it illustrative, adding the
    `orchestration.md` header note with the version reading v0.10.0 if the page lacks one. Per D-16
    add no dated callout. Run `mdbook build docs/` and `./scripts/check-doc-config.sh` (the page
    carries YAML fences), then commit with
    `git add -- docs/src/getting-started/installation.md && git commit -q -m "docs(35): bring installation to the v0.10.0 pins, MSRV 1.88 and the full feature-flag inventory (MB-06)" -- docs/src/getting-started/installation.md`
    (D-24).
  </action>
  <verify>
    <automated>cd /workspace &amp;&amp; ! grep -qE '"0\.[5-9]\.[0-9]+"' docs/src/getting-started/installation.md &amp;&amp; ! grep -qE '\b1\.(70|75|85)(\.[0-9]+)?\b' docs/src/getting-started/installation.md &amp;&amp; for f in otel dev-ui redis-cache storage-postgres llm-gemini llm-ollama; do grep -q "$f" docs/src/getting-started/installation.md || exit 1; done &amp;&amp; mdbook build docs/ &amp;&amp; ./scripts/check-doc-config.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '"0\.[5-9]\.[0-9]+"' docs/src/getting-started/installation.md` returns `0`
    - `grep -cE '\b1\.(70|75|85)(\.[0-9]+)?\b' docs/src/getting-started/installation.md` returns `0` and the page contains the literal `1.88`
    - The page names each of `otel`, `dev-ui`, `redis-cache`, `storage-postgres`, `llm-kimi`, `llm-qwen`, `llm-grok`, `llm-ollama`, `llm-gemini`, `llm-openai-compatible`, `vision`, `content-processing`, `web-server`, `notifications`, `qdrant`, `cli`
    - The page states that the `paladin-cli` binary requires the `cli` feature
    - `mdbook build docs/` exits 0 with `No broken links found`; `./scripts/check-doc-config.sh` exits 0
    - `git log --oneline -1 -- docs/src/getting-started/installation.md` subject matches `docs\(35\): .*\(MB-06\)`
  </acceptance_criteria>
  <done>MB-06 is closed and the D-20 + D-22 correction shape is proven on the page that carries the most of both.</done>
</task>

<task type="auto">
  <name>Task 2: MB-07, MB-18, MB-21, MB-25, MB-26 — the version-string sweep</name>
  <files>docs/src/getting-started/quickstart.md, docs/src/user-guides/agent-orchestrator-bridge.md, docs/src/user-guides/content-processing.md, docs/src/user-guides/maneuver-flow-dsl.md, docs/src/user-guides/orchestration.md</files>
  <read_first>
    - `docs/src/getting-started/quickstart.md` lines 20-70 (the dependency block and the execution-service sample the audit confirmed correct)
    - `docs/src/user-guides/agent-orchestrator-bridge.md` line 14
    - `docs/src/user-guides/content-processing.md` lines 9-15 and line 162
    - `docs/src/user-guides/maneuver-flow-dsl.md` line 55 and line 1154
    - `docs/src/user-guides/orchestration.md` lines 14-17
    - `.planning/phases/34-documentation-currency-audit/34-AUDIT.md` §2 rows 67, 74, 78, 85, 87
    - `Cargo.toml` `[workspace.package] version`
  </read_first>
  <action>
    One commit per page, five commits, each correcting only what its §2 row cites plus any other
    stale pin the D-20 exit grep would catch on that same page.

    `quickstart.md` (MB-07): change the three dependency pins in the getting-started block from the
    0.7.0-era string to the literal `0.10.0`; leave the execution-service constructor call alone —
    the audit confirmed the four-argument form matches the live signature. Subject
    `docs(35): pin the quickstart dependencies at 0.10.0 (MB-07)`.

    `agent-orchestrator-bridge.md` (MB-18): change the "Every example targets the current vX
    workspace" sentence to v0.10.0. Subject
    `docs(35): retarget the bridge guide at the v0.10.0 workspace (MB-18)`.

    `content-processing.md` (MB-21): change both the header sentence and the later in-page version
    reference to v0.10.0; leave the declared-but-no-adapter feature claims and the commented-out
    filtering-service claim alone — the audit re-verified both against the live crate. Subject
    `docs(35): retarget the content-processing guide at the v0.10.0 workspace (MB-21)`.

    `maneuver-flow-dsl.md` (MB-25): change the crate pin in the dependency block and the page's
    closing Version line to `0.10.0`; leave the subcommand documentation alone — the audit confirmed
    it matches the live command enum including the format argument. Subject
    `docs(35): pin the Maneuver DSL guide at 0.10.0 (MB-25)`.

    `orchestration.md` (MB-26): change the header sentence to v0.10.0; leave the builder samples and
    the seven `{{#include}}` anchors alone — D-26 forbids editing the module behind them. Subject
    `docs(35): retarget the orchestration guide at the v0.10.0 workspace (MB-26)`.

    Per D-11(b) mark any fence that is not `{{#include}}`-backed as illustrative and add the header
    note to any of these pages that lacks one. Per D-16 add no dated callout. Run
    `mdbook build docs/` after the five commits.
  </action>
  <verify>
    <automated>cd /workspace &amp;&amp; ! grep -rqE '"0\.[5-9]\.[0-9]+"' docs/src/getting-started/quickstart.md docs/src/user-guides/agent-orchestrator-bridge.md docs/src/user-guides/content-processing.md docs/src/user-guides/maneuver-flow-dsl.md docs/src/user-guides/orchestration.md &amp;&amp; ! grep -rqE 'current \*\*v0\.[5-9]' docs/src/user-guides/agent-orchestrator-bridge.md docs/src/user-guides/content-processing.md docs/src/user-guides/orchestration.md &amp;&amp; mdbook build docs/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rcE '"0\.[5-9]\.[0-9]+"' docs/src/getting-started/quickstart.md docs/src/user-guides/maneuver-flow-dsl.md` returns `0` for both files
    - `grep -rcE 'current \*\*v0\.[5-9]' docs/src/user-guides/agent-orchestrator-bridge.md docs/src/user-guides/content-processing.md docs/src/user-guides/orchestration.md` returns `0` for all three
    - Each of the five pages contains the literal `0.10.0` or `v0.10.0` where its stale version string stood
    - `git log --oneline --grep 'MB-07'`, `--grep 'MB-18'`, `--grep 'MB-21'`, `--grep 'MB-25'` and `--grep 'MB-26'` each return at least one commit, and the five subjects differ (one commit per page, D-24)
    - `mdbook build docs/` exits 0 with `No broken links found`
  </acceptance_criteria>
  <done>The five version-string rows are closed, one commit per page, with no unrelated content touched.</done>
</task>

<task type="auto">
  <name>Task 3: MB-22, MB-23, MB-29 — the three line-pinned content corrections</name>
  <files>docs/src/user-guides/control-flow.md, docs/src/user-guides/fault-tolerance.md, docs/src/user-guides/tool-integration.md</files>
  <read_first>
    - `docs/src/user-guides/control-flow.md` lines 25-35 (the deferred-engine-guide sentence) and lines 70-80 (the Parley prose)
    - `docs/src/user-guides/fault-tolerance.md` around line 172 (the fingerprint-version claim)
    - `docs/src/user-guides/tool-integration.md` lines 33-40 (the Reachability note)
    - `.planning/phases/34-documentation-currency-audit/34-AUDIT.md` §2 rows 79, 81 and 93
    - `crates/paladin-core/src/platform/container/directive.rs` lines 40-88 (the live `NextStep` enum and the Parley variant's own rustdoc)
    - `crates/paladin-battalion/src/engine/mod.rs` lines 655-675 (the superseded-error doc comment stating the variant is no longer reachable from any production path)
    - `crates/paladin-core/src/platform/container/waypoint.rs` around line 389 (`GRAPH_FINGERPRINT_VERSION`)
    - `docs/src/user-guides/agent-runtime.md` §"Tool-Call Protocol" (the shipped middleware pair and the `reasoning_agent` preset)
    - `docs/src/user-guides/superstep-engine.md` (the link target plan 35-01 landed)
    - `docs/src/user-guides/parley-and-chronicle.md` (the sibling page that already documents the real Parley/Gate mechanism — the target state to agree with)
  </read_first>
  <action>
    Three commits, one per page.

    `control-flow.md` (MB-22): replace the sentence at lines 29-30 that defers deeper engine
    coverage to future documentation and points at a phase-context file with a sentence pointing at
    the new engine guide, linked as `[WarEngine: Battlefield State & Superstep Execution](superstep-engine.md)`;
    D-10 requires the planning-corpus reference to go with it. Rewrite the Parley prose at lines
    72-74: the variant is fully implemented as of Phase 24 and the run no longer fails with the
    superseded not-supported error, so drop the "declared but not implemented this phase" framing
    and the error-variant claim, and point the reader at `parley-and-chronicle.md` for the shipped
    pause/resume mechanism. Add the `APP_ENGINE_MAX_MUSTER_TASKS` environment override beside the
    existing `max_muster_tasks` mention. Leave the `NextStep` code sample alone — the audit
    confirmed the variant order and fields match the live enum. Subject
    `docs(35): point control flow at the engine guide and correct the Parley status (MB-22)`.

    `fault-tolerance.md` (MB-23): change the fingerprint-version claim near line 172 from v5 to v6,
    matching `GRAPH_FINGERPRINT_VERSION`, and note that the Phase 26 output-schema section is what
    bumped it, linking the new engine guide's fingerprint section. Leave the eleven
    `{{#include}}` anchors untouched — D-26 forbids editing the module behind them. Subject
    `docs(35): correct the graph fingerprint version to v6 (MB-23)`.

    `tool-integration.md` (MB-29): extend the Reachability note so it keeps the still-true narrower
    fact (no shipped LLM adapter populates the response function-call field) and adds the shipped
    opt-in mechanism it omits — the prompt-level tool-call protocol middleware pair
    `ToolCallProtocolMiddleware` and `FinishOnPlainAnswerMiddleware`, installed by the
    `reasoning_agent` preset, which does let a Paladin's own reasoning loop trigger an Armament
    without a consumer-written port implementation — with a link to `agent-runtime.md`. Leave the
    tool-call struct-literal samples alone; the audit found them already matching the live field
    names. Subject
    `docs(35): record the shipped prompt-level tool-call protocol in the reachability note (MB-29)`.

    Per D-16 add no dated callout to any of the three. Run `mdbook build docs/` and
    `./scripts/check-doc-examples.sh` after the three commits.
  </action>
  <verify>
    <automated>cd /workspace &amp;&amp; grep -q 'superstep-engine.md' docs/src/user-guides/control-flow.md &amp;&amp; ! grep -q '23-CONTEXT.md' docs/src/user-guides/control-flow.md &amp;&amp; ! grep -q 'ParleyNotSupported' docs/src/user-guides/control-flow.md &amp;&amp; grep -q 'APP_ENGINE_MAX_MUSTER_TASKS' docs/src/user-guides/control-flow.md &amp;&amp; grep -q 'v6' docs/src/user-guides/fault-tolerance.md &amp;&amp; grep -q 'ToolCallProtocolMiddleware' docs/src/user-guides/tool-integration.md &amp;&amp; grep -q 'reasoning_agent' docs/src/user-guides/tool-integration.md &amp;&amp; mdbook build docs/ &amp;&amp; ./scripts/check-doc-examples.sh</automated>
  </verify>
  <acceptance_criteria>
    - `docs/src/user-guides/control-flow.md` contains a markdown link whose target is `superstep-engine.md`, and `grep -c '23-CONTEXT.md' docs/src/user-guides/control-flow.md` returns `0`
    - `grep -c 'ParleyNotSupported' docs/src/user-guides/control-flow.md` returns `0` and the page contains a link to `parley-and-chronicle.md`
    - `docs/src/user-guides/control-flow.md` contains the literal `APP_ENGINE_MAX_MUSTER_TASKS`
    - `grep -cE 'fingerprint version `v5`|version `v5`' docs/src/user-guides/fault-tolerance.md` returns `0` and the page states the fingerprint version as `v6`
    - `docs/src/user-guides/tool-integration.md` contains the literals `ToolCallProtocolMiddleware`, `FinishOnPlainAnswerMiddleware` and `reasoning_agent` and a link to `agent-runtime.md`
    - `git log --oneline --grep 'MB-22'`, `--grep 'MB-23'` and `--grep 'MB-29'` each return at least one commit
    - `mdbook build docs/` exits 0 with `No broken links found`; `./scripts/check-doc-examples.sh` exits 0
  </acceptance_criteria>
  <done>The three line-pinned rows are closed, the engine guide has its first inbound link, and no planning-corpus reference survives on a shipped page.</done>
</task>

</tasks>

<!-- planner-discipline-allow: 23-CONTEXT.md -->
<!-- planner-discipline-allow: ParleyNotSupported -->
<!-- planner-discipline-allow: "0\.[5-9]\.[0-9]+" -->

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository -> published book (`docs/book/html/`) | Anything written into `docs/src` is published |
| planning corpus -> shipped page | `.planning/` content quoted into a page becomes public |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-05 | Information Disclosure | `docs/src/user-guides/control-flow.md` and the eight sibling pages | low | mitigate | D-10 removes the last planning-corpus forward reference from a shipped page; the acceptance criterion asserts the phase-context filename is absent, so internal planning material cannot leak through the published book |
| T-35-SC | Tampering | npm/pip/cargo installs | low | accept | Prose-only plan: no package is installed and no Rust file is added or edited, so the supply-chain surface is unchanged |
</threat_model>

<verification>
- `mdbook build docs/` exits 0 with `No broken links found`
- `./scripts/check-doc-examples.sh` and `./scripts/check-doc-config.sh` both exit 0
- `git diff 35-01..HEAD --name-only -- src/ crates/` is empty for this plan's commits (D-00c)
- `grep -rnE '"0\.[5-9]\.[0-9]+"' docs/src/getting-started docs/src/user-guides` returns nothing for the nine pages this plan owns
</verification>

<success_criteria>
- All nine rows closed with one commit per page (D-24)
- The engine guide has its MB-22 inbound link and the planning-corpus reference is gone (D-10)
- The installation page's feature inventory is regenerated from `Cargo.toml` (D-22)
- The full `docs.yml` sequence green on this plan's final commit
</success_criteria>

<output>
Create `.planning/phases/35-mdbook-currency/35-03-SUMMARY.md` when done, with the D-23 closure table
(`MB-nn | page | disposition | commit | how the §5 finding was addressed`) and, if any observation
was made on a page the audit settled `current`, a `## Deferred observations` section for plan 35-10
to fold into `deferred-items.md` (D-27).
</output>
