---
phase: 35-mdbook-currency
plan: 04
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - docs/src/introduction.md
  - docs/src/architecture/domain-model.md
  - docs/src/architecture/commissary.md
  - docs/src/architecture/overview.md
  - docs/src/architecture/hexagonal-design.md
  - docs/src/architecture/design-patterns.md
autonomous: true
requirements: [CURR-06, CURR-07, CURR-08, CURR-09]
must_haves:
  truths:
    - "D-17: `grep -rniE '\\bQuartermaster\\b' docs/src` returns nothing once `docs/src/architecture/commissary.md` line 7 is reworded, with no allowlisted exception"
    - "D-18: the Medieval Military Theme table on `docs/src/introduction.md` is a labelled excerpt of at most eight terms, spelled as `docs/src/architecture/domain-model.md` spells them, with a sentence naming it an excerpt and linking that page's full table"
    - "D-18: the book still carries exactly one complete in-book ubiquitous-language list, on `docs/src/architecture/domain-model.md`"
    - "D-10: `docs/src/introduction.md`, `docs/src/architecture/overview.md` and `docs/src/architecture/domain-model.md` each link `user-guides/superstep-engine.md`"
    - "MB-05: `docs/src/introduction.md`'s nav index links `control-flow.md`, `fault-tolerance.md`, `parley-and-chronicle.md`, `agent-runtime.md`, `eval-harness.md`, `platform-api.md`, `observability.md`, `commissary.md` and the deployment-topologies chapter"
    - "D-19: the Garrison-entry struct on `docs/src/architecture/domain-model.md` matches `crates/paladin-core/src/platform/container/garrison.rs` field for field — `id`, `role`, `content`, `timestamp`, `metadata`, `token_count`, `is_summary` — fenced `rust,ignore` with the source path in a comment"
    - "D-19: the Core Domain Entities section on `docs/src/architecture/domain-model.md` carries short entries for `Battlefield`, `Waypoint`, `Aegis` and `TraceRecord`, each linking its guide"
    - "MB-08: `docs/src/architecture/overview.md` names eleven library crates plus the facade, including `paladin-eval` and `paladin-herald`"
    - "MB-09: `docs/src/architecture/overview.md` names `WarEngine`, `Battlefield`, `Waypoint`, `Aegis`, `Commissary`, `TraceRecord` and the Platform API route surface"
    - "MB-10: the LLM-port trait excerpt on `docs/src/architecture/hexagonal-design.md` shows the single-request generate signature from `crates/paladin-ports/src/output/llm_port.rs`"
    - "MB-12: the execution-service constructor on `docs/src/architecture/design-patterns.md` shows the live fourth parameter, the optional Arsenal port, not a Herald"
    - statement: "D-00d: the vocabulary on these six pages follows the Phase 30 rule — domain roles use the Medieval-military term, units and technical ports keep industry names"
      verification: backstop
  artifacts:
    - docs/src/architecture/domain-model.md
    - docs/src/introduction.md
  key_links:
    - "`docs/src/introduction.md` nav index -> every User Guides, Architecture, Deployment Topologies, Operations and Contributing page it currently omits, including `user-guides/superstep-engine.md`"
    - "`docs/src/architecture/domain-model.md` Core Domain Entities -> `user-guides/superstep-engine.md`, `user-guides/fault-tolerance.md`, `operations/observability.md` (the four newly documented entities)"
    - "`docs/src/architecture/commissary.md` -> ADR-0049 as the historical record of the rename (D-17)"
  prohibitions:
    - statement: "D-01: no page under docs/src is deleted and no entry is removed from docs/src/SUMMARY.md"
      status: recalled
      verification: flagged-unverified
    - statement: "D-18: the introduction's term table is not expanded into a fourth complete ubiquitous-language list"
      status: recalled
      verification: flagged-unverified
    - statement: "D-26: no existing crates/doc-examples module, support.rs, examples/ program or examples/README.md is edited"
      status: recalled
      verification: flagged-unverified
    - statement: "D-16: no new dated correction callout is added to any page"
      status: recalled
      verification: flagged-unverified
---

<objective>
Close the introduction and architecture rows — MB-04, MB-05, MB-03, MB-11, MB-02, MB-08, MB-09,
MB-10 and MB-12 — and with MB-02 close the phase's vocabulary criterion at its single remaining
source.

This plan carries D-17 (the one literal that keeps the SC4 exit grep non-empty, reworded to point at
ADR-0049 instead of naming the pre-rename role), D-18 (the introduction's term table cut to a
labelled excerpt so the three ubiquitous-language lists stay three), D-19 (the Garrison-entry struct
brought to the live shape and four missing domain entities added), and three of D-10's four engine
dependents — MB-05, MB-09 and MB-11. D-00d is the governing vocabulary rule; D-11(b) governs the
fences, which stay illustrative fragments corrected against the tree.

`docs/src/architecture/crate-map.md` is deliberately not in this plan: D-24 treats the two crate-map
pages as a tightly-coupled pair and plan 35-05 owns both (MB-13 and MB-14) in one commit.

Purpose: make SC4 true and give the architecture chapter the Phase 22-33 surface it never gained.
Output: six corrected pages, one commit per page.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-mdbook-currency/35-CONTEXT.md
@.planning/phases/35-mdbook-currency/35-RESEARCH.md
@.planning/phases/35-mdbook-currency/35-PATTERNS.md
@.planning/phases/35-mdbook-currency/35-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
New symbols created by Phase 35 — exclude from drift verification against the pre-phase tree. This
plan creates no new file; it links to `docs/src/user-guides/superstep-engine.md`, created by plan
35-01 in wave 1.

Created by sibling plans and referenced here: `docs/src/user-guides/superstep-engine.md` and
`crates/doc-examples/src/superstep_engine.rs` (plan 35-01); `crates/doc-examples/src/`
`paladin_agents.rs`, `arsenal_tools.rs`, `herald_output.rs`, `battalion_patterns.rs`,
`sanctum_vector_memory.rs` plus their `pub mod` lines (plan 35-02);
`docs/src/contributing/adr-index.md` and the retitled Contributing nav entry (plan 35-05);
`CHANGELOG.md` `### Documentation` under `## [0.10.0]` (plan 35-10);
`.planning/phases/35-mdbook-currency/{deferred-items.md,35-EVIDENCE.md}`; requirement IDs
`CURR-06`…`CURR-10`.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: MB-04 and MB-05 — introduction.md end to end</name>
  <files>docs/src/introduction.md</files>
  <read_first>
    - `docs/src/introduction.md` (whole page — the Medieval Military Theme table at lines 78-91 and every `## ` nav-index section's link list)
    - `.planning/phases/34-documentation-currency-audit/34-AUDIT.md` §2 row 68 (MB-04's and MB-05's findings) and §5 rows 2 and 3
    - `docs/src/architecture/domain-model.md` lines 12-32 (the full in-book term table whose spellings the excerpt must match and which the excerpt links)
    - `docs/src/SUMMARY.md` (the authoritative nav, including the engine-guide entry plan 35-01 inserted — the source for which pages the index omits)
    - `.planning/phases/35-mdbook-currency/35-CONTEXT.md` D-18 (the excerpt rule) and D-10 (the engine link)
  </read_first>
  <action>
    MB-04, per D-18: cut the Medieval Military Theme table to a labelled excerpt of at most eight
    terms, each spelled exactly as `docs/src/architecture/domain-model.md` spells it, and add one
    sentence immediately above it naming it an excerpt and linking `architecture/domain-model.md`'s
    table as the full in-book list. Do not expand it into a fourth complete list — the three
    ubiquitous-language lists stay three, and only the domain-model page is a docs page among them.

    MB-05, per D-10: bring the page's five nav-index sections into agreement with
    `docs/src/SUMMARY.md`. The audit found the index never links `control-flow.md`,
    `fault-tolerance.md`, `parley-and-chronicle.md`, `agent-runtime.md`, `platform-api.md`,
    `observability.md`, `eval-harness.md`, `commissary.md` or any `deployment-topologies/` page; add
    each under its correct section with a one-line description, and add the new engine guide
    `user-guides/superstep-engine.md` beside the other User Guides entries. Leave the
    Architecture-Layers three-layer description alone — the audit confirmed it accurate.

    Per D-11(b) correct any remaining fence against the tree and mark it illustrative. Per D-16 add
    no dated callout. Run `mdbook build docs/`, then commit with
    `git add -- docs/src/introduction.md && git commit -q -m "docs(35): cut the term table to an excerpt and complete the nav index (MB-04, MB-05)" -- docs/src/introduction.md`
    (D-24 allows one commit for a tightly-coupled pair on the same page).
  </action>
  <verify>
    <automated>cd /workspace &amp;&amp; for p in control-flow.md fault-tolerance.md parley-and-chronicle.md agent-runtime.md eval-harness.md superstep-engine.md platform-api.md observability.md commissary.md deployment-topologies; do grep -q "$p" docs/src/introduction.md || exit 1; done &amp;&amp; grep -q 'domain-model.md' docs/src/introduction.md &amp;&amp; mdbook build docs/</automated>
  </verify>
  <acceptance_criteria>
    - `docs/src/introduction.md` links each of `control-flow.md`, `fault-tolerance.md`, `parley-and-chronicle.md`, `agent-runtime.md`, `eval-harness.md`, `superstep-engine.md`, `platform-api.md`, `observability.md`, `commissary.md` and at least one `deployment-topologies/` page
    - The term table on `docs/src/introduction.md` has at most eight data rows, and the sentence above it names it an excerpt and links `architecture/domain-model.md`
    - Every term in the excerpt appears with the same spelling in `docs/src/architecture/domain-model.md`'s table
    - `mdbook build docs/` exits 0 with `No broken links found`
    - `git log --oneline -1 -- docs/src/introduction.md` subject matches `docs\(35\): .*\(MB-04, MB-05\)`
  </acceptance_criteria>
  <done>MB-04 and MB-05 are closed and the edit-build-commit path is proven for the remaining four pages in this plan.</done>
</task>

<task type="auto">
  <name>Task 2: MB-03, MB-11 and MB-02 — domain-model.md and commissary.md</name>
  <files>docs/src/architecture/domain-model.md, docs/src/architecture/commissary.md</files>
  <read_first>
    - `docs/src/architecture/domain-model.md` lines 12-32 (the term table), lines 63-159 (Core Domain Entities) and lines 96-104 (the Garrison-entry snippet)
    - `docs/src/architecture/commissary.md` lines 1-15 (the opening, including line 7)
    - `.planning/phases/34-documentation-currency-audit/34-AUDIT.md` §2 rows 44 and 47, plus the "Vocabulary sweep (D-10, D-00f)" and "Phase 31 D-29 `token_count` hit-list re-check" subsections that mint MB-02 and MB-03
    - `crates/paladin-core/src/platform/container/garrison.rs` lines 57-75 (the live struct and its rustdoc)
    - `crates/paladin-core/src/platform/container/battlefield.rs`, `waypoint.rs`, `aegis.rs` and `trace.rs` (the four entities the section omits)
    - `.planning/decisions/0049-commissary-design-and-rename.md` (the ADR that holds the rename rationale and the rejected-name list)
    - `docs/src/user-guides/superstep-engine.md`, `docs/src/user-guides/fault-tolerance.md`, `docs/src/operations/observability.md` (the guides the four new entity entries link)
  </read_first>
  <action>
    Two commits, one per page.

    `domain-model.md` (MB-03 and MB-11, one commit — same page). Per D-19 replace the Garrison-entry
    snippet at lines 96-104 with the live struct from
    `crates/paladin-core/src/platform/container/garrison.rs`: the identifier field typed `Uuid`, the
    role field typed `ConversationRole` (not the stale message-role type name), the content field,
    the timestamp field typed `DateTime<Utc>`, the metadata map, the token-count field typed
    `Option<u32>` (not `usize`), and the Phase 26 boolean summary marker. Fence it `rust,ignore` with
    the source path in a leading comment. The token-count field here is a Garrison memory-entry
    field, not a bare total beside a split usage type, so the Phase 31 rule does not apply and no
    usage-type rewrite is made. Per D-19 and D-10 add short entries to the Core Domain Entities
    section for `Battlefield`, `Waypoint`, `Aegis` and `TraceRecord` — all four live in the same
    container directory the section already documents — each one or two sentences long and each
    linking its guide: the engine guide for the first two, `user-guides/fault-tolerance.md` for
    Aegis, `operations/observability.md` for the trace record. Leave the term table alone; the audit
    confirmed it current. Subject
    `docs(35): correct the Garrison entry struct and add the missing domain entities (MB-03, MB-11)`.

    `commissary.md` (MB-02). Per D-17 reword the sentence at line 7 so the pre-rename role name is
    no longer written on the page at all, replacing it with a pointer of the shape "the rename
    rationale and the rejected-name list are in ADR-0049". There is no allowlisted exception: SC4
    and the Phase 30 rule are literal, and the ADR remains the historical record of the old name.
    Change nothing else on the page — the audit settled every other signal class current. Subject
    `docs(35): reword the Commissary rename pointer to ADR-0049 (MB-02)`.

    Per D-16 add no dated callout to either page. Run `mdbook build docs/` and the D-17 exit grep
    after both commits.
  </action>
  <verify>
    <automated>cd /workspace &amp;&amp; ! grep -rqiE '\bQuartermaster\b' docs/src &amp;&amp; grep -q 'ADR-0049' docs/src/architecture/commissary.md &amp;&amp; grep -q 'ConversationRole' docs/src/architecture/domain-model.md &amp;&amp; grep -q 'is_summary' docs/src/architecture/domain-model.md &amp;&amp; grep -q 'Option&lt;u32&gt;' docs/src/architecture/domain-model.md &amp;&amp; for e in Battlefield Waypoint Aegis TraceRecord; do grep -q "$e" docs/src/architecture/domain-model.md || exit 1; done &amp;&amp; mdbook build docs/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rniE '\bQuartermaster\b' docs/src` returns no lines, phase-wide
    - `docs/src/architecture/commissary.md` contains the literal `ADR-0049` on the reworded line
    - `docs/src/architecture/domain-model.md`'s Garrison-entry fence names `id`, `role`, `content`, `timestamp`, `metadata`, `token_count` and `is_summary`, types the role field `ConversationRole` and the token-count field `Option<u32>`, and carries the source path `crates/paladin-core/src/platform/container/garrison.rs` in a comment
    - `grep -c 'MessageRole' docs/src/architecture/domain-model.md` returns `0`
    - `docs/src/architecture/domain-model.md` names `Battlefield`, `Waypoint`, `Aegis` and `TraceRecord` in its Core Domain Entities section and links `superstep-engine.md`, `fault-tolerance.md` and `observability.md`
    - `mdbook build docs/` exits 0 with `No broken links found`
    - `git log --oneline --grep 'MB-02'` and `--grep 'MB-03'` each return at least one commit
  </acceptance_criteria>
  <done>MB-02, MB-03 and MB-11 are closed; the SC4 vocabulary grep is empty phase-wide and the domain model describes the entities Phases 22-28 shipped.</done>
</task>

<task type="auto">
  <name>Task 3: MB-08, MB-09, MB-10 and MB-12 — overview, hexagonal design and design patterns</name>
  <files>docs/src/architecture/overview.md, docs/src/architecture/hexagonal-design.md, docs/src/architecture/design-patterns.md</files>
  <read_first>
    - `docs/src/architecture/overview.md` (line 3's crate count, the crate table at lines 15-24, and the Technology Stack table near line 240 whose MSRV line the audit confirmed correct)
    - `docs/src/architecture/hexagonal-design.md` lines 32-47 (the LLM-port trait sample)
    - `docs/src/architecture/design-patterns.md` lines 137-144 (the pattern-5 constructor sample)
    - `.planning/phases/34-documentation-currency-audit/34-AUDIT.md` §2 rows 46, 48 and 49
    - `crates/paladin-ports/src/output/llm_port.rs` lines 1280-1310 (the live generate signature and its doctest)
    - `src/application/services/paladin/paladin_execution_service.rs` lines 355-370 (the live four-argument constructor)
    - `ls crates/` and each crate's `Cargo.toml` `name` field (the eleven library crates plus the facade)
    - `docs/src/user-guides/superstep-engine.md` and `docs/src/api-reference/platform-api.md` (the link targets the overview page gains)
  </read_first>
  <action>
    Three commits, one per page.

    `overview.md` (MB-08 and MB-09, one commit — same page). MB-08: correct the crate count in the
    opening line and the crate table so both name eleven library crates plus the facade, adding
    `paladin-eval` (the Phase 28 evaluation composition crate) and `paladin-herald` with one line
    each on what they hold. MB-09, per D-10: the page is the architecture entry point and names none
    of the Phase 22-33 surface — add short, linked mentions of `WarEngine` and `Battlefield` /
    `Waypoint` (linking `user-guides/superstep-engine.md`), `Aegis` (linking
    `user-guides/fault-tolerance.md`), `Commissary` (linking `architecture/commissary.md`),
    `TraceRecord` (linking `operations/observability.md`) and the Platform API route surface
    (linking `api-reference/platform-api.md`). Leave the Technology Stack MSRV line alone — the
    audit confirmed it already reads 1.88. Subject
    `docs(35): name the eleven crates and the Phase 22-33 surface in the architecture overview (MB-08, MB-09)`.

    `hexagonal-design.md` (MB-10). Correct the LLM-port trait excerpt to the live signature: generate
    takes one request value of the builder request type and returns the response-or-error result;
    the page's two-parameter messages-and-config form does not exist. Fence it `rust,ignore` with the
    source path `crates/paladin-ports/src/output/llm_port.rs` in a comment per D-11(b) — this is a
    trait excerpt, a fragment, so it is corrected in place rather than promoted to a
    `doc-examples` anchor. Leave the other port samples alone; the audit did not re-verify them and
    did not flag them. Subject
    `docs(35): correct the LlmPort trait excerpt to the request-object signature (MB-10)`.

    `design-patterns.md` (MB-12). Correct the pattern-5 constructor sample so the fourth parameter is
    the optional Arsenal port, matching the live four-argument constructor; the page currently shows
    a Herald there. Fence it `rust,ignore` with the source path in a comment per D-11(b): this is a
    constructor line inside a pattern catalogue, not a complete flow, so under D-12's own
    drop-a-candidate rule it does not get a `doc-examples` module. Subject
    `docs(35): correct the execution-service constructor in the design-patterns catalogue (MB-12)`.

    Per D-16 add no dated callout to any of the three. Run `mdbook build docs/` after the three
    commits.
  </action>
  <verify>
    <automated>cd /workspace &amp;&amp; grep -q 'paladin-eval' docs/src/architecture/overview.md &amp;&amp; grep -q 'paladin-herald' docs/src/architecture/overview.md &amp;&amp; grep -q 'superstep-engine.md' docs/src/architecture/overview.md &amp;&amp; grep -q 'platform-api.md' docs/src/architecture/overview.md &amp;&amp; grep -q 'LlmRequest' docs/src/architecture/hexagonal-design.md &amp;&amp; grep -q 'ArsenalPort' docs/src/architecture/design-patterns.md &amp;&amp; mdbook build docs/</automated>
  </verify>
  <acceptance_criteria>
    - `docs/src/architecture/overview.md` names `paladin-eval` and `paladin-herald` in its crate table and its opening line states eleven library crates plus the facade
    - `docs/src/architecture/overview.md` names `WarEngine`, `Battlefield`, `Waypoint`, `Aegis`, `Commissary` and `TraceRecord`, and links `user-guides/superstep-engine.md`, `user-guides/fault-tolerance.md`, `architecture/commissary.md`, `operations/observability.md` and `api-reference/platform-api.md`
    - `grep -c 'nine focused crates\|nine workspace crates\|nine member crates' docs/src/architecture/overview.md` returns `0`
    - `docs/src/architecture/hexagonal-design.md`'s LLM-port fence shows a single request parameter, names `LlmRequest`, is fenced `rust,ignore` and carries the source path in a comment
    - `docs/src/architecture/design-patterns.md`'s pattern-5 fence names `ArsenalPort` as the fourth parameter and no longer names a Herald there
    - `mdbook build docs/` exits 0 with `No broken links found`
    - `git log --oneline --grep 'MB-08'`, `--grep 'MB-10'` and `--grep 'MB-12'` each return at least one commit
  </acceptance_criteria>
  <done>MB-08, MB-09, MB-10 and MB-12 are closed; the architecture chapter names the crates and the subsystems that actually ship.</done>
</task>

</tasks>

<!-- planner-discipline-allow: \bQuartermaster\b -->
<!-- planner-discipline-allow: MessageRole -->

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository -> published book (`docs/book/html/`) | Anything written into `docs/src` is published |
| planning corpus -> shipped page | ADR content referenced from a page becomes reader-facing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-06 | Information Disclosure | `docs/src/architecture/domain-model.md`, `docs/src/architecture/commissary.md` | low | mitigate | The struct excerpts are copied from public `crates/paladin-core` source and carry no secret; the ADR-0049 pointer names the decision record by ID and does not quote internal deliberation |
| T-35-SC | Tampering | npm/pip/cargo installs | low | accept | Prose-only plan: no package is installed and no Rust file is added or edited, so the supply-chain surface is unchanged |
</threat_model>

<verification>
- `grep -rniE '\bQuartermaster\b' docs/src` returns nothing — the SC4 exit grep is empty phase-wide
- `mdbook build docs/` exits 0 with `No broken links found`
- `./scripts/check-doc-examples.sh` and `./scripts/check-doc-config.sh` both exit 0
- `git diff 35-01..HEAD --name-only -- src/ crates/` is empty for this plan's commits (D-00c)
</verification>

<success_criteria>
- All nine rows closed, one commit per page (D-24; the two same-page pairs share a commit)
- The vocabulary exit grep is empty with no allowlisted exception (D-17)
- The introduction carries an excerpt, not a fourth complete term list (D-18)
- Three of D-10's four engine dependents link the new page
- The full `docs.yml` sequence green on this plan's final commit
</success_criteria>

<output>
Create `.planning/phases/35-mdbook-currency/35-04-SUMMARY.md` when done, with the D-23 closure table
(`MB-nn | page | disposition | commit | how the §5 finding was addressed`) and, if any observation
was made on a page the audit settled `current`, a `## Deferred observations` section for plan 35-10
to fold into `deferred-items.md` (D-27).
</output>
