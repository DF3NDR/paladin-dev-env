---
phase: 35-mdbook-currency
plan: 05
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - docs/src/SUMMARY.md
  - docs/src/contributing/adr-index.md
  - docs/src/contributing/architecture-decisions.md
  - docs/src/architecture/crate-map.md
  - docs/src/api-reference/crate-map.md
  - docs/src/api-reference/feature-flags.md
  - docs/src/api-reference/migration-guide.md
  - docs/src/api-reference/stable-api.md
autonomous: true
requirements: [CURR-06, CURR-07, CURR-08]
must_haves:
  truths:
    - "D-05: `docs/src/contributing/adr-index.md` exists, is titled Architecture Decisions, and carries one table row per ADR with number, title, one-line decision and a GitHub blob link"
    - "D-05: the existing `contributing/architecture-decisions.md` keeps its path and its `docs/src/SUMMARY.md` entry is retitled Adapter Development Guide, with the new ADR index entry inserted directly after it"
    - "D-22: the feature tables on `docs/src/api-reference/feature-flags.md`, `docs/src/architecture/crate-map.md` and `docs/src/api-reference/crate-map.md` are regenerated from the facade and per-crate `Cargo.toml` `[features]` blocks and name `otel`, `dev-ui`, `redis-cache` and `storage-postgres`"
    - "D-22: both crate-map pages name eleven library crates plus the facade, including `paladin-eval` and `paladin-herald`"
    - "D-22: the mermaid crate graph on `docs/src/api-reference/crate-map.md` carries the `mem --> llm` edge so the diagram agrees with the prose"
    - "D-22: the catalogue paths on `docs/src/api-reference/stable-api.md` root at the live crate names `paladin_core::` and `paladin_ports::` rather than the pre-workspace facade layout"
    - "D-06: `docs/src/api-reference/migration-guide.md` reads up to the current v0.10.0 release, its Timeline marks v0.10.0 as current, and it keeps its pointer-only design"
    - "D-20: `docs/src/api-reference/feature-flags.md` names the real Dockerfile builder base image `rust:1.93-slim-bookworm` (or points at `Dockerfile`) instead of a 1.75 pin, and every dependency pin on these five pages reads 0.10.0"
    - "D-13: `grep -rn 'paladin::infrastructure::adapters::llm::' docs/src/api-reference/feature-flags.md` returns nothing"
    - "D-00e: `mdbook build docs/` exits 0 with no broken links after the new page and the two nav edits land"
    - statement: "D-00f: `docs/src/api-reference/upgrading.md` is not edited — the audit recorded it already agreeing with MIGRATION.md §9.1 (4/4) and §9.8 (7/7)"
      verification: backstop
  artifacts:
    - docs/src/contributing/adr-index.md
    - docs/src/api-reference/feature-flags.md
  key_links:
    - "`docs/src/SUMMARY.md` -> `contributing/adr-index.md` (the new Contributing entry) and the retitled `contributing/architecture-decisions.md` entry"
    - "`docs/src/contributing/adr-index.md` -> `.planning/decisions/*.md` through GitHub blob URLs (the `migration-guide.md` link form; `follow-web-links = false` keeps linkcheck from fetching them)"
    - "facade and per-crate `Cargo.toml` `[features]` -> the three regenerated feature tables (D-22 source of truth)"
  prohibitions:
    - statement: "D-01: no page under docs/src is deleted and no entry is removed from docs/src/SUMMARY.md — the adapter guide keeps its path so inbound links stay valid"
      status: recalled
      verification: flagged-unverified
    - statement: "D-00f: docs/src/api-reference/upgrading.md is not edited in this phase"
      status: recalled
      verification: flagged-unverified
    - statement: "D-26: no existing crates/doc-examples module, support.rs, examples/ program or examples/README.md is edited"
      status: recalled
      verification: flagged-unverified
    - statement: "D-16: no new dated correction callout is added to any page"
      status: recalled
      verification: flagged-unverified
---

<objective>
Close the API Reference and Contributing rows — MB-35, MB-13, MB-14, MB-15, MB-16 and MB-17.

MB-35 is the phase's one `retitle` disposition under D-01: per D-05 the adapter-development guide
keeps its path and its nav entry is renamed to match its content, and a new short
`docs/src/contributing/adr-index.md` takes the Architecture Decisions title with a table of the
ADRs that change what a crate consumer or operator sees. This plan is the only wave-2 plan that
touches `docs/src/SUMMARY.md`.

MB-13 and MB-14 are the two crate-map pages, treated as one tightly-coupled pair in one commit per
D-24. MB-15 and MB-17 are the two largest D-22 regeneration rows. MB-16 is D-06's self-consistency
fix; D-00f keeps `upgrading.md` out of scope because the audit already recorded it agreeing with
`MIGRATION.md`. D-20 supplies the version, MSRV and Dockerfile-base rules, and D-13's import rule
also closes the relocated-adapter occurrence that sits on the feature-flags page.

Purpose: make the reference chapter describe the crates, flags and paths that actually ship, and
give the book a discoverable ADR index.
Output: one new page, one nav retitle, one nav insertion and five corrected pages.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-mdbook-currency/35-CONTEXT.md
@.planning/phases/35-mdbook-currency/35-RESEARCH.md
@.planning/phases/35-mdbook-currency/35-PATTERNS.md
@.planning/phases/35-mdbook-currency/35-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
New symbols created by Phase 35 — exclude from drift verification against the pre-phase tree. This
plan creates `docs/src/contributing/adr-index.md` and two `docs/src/SUMMARY.md` changes: the
`[Architecture Decisions](contributing/adr-index.md)` entry, and the retitle of the existing
`contributing/architecture-decisions.md` entry to "Adapter Development Guide".

Created by sibling plans and referenced here: `docs/src/user-guides/superstep-engine.md` and
`crates/doc-examples/src/superstep_engine.rs` (plan 35-01); `crates/doc-examples/src/`
`paladin_agents.rs`, `arsenal_tools.rs`, `herald_output.rs`, `battalion_patterns.rs`,
`sanctum_vector_memory.rs` plus their `pub mod` lines (plan 35-02); `CHANGELOG.md`
`### Documentation` under `## [0.10.0]` (plan 35-10);
`.planning/phases/35-mdbook-currency/{deferred-items.md,35-EVIDENCE.md}`; requirement IDs
`CURR-06`…`CURR-10`.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: MB-35 — retitle the adapter guide and add the ADR index page end to end</name>
  <files>docs/src/SUMMARY.md, docs/src/contributing/adr-index.md, docs/src/contributing/architecture-decisions.md</files>
  <read_first>
    - `docs/src/SUMMARY.md` lines 77-84 (the Contributing section, and the engine entry plan 35-01 inserted above it)
    - `docs/src/contributing/architecture-decisions.md` lines 1-40 (its own H1 already reads Adapter Development Guide, and its table of contents)
    - `.planning/phases/34-documentation-currency-audit/34-AUDIT.md` §2 row 50 (MB-35's finding) and §5 row 35
    - `.planning/phases/35-mdbook-currency/35-RESEARCH.md` §"D-05 ADR index table" (the nine confirmed ADR numbers, titles and one-line decisions)
    - `docs/src/api-reference/migration-guide.md` lines 7-14 (the GitHub blob-URL link form to reuse)
    - `ls .planning/decisions/` (confirm each ADR filename before writing its blob URL)
    - `Cargo.toml` line 64 (`repository` — the base of the blob URL)
  </read_first>
  <action>
    Create `docs/src/contributing/adr-index.md` with the H1 `Architecture Decisions`, a two-sentence
    lead saying the project's decision records live under `.planning/decisions/` in the repository
    and that this page indexes the ones that change what a crate consumer or operator sees, and one
    markdown table with the columns ADR, Title, Decision and Record. Populate it with the nine ADRs
    confirmed in `35-RESEARCH.md`: 0033 (the one `cargo doc` bar), 0037 (the agent route surface is
    `/v1`), 0039 (HTTP-served agents carry no Garrison and no Arsenal), 0042 (LLM-native tool calling
    deferred), 0047 (the architecture appendix disposition), 0048 (`paladin-eval` as a published
    composition crate), 0049 (`Commissary` design and rename), 0050 (`Treasurer` reserved for
    cross-run spend governance) and 0051 (token-economy versioning). Each Record cell is a GitHub
    blob URL of the form `https://github.com/DF3NDR/paladin-dev-env/blob/main/.planning/decisions/<file>.md`,
    matching the link form `migration-guide.md` already uses — `follow-web-links = false` in
    `docs/book.toml` means linkcheck does not fetch them. Re-read `ls .planning/decisions/` and use
    the exact filename for each number; do not guess a slug.

    In `docs/src/SUMMARY.md`, retitle the existing Contributing entry for
    `contributing/architecture-decisions.md` to `Adapter Development Guide` — the file keeps its
    path so no inbound link breaks (D-01, D-05) — and insert
    `- [Architecture Decisions](contributing/adr-index.md)` directly after it. On
    `contributing/architecture-decisions.md` itself, change only the one-line lead if it claims to
    cover architecture decisions, adding a sentence pointing at the new index page; its H1 already
    reads Adapter Development Guide and its body is accurate adapter guidance the audit did not
    fault.

    Run `mdbook build docs/` (a bad nav path or blob URL fails the build), then commit all three
    files with
    `git add -- docs/src/SUMMARY.md docs/src/contributing/adr-index.md docs/src/contributing/architecture-decisions.md && git commit -q -m "docs(35): retitle the adapter guide and add an ADR index page (MB-35)" -- docs/src/SUMMARY.md docs/src/contributing/adr-index.md docs/src/contributing/architecture-decisions.md`
    (D-24).
  </action>
  <verify>
    <automated>cd /workspace &amp;&amp; test -f docs/src/contributing/adr-index.md &amp;&amp; grep -q 'contributing/adr-index.md' docs/src/SUMMARY.md &amp;&amp; grep -q 'Adapter Development Guide' docs/src/SUMMARY.md &amp;&amp; grep -c 'blob/main/.planning/decisions/' docs/src/contributing/adr-index.md | grep -qE '^(9|1[0-9])$' &amp;&amp; for n in 0033 0037 0039 0042 0047 0048 0049 0050 0051; do grep -q "$n" docs/src/contributing/adr-index.md || exit 1; done &amp;&amp; mdbook build docs/</automated>
  </verify>
  <acceptance_criteria>
    - `docs/src/contributing/adr-index.md` exists, its first line is `# Architecture Decisions`, and it contains a table with the header cells ADR, Title, Decision, Record
    - The page names each of 0033, 0037, 0039, 0042, 0047, 0048, 0049, 0050, 0051 and carries at least nine `https://github.com/DF3NDR/paladin-dev-env/blob/main/.planning/decisions/` links
    - Every blob URL's filename matches a real entry in `ls .planning/decisions/`
    - `docs/src/SUMMARY.md` contains `- [Adapter Development Guide](contributing/architecture-decisions.md)` and, on the following line, `- [Architecture Decisions](contributing/adr-index.md)`
    - `git diff HEAD~1 -- docs/src/SUMMARY.md` shows no deleted entry other than the retitled line
    - `mdbook build docs/` exits 0 with `No broken links found`
    - `git log --oneline -1 -- docs/src/contributing/adr-index.md` subject matches `docs\(35\): .*\(MB-35\)`
  </acceptance_criteria>
  <done>MB-35 is closed as a retitle-and-add, the nav names what each page contains, and the new-page + nav + linkcheck path is proven for this plan.</done>
</task>

<task type="auto">
  <name>Task 2: MB-13, MB-14 and MB-15 — the crate-map pair and the feature-flags page</name>
  <files>docs/src/architecture/crate-map.md, docs/src/api-reference/crate-map.md, docs/src/api-reference/feature-flags.md</files>
  <read_first>
    - `docs/src/architecture/crate-map.md` (line 3's crate count, line 9's version claim, the mermaid graph at lines 23-62 which already carries the `mem --> llm` edge, the `paladin-llm` feature table at lines 152-162, and the prose at lines 174-176)
    - `docs/src/api-reference/crate-map.md` (line 12's version claim, line 17's crate count, the mermaid graph at lines 44-83 which lacks the edge, and the consumer-profile pins at lines 184-226)
    - `docs/src/api-reference/feature-flags.md` (line 120's pin, the usage-example pins, line 302's adapter import, line 419's Dockerfile excerpt, and every flag table)
    - `.planning/phases/34-documentation-currency-audit/34-AUDIT.md` §2 rows 2, 3 and 45
    - `Cargo.toml` `[features]` (facade) and each `crates/*/Cargo.toml` `[features]` block
    - `Dockerfile` line 10 (the builder base image)
    - `crates/paladin-memory/Cargo.toml` (the unconditional `paladin-llm` dependency the graph edge represents)
    - `.planning/phases/35-mdbook-currency/35-RESEARCH.md` §"Facade and per-crate `[features]` tables" and §"Mermaid-edge parity"
  </read_first>
  <action>
    Two commits: the crate-map pair in one, feature-flags in another.

    Crate-map pair (MB-13 and MB-14, one commit — D-24 names these two pages as the tightly-coupled
    pair). On both pages correct the crate count and the crate inventory to eleven library crates
    plus the facade, adding `paladin-eval` and `paladin-herald` with one line each, and change every
    version claim and consumer-profile pin to the literal `0.10.0` per D-20. On
    `architecture/crate-map.md` extend the `paladin-llm` feature table with the six Phase 17 flags it
    omits — `kimi`, `qwen`, `grok`, `ollama`, `openai-compatible`, `gemini` — read from
    `crates/paladin-llm/Cargo.toml`. On `api-reference/crate-map.md` add the `mem --> llm` edge to
    the mermaid dependency graph so the diagram agrees with the prose; `architecture/crate-map.md`
    already has that edge and needs no graph change (D-22, RESEARCH.md's mermaid-edge parity
    finding). Subject
    `docs(35): bring both crate maps to eleven crates, the 0.10.0 pins and the mem-to-llm edge (MB-13, MB-14)`.

    Feature flags (MB-15). Regenerate every flag table from the facade `Cargo.toml` `[features]`
    block and each crate's own `[features]` block per D-22, so each shipped flag appears once with
    its crate and what it gates, adding the four the audit found entirely undocumented — `otel`,
    `dev-ui`, `redis-cache` and `storage-postgres` — alongside the aggregates `llm-all`, `storage`
    and `full` and the `default` set. Change the crate pin at line 120 and every other pin in the
    usage-example section to the literal `0.10.0`. Replace the Dockerfile excerpt at line 419, which
    pins a 1.75 base image, with the real builder base image `rust:1.93-slim-bookworm` read from
    `Dockerfile` line 10, or with a pointer to `Dockerfile` as the source of truth. Fix the
    relocated-LLM-adapter import at line 302 under D-13's rule: the adapters live in the
    `paladin_llm` crate, reached as `paladin_llm::openai::OpenAIAdapter`,
    `paladin_llm::anthropic::AnthropicAdapter` and `paladin_llm::deepseek::DeepSeekAdapter` — note
    the capital I and A in the OpenAI adapter's type name — and the facade kept no shim for the old
    module path. Any YAML fence on the page must still parse, so run `./scripts/check-doc-config.sh`
    before committing. Subject
    `docs(35): regenerate the feature-flag tables from Cargo.toml (MB-15)`.

    Per D-16 add no dated callout to any of the three pages. Run `mdbook build docs/` and
    `./scripts/check-doc-config.sh` after both commits.
  </action>
  <verify>
    <automated>cd /workspace &amp;&amp; grep -q 'mem --> llm' docs/src/api-reference/crate-map.md &amp;&amp; for f in otel dev-ui redis-cache storage-postgres; do grep -q "$f" docs/src/api-reference/feature-flags.md || exit 1; done &amp;&amp; grep -q 'rust:1.93-slim-bookworm' docs/src/api-reference/feature-flags.md &amp;&amp; ! grep -rqE '"0\.[5-9]\.[0-9]+"' docs/src/architecture/crate-map.md docs/src/api-reference/crate-map.md docs/src/api-reference/feature-flags.md &amp;&amp; ! grep -q 'paladin::infrastructure::adapters::llm::' docs/src/api-reference/feature-flags.md &amp;&amp; mdbook build docs/ &amp;&amp; ./scripts/check-doc-config.sh</automated>
  </verify>
  <acceptance_criteria>
    - Both crate-map pages name `paladin-eval` and `paladin-herald`, and `grep -c 'nine workspace crates\|nine member crates\|nine focused crates' docs/src/architecture/crate-map.md docs/src/api-reference/crate-map.md` returns `0` for both
    - `grep -c 'mem --> llm' docs/src/api-reference/crate-map.md` returns at least `1`
    - `docs/src/architecture/crate-map.md`'s `paladin-llm` feature table names `kimi`, `qwen`, `grok`, `ollama`, `openai-compatible` and `gemini`
    - `docs/src/api-reference/feature-flags.md` names `otel`, `dev-ui`, `redis-cache`, `storage-postgres`, `llm-all`, `storage`, `full` and contains `rust:1.93-slim-bookworm` or a link to `Dockerfile`
    - `grep -cE '"0\.[5-9]\.[0-9]+"' docs/src/architecture/crate-map.md docs/src/api-reference/crate-map.md docs/src/api-reference/feature-flags.md` returns `0` for all three
    - `grep -c 'paladin::infrastructure::adapters::llm::' docs/src/api-reference/feature-flags.md` returns `0`, and any adapter type the page names is spelled `OpenAIAdapter`, `AnthropicAdapter` or `DeepSeekAdapter`
    - `mdbook build docs/` exits 0 with `No broken links found`; `./scripts/check-doc-config.sh` exits 0
    - `git log --oneline --grep 'MB-13'` and `--grep 'MB-15'` each return at least one commit
  </acceptance_criteria>
  <done>MB-13, MB-14 and MB-15 are closed: the crate inventory, the feature inventory, the dependency diagram and the container base image all match the tree.</done>
</task>

<task type="auto">
  <name>Task 3: MB-16 and MB-17 — migration-guide.md and stable-api.md</name>
  <files>docs/src/api-reference/migration-guide.md, docs/src/api-reference/stable-api.md</files>
  <read_first>
    - `docs/src/api-reference/migration-guide.md` line 3, lines 5-50 (the v0.10.0 bridging section the audit confirmed accurate) and the Timeline table near line 512
    - `docs/src/api-reference/stable-api.md` line 3, the catalogue paths throughout, the Public crates list at lines 920-931 and the footer at lines 898-900
    - `.planning/phases/34-documentation-currency-audit/34-AUDIT.md` §2 rows 4 and 6, and the "D-09 subsection" that records `upgrading.md` as already agreeing with `MIGRATION.md`
    - `crates/paladin-core/src/platform/container/paladin.rs` and `crates/paladin-ports/src/output/paladin_port.rs` (the live crate-rooted paths the catalogue must use)
    - `crates/paladin-core/Cargo.toml` (`name = "paladin-ai-core"` — the package name the crate path `paladin_core::` derives from)
    - `ls crates/` (the eleven library crates for the Public crates list)
  </read_first>
  <action>
    Two commits, one per page.

    `migration-guide.md` (MB-16, per D-06). Correct the opening sentence so it reads up to the
    current v0.10.0 release, and correct the Timeline table row that still marks 0.1.0 as Current so
    v0.10.0 carries that marker. Keep the page's pointer-only design: it must not duplicate
    `MIGRATION.md` §9.1 or §9.8, both of which the audit already recorded as agreeing with
    `upgrading.md`. Do not edit `docs/src/api-reference/upgrading.md` — D-00f keeps it out of scope.
    Historical Timeline rows other than the Current marker stay as they are; they are exempt under
    D-20 and are named in the D-21 allowlist by plan 35-10. Subject
    `docs(35): correct the migration guide's current-release claim and Timeline (MB-16)`.

    `stable-api.md` (MB-17). Rewrite every fully-qualified catalogue path from the pre-workspace
    facade layout to the live crate paths: domain container types root at
    `paladin_core::platform::container::…` (the `paladin-ai-core` package) and port traits root at
    `paladin_ports::output::…`. Check each rewritten path against the file the audit names for it
    before writing it. Correct the Version line at line 3 and the footer version at lines 898-900 to
    0.10.0, and extend the Public crates list so it holds all eleven library crates plus the facade,
    adding `paladin-eval` and `paladin-herald`. Per D-11(b) every fence on this page is a catalogue
    fragment: correct it in place and mark it illustrative, adding the illustrative-fragment header
    note if the page lacks one. Subject
    `docs(35): reroot the stable-API catalogue on the live crate paths (MB-17)`.

    Per D-16 add no dated callout to either page. Run `mdbook build docs/` after both commits.
  </action>
  <verify>
    <automated>cd /workspace &amp;&amp; grep -q 'v0.10.0' docs/src/api-reference/migration-guide.md &amp;&amp; ! grep -qE 'up to the current \*\*v0\.[5-9]' docs/src/api-reference/migration-guide.md &amp;&amp; grep -q 'paladin_core::platform::container' docs/src/api-reference/stable-api.md &amp;&amp; grep -q 'paladin_ports::output' docs/src/api-reference/stable-api.md &amp;&amp; grep -q 'paladin-eval' docs/src/api-reference/stable-api.md &amp;&amp; grep -q 'paladin-herald' docs/src/api-reference/stable-api.md &amp;&amp; git diff --quiet HEAD -- docs/src/api-reference/upgrading.md &amp;&amp; mdbook build docs/</automated>
  </verify>
  <acceptance_criteria>
    - `docs/src/api-reference/migration-guide.md` line 3 names v0.10.0 as the current release, and its Timeline table marks v0.10.0 — not 0.1.0 — as Current
    - `git log --oneline -- docs/src/api-reference/upgrading.md` shows no Phase 35 commit (D-00f)
    - `grep -c 'paladin::core::platform::container' docs/src/api-reference/stable-api.md` returns `0`; the page uses `paladin_core::platform::container::` and `paladin_ports::output::` instead
    - `docs/src/api-reference/stable-api.md` names `paladin-eval` and `paladin-herald` in its Public crates list, and its Version line and footer both read 0.10.0
    - `grep -cE '^```rust$' docs/src/api-reference/stable-api.md` returns `0` — catalogue fences are marked illustrative per D-11(b)
    - `mdbook build docs/` exits 0 with `No broken links found`
    - `git log --oneline --grep 'MB-16'` and `--grep 'MB-17'` each return at least one commit
  </acceptance_criteria>
  <done>MB-16 and MB-17 are closed: the migration guide states the real current release and every stable-API path resolves against the workspace crates.</done>
</task>

</tasks>

<!-- planner-discipline-allow: paladin::infrastructure::adapters::llm:: -->
<!-- planner-discipline-allow: paladin::core::platform::container -->
<!-- planner-discipline-allow: "0\.[5-9]\.[0-9]+" -->

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository -> published book (`docs/book/html/`) | Anything written into `docs/src` is published |
| published book -> github.com | The ADR index emits outbound links the reader follows |
| planning corpus -> shipped page | ADR records under `.planning/decisions/` become reader-facing through the index |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-07 | Spoofing | `docs/src/contributing/adr-index.md` outbound links | medium | mitigate | Every Record cell is a blob URL under the single canonical repository host and path recorded in `Cargo.toml` `repository`; no third-party host, shortener or user-supplied URL appears, and the acceptance criterion asserts the exact prefix, so a reader cannot be steered off the project's own repository |
| T-35-08 | Information Disclosure | the ADR index's link targets | low | mitigate | The nine indexed ADRs are decision records already published in the repository; the index links them by URL and does not copy internal deliberation, credentials or unreleased-surface detail into the book |
| T-35-SC | Tampering | npm/pip/cargo installs | low | accept | Prose-only plan: no package is installed and no Rust file is added or edited, so the supply-chain surface is unchanged |
</threat_model>

<verification>
- `mdbook build docs/` exits 0 with `No broken links found` — proves the nav retitle, the new entry and every relative link resolve
- `./scripts/check-doc-config.sh` exits 0 — the feature-flags page's YAML fences still parse
- `./scripts/check-doc-examples.sh` exits 0
- `git diff 35-01..HEAD --name-only -- src/ crates/` is empty for this plan's commits (D-00c)
- `git log --oneline -- docs/src/api-reference/upgrading.md` shows no Phase 35 commit (D-00f)
</verification>

<success_criteria>
- All six rows closed: one retitle, one new page, four corrections plus the crate-map pair
- The ADR index links nine decision records through the blob-URL form linkcheck does not fetch
- Both crate maps and the feature-flags page are regenerated from `Cargo.toml` (D-22)
- The full `docs.yml` sequence green on this plan's final commit
</success_criteria>

<output>
Create `.planning/phases/35-mdbook-currency/35-05-SUMMARY.md` when done, with the D-23 closure table
(`MB-nn | page | disposition | commit | how the §5 finding was addressed`) and, if any observation
was made on a page the audit settled `current`, a `## Deferred observations` section for plan 35-10
to fold into `deferred-items.md` (D-27).
</output>
