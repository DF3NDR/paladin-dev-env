---
phase: 35-mdbook-currency
plan: 06
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - docs/src/deployment/cicd.md
  - docs/src/contributing/testing-guide.md
  - docs/src/operations/monitoring.md
  - docs/src/operations/performance-tuning.md
  - docs/src/operations/troubleshooting.md
autonomous: true
requirements: [CURR-06, CURR-07, CURR-08]
must_haves:
  truths:
    - "D-15: `docs/src/deployment/cicd.md` carries a table of the real `.github/workflows/ci.yml` and `release.yml` jobs with what each gates and whether it is required or advisory per `.github/rulesets/protect-main-branch.json`, and any YAML it keeps is a verbatim excerpt captioned with its source file and job"
    - "D-15: `docs/src/deployment/cicd.md` lists `codeql.yml` among the workflow files and words its disposition as `.github/instructions/security.instructions.md` words it — advisory-only, does not gate a merge"
    - "D-15: `docs/src/contributing/testing-guide.md` names no workflow file that does not exist under `.github/workflows/` and no job that does not exist in a real workflow"
    - "MB-36: `docs/src/contributing/testing-guide.md` names `scripts/coverage.sh` as the single source of truth for the coverage command and shows the feature list `integration-tests,llm-all` with the `--fail-under-lines` floor of 82"
    - "D-16: the superseded dated callouts on `docs/src/operations/monitoring.md`, `docs/src/operations/troubleshooting.md` and `docs/src/operations/performance-tuning.md` are rewritten to the current truth or removed, and no new dated callout is added anywhere"
    - "MB-32: `docs/src/operations/monitoring.md` states that OpenTelemetry is a real optional workspace dependency behind the `otel` feature, names the shipped OTLP trace sink, and links `operations/observability.md`"
    - "MB-34: `docs/src/operations/troubleshooting.md` no longer claims OpenTelemetry is absent from the workspace while keeping its correct no-metrics-route conclusion"
    - "MB-33: `docs/src/operations/performance-tuning.md` names `benches/engine_benchmarks.rs` alongside `benches/config_benchmarks.rs`"
    - "D-00e: `mdbook build docs/` exits 0 with no broken links and `./scripts/check-doc-config.sh` exits 0 on every YAML fence these pages keep"
    - statement: "D-15: no invented job name and no invented workflow filename survives on either CI page"
      verification: backstop
  artifacts:
    - docs/src/deployment/cicd.md
    - docs/src/contributing/testing-guide.md
  key_links:
    - "`.github/workflows/ci.yml` and `release.yml` -> the job tables on `docs/src/deployment/cicd.md` and `docs/src/contributing/testing-guide.md` (D-15 source of truth)"
    - "`.github/rulesets/protect-main-branch.json` -> the required-versus-advisory column of those tables"
    - "`scripts/coverage.sh` -> the coverage command on `docs/src/contributing/testing-guide.md` (the shared script both `make coverage` and the CI coverage job execute)"
    - "`docs/src/operations/monitoring.md` -> `operations/observability.md` (the page that already documents the shipped trace export)"
  prohibitions:
    - statement: "D-01: no page under docs/src is deleted and no entry is removed from docs/src/SUMMARY.md"
      status: recalled
      verification: flagged-unverified
    - statement: "D-16: no new dated correction callout is added to any page"
      status: recalled
      verification: flagged-unverified
    - statement: "D-15: the Snyk disposition is stated as evaluated and removed with zero Rust coverage and is never re-argued from scratch"
      status: recalled
      verification: flagged-unverified
    - statement: "D-26: no existing crates/doc-examples module, support.rs, examples/ program or examples/README.md is edited"
      status: recalled
      verification: flagged-unverified
---

<objective>
Close the CI and Operations rows — MB-31, MB-36, MB-32, MB-33 and MB-34 — the five pages whose
findings are fabricated CI content and superseded dated callouts.

D-15 governs the two CI pages: the invented workflow YAML is replaced by a table of the real jobs
derived from `.github/workflows/ci.yml`, `release.yml` and `.github/rulesets/protect-main-branch.json`,
with any retained YAML a verbatim captioned excerpt, and `codeql.yml` worded exactly as
`.github/instructions/security.instructions.md` words it. D-16 governs the three Operations pages:
the "Corrected 2026-08-24" callouts whose premise Phase 22 and Phase 28 have since falsified are
rewritten to the current truth or removed, the one that still holds stays, and no new dated callout
replaces them — provenance lives in the commits and the CHANGELOG entry.

Purpose: make SC3 true for the pages a reader would follow to reproduce CI, and retire the dated
correction style the audit found had itself gone stale.
Output: five corrected pages, one commit per page.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-mdbook-currency/35-CONTEXT.md
@.planning/phases/35-mdbook-currency/35-RESEARCH.md
@.planning/phases/35-mdbook-currency/35-PATTERNS.md
@.planning/phases/35-mdbook-currency/35-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
New symbols created by Phase 35 — exclude from drift verification against the pre-phase tree. This
plan creates no new file.

Created by sibling plans and referenced here: `docs/src/user-guides/superstep-engine.md` and
`crates/doc-examples/src/superstep_engine.rs` (plan 35-01); `crates/doc-examples/src/`
`paladin_agents.rs`, `arsenal_tools.rs`, `herald_output.rs`, `battalion_patterns.rs`,
`sanctum_vector_memory.rs` plus their `pub mod` lines (plan 35-02);
`docs/src/contributing/adr-index.md` and the retitled Contributing nav entry (plan 35-05);
`CHANGELOG.md` `### Documentation` under `## [0.10.0]` (plan 35-10);
`.planning/phases/35-mdbook-currency/{deferred-items.md,35-EVIDENCE.md}`; requirement IDs
`CURR-06`…`CURR-10`.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: MB-31 — cicd.md rebuilt on the real workflow inventory, end to end</name>
  <files>docs/src/deployment/cicd.md</files>
  <read_first>
    - `docs/src/deployment/cicd.md` (the Workflow Structure listing at lines 30-40, the CI Pipeline YAML sample at lines 46-142, the Release Pipeline sample at lines 177-274, and the Security Scanning section at lines 346-378 which the audit confirmed already correct)
    - `.planning/phases/34-documentation-currency-audit/34-AUDIT.md` §2 row 61 (MB-31's three findings) and §5 row 26
    - `.github/workflows/ci.yml` — every top-level job key and the `coverage` job body
    - `.github/workflows/release.yml` — every top-level job key and each job's `name:` display string
    - `.github/workflows/codeql.yml` and `.github/instructions/security.instructions.md` §"Known gap: no Rust SAST" (the exact advisory-only wording to repeat)
    - `.github/rulesets/protect-main-branch.json` — the `required_status_checks` array (the required-versus-advisory column)
    - `ls .github/workflows/` (the real workflow file inventory)
  </read_first>
  <action>
    Replace the fabricated pipeline YAML with a table of the real jobs per D-15. Add the missing
    workflow file to the Workflow Structure listing so it holds every file `ls .github/workflows/`
    returns, including the CodeQL workflow. Build one table for `ci.yml` with a row per top-level job
    key — `lint`, `actionlint`, `security-audit`, `cargo-deny`, `osv-scanner`, `api-surface`, `msrv`,
    `semver`, `test`, `examples`, `crate-isolation`, `integration-tests`, `docker-integration`,
    `ollama-integration`, `postgres-integration`, `redis-cache-integration`, `redis-queue`,
    `sdk-clients`, `coverage`, `cli-tests`, `bench-check`, `docker`, `kubernetes-smoke`,
    `benchmark-regression-signal`, `publish-dry-run` and any further key the file holds — with
    columns Job, What it gates, Required or advisory; fill the third column from the
    `required_status_checks` array in `.github/rulesets/protect-main-branch.json`, never from
    memory. Build a second table the same way for `release.yml`, whose real jobs are
    `verify-tag-source`, `test`, `create-release`, `build-docker`, `build-binaries`,
    `check-release-consistency`, `sbom`, `finalize-release-body` and `publish-crates`; the page's
    current sample names a release-build job that does not exist and never mentions the tag-source
    guard, so state that guard's role explicitly. If any YAML fence is kept, it must be a verbatim
    excerpt of a real job with a caption of the form `# excerpt: .github/workflows/ci.yml — job:
    coverage`, and the `coverage` excerpt must include the `--fail-under-lines` flag the current
    sample omits. Add a CodeQL row and one sentence repeating the advisory-only disposition exactly
    as `.github/instructions/security.instructions.md` states it — it runs on every push, pull
    request and schedule, reports into the code-scanning UI, is not pinned in any ruleset and does
    not gate a merge. Leave the Security Scanning section's existing correction alone; the audit
    confirmed it accurate, and its instruction not to reintroduce a Snyk step stands.

    Per D-16 add no new dated callout. Run `mdbook build docs/` and `./scripts/check-doc-config.sh`
    (every YAML fence must still parse), then commit with
    `git add -- docs/src/deployment/cicd.md && git commit -q -m "docs(35): replace the fabricated CI YAML with the real job tables (MB-31)" -- docs/src/deployment/cicd.md`
    (D-24).
  </action>
  <verify>
    <automated>cd /workspace &amp;&amp; grep -q 'codeql.yml' docs/src/deployment/cicd.md &amp;&amp; grep -q 'verify-tag-source' docs/src/deployment/cicd.md &amp;&amp; grep -q 'check-release-consistency' docs/src/deployment/cicd.md &amp;&amp; grep -q 'fail-under-lines' docs/src/deployment/cicd.md &amp;&amp; ! grep -qE 'build-release' docs/src/deployment/cicd.md &amp;&amp; for j in actionlint api-surface osv-scanner crate-isolation publish-dry-run; do grep -q "$j" docs/src/deployment/cicd.md || exit 1; done &amp;&amp; mdbook build docs/ &amp;&amp; ./scripts/check-doc-config.sh</automated>
  </verify>
  <acceptance_criteria>
    - `docs/src/deployment/cicd.md` names every file `ls .github/workflows/` returns, including the CodeQL workflow
    - The page contains a job table naming at least `lint`, `actionlint`, `security-audit`, `cargo-deny`, `osv-scanner`, `api-surface`, `msrv`, `semver`, `test`, `examples`, `crate-isolation`, `coverage`, `publish-dry-run`, each with a required-or-advisory value
    - The page names `verify-tag-source`, `create-release`, `build-docker`, `build-binaries`, `check-release-consistency`, `sbom`, `finalize-release-body` and `publish-crates`, and `grep -c 'build-release' docs/src/deployment/cicd.md` returns `0`
    - The page contains the literal `--fail-under-lines`
    - Every retained YAML fence is preceded by a caption line beginning `# excerpt: .github/workflows/`
    - The CodeQL sentence states it does not gate a merge
    - `mdbook build docs/` exits 0 with `No broken links found`; `./scripts/check-doc-config.sh` exits 0
    - `git log --oneline -1 -- docs/src/deployment/cicd.md` subject matches `docs\(35\): .*\(MB-31\)`
  </acceptance_criteria>
  <done>MB-31 is closed with a table a reader can check against the workflow files, and the D-15 correction shape is proven for the sibling CI page.</done>
</task>

<task type="auto">
  <name>Task 2: MB-36 — testing-guide.md's CI section and coverage command</name>
  <files>docs/src/contributing/testing-guide.md</files>
  <read_first>
    - `docs/src/contributing/testing-guide.md` line 68 (the coverage-floor statement the audit confirmed correct), lines 401 and 446-449 (the page's own promise and its stated coverage invocation), and lines 628-686 (the fabricated workflow sample)
    - `.planning/phases/34-documentation-currency-audit/34-AUDIT.md` §2 row 54 and the "Coverage command comparison" subsection that records the three-way comparison verbatim
    - `scripts/coverage.sh` lines 90-110 (the real invocation both `make coverage` and the CI coverage job execute, and the floor default)
    - `Makefile` lines 305-315 (the `coverage` target that delegates to the script)
    - `.github/workflows/ci.yml` — the `coverage` job and its own delegation comment
    - `ls .github/workflows/` (the real workflow inventory)
    - `docs/src/deployment/cicd.md` (the job table Task 1 landed — the target state this page's CI section should agree with)
  </read_first>
  <action>
    Delete the fabricated GitHub Actions sample at lines 628-686 — it names a workflow file that has
    never existed alongside the seven real ones, uses a deprecated toolchain action the real CI does
    not use, and shows no coverage-floor enforcement — and replace it with a short section that
    names the real workflows, points at `docs/src/deployment/cicd.md` for the full job table, and, if
    it keeps any YAML, keeps only a verbatim captioned excerpt of a real job per D-15.

    Correct the coverage section. The page's own promise is that every command it shows is the
    command CI runs; today its invocation measures a strictly smaller feature set than CI does.
    State `scripts/coverage.sh` as the single source of truth that both `make coverage` and the CI
    `coverage` job delegate to, show its real invocation with the feature list
    `integration-tests,llm-all` and the `--fail-under-lines` floor of 82, and explain in one sentence
    why the aggregate LLM feature matters: the default feature set builds three of the nine shipped
    provider adapters, so a measurement without it silently excludes the other six adapters' lines.
    Leave the coverage-floor figure of 82 alone — the audit confirmed all three sources agree with
    ADR-0006 — and leave the object-store image pins alone, already checked clean. The end-to-end
    Docker-machine reproduction stays out of scope; it is the maintainer's own pending item in the
    Phase 34 deferred register and is not absorbed here.

    Per D-16 add no dated callout. Run `mdbook build docs/` and `./scripts/check-doc-config.sh`, then
    commit with
    `git add -- docs/src/contributing/testing-guide.md && git commit -q -m "docs(35): replace the fabricated workflow sample and correct the coverage invocation (MB-36)" -- docs/src/contributing/testing-guide.md`
    (D-24).
  </action>
  <verify>
    <automated>cd /workspace &amp;&amp; grep -q 'scripts/coverage.sh' docs/src/contributing/testing-guide.md &amp;&amp; grep -q 'integration-tests,llm-all' docs/src/contributing/testing-guide.md &amp;&amp; grep -q 'fail-under-lines' docs/src/contributing/testing-guide.md &amp;&amp; ! grep -q 'workflows/test.yml' docs/src/contributing/testing-guide.md &amp;&amp; ! grep -q 'actions-rs/toolchain' docs/src/contributing/testing-guide.md &amp;&amp; mdbook build docs/ &amp;&amp; ./scripts/check-doc-config.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'workflows/test.yml' docs/src/contributing/testing-guide.md` returns `0` and every workflow filename the page names exists under `.github/workflows/`
    - `grep -c 'actions-rs/toolchain' docs/src/contributing/testing-guide.md` returns `0`
    - The page contains the literals `scripts/coverage.sh`, `integration-tests,llm-all`, `--fail-under-lines` and `82`
    - The page links `docs/src/deployment/cicd.md` (or its book-relative path) for the full job table
    - Every retained YAML fence is preceded by a caption line beginning `# excerpt: .github/workflows/`
    - `mdbook build docs/` exits 0 with `No broken links found`; `./scripts/check-doc-config.sh` exits 0
    - `git log --oneline -1 -- docs/src/contributing/testing-guide.md` subject matches `docs\(35\): .*\(MB-36\)`
  </acceptance_criteria>
  <done>MB-36 is closed: the testing guide names only real workflows and shows the coverage command CI actually runs.</done>
</task>

<task type="auto">
  <name>Task 3: MB-32, MB-33 and MB-34 — the three superseded Operations callouts</name>
  <files>docs/src/operations/monitoring.md, docs/src/operations/performance-tuning.md, docs/src/operations/troubleshooting.md</files>
  <read_first>
    - `docs/src/operations/monitoring.md` (the top-level not-implemented disclaimer, the Health Checks callout the audit confirmed still accurate, and the Distributed Tracing section at lines 384-410)
    - `docs/src/operations/troubleshooting.md` (the repeated dependency-absence premise and the Enable Debug Logging section the audit re-verified)
    - `docs/src/operations/performance-tuning.md` (the Benchmark Results callout near lines 100-120)
    - `.planning/phases/34-documentation-currency-audit/34-AUDIT.md` §2 rows 70, 72 and 73
    - `Cargo.toml` — the `opentelemetry`, `opentelemetry_sdk` and `opentelemetry-otlp` dependency lines and the `otel` feature
    - `src/infrastructure/telemetry/mod.rs` (`build_run_sink`) and `docs/src/operations/observability.md` (the page that already documents the shipped trace export)
    - `ls benches/` (the real benchmark files)
    - `Dockerfile` around line 68 (the exposed ports the troubleshooting page's conclusion rests on)
  </read_first>
  <action>
    Three commits, one per page. Per D-16 every correction is clean: rewrite the superseded callout
    to the current truth or remove it, keep the callouts that still hold, and add no new dated
    marker.

    `monitoring.md` (MB-32). The page's blanket premise that neither metrics nor tracing crates are
    workspace dependencies was true when written and is now false for the tracing half: the
    OpenTelemetry crates are real optional workspace dependencies behind the `otel` feature. Rewrite
    that premise so it states which half still holds — no metrics crate, therefore still no scrape
    route — and which no longer does. Replace the Distributed Tracing section, which shows code
    against two crates that are not dependencies at all, with a short section naming the shipped
    OTLP trace sink and the `otel` feature and linking `operations/observability.md` as the page
    that documents it. Leave the Health Checks callout alone; the audit re-verified it. Subject
    `docs(35): correct the tracing premise and point at the shipped OTLP sink (MB-32)`.

    `troubleshooting.md` (MB-34). The page repeats the same dependency-absence premise verbatim;
    correct it the same way while keeping its conclusions, which the audit confirmed still hold —
    there is no metrics scrape route, the port the page called out as fabricated is still
    fabricated, the real ports are the two the Dockerfile exposes, and there is no logging key in
    the settings type. Subject
    `docs(35): correct the repeated tracing-dependency premise (MB-34)`.

    `performance-tuning.md` (MB-33). The Benchmark Results callout claims the benchmark directory
    holds only the configuration benchmark; a superstep-engine benchmark file was added after that
    callout was written. Rewrite the callout to name both real benchmark files, keep the accurate
    part about the never-fixed drafted files, and link the new engine guide where the engine
    benchmark is relevant. Subject
    `docs(35): name the engine benchmark file in the performance-tuning callout (MB-33)`.

    Run `mdbook build docs/` and `./scripts/check-doc-config.sh` after the three commits.
  </action>
  <verify>
    <automated>cd /workspace &amp;&amp; grep -q 'otel' docs/src/operations/monitoring.md &amp;&amp; grep -q 'observability.md' docs/src/operations/monitoring.md &amp;&amp; ! grep -q 'opentelemetry_jaeger' docs/src/operations/monitoring.md &amp;&amp; ! grep -q 'tracing_opentelemetry' docs/src/operations/monitoring.md &amp;&amp; grep -q 'engine_benchmarks.rs' docs/src/operations/performance-tuning.md &amp;&amp; ! grep -rq 'Corrected 2026-09' docs/src/operations &amp;&amp; mdbook build docs/ &amp;&amp; ./scripts/check-doc-config.sh</automated>
  </verify>
  <acceptance_criteria>
    - `docs/src/operations/monitoring.md` names the `otel` feature and the shipped OTLP trace sink and links `observability.md`; `grep -cE 'opentelemetry_jaeger|tracing_opentelemetry' docs/src/operations/monitoring.md` returns `0`
    - Neither `docs/src/operations/monitoring.md` nor `docs/src/operations/troubleshooting.md` still asserts that OpenTelemetry is not a dependency anywhere in the workspace, while both keep their no-metrics-route conclusion
    - `docs/src/operations/performance-tuning.md` names both `config_benchmarks.rs` and `engine_benchmarks.rs` and links `user-guides/superstep-engine.md`
    - `grep -rc 'Corrected 2026-09' docs/src/operations` returns `0` for all three files (D-16)
    - `mdbook build docs/` exits 0 with `No broken links found`; `./scripts/check-doc-config.sh` exits 0
    - `git log --oneline --grep 'MB-32'`, `--grep 'MB-33'` and `--grep 'MB-34'` each return at least one commit
  </acceptance_criteria>
  <done>MB-32, MB-33 and MB-34 are closed and the dated-callout style is retired without leaving a new dated marker behind.</done>
</task>

</tasks>

<!-- planner-discipline-allow: build-release -->
<!-- planner-discipline-allow: workflows/test.yml -->
<!-- planner-discipline-allow: actions-rs/toolchain -->
<!-- planner-discipline-allow: opentelemetry_jaeger -->
<!-- planner-discipline-allow: tracing_opentelemetry -->
<!-- planner-discipline-allow: Corrected 2026-09 -->

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository -> published book (`docs/book/html/`) | Anything written into `docs/src` is published |
| CI configuration -> documentation | The book describes the gates that protect `main`; a wrong description misleads a contributor about what is enforced |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-09 | Repudiation | `docs/src/deployment/cicd.md`, `docs/src/contributing/testing-guide.md` | medium | mitigate | The required-versus-advisory column is filled from `.github/rulesets/protect-main-branch.json` and the CodeQL sentence repeats `.github/instructions/security.instructions.md` verbatim, so the book cannot read as assurance that a merge is gated by a scan that is advisory-only — the exact misreading the Snyk removal record warns against |
| T-35-10 | Information Disclosure | retained YAML excerpts on the two CI pages | low | mitigate | Only verbatim excerpts of real public workflow jobs are kept, each captioned with its source file and job name; no secret name, token value or runner credential is transcribed into the book |
| T-35-SC | Tampering | npm/pip/cargo installs | low | accept | Prose-only plan: no package is installed and no Rust file is added or edited, so the supply-chain surface is unchanged |
</threat_model>

<verification>
- `mdbook build docs/` exits 0 with `No broken links found`
- `./scripts/check-doc-config.sh` exits 0 — every YAML fence on the two CI pages still parses
- `./scripts/check-doc-examples.sh` exits 0
- `grep -rn 'test\.yml\|build-release' docs/src/deployment/cicd.md docs/src/contributing/testing-guide.md` returns nothing (the D-21 fabricated-CI-name grep, on the two pages that carried the hits)
- `git diff 35-01..HEAD --name-only -- src/ crates/` is empty for this plan's commits (D-00c)
</verification>

<success_criteria>
- All five rows closed, one commit per page (D-24)
- Both CI pages describe only jobs and workflow files that exist, with required-versus-advisory taken from the ruleset (D-15)
- The coverage command shown is the command CI runs (MB-36)
- The three superseded dated callouts are gone and no new one replaces them (D-16)
- The full `docs.yml` sequence green on this plan's final commit
</success_criteria>

<output>
Create `.planning/phases/35-mdbook-currency/35-06-SUMMARY.md` when done, with the D-23 closure table
(`MB-nn | page | disposition | commit | how the §5 finding was addressed`) and, if any observation
was made on a page the audit settled `current`, a `## Deferred observations` section for plan 35-10
to fold into `deferred-items.md` (D-27).
</output>
