---
phase: 36-rustdoc-zero-warning-bar-examples-currency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/paladin-memory/src/token_counter/mod.rs
  - crates/paladin-ports/src/output/structured_executor_port.rs
  - crates/paladin-storage/src/waypoint/contract_tests.rs
  - examples/token_economy_commissary.rs
  - examples/README.md
  - .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-EVIDENCE.md
  - .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-01-baseline.txt
  - .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-01-percrate.txt
  - .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-01-example-run.txt
autonomous: true
requirements: [CURR-11, CURR-12, CURR-14, CURR-15]
must_haves:
  truths:
    - "`RUSTDOCFLAGS=\"-D warnings\" cargo doc -p paladin-memory --all-features --no-deps` exits 0 and `cargo doc -p paladin-memory --no-deps` emits zero rustdoc diagnostic lines, closing RD-01, RD-66 and RD-126 (the WINDOWS.md row 37 `HeuristicTokenCounter` link)."
    - "`RUSTDOCFLAGS=\"-D warnings\" cargo doc -p paladin-ports --all-features --no-deps` exits 0, closing RD-51 and RD-127."
    - "`RUSTDOCFLAGS=\"-D warnings\" cargo doc -p paladin-storage --all-features --no-deps` exits 0, closing RD-46 and RD-128."
    - "`cargo run --example token_economy_commissary` exits 0 offline with no provider key set, and its stdout names every capability EX-109, EX-111, EX-112, EX-113, EX-114 and EX-115 stands for."
    - "`examples/README.md` carries a `### [token_economy_commissary.rs](token_economy_commissary.rs)` section with a **Demonstrates:** line and a TOC entry that links to it."
    - "`make api-surface` reports the surface unchanged after every commit in this plan."
    - "`36-EVIDENCE.md` exists and carries the verbatim baseline capture of both bar commands plus this plan's per-crate and example-run captures."
  artifacts:
    - "crates/paladin-memory/src/token_counter/mod.rs — module doc block with a resolvable link to HeuristicTokenCounter"
    - "crates/paladin-ports/src/output/structured_executor_port.rs — de-linked private mention"
    - "crates/paladin-storage/src/waypoint/contract_tests.rs — de-linked private mention"
    - "examples/token_economy_commissary.rs — new runnable example binary"
    - "examples/README.md — new Token Economy TOC section and program section"
    - ".planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-EVIDENCE.md"
    - ".planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/ — per-plan verbatim captures"
  key_links:
    - "The module doc link in crates/paladin-memory/src/token_counter/mod.rs must resolve against the `pub use heuristic::HeuristicTokenCounter;` re-export declared later in the same file — the Pitfall 1 bare-shorthand family that also governs the paladin-battalion engine/mod.rs cluster."
    - "examples/token_economy_commissary.rs must import Commissary, resolve_context_window, WindowSource, WindowFallbackPolicy and ResolvedWindow through the `paladin::` facade re-exports (src/lib.rs:194-211), not through crate-internal paths."
    - "The README section header must stay matchable by `grep -oE '^### \\[[a-zA-Z0-9_]+\\.rs\\]' examples/README.md` so the Phase 34 listed-vs-on-disk cross-check keeps working."
  prohibitions:
    - statement: "No private item is widened from pub(crate) to pub to satisfy a documentation link (D-05)."
      status: recalled
      verification: flagged-unverified
    - statement: "No rustdoc lint-suppression attribute is added to silence a private intra-doc link (D-05)."
      status: recalled
      verification: flagged-unverified
    - statement: "No library source behaviour changes — every edit in this plan is a doc comment, a new example binary, a README section or a .planning/ artifact (ROADMAP Phase 36 boundary)."
      status: recalled
      verification: flagged-unverified
    - statement: "No example prints or hard-codes an API key; a program that reads a key from the environment says so in its header comment (D-29)."
      status: recalled
      verification: flagged-unverified
---

<objective>
Prove the whole Phase 36 fix-and-prove loop end-to-end on the thinnest possible slice, then close
the three single-location-group rustdoc crates.

This plan implements D-00a (the zero bar), D-00b (the `34-AUDIT.md` §6 work list and stable IDs),
D-00c (shipped tree outranks any document), D-00f (doctests stay green), D-00h (the `CURR-*`
requirement prefix — this run minted CURR-11…CURR-15), D-00i (Medieval-military vocabulary and no
bare token total beside a `TokenUsage` split), D-01 (the re-baseline, which `36-RESEARCH.md`
re-ran at HEAD `619551a7` and found **zero drift**), D-02 (the bar is zero, not "the 143 rows" —
any diagnostic with no §6 row is minted `RD-144`/`EX-123` onward), D-05 (private intra-doc link
technique), D-06 (unresolved link technique), D-09 (lead-row discipline), D-10 (per-crate
verification as fixes land), D-15 (one capability cluster → one named program → one README
section), D-16 (offline-first via the mock LLM adapter), D-24 (closure table in the SUMMARY),
D-25 (`36-EVIDENCE.md` + `36-evidence/`), D-26 (commit granularity) and D-28 (`make api-surface`
before every commit touching `src/` or `crates/`).

Purpose: the riskiest technical unknown in this phase is whether a bare-shorthand intra-doc link
to a sibling item declared later in the same file can be made to resolve at all
(`36-RESEARCH.md` Pitfall 1 / Pattern 3, Assumption A1). `crates/paladin-memory/src/token_counter/mod.rs:3`
is the smallest instance of exactly that family, and it is also the WINDOWS.md row 37 row. Fixing
it first, together with one complete example program, its README section and the evidence
harness, proves every layer this phase touches before eleven expansion plans commit to the same
technique.

Spec-less probe fallback skipped (visible, recorded): Phase 36 has no SPEC, and its requirement
IDs are minted by this planning run, so there is nothing to probe. `must_haves` are derived from
the five ROADMAP Phase 36 Success Criteria and the CONTEXT decisions instead.

Output: three rustdoc crates green, one new example program, one README section, and
`36-EVIDENCE.md` seeded with the baseline both later plans and the closing plan append to.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-CONTEXT.md
@.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-RESEARCH.md
@.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-PATTERNS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "one rustdoc group closed, one capability demonstrated" — the whole loop on one path</name>
  <files>crates/paladin-memory/src/token_counter/mod.rs, examples/token_economy_commissary.rs, examples/README.md, .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-EVIDENCE.md, .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-01-baseline.txt, .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-01-percrate.txt, .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-01-example-run.txt</files>
  <read_first>
    - crates/paladin-memory/src/token_counter/mod.rs (the whole file — 3 lines of doc block plus the `pub mod heuristic;` / `pub use heuristic::HeuristicTokenCounter;` declarations it links to)
    - crates/paladin-memory/src/token_counter/heuristic.rs (confirms `HeuristicTokenCounter` is a unit struct and what its `TokenCounterPort` impl guarantees)
    - crates/paladin-battalion/src/engine/mod.rs lines 1-70 (the in-tree contrast pair: the failing bare-shorthand `//!` link at line 20 and the working path-syntax `///` link at lines 59-61 — this is the technique this task validates for the whole phase)
    - src/lib.rs lines 178-227 (the facade re-export block: `MockLlmAdapter`, `Commissary`, `CommissaryPlan`, `Consignment`, `ConsignmentItem`, `DispensedItem`, `ResolvedWindow`, `UnknownContextWindow`, `WindowFallbackPolicy`, `WindowSource`, `resolve_context_window`)
    - crates/paladin-llm/src/services/commissary.rs lines 290-400 (the `Commissary` struct, its `new` signature after the PRIM-02 argument removal, and `CommissaryPlan`)
    - crates/paladin-llm/src/window.rs lines 100-200 (`resolve_context_window`, `WindowSource`, `WindowFallbackPolicy`, `ResolvedWindow`)
    - crates/paladin-ports/src/output/token_counter_port.rs (the `TokenCounterPort` trait and the defaulted `is_exact` method)
    - examples/basic_paladin.rs (the house header-comment, import-ordering and `#[tokio::main] async fn main() -> Result&lt;(), Box&lt;dyn std::error::Error&gt;&gt;` shape)
    - examples/README.md lines 1-75 (the Table of Contents bullet style and the `### [basic_paladin.rs](basic_paladin.rs)` / **Demonstrates:** / sentence / fenced run command / **Key concepts:** section shape)
    - .planning/phases/34-documentation-currency-audit/34-AUDIT.md §6 rows RD-01, RD-66, RD-126 and rows EX-109, EX-111, EX-112, EX-113, EX-114, EX-115
  </read_first>
  <action>
Capture the baseline first, then fix, then demonstrate, then record — in that order, so the
evidence file proves the starting state as well as the end state.

(1) Baseline capture (D-01, D-25). Create the `36-evidence/` directory under the phase directory.
Run the `ci.yml:63` command verbatim — `cargo doc --workspace --no-deps` with output teed to a
file, then the negated grep — and the second bar command
`RUSTDOCFLAGS="-D warnings" cargo doc --workspace --all-features --no-deps`, recording both exit
codes into `36-evidence/36-01-baseline.txt`. Expect the default-feature run to report 65 content
diagnostics across 8 crates and the all-features run to exit 101; `36-RESEARCH.md`'s Re-baseline
delta section records those exact figures at HEAD `619551a7` with zero drift against
`34-AUDIT.md`. If the observed figures differ from 65 / 101, do not adjust the plan silently —
record the difference in the evidence file and mint the extra rows as `RD-144` onward per D-02.

(2) Fix RD-01 / RD-66 / RD-126. In `crates/paladin-memory/src/token_counter/mod.rs`, the
module-level doc block names `HeuristicTokenCounter` by bare shorthand on line 3, while the item
it refers to is the `pub use heuristic::HeuristicTokenCounter;` re-export declared further down
the same file. Per D-06 and `36-RESEARCH.md` Pattern 3, replace the bare shorthand with an
explicit path form — `heuristic::HeuristicTokenCounter` as the link target, or the `self::`
qualified path — so the link resolves under both the default and the all-features build. Do not
widen any visibility and do not add a lint-suppression attribute (D-05). Do not rewrite the
surrounding prose beyond what the link change requires (D-04). Leave the adjacent
`garrison::TiktokenCounter` mention in plain code font as it already is.

(3) New example program `examples/token_economy_commissary.rs`, closing EX-109, EX-111, EX-112,
EX-113, EX-114 and EX-115 (D-15 — one named program per cluster). It must be a single
`examples/*.rs` binary with no `required-features`, buildable under default features so the bulk
`cargo build --examples` selector picks it up. Content, in this order, each part printing what it
demonstrates so the reader sees the capability without reading source:
  - `Commissary` constructed directly through the `paladin::` facade re-export, using the
    post-PRIM-02 `Commissary::new` signature that no longer takes an `is_exact_counter` argument
    (EX-113, EX-109) — exactness is read live from the counter's own `is_exact`.
  - A `TokenCounterPort` implementor whose `is_exact` answer is printed for both the defaulted
    (heuristic) case and an exact case, so the defaulted behaviour is visible (EX-112).
  - `resolve_context_window` called through the facade, printing the returned `ResolvedWindow`
    together with its `WindowSource` and the `WindowFallbackPolicy` that produced it (EX-114,
    EX-115).
  - An Anthropic-shaped usage record produced through `MockLlmAdapter` plus an explanatory
    comment stating that Anthropic's `prompt_tokens` figure now includes cache-read and
    cache-write tokens (EX-111) — demonstrated with the mock's usage shape because no live
    provider key runs in CI (D-16).
Vocabulary rules are binding (D-00i): use `Commissary` for the input-side per-call
window-rationing officer, never `Quartermaster`, and never print a bare token total beside a
`TokenUsage` prompt/completion split. Header comment follows the house shape: what it
demonstrates (naming the capability the README section will name), the exact
`cargo run --example token_economy_commissary` command, and an explicit statement that it needs
no provider key and no external service (D-29). Use `?` propagation with a
`Box&lt;dyn std::error::Error&gt;` main; no `unwrap()` or `expect()` in the body.

(4) README section (D-15, D-20 shape). Add a `## Token Economy Examples` section to
`examples/README.md` in the existing house shape and a matching Table of Contents bullet. The
program section is `### [token_economy_commissary.rs](token_economy_commissary.rs)`, a
**Demonstrates:** line naming the Commissary window-rationing and context-window-resolution
capability, one sentence, the fenced run command, and a **Key concepts:** list. Per D-21 the new
section carries no "Code snippet" block.

(5) Evidence and closure (D-24, D-25). Create `36-EVIDENCE.md` in the phase directory following
the house `NN-EVIDENCE.md` shape: a short header naming the toolchain and the HEAD it measures, a
Baseline section pointing at `36-evidence/36-01-baseline.txt`, and a per-plan section for this
plan. Capture the post-fix per-crate sweep into `36-evidence/36-01-percrate.txt` and the example
run's exit code plus its first lines of stdout into `36-evidence/36-01-example-run.txt`.

(6) Commits (D-26): one commit for the rustdoc fix
(`docs(36): resolve rustdoc link in paladin-memory (RD-01, RD-66, RD-126)`), one for the program
(`docs(36): add token_economy_commissary example (EX-109, EX-111, EX-112, EX-113, EX-114, EX-115)`),
one for the README section and one for the evidence files. Run `make api-surface` and expect no
change before each commit that touches `crates/` or `src/` (D-28); run `cargo fmt --all -- --check`
and `cargo check --workspace --all-targets --all-features` before the program commit. Stage with
`git add -- &lt;paths&gt;`, never `git add .`.
  </action>
  <verify>
    <automated>touch crates/paladin-memory/src/lib.rs && cargo doc -p paladin-memory --no-deps 2>&1 | tee /tmp/36-01-memory-default.txt && test "$(grep -c 'warning:' /tmp/36-01-memory-default.txt)" = 0 && RUSTDOCFLAGS="-D warnings" cargo doc -p paladin-memory --all-features --no-deps && cargo build --example token_economy_commissary && cargo run --example token_economy_commissary && grep -q '^### \[token_economy_commissary\.rs\](token_economy_commissary\.rs)$' examples/README.md && cargo fmt --all -- --check && ./scripts/check-api-surface.sh .project/current-exports.txt</automated>
  </verify>
  <acceptance_criteria>
    - `RUSTDOCFLAGS="-D warnings" cargo doc -p paladin-memory --all-features --no-deps` exits 0.
    - A forced re-documentation of `paladin-memory` under default features produces a capture containing a `Documenting paladin-memory` line and zero lines matching `warning:`.
    - `grep -c 'HeuristicTokenCounter' crates/paladin-memory/src/token_counter/mod.rs` is at least 2 (the doc link and the `pub use` re-export both still name it).
    - `grep -rn 'allow(rustdoc::private_intra_doc_links)' crates/paladin-memory/src` produces no output.
    - `git diff --stat` for `crates/paladin-memory/` shows only `src/token_counter/mod.rs` changed, and `git diff crates/paladin-memory/` adds and removes only lines beginning with `//!` or `///`.
    - `examples/token_economy_commissary.rs` exists, `cargo build --example token_economy_commissary` exits 0 with no `--features` flag, and `cargo run --example token_economy_commissary` exits 0 with `OPENAI_API_KEY`, `ANTHROPIC_API_KEY` and `DEEPSEEK_API_KEY` unset.
    - `grep -c 'Quartermaster' examples/token_economy_commissary.rs` is 0.
    - The program's stdout mentions `Commissary`, `is_exact`, `resolve_context_window`, `WindowSource` and `prompt_tokens`.
    - `examples/README.md` contains `## Token Economy Examples`, a TOC bullet linking to `#token-economy-examples`, the header `### [token_economy_commissary.rs](token_economy_commissary.rs)`, a line starting `**Demonstrates:**` inside that section, and a fenced `cargo run --example token_economy_commissary` command.
    - `grep -A6 '^### \[token_economy_commissary\.rs\]' examples/README.md` does not contain `**Code snippet:**`.
    - `36-EVIDENCE.md` exists and contains the verbatim exit codes of both baseline bar commands and the string `619551a7` or the actual measured HEAD SHA.
    - `36-evidence/36-01-example-run.txt` records the example's exit code as 0.
    - `./scripts/check-api-surface.sh .project/current-exports.txt` reports the surface unchanged.
  </acceptance_criteria>
  <done>The bare-shorthand intra-doc link technique is proven on a real row group, one capability cluster has a runnable offline program listed in the gallery, and the evidence harness every later plan appends to exists — all committed.</done>
</task>

<task type="auto">
  <name>Task 2: Close the two remaining single-group crates — paladin-ports (RD-51, RD-127) and paladin-storage (RD-46, RD-128)</name>
  <files>crates/paladin-ports/src/output/structured_executor_port.rs, crates/paladin-storage/src/waypoint/contract_tests.rs, .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-01-percrate.txt</files>
  <read_first>
    - crates/paladin-ports/src/output/structured_executor_port.rs lines 130-200 (the `run_structured` doc block at line 158 and the `repair_prompt` item it links to — confirm `repair_prompt`'s actual visibility before choosing the fix)
    - crates/paladin-storage/src/waypoint/contract_tests.rs lines 650-700 (the `muster_progress_round_trips` doc block at line 673 and the `muster_progress_fixture` helper it links to)
    - crates/paladin-memory/src/token_counter/mod.rs (as fixed in Task 1 — the in-plan reference for the technique)
    - .planning/phases/34-documentation-currency-audit/34-AUDIT.md §6 rows RD-46, RD-51, RD-127, RD-128
  </read_first>
  <action>
Both remaining rows are the D-05 "private intra-doc link" kind, each a single location group with
one all-features follower closed by the same source-line fix (D-09).

`crates/paladin-ports/src/output/structured_executor_port.rs:158` — the public documentation for
`run_structured` links to the private item `repair_prompt` (RD-51, follower RD-127). Apply D-05:
replace the link with plain code font, or reword the sentence to name the public entry point that
reaches the repair behaviour. Do not widen `repair_prompt`'s visibility and do not add a
lint-suppression attribute.

`crates/paladin-storage/src/waypoint/contract_tests.rs:673` — the public documentation for
`muster_progress_round_trips` links to the private item `muster_progress_fixture` (RD-46,
follower RD-128). Same D-05 treatment.

If either fix turns out to need a different line than the audit cited, record the actual line
beside the cited one in the closure table — the cited line is the audit's contract, the actual
line is the fix's evidence (D-09).

Append both crates' post-fix per-crate sweep output to `36-evidence/36-01-percrate.txt`.

Commit one per crate per D-26: `docs(36): resolve rustdoc link in paladin-ports (RD-51, RD-127)`
and `docs(36): resolve rustdoc link in paladin-storage (RD-46, RD-128)`. Run `make api-surface`
and expect no change before each commit (D-28). Stage with explicit paths.
  </action>
  <verify>
    <automated>touch crates/paladin-ports/src/lib.rs crates/paladin-storage/src/lib.rs && cargo doc -p paladin-ports -p paladin-storage --no-deps 2>&1 | tee /tmp/36-01-ports-storage.txt && test "$(grep -c 'warning:' /tmp/36-01-ports-storage.txt)" = 0 && RUSTDOCFLAGS="-D warnings" cargo doc -p paladin-ports --all-features --no-deps && RUSTDOCFLAGS="-D warnings" cargo doc -p paladin-storage --all-features --no-deps && cargo check --workspace --all-targets --all-features && ./scripts/check-api-surface.sh .project/current-exports.txt</automated>
  </verify>
  <acceptance_criteria>
    - `RUSTDOCFLAGS="-D warnings" cargo doc -p paladin-ports --all-features --no-deps` exits 0.
    - `RUSTDOCFLAGS="-D warnings" cargo doc -p paladin-storage --all-features --no-deps` exits 0.
    - A forced re-documentation of both crates under default features produces a capture with zero lines matching `warning:`.
    - `grep -rn 'allow(rustdoc::private_intra_doc_links)' crates/paladin-ports/src crates/paladin-storage/src` produces no output.
    - `grep -rn 'pub fn repair_prompt' crates/paladin-ports/src` produces no output (visibility was not widened).
    - `git diff crates/paladin-ports/ crates/paladin-storage/` adds and removes only doc-comment lines.
    - `cargo check --workspace --all-targets --all-features` exits 0.
    - `./scripts/check-api-surface.sh .project/current-exports.txt` reports the surface unchanged.
  </acceptance_criteria>
  <done>paladin-ports and paladin-storage each document clean under both the default and the all-features build; RD-46, RD-51, RD-127 and RD-128 are closed and committed with their IDs in the commit subjects.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Symbols and files created by **this plan** (new to the repository — not pre-existing):

- `examples/token_economy_commissary.rs` — new example binary target `token_economy_commissary`
- `.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-EVIDENCE.md` — new file
- `.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/` — new directory with `36-01-baseline.txt`, `36-01-percrate.txt`, `36-01-example-run.txt`
- `examples/README.md` heading `## Token Economy Examples` and section `### [token_economy_commissary.rs](token_economy_commissary.rs)` — new anchors

Created elsewhere in Phase 36 (listed so drift verification does not flag them as unknown
symbols): example targets `war_engine_configuration`, `control_flow_dynamic_routing`,
`human_in_the_loop_gate`, `graceful_shutdown`, `agent_runtime_middleware`,
`sanctum_rag_retrieval`, `platform_api_client`, `webhook_receiver`, `node_result_cache`,
`observability_tracing`, `observability_otel_export`, `eval_scenarios_demo`; Makefile targets
`doc-check` and `check-examples`; the `.pre-commit-config.yaml` hook id `doc-check`; the CI step
name `Check documentation (all features, -D warnings)`; requirement IDs `CURR-11`…`CURR-15`;
`36-CI-EVIDENCE.md`.

No new public Rust API is created anywhere in Phase 36 — SC5 requires `make api-surface` to
report no change.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer shell → example process | `cargo run --example token_economy_commissary` executes in the developer's environment and may read process environment variables |
| repository → published rustdoc site | doc comments edited here are rendered publicly by `cargo doc`; prose that over-describes an internal item leaks design detail |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-01 | Information Disclosure | `examples/token_economy_commissary.rs` | medium | mitigate | The program is offline by construction (mock LLM adapter, no provider client). It reads no API key, prints no environment value, and its header comment states that no key and no external service is needed (D-29). Acceptance criterion greps the file for credential-shaped output. |
| T-36-02 | Information Disclosure | de-linked private mentions in `paladin-ports` / `paladin-storage` doc comments | low | mitigate | D-05 requires rewording toward the public entry point rather than describing internal mechanics, so a de-linked mention never carries more detail than the private item's own doc comment already did. |
| T-36-03 | Tampering | public API surface | high | mitigate | `make api-surface` runs before every commit touching `src/` or `crates/` and must report no change (D-28); widening `pub(crate)` to `pub` to satisfy a link is forbidden (D-05). |
| T-36-SC | Tampering | cargo package installs | low | accept | This phase installs no new external package — `36-RESEARCH.md`'s Package Legitimacy Audit records "not applicable", and every crate the new examples use (`schemars`, `axum`, `reqwest`, `paladin-eval`, `paladin-storage` features) is already a pinned workspace dependency. No `cargo add` runs. |
</threat_model>

<verification>
- `RUSTDOCFLAGS="-D warnings" cargo doc -p paladin-memory --all-features --no-deps`, the same for `paladin-ports` and `paladin-storage` — all exit 0.
- `cargo doc -p paladin-memory -p paladin-ports -p paladin-storage --no-deps` re-run after `touch`ing each crate root emits zero lines matching `warning:`.
- `cargo build --example token_economy_commissary` and `cargo run --example token_economy_commissary` both exit 0 with no provider key in the environment.
- `cargo test --workspace --doc` stays green (462 passed / 0 failed at baseline, D-00f).
- `cargo fmt --all -- --check`, `cargo check --workspace --all-targets --all-features` and `make api-surface` all green.
- `36-EVIDENCE.md` and the three `36-evidence/` captures exist and are committed.
</verification>

<success_criteria>
- RD-01, RD-46, RD-51, RD-66, RD-126, RD-127 and RD-128 are closed at their cited file:line, with the closure table in the SUMMARY listing cited and actual lines.
- EX-109, EX-111, EX-112, EX-113, EX-114 and EX-115 each map to `examples/token_economy_commissary.rs` and to the one README section whose **Demonstrates:** line names the capability.
- The bare-shorthand link technique is empirically validated, so plans 36-02…36-05 can apply it without re-deriving it.
- `make api-surface` reports no change.
</success_criteria>

<output>
Create `.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-01-SUMMARY.md` when done, carrying the D-24 closure table: `ID | file:line (cited) | file:line (actual, if different) | kind | fix | commit`, with RD followers on their lead's row.
</output>
