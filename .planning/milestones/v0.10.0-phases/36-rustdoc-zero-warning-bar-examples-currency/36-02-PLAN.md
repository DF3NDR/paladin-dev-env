---
phase: 36-rustdoc-zero-warning-bar-examples-currency
plan: 02
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - crates/paladin-battalion/src/engine/mod.rs
  - crates/paladin-battalion/src/engine/graph.rs
  - crates/paladin-battalion/src/engine/cache_key.rs
  - crates/paladin-battalion/src/engine/directive_parser.rs
  - crates/paladin-battalion/src/engine/input_mapping.rs
  - crates/paladin-battalion/src/commander.rs
  - crates/paladin-battalion/src/edge_evaluator.rs
  - crates/paladin-battalion/src/llm_decision.rs
  - crates/paladin-battalion/src/llm_failure.rs
  - .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-02-battalion.txt
autonomous: true
requirements: [CURR-11, CURR-12, CURR-15]
must_haves:
  truths:
    - "`RUSTDOCFLAGS=\"-D warnings\" cargo doc -p paladin-battalion --all-features --no-deps` exits 0."
    - "A forced re-documentation of `paladin-battalion` under default features emits zero rustdoc diagnostic lines, down from the 36 the audit and the re-baseline both measured."
    - "All 72 `paladin-battalion` rows in `34-AUDIT.md` §6 — the battalion IDs in RD-10 through RD-45 plus RD-67 through RD-102 — are closed across the 34 location groups they name."
    - "`cargo check --workspace --all-targets --all-features` exits 0 and `cargo test --workspace --doc` stays green after the doc-comment rewrites."
    - "`make api-surface` reports the surface unchanged."
  artifacts:
    - "crates/paladin-battalion/src/engine/mod.rs — resolvable submodule list and item links"
    - "crates/paladin-battalion/src/engine/graph.rs — de-linked private mentions"
    - "crates/paladin-battalion/src/engine/cache_key.rs — resolvable prefix-constant mentions"
    - "crates/paladin-battalion/src/engine/directive_parser.rs — de-linked private mention"
    - "crates/paladin-battalion/src/engine/input_mapping.rs — collapsed redundant explicit links"
    - "crates/paladin-battalion/src/commander.rs — de-linked private mentions"
    - "crates/paladin-battalion/src/edge_evaluator.rs, llm_decision.rs and llm_failure.rs — resolvable cross-crate links"
    - ".planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-02-battalion.txt"
  key_links:
    - "The engine module header's Submodules list links must resolve to the sibling public modules declared later in the same file — the Pitfall 1 family validated in plan 36-01."
    - "The llm_failure and edge_evaluator files mention items that live in paladin-core, so their links must use that crate's path rather than a bare shorthand."
    - "Every fix must hold under both the default and the all-features build (D-07), because each default-feature row carries an all-features follower closed by the same source line."
  prohibitions:
    - statement: "No private item is widened from pub(crate) to pub to satisfy a documentation link (D-05)."
      status: recalled
      verification: flagged-unverified
    - statement: "No rustdoc lint-suppression attribute is added to silence a private intra-doc link (D-05)."
      status: recalled
      verification: flagged-unverified
    - statement: "Unclosed HTML tags are fixed by wrapping the generic in backticks, never by escaping with an HTML entity (D-08)."
      status: recalled
      verification: flagged-unverified
    - statement: "No new rustdoc prose is written beyond what a link fix requires; there are zero missing-docs rows in this phase (D-04)."
      status: recalled
      verification: flagged-unverified
---

<objective>
Close every `paladin-battalion` rustdoc row — 72 of the 143, in 34 location groups, the largest
single-crate block in `34-AUDIT.md` §6.

This plan implements D-00b (the §6 work list, IDs never renumbered), D-00c (a doc link that points
at something the tree does not expose is corrected toward the tree), D-04 (all rows are link and
HTML defects, all sized S, no new prose), D-05 (private intra-doc link technique), D-06 (unresolved
link technique, including the module-disambiguator and explicit-path forms `36-RESEARCH.md`
Pattern 3 establishes), D-08 (redundant explicit links collapse; unclosed HTML tags get backticks),
D-09 (lead-row discipline — fix once at the lead row's file:line and close every ID in its Blocks
cell), D-10 (per-crate verification as fixes land), D-24 (closure table), D-26 (one commit per
crate) and D-28 (`make api-surface` before the commit).

Purpose: `paladin-battalion` contributes 36 of the 65 default-feature diagnostics and 36 of the 77
all-features errors. Nothing else in the phase can reach a workspace zero until it is green.

Scheduling note: `depends_on` lists 36-01 as a genuine prerequisite — its tracer task validates the
link technique this plan applies 34 times. Elsewhere in this phase some `depends_on` entries are
worktree-serialization edges rather than code dependencies: each isolated executor worktree builds
a cold `target/` at roughly 16 GB and the volume fits at most two at once, so no wave may hold more
than two build-heavy plans.

Spec-less probe fallback skipped (visible, recorded): Phase 36 has no SPEC and its requirement IDs
were minted by this planning run, so `must_haves` are derived from the five ROADMAP Phase 36
Success Criteria and the CONTEXT decisions.

Output: `paladin-battalion` documenting clean under both bar commands, in one commit whose subject
cites the RD range.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-CONTEXT.md
@.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-RESEARCH.md
@.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-PATTERNS.md
@.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The engine module header — the Submodules list and the module's own item links (16 groups, 34 rows)</name>
  <files>crates/paladin-battalion/src/engine/mod.rs</files>
  <read_first>
    - crates/paladin-battalion/src/engine/mod.rs lines 1-90 (the whole module-level doc header, the public module declarations it lists, and the working path-syntax links at lines 59-61 that prove which form resolves)
    - crates/paladin-battalion/src/engine/mod.rs lines 730-760, 1070-1100, 1410-1440, 2670-2700 (the four later doc blocks carrying RD-36/93, RD-45/102, RD-37/94 and RD-38/95)
    - crates/paladin-memory/src/token_counter/mod.rs (plan 36-01's committed fix — the validated form of the same technique)
    - crates/paladin-core/src/platform/container/waypoint.rs lines 1-60 and 380-400 (confirms where `Waypoint` and `GRAPH_FINGERPRINT_VERSION` actually live, for the cross-crate link path)
    - crates/paladin-ports/src/output (locate `WaypointPort`'s real module path before linking to it)
    - .planning/phases/34-documentation-currency-audit/34-AUDIT.md §3 "Default-feature enumeration" rows RD-13 through RD-25, RD-36, RD-37, RD-38 and RD-45 (each carries the verbatim first line of its own diagnostic)
  </read_first>
  <action>
Fix all sixteen location groups in the engine's module root file. Each cited line below is the lead
row's line from `34-AUDIT.md` §6; its all-features follower closes with the same edit (D-09). If a
fix lands on a different line than the audit cited, record both in the closure table.

Module-header doc block, unresolved links (D-06): line 3 names `WarGraph`; line 4 names
`StateNode`; line 5 names `Battlefield`; line 6 names both `Waypoint` and `WaypointPort` (two IDs,
one line); line 10 names `WarEngine::start`. Lines 20, 25, 27, 31, 33, 34 and 36 are the
"Submodules:" list and name the sibling modules `bridges`, `graph`, `directive_parser`,
`input_mapping`, `node`, `dispatch_registry` and `hooks` by bare shorthand. Replace every bare
shorthand with a form that resolves: for sibling modules use the module disambiguator or a
self-qualified path; for types declared in a submodule of this crate use the path form the file's
own lines 59-61 already prove works; for `Waypoint` and `WaypointPort`, which are not this crate's
items, use the owning crate's path. Where a mention names an item the tree does not expose at all,
correct the mention to the shipped name per D-00c, using `MIGRATION.md` §9.2 as the rename
register — never invent a target.

Line 744: the public documentation for `ResponseShapeInvalid` links to the private item
`graph::validate_parley_value_for_kind`. Apply D-05 — plain code font, or reword to name the public
entry point that reaches the validation.

Line 1085: a redundant explicit link target. Apply D-08 — collapse the explicit two-part form to
the shorthand form.

Line 1423: an unresolved link to `Waypoint`. Apply D-06 with the owning crate's path.

Line 2686: the public documentation for `replay` links to the private item
`superstep::run_with_namespace`. Apply D-05.

Do not widen any visibility, do not add a lint-suppression attribute, and do not rewrite prose
beyond what each link change requires (D-04, D-05). Do not commit yet — D-26 requires a single
`paladin-battalion` commit, which Task 3 makes.
  </action>
  <verify>
    <automated>touch crates/paladin-battalion/src/lib.rs &amp;&amp; cargo doc -p paladin-battalion --no-deps 2>&amp;1 | tee /tmp/36-02-t1.txt &amp;&amp; test "$(grep -c 'engine/mod' /tmp/36-02-t1.txt)" = 0 &amp;&amp; cargo check -p paladin-battalion --all-features</automated>
  </verify>
  <acceptance_criteria>
    - A forced re-documentation of `paladin-battalion` under default features produces a capture containing a `Documenting paladin-battalion` line and zero lines referencing the engine module root file.
    - `grep -c '^//!' crates/paladin-battalion/src/engine/mod.rs` is unchanged from its pre-edit value — no doc lines were deleted, only rewritten.
    - `grep -rn 'allow(rustdoc::private_intra_doc_links)\|allow(rustdoc::broken_intra_doc_links)' crates/paladin-battalion/src` produces no output.
    - `git diff crates/paladin-battalion/src/engine/mod.rs` adds and removes only lines beginning with a doc-comment marker.
    - `cargo check -p paladin-battalion --all-features` exits 0.
  </acceptance_criteria>
  <done>Every diagnostic against the engine module root is gone from the default-feature run; RD-13…RD-25, RD-36, RD-37, RD-38, RD-45 and their followers RD-70…RD-82, RD-93, RD-94, RD-95 and RD-102 are fixed in the working tree.</done>
</task>

<task type="auto">
  <name>Task 2: engine graph, cache_key, directive_parser and input_mapping (11 groups, 24 rows)</name>
  <files>crates/paladin-battalion/src/engine/graph.rs, crates/paladin-battalion/src/engine/cache_key.rs, crates/paladin-battalion/src/engine/directive_parser.rs, crates/paladin-battalion/src/engine/input_mapping.rs</files>
  <read_first>
    - crates/paladin-battalion/src/engine/graph.rs lines 760-790, 1360-1390 and 2240-2370 (the `validate`, `validate_node_cache_backend` and five `fingerprint` doc blocks, plus the `validate_schedulable`, `validate_aegis_undeclared_nodes` and `push_field` items they link to — confirm each target's real visibility)
    - crates/paladin-battalion/src/engine/cache_key.rs lines 1-40 (the doc block at line 23 and the `graph_prefix` and `node_prefix` items it names)
    - crates/paladin-battalion/src/engine/directive_parser.rs lines 30-60 (the module doc block at line 47)
    - crates/paladin-battalion/src/engine/input_mapping.rs lines 20-50 (the two redundant explicit link targets at lines 30 and 40)
    - crates/paladin-battalion/src/engine/mod.rs (as fixed in Task 1 — the in-plan reference for the resolvable link forms)
    - .planning/phases/34-documentation-currency-audit/34-AUDIT.md §3 rows RD-26 through RD-35, RD-43 and RD-44
  </read_first>
  <action>
The graph file carries seven groups, all the D-05 private intra-doc link kind: line 772 (`validate`
to `WarGraph::validate_schedulable`), line 1375 (`validate_node_cache_backend` to
`WarGraph::validate_aegis_undeclared_nodes`), and lines 2258, 2287, 2303, 2337 and 2358 (five
`fingerprint` doc blocks each linking to the private `push_field`). Replace each link with plain
code font, or reword to name the public entry point that reaches the private helper. The five
`push_field` mentions are five separate doc blocks on five separate lines — fix each at its own
cited line; they are not one group.

The cache-key file at line 23 carries unresolved links to `graph_prefix` and `node_prefix` (RD-26
and RD-27, plus followers RD-83 and RD-84: four IDs on one source line). Check each target's real
visibility first — if the item is public, resolve it with an explicit path (D-06); if it is
private, use plain code font (D-05).

The directive-parser file at line 47 has its module documentation linking to the private item
`crate::engine::graph::validate_parley_value_for_kind`. Apply D-05, matching the wording choice
Task 1 made for the same target at line 744 of the engine module root so the two mentions stay
consistent.

The input-mapping file at lines 30 and 40 carries redundant explicit link targets. Apply D-08 —
collapse the explicit two-part form to the shorthand form; do not delete the link.

Do not widen visibility and do not add a lint-suppression attribute (D-05). Do not commit yet —
Task 3 makes the single crate commit (D-26).
  </action>
  <verify>
    <automated>touch crates/paladin-battalion/src/lib.rs &amp;&amp; cargo doc -p paladin-battalion --no-deps 2>&amp;1 | tee /tmp/36-02-t2.txt &amp;&amp; test "$(grep -cE 'engine/(graph|cache_key|directive_parser|input_mapping)' /tmp/36-02-t2.txt)" = 0 &amp;&amp; cargo check -p paladin-battalion --all-features</automated>
  </verify>
  <acceptance_criteria>
    - A forced re-documentation of `paladin-battalion` under default features produces a capture with zero lines referencing any of the four files this task edits.
    - `grep -rn 'pub fn push_field\|pub fn validate_schedulable\|pub fn validate_aegis_undeclared_nodes\|pub fn validate_parley_value_for_kind' crates/paladin-battalion/src` produces no output — no visibility was widened.
    - `grep -rn 'allow(rustdoc::' crates/paladin-battalion/src` produces no output.
    - `git diff` for the four files adds and removes only lines beginning with a doc-comment marker.
    - `cargo check -p paladin-battalion --all-features` exits 0.
  </acceptance_criteria>
  <done>RD-26…RD-35, RD-43, RD-44 and their followers RD-83…RD-92, RD-100 and RD-101 are fixed in the working tree; no diagnostic remains against these four files.</done>
</task>

<task type="auto">
  <name>Task 3: commander, edge_evaluator, llm_decision and llm_failure (7 groups, 14 rows) and the single paladin-battalion commit</name>
  <files>crates/paladin-battalion/src/commander.rs, crates/paladin-battalion/src/edge_evaluator.rs, crates/paladin-battalion/src/llm_decision.rs, crates/paladin-battalion/src/llm_failure.rs, .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-02-battalion.txt</files>
  <read_first>
    - crates/paladin-battalion/src/commander.rs lines 25-60 (the `StrategySelection` and `Heuristic` doc blocks at lines 35 and 49, and the `Commander::analyze_and_select` method they link to — confirm its visibility)
    - crates/paladin-battalion/src/edge_evaluator.rs lines 1-20 (the module doc block at line 3 naming `EdgeCondition`)
    - crates/paladin-core/src/platform/container/battalion/campaign.rs lines 25-60 (confirms `EdgeCondition` is a public enum in paladin-core, giving the cross-crate link path)
    - crates/paladin-battalion/src/llm_decision.rs lines 30-50 (the doc block at line 40 naming `llm_error_class`)
    - crates/paladin-battalion/src/llm_failure.rs lines 1-50 (the doc blocks at lines 1, 8 and 38 naming `PaladinError::LlmFailure` and `PaladinError::is_retryable`)
    - MIGRATION.md §9.2 (the rename register — consult it if a mention names an item the tree no longer exposes under that name, per D-00c and D-06)
    - .planning/phases/34-documentation-currency-audit/34-AUDIT.md §3 rows RD-10, RD-11, RD-12, RD-39, RD-40, RD-41 and RD-42
  </read_first>
  <action>
`commander.rs:35` and `:49` — the public documentation for `StrategySelection` and for `Heuristic`
each links to the private item `Commander::analyze_and_select`. Apply D-05: plain code font, or
reword to name the public entry point a caller actually reaches the routing decision through. Use
the same wording for both so the two mentions stay consistent.

`edge_evaluator.rs:3` — unresolved link to `EdgeCondition`. The type is paladin-core's, so apply
D-06 with that crate's path; the crate is already a dependency.

`llm_decision.rs:40` — unresolved link to `llm_error_class`. Resolve to the item's real path if it
is public; if it is private, apply D-05 and use plain code font.

`llm_failure.rs:1`, `:8` and `:38` — unresolved links to `PaladinError::LlmFailure` (twice) and
`PaladinError::is_retryable`. `PaladinError` is not this crate's type, so apply D-06 with the
owning crate's path. If either associated item does not exist under that name in the shipped tree,
correct the mention to the shipped name per D-00c, using `MIGRATION.md` §9.2 as the rename source —
never invent a target.

Then close the crate (D-10, D-26, D-28):
  - Run the per-crate sweep in both forms and capture verbatim into
    `36-evidence/36-02-battalion.txt`: a forced re-documentation under default features with the
    diagnostic count, and `RUSTDOCFLAGS="-D warnings" cargo doc -p paladin-battalion
    --all-features --no-deps` with its exit code.
  - Run `cargo fmt --all -- --check`, `cargo check --workspace --all-targets --all-features`,
    `cargo test --workspace --doc` and `make api-surface` (expect no change).
  - Make the single `paladin-battalion` commit per D-26 with the ID range in the subject:
    `docs(36): resolve rustdoc links in paladin-battalion (RD-10…RD-45, RD-67…RD-102)`. Stage with
    `git add -- crates/paladin-battalion/ .planning/phases/36-*/36-evidence/36-02-battalion.txt`,
    never `git add .`. Allow a long timeout — the pre-commit hook runs workspace clippy whenever a
    Rust file is staged.
  </action>
  <verify>
    <automated>touch crates/paladin-battalion/src/lib.rs &amp;&amp; cargo doc -p paladin-battalion --no-deps 2>&amp;1 | tee /tmp/36-02-battalion-default.txt &amp;&amp; test "$(grep -c 'warning:' /tmp/36-02-battalion-default.txt)" = 0 &amp;&amp; RUSTDOCFLAGS="-D warnings" cargo doc -p paladin-battalion --all-features --no-deps &amp;&amp; cargo fmt --all -- --check &amp;&amp; cargo check --workspace --all-targets --all-features &amp;&amp; cargo test --workspace --doc &amp;&amp; ./scripts/check-api-surface.sh .project/current-exports.txt</automated>
  </verify>
  <acceptance_criteria>
    - `RUSTDOCFLAGS="-D warnings" cargo doc -p paladin-battalion --all-features --no-deps` exits 0.
    - A forced re-documentation of `paladin-battalion` under default features produces a capture containing a `Documenting paladin-battalion` line and zero lines matching `warning:`.
    - `grep -rn 'allow(rustdoc::' crates/paladin-battalion/src` produces no output.
    - `git diff --stat HEAD~1 -- crates/` lists only paths under `crates/paladin-battalion/` — no other crate is in the commit.
    - `git log -1 --pretty=%s` starts with `docs(36): resolve rustdoc links in paladin-battalion` and contains `RD-`.
    - `cargo test --workspace --doc` reports 0 failed.
    - `./scripts/check-api-surface.sh .project/current-exports.txt` reports the surface unchanged.
    - `36-evidence/36-02-battalion.txt` contains both the default-feature diagnostic count and the all-features exit code.
  </acceptance_criteria>
  <done>All 72 `paladin-battalion` rows are closed, the crate documents clean under both bar commands, and the work is in one commit whose subject cites the RD range.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

This plan creates no new Rust symbols, no new files under `src/` or `crates/`, and no new public
API — every change is a rewritten doc-comment line inside an existing `paladin-battalion` file.
SC5 requires `make api-surface` to report no change.

New file created by this plan:
- `.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-02-battalion.txt`

New symbols created elsewhere in Phase 36 (listed so drift verification does not flag them):
example targets `token_economy_commissary`, `war_engine_configuration`,
`control_flow_dynamic_routing`, `human_in_the_loop_gate`, `graceful_shutdown`,
`agent_runtime_middleware`, `structured_output_schema`, `sanctum_rag_retrieval`,
`platform_api_client`, `webhook_receiver`, `node_result_cache`, `observability_tracing`,
`observability_otel_export`, `eval_scenarios_demo`; Makefile targets `doc-check` and
`check-examples`; the `.pre-commit-config.yaml` hook id `doc-check`; the CI step name
`Check documentation (all features, -D warnings)`; requirement IDs `CURR-11`…`CURR-15`;
`36-EVIDENCE.md` and `36-CI-EVIDENCE.md`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository → published rustdoc site | doc comments edited here render publicly; prose that over-describes an internal item leaks design detail |
| doc-comment edit → public API baseline | a link "fixed" by widening visibility silently moves the published surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-04 | Tampering | `paladin-battalion` public API surface | high | mitigate | `make api-surface` runs before the single crate commit and must report no change (D-28); widening `pub(crate)` to `pub` is forbidden by D-05, and the acceptance criteria grep that the specific private helpers — `push_field`, `validate_schedulable`, `validate_aegis_undeclared_nodes`, `validate_parley_value_for_kind` and `analyze_and_select` — never gain a public declaration. |
| T-36-05 | Information Disclosure | de-linked private mentions in the graph, commander and cache-key files | low | mitigate | D-05 requires rewording toward the public entry point rather than describing internal mechanics — a de-linked mention of the graph fingerprint helper or the cache-key prefix constants must not describe the internal algorithm in more detail than the private item's own doc comment already did. |
| T-36-06 | Repudiation | closure bookkeeping | medium | mitigate | The commit subject cites the RD range and the SUMMARY carries the D-24 closure table with cited and actual file:line, so every one of the 72 IDs is traceable to a commit (D-09, D-24, D-26). |
| T-36-SC | Tampering | cargo package installs | low | accept | No package-manager install runs in this plan; `36-RESEARCH.md`'s Package Legitimacy Audit records "not applicable — this phase installs no new external package". |
</threat_model>

<verification>
- `RUSTDOCFLAGS="-D warnings" cargo doc -p paladin-battalion --all-features --no-deps` exits 0.
- A forced default-feature re-documentation of `paladin-battalion` emits zero lines matching `warning:`, down from 36.
- `cargo check --workspace --all-targets --all-features` exits 0.
- `cargo test --workspace --doc` reports 0 failed — a doc rewrite must not break an existing doctest (D-23).
- `cargo fmt --all -- --check` and `make api-surface` are green.
- Exactly one commit touches `crates/paladin-battalion/` and it touches no other crate (D-26).
</verification>

<success_criteria>
- All 34 `paladin-battalion` location groups are fixed at their cited file:line, closing all 72 IDs including every follower named in a lead row's Blocks cell.
- `paladin-battalion`'s contribution to both bar commands is zero.
- `make api-surface` reports no change.
</success_criteria>

<output>
Create `.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-02-SUMMARY.md` when done, carrying the D-24 closure table: `ID | file:line (cited) | file:line (actual, if different) | kind | fix | commit`, with every follower listed on its lead's row.
</output>
