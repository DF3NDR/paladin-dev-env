---
phase: 36-rustdoc-zero-warning-bar-examples-currency
plan: 05
type: execute
wave: 3
depends_on: ["36-01", "36-03"]
files_modified:
  - src/application/services/paladin/paladin_execution_service.rs
  - src/application/services/parley/adapter.rs
  - src/application/services/run/worker.rs
  - src/config/agent_runtime.rs
  - src/presets/mod.rs
  - src/application/cli/commands/eval.rs
  - src/infrastructure/telemetry/otel_sink.rs
  - .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-05-facade.txt
autonomous: true
requirements: [CURR-11, CURR-12, CURR-15]
must_haves:
  truths:
    - "`RUSTDOCFLAGS=\"-D warnings\" cargo doc -p paladin-ai --all-features --no-deps` exits 0."
    - "A forced re-documentation of `paladin-ai` under default features emits zero rustdoc diagnostic lines, down from the 5 the audit and the re-baseline both measured."
    - "All 12 facade rows in `34-AUDIT.md` §6 — RD-02, RD-03, RD-04, RD-05, RD-06, RD-137, RD-138, RD-139, RD-140, RD-141, RD-142 and RD-143 — are closed across the 7 location groups they name."
    - "The two rows that reproduce only under all features — RD-137 behind the cli gate and RD-142 behind the otel gate — are fixed in place, with the doc'd item and its target under the same gate."
    - "`cargo check --workspace --all-targets --all-features` exits 0 and `cargo test --workspace --doc` stays green."
    - "`make api-surface` reports the surface unchanged."
  artifacts:
    - "src/application/services/paladin/paladin_execution_service.rs — de-linked private mention"
    - "src/application/services/parley/adapter.rs — de-linked private mention"
    - "src/application/services/run/worker.rs — de-linked private mention"
    - "src/config/agent_runtime.rs — de-linked private constant mention with the precedence claim kept true"
    - "src/presets/mod.rs — de-linked private constant mention"
    - "src/application/cli/commands/eval.rs — de-linked private mention under the cli gate"
    - "src/infrastructure/telemetry/otel_sink.rs — de-linked private mention under the otel gate"
    - ".planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-05-facade.txt"
  key_links:
    - "All seven groups are the D-05 private intra-doc link kind — every target is an internal helper or constant of the facade crate, so none is resolvable by any link form; the only correct fix is to de-link or reword toward the public entry point."
    - "The facade package is paladin-ai; cargo doc with a paladin-core package selector is not a valid invocation anywhere in this phase."
    - "The agent-runtime config's chain-resolution doc block describes provider-name precedence; the de-linked constant mention must keep the precedence statement true against the shipped list."
  prohibitions:
    - statement: "No private item is widened from pub(crate) to pub to satisfy a documentation link (D-05)."
      status: recalled
      verification: flagged-unverified
    - statement: "No rustdoc lint-suppression attribute is added to silence a private intra-doc link (D-05)."
      status: recalled
      verification: flagged-unverified
    - statement: "No docsrs conditional-attribute machinery is introduced — a docs.rs metadata build is out of scope for this phase (D-07)."
      status: recalled
      verification: flagged-unverified
    - statement: "No new rustdoc prose is written beyond what a link fix requires (D-04)."
      status: recalled
      verification: flagged-unverified
---

<objective>
Close every `paladin-ai` facade rustdoc row — 12 of the 143, in 7 location groups, all of them the
private intra-doc link kind.

This plan implements D-00b (the §6 work list and stable IDs), D-04 (all rows are link defects,
sized S, no new prose), D-05 (private intra-doc link technique — plain code font or a reword naming
the public entry point, never a visibility widening and never a lint-suppression attribute), D-06
(unresolved link technique, should a target turn out to be public after all), D-07 (feature-gated
targets: RD-137 sits behind the cli gate and RD-142 behind otel, and in both cases the doc'd item
is gated by the same feature, so the fix holds under both builds with no docsrs
conditional-attribute machinery), D-09 (lead-row discipline), D-10 (per-crate verification), D-24
(closure table), D-26 (one commit per crate) and D-28 (`make api-surface` before the commit).

Purpose: the facade is the crate a library consumer reads first, and it is the last of the eight
crates in the defect list. With this plan and 36-02, 36-03 and 36-04 landed, every RD row in
`34-AUDIT.md` §6 is closed and the workspace zero becomes measurable in the closing wave.

Scheduling note: the dependency on 36-03 is a worktree-serialization edge, not a code dependency —
each isolated executor worktree builds a cold `target/` at roughly 16 GB and the volume fits at
most two at once, so no wave may hold more than two build-heavy plans. The dependency on 36-01 is a
genuine prerequisite: its tracer task validates the fix technique.

Spec-less probe fallback skipped (visible, recorded): Phase 36 has no SPEC and its requirement IDs
were minted by this planning run, so `must_haves` are derived from the five ROADMAP Phase 36
Success Criteria and the CONTEXT decisions.

Output: `paladin-ai` documenting clean under both bar commands, in one commit whose subject cites
the RD range.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-CONTEXT.md
@.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-RESEARCH.md
@.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-PATTERNS.md
@.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The five default-feature facade groups (RD-02…RD-06 and followers RD-138…RD-141, RD-143)</name>
  <files>src/application/services/paladin/paladin_execution_service.rs, src/application/services/parley/adapter.rs, src/application/services/run/worker.rs, src/config/agent_runtime.rs, src/presets/mod.rs</files>
  <read_first>
    - src/application/services/paladin/paladin_execution_service.rs lines 995-1035 (the `execute_scoped` doc block at line 1014 and the private bounded-execution method it links to)
    - src/application/services/parley/adapter.rs lines 15-45 (the adapter module doc block at line 28 and the private shadow-validation item it links to)
    - src/application/services/run/worker.rs lines 625-660 (the `with_event_bus` doc block at line 641 and the private engine-failure recording method it links to)
    - src/config/agent_runtime.rs lines 1155-1195 (the chain-resolution doc block at line 1174 and the private known-provider-names constant it links to — read the constant's actual contents so the reworded prose stays true)
    - src/presets/mod.rs lines 40-75 (the reasoning-agent options doc block at line 55 and the private default-system-prompt constant it links to)
    - crates/paladin-llm/src/services/commissary.rs (as fixed in plan 36-04 — the in-plan reference for the de-link wording, if that plan has landed; otherwise use plan 36-01's ports fix)
    - .planning/phases/34-documentation-currency-audit/34-AUDIT.md §3 rows RD-02, RD-03, RD-04, RD-05 and RD-06 (each carries the verbatim first line of its own diagnostic, naming the doc'd item and the private target)
  </read_first>
  <action>
All five groups are the D-05 private intra-doc link kind: a public item's documentation links to an
internal helper or constant. For each, replace the link with plain code font, or reword the sentence
to name the public entry point a caller actually reaches the behaviour through. Never widen the
target's visibility and never add a lint-suppression attribute. Each group's all-features follower
closes with the same source-line fix (D-09).

  - `src/application/services/paladin/paladin_execution_service.rs:1014` — `execute_scoped`'s
    documentation links to the private `Self::execute_bounded` (RD-02, follower RD-138).
  - `src/application/services/parley/adapter.rs:28` — the adapter module's documentation links to
    the private `shadow_validate` (RD-03, follower RD-139).
  - `src/application/services/run/worker.rs:641` — `with_event_bus`'s documentation links to the
    private `Self::record_engine_failure` (RD-04, follower RD-140).
  - `src/config/agent_runtime.rs:1174` — `resolve_chain`'s documentation links to the private
    `KNOWN_PROVIDER_NAMES` (RD-05, follower RD-141). The block describes provider-name precedence;
    after de-linking, the prose must still state a precedence that matches the constant's actual
    contents, which is why the constant is in this task's read list.
  - `src/presets/mod.rs:55` — `ReasoningAgentOptions`'s documentation links to the private
    `DEFAULT_SYSTEM_PROMPT` (RD-06, follower RD-143).

If any of these targets turns out to be public rather than private, apply D-06 instead and resolve
it to its real path. Do not rewrite prose beyond what each link change requires (D-04). Do not
commit yet — D-26 requires a single `paladin-ai` commit, which Task 2 makes.
  </action>
  <verify>
    <automated>touch src/lib.rs &amp;&amp; cargo doc -p paladin-ai --no-deps 2>&amp;1 | tee /tmp/36-05-t1.txt &amp;&amp; test "$(grep -c 'warning:' /tmp/36-05-t1.txt)" = 0 &amp;&amp; cargo check --workspace --all-targets --all-features</automated>
  </verify>
  <acceptance_criteria>
    - A forced re-documentation of `paladin-ai` under default features produces a capture containing a `Documenting paladin-ai` line and zero lines matching `warning:`.
    - `grep -rn 'allow(rustdoc::' src` produces no output.
    - `grep -rn 'pub const KNOWN_PROVIDER_NAMES\|pub const DEFAULT_SYSTEM_PROMPT\|pub fn shadow_validate' src` produces no output.
    - `grep -rn 'pub async fn execute_bounded\|pub fn execute_bounded\|pub async fn record_engine_failure\|pub fn record_engine_failure' src` produces no output.
    - `git diff src/` adds and removes only lines beginning with a doc-comment marker.
    - `cargo check --workspace --all-targets --all-features` exits 0.
  </acceptance_criteria>
  <done>The five default-feature facade groups are fixed in the working tree and `paladin-ai` emits no default-feature rustdoc diagnostic.</done>
</task>

<task type="auto">
  <name>Task 2: The two feature-gated facade groups (RD-137 under cli, RD-142 under otel) and the single paladin-ai commit</name>
  <files>src/application/cli/commands/eval.rs, src/infrastructure/telemetry/otel_sink.rs, .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-05-facade.txt</files>
  <read_first>
    - src/application/cli/commands/eval.rs lines 260-300 (the `first_divergence` doc block at line 281 and the private stabilized-fingerprints item it links to)
    - src/infrastructure/telemetry/otel_sink.rs lines 25-60 (the module doc block at line 42 and the private HTTP-client builder it links to)
    - Cargo.toml lines 487-560 (the features block — confirms cli and otel are the gates that make these two rows all-features-only)
    - src/presets/mod.rs (as fixed in Task 1 — the in-plan reference for the de-link wording)
    - .planning/phases/34-documentation-currency-audit/34-AUDIT.md per-crate sweep rows RD-137 and RD-142
  </read_first>
  <action>
`src/application/cli/commands/eval.rs:281` — the public documentation for `first_divergence` links
to the private item `stabilized_fingerprints` (RD-137). This row reproduces only under all features
because the module sits behind the cli feature. Apply D-05: plain code font, or reword toward the
public eval-divergence behaviour. Because the doc'd item is itself gated by cli, D-07 is satisfied
in place — no gate annotation is added.

`src/infrastructure/telemetry/otel_sink.rs:42` — the public documentation for the sink module links
to the private helper `build_reqwest_client` (RD-142), behind the otel gate. Apply D-05. The helper
builds an HTTP client that may carry credentials to a collector endpoint; the de-linked mention
must not describe its header or redirect handling in more detail than the private helper's own doc
comment already does.

Then close the crate (D-10, D-26, D-28):
  - Capture verbatim into `36-evidence/36-05-facade.txt`: a forced re-documentation of `paladin-ai`
    under default features with its diagnostic count, and
    `RUSTDOCFLAGS="-D warnings" cargo doc -p paladin-ai --all-features --no-deps` with its exit
    code. Also record, as an informational line and not as a gate, the result of running
    `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --all-features --no-deps` from this worktree
    — the workspace-wide figure is only authoritative once every wave has merged, which is plan
    36-12's job.
  - Run `cargo fmt --all -- --check`, `cargo check --workspace --all-targets --all-features`,
    `cargo test --workspace --doc` and `make api-surface` (expect no change).
  - Commit per D-26:
    `docs(36): resolve rustdoc links in paladin-ai (RD-02…RD-06, RD-137…RD-143)`. Stage with
    `git add -- src/ .planning/phases/36-*/36-evidence/36-05-facade.txt`, never `git add .`. Allow
    a long timeout for the clippy pre-commit hook.
  </action>
  <verify>
    <automated>touch src/lib.rs &amp;&amp; cargo doc -p paladin-ai --no-deps 2>&amp;1 | tee /tmp/36-05-facade-default.txt &amp;&amp; test "$(grep -c 'warning:' /tmp/36-05-facade-default.txt)" = 0 &amp;&amp; RUSTDOCFLAGS="-D warnings" cargo doc -p paladin-ai --all-features --no-deps &amp;&amp; cargo fmt --all -- --check &amp;&amp; cargo check --workspace --all-targets --all-features &amp;&amp; cargo test --workspace --doc &amp;&amp; ./scripts/check-api-surface.sh .project/current-exports.txt</automated>
  </verify>
  <acceptance_criteria>
    - `RUSTDOCFLAGS="-D warnings" cargo doc -p paladin-ai --all-features --no-deps` exits 0.
    - A forced re-documentation of `paladin-ai` under default features produces a capture containing a `Documenting paladin-ai` line and zero lines matching `warning:`.
    - `grep -rn 'allow(rustdoc::' src` produces no output.
    - `grep -rn 'cfg_attr(docsrs' src` produces no output.
    - `grep -rn 'pub fn stabilized_fingerprints\|pub fn build_reqwest_client' src` produces no output.
    - `git diff --stat HEAD~1 -- crates/` is empty — the commit touches the facade only.
    - `git log -1 --pretty=%s` starts with `docs(36): resolve rustdoc links in paladin-ai` and contains `RD-`.
    - `cargo test --workspace --doc` reports 0 failed.
    - `./scripts/check-api-surface.sh .project/current-exports.txt` reports the surface unchanged.
    - `36-evidence/36-05-facade.txt` records both per-crate results and the informational workspace-wide run.
  </acceptance_criteria>
  <done>All 12 facade rows are closed, `paladin-ai` documents clean under both bar commands, and the work is in one commit citing the RD range.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

This plan creates no new Rust symbols and no new public API — every change is a rewritten
doc-comment line inside an existing `src/` file. SC5 requires `make api-surface` to report no
change.

New file created by this plan:
- `.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-05-facade.txt`

New symbols created elsewhere in Phase 36 (listed so drift verification does not flag them):
example targets `token_economy_commissary`, `war_engine_configuration`,
`control_flow_dynamic_routing`, `human_in_the_loop_gate`, `graceful_shutdown`,
`agent_runtime_middleware`, `structured_output_schema`, `sanctum_rag_retrieval`,
`platform_api_client`, `webhook_receiver`, `node_result_cache`, `observability_tracing`,
`observability_otel_export`, `eval_scenarios_demo`; Makefile targets `doc-check` and
`check-examples`; the `.pre-commit-config.yaml` hook id `doc-check`; the CI step name
`Check documentation (all features, -D warnings)`; requirement IDs `CURR-11`…`CURR-15`;
`36-EVIDENCE.md` and `36-CI-EVIDENCE.md`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository → published rustdoc site | the facade's doc comments are what a library consumer reads first and render publicly |
| doc-comment edit → public API baseline | a link "fixed" by widening visibility silently moves the published surface |
| telemetry sink → collector endpoint | the sink's private client builder carries credentials outward; its documentation must not over-describe that path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-14 | Tampering | `paladin-ai` public API surface | high | mitigate | `make api-surface` runs before the commit and must report no change (D-28). Acceptance criteria grep that the seven named private items never gain a public declaration. |
| T-36-15 | Information Disclosure | the telemetry sink's de-linked client-builder mention | medium | mitigate | The private helper builds an HTTP client that may carry a credential header to a collector endpoint; D-05's "reword toward the public entry point" keeps the prose on the public sink contract rather than the client's header or redirect handling. |
| T-36-16 | Spoofing | the agent-runtime provider-precedence prose | medium | mitigate | De-linking the known-provider-names constant must not leave a precedence claim that disagrees with the shipped constant — the constant is in the task's read list precisely so the reworded sentence stays true (D-00c: the shipped tree outranks the document). |
| T-36-SC | Tampering | cargo package installs | low | accept | No package-manager install runs in this plan; `36-RESEARCH.md`'s Package Legitimacy Audit records "not applicable — this phase installs no new external package". |
</threat_model>

<verification>
- `RUSTDOCFLAGS="-D warnings" cargo doc -p paladin-ai --all-features --no-deps` exits 0.
- A forced default-feature re-documentation of `paladin-ai` emits zero lines matching `warning:`, down from 5.
- `cargo check --workspace --all-targets --all-features` exits 0 and `cargo test --workspace --doc` reports 0 failed.
- `cargo fmt --all -- --check` and `make api-surface` are green.
- Exactly one commit, touching `src/` only (D-26).
</verification>

<success_criteria>
- All 7 facade location groups are fixed at their cited file:line, closing RD-02…RD-06 and RD-137…RD-143.
- With plans 36-02, 36-03 and 36-04 merged, every one of the 143 `RD-nn` rows in `34-AUDIT.md` §6 is closed.
- `make api-surface` reports no change.
</success_criteria>

<output>
Create `.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-05-SUMMARY.md` when done, carrying the D-24 closure table: `ID | file:line (cited) | file:line (actual, if different) | kind | fix | commit`, with every follower listed on its lead's row and RD-137 and RD-142 marked as all-features-only.
</output>
