---
phase: 36-rustdoc-zero-warning-bar-examples-currency
plan: 09
type: execute
wave: 5
depends_on: ["36-01", "36-07"]
files_modified:
  - examples/http_service_host.rs
  - crates/doc-examples/src/http_service_host.rs
  - examples/platform_api_client.rs
  - examples/webhook_receiver.rs
  - Cargo.toml
  - .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-09-platform.txt
autonomous: true
requirements: [CURR-13, CURR-14, CURR-15]
must_haves:
  truths:
    - "`examples/http_service_host.rs` and `crates/doc-examples/src/http_service_host.rs` each assemble the app by merging the agent router, the thread router and the run router in the same order the shipped server binary does, so the same-routes-as-the-server claim is true again (EX-33, EX-55)."
    - "`./scripts/check-doc-examples.sh` exits 0 and `mdbook build docs/` succeeds after the doc-examples module edit, because the HTTP-service-host deployment page includes that module's anchor."
    - "`cargo run --example platform_api_client --features \"web-server,dev-ui\"` exits 0 offline and its stdout prints each route it called and the status it got, in the order submit run, stream, cancel, assistants, schedules, thread state, thread resume, thread history, dev-ui inspector, plus the token usage the execute response carried."
    - "`cargo run --example webhook_receiver --features \"web-server\"` exits 0 offline and its stdout shows a signature verified over the exact raw request bytes it captured, a tampered body rejected, and the private-address override named."
    - "Both new gated programs have an example declaration in the root manifest naming their required features, so the explicit build succeeds and the bulk selector does not attempt them under default features."
    - "`cargo check --workspace --all-targets --all-features` exits 0 and `make api-surface` reports the surface unchanged."
  artifacts:
    - "examples/http_service_host.rs — router parity restored"
    - "crates/doc-examples/src/http_service_host.rs — router parity restored, page include re-rendered"
    - "examples/platform_api_client.rs — new gated example binary"
    - "examples/webhook_receiver.rs — new gated example binary"
    - "Cargo.toml — two new example declarations with required-features"
    - ".planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-09-platform.txt"
  key_links:
    - "The shipped server binary merges the agent, thread and run routers and only then merges the OpenAPI docs router; both HTTP-service-host files currently stop at the agent router plus docs, which is exactly the EX-33 and EX-55 defect."
    - "docs/src/deployment-topologies/http-service-host.md line 151 includes the doc-examples module's anchor, so editing that module re-renders the page and both the doc-examples check and the book build must be re-run."
    - "The webhook signature header carries a hex digest computed over the exact byte buffer stored on the delivery row and sent verbatim; a receiver must recompute over the raw bytes it captured itself, never over a re-serialised body."
    - "The dev-ui feature resolves to the web crate's own dev-ui feature and does not itself enable the web dependency, so the dev-ui route demonstration needs both web-server and dev-ui in its required-features list."
  prohibitions:
    - statement: "The webhook receiver never logs or prints the shared signing secret, and never prints a signature-verification input that could reveal it (D-29, security.instructions.md)."
      status: recalled
      verification: flagged-unverified
    - statement: "No example prints or hard-codes an API key; a program that reads a key from the environment says so in its header comment (D-29)."
      status: recalled
      verification: flagged-unverified
    - statement: "No example body uses unwrap or expect — errors propagate with the question-mark operator through a boxed-error main (CLAUDE.md, house example pattern)."
      status: recalled
      verification: flagged-unverified
    - statement: "No example is deleted; the two HTTP-service-host files are updated to the shipped router assembly rather than replaced (D-18)."
      status: recalled
      verification: flagged-unverified
    - statement: "No library source under src/ or under any paladin crate changes — the only crates/ edit is the doc-examples module, which D-00e assigns to this phase (ROADMAP Phase 36 boundary)."
      status: recalled
      verification: flagged-unverified
---

<objective>
Restore the two HTTP-service-host files' router-parity claim and demonstrate the whole Phase 24 and
Phase 27 Platform API surface: EX-33, EX-55, EX-77, EX-78, EX-79, EX-91, EX-92, EX-93, EX-94,
EX-95, EX-96, EX-97, EX-98, EX-99, EX-104 and EX-110.

This plan implements D-00d (the doc-examples crate is out of the missing-docs bar but any rustdoc
link warning it emits still counts, so the module edit must not introduce one), D-00e (edits to
existing doc-examples module anchors and to the shared support module are Phase 36's, not Phase
35's), D-15 (each capability maps to exactly one named program), D-16 (offline-first — the axum app
runs in-process and is driven by an HTTP client in the same process, so no external server is
needed), D-17 (feature gating: a program is declared as an example target with required-features
only when the capability *is* the feature, and each gated target needs its own CI and script
invocation — those lines are added by plan 36-12, which owns those files), D-18 (no example is
deleted), D-19 (the EX-33 and EX-55 router-parity fix and the re-run of the doc-examples check and
the book build), D-24 (closure table), D-26 (one commit per program) and D-28 (`make api-surface`
before every commit), plus D-29 (the webhook receiver's credential rules).

Purpose: the Platform API is the largest shipped surface with no runnable client anywhere in the
gallery, and the two HTTP-service-host files actively claim server parity they no longer have — a
reader copying them gets an app missing the thread and run routes entirely.

Scheduling note: the dependency on 36-07 is a worktree-serialization edge, not a code dependency —
each isolated executor worktree builds a cold `target/` at roughly 16 GB and the volume fits at most
two at once, so no wave may hold more than two build-heavy plans.

Spec-less probe fallback skipped (visible, recorded): Phase 36 has no SPEC and its requirement IDs
were minted by this planning run, so `must_haves` are derived from the five ROADMAP Phase 36
Success Criteria and the CONTEXT decisions.

Output: two corrected files, two new gated example binaries, two manifest declarations. Their
`examples/README.md` sections are written by plan 36-11; their CI and script invocations by plan
36-12.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-CONTEXT.md
@.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-RESEARCH.md
@.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-PATTERNS.md
@.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Router parity for both HTTP-service-host files — EX-33, EX-55</name>
  <files>examples/http_service_host.rs, crates/doc-examples/src/http_service_host.rs, .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-09-platform.txt</files>
  <read_first>
    - src/bin/paladin-server.rs lines 200-260 (the shipped router assembly — the agent router merged with the thread router and the run router, and only then the OpenAPI docs router; also how the thread state and the run state handles are constructed)
    - examples/http_service_host.rs (the whole file, 140 lines — the current assembly stops at the agent router plus the docs router, and its header claims server parity)
    - crates/doc-examples/src/http_service_host.rs (the whole file, 65 lines — the anchored snippet the deployment page includes)
    - docs/src/deployment-topologies/http-service-host.md lines 130-170 (the surrounding prose whose claims must stay true after the include re-renders)
    - crates/paladin-web/src/thread_controller.rs lines 1140-1160 (the thread router's signature and the state it needs)
    - crates/paladin-web/src/run_controller.rs lines 1015-1040 (the run router's signature and the state it needs)
    - .planning/phases/34-documentation-currency-audit/34-AUDIT.md §4 rows EX-33 and EX-55, and the D-18 module-to-page include map
  </read_first>
  <action>
Both files assemble only the agent router and the OpenAPI docs router, while the shipped server
binary merges three routers. Update both so the merge order matches the server binary exactly: the
agent router first, then the thread router, then the run router, and only then the docs router if an
OpenAPI spec is present.

  - `examples/http_service_host.rs` (EX-33): construct the thread state and the run state the same
    way the server binary does, merge both routers, and extend the program's drive sequence so it
    calls at least one thread route and one run route and prints the status it got — otherwise the
    parity claim is compile-true but not demonstrated. Keep the existing agent-route drive and the
    OpenAPI title read; nothing is removed (D-18).
  - `crates/doc-examples/src/http_service_host.rs` (EX-55): apply the same merge inside the anchored
    region. This module is a compile-verified snippet, so it must stay minimal and compiling; keep
    the anchor boundaries intact so the include still resolves.

Because the doc-examples module edit re-renders the HTTP-service-host deployment page, run both
re-render checks per D-19 and record them green in `36-evidence/36-09-platform.txt`:
  - `./scripts/check-doc-examples.sh`
  - `mdbook build docs/`
Read the surrounding page prose first — if the page now states something the corrected snippet
contradicts, correct the page toward the tree (D-00c). The doc-examples crate is outside the
missing-docs bar but inside the rustdoc link bar (D-00d), so also confirm the crate still documents
clean after the edit.

Commit per D-26: `docs(36): mount thread and run routers in http_service_host (EX-33, EX-55)`. Run
`cargo fmt --all -- --check`, `cargo check --workspace --all-targets --all-features` and
`make api-surface` (expect no change) first. Stage with explicit paths, never `git add .`.
  </action>
  <verify>
    <automated>cargo build --example http_service_host --features "web-server" &amp;&amp; env -u OPENAI_API_KEY -u ANTHROPIC_API_KEY -u DEEPSEEK_API_KEY cargo run --example http_service_host --features "web-server" &amp;&amp; ./scripts/check-doc-examples.sh &amp;&amp; mdbook build docs/ &amp;&amp; RUSTDOCFLAGS="-D warnings" cargo doc -p paladin-doc-examples --all-features --no-deps &amp;&amp; cargo fmt --all -- --check &amp;&amp; cargo check --workspace --all-targets --all-features &amp;&amp; ./scripts/check-api-surface.sh .project/current-exports.txt</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'thread_router' examples/http_service_host.rs` is at least 1 and `grep -c 'run_router' examples/http_service_host.rs` is at least 1.
    - `grep -c 'thread_router' crates/doc-examples/src/http_service_host.rs` is at least 1 and `grep -c 'run_router' crates/doc-examples/src/http_service_host.rs` is at least 1.
    - `grep -c 'http_host' crates/doc-examples/src/http_service_host.rs` is at least 2 — the anchor's opening and closing markers survive.
    - `cargo run --example http_service_host --features "web-server"` exits 0 and its stdout prints a status for at least one thread route and one run route.
    - `./scripts/check-doc-examples.sh` exits 0 and `mdbook build docs/` exits 0.
    - `RUSTDOCFLAGS="-D warnings" cargo doc -p paladin-doc-examples --all-features --no-deps` exits 0.
    - `git diff --stat HEAD~1 -- src/` is empty, and the only crates path in the commit is the doc-examples HTTP-service-host module.
    - `./scripts/check-api-surface.sh .project/current-exports.txt` reports the surface unchanged.
  </acceptance_criteria>
  <done>Both HTTP-service-host files mount the three routers the shipped server mounts, the book page that includes the module re-renders green, and EX-33 and EX-55 are closed.</done>
</task>

<task type="auto">
  <name>Task 2: examples/platform_api_client.rs — EX-77, EX-78, EX-79, EX-91, EX-92, EX-93, EX-94, EX-95, EX-98, EX-99, EX-104, EX-110</name>
  <files>examples/platform_api_client.rs, Cargo.toml, .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-09-platform.txt</files>
  <read_first>
    - examples/http_service_host.rs (as corrected in Task 1 — the in-process bind-serve-drive pattern this program extends, and the three-router assembly it now has)
    - src/bin/paladin-server.rs lines 150-260 (state construction for all three routers, including what the run state's handles need)
    - crates/paladin-web/src/thread_controller.rs (the thread state, resume and history route paths and their request and response shapes — EX-77, EX-78, EX-79)
    - crates/paladin-web/src/run_controller.rs (the run submission, streaming, cancel, assistants and schedules route paths and their shapes — EX-91 through EX-95)
    - crates/paladin-web/src/agent_controller.rs lines 110-140 (the token-usage response DTO on the execute response — EX-110)
    - crates/paladin-web/src/dev_ui_controller.rs (the admin-gated dev-ui inspector route and what principal it requires — EX-104; its doc comments were corrected by plan 36-04)
    - crates/paladin-ports/src/output/run_queue_port.rs lines 100-160 (the run-queue trait and its in-memory implementation — EX-98)
    - src/config (locate the run-store backend selector and its environment variable — EX-99)
    - Cargo.toml lines 444-465 (the four existing example declarations — the shape the two new ones copy) and lines 487-560 (the features block confirming web-server and dev-ui)
    - .planning/phases/34-documentation-currency-audit/34-AUDIT.md §4 capability rows EX-77, EX-78, EX-79, EX-91, EX-92, EX-93, EX-94, EX-95, EX-98, EX-99, EX-104 and EX-110
  </read_first>
  <action>
Create `examples/platform_api_client.rs` — an in-process Platform API client. Add an example
declaration to the root manifest in the same shape as the existing four, with the name
`platform_api_client`, the path `examples/platform_api_client.rs` and required-features
`["web-server", "dev-ui"]`. Both features are needed: the dev-ui feature resolves to the web crate's
own dev-ui feature and does not itself enable the web dependency, so web-server must be listed too
(D-17).

The program boots the shipped app in-process — the same three-router assembly Task 1 restored — on
an ephemeral port, then drives it with an HTTP client in the same process, printing each route it
calls and the status it got, in this order:

  1. **Submit a run (EX-91)** — post to the run submission route and print the run id.
  2. **Stream the run (EX-92)** — open the server-sent-events stream for that run and print each
     wire event name as it arrives, so the frozen event set is visible.
  3. **Cancel a run (EX-93)** — submit a second run and cancel it, printing the status.
  4. **Assistants (EX-94)** — create and list immutable assistant versions, printing the version
     identifiers.
  5. **Schedules (EX-95)** — create and list a cron-driven schedule, printing what it would submit.
  6. **Thread state, resume and history (EX-77, EX-78, EX-79)** — read a paused thread's state,
     resume it, and page its history, printing the status and the page boundaries.
  7. **Dev-ui inspector route (EX-104)** — call the admin-gated dev-ui thread route with an admin
     principal and print the status; state in the printed output that the route is both admin-gated
     and feature-gated.
  8. **Token usage (EX-110)** — print the execute response's usage object showing the prompt and
     completion split, never a bare total beside it (D-00i).
  9. **Queue and store selection (EX-98, EX-99)** — construct the in-memory run-queue implementation
     the app uses, print which implementation is in force, and print the current run-store backend
     selection together with a line naming the durable alternatives. A Redis-backed queue is named
     but not started — no external service runs here (D-16).

All model calls go through the mock adapter. Auth uses a locally generated sample key held only in
memory, exactly as the existing HTTP-service-host example already does; the key value is never
printed (D-29). Header comment follows the house shape: what it demonstrates naming each capability,
the exact `cargo run --example platform_api_client --features "web-server,dev-ui"` command, and an
explicit statement that it needs no provider key and no external service. Use question-mark
propagation through a boxed-error main; no unwrap or expect in the body.

If any route in this list cannot be driven in-process without an external service, do not drop the
row: print the route, state the prerequisite in the output and in the header comment, and record the
constraint in the SUMMARY as a deviation for the planner (D-16, and D-28's stop-and-record rule
applied to scope).

Run it once and record the exit code and the first lines of stdout in
`36-evidence/36-09-platform.txt`.

Commit per D-26:
`docs(36): add platform_api_client example (EX-77, EX-78, EX-79, EX-91, EX-92, EX-93, EX-94, EX-95, EX-98, EX-99, EX-104, EX-110)`.
Run `cargo fmt --all -- --check`, `cargo check --workspace --all-targets --all-features` and
`make api-surface` (expect no change) first. Stage the program and the manifest explicitly.
  </action>
  <verify>
    <automated>cargo build --example platform_api_client --features "web-server,dev-ui" &amp;&amp; timeout 300 env -u OPENAI_API_KEY -u ANTHROPIC_API_KEY -u DEEPSEEK_API_KEY cargo run --example platform_api_client --features "web-server,dev-ui" &amp;&amp; cargo build --examples &amp;&amp; cargo fmt --all -- --check &amp;&amp; cargo check --workspace --all-targets --all-features &amp;&amp; ./scripts/check-api-surface.sh .project/current-exports.txt</automated>
  </verify>
  <acceptance_criteria>
    - The root manifest contains an example block naming `platform_api_client` with required-features `["web-server", "dev-ui"]`.
    - `cargo build --example platform_api_client --features "web-server,dev-ui"` exits 0.
    - `cargo build --examples` exits 0 with no features flag and does not attempt this target — the bulk selector skips unmet required-features.
    - `timeout 300 cargo run --example platform_api_client --features "web-server,dev-ui"` exits 0 with all three provider key variables unset and no external service running.
    - The program's stdout prints a status line for each of: run submission, the event stream, cancel, assistants, schedules, thread state, thread resume, thread history, and the dev-ui inspector route.
    - The program's stdout contains a prompt and completion token split and no bare total printed beside it.
    - `grep -cE '\.unwrap\(\)|\.expect\(' examples/platform_api_client.rs` is 0.
    - `grep -cE 'OPENAI_API_KEY|ANTHROPIC_API_KEY|DEEPSEEK_API_KEY' examples/platform_api_client.rs` is 0.
    - `git diff --stat HEAD~1 -- src/ crates/` is empty.
    - `./scripts/check-api-surface.sh .project/current-exports.txt` reports the surface unchanged.
  </acceptance_criteria>
  <done>Twelve Platform API capabilities have one runnable, in-process, offline program that prints each route it calls and the status it got, committed with their EX IDs in the subject and declared as a gated example target.</done>
  <reversibility rating="costly">The program name becomes a README anchor, a CI invocation, a local examples-script line and a CHANGELOG bullet; renaming after the phase is a multi-file edit (D-15, D-17).</reversibility>
</task>

<task type="auto">
  <name>Task 3: examples/webhook_receiver.rs — EX-96, EX-97</name>
  <files>examples/webhook_receiver.rs, Cargo.toml, .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-09-platform.txt</files>
  <read_first>
    - crates/paladin-core/src/platform/container/webhook.rs lines 1-40 and 170-200 (the delivery row's own documented invariants, including that no signing key is stored on the row, and the schema-version constant)
    - src/application/services/run/webhook/ssrf.rs (the SSRF guard's own module docs: the rejected address classes, the cloud metadata address that is always rejected regardless of the private-address override, and the documented DNS-rebinding limitation)
    - src/config/webhooks.rs lines 120-150 (the private-address override read from the environment — EX-97)
    - src/application/services/run/webhook (locate where the signature header is computed, and confirm it is computed over the exact byte buffer stored on the delivery row and sent verbatim)
    - .github/instructions/security.instructions.md, the webhook-delivery subsection (the binding rules this example must demonstrate rather than reinvent)
    - examples/http_service_host.rs (as corrected in Task 1 — the in-process bind-and-serve pattern the receiver reuses)
    - Cargo.toml lines 444-465 (the example declaration shape)
    - .planning/phases/34-documentation-currency-audit/34-AUDIT.md §4 capability rows EX-96 and EX-97
  </read_first>
  <action>
Create `examples/webhook_receiver.rs` — a small in-process webhook receiver that demonstrates
correct signature verification from the receiving side. Add an example declaration to the root
manifest with the name `webhook_receiver`, the path `examples/webhook_receiver.rs` and
required-features `["web-server"]` (D-17).

The program:

  1. **Stands up a receiver handler** on an ephemeral local port that captures the raw request body
     bytes before any deserialisation, and recomputes the keyed hash over exactly those bytes.
  2. **Verifies a genuine delivery (EX-96).** Sends a delivery whose signature header carries the
     hex digest computed over the same byte buffer, and prints that the recomputed digest matched.
     Do not hand-roll a different construction — reuse the same scheme and the same crate the shipped
     delivery service uses, and state in a comment that the signature is computed once over the
     stored buffer and sent verbatim, which is why a receiver's recomputation over its own raw
     capture always matches.
  3. **Rejects a tampered body.** Sends the same signature with a mutated body and prints the
     rejection, using a constant-time comparison for the digest check. The rejection is the
     capability; it must be printed.
  4. **Names the private-address override (EX-97).** Prints the current override value and one
     sentence stating what it relaxes — loopback and private-range destinations for local
     development — and one sentence stating what it does **not** relax: the cloud metadata address is
     rejected regardless. Also state, in one line, the documented DNS-rebinding limitation the
     guard's own module docs record, so the example does not read as a stronger guarantee than the
     tree provides.

Credential rules are binding (D-29): the shared secret is generated in-process, is never printed,
never logged, and never appears in a header comment or a fenced command. Print only whether
verification succeeded or failed, never the digest input. Header comment follows the house shape:
what it demonstrates, the exact `cargo run --example webhook_receiver --features "web-server"`
command, and an explicit statement that it needs no provider key and no external service. Use
question-mark propagation through a boxed-error main; no unwrap or expect.

Run it once and append the exit code and the first lines of stdout to
`36-evidence/36-09-platform.txt`.

Commit per D-26: `docs(36): add webhook_receiver example (EX-96, EX-97)`. Run
`cargo fmt --all -- --check`, `cargo check --workspace --all-targets --all-features`,
`cargo build --examples` and `make api-surface` (expect no change) first.
  </action>
  <verify>
    <automated>cargo build --example webhook_receiver --features "web-server" &amp;&amp; timeout 180 env -u OPENAI_API_KEY -u ANTHROPIC_API_KEY -u DEEPSEEK_API_KEY cargo run --example webhook_receiver --features "web-server" &amp;&amp; cargo build --examples &amp;&amp; cargo fmt --all -- --check &amp;&amp; cargo check --workspace --all-targets --all-features &amp;&amp; ./scripts/check-api-surface.sh .project/current-exports.txt</automated>
  </verify>
  <acceptance_criteria>
    - The root manifest contains an example block naming `webhook_receiver` with required-features `["web-server"]`.
    - `cargo build --example webhook_receiver --features "web-server"` exits 0, and `cargo build --examples` with no features exits 0 without attempting this target.
    - `timeout 180 cargo run --example webhook_receiver --features "web-server"` exits 0 offline.
    - The program's stdout contains a successful verification line, a rejection line for the tampered body, the private-address override value, a statement that the cloud metadata address is rejected regardless, and a statement of the DNS-rebinding limitation.
    - `grep -cE 'println!.*(secret|signing_key|SIGNING_KEY)' examples/webhook_receiver.rs` is 0 — the secret is never printed.
    - `grep -c 'X-Paladin-Signature' examples/webhook_receiver.rs` is at least 1.
    - The verification function uses a constant-time digest comparison rather than a plain equality operator on the digest bytes — confirmed by reading it.
    - `grep -cE '\.unwrap\(\)|\.expect\(' examples/webhook_receiver.rs` is 0.
    - `git diff --stat HEAD~1 -- src/ crates/` is empty.
    - `./scripts/check-api-surface.sh .project/current-exports.txt` reports the surface unchanged.
  </acceptance_criteria>
  <done>Webhook signature verification and the private-address override have one runnable offline program that verifies over raw bytes, rejects a tampered body in constant time, never prints the secret, and states the guard's documented limitation.</done>
  <reversibility rating="costly">The program name becomes a README anchor, a CI invocation and a CHANGELOG bullet (D-15, D-17).</reversibility>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

New files and binary targets created by **this plan**:
- `examples/platform_api_client.rs` — new example binary target `platform_api_client`, declared with required-features `["web-server", "dev-ui"]`
- `examples/webhook_receiver.rs` — new example binary target `webhook_receiver`, declared with required-features `["web-server"]`
- `.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-09-platform.txt`

Modified (not new): `examples/http_service_host.rs`, `crates/doc-examples/src/http_service_host.rs`,
and the root `Cargo.toml` (two new example target declarations only).

New symbols created elsewhere in Phase 36 (listed so drift verification does not flag them):
example targets `token_economy_commissary`, `war_engine_configuration`,
`control_flow_dynamic_routing`, `human_in_the_loop_gate`, `graceful_shutdown`,
`agent_runtime_middleware`, `structured_output_schema`, `sanctum_rag_retrieval`,
`node_result_cache`, `observability_tracing`, `observability_otel_export`, `eval_scenarios_demo`;
Makefile targets `doc-check` and `check-examples`; the `.pre-commit-config.yaml` hook id
`doc-check`; the CI step name `Check documentation (all features, -D warnings)`; requirement IDs
`CURR-11`…`CURR-15`; `36-EVIDENCE.md` and `36-CI-EVIDENCE.md`.

No new public Rust API is created — SC5 requires `make api-surface` to report no change.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → in-process API | the example drives real HTTP routes with auth, including an admin-gated dev-ui route |
| webhook sender → webhook receiver | an untrusted request body arrives carrying a signature header that must be verified before the body is trusted |
| example process → local network | the SSRF-guard discussion concerns outbound destinations chosen by a caller |
| repository → book page | editing the doc-examples module re-renders a deployment-topologies page |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-29 | Spoofing | the webhook receiver's signature verification | critical | mitigate | Verification recomputes the keyed hash over the exact raw body bytes captured before deserialisation, using the same scheme as the shipped delivery service, compared in constant time. An example that re-serialised the body or compared with a plain equality operator would teach a forgeable and timing-leaky check. |
| T-36-30 | Information Disclosure | the webhook shared secret | critical | mitigate | The secret is generated in-process, never printed, never logged, never placed in a header comment or a fenced command. An acceptance criterion greps the file for any print of a secret-shaped identifier (D-29). |
| T-36-31 | Information Disclosure | the SSRF-guard documentation in the example | high | mitigate | The example states both what the private-address override relaxes and what it does not — the cloud metadata address is rejected regardless — and states the documented DNS-rebinding limitation, so it does not read as a stronger guarantee than the tree provides. |
| T-36-32 | Elevation of Privilege | the admin-gated dev-ui route demonstration | high | mitigate | The dev-ui call uses an explicitly constructed admin principal and the printed output states that the route is both admin-gated and feature-gated, so a reader does not copy an unauthenticated call. The auth key is held in memory and never printed. |
| T-36-33 | Tampering | the HTTP-service-host parity claim | medium | mitigate | The current files claim server parity while omitting two routers; a reader copying them deploys an app missing the thread and run routes. The fix mounts all three routers in the shipped order and drives at least one route from each so the claim is demonstrated, not merely compiled. |
| T-36-34 | Repudiation | the book page re-render | medium | mitigate | The doc-examples module is included by the HTTP-service-host deployment page; D-19 requires the doc-examples check and the book build to be re-run and recorded green after the edit. |
| T-36-SC | Tampering | cargo package installs | low | accept | No package-manager install runs in this plan; axum and the HTTP client crate are already pinned workspace dependencies, per `36-RESEARCH.md`'s Package Legitimacy Audit. The only manifest edit is two example target declarations. |
</threat_model>

<verification>
- `cargo build --example platform_api_client --features "web-server,dev-ui"` and `cargo build --example webhook_receiver --features "web-server"` both exit 0.
- `cargo build --examples` with no features exits 0 and does not attempt either gated target.
- Both new programs and the corrected HTTP-service-host example run to exit 0 offline inside their timeouts.
- `./scripts/check-doc-examples.sh` and `mdbook build docs/` exit 0; the doc-examples crate documents clean under the all-features bar.
- `cargo fmt --all -- --check`, `cargo check --workspace --all-targets --all-features` and `make api-surface` are green.
- `36-evidence/36-09-platform.txt` records every program's exit code, first output lines, and the two re-render checks.
</verification>

<success_criteria>
- EX-33 and EX-55 are closed by the router-parity fix in both files, with the book include re-rendered green.
- EX-77, EX-78, EX-79, EX-91, EX-92, EX-93, EX-94, EX-95, EX-98, EX-99, EX-104 and EX-110 map to `examples/platform_api_client.rs`; EX-96 and EX-97 map to `examples/webhook_receiver.rs`.
- Both new gated targets are declared in the root manifest with their required features, ready for the CI and script invocations plan 36-12 adds.
- `make api-surface` reports no change.
</success_criteria>

<output>
Create `.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-09-SUMMARY.md` when done, carrying the D-24 closure table with one row per EX ID: `ID | capability | program or file | commit`, plus the two new gated targets and their required-features lists so plan 36-12 can add their CI and script invocations.
</output>
