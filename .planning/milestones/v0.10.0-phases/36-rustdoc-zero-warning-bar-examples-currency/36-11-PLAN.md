---
phase: 36-rustdoc-zero-warning-bar-examples-currency
plan: 11
type: execute
wave: 7
depends_on: ["36-01", "36-06", "36-07", "36-08", "36-09", "36-10"]
files_modified:
  - examples/README.md
  - .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-11-readme.txt
autonomous: true
requirements: [CURR-14]
must_haves:
  truths:
    - "`examples/README.md` states the workspace's real minimum Rust version, and every feature name its \"Run with Specific Features\" block mentions exists in the root `Cargo.toml` `[features]` table (EX-01)."
    - "Every `.rs` file on disk under `examples/` has a `### [name.rs](name.rs)` section in `examples/README.md`, and every listed name has a file on disk — the Phase 34 listed-versus-on-disk cross-check is empty in both directions (EX-121)."
    - "The three drifted snippet lines read the real `PaladinResult` field names — `output`, `usage` and `execution_time_ms` — and no README line names a `PaladinResult` field that does not exist (EX-122)."
    - "Every one of the 59 `EX-nn` gap rows maps to exactly one program section whose **Demonstrates:** line names that capability, and every section carries a fenced run command with the features the program requires."
    - "Every section for a program that needs an external service or a provider key states that prerequisite."
    - "No new section carries a \"Code snippet\" block."
  artifacts:
    - "examples/README.md — corrected Getting Started block, corrected snippets, complete Table of Contents, and a section for every program on disk"
    - ".planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-11-readme.txt"
  key_links:
    - "Section headers must stay matchable by `grep -oE '^### \\[[a-zA-Z0-9_]+\\.rs\\]' examples/README.md` so a later audit can re-run the listed-versus-on-disk cross-check mechanically (D-20)."
    - "Each gated program's fenced run command must carry the same feature list its `[[example]]` block declares in `Cargo.toml`, or a reader following the README gets a target cargo silently skips."
    - "The Table of Contents anchors must match the new `##` headings exactly, or the gallery's own navigation breaks."
  prohibitions:
    - statement: "A new README program section must not contain a fenced Rust snippet block previewing the program's source — the program file is the snippet, and nothing checks README snippets for compile currency (D-21)."
      status: recalled
      verification: flagged-unverified
    - statement: "No existing snippet block is deleted; the three drifted field names are corrected in place (D-21)."
      status: recalled
      verification: flagged-unverified
    - statement: "No example is deleted and no example file is renamed by this plan (D-18)."
      status: recalled
      verification: flagged-unverified
    - statement: "This plan does not add an Examples heading to any entry point, does not refreeze the entry-point baseline file, and does not wire the public-API examples script into CI or make — all three belong to Phase 36.1 SC2 (D-23, D-00g)."
      status: recalled
      verification: flagged-unverified
    - statement: "No file outside examples/README.md and the phase's own .planning/ evidence directory is modified by this plan."
      status: recalled
      verification: flagged-unverified
---

<objective>
Make `examples/README.md` a complete, cross-checkable index of the gallery: close EX-01, EX-121 and
EX-122, and give every program this phase added the section D-15 promises it.

This plan implements D-00e (edits to `examples/README.md` are Phase 36's, not Phase 35's), D-00g
(the entry-point heading rule applies to the frozen 76-item set only, and its drift is Phase
36.1's), D-15 (every gap row maps to one program *and* one README section whose **Demonstrates:**
line names the capability), D-18 (no example is deleted), D-20 (the house section shape and the
new Table-of-Contents sections), D-21 (the drifted `PaladinResult` snippet fields are corrected in
place and new sections carry no snippet block), D-22 (the minimum-Rust-version correction and the
features-block currency check, recorded under EX-01 rather than as a new ID), D-23 (this phase
adds no entry-point headings, does not refreeze the entry-point file and does not wire the
public-API examples script) and D-26 (a dedicated README commit).

Purpose: a reader browsing the gallery today cannot discover eleven of the forty-eight programs at
all — there is no Vision, Document Processing, HTTP Service Host, RAG or Commander-strategies
section — and the fourteen programs this phase adds would be invisible the same way. The README is
also the only place a gated program's required features and an external-service prerequisite are
stated in a form a reader will actually see.

Spec-less probe fallback skipped (visible, recorded): Phase 36 has no SPEC and its requirement IDs
were minted by this planning run, so `must_haves` are derived from the five ROADMAP Phase 36
Success Criteria and the CONTEXT decisions.

Output: one corrected, complete `examples/README.md`, in its own commit.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-CONTEXT.md
@.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-PATTERNS.md
@.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-06-SUMMARY.md
@.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-07-SUMMARY.md
@.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-08-SUMMARY.md
@.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-09-SUMMARY.md
@.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-10-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: EX-01 and EX-122 — the Getting Started currency fixes and the drifted PaladinResult snippet fields</name>
  <files>examples/README.md, .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-11-readme.txt</files>
  <read_first>
    - examples/README.md lines 1-80 (the Table of Contents, the Getting Started block whose minimum-Rust-version line is stale, and the first snippet block whose response-field line is wrong)
    - examples/README.md lines 1310-1345 (the Logging and Observability advanced snippet whose two token-usage and execution-time lines are wrong)
    - examples/README.md the "Run with Specific Features" block inside the Running Examples section (the feature names it claims)
    - Cargo.toml (the workspace `rust-version` value and the full `[features]` table — both are the source of truth these two fixes are checked against)
    - crates/paladin-core/src/platform/container/execution_result.rs lines 40-80 (the real `PaladinResult` fields: `output`, `usage` and `execution_time_ms`)
    - examples/basic_paladin.rs (the program the first snippet previews — it already reads the correct field names)
    - .planning/phases/34-documentation-currency-audit/34-AUDIT.md §4 rows EX-01 and EX-122 and the `examples/README.md` audit subsection
  </read_first>
  <action>
EX-01 (D-22): the Getting Started block states a minimum Rust version that predates the workspace's
own `rust-version`. Correct it to the value in the root `Cargo.toml`. While on that page, check
every feature name the "Run with Specific Features" block mentions against the root `Cargo.toml`
`[features]` table and correct any name that does not exist — a currency fix on the same page,
recorded under EX-01's closure, not a new ID.

EX-122 (D-21): three snippet lines name `PaladinResult` fields that do not exist. The real fields
are `output`, `usage` (a token-usage value carrying the prompt and completion split alongside a
total) and `execution_time_ms`. Correct the line in the Basic Paladin Examples snippet and the two
lines in the Logging and Observability advanced snippet so they name the real fields, and check the
rest of the page for any other `PaladinResult` field name that does not exist. Do not delete the
snippet blocks — existing ones stay in place once corrected.

Record both fixes, with the before and after text and the `Cargo.toml` values they were checked
against, in `36-evidence/36-11-readme.txt`.

Do not commit yet — D-26 asks for one README commit, which Task 3 makes.
  </action>
  <verify>
    <automated>test "$(grep -cE 'response\.content|response\.token_usage|response\.execution_time([^_]|$)' examples/README.md)" = 0 &amp;&amp; RUSTV=$(grep -m1 '^rust-version' Cargo.toml | sed 's/.*"\(.*\)".*/\1/') &amp;&amp; grep -q "Rust $RUSTV" examples/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'response\.content|response\.token_usage|response\.execution_time([^_]|$)' examples/README.md` is 0.
    - The README's Getting Started block names the same version string as the root `Cargo.toml` `rust-version` value.
    - Every feature name in the "Run with Specific Features" block appears in the root `Cargo.toml` `[features]` table — verified by listing the block's feature names and checking each against `Cargo.toml`.
    - The corrected snippet lines name `output`, `usage` and `execution_time_ms`.
    - Both pre-existing snippet blocks are still present — they were corrected, not deleted.
    - `git status --porcelain` lists no modified file outside `examples/README.md` and the phase's own `.planning/` evidence directory.
  </acceptance_criteria>
  <done>The gallery page no longer claims a Rust version, a feature name or a `PaladinResult` field the tree does not have; EX-01 and EX-122 are fixed in the working tree.</done>
</task>

<task type="auto">
  <name>Task 2: EX-121 — sections and Table-of-Contents entries for the eleven previously unlisted programs</name>
  <files>examples/README.md, .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-11-readme.txt</files>
  <read_first>
    - examples/README.md lines 1-20 (the Table of Contents bullet style) and lines 49-75 (the canonical section shape: the `### [name.rs](name.rs)` header, a **Demonstrates:** line, one sentence, a fenced run command, a **Key concepts:** list)
    - examples/README.md lines 1234-1260 (the Performance Benchmarking section that `war_engine_memory_baseline.rs` joins)
    - the header comments only of: examples/commander_council.rs, examples/commander_grove.rs, examples/conclave_expert_panel.rs, examples/council_discussion.rs, examples/grove_routing.rs
    - the header comments only of: examples/document_processing.rs, examples/vision_analysis.rs, examples/vision_battalion.rs, examples/http_service_host.rs, examples/paladin_with_rag.rs, examples/war_engine_memory_baseline.rs
    - Cargo.toml lines 444-480 (all `[[example]]` declarations — each pre-existing gated target's required-features list must appear in its section's run command)
    - .planning/phases/34-documentation-currency-audit/34-AUDIT.md §4 row EX-121 and the `examples/README.md` audit subsection, which names all eleven files
  </read_first>
  <action>
Eleven on-disk programs have zero mentions anywhere in the gallery page: `commander_council.rs`,
`commander_grove.rs`, `conclave_expert_panel.rs`, `council_discussion.rs`, `document_processing.rs`,
`grove_routing.rs`, `http_service_host.rs`, `paladin_with_rag.rs`, `vision_analysis.rs`,
`vision_battalion.rs` and `war_engine_memory_baseline.rs`.

Add a section for each in the house shape (D-20): the `### [name.rs](name.rs)` header, a
**Demonstrates:** line, one sentence, a fenced `cargo run --example <name>` command carrying
`--features "<list>"` where the program's `[[example]]` block declares required features, and a
**Key concepts:** list. Write each **Demonstrates:** line from the program's own header comment,
never from a guess. New sections carry no snippet block (D-21).

Add the Table-of-Contents sections D-20 names, each as a new `##` heading with a matching TOC
bullet whose anchor matches the heading exactly: **Vision** (the two vision programs),
**Document Processing**, **HTTP Service Host**, **RAG & Retrieval** (`paladin_with_rag.rs`, joined
in Task 3 by `sanctum_rag_retrieval.rs`) and **Commander Strategies (Council / Grove / Conclave)**
(the five Commander-strategy programs). `war_engine_memory_baseline.rs` joins the existing
**Performance Benchmarking** section rather than getting a new one.

For `http_service_host.rs`, the section's sentence must reflect the router parity plan 36-09
restored — it mounts the agent, thread and run routers the shipped server mounts.

Append the eleven section headers you added to `36-evidence/36-11-readme.txt`. Do not commit yet —
Task 3 makes the single README commit.
  </action>
  <verify>
    <automated>grep -oE '^### \[[a-zA-Z0-9_]+\.rs\]' examples/README.md | sed 's/^### \[//; s/\]$//' | sort -u > /tmp/36-11-listed.txt &amp;&amp; printf '%s\n' commander_council.rs commander_grove.rs conclave_expert_panel.rs council_discussion.rs document_processing.rs grove_routing.rs http_service_host.rs paladin_with_rag.rs vision_analysis.rs vision_battalion.rs war_engine_memory_baseline.rs | sort -u > /tmp/36-11-eleven.txt &amp;&amp; test -z "$(comm -23 /tmp/36-11-eleven.txt /tmp/36-11-listed.txt)"</automated>
  </verify>
  <acceptance_criteria>
    - Every one of the eleven file names appears as a `### [name.rs](name.rs)` header in `examples/README.md` — proven by the `comm -23` difference against the extracted header list being empty.
    - Each of the eleven sections contains a line starting `**Demonstrates:**` and a fenced `cargo run --example` command.
    - The sections for `vision_analysis.rs`, `vision_battalion.rs`, `document_processing.rs` and `http_service_host.rs` each carry a `--features` flag in their run command matching that target's `[[example]]` `required-features` list in `Cargo.toml`.
    - The Table of Contents contains bullets for Vision, Document Processing, HTTP Service Host, RAG & Retrieval and Commander Strategies, and each bullet's anchor matches a `##` heading present in the file.
    - `grep -c 'war_engine_memory_baseline' examples/README.md` is at least 1 and the program's section sits under the existing Performance Benchmarking heading.
    - `git status --porcelain` lists no modified file outside `examples/README.md` and the phase's own `.planning/` evidence directory.
  </acceptance_criteria>
  <done>All eleven previously undiscoverable programs have a gallery section and a navigable TOC entry; EX-121's on-disk-but-unlisted direction is closed in the working tree.</done>
</task>

<task type="auto">
  <name>Task 3: Sections for the fourteen programs this phase added, the both-directions cross-check, and the single README commit</name>
  <files>examples/README.md, .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-11-readme.txt</files>
  <read_first>
    - the header comments only of the thirteen new programs whose sections are still missing: examples/war_engine_configuration.rs, examples/control_flow_dynamic_routing.rs, examples/human_in_the_loop_gate.rs, examples/graceful_shutdown.rs, examples/agent_runtime_middleware.rs, examples/structured_output_schema.rs, examples/sanctum_rag_retrieval.rs, examples/platform_api_client.rs, examples/webhook_receiver.rs, examples/node_result_cache.rs, examples/observability_tracing.rs, examples/observability_otel_export.rs, examples/eval_scenarios_demo.rs
    - examples/README.md the Token Economy Examples section plan 36-01 already added for `token_economy_commissary.rs` (the in-file precedent for a new-program section — the fourteenth new program already has its section)
    - the closure tables in 36-06-SUMMARY.md, 36-07-SUMMARY.md, 36-08-SUMMARY.md, 36-09-SUMMARY.md and 36-10-SUMMARY.md (each maps EX IDs to a program; the **Demonstrates:** lines must name the same capabilities)
    - Cargo.toml lines 444-490 (every `[[example]]` declaration, including the four gated targets this phase added, so each run command carries the right feature list)
    - .planning/phases/34-documentation-currency-audit/34-AUDIT.md §4 D-17(c) capability gap list (the authoritative capability wording for all 59 rows)
  </read_first>
  <action>
Add a section for each of the thirteen remaining new programs, in the same house shape, grouped
under new `##` Table-of-Contents sections named for their capability clusters — for example
**WarEngine Configuration & Checkpoints**, **Control Flow & Dynamic Routing**,
**Human-in-the-Loop**, **Graceful Shutdown**, **Agent Runtime & Middleware**,
**Structured Output**, **Platform API**, **Node-Result Cache**, **Observability & Tracing** and
**Evaluation**. `sanctum_rag_retrieval.rs` joins the **RAG & Retrieval** section Task 2 created.
Add a matching TOC bullet for every new `##` heading.

Each **Demonstrates:** line must name the capabilities the corresponding closure table assigns to
that program, using the audit's own capability wording so a later reader can trace an `EX-nn` row
to the sentence that claims it. Each section's fenced run command carries the exact feature list
the program's `[[example]]` block declares — `platform_api_client` needs two features,
`webhook_receiver`, `node_result_cache` and `observability_otel_export` one each; the remaining
programs carry no feature flag.

Every section for a program that cannot be run offline states the prerequisite in its own text:
`node_result_cache` needs a Redis server (`make services-up` starts the dev stack) and
`observability_otel_export` needs a reachable OTLP collector; both are build-only in CI (D-16).
No new section carries a snippet block (D-21).

Then close the plan:
  - Run the Phase 34 cross-check in both directions: extract every `### [name.rs](name.rs)` header
    from the README, list every `.rs` file on disk under `examples/`, and confirm each list is a
    subset of the other. Both differences must be empty. Record the two lists and the empty
    differences in `36-evidence/36-11-readme.txt`.
  - Confirm every one of the 59 gap `EX-nn` rows is claimed by exactly one **Demonstrates:** line,
    and record that mapping in the SUMMARY's closure table.
  - Make the single README commit per D-26:
    `docs(36): complete the examples gallery index (EX-01, EX-121, EX-122 and the new program sections)`.
    Stage with `git add -- examples/README.md .planning/phases/36-*/36-evidence/36-11-readme.txt`,
    never `git add .`. No `.rs` file is staged, so the clippy pre-commit hook has nothing to run.

This plan adds no entry-point heading to any public item, does not refreeze
`16-DOCS-03-ENTRY-POINTS.md`, and does not wire `scripts/check-public-api-examples.sh` into CI or
`make` — all three are Phase 36.1 SC2's deliverable (D-23, D-00g).
  </action>
  <verify>
    <automated>ls examples/*.rs | xargs -n1 basename | sort -u > /tmp/36-11-ondisk.txt &amp;&amp; grep -oE '^### \[[a-zA-Z0-9_]+\.rs\]' examples/README.md | sed 's/^### \[//; s/\]$//' | sort -u > /tmp/36-11-listed.txt &amp;&amp; test -z "$(comm -23 /tmp/36-11-ondisk.txt /tmp/36-11-listed.txt)" &amp;&amp; test -z "$(comm -13 /tmp/36-11-ondisk.txt /tmp/36-11-listed.txt)" &amp;&amp; test "$(grep -c '^\*\*Demonstrates:\*\*' examples/README.md)" = "$(wc -l < /tmp/36-11-ondisk.txt)"</automated>
  </verify>
  <acceptance_criteria>
    - The on-disk example file list and the README section-header list are identical: both `comm -23` and `comm -13` differences are empty.
    - The count of `**Demonstrates:**` lines in `examples/README.md` equals the count of `.rs` files under `examples/`.
    - `grep -c 'platform_api_client --features "web-server,dev-ui"' examples/README.md` is at least 1; the run commands for `webhook_receiver`, `node_result_cache` and `observability_otel_export` each carry their single declared feature.
    - The `node_result_cache` section names a Redis prerequisite and the `observability_otel_export` section names an OTLP collector prerequisite; both say they are build-only in CI.
    - No section added by this phase is followed by a fenced `rust` code block — confirmed by reading each new section.
    - `git diff --stat HEAD~1` lists only `examples/README.md` and the phase's own evidence file.
    - `git log -1 --pretty=%s` starts with `docs(36): complete the examples gallery index` and names EX-01, EX-121 and EX-122.
    - `36-evidence/36-11-readme.txt` contains both cross-check lists and the two empty differences.
  </acceptance_criteria>
  <done>Every program on disk has a gallery section, every gallery section has a program on disk, every gap capability is claimed by exactly one **Demonstrates:** line, and the whole README pass is in one commit.</done>
  <reversibility rating="costly">Program names are now README anchors; renaming any of them after this commit is a multi-file edit across the README, `Cargo.toml`, `ci.yml`, `scripts/check-all-examples.sh` and `CHANGELOG.md` (D-15).</reversibility>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

New file created by **this plan**:
- `.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-11-readme.txt`

New anchors created in `examples/README.md` by this plan: the `##` headings **Vision**,
**Document Processing**, **HTTP Service Host**, **RAG & Retrieval**,
**Commander Strategies (Council / Grove / Conclave)**, **WarEngine Configuration & Checkpoints**,
**Control Flow & Dynamic Routing**, **Human-in-the-Loop**, **Graceful Shutdown**,
**Agent Runtime & Middleware**, **Structured Output**, **Platform API**, **Node-Result Cache**,
**Observability & Tracing** and **Evaluation**, plus a `### [name.rs](name.rs)` section for every
program not already listed.

No Rust symbol, no file under `src/`, `crates/` or `examples/*.rs`, and no public API is created or
modified by this plan.

New symbols created elsewhere in Phase 36 (listed so drift verification does not flag them):
example targets `token_economy_commissary`, `war_engine_configuration`,
`control_flow_dynamic_routing`, `human_in_the_loop_gate`, `graceful_shutdown`,
`agent_runtime_middleware`, `structured_output_schema`, `sanctum_rag_retrieval`,
`platform_api_client`, `webhook_receiver`, `node_result_cache`, `observability_tracing`,
`observability_otel_export`, `eval_scenarios_demo`; Makefile targets `doc-check` and
`check-examples`; the `.pre-commit-config.yaml` hook id `doc-check`; the CI step name
`Check documentation (all features, -D warnings)`; requirement IDs `CURR-11`…`CURR-15`;
`36-EVIDENCE.md` and `36-CI-EVIDENCE.md`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository → reader following the README | a run command or a prerequisite the page states wrongly sends a reader down a path that fails or, worse, one that silently skips a target |
| README prose → shipped API | a snippet naming a field that does not exist teaches an API the tree does not have |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-39 | Spoofing | gated programs' run commands | medium | mitigate | A run command missing its `--features` list produces a target cargo silently skips with exit code 0 — the same silent-skip failure mode the CI job's four-invocation split exists to catch. Each gated section's command is checked against that target's `[[example]]` `required-features` list in `Cargo.toml`. |
| T-36-40 | Information Disclosure | example prerequisites stated in the README | low | mitigate | Sections for programs needing Redis or an OTLP collector name the service rather than embedding an endpoint or a credential, so a reader does not copy a connection string out of documentation. |
| T-36-41 | Tampering | corrected snippet blocks | medium | mitigate | The three drifted lines are corrected against `crates/paladin-core/src/platform/container/execution_result.rs`, the shipped struct, not against another document — D-00c's shipped-tree-outranks-document rule. New sections carry no snippet block at all, so no fourth copy of an API shape is created (D-21). |
| T-36-42 | Repudiation | gallery completeness claim | medium | mitigate | The both-directions `comm` cross-check is run and its two empty differences recorded in the evidence file, so "the gallery is complete" is a reproduced measurement rather than an assertion. |
| T-36-SC | Tampering | cargo package installs | low | accept | This plan touches one markdown file and one evidence file; no package-manager install runs and no `.rs` file is staged. |
</threat_model>

<verification>
- Both directions of the listed-versus-on-disk cross-check are empty.
- The `**Demonstrates:**` line count equals the `examples/*.rs` file count.
- No README line names a `PaladinResult` field that does not exist, and the minimum Rust version matches the workspace `rust-version`.
- `git diff --stat` for the plan's single commit lists only `examples/README.md` and the phase's evidence file.
- `36-evidence/36-11-readme.txt` carries the before/after text of the EX-01 and EX-122 fixes and both cross-check lists.
</verification>

<success_criteria>
- EX-01, EX-121 and EX-122 are closed, completing all five stale `EX-nn` rows when combined with plan 36-09's EX-33 and EX-55.
- All 59 gap `EX-nn` rows have both a named program and a README section whose **Demonstrates:** line names the capability, satisfying D-15's two-part requirement.
- Nothing belonging to Phase 36.1 SC2 is absorbed: no entry-point heading is added, the entry-point baseline is not refrozen, and the public-API examples script is not wired.
</success_criteria>

<output>
Create `.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-11-SUMMARY.md` when done, carrying the D-24 closure table for EX-01, EX-121 and EX-122 plus a full `EX-nn → program → README section` map for all 59 gap rows.
</output>
