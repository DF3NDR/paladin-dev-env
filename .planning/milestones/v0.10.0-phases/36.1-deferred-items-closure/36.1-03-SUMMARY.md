---
phase: 36.1-deferred-items-closure
plan: 03
subsystem: docs
tags: [mdbook, rustdoc, cli-config, cicd, contributing-guide]

# Dependency graph
requires:
  - phase: 36.1-deferred-items-closure/01
    provides: the tracer/closure-chain pattern this wave-2 plan follows
provides:
  - contributing-providers.md's two hypothetical import paths corrected to the shipped
    per-provider crate layout (paladin_llm::myprovider::*)
  - the nine-signal page-check re-run for contributing-providers.md, captured as evidence,
    with an explicit verdict on the module-declaration line (left unfixed, reasoned)
  - cli-configuration.md's Garrison and Arsenal troubleshooting entries rewritten to name
    live config keys and loader functions instead of an absent source-line marker
  - cicd.md's Kubernetes deployment block and eight Best Practices YAML fragments captioned
    illustrative, plus one section-intro sentence, with zero body/secret-expression edits
affects: [36.1-04, 36.1-13 (WINDOWS.md ledger reconciliation), 37 (release readiness)]

tech-stack:
  added: []
  patterns:
    - "Illustrative-only bold caption inverting a page's own excerpt-caption voice (D-08)"
    - "Troubleshooting entries point at live config key + loader function, not a source-line marker (D-06)"

key-files:
  created:
    - .planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-03-signals-contributing-providers.txt
  modified:
    - docs/src/contributing/contributing-providers.md
    - docs/src/appendix/cli-configuration.md
    - docs/src/deployment/cicd.md

key-decisions:
  - "Line 233/236's `pub mod myprovider_adapter;` module-declaration instruction is left unfixed: the nine-signal script's class 4 (module/source paths) only regexes for literal .rs file-path strings, and that line's defect is a module-name mismatch inside a code snippet, not a broken path — class 4 confirmed every cited .rs path on the page is real (test -f true) and raised no hit on this line. Per D-04's bound ('same relocated-path class'), a defect class 4 cannot structurally detect is not in scope for this commit; recorded here per RESEARCH.md Open Question 1's instruction to flag the verdict explicitly rather than leave it silent. No WINDOWS.md row minted (D-17's 12-row mapping has no slot for it, and the plan's own action forbids minting one)."

requirements-completed: [CURR-16]

coverage:
  - id: D1
    description: "contributing-providers.md's two hypothetical adapter import paths corrected from the pre-decomposition facade path to paladin_llm::myprovider::*, mirroring the shipped per-provider directory layout"
    requirement: "CURR-16"
    verification:
      - kind: other
        ref: "grep -c 'paladin::infrastructure::adapters' docs/src/contributing/contributing-providers.md (0) && grep -q 'paladin_llm::myprovider' (present at lines 272, 367)"
        status: pass
    human_judgment: false
  - id: D2
    description: "Nine-signal page check re-run against contributing-providers.md, captured as evidence, with an explicit recorded verdict on the module-declaration line"
    requirement: "CURR-16"
    verification:
      - kind: other
        ref: ".planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-03-signals-contributing-providers.txt (non-empty, names the page)"
        status: pass
    human_judgment: false
  - id: D3
    description: "cli-configuration.md's Garrison and Arsenal troubleshooting entries rewritten to name the live config key and loader function the agent command calls, instead of a numbered source-line TODO check"
    requirement: "CURR-16"
    verification:
      - kind: other
        ref: "grep -rn 'verify no TODO' docs/src (empty, tree-wide)"
        status: pass
    human_judgment: false
  - id: D4
    description: "cicd.md's Kubernetes deployment block and all eight Best Practices YAML fragments captioned illustrative with a link to the real release path, plus one section-intro sentence, with zero fenced-block-body or secret-expression edits"
    requirement: "CURR-16"
    verification:
      - kind: other
        ref: "grep -c 'Illustrative only' docs/src/deployment/cicd.md (9) && grep -c 'release.yml' (20, includes existing Release Pipeline section) && secret-expression count unchanged (3 before, 3 after)"
        status: pass
    human_judgment: false
  - id: D5
    description: "Documentation-workflow gate sequence re-run on the three touched pages: mermaid install, linkcheck build, doc-examples check, doc-config check, docs tree clean afterwards"
    requirement: "CURR-16"
    verification:
      - kind: other
        ref: "mdbook-mermaid install docs/ (no drift) && mdbook build docs/ exit 0 ('No broken links found') && scripts/check-doc-examples.sh exit 0 (0 failed) && scripts/check-doc-config.sh exit 0 (151 YAML blocks, 0 failed) && git status --porcelain -- docs (clean except the intended edit)"
        status: pass
    human_judgment: false

duration: 20min
completed: 2026-09-18
status: complete
---

# Phase 36.1 Plan 03: Close three docs/src prose defects (D-04, D-06, D-08) Summary

**Corrected the provider-guide adapter path, rewrote two unfollowable CLI troubleshooting entries against live wiring, and captioned nine illustrative CI/CD YAML blocks — zero fenced-block-body edits, mdBook still builds clean.**

## Performance

- **Duration:** ~20 min
- **Started:** 2026-09-18T03:45:00Z (approx.)
- **Completed:** 2026-09-18T03:52:28Z
- **Tasks:** 3
- **Files modified:** 3 (plus 1 evidence file created)

## Accomplishments
- `contributing-providers.md` lines 272 and 367 no longer point a contributor at the
  pre-decomposition facade module tree (`paladin::infrastructure::adapters::llm::myprovider_adapter::*`);
  both now read `paladin_llm::myprovider::*`, matching the shipped `openai`/`anthropic`/`deepseek`/etc.
  per-provider directory layout (each a directory holding `mod.rs` + `adapter.rs`).
- Re-ran the Phase 34 nine-signal page check (`34-signals.sh`) against this one page and captured
  the full output as evidence; recorded an explicit verdict on the flagged module-declaration line
  (see Key Decisions) rather than leaving it silently unaddressed.
- `cli-configuration.md`'s Garrison (line 409) and Arsenal (line 422) troubleshooting entries no
  longer instruct a reader to "verify no TODO at line 293/296" (that marker was removed three
  phases ago and the instruction was unfollowable). Both now name the live config key
  (`garrison.type` / `arsenal.mcp_servers`), the loader function that reads it
  (`instantiate_garrison` / `instantiate_arsenal`, `src/application/cli/config/loader.rs`), and
  what the reader should verify when the port isn't wired — mirroring the Scheduler entry's
  structural model on the same page.
- `cicd.md`'s "Deploy to Kubernetes" block and all eight "Best Practices" YAML fragments now carry
  a bold `**Illustrative only …**` caption naming what the block is *not* and linking the real
  release path (`release.yml`), inverting the page's own existing `# excerpt: …` caption voice.
  One sentence was added to the Deployment Automation section intro saying the same thing once in
  prose. No line inside any fenced block was touched, and the `${{ secrets.* }}` expression count
  is unchanged (3 → 3).
- Re-ran the full D-00f documentation-workflow gate sequence on the three touched pages:
  `mdbook-mermaid install docs/` (no drift), `mdbook build docs/` with the linkcheck backend
  (exit 0, "No broken links found"), `scripts/check-doc-examples.sh` (0 failed), and
  `scripts/check-doc-config.sh` (151 YAML blocks checked, 0 failed). `git status --porcelain --
  docs` was clean afterwards (aside from the intended edits, prior to each commit).

## Task Commits

Each task was committed atomically:

1. **Task 1: Correct the provider-adapter module path in the contributor guide and re-run the nine-signal page check** - `c0661030` (docs)
2. **Task 2: Rewrite the Garrison and Arsenal troubleshooting entries to name live wiring** - `a807c025` (docs)
3. **Task 3: Caption the CI/CD deployment and best-practice YAML as illustrative** - `792bc3dd` (docs)

**Plan metadata:** committed below (this SUMMARY + STATE.md + ROADMAP.md + REQUIREMENTS.md)

## Files Created/Modified
- `docs/src/contributing/contributing-providers.md` - two import paths corrected to `paladin_llm::myprovider::*`
- `docs/src/appendix/cli-configuration.md` - Garrison and Arsenal troubleshooting entries rewritten against live wiring
- `docs/src/deployment/cicd.md` - nine illustrative captions + one intro sentence added
- `.planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-03-signals-contributing-providers.txt` - nine-signal check capture (new)

## Decisions Made
- **Module-declaration line (233/236) left unfixed, verdict recorded, no ledger row minted.**
  RESEARCH.md's Open Question 1 asked whether `pub mod myprovider_adapter;` (which, per the
  live tree, should read `pub mod myprovider;` to match the `openai`/`anthropic`/etc. per-provider
  module naming) falls under D-04's "same relocated-path class" extension clause. The nine-signal
  script's class 4 (module/source paths) regexes only for literal `.rs`-suffixed path strings; the
  line's actual defect — a Rust `pub mod` statement naming the wrong identifier — is not itself a
  path and so cannot be reproduced as a class-4 hit (confirmed: all seven class-4 hits on this page
  are real, existing `.rs` files, `test -f` true on each). Per the plan's own instruction to decide
  "on the signal script's own class-4 result, not on taste," the verdict is: **not the same
  relocated-path class, therefore out of scope for this commit.** No `WINDOWS.md` row was minted
  for it (D-17's 12-row mapping has no slot, and the plan's action text explicitly forbids minting
  one); the verdict is recorded here per the plan's requirement that it not be left silently
  unaddressed.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- SC1's three page fixes owned by this plan (D-04, D-06, D-08) are closed on the tree.
- `36.1-evidence/36.1-03-signals-contributing-providers.txt` is available for the closing plan's
  `36.1-EVIDENCE.md` closure table (D-20).
- The open module-declaration verdict above is available for the closing plan to reference if it
  decides a future observation is warranted; no action is required of it since D-17's row count is
  fixed and unaffected.
- No blockers for the remaining SC1 items (`OpenAiAdapter` casing, `testing-guide.md` tree) or any
  other 36.1 plan; this plan's file set (`contributing-providers.md`, `cli-configuration.md`,
  `cicd.md`) does not overlap any other in-flight plan's `files_modified`.

## Self-Check: PASSED

- `docs/src/contributing/contributing-providers.md` — FOUND
- `docs/src/appendix/cli-configuration.md` — FOUND
- `docs/src/deployment/cicd.md` — FOUND
- `.planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-03-signals-contributing-providers.txt` — FOUND
- `c0661030` — FOUND in `git log --oneline --all`
- `a807c025` — FOUND in `git log --oneline --all`
- `792bc3dd` — FOUND in `git log --oneline --all`

---
*Phase: 36.1-deferred-items-closure*
*Completed: 2026-09-18*
