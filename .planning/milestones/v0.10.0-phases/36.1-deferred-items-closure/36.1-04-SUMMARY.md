---
phase: 36.1-deferred-items-closure
plan: 04
subsystem: docs
tags: [mdbook, testing-guide, adapter-casing, scratch-compile-probe, sc1]

# Dependency graph
requires:
  - phase: 36.1-01
    provides: the phase's shared evidence directory and D-05/D-07 decision text in 36.1-CONTEXT.md
provides:
  - "docs/src/contributing/testing-guide.md's tests/ tree rebuilt from ls -p tests, config.test.yml relocated to the repository root as a sibling of tests/"
  - "eleven word-boundary OpenAiAdapter -> OpenAIAdapter casing corrections across seven pages"
  - "a recorded compile-probe verdict (all four fail) for the four battalion-patterns-guide.md bodies Phase 35 flagged, ready for plan 36.1-13 to mint the four waived unmet-truth ledger rows"
  - "a green documentation-gate run (mdbook-mermaid install, mdbook build w/ linkcheck, check-doc-examples.sh, check-doc-config.sh) captured for the seven-page edit set"
affects: [36.1-13]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Throwaway examples/_scratch.rs + cargo check --example _scratch --features llm-openai as the standard D-00e appendix-body compile probe; deleted before every commit, never added to Cargo.toml (autoexamples picks it up)."

key-files:
  created:
    - .planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-04-scratch-probe.txt
    - .planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-04-docs-gate.txt
  modified:
    - docs/src/contributing/testing-guide.md
    - docs/src/contributing/architecture-decisions.md
    - docs/src/user-guides/memory-management.md
    - docs/src/user-guides/tool-integration.md
    - docs/src/appendix/battalion-vision-support.md
    - docs/src/appendix/battalion-patterns-guide.md
    - docs/src/appendix/conclave-pattern.md

key-decisions:
  - "Beyond the ASCII tree itself, three downstream code-comment file-path references in testing-guide.md (tests/integration/redis_queue_test.rs x2, tests/functional/battalion_execution_test.rs) repeated the same three fabricated names the tree carried and were corrected to real paths (redis_queue_integration_test.rs, tests/integration/battalion/formation_integration_test.rs) so the plan's own whole-file exit check (grep -c across the full page, not just the tree block) would be true, not just locally true inside the tree."
  - "All four battalion-patterns-guide.md flagged bodies (lines 43, 134, 232, 354) fail to compile even after the casing fix — none import std::sync::Arc or paladin_llm::OpenAIAdapter into scope, and each chains a non-existent synchronous .build() off OpenAIAdapter::new() where the real constructor takes an OpenAIConfig argument and PaladinBuilder::build() is async. Per D-07 these are NOT rewritten here; each is recorded for plan 36.1-13 to mint as a waived unmet-truth ledger row."

patterns-established: []

requirements-completed: [CURR-16]

coverage:
  - id: D1
    description: "testing-guide.md's tests/ tree matches ls -p tests exactly (real names only, config.test.yml at repo root) with no fabricated file left anywhere on the page"
    requirement: "CURR-16"
    verification:
      - kind: other
        ref: "grep -c 'sample_data.json|redis_queue_test.rs|battalion_execution_test.rs' docs/src/contributing/testing-guide.md == 0 (whole-file); grep -q 'helpers/' and 'config.test.yml' both present"
        status: pass
    human_judgment: false
  - id: D2
    description: "every word-boundary OpenAiAdapter occurrence across the book corrected to the shipped OpenAIAdapter casing, casing-only diff"
    requirement: "CURR-16"
    verification:
      - kind: other
        ref: "grep -rnw OpenAiAdapter docs/src returns nothing; grep -rlw OpenAIAdapter docs/src >= 7 files; git diff for the sweep commit is casing-only on every touched line"
        status: pass
    human_judgment: false
  - id: D3
    description: "the four flagged battalion-patterns-guide.md bodies each ran through the throwaway scratch compile probe with a captured verdict, none fixed"
    verification:
      - kind: other
        ref: ".planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-04-scratch-probe.txt (four cargo check runs, all fail; examples/_scratch.rs absent from working tree and from every commit)"
        status: pass
    human_judgment: false
  - id: D4
    description: "the documentation gate (mdbook-mermaid install, mdbook build w/ linkcheck, check-doc-examples.sh, check-doc-config.sh) passes clean on the edited tree"
    verification:
      - kind: other
        ref: ".planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-04-docs-gate.txt (all four steps exit 0, 'No broken links found', git status --porcelain -- docs empty)"
        status: pass
    human_judgment: false

duration: ~35min
completed: 2026-09-18
status: complete
---

# Phase 36.1 Plan 04: SC1 Testing-Guide Tree and Adapter-Casing Closure Summary

**Rebuilt the fabricated `tests/` tree against `ls -p tests` truth, swept all eleven mis-cased `OpenAiAdapter` occurrences to `OpenAIAdapter` across seven pages, and probed the four flagged appendix example bodies — all four fail to compile, recorded (not rewritten) for plan 36.1-13.**

## Performance

- **Duration:** ~35 min
- **Started:** 2026-09-18T03:53:00Z
- **Completed:** 2026-09-18T04:02:30Z
- **Tasks:** 3
- **Files modified:** 9 (7 docs pages + 2 new evidence files)

## Accomplishments

- `docs/src/contributing/testing-guide.md`'s `tests/` ASCII tree replaced wholesale: names all eight real top-level directories (`cli/`, `fixtures/`, `functional/`, `helpers/`, `integration/`, `repository/`, `scripts/`, `unit/`), shows `config.test.yml` at the repository root as a sibling of `tests/` (not under `fixtures/`), and names no file the repository lacks. Three downstream code-comment path references that repeated the same fabricated names were also corrected.
- All eleven word-boundary `OpenAiAdapter` occurrences across seven pages (`architecture-decisions.md:236`, `testing-guide.md:324`(now `357`), `memory-management.md:113`, `tool-integration.md:187,295`, `battalion-vision-support.md:278`, `battalion-patterns-guide.md:43,134,232,354`, `conclave-pattern.md:87`) corrected to the shipped `OpenAIAdapter` casing (confirmed against `crates/paladin-llm/src/openai/adapter.rs:280`). The sweep diff is casing-only on every line — no argument, method chain, import or fence attribute touched.
- The four `battalion-patterns-guide.md` bodies Phase 35 plan 35-09 flagged (lines 43, 134, 232, 354 — Formation, Phalanx, Campaign, Chain of Command examples) were each run through the throwaway `examples/_scratch.rs` + `cargo check --example _scratch --features llm-openai` probe. **All four fail to compile**, each with the identical first two errors (`Arc` not imported, `OpenAIAdapter` not imported) followed by further errors specific to each body (async `.build()` awaited without `.await`, wrong constructor arity, `Campaign`/`ChainOfCommand`/`Phalanx` builder API drift). Full `cargo check` output for all four captured in `36.1-04-scratch-probe.txt`. The scratch file was deleted after each run and never appears in any commit.
- Ran the exact `.github/workflows/docs.yml` sequence (`mdbook-mermaid install docs/` → `mdbook build docs/` with the linkcheck backend's `warning-policy = "error"` → `scripts/check-doc-examples.sh` → `scripts/check-doc-config.sh`) against the edited tree. All four steps exit 0; linkcheck reports "No broken links found" (zero pre-existing broken links found on any page, touched or untouched — nothing to route to plan 36.1-13). `git status --porcelain -- docs` was empty both before and after the run.

## Task Commits

1. **Task 1: Rebuild the testing guide's tests tree from the real directory listing** - `685bab79` (docs)
2. **Task 2: Sweep the mis-cased adapter type across all seven pages and probe the four flagged bodies** - `a1bd0ca5` (docs)
3. **Task 3: Run the documentation gate on the seven edited pages** - `cabde22a` (docs)

_No plan-metadata commit yet — this SUMMARY, STATE.md and ROADMAP.md land in the plan's closing metadata commit._

## Files Created/Modified

- `docs/src/contributing/testing-guide.md` - tests/ tree rebuilt; three downstream fabricated-path comments corrected; OpenAiAdapter casing fixed
- `docs/src/contributing/architecture-decisions.md` - OpenAiAdapter casing fixed (1 site)
- `docs/src/user-guides/memory-management.md` - OpenAiAdapter casing fixed (1 site)
- `docs/src/user-guides/tool-integration.md` - OpenAiAdapter casing fixed (2 sites)
- `docs/src/appendix/battalion-vision-support.md` - OpenAiAdapter casing fixed (1 site)
- `docs/src/appendix/battalion-patterns-guide.md` - OpenAiAdapter casing fixed (4 sites, one per flagged body)
- `docs/src/appendix/conclave-pattern.md` - OpenAiAdapter casing fixed (1 site)
- `.planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-04-scratch-probe.txt` - four full `cargo check` runs, one per flagged body
- `.planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-04-docs-gate.txt` - the four-step documentation-gate run, captured verbatim

## Decisions Made

- **Tests-tree correction scope extended past the ASCII block itself.** The plan's own exit check (`grep -c` across the *whole file*, not scoped to the tree) meant three downstream code-comment path references (`// tests/integration/redis_queue_test.rs` x2, `// tests/functional/battalion_execution_test.rs`) repeating the same three fabricated names the tree carried had to be corrected too, or the commit's own verify would fail. Corrected to the real paths (`redis_queue_integration_test.rs`, `tests/integration/battalion/formation_integration_test.rs`) rather than left as-is. Two adjacent illustrative examples with similarly loose path comments (`minio_storage_test.rs`, `llm_provider_test.rs`) were deliberately left untouched — they are not required by the exit check and are outside D-05's named defect (the fabricated tree names), so touching them would be unrequested scope growth.
- **Probe verdict: all four bodies fail, none rewritten.** Per D-07, the sweep is casing-only; the probe is diagnostic, not corrective. All four `battalion-patterns-guide.md` bodies fail to compile for reasons beyond casing (missing `Arc`/`OpenAIAdapter` imports, async `.build()` awaited without `.await`, further per-body API-shape drift in `Campaign`/`Phalanx`/`ChainOfCommand`). None were rewritten. **Plan 36.1-13 must mint four waived `unmet-truth` ledger rows** (one per body — lines 43, 134, 232, 354), each with the reason "appendix sample fenced `rust,ignore`, owner: v0.11.0 docs-currency pass, re-check at that milestone's discuss step." This is an addition of **4 rows** to the twelve-row mapping the phase's other plans are building — 36.1-13 needs the exact count stated, not inferred: **4**.
- **Phase 35 `35-EVIDENCE.md` D-21 allowlist row retirement.** The `35-EVIDENCE.md` D-21 allowlist row that excused the stale `tests/` tree as a false positive is retired by this plan's Task 1 commit (`685bab79`) — the tree is now corrected, so the false-positive excuse no longer applies. Plan 36.1-13 carries this retirement statement into `36.1-EVIDENCE.md`.

## Deviations from Plan

None — plan executed exactly as written. The three additional downstream comment-path corrections in Task 1 (see Decisions Made) were required by the task's own stated exit check, not a discovered defect outside the plan's scope, so this is not logged as a Rule 1/2/3 deviation.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- The remaining three of ROADMAP SC1's six named documentation defects (the fabricated tests tree, and the mis-cased adapter type on all seven pages) are closed on the page, against the tree.
- Plan 36.1-13 has everything it needs to mint the four waived ledger rows for the non-compiling appendix bodies and to retire the Phase 35 D-21 allowlist row in `36.1-EVIDENCE.md`.
- No pre-existing broken link was found by the documentation gate on any page this plan did not touch — nothing further to route to 36.1-13 on that front.

## Self-Check: PASSED

- `docs/src/contributing/testing-guide.md` — FOUND, tree confirmed matching `ls -p tests` (verify command re-run clean).
- `.planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-04-scratch-probe.txt` — FOUND, non-empty (530 lines, four probe runs).
- `.planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-04-docs-gate.txt` — FOUND, non-empty (612 lines, four gate steps).
- Commit `685bab79` — FOUND in `git log --oneline --all`.
- Commit `a1bd0ca5` — FOUND in `git log --oneline --all`.
- Commit `cabde22a` — FOUND in `git log --oneline --all`.
- `grep -rnw OpenAiAdapter docs/src` — confirmed empty.
- `git status --porcelain -- docs` — confirmed empty.
- `examples/_scratch.rs` — confirmed absent from working tree and from every commit's file list.

---
*Phase: 36.1-deferred-items-closure*
*Completed: 2026-09-18*
