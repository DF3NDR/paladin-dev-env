---
phase: 36.1-deferred-items-closure
plan: 05
type: execute
wave: 2
depends_on: ["36.1-01"]
files_modified:
  - crates/paladin-ports/src/output/structured_executor_port.rs
  - crates/paladin-ports/src/output/node_cache_port.rs
  - crates/paladin-ports/src/input/run_submission_port.rs
  - crates/paladin-ports/src/input/schedule_admin_port.rs
  - crates/paladin-ports/src/output/run_queue_port.rs
  - crates/paladin-ports/src/output/webhook_delivery_port.rs
autonomous: true
requirements: [CURR-17]
must_haves:
  truths:
    - "`StructuredExecutorPort`, `NodeCachePort`, `RunSubmissionPort`, `ScheduleAdminPort`, `RunQueuePort` and `WebhookDeliveryRepositoryPort` each carry a plural `# Examples` heading in their own doc block, and the public-API examples script reports each as satisfied rather than missing (D-09, CURR-17)."
    - "Each of the six sections carries at least one doctest that compiles and is reported as passed — not ignored — by `cargo test -p paladin-ports --doc`, so the heading is backed by a real example rather than by an unrun block (D-10)."
    - "Every doctest builds its example from a minimal local struct implementing the trait inline; none imports a storage adapter, because the ports crate has no dependency edge to the storage crate and adding one would invert the dependency direction the architecture requires."
    - "`cargo doc -p paladin-ports --no-deps` still emits zero warning lines, so the six new sections do not reopen the rustdoc bar Phase 36 closed."
    - "The public-API baseline is unmoved — a doc comment is not a public item (CURR-21 carryover from CURR-15)."
  artifacts:
    - "crates/paladin-ports/src/output/structured_executor_port.rs — heading added above the existing compiling example"
    - "crates/paladin-ports/src/output/node_cache_port.rs — new `# Examples` section with an inline cache implementation"
    - "crates/paladin-ports/src/input/run_submission_port.rs — new `# Examples` section"
    - "crates/paladin-ports/src/input/schedule_admin_port.rs — new `# Examples` section"
    - "crates/paladin-ports/src/output/run_queue_port.rs — new `# Examples` section"
    - "crates/paladin-ports/src/output/webhook_delivery_port.rs — new `# Examples` section"
  key_links:
    - "The ports crate depends only on the core crate plus async-trait, serde, thiserror, uuid, chrono, tokio, serde_json, futures, log, md5 and mime_guess — a doctest's reachable namespace is bounded by that list, not by the workspace."
    - "`TokenCounterPort`'s existing section in the same crate is the working model: a local unit struct, an inline trait implementation, then two assertions against it."
    - "`StructuredExecutorPort` already carries a compiling example proving object safety; it is reported missing only because no heading precedes it, which makes it the cheapest of the nineteen."
    - "`RunSubmissionPort`, `ScheduleAdminPort` and `WebhookDeliveryRepositoryPort` each already contain an internal test-module implementation of their own trait, which is the exact minimal shape to model the doctest on — those modules are not reachable from a doctest, so the shape is copied, never imported."
  prohibitions:
    - statement: "No `.rs` file outside the D-27 list is modified — this plan touches six port trait files' doc comments only."
      status: recalled
      verification: flagged-unverified
    - statement: "No doctest in this plan imports the storage crate or any other crate absent from the ports crate's own dependency list."
      status: recalled
      verification: flagged-unverified
    - statement: "No new `# Examples` section is fenced so that its block is skipped rather than compiled — every section's doctest is reported as passed, never as ignored (D-10)."
      status: recalled
      verification: flagged-unverified
    - statement: "No production item is added, removed or re-signed in any touched file; the diff is doc comments only."
      status: recalled
      verification: flagged-unverified
    - statement: "No rustdoc lint suppression attribute is added to or removed from any file in this phase (D-29)."
      status: recalled
      verification: flagged-unverified
    - statement: "The executor never runs `git push`."
      status: recalled
      verification: flagged-unverified
---

<objective>
Add the first six of the nineteen missing `# Examples` sections: the lighter port traits, where the
inline implementation a doctest needs is short.

Decisions honoured: D-00d (the plural-heading rule was ratified against a frozen seventy-six-item
set; its growth to a hundred and one, the nineteen missing items and the unwired gate were handed to
this phase, which decides the apparatus), D-09 (the disposition is wire, refreeze and fix — not
waive, and not read the frozen list; the script keeps deriving the set live, which is what makes it
a gate), D-10 (a compliant section is a plural heading followed by at least one real doctest that
compiles under the workspace doctest run; a block that needs a live service, key or database may be
marked not-run, but a block that is skipped outright is not allowed, because a heading over a skipped
block is theatre), D-27 (nothing under `src/` or `crates/` changes except the two policy arms, the
widened sanitizer and its test, the nineteen doc comments, and the isolation test), D-28
(`docs(36.1):` subjects).

Purpose: the rule exists so that every public builder, port and service has a worked example. These
six ports shipped in Phases 22 through 27 without one. Freezing the set at an old snapshot would
blind the gate to every future entry point; fixing the items is what closes the gap.

Output: six compliant sections, each proven by a doctest that actually runs.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/36.1-deferred-items-closure/36.1-CONTEXT.md
@.planning/phases/36.1-deferred-items-closure/36.1-RESEARCH.md
@.planning/phases/36.1-deferred-items-closure/36.1-PATTERNS.md
@crates/paladin-ports/src/output/token_counter_port.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: StructuredExecutorPort and NodeCachePort</name>
  <files>crates/paladin-ports/src/output/structured_executor_port.rs, crates/paladin-ports/src/output/node_cache_port.rs</files>
  <read_first>
    - crates/paladin-ports/src/output/token_counter_port.rs lines 45-80 — the working model for this whole plan: heading, plain fence, local unit struct, inline trait implementation, assertions
    - crates/paladin-ports/src/output/structured_executor_port.rs lines 40-95 — the existing example block near 57-62 that already compiles, the trait declaration at line 82, and its two async methods with their argument and return types
    - crates/paladin-ports/src/output/node_cache_port.rs lines 100-175 — the trait declaration at line 135, its two async methods, and the `NodeCacheError` type with its non-exhaustive backend and serialization variants, plus the doc statement that a backend error is never fatal to a run
    - crates/paladin-ports/Cargo.toml — the complete dependency list, which bounds what any doctest in this crate may import
    - .planning/phases/36.1-deferred-items-closure/36.1-RESEARCH.md, the twelve-port inventory table — the per-trait notes for these two
  </read_first>
  <behavior>
    - Running the workspace doctest suite filtered to the structured-executor module reports at least one passed doctest and zero failed.
    - Running it filtered to the node-cache module reports at least one passed doctest and zero failed.
    - The public-API examples script no longer reports either trait as missing.
  </behavior>
  <action>
**`StructuredExecutorPort`.** The trait at line 82 already has a compiling example block above it
that proves object safety. Add the plural `# Examples` heading directly above that existing block,
inside the trait's own doc comment, and leave the block itself alone. This is the cheapest of the
nineteen — do not replace the example, do not expand it, and do not re-fence it.

**`NodeCachePort`.** Add a `# Examples` section to the trait's own doc comment at line 135. The
example declares a local struct holding a map behind a mutex, implements the trait's two async
methods on it inline, then stores a value and reads it back with an assertion. Model the structure on
`TokenCounterPort`'s section: a local type, an inline implementation, then assertions that say
something. Wrap the async body the way a doctest must — a runtime built in the block, or a
`tokio::main` entry function — and use the crate's own tokio dependency rather than adding one. Keep
the example short: construction and one round trip, not a full cache policy.

Never import the storage crate or any crate outside the ports crate's own dependency list — the
doctest's reachable namespace is that list, and a storage import would both fail to resolve and
invert the architecture's dependency direction.

Use a plain compiling fence. If a block genuinely cannot run in a test process, mark it not-run
rather than skipped — but neither of these two needs that: both are pure in-memory.

Run the crate's doctests filtered to each module and confirm each reports a non-zero passed count; a
skipped block reports zero passed and one ignored, which fails the acceptance criteria by design.

Commit as `docs(36.1): add # Examples to StructuredExecutorPort and NodeCachePort (D-10)`. Stage the
two files by explicit path. Use plain `git commit` with a 600-second timeout; do not use the
`gsd-tools` commit helper for a `.rs` commit.
  </action>
  <verify>
    <automated>cargo test -p paladin-ports --doc structured_executor_port 2>&amp;1 | grep -E '^test result: ok\. [1-9][0-9]* passed' &amp;&amp; cargo test -p paladin-ports --doc node_cache_port 2>&amp;1 | grep -E '^test result: ok\. [1-9][0-9]* passed'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p paladin-ports --doc structured_executor_port` prints a line matching `^test result: ok. [1-9][0-9]* passed` with zero failed and zero ignored.
    - `cargo test -p paladin-ports --doc node_cache_port` prints the same shape.
    - `bash scripts/check-public-api-examples.sh --list | grep -E 'StructuredExecutorPort|NodeCachePort'` shows both rows with the satisfied verdict.
    - The structured-executor diff adds a heading line only — the existing example block is byte-identical.
    - The node-cache example declares a local type and implements the port on it; `grep -c 'paladin_storage' crates/paladin-ports/src/output/node_cache_port.rs` is 0.
    - `git diff HEAD~1 -- crates/paladin-ports/` shows changes on comment lines only — no production item added, removed or re-signed.
  </acceptance_criteria>
  <done>Both traits carry a plural heading backed by a doctest the suite actually runs, and the examples script counts them as satisfied.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RunSubmissionPort and ScheduleAdminPort</name>
  <files>crates/paladin-ports/src/input/run_submission_port.rs, crates/paladin-ports/src/input/schedule_admin_port.rs</files>
  <read_first>
    - crates/paladin-ports/src/input/run_submission_port.rs lines 150-260 — the trait declaration at line 180 with its three async methods, and the internal test-module implementation near line 229 that is the exact minimal shape to model on
    - crates/paladin-ports/src/input/schedule_admin_port.rs lines 60-170 — the trait declaration at line 90 with its five async methods, and the internal test-module shape reference near line 143
    - crates/paladin-ports/src/output/token_counter_port.rs lines 45-80 — the section model
    - crates/paladin-ports/Cargo.toml — the dependency bound on what a doctest may import
    - .planning/phases/36.1-deferred-items-closure/36.1-RESEARCH.md, the twelve-port inventory — the note that a request-struct constructor on the admin port is not the port itself and must not be confused with it when writing the example
  </read_first>
  <behavior>
    - The workspace doctest suite filtered to the run-submission module reports at least one passed doctest and zero failed.
    - The same holds for the schedule-admin module.
    - The public-API examples script no longer reports either trait as missing.
  </behavior>
  <action>
Add a `# Examples` section to each trait's own doc comment: the submission port at line 180 and the
schedule-admin port at line 90.

Each example declares a local struct, implements the trait on it inline with every method the trait
declares — a method left out will not compile — and then exercises one method with an assertion. For
the submission port that is the submit path with a returned identifier asserted; for the schedule
port, create followed by get, asserting the retrieved value matches. Both traits already carry an
internal test-module implementation of themselves; read it and copy its shape. That module is not
reachable from a doctest, so copy the shape, never an import.

On the schedule-admin port, note that a constructor near line 46 belongs to a request struct, not to
the port — the example must implement the port trait itself, not construct that struct and call it
an implementation.

Keep every example to construction plus one exercised method. Wrap the async body with the crate's
own tokio dependency. Use a plain compiling fence; do not mark a block skipped.

Commit as `docs(36.1): add # Examples to RunSubmissionPort and ScheduleAdminPort (D-10)`. Stage both
files by explicit path; plain `git commit` with a 600-second timeout.
  </action>
  <verify>
    <automated>cargo test -p paladin-ports --doc run_submission_port 2>&amp;1 | grep -E '^test result: ok\. [1-9][0-9]* passed' &amp;&amp; cargo test -p paladin-ports --doc schedule_admin_port 2>&amp;1 | grep -E '^test result: ok\. [1-9][0-9]* passed'</automated>
  </verify>
  <acceptance_criteria>
    - Each of the two filtered doctest runs prints `^test result: ok. [1-9][0-9]* passed` with zero failed and zero ignored.
    - `bash scripts/check-public-api-examples.sh --list | grep -E 'RunSubmissionPort|ScheduleAdminPort'` shows both rows with the satisfied verdict.
    - Each example implements every method its trait declares and exercises at least one with an assertion whose failure message is readable.
    - `grep -c 'paladin_storage' crates/paladin-ports/src/input/run_submission_port.rs` and the same for the schedule-admin port are both 0.
    - `git diff HEAD~1 -- crates/paladin-ports/` shows changes on comment lines only.
  </acceptance_criteria>
  <done>Both input ports carry a plural heading backed by a running doctest, each built from a local implementation rather than an unreachable import.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RunQueuePort and WebhookDeliveryRepositoryPort, then re-measure the crate</name>
  <files>crates/paladin-ports/src/output/run_queue_port.rs, crates/paladin-ports/src/output/webhook_delivery_port.rs</files>
  <read_first>
    - crates/paladin-ports/src/output/run_queue_port.rs lines 20-170 — the lease-token type's constructor near line 36 and the trait declaration at line 115 with its enqueue, dequeue, lease-extension and acknowledgement methods
    - crates/paladin-ports/src/output/webhook_delivery_port.rs lines 60-160 — the trait declaration at line 78 with its five async methods, and the inline test-double pattern near line 136 to model on
    - crates/paladin-ports/src/output/token_counter_port.rs lines 45-80 — the section model
    - crates/paladin-ports/Cargo.toml — the dependency bound
    - .planning/decisions/0033-cargo-doc-warning-bar.md — the rustdoc bar this crate sits under, so the new sections do not reopen it
  </read_first>
  <behavior>
    - The workspace doctest suite filtered to the run-queue module reports at least one passed doctest and zero failed.
    - The same holds for the webhook-delivery module.
    - `cargo doc -p paladin-ports --no-deps` emits zero warning lines with all six new sections in place.
  </behavior>
  <action>
Add a `# Examples` section to the queue port's doc comment at line 115 and to the webhook-delivery
repository port's at line 78, following the same rules as the previous two tasks: a local struct, an
inline implementation of every declared method, one exercised path with an assertion, a plain
compiling fence, no import outside the crate's own dependency list.

For the queue port, use the lease-token type's own constructor near line 36 rather than fabricating a
token value, and exercise enqueue followed by dequeue. For the webhook-delivery port, exercise
enqueue followed by the per-run listing, asserting the listed count.

Then re-measure the crate with all six sections in place: run the crate's full doctest suite and
`cargo doc -p paladin-ports --no-deps`, and confirm the documentation build emits no warning line.
Phase 36 closed the rustdoc bar for this crate; six new doc blocks are exactly the kind of change
that reopens it, and finding that now rather than in the closing plan is the point of measuring here.
Capture both outputs to
`.planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-05-ports-measure.txt`.

Commit the two sections as `docs(36.1): add # Examples to RunQueuePort and WebhookDeliveryRepositoryPort (D-10)`
and the capture as `docs(36.1): record the ports-crate doctest and rustdoc measurement`. Stage by
explicit path; plain `git commit` with a 600-second timeout.
  </action>
  <verify>
    <automated>cargo test -p paladin-ports --doc run_queue_port 2>&amp;1 | grep -E '^test result: ok\. [1-9][0-9]* passed' &amp;&amp; cargo test -p paladin-ports --doc webhook_delivery_port 2>&amp;1 | grep -E '^test result: ok\. [1-9][0-9]* passed' &amp;&amp; cargo doc -p paladin-ports --no-deps 2>&amp;1 | tee /tmp/36.1-05-doc.txt &amp;&amp; ! grep -q 'warning:' /tmp/36.1-05-doc.txt</automated>
  </verify>
  <acceptance_criteria>
    - Each of the two filtered doctest runs prints `^test result: ok. [1-9][0-9]* passed` with zero failed and zero ignored.
    - `cargo doc -p paladin-ports --no-deps` produces no line containing a warning marker.
    - `cargo test -p paladin-ports --doc` across the whole crate reports zero failed.
    - `bash scripts/check-public-api-examples.sh --list | grep -c MISSING` is 13 or fewer — the six items this plan closed are gone from the missing set.
    - The measurement capture exists, is non-empty, and records both command lines with their output.
    - `git diff HEAD~2 -- crates/paladin-ports/` shows changes on comment lines only.
  </acceptance_criteria>
  <done>Six of the nineteen missing sections are closed, each proven by a running doctest, and the ports crate's rustdoc bar is still at zero.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Created by **this plan**:
- `# Examples` doc sections on `StructuredExecutorPort`, `NodeCachePort`, `RunSubmissionPort`,
  `ScheduleAdminPort`, `RunQueuePort` and `WebhookDeliveryRepositoryPort` (doc comments on existing
  public traits — no new public item)
- Local example types declared inside those doctests (doctest-scope only, not crate items)
- `.planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-05-ports-measure.txt` (new file)

New symbols created elsewhere in Phase 36.1 (so drift verification does not flag them): the
`pub(crate)` visibility of `ToolResultFormatter::sanitize_tool_text` and the `fail_run_`-prefixed
pinning test (36.1-02); the remaining thirteen `# Examples` sections on `RunTracePort`,
`AssistantAdminPort`, `AssistantRepositoryPort`, `RunRepositoryPort`, `RunScheduleRepositoryPort`,
`WaypointPort`, `ScheduleService`, `WebhookDeliveryService`, `RunSubmissionService`,
`RunInspectorService`, `RunEventStreamService`, `AssistantService` and `WaypointRetentionService`
(36.1-06 through 36.1-11); the `make check-api-examples` target, the hook id `check-api-examples`
and the CI lint step named "Check public-API `# Examples` headings" (36.1-11); the files
`36.1-ENTRY-POINTS.md`, `36.1-EVIDENCE.md`, `36.1-CI-EVIDENCE.md` and `COVERAGE.md`; requirement ids
`CURR-16` through `CURR-21`; `WINDOWS.md` rows 39 through 50.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doctest namespace → crate dependency graph | a doctest may only reach its own crate's dependencies, not the workspace |
| example code → reader's own implementation | a reader copies the example into their own adapter |
| doc comment → public API baseline | doc changes must not move the committed surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36.1-17 | Information Disclosure | example placeholder values | medium | mitigate | A documentation example that hardcodes a plausible-looking credential teaches readers to do the same and can be mistaken for a real key. None of these six ports needs a credential; where a placeholder string is required the example uses an obviously fake value in the house style the contributor guide already uses. |
| T-36.1-18 | Spoofing | a heading over a skipped block | high | mitigate | The gate script checks heading text only and never inspects fence attributes, so a skipped block would satisfy the script while teaching nothing. Every acceptance criterion asserts a non-zero passed count from the doctest runner, which a skipped block cannot produce (D-10). |
| T-36.1-19 | Tampering | crate dependency graph | medium | mitigate | Reaching for a storage adapter in a ports doctest tempts an executor into adding a dev-dependency that inverts the architecture's dependency direction. The action forbids the import, the acceptance criteria assert the storage crate name is absent from each touched file, and the ports crate's dependency list is required reading. |
| T-36.1-20 | Tampering | the rustdoc zero-warning bar | medium | mitigate | Six new doc blocks are exactly the change that reopens a closed bar, and the closing plan would discover it too late to be cheap. Task 3 re-measures the crate's documentation build and requires a zero warning count before the plan closes. |
| T-36.1-SC | Tampering | cargo package installs | low | accept | No package-manager install runs; no manifest or lockfile changes. |
</threat_model>

<verification>
- Six filtered doctest runs each report a non-zero passed count with zero failed and zero ignored.
- `cargo doc -p paladin-ports --no-deps` emits no warning line.
- The public-API examples script's missing count has dropped by six.
- Every touched file's diff is confined to comment lines, and none names the storage crate.
- No rustdoc suppression attribute is added or removed anywhere.
- The plan's SUMMARY carries a `## Self-Check` section.
</verification>

<success_criteria>
- Six of the nineteen entry points ROADMAP SC2 names carry a plural heading backed by a doctest the suite runs.
- The ports crate's rustdoc bar is still at zero with the new sections in place.
- No dependency edge was added to the ports crate to make an example work.
</success_criteria>

<output>
Create `.planning/phases/36.1-deferred-items-closure/36.1-05-SUMMARY.md` when done, recording: the commit SHAs, the observed `test result` line for each of the six filtered doctest runs, the crate's documentation-build warning count, the examples script's new missing count, and a `## Self-Check` section.
</output>
