---
phase: 36.1-deferred-items-closure
plan: 07
type: execute
wave: 2
depends_on: ["36.1-01"]
files_modified:
  - crates/paladin-ports/src/output/run_repository_port.rs
  - crates/paladin-ports/src/output/run_schedule_repository_port.rs
  - crates/paladin-ports/src/output/waypoint_port.rs
autonomous: true
requirements: [CURR-17]
must_haves:
  truths:
    - "`RunRepositoryPort`, `RunScheduleRepositoryPort` and `WaypointPort` each carry a plural `# Examples` heading in their own doc block, and the public-API examples script reports each as satisfied rather than missing (D-09, CURR-17)."
    - "Each of the three sections carries at least one doctest that compiles and is reported as passed — not ignored — by `cargo test -p paladin-ports --doc` (D-10)."
    - "Every doctest builds its example from a minimal local struct implementing the trait inline; none imports a storage adapter, because the ports crate has no dependency edge to the storage crate."
    - "After this plan the twelve port traits are all satisfied and the public-API examples script's missing count is 7 — the seven facade services and nothing else."
    - "`cargo doc -p paladin-ports --no-deps` still emits zero warning lines with all twelve sections in place."
  artifacts:
    - "crates/paladin-ports/src/output/run_repository_port.rs — new `# Examples` section"
    - "crates/paladin-ports/src/output/run_schedule_repository_port.rs — new `# Examples` section"
    - "crates/paladin-ports/src/output/waypoint_port.rs — new `# Examples` section"
    - ".planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-07-ports-complete.txt — the all-twelve-ports measurement"
  key_links:
    - "These are the three widest port traits in the crate — seven or more declared methods each — so the inline implementation dominates each example and every declared method must be present for it to compile."
    - "The ports crate's dependency list bounds every doctest's reachable namespace: the core crate plus async-trait, serde, thiserror, uuid, chrono, tokio, serde_json, futures, log, md5 and mime_guess."
    - "`TokenCounterPort`'s existing section in the same crate is the working model: a local type, an inline trait implementation, then assertions."
    - "`WaypointPort`'s example may return empty or successful results from most methods and exercise only the save-then-latest path, which keeps the widest trait's example readable."
  prohibitions:
    - statement: "No `.rs` file outside the D-27 list is modified — this plan touches three port trait files' doc comments only."
      status: recalled
      verification: flagged-unverified
    - statement: "No doctest in this plan imports the storage crate or any other crate absent from the ports crate's own dependency list."
      status: recalled
      verification: flagged-unverified
    - statement: "No new `# Examples` section is fenced so that its block is skipped rather than compiled — every section's doctest is reported as passed, never as ignored (D-10)."
      status: recalled
      verification: flagged-unverified
    - statement: "No production item is added, removed or re-signed in any touched file; the diff is doc comments only."
      status: recalled
      verification: flagged-unverified
    - statement: "No rustdoc lint suppression attribute is added to or removed from any file in this phase (D-29)."
      status: recalled
      verification: flagged-unverified
    - statement: "The executor never runs `git push`."
      status: recalled
      verification: flagged-unverified
---

<objective>
Close the last three of the twelve missing port-trait `# Examples` sections — the run repository, the
schedule repository and the waypoint store, the three widest traits in the crate.

Decisions honoured: D-00d (the plural-heading rule and the drift handed to this phase), D-09 (wire,
refreeze and fix; the script keeps deriving the entry-point set live), D-10 (a compliant section is a
plural heading followed by at least one doctest that compiles under the workspace doctest run;
not-run where a live dependency is needed, never skipped), D-27 (the bounded source-file list), D-28
(`docs(36.1):` subjects).

Purpose: after this plan every port trait in the crate satisfies the rule and the remaining gap is
exactly the seven facade services — a clean, checkable boundary for the plans that follow, and the
number the closing plan's snapshot has to reconcile against.

Output: three compliant sections, a crate with all twelve ports satisfied, and a recorded
measurement proving it.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/36.1-deferred-items-closure/36.1-CONTEXT.md
@.planning/phases/36.1-deferred-items-closure/36.1-RESEARCH.md
@.planning/phases/36.1-deferred-items-closure/36.1-PATTERNS.md
@crates/paladin-ports/src/output/token_counter_port.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RunRepositoryPort</name>
  <files>crates/paladin-ports/src/output/run_repository_port.rs</files>
  <read_first>
    - crates/paladin-ports/src/output/run_repository_port.rs in full — the trait declaration at line 145 and all seven async methods it declares, covering insert, get, status update, outcome recording, listing, the active-run-for-thread lookup and cancellation request, with their argument and return types and the error type
    - crates/paladin-ports/src/output/token_counter_port.rs lines 45-80 — the section model
    - crates/paladin-ports/Cargo.toml — the dependency bound on what a doctest may import
    - .planning/phases/36.1-deferred-items-closure/36.1-RESEARCH.md, the twelve-port inventory row for this trait, which names it the heaviest of the twelve and suggests a map behind a mutex as the backing store
  </read_first>
  <behavior>
    - The workspace doctest suite filtered to the run-repository module reports at least one passed doctest and zero failed.
    - The example exercises insert followed by get and asserts the retrieved record matches what was inserted.
    - The public-API examples script no longer reports this trait as missing.
  </behavior>
  <action>
Add a `# Examples` section to the trait's own doc comment at line 145. The example declares a local
struct holding runs in a map behind a mutex, implements all seven declared methods on it inline, then
exercises insert followed by get with an assertion that the retrieved record matches. Bodies for the
methods the example does not exercise may be trivial but must type-check — a trait implementation
missing a method will not compile, which is the main cost of this trait.

Wrap the async body with the crate's own tokio dependency. Use a plain compiling fence; do not mark
the block skipped. Import nothing outside the ports crate's own dependency list — in particular not
the storage crate, which is not a dependency here and whose use would invert the architecture's
dependency direction.

Commit as `docs(36.1): add # Examples to RunRepositoryPort (D-10)`. Stage the file by explicit path;
plain `git commit` with a 600-second timeout, not the `gsd-tools` commit helper.
  </action>
  <verify>
    <automated>cargo test -p paladin-ports --doc run_repository_port 2>&amp;1 | grep -E '^test result: ok\. [1-9][0-9]* passed'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p paladin-ports --doc run_repository_port` prints `^test result: ok. [1-9][0-9]* passed` with zero failed and zero ignored.
    - `bash scripts/check-public-api-examples.sh --list | grep RunRepositoryPort` shows the satisfied verdict.
    - The example contains an inline implementation with all seven declared methods present and asserts an insert-then-get round trip.
    - `grep -c 'paladin_storage' crates/paladin-ports/src/output/run_repository_port.rs` is 0.
    - `git diff HEAD~1 -- crates/paladin-ports/` shows changes on comment lines only.
  </acceptance_criteria>
  <done>The run repository port carries an example a reader can lift into their own adapter, proven by a doctest the suite runs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RunScheduleRepositoryPort</name>
  <files>crates/paladin-ports/src/output/run_schedule_repository_port.rs</files>
  <read_first>
    - crates/paladin-ports/src/output/run_schedule_repository_port.rs in full — the trait declaration at line 84 and all seven async methods covering insert, get, list, update, delete, the due query and the tick claim, with their argument and return types and the error type
    - crates/paladin-ports/src/output/token_counter_port.rs lines 45-80 — the section model
    - crates/paladin-ports/Cargo.toml — the dependency bound
    - .planning/phases/36.1-deferred-items-closure/36.1-RESEARCH.md, the twelve-port inventory row for this trait
  </read_first>
  <behavior>
    - The workspace doctest suite filtered to the run-schedule-repository module reports at least one passed doctest and zero failed.
    - The example exercises insert followed by list and asserts the listed count.
    - The public-API examples script no longer reports this trait as missing.
  </behavior>
  <action>
Add a `# Examples` section to the trait's own doc comment at line 84. The example declares a local
struct holding schedules in a map behind a mutex, implements all seven declared methods on it inline,
and exercises insert followed by list with an assertion on the listed count. Bodies for unexercised
methods may be trivial but must type-check.

Plain compiling fence, no skipped block, async body wrapped with the crate's own tokio dependency, no
import outside the ports crate's dependency list.

Commit as `docs(36.1): add # Examples to RunScheduleRepositoryPort (D-10)`. Stage the file by
explicit path.
  </action>
  <verify>
    <automated>cargo test -p paladin-ports --doc run_schedule_repository_port 2>&amp;1 | grep -E '^test result: ok\. [1-9][0-9]* passed'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p paladin-ports --doc run_schedule_repository_port` prints `^test result: ok. [1-9][0-9]* passed` with zero failed and zero ignored.
    - `bash scripts/check-public-api-examples.sh --list | grep RunScheduleRepositoryPort` shows the satisfied verdict.
    - The example contains an inline implementation with all seven declared methods present and asserts a listed count after an insert.
    - `grep -c 'paladin_storage' crates/paladin-ports/src/output/run_schedule_repository_port.rs` is 0.
    - `git diff HEAD~1 -- crates/paladin-ports/` shows changes on comment lines only.
  </acceptance_criteria>
  <done>The schedule repository port carries a running example, and eleven of the twelve ports are satisfied.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: WaypointPort, then measure the crate with all twelve ports satisfied</name>
  <files>crates/paladin-ports/src/output/waypoint_port.rs</files>
  <read_first>
    - crates/paladin-ports/src/output/waypoint_port.rs in full — the trait declaration at line 167 and every async method it declares, covering save, latest, get, history, thread listing, thread deletion and waypoint deletion, with their argument and return types and the error type. This is the widest trait in the crate; read the whole declaration before writing anything
    - crates/paladin-ports/src/output/token_counter_port.rs lines 45-80 — the section model
    - crates/paladin-ports/Cargo.toml — the dependency bound
    - .planning/decisions/0033-cargo-doc-warning-bar.md — the rustdoc bar this crate sits under
    - .planning/phases/36.1-deferred-items-closure/36.1-RESEARCH.md, the twelve-port inventory row for this trait, which allows the example to return empty or successful results from most methods and exercise only the save-then-latest path
  </read_first>
  <behavior>
    - The workspace doctest suite filtered to the waypoint module reports at least one passed doctest and zero failed.
    - The example exercises save followed by latest and asserts the returned waypoint is the one saved.
    - `cargo doc -p paladin-ports --no-deps` emits zero warning lines with all twelve sections in place.
    - The public-API examples script reports no port trait as missing; the remaining missing count is exactly 7.
  </behavior>
  <action>
Add a `# Examples` section to the trait's own doc comment at line 167. The example declares a local
struct holding waypoints per thread in a map behind a mutex, implements every declared method on it
inline, and exercises save followed by latest with an assertion that the returned waypoint is the one
saved. Every other method may return an empty or successful result — the trait is wide, and a
readable example beats an exhaustive one — but each must be present and must type-check.

Plain compiling fence, no skipped block, async body wrapped with the crate's own tokio dependency, no
import outside the ports crate's dependency list.

Then measure the crate with all twelve port sections in place: run the crate's full doctest suite,
run `cargo doc -p paladin-ports --no-deps`, and run the public-API examples script in report mode.
Confirm the documentation build emits no warning line and that the script's missing count is exactly
7 — the seven facade services, and no port. If it is anything other than 7, stop and record the
discrepancy in the SUMMARY rather than adjusting a number to match: the count is the contract the
closing plan's snapshot reconciles against. Capture all three outputs to
`.planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-07-ports-complete.txt`.

Commit the section as `docs(36.1): add # Examples to WaypointPort (D-10)` and the capture as
`docs(36.1): record the all-ports measurement (D-09)`. Stage by explicit path.
  </action>
  <verify>
    <automated>cargo test -p paladin-ports --doc waypoint_port 2>&amp;1 | grep -E '^test result: ok\. [1-9][0-9]* passed' &amp;&amp; test "$(bash scripts/check-public-api-examples.sh --list | grep -c MISSING)" = 7 &amp;&amp; cargo doc -p paladin-ports --no-deps 2>&amp;1 | tee /tmp/36.1-07-doc.txt &amp;&amp; ! grep -q 'warning:' /tmp/36.1-07-doc.txt</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p paladin-ports --doc waypoint_port` prints `^test result: ok. [1-9][0-9]* passed` with zero failed and zero ignored.
    - `bash scripts/check-public-api-examples.sh --list | grep -c MISSING` is exactly 7, and none of the seven is a port trait.
    - `cargo doc -p paladin-ports --no-deps` produces no line containing a warning marker.
    - `cargo test -p paladin-ports --doc` across the whole crate reports zero failed.
    - The example contains an inline implementation with every declared method present and asserts a save-then-latest round trip.
    - The measurement capture records all three command lines with their output.
    - `git diff HEAD~2 -- crates/paladin-ports/` shows changes on comment lines only.
  </acceptance_criteria>
  <done>All twelve port traits satisfy the plural-heading rule with running doctests, and the remaining gap is exactly the seven facade services.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Created by **this plan**:
- `# Examples` doc sections on `RunRepositoryPort`, `RunScheduleRepositoryPort` and `WaypointPort`
  (doc comments on existing public traits — no new public item)
- Local example types declared inside those doctests (doctest-scope only, not crate items)
- `.planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-07-ports-complete.txt` (new file)

New symbols created elsewhere in Phase 36.1 (so drift verification does not flag them): the
`pub(crate)` visibility of `ToolResultFormatter::sanitize_tool_text` and the `fail_run_`-prefixed
pinning test (36.1-02); the other sixteen `# Examples` sections listed in plan 36.1-05's artifacts
section (36.1-05, 36.1-06, 36.1-08 through 36.1-11); the `make check-api-examples` target, the hook
id `check-api-examples` and the CI lint step named "Check public-API `# Examples` headings"
(36.1-11); the files `36.1-ENTRY-POINTS.md`, `36.1-EVIDENCE.md`, `36.1-CI-EVIDENCE.md` and
`COVERAGE.md`; requirement ids `CURR-16` through `CURR-21`; `WINDOWS.md` rows 39 through 50.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doctest namespace → crate dependency graph | a doctest may only reach its own crate's dependencies, not the workspace |
| example code → reader's own implementation | a reader copies the example into their own adapter |
| measured missing count → the closing plan's snapshot | the closing plan reconciles its refrozen record against this number |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36.1-25 | Spoofing | a heading over a skipped block | high | mitigate | The gate script checks heading text only and never inspects fence attributes. Every acceptance criterion asserts a non-zero passed count from the doctest runner, which a skipped block cannot produce (D-10). |
| T-36.1-26 | Tampering | crate dependency graph | medium | mitigate | These three are the traits whose examples most tempt a reach for a shipped in-memory adapter. The action forbids it, the acceptance criteria assert the storage crate name is absent, and the dependency list is required reading. |
| T-36.1-27 | Repudiation | the remaining missing count | high | mitigate | A count quietly adjusted to match an expectation would corrupt the closing plan's reconciliation. The action requires the executor to stop and record any figure other than 7 rather than reconcile it, and the verify asserts the exact value. |
| T-36.1-28 | Tampering | the rustdoc zero-warning bar | medium | mitigate | Twelve new doc blocks across one crate is the change most likely to reopen a closed bar. Task 3 re-measures the documentation build with all twelve in place and requires a zero warning count. |
| T-36.1-SC | Tampering | cargo package installs | low | accept | No package-manager install runs; no manifest or lockfile changes. |
</threat_model>

<verification>
- Three filtered doctest runs each report a non-zero passed count with zero failed and zero ignored.
- The public-API examples script's missing count is exactly 7 and contains no port trait.
- `cargo doc -p paladin-ports --no-deps` emits no warning line.
- Every touched file's diff is confined to comment lines, and none names the storage crate.
- No rustdoc suppression attribute is added or removed anywhere.
- The plan's SUMMARY carries a `## Self-Check` section.
</verification>

<success_criteria>
- All twelve port traits ROADMAP SC2 names carry a plural heading backed by a doctest the suite runs.
- The remaining gap is exactly the seven facade services, measured and recorded rather than assumed.
- The ports crate's rustdoc bar is still at zero.
</success_criteria>

<output>
Create `.planning/phases/36.1-deferred-items-closure/36.1-07-SUMMARY.md` when done, recording: the commit SHAs, the observed `test result` line for each of the three filtered doctest runs, the exact missing count with the seven remaining item names, the crate's documentation-build warning count, and a `## Self-Check` section.
</output>
