---
phase: 36.1-deferred-items-closure
plan: 11
type: execute
wave: 3
depends_on: ["36.1-05", "36.1-06", "36.1-07", "36.1-08", "36.1-09", "36.1-10"]
files_modified:
  - src/application/services/waypoint_retention.rs
  - Makefile
  - .pre-commit-config.yaml
  - .github/workflows/ci.yml
  - .planning/phases/36.1-deferred-items-closure/36.1-ENTRY-POINTS.md
  - .planning/codebase/CONVENTIONS.md
  - scripts/check-public-api-examples.sh
autonomous: true
requirements: [CURR-17]
must_haves:
  truths:
    - "`WaypointRetentionService` carries a plural `# Examples` heading backed by a doctest reported as passed, and the public-API examples script in gate mode exits 0 with 101 entry points, 101 satisfied and 0 missing (D-09, D-10, CURR-17)."
    - "The nineteenth fix and the gate wiring land in a single commit, so no commit in this phase's history has the gate wired and failing (D-12, Phase 36 ordering precedent)."
    - "`make check-api-examples` runs the script in gate mode with a labelled echo in the Makefile's own house style, and `clean-code` depends on it (D-12)."
    - "`.pre-commit-config.yaml` carries a `check-api-examples` hook at the pre-push stage directly after the rustdoc hook, with the same source-file filter and a comment in the same voice (D-12)."
    - "The CI lint job carries one new step named for the public-API heading check, placed directly after the all-features documentation step (D-12)."
    - "`36.1-ENTRY-POINTS.md` records the 101-row derived table verbatim at closure, with a header naming the generating commit and command and stating that the live derivation is the enforcement and the file is the snapshot (D-11)."
    - "Both pointers to the archived seventy-six-item freeze — the codebase conventions paragraph and the script's own header comment — now cite the new snapshot instead, and the archived Phase 16 file itself is unmodified (D-11)."
  artifacts:
    - "src/application/services/waypoint_retention.rs — the nineteenth `# Examples` section"
    - "Makefile — new `check-api-examples` target and an extended `clean-code` dependency list"
    - ".pre-commit-config.yaml — new pre-push hook"
    - ".github/workflows/ci.yml — new lint-job step"
    - ".planning/phases/36.1-deferred-items-closure/36.1-ENTRY-POINTS.md — the 101-row snapshot"
    - ".planning/codebase/CONVENTIONS.md — re-pointed heading-rule paragraph"
    - "scripts/check-public-api-examples.sh — re-pointed header comment"
  key_links:
    - "The rustdoc bar's three-place wiring is the exact pattern to copy: the Makefile target with its labelled echo, the `clean-code` dependency list, the pre-push hook with its source-file filter, and the CI lint step — placement and voice, not just intent."
    - "The script's gate mode exits non-zero on any missing or singular heading and also on a degenerate derivation of zero entry points, so wiring it is wiring a real gate rather than a reporting step."
    - "The script's report mode always exits 0 and is not a gate — the Makefile target must invoke gate mode, not report mode."
    - "The conventions paragraph is stale in two ways at once: it cites a file path that no longer exists at that location, and a count that is now 101; the snapshot replaces both."
    - "A change to the CI lint job is a change to a required check, so a real run on the pushed branch is recorded in `36.1-CI-EVIDENCE.md` by plan 36.1-14 before the phase is verified."
  prohibitions:
    - statement: "No `.rs` file outside the D-27 list is modified — the only source change here is the retention service's doc comment."
      status: recalled
      verification: flagged-unverified
    - statement: "No commit in this plan leaves the gate wired while the script fails — the nineteenth fix and the wiring are one commit."
      status: recalled
      verification: flagged-unverified
    - statement: "The archived Phase 16 entry-point freeze file is read but never edited (D-11)."
      status: recalled
      verification: flagged-unverified
    - statement: "The snapshot file is generated from the script's own report mode, never hand-assembled or abridged."
      status: recalled
      verification: flagged-unverified
    - statement: "No new `# Examples` section is fenced so that its block is skipped rather than compiled (D-10)."
      status: recalled
      verification: flagged-unverified
    - statement: "No rustdoc lint suppression attribute is added to or removed from any file in this phase (D-29)."
      status: recalled
      verification: flagged-unverified
    - statement: "The executor never runs `git push` — the branch push is the maintainer's action at the plan 36.1-14 checkpoint."
      status: recalled
      verification: flagged-unverified
---

<objective>
Close the last missing entry point and wire the gate that stops the drift from recurring — in one
commit — then record the refrozen set and re-point the two references that still describe the old
one.

Decisions honoured: D-00d (the rule's frozen set, its drift and its unwired gate were handed to this
phase to decide), D-00g (the rustdoc bar's placement in the code-quality target, the pre-push hook
and the CI lint job is the pattern the new gate copies, in placement and in voice), D-09 (wire,
refreeze and fix; the script keeps deriving the set live, which is what makes it a gate, and the
refrozen baseline is the record of the set at closure, not a list the script reads), D-10 (a
compliant section is a plural heading backed by a real compiling doctest), D-11 (the record is a new
snapshot file generated from the script's report mode at closure, with two pointers updated to it and
the archived Phase 16 file left untouched), D-12 (the exact three-place wiring, and the ordering rule
that the wiring lands in the same commit as the nineteenth fix so no commit is wired and red), D-27
(the bounded source-file list), D-28 (`docs(36.1):` for the doc section and the snapshot,
`build(36.1):` for the make, hook and CI wiring).

Purpose: the rule was ratified against a seventy-six-item set and the tree grew to a hundred and one
while nothing ran the check. Fixing the nineteen without wiring the gate would leave the same silent
drift ahead of us; wiring the gate without fixing the nineteen would put a red gate on main. Both, in
one commit, is the only ordering that avoids either.

Output: a satisfied gate, wired in three places, with the closing set recorded and the stale pointers
retired.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/36.1-deferred-items-closure/36.1-CONTEXT.md
@.planning/phases/36.1-deferred-items-closure/36.1-RESEARCH.md
@.planning/phases/36.1-deferred-items-closure/36.1-PATTERNS.md
@.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-12-SUMMARY.md
</context>

<tasks>

<!-- planner-discipline-allow: 16-DOCS-03-ENTRY-POINTS -->

<task type="auto" tdd="true">
  <name>Task 1: The nineteenth section and the three-place gate wiring, in one commit</name>
  <files>src/application/services/waypoint_retention.rs, Makefile, .pre-commit-config.yaml, .github/workflows/ci.yml</files>
  <precondition>Plans 36.1-05 through 36.1-10 are all committed on this branch and `bash scripts/check-public-api-examples.sh --list | grep -c MISSING` reports exactly 1, naming the retention service. If it reports any other number, halt and report — the single-commit ordering rule this task implements is not satisfiable otherwise.</precondition>
  <read_first>
    - src/application/services/waypoint_retention.rs lines 60-120 — the struct declaration at line 73, its constructor taking the waypoint port and a retention configuration, and that configuration type's own construction path in the same file
    - crates/paladin-storage/src/waypoint/in_memory.rs lines 30-55 — the shipped in-memory waypoint store and its constructor
    - Makefile lines 420-440 — the rustdoc target with its labelled echo and the `clean-code` dependency list, which the new target and dependency copy
    - .pre-commit-config.yaml lines 118-145 — the api-surface and rustdoc pre-push hooks: their comment voice, their stage, their filename filter and their pass-filenames setting
    - .github/workflows/ci.yml lines 55-75 — the lint job's two documentation steps, the second of which the new step follows, and the comment style those steps carry
    - scripts/check-public-api-examples.sh lines 1-40 — the usage block distinguishing gate mode from report mode, and the exit contract of each
    - .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-12-SUMMARY.md — how the previous phase performed the identical three-place wiring, including what it staged and in what order
  </read_first>
  <behavior>
    - The workspace doctest suite filtered to the retention service reports at least one passed doctest and zero failed.
    - The public-API examples script in gate mode exits 0.
    - `make check-api-examples` exits 0 and prints a labelled line in the Makefile's house style.
    - Running the pre-push hook set against the changed files exits 0.
    - Every commit in this task's history has the gate either unwired, or wired and passing — never wired and failing.
  </behavior>
  <action>
Do all four edits, verify them together, and make **one** commit. This ordering is the decision, not
a convenience: a commit that wires the gate while the script still fails would sit in the branch's
history as a red gate, which is what the previous phase's own ordering rule exists to prevent.

**The nineteenth section.** Add a `# Examples` section to the retention service's doc comment at
line 73. The example constructs the shipped in-memory waypoint store, builds the retention
configuration by whatever construction path that type actually offers — read the same file for it,
research did not verify one — and constructs the service from the two. Construction plus at most one
cheap call; plain compiling fence if the body runs, the not-run form otherwise, never a skipped
block. Do not add a production constructor or a derived default to make the example compile.

**The Makefile.** Add a `check-api-examples` target that echoes a labelled line in the same
colour-variable style the rustdoc target uses and then runs the script in **gate** mode — not report
mode, which always exits 0 and is not a gate. Declare it phony beside the neighbouring targets. Then
extend the `clean-code` dependency list so it reads, in order: formatting, linting, shell linting,
checking, the rustdoc bar, and the new examples gate.

**The hook.** Add an entry to `.pre-commit-config.yaml` immediately after the rustdoc entry, with the
id `check-api-examples`, a name describing the public-API heading check, the new make target as its
entry, the system language, the pre-push stage, pass-filenames disabled, and the same source-file
filter the two neighbouring hooks use. Precede it with a short comment in the same voice as its
neighbours, explaining why it sits at pre-push rather than pre-commit.

**The CI step.** Add one step to the lint job, placed directly after the all-features documentation
step, named for the public-API heading check, running the script. One explicit step — do not fold it
into an existing step or build a matrix.

**Verify together, then commit once.** Run the script in gate mode, run `make check-api-examples`,
run the retention service's doctest, and run the hook set against the changed files. Only when all
four are green, stage the four files by explicit path and make a single commit:
`build(36.1): wire the public-API # Examples gate and close the last entry point (D-12)`. Use plain
`git commit` with a 600-second timeout; do not use the `gsd-tools` commit helper for a commit
carrying a `.rs` file.
  </action>
  <verify>
    <automated>cargo test -p paladin-ai --doc WaypointRetentionService 2>&amp;1 | grep -E '^test result: ok\. [1-9][0-9]* passed' &amp;&amp; bash scripts/check-public-api-examples.sh &amp;&amp; make check-api-examples &amp;&amp; test "$(grep -c 'check-api-examples' Makefile)" -ge 2 &amp;&amp; test "$(grep -c 'check-api-examples' .pre-commit-config.yaml)" -ge 1 &amp;&amp; grep -q 'Examples' .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p paladin-ai --doc WaypointRetentionService` prints `^test result: ok. [1-9][0-9]* passed` with zero failed and zero ignored.
    - `bash scripts/check-public-api-examples.sh` exits 0 and its total line reads 101 entry points with 101 satisfied, 0 missing and 0 singular.
    - `make check-api-examples` exits 0 and prints a labelled line matching the Makefile's existing colour-variable echo style.
    - `grep -n 'clean-code:' Makefile` shows the new target present in the dependency list after the rustdoc bar.
    - `.pre-commit-config.yaml` contains a hook whose id is `check-api-examples`, at the pre-push stage, with the same filename filter as the two hooks above it, and it appears after the rustdoc hook in file order.
    - `.github/workflows/ci.yml`'s lint job contains one new step placed after the all-features documentation step, and `grep -c 'check-public-api-examples' .github/workflows/ci.yml` is 1.
    - `git log -1 --name-only` for this task lists exactly four files: the retention service, the Makefile, the pre-commit config and the CI workflow — one commit, not four.
    - `git log -1 --pretty=%s` names D-12.
  </acceptance_criteria>
  <reversibility rating="costly">Once this lands on a merged branch, every pull request gates on the public-API heading check; removing it later is a visible weakening of a ratified bar rather than a neutral revert (D-12).</reversibility>
  <done>The gate is wired in the same three places the rustdoc bar is, it passes, and no commit exists in which it is wired and red.</done>
</task>

<task type="auto">
  <name>Task 2: Record the refrozen entry-point set and re-point the two stale references</name>
  <files>.planning/phases/36.1-deferred-items-closure/36.1-ENTRY-POINTS.md, .planning/codebase/CONVENTIONS.md, scripts/check-public-api-examples.sh</files>
  <read_first>
    - .planning/milestones/v0.8.0-phases/16-documentation-currency-the-architecture-gap/16-DOCS-03-ENTRY-POINTS.md — the archived seventy-six-item freeze: read its header and table format as the model for the new file, and never edit it
    - .planning/codebase/CONVENTIONS.md lines 138-152 — the heading-rule paragraph, which is stale twice over: it cites the archived file at a path that no longer holds it, and it states a count of seventy-six
    - scripts/check-public-api-examples.sh lines 1-15 — the header comment's own citation of that same archived file
    - .planning/phases/36.1-deferred-items-closure/36.1-CONTEXT.md decision D-11 — the exact contents the snapshot header must state
  </read_first>
  <action>
**The snapshot.** Generate
`.planning/phases/36.1-deferred-items-closure/36.1-ENTRY-POINTS.md` from the script's **report**
mode, run at this plan's head commit. Open the file with the exact command that produced it and the
commit SHA it ran against, then a short header stating plainly that the script derives the entry-point
set live on every run and that this file is the point-in-time snapshot rather than a list the script
reads — so a future reader diffs a fresh report against it instead of treating it as the source of
truth. Then the full derived table, all 101 rows, verbatim: kind, item, file and line, verdict, with
every verdict satisfied. Do not abridge, summarise or re-sort the rows; a reader must be able to diff
a future report against this file directly.

**The conventions pointer.** Rewrite the heading-rule paragraph's citation so it names the new
snapshot file and the count of 101, and drop the archived Phase 16 citation and every stale mention
of seventy-six. Keep the rest of the paragraph — the plural spelling rule, the reason the rule is
scoped narrowly, and the statement that the script is the mechanical enforcer because no stable-Rust
lint performs this check — and add one clause noting the check is now wired into the code-quality
target, the pre-push hook and the CI lint job. Change nothing else in that file; a full refresh of the
conventions document is out of scope.

**The script header.** Rewrite the header comment's citation the same way: name the new snapshot and
drop the archived Phase 16 path. Leave the rest of the header — the rule statement, the stable-Rust
lint explanation and the usage and exit contract — as it is.

Because the script is a tracked shell file and this task changes it, stage it **before** running the
shell linter: that linter scans tracked files only, so an unstaged change reads as clean. Run
`make lint-shell` after staging and confirm it passes.

Commit as `docs(36.1): record the refrozen entry-point set and re-point its references (D-11)`. Stage
the three files by explicit path.
  </action>
  <verify>
    <automated>test "$(grep -cE '\bOK\b' .planning/phases/36.1-deferred-items-closure/36.1-ENTRY-POINTS.md)" -ge 101 &amp;&amp; test "$(grep -c '16-DOCS-03-ENTRY-POINTS' .planning/codebase/CONVENTIONS.md)" = 0 &amp;&amp; test "$(grep -c '16-DOCS-03-ENTRY-POINTS' scripts/check-public-api-examples.sh)" = 0 &amp;&amp; grep -q '36.1-ENTRY-POINTS' .planning/codebase/CONVENTIONS.md &amp;&amp; grep -q '36.1-ENTRY-POINTS' scripts/check-public-api-examples.sh &amp;&amp; make lint-shell</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/36.1-deferred-items-closure/36.1-ENTRY-POINTS.md` contains at least 101 rows each carrying the satisfied verdict, and zero rows carrying any other verdict.
    - The file's header names the exact command that generated it and the commit SHA it ran at, and states that live derivation is the enforcement and the file is a snapshot.
    - Neither `.planning/codebase/CONVENTIONS.md` nor `scripts/check-public-api-examples.sh` still cites the archived Phase 16 enumeration, and both cite the new snapshot.
    - The conventions paragraph states the count 101 and no longer states 76, and names the three places the check is now wired.
    - `git diff .planning/codebase/CONVENTIONS.md` touches only the heading-rule paragraph.
    - The archived Phase 16 file is unmodified — `git status --porcelain -- .planning/milestones/` prints nothing.
    - `make lint-shell` exits 0 with the script's change staged.
  </acceptance_criteria>
  <done>The set at closure is on disk in a form a future reader can diff against, and both references that described the old seventy-six-item set now describe the real one.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Created by **this plan**:
- The nineteenth `# Examples` doc section, on `WaypointRetentionService` (doc comment on an existing
  public struct — no new public item)
- Makefile target `check-api-examples`, and its addition to the `clean-code` dependency list
- `.pre-commit-config.yaml` hook id `check-api-examples` (pre-push stage)
- A new step in the CI lint job named for the public-API `# Examples` heading check
- `.planning/phases/36.1-deferred-items-closure/36.1-ENTRY-POINTS.md` (new file — the 101-row snapshot)
- Re-pointed citations in `.planning/codebase/CONVENTIONS.md` and in the header comment of
  `scripts/check-public-api-examples.sh`

New symbols created elsewhere in Phase 36.1 (so drift verification does not flag them): the
`pub(crate)` visibility of `ToolResultFormatter::sanitize_tool_text` and the `fail_run_`-prefixed
pinning test (36.1-02); the other eighteen `# Examples` sections (36.1-05 through 36.1-10); the files
`36.1-EVIDENCE.md`, `36.1-CI-EVIDENCE.md` and `COVERAGE.md`; requirement ids `CURR-16` through
`CURR-21`; `WINDOWS.md` rows 39 through 50.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local gate → required CI check | a step added to the lint job becomes a gate on every future pull request |
| script report mode → snapshot file | the recorded baseline must be what the tool produced, not what a human typed |
| snapshot file → future readers | two references point readers at the recorded set |
| branch history → reviewers | a commit in which the gate is wired and failing is itself a defect |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36.1-44 | Denial of Service | the CI lint job | high | mitigate | Wiring a failing gate into a required check blocks every pull request on the branch. The precondition requires exactly one item outstanding before the task starts, the action requires all four verifications green before a single commit, and the acceptance criteria require the commit to carry all four files at once. Plan 36.1-14 records a real run on the pushed branch before the phase is verified. |
| T-36.1-45 | Spoofing | the Makefile target's invocation mode | high | mitigate | The script's report mode always exits 0 and is explicitly not a gate; a target wired to report mode would look green forever while checking nothing. The action names gate mode explicitly, the required reading includes the script's own exit contract, and the verify runs the target and the script separately. |
| T-36.1-46 | Tampering | the snapshot file | medium | mitigate | A hand-assembled or abridged baseline cannot be diffed against a future report, which is the only thing the file is for. The action requires generation from report mode with the command and SHA recorded, and a prohibition forbids hand assembly. |
| T-36.1-47 | Repudiation | the archived Phase 16 freeze | low | mitigate | Editing the archived file to "update" it would rewrite a closed milestone's record. It is required reading and explicitly never edited; acceptance asserts the milestones tree is untouched. |
| T-36.1-48 | Tampering | shell lint coverage | medium | mitigate | The shell linter scans tracked files only, so an unstaged script change reads as clean and a real finding is missed. The action requires staging the script before running the linter and the verify runs it after. |
| T-36.1-SC | Tampering | cargo/npm package installs | low | accept | No package-manager install runs; no manifest or lockfile changes. |
</threat_model>

<verification>
- The retention service's doctest reports a non-zero passed count with zero failed and zero ignored.
- The public-API examples script in gate mode exits 0 at 101 satisfied, 0 missing, 0 singular.
- `make check-api-examples` exits 0 and `clean-code` depends on it.
- The pre-push hook set exits 0 against the changed files, and the CI lint job carries exactly one new step invoking the script.
- The nineteenth fix and the wiring are one commit carrying exactly four files.
- The snapshot has 101 satisfied rows and both stale references now cite it; the archived Phase 16 file is unmodified.
- `make lint-shell` exits 0 with the script staged.
- The plan's SUMMARY carries a `## Self-Check` section.
</verification>

<success_criteria>
- ROADMAP SC2's disposition is delivered in full: the nineteen items fixed, the set refrozen as a record, and the script wired where the rustdoc bar is wired.
- No commit in the phase's history has the gate wired and failing.
- The two references that described the retired seventy-six-item set now describe the real one.
</success_criteria>

<output>
Create `.planning/phases/36.1-deferred-items-closure/36.1-11-SUMMARY.md` when done, recording: the single wiring commit's SHA with its four-file list, the script's gate-mode total line, the `make check-api-examples` output, the three wiring locations with their line numbers, the snapshot's row count and generating command, the two re-pointed references, the shell-lint result, and a `## Self-Check` section.
</output>
