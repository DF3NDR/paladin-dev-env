---
phase: 36.1-deferred-items-closure
plan: 12
type: execute
wave: 4
depends_on: ["36.1-02", "36.1-03", "36.1-04", "36.1-11"]
files_modified:
  - .planning/PROJECT.md
  - .planning/decisions/0033-cargo-doc-warning-bar.md
  - .planning/todos/pending/2026-08-13-verify-local-coverage-reproduction.md
  - .planning/todos/pending/2026-09-13-evaluate-rustfs-replacement-for-minio.md
  - .planning/REQUIREMENTS.md
  - CHANGELOG.md
autonomous: true
requirements: [CURR-17, CURR-20, CURR-21]
must_haves:
  truths:
    - "Both places `.planning/PROJECT.md` states the stale examples figures carry a dated in-place correction in the file's own house style: 62 source files, 8 declared example targets, and the one crate that does ship its own examples directory named (D-13, CURR-17)."
    - "`.planning/decisions/0033-cargo-doc-warning-bar.md` carries a dated amendment section listing all eight crate-level rustdoc suppressions with file, line and lint, their per-crate hidden diagnostic counts, the structural reason each is hard to clear in isolation, and the plain statement that the bar measures the tree with those suppressions in force (D-29)."
    - "No `.rs` file changes for the suppression disposition — the eight attributes stay exactly as committed and the workspace documentation build stays at zero warnings (D-29)."
    - "Both `todos/pending/` files stay where they are and gain an owner, a deferred-past marker, a re-check date and a dispositioned-by marker in frontmatter, plus a dated disposition section stating what was verified now, what remains blocked and why, and the concrete re-check trigger (D-00k, D-22)."
    - "`gsd-tools list-todos` still reports both todos with their original title, area and severity, so the deferral does not hide them (D-22)."
    - "`.planning/REQUIREMENTS.md` carries a v2 platform-and-tooling line naming the object-store adapter evaluation as a v0.11.0 candidate with a pointer to its todo file (D-24)."
    - "`CHANGELOG.md`'s `[0.10.0]` section carries one new documentation bullet and one new fixed bullet, both reader-facing, appended to the existing subsections, naming no register entry, ledger row or requirement identifier (D-00i, D-26)."
  artifacts:
    - ".planning/PROJECT.md — two dated corrections"
    - ".planning/decisions/0033-cargo-doc-warning-bar.md — dated known-suppressions amendment"
    - ".planning/todos/pending/2026-08-13-verify-local-coverage-reproduction.md — frontmatter fields and a disposition section"
    - ".planning/todos/pending/2026-09-13-evaluate-rustfs-replacement-for-minio.md — frontmatter fields and a disposition section"
    - ".planning/REQUIREMENTS.md — one new v2 platform-and-tooling line"
    - "CHANGELOG.md — one documentation bullet and one fixed bullet under the current release section"
  key_links:
    - "The requirement identifiers CURR-16 through CURR-21 were minted into `.planning/REQUIREMENTS.md` and written to the ROADMAP phase entry at planning time; this plan adds only the v2 line, and must not renumber or restate them."
    - "The todo-listing tool reads only the created, title, area and severity frontmatter keys, so the four added keys are invisible to it and both todos keep surfacing."
    - "This environment has no container runtime, so the coverage walk cannot be performed here at all — the disposition is a re-homing with a date, not a deferral of something that could have been done."
    - "The measured per-crate hidden diagnostic counts for the amendment are recorded verbatim in the Phase 36 closing measurement capture; take them from there rather than re-measuring."
    - "The existing changelog subsections for this release already exist and already carry entries from two prior phases — this plan appends to both, creating neither."
  prohibitions:
    - statement: "No `.rs` file is modified by this plan — it edits planning corpus and the changelog only (D-27)."
      status: recalled
      verification: flagged-unverified
    - statement: "No rustdoc lint suppression attribute is added to or removed from any file; the eight are kept and justified, not cleared (D-29)."
      status: recalled
      verification: flagged-unverified
    - statement: "Neither todo file is moved out of the pending directory, renamed, or given a phase-resolution tag — both must outlive this milestone (D-22)."
      status: recalled
      verification: flagged-unverified
    - statement: "No changelog bullet names a register entry, a ledger row id or a requirement identifier — a reader has no access to those (D-00i)."
      status: recalled
      verification: flagged-unverified
    - statement: "No sentence in the project corpus file is touched other than the two stale figure statements (D-13)."
      status: recalled
      verification: flagged-unverified
    - statement: "`.planning/WINDOWS.md` is not touched by this plan; all ledger mutation is plan 36.1-13's work (D-18)."
      status: recalled
      verification: flagged-unverified
    - statement: "The executor never runs `git push`."
      status: recalled
      verification: flagged-unverified
---

<objective>
Do the phase's written work that is not a ledger row: correct the two stale corpus statements, write
down why eight rustdoc suppressions are being kept rather than cleared, disposition the two pending
todos with an owner and a date, and tell a changelog reader what changed.

Decisions honoured: D-00c (the requirement prefix and the one-per-success-criterion minting rule —
the identifiers were minted at planning time and this plan neither renumbers nor restates them, it
only adds the v2 candidate line), D-00i (the current release section already carries both subsections
this plan appends to; it creates neither), D-00k (the fold rule that was set aside in five prior
phases to avoid two owners for one item — this phase is the owner the roadmap names, so both todos
are folded here), D-13 (the corpus correction is made in place at both occurrences, in the file's own
dated-correction house style, with the three figures named), D-22 (the deferral form: the file stays
in the pending directory and gains four frontmatter keys plus a dated disposition section), D-23 (the
coverage walk is re-homed to the maintainer with a re-check date, and its comparison target is
updated from the historic figure to whatever the pre-merge CI coverage run records), D-24 (the
object-store evaluation is re-homed likewise and additionally gets a v2 requirement line), D-26 (one
documentation bullet and one fixed bullet, reader-facing, with no internal identifier), D-29 (the
eight crate-level suppressions are kept and justified through a dated amendment, with no source file
changed and the documentation build still at zero), D-28 (`docs(36.1):` for corpus and changelog,
`chore(36.1):` for the todo mutations).

Purpose: every item here is something a later reader will otherwise have to rediscover — why a
sentence was wrong, why 309 hidden diagnostics were acceptable for this tag, who owns a walk nobody
could do in this environment, and what a user of the release actually gets.

Output: two corrected statements, one dated amendment, two dispositioned todos, one v2 line, and two
changelog bullets.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/36.1-deferred-items-closure/36.1-CONTEXT.md
@.planning/phases/36.1-deferred-items-closure/36.1-RESEARCH.md
@.planning/phases/36.1-deferred-items-closure/36.1-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct both stale corpus statements and amend the rustdoc-bar decision record</name>
  <files>.planning/PROJECT.md, .planning/decisions/0033-cargo-doc-warning-bar.md</files>
  <read_first>
    - .planning/PROJECT.md lines 24-36 — the Phase 4 amendment block carrying the three stale figures, and the dated-correction house style used elsewhere in the same file
    - .planning/PROJECT.md lines 595-612 — the validated-requirements bullet repeating the same three figures
    - .planning/decisions/0033-cargo-doc-warning-bar.md in full, and especially its existing dated amendment section — the heading and dating convention the new section copies
    - .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/deferred-items.md — the register entry naming the eight attributes, their option (b) disposition and the measurement it cites
    - .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-12-closing-measurement.txt — the verbatim capture behind the per-crate hidden counts; take the figures from here rather than re-measuring
    - The eight attribute sites themselves, read-only, to confirm file and line before writing them down: src/lib.rs lines 115-121, crates/paladin-llm/src/lib.rs line 49, crates/paladin-ports/src/lib.rs lines 51-52 including its own explanatory comment, crates/paladin-storage/src/lib.rs line 19 and crates/paladin-notifications/src/lib.rs line 14
  </read_first>
  <action>
**The corpus corrections.** The project corpus file states three figures in two places — the Phase 4
amendment block near lines 28-33 and the validated-requirements bullet near lines 601-606 — and all
three are now wrong. Correct both occurrences with the identical dated note, in the file's own
house style of an italic parenthetical naming the date and this phase: the source-file count under
the examples directory is 62, not 47; the number declared as example targets in the manifest is 8,
not 4; and the claim that no crate under the crates directory ships its own examples directory
becomes a statement naming the one that does, with the path to the program it holds. Verify all three
figures against the tree before writing them, with a file count, a manifest grep and a directory
search. Touch no other sentence in the file: the rest is planning corpus and out of scope.

**The amendment.** Add a dated section to the rustdoc-bar decision record, copying the heading and
dating convention of its existing amendment section. The section records that eight crate-level
rustdoc suppressions are kept for this release rather than cleared, and contains: each attribute with
its file, its line and the lint it suppresses; the per-crate count of diagnostics each hides, taken
from the Phase 36 closing capture; the structural reason each is hard to clear in isolation — that
cross-crate documentation links resolve in the facade but not in an isolated crate, which the ports
crate's own comment beside its attribute already states; and the plain statement that the bar
measures the tree with these suppressions in force, so a green bar is not a claim that no diagnostic
exists. State the owner and the re-check trigger in the same words the ledger row will carry, so the
two records agree.

Change no `.rs` file. The attributes stay exactly as committed; removing them and fixing what
surfaces is a later milestone's work, sized at more than this whole phase. Confirm the workspace
documentation build still emits no warning line after this task, purely to prove nothing was touched.

Commit as two commits per D-28:
`docs(36.1): correct the stale examples figures in the project corpus (D-13)` and
`docs(36.1): record the kept crate-level rustdoc suppressions in ADR-0033 (D-29)`. Stage by explicit
path.
  </action>
  <verify>
    <automated>test "$(grep -c 'Corrected 2026-09-18' .planning/PROJECT.md)" -ge 2 &amp;&amp; test "$(grep -c 'paladin-llm' .planning/PROJECT.md)" -ge 2 &amp;&amp; grep -q '2026-09-18' .planning/decisions/0033-cargo-doc-warning-bar.md &amp;&amp; test "$(grep -cE 'lib\.rs' .planning/decisions/0033-cargo-doc-warning-bar.md)" -ge 5 &amp;&amp; cargo doc --workspace --no-deps 2>&amp;1 | tee /tmp/36.1-12-doc.txt &amp;&amp; ! grep -q 'warning:' /tmp/36.1-12-doc.txt</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/PROJECT.md` carries a dated correction note at both former occurrences, each naming the date and this phase.
    - Both corrected passages state 62 source files and 8 declared example targets, and both name the one crate that ships its own examples directory together with the path of the program inside it.
    - `find examples -name '*.rs' | wc -l` returns 62 and `grep -c '^\[\[example\]\]' Cargo.toml` returns 8, confirming the written figures.
    - `git diff .planning/PROJECT.md` shows changes confined to the two figure passages.
    - The decision record carries a new dated section listing eight attributes across five library files with their lints, five per-crate counts, the structural reason, and the statement that the bar measures the tree with the suppressions in force.
    - The amendment's owner and re-check trigger wording matches the reason text plan 36.1-13 will put on the ledger row.
    - `git diff --stat HEAD~2 -- src/ crates/ tests/ docs/` is empty and `cargo doc --workspace --no-deps` emits no warning line.
  </acceptance_criteria>
  <done>The corpus no longer contradicts the tree, and a reader of the rustdoc bar learns what it does and does not measure without having to find a register entry.</done>
</task>

<task type="auto">
  <name>Task 2: Disposition both pending todos and add the v2 candidate line</name>
  <files>.planning/todos/pending/2026-08-13-verify-local-coverage-reproduction.md, .planning/todos/pending/2026-09-13-evaluate-rustfs-replacement-for-minio.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/todos/pending/2026-08-13-verify-local-coverage-reproduction.md in full — its existing frontmatter keys, its problem statement and the historic figure it compares against
    - .planning/todos/pending/2026-09-13-evaluate-rustfs-replacement-for-minio.md in full — its existing frontmatter keys and its own statement that the evaluation sits past this milestone
    - .planning/REQUIREMENTS.md, the v2 requirements section and its platform-and-tooling subsection — the existing line format, including how each line is numbered and how it cites its source
    - .planning/decisions/0006-coverage-gate.md — the coverage floor the walk compares against, and the single-figure rule that replaced the older split targets
    - .planning/ROADMAP.md, the Phase 37 entry — its second success criterion, which is the pre-merge CI coverage run the walk's comparison target is being re-pointed at
    - .planning/phases/36.1-deferred-items-closure/36.1-CONTEXT.md decisions D-22, D-23 and D-24 — the exact frontmatter keys, the owner, the re-check date and the required disposition-section contents
  </read_first>
  <action>
**Both todos, same shape.** Leave each file exactly where it is — in the pending directory, under its
existing name, with its existing keys untouched — and add four frontmatter keys: an owner, a
deferred-past marker naming this milestone version, a re-check date of 2026-10-16, and a
dispositioned-by marker naming this phase and today's date. Add no phase-resolution tag: both files
say in their own text that they must outlive this milestone. Then append a dated disposition section
stating three things: what was verified now, what remains blocked and why, and the concrete re-check
trigger.

**The coverage walk.** Its owner is the repository maintainer. What was verified now: the
documentation half was settled by an earlier phase, and this environment has no container runtime at
all, so the two-step local procedure cannot be walked here — say so with the check that proves it
rather than as an assertion. What remains blocked: the end-to-end reproduction on a container-capable
machine. Update the comparison target while you are there: the historic figure at an old commit is
superseded by whatever figure the pre-merge CI coverage run records for the next phase, and the walk
compares against that run at whole-percent precision.

**The object-store evaluation.** Its owner is the repository maintainer, its re-check date is the
same, or the next milestone's planning kickoff if that comes first. What was verified now: the
documentation-currency half was settled by an earlier phase's sweep. What remains blocked: the
evaluation itself, which is an adapter build with parity tests and is past this milestone by the
file's own text.

**The v2 line.** Add one line to the requirements file's v2 platform-and-tooling subsection, in the
existing line format and continuing its numbering, naming the object-store adapter evaluation as a
candidate for the next milestone and pointing at the todo file by path — so the milestone backlog
carries it as well as the todo directory. Add nothing else to that file: the requirement identifiers
for this phase were minted at planning time and must not be renumbered or restated here.

Afterwards confirm the todo-listing tool still reports both files with their original title, area and
severity: that tool reads only four frontmatter keys, so the four new ones must be invisible to it. If
either todo disappears from the listing, the added keys broke the parse — fix the frontmatter rather
than accepting the loss.

Commit as `chore(36.1): disposition the two pending todos with owners and re-check dates (D-22, D-23, D-24)`
and `docs(36.1): record the object-store adapter evaluation as a v2 candidate (D-24)`. Stage by
explicit path.
  </action>
  <verify>
    <automated>test "$(node .claude/gsd-core/bin/gsd-tools.cjs query list-todos | grep -c '"file"')" = 2 &amp;&amp; test "$(grep -c '^owner:' .planning/todos/pending/2026-08-13-verify-local-coverage-reproduction.md)" = 1 &amp;&amp; test "$(grep -c '^owner:' .planning/todos/pending/2026-09-13-evaluate-rustfs-replacement-for-minio.md)" = 1 &amp;&amp; test "$(grep -c 'recheck_by: 2026-10-16' .planning/todos/pending/2026-08-13-verify-local-coverage-reproduction.md)" = 1 &amp;&amp; test "$(grep -c 'recheck_by: 2026-10-16' .planning/todos/pending/2026-09-13-evaluate-rustfs-replacement-for-minio.md)" = 1 &amp;&amp; grep -q 'RustFS' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - Both todo files remain at their original paths under the pending directory, with their original names.
    - Each file's frontmatter contains exactly one owner key, one deferred-past key naming this milestone version, one re-check key dated 2026-10-16, and one dispositioned-by key naming this phase and today's date.
    - Neither file contains a phase-resolution tag.
    - Each file ends with a dated disposition section stating what was verified now, what remains blocked and why, and the concrete re-check trigger.
    - The coverage todo's disposition section names the pre-merge CI coverage run as the new comparison target at whole-percent precision, and cites the check that proves no container runtime is available here.
    - `node .claude/gsd-core/bin/gsd-tools.cjs query list-todos` reports a count of 2, with both titles, areas and severities unchanged from before this task.
    - The requirements file's v2 platform-and-tooling subsection has exactly one new line, which names the object-store adapter evaluation and cites the todo file's path.
    - `git diff .planning/REQUIREMENTS.md` touches only the v2 platform-and-tooling subsection.
  </acceptance_criteria>
  <done>Neither todo is an undated pointer any more: each names an owner, a date and a trigger, and both still surface in the tool that lists them.</done>
</task>

<task type="auto">
  <name>Task 3: Append the reader-facing changelog bullets</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md lines 375-440 — the current release section's fixed subsection at line 383 and documentation subsection at line 421, with the bullets two prior phases already placed there: the voice, the length and the append point
    - .planning/phases/36.1-deferred-items-closure/36.1-02-SUMMARY.md — what the tool-error fix actually changed, in terms a user would recognise
    - .planning/phases/36.1-deferred-items-closure/36.1-03-SUMMARY.md and 36.1-04-SUMMARY.md — the six documentation corrections, summarised
    - .planning/phases/36.1-deferred-items-closure/36.1-11-SUMMARY.md — the nineteen sections and the new gate, summarised
    - .planning/phases/36.1-deferred-items-closure/36.1-CONTEXT.md decisions D-00i and D-26 — that both subsections already exist and are appended to, and what each bullet must cover
  </read_first>
  <action>
Append to the two existing subsections of the current release section. Do not create a subsection, do
not reorder existing entries, and do not touch any other release section.

**The documentation bullet** covers, in a reader's terms: the six corrected pages — the provider
contribution guide's adapter path, the testing guide's directory tree, the CLI appendix's two
troubleshooting entries, the adapter type name across the book, and the deployment page's YAML now
marked as illustrative; the nineteen public entry points that gained worked examples; the new
code-quality target that keeps every future public builder, port and service from shipping without
one; and the decision-record amendment listing the eight crate-level documentation suppressions that
are knowingly kept for this release.

**The fixed bullet** covers the tool-error change: when a tool failure is configured to fail the run,
the resulting error's reason is now redacted and bounded the same way the text fed back to the model
already was, so a credential in an upstream tool's error message no longer reaches the error surface
verbatim.

Both bullets are written for someone reading the published changelog. Name no register entry, no
ledger row id and no requirement identifier: a reader has access to none of them. Match the voice and
length of the bullets already in each subsection.

Commit as `docs(36.1): add the phase entries to the changelog (D-26)`. Stage the file by explicit
path.
  </action>
  <verify>
    <automated>test "$(grep -cE 'CURR-[0-9]+|RD-[0-9]+|EX-[0-9]+|WINDOWS' CHANGELOG.md)" = 0 &amp;&amp; test "$(grep -c '^### Documentation' CHANGELOG.md)" = 2 &amp;&amp; test "$(grep -c '^### Fixed' CHANGELOG.md)" = 9</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'CURR-[0-9]+|RD-[0-9]+|EX-[0-9]+|WINDOWS' CHANGELOG.md` is 0.
    - The counts of documentation and fixed subsection headings are unchanged from before this task — 2 and 9 respectively — proving no subsection was created.
    - Both new bullets sit inside the current release section, after the entries the two prior phases placed there.
    - The documentation bullet names the six corrected pages by subject, the nineteen examples, the new code-quality target and the suppressions amendment.
    - The fixed bullet describes the tool-failure reason as now redacted and bounded like the text fed back to the model, in terms a user of the library would recognise.
    - `git diff CHANGELOG.md` shows additions only — no existing line reordered, reworded or removed.
    - `git diff --stat HEAD~1 -- src/ crates/ docs/ .planning/` is empty.
  </acceptance_criteria>
  <done>A reader of the published changelog learns what this release's documentation and tool-error behaviour gained, without being shown an internal identifier.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Created by **this plan**:
- Two dated correction notes in `.planning/PROJECT.md`
- A dated "known crate-level suppressions" amendment section in
  `.planning/decisions/0033-cargo-doc-warning-bar.md`
- Four new frontmatter keys (`owner`, `deferred_past`, `recheck_by`, `dispositioned_by`) and a dated
  disposition section in each of the two `todos/pending/` files
- One new v2 platform-and-tooling requirement line in `.planning/REQUIREMENTS.md`
- One documentation bullet and one fixed bullet in `CHANGELOG.md`

Requirement identifiers `CURR-16` through `CURR-21` were minted into `.planning/REQUIREMENTS.md` and
written to the ROADMAP phase entry at planning time, not by this plan.

New symbols created elsewhere in Phase 36.1 (so drift verification does not flag them): the
`pub(crate)` visibility of `ToolResultFormatter::sanitize_tool_text` and the `fail_run_`-prefixed
pinning test (36.1-02); nineteen `# Examples` doc sections (36.1-05 through 36.1-11); the
`make check-api-examples` target, the hook id `check-api-examples` and the CI lint step named "Check
public-API `# Examples` headings" (36.1-11); the files `36.1-ENTRY-POINTS.md`, `36.1-EVIDENCE.md`,
`36.1-CI-EVIDENCE.md` and `COVERAGE.md`; `WINDOWS.md` rows 39 through 50 (36.1-13).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning corpus → future readers | a corrected sentence and an amendment are what a later phase or auditor reads instead of re-deriving |
| todo frontmatter → the listing tool | added keys must not break the parser that surfaces the todos |
| repository → published changelog | changelog bullets reach users who have no access to planning artifacts |
| decision record → the meaning of a green bar | the amendment states what the documentation bar does and does not measure |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36.1-49 | Repudiation | the kept rustdoc suppressions | high | mitigate | Eight suppressions hiding several hundred diagnostics beneath a green bar, with no written record, would read to a later auditor as a bar that was met. The amendment lists every attribute with file, line, lint and hidden count, states the structural reason, and says plainly that the bar measures the tree with them in force; the matching ledger row carries the same owner and re-check wording (D-29). |
| T-36.1-50 | Denial of Service | the todo-listing tool | medium | mitigate | Frontmatter keys added carelessly can break the parser that surfaces a todo, making a deferred item invisible — the opposite of a disposition. The action requires re-running the listing tool after the edit and fixing the frontmatter rather than accepting a loss; the verify asserts a count of two. |
| T-36.1-51 | Information Disclosure | changelog bullets | low | mitigate | Internal work-list, ledger and requirement identifiers imply access to planning artifacts that are not published. The verify greps the whole changelog for every such prefix and requires zero. |
| T-36.1-52 | Tampering | the release changelog | medium | mitigate | An append that accidentally creates a second subsection or reorders existing entries corrupts a file the release process reads. The acceptance criteria assert the subsection heading counts are unchanged and that the diff is additions only. |
| T-36.1-53 | Spoofing | the corrected corpus figures | medium | mitigate | Writing down new figures without checking them would replace one wrong claim with another. The action requires verifying all three against the tree first and the acceptance criteria re-run those checks. |
| T-36.1-SC | Tampering | package installs | low | accept | This plan touches planning corpus and the changelog only; no package-manager install runs and no source file is staged. |
</threat_model>

<verification>
- Both stale corpus passages carry a dated correction, and the three figures written match what the tree reports.
- The decision record carries a dated amendment naming eight attributes across five library files, with per-crate counts and the structural reason.
- No `.rs` file is modified and `cargo doc --workspace --no-deps` still emits no warning line.
- Both todos remain in the pending directory with four new frontmatter keys and a dated disposition section, and the listing tool still reports both.
- The requirements file gained exactly one v2 line, naming the object-store evaluation and its todo path.
- The changelog gained two bullets inside existing subsections, with no internal identifier and no reordering.
- The plan's SUMMARY carries a `## Self-Check` section.
</verification>

<success_criteria>
- ROADMAP SC5 is closed: neither todo is an undated pointer, each has an owner and a re-check date, and both still surface.
- The corpus statement ROADMAP SC2 names is corrected at both places it occurs.
- The eight rustdoc suppressions are written down as a deliberate, owned decision rather than an unexplained green bar.
- The release changelog tells a reader what the phase delivered.
</success_criteria>

<output>
Create `.planning/phases/36.1-deferred-items-closure/36.1-12-SUMMARY.md` when done, recording: the commit SHAs, the three verified corpus figures with the commands that produced them, the eight attributes as written into the amendment with their per-crate counts, the four frontmatter keys added to each todo and the listing tool's post-edit count, the v2 line text, the two changelog bullets, and a `## Self-Check` section.
</output>
