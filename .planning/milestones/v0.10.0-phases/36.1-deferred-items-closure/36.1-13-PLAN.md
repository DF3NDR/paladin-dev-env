---
phase: 36.1-deferred-items-closure
plan: 13
type: execute
wave: 5
depends_on: ["36.1-12"]
files_modified:
  - .planning/WINDOWS.md
  - .planning/phases/36.1-deferred-items-closure/36.1-EVIDENCE.md
autonomous: true
requirements: [CURR-19, CURR-21]
must_haves:
  truths:
    - "Every open entry from the Phase 31, 32, 34, 35 and 36 registers has exactly one `WINDOWS.md` row, minted through the ledger command line with the kind the schema names, and each row reads `fixed` or `waived` with its reason carried where the tool allows it (D-17, D-18, CURR-19)."
    - "Every row destined for `fixed` was appended after its fixing commit existed, with that commit's short SHA and the plan number already in the row description, because the tool's fixed subcommand records no reason (D-18)."
    - "Every row destined for `waived` carries a reason naming an owner and a re-check date or trigger, in a form a single search finds across the whole ledger (D-18)."
    - "`gsd-tools windows status` reports an open count of zero — including ledger row 38, which this phase's tool-error fix closed (D-03, D-20)."
    - "Rows 36 and 37 are verified, not re-done: the closure table cites the Phase 36 commit that fixed them and re-runs both verification commands on this phase's closing commit (D-21)."
    - "Register entries that need no new row — the two already covered by rows 36 and 37, the observation an earlier commit resolved, and the standing rule that is not a defect — each carry a closure-table line saying so, rather than being absent (D-19)."
    - "The closure table in `36.1-EVIDENCE.md` is complete: every register entry and both todos carry a disposition, a ledger row id or an explicit reason there is none, a closing commit, a verification command and an output anchor (D-20)."
    - "The closing gate sequence runs in the documented order on the closing commit with every output captured, and the public API baseline is unchanged (D-25, CURR-21)."
  artifacts:
    - ".planning/WINDOWS.md — twelve new rows plus the resolution of row 38"
    - ".planning/phases/36.1-deferred-items-closure/36.1-EVIDENCE.md — the completed closure table and the closing-gate record"
    - ".planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-13-closing-gates.txt — verbatim gate outputs"
  key_links:
    - "The ledger recomputes its own counts and rejects a file whose frontmatter and rows disagree, so every mutation goes through the command line: append, waive, fixed — never a hand edit."
    - "The fixed subcommand takes a row id and nothing else, which is the whole reason the ledger work sits in this plan rather than the first: the commit SHA has to be in the description at append time."
    - "The ledger currently holds 38 rows with one open, so the twelve new rows are ids 39 through 50; any extra row this phase's earlier plans reported continues from 51."
    - "Descriptions and reasons must not contain a run of four backticks, and any path they name must be repository-relative with no parent-directory segment."
    - "The closing gate order is fixed by decision: the code-quality target, the shell linter after staging, the security targets, the workspace test run, the full feature-union run, the documentation build sequence, and the API baseline check last."
  prohibitions:
    - statement: "`.planning/WINDOWS.md` is never edited by hand — every row is created and every status moved through the ledger command line (D-00b)."
      status: recalled
      verification: flagged-unverified
    - statement: "No existing `WINDOWS.md` row is deleted, and no row's description is rewritten after it is minted (D-00b)."
      status: recalled
      verification: flagged-unverified
    - statement: "No second row is minted for a register entry already covered by rows 36 or 37 (D-19)."
      status: recalled
      verification: flagged-unverified
    - statement: "No line is removed from the closure table — an entry with no disposition is recorded as outstanding, never dropped (D-20)."
      status: recalled
      verification: flagged-unverified
    - statement: "No `.rs` file, documentation page, Makefile, hook or workflow is modified by this plan."
      status: recalled
      verification: flagged-unverified
    - statement: "A failing gate is recorded as failing; no gate output is edited, filtered or re-run selectively to produce a green record (D-25)."
      status: recalled
      verification: flagged-unverified
    - statement: "The executor never runs `git push` — the branch push is the maintainer's action at the plan 36.1-14 checkpoint."
      status: recalled
      verification: flagged-unverified
---

<objective>
Bring the ledger back into agreement with the registers, complete the closure table the next phase
verifies by, and run the closing gates on the final commit.

Decisions honoured: D-00b (rows are never deleted, only moved to fixed or waived, and every mutation
goes through the ledger command line), D-01 (the state already closed by drift: rows 36 and 37 read
fixed from an earlier commit and the CI examples-count comment is already correct, so both are
recorded by verification rather than edited), D-14 (that CI comment is explicitly not edited — the
closure table records the verification command and the commit that fixed it), D-17 (one row per open
register entry, with the kind chosen from the schema and the phase and citation cells taken from the
register), D-18 (fixed-destined rows are minted after their fixing commit with the SHA in the
description; waived rows carry an owner and a re-check date in the reason), D-19 (entries already
covered, already resolved, or never a defect get a closure-table line saying so and no row), D-20
(the closure table lives here, one line per register entry and per todo, and the ledger's open count
must reach zero), D-21 (rows 36 and 37 are verified by re-running their two commands on this
commit), D-25 (the closing gate order and the captured outputs), D-03 (row 38 closes because this
phase fixed it), D-28 (`chore(36.1):` for ledger mutation, `docs(36.1):` for the evidence).

Purpose: the whole phase exists because the ledger and the registers disagreed. A release tool reads
the ledger's open count; a person reads the registers. Until both say the same thing, neither can be
trusted, and the next phase cannot verify this one by identifier.

Output: twelve new rows plus one resolved, an open count of zero, a complete closure table, and a
captured gate run.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/36.1-deferred-items-closure/36.1-CONTEXT.md
@.planning/phases/36.1-deferred-items-closure/36.1-RESEARCH.md
@.planning/phases/36.1-deferred-items-closure/36.1-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mint and resolve the twelve register rows, and close row 38</name>
  <files>.planning/WINDOWS.md</files>
  <precondition>Every fixing commit this task cites must already exist on this branch — plans 36.1-01 through 36.1-12 are all committed. Run `node .claude/gsd-core/bin/gsd-tools.cjs query windows status` first and confirm the ledger holds 38 rows with exactly one open; if the totals differ, halt and report rather than minting against an unexpected baseline.</precondition>
  <read_first>
    - .planning/WINDOWS.md — the whole ledger: its header showing the command line, the schema note in row 21 listing the kinds and statuses, rows 1 and 22 showing that a fixed row's reason cell is normally empty, rows 12, 13, 20 and 21 showing how a reason rides in the description instead, and rows 36, 37 and 38 as they stand
    - .planning/phases/31-lossless-token-accounting/deferred-items.md and .planning/phases/32-unified-token-primitives/deferred-items.md — the two entries that map to rows 36 and 37, and the isolation-test entry that appears in both registers and gets one row
    - .planning/phases/34-documentation-currency-audit/deferred-items.md — its five entries with their own file and line citations
    - .planning/phases/35-mdbook-currency/deferred-items.md — its entries, including the two that get no row: the standing rule, and the observation an earlier commit already resolved
    - .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/deferred-items.md — the suppressions entry and its option (b) disposition
    - The SUMMARY of every plan from 36.1-01 through 36.1-12 — the commit SHAs each row's description cites, and any extra row plans 36.1-01 and 36.1-04 reported as needed
    - .planning/decisions/0033-cargo-doc-warning-bar.md, the amendment plan 36.1-12 added — the owner and re-check wording the suppressions row's reason must match
  </read_first>
  <action>
Mint twelve rows through the ledger command line, in this order, then resolve each immediately. Use
the append subcommand with a kind, a phase, a description and, where the register cites one, a file
and a line. Descriptions must contain no run of four backticks and every path they name must be
repository-relative with no parent-directory segment.

Each row destined for `fixed` gets its closing commit's short SHA and the plan number in the
description before it is appended, in the form the phase's own convention names — the fixed
subcommand takes an id and nothing else, so there is no later opportunity. Each row destined for
`waived` is waived immediately with a reason ending in an owner and a re-check date or trigger, in a
form one search finds across the whole ledger.

The twelve, with their kind, phase cell and disposition:
  1. The isolation test's failure under the full feature union — kind deviation, phase 31, cited at
     the test file and the guard's line; fixed, closed by plan 36.1-01's commit.
  2. The container-machine coverage walk — kind todo, phase 34, cited at its todo file; waived, owner
     the repository maintainer, re-check 2026-10-16.
  3. The public-API examples apparatus: its scope drift, its nineteen missing items and its unwired
     gate — kind deviation, phase 34, cited at the script; fixed, closed by plan 36.1-11's wiring
     commit.
  4. The CI examples-count comment — kind unmet-truth, phase 34, cited at the workflow and the
     comment's line; fixed, with the description naming the two verification commands and the Phase
     36 commit that made the comment correct. Nothing in the workflow is edited (D-14).
  5. The corpus claim about crate-level example directories — kind unmet-truth, phase 34, cited at
     the corpus file and line; fixed, closed by plan 36.1-12's commit.
  6. The object-store adapter evaluation — kind todo, phase 34, cited at its todo file; waived, owner
     the repository maintainer, re-check 2026-10-16 or the next milestone's planning kickoff,
     whichever is earlier.
  7. The provider guide's adapter import path — kind unmet-truth, phase 35, cited at the page and
     line 272; fixed, closed by plan 36.1-03's commit.
  8. The testing guide's fixture-path claim — kind unmet-truth, phase 35, cited at the page and line
     96; fixed, closed by plan 36.1-04's commit.
  9. The CLI appendix's two troubleshooting entries — kind unmet-truth, phase 35, cited at the page
     and line 409; fixed, closed by plan 36.1-03's commit.
  10. The adapter type casing across the book — kind unmet-truth, phase 35, cited at the patterns
      guide and line 43; fixed, closed by plan 36.1-04's commit.
  11. The deployment page's uncaptioned YAML — kind unmet-truth, phase 35, cited at the page and line
      280; fixed, closed by plan 36.1-03's commit.
  12. The eight crate-level rustdoc suppressions — kind lint-warning, phase 36, cited at the facade
      library file and line 117; waived, with the reason matching the decision-record amendment's own
      owner and re-check wording word for word.

Then mark row 38 fixed: this phase's tool-error change closed it, and its own description already
names the defect. Add no new row for it.

If plan 36.1-01 reported a target other than the isolation test failing under the full feature union,
or plan 36.1-04 reported an example body that does not compile, mint one additional row per finding,
continuing the id sequence, with the kind and waive reason those plans specified. State the final row
count explicitly in the SUMMARY rather than assuming twelve.

Finally run the ledger's status query and confirm the open count is zero. If it is not, do not
hand-edit anything: find the row that is still open and resolve it through the tool.

Commit as `chore(36.1): reconcile the ledger with the deferred-item registers (D-17, D-18)`. Stage
the ledger by explicit path.
  </action>
  <verify>
    <automated>test "$(node .claude/gsd-core/bin/gsd-tools.cjs query windows status | grep -c '"open_count": 0')" = 1 &amp;&amp; test "$(node .claude/gsd-core/bin/gsd-tools.cjs query windows status | grep -cE '"id": (3[89]|4[0-9]|50)')" -ge 13</automated>
  </verify>
  <acceptance_criteria>
    - `node .claude/gsd-core/bin/gsd-tools.cjs query windows status` reports an open count of zero and a total count of 50 or more.
    - Rows 39 through 50 exist, each with a kind drawn from the schema, a phase cell matching the register it came from, and a status of either fixed or waived.
    - Every row whose status is fixed carries a short commit SHA and a plan number in its description.
    - Every row whose status is waived carries a reason naming an owner and a re-check date or trigger.
    - Row 38's status is fixed and no new row duplicates it.
    - No row's id is missing from the sequence and no pre-existing row changed status except row 38.
    - `git diff .planning/WINDOWS.md` shows only tool-shaped changes — appended rows, status and timestamp fields, and the recomputed counts block.
    - `git diff --stat HEAD~1 -- src/ crates/ docs/ tests/ Makefile .github/` is empty.
  </acceptance_criteria>
  <done>The ledger says what the registers say: every open entry has exactly one row, every row has a disposition with a reason or a commit, and the open count is zero.</done>
</task>

<task type="auto">
  <name>Task 2: Complete the closure table</name>
  <files>.planning/phases/36.1-deferred-items-closure/36.1-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/36.1-deferred-items-closure/36.1-EVIDENCE.md — the seventeen-line skeleton plan 36.1-01 seeded and the one line it completed, which fixes the column shape
    - The SUMMARY of every plan from 36.1-01 through 36.1-12 — each one's commit SHAs, verification commands and observed outputs, which are the cells this table fills
    - .planning/phases/36.1-deferred-items-closure/36.1-evidence/ — every capture file written so far, which the output-anchor column points at
    - .planning/phases/35-mdbook-currency/35-EVIDENCE.md and .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-EVIDENCE.md — the closure-table house pattern
    - .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-VERIFICATION.md line 50 — the record that rows 36 and 37 were fixed and row 38 deliberately left open
    - .planning/phases/35-mdbook-currency/35-EVIDENCE.md, the allowlist row that excused the stale testing-guide tree as a false positive — the row this phase retires
  </read_first>
  <action>
Fill every remaining cell of the closure table. One line per register entry and per todo, no line
added and none removed. Each line carries: the register, the entry, the disposition, the ledger row
id, the closing commit, the verification command and the output anchor.

Dispositions in use are: fixed; waived; re-homed, for the two todos whose ledger rows are waived and
whose files now carry an owner and a date; closed-by-drift, for the CI examples-count comment that was
already correct before this phase began; and covered-by-row, for an entry that maps onto an existing
row rather than a new one. Use the same word consistently — the next phase greps this column.

Four lines need particular care:
  - The two entries that map onto rows 36 and 37 get the covered-by-row disposition naming those ids,
    and their verification cells carry the two commands re-run on this phase's closing commit: the
    workspace documentation build with its warning count, and the per-crate documentation build under
    the strict flag with its exit status. Run both now and record what they printed — this is
    verification of an earlier phase's work, not a repeat of it.
  - The observation an earlier commit already resolved gets a line saying so, naming that commit, with
    no ledger row.
  - The standing rule for cross-phase pointers gets a line saying it is a rule rather than a defect,
    with no ledger row.
  - Row 38 gets its own line, disposition fixed, naming this phase's tool-error commit.

Then add three short sections after the table. First, a retirement note stating that the Phase 35
allowlist row which excused the stale testing-guide directory tree as a false positive is retired,
because this phase corrected the tree. Second, a findings section listing anything the earlier plans
reported that did not become a register line: any example body that would not compile, any target
other than the isolation test failing under the full feature union, any pre-existing broken
documentation link, and the verdict recorded on the provider guide's module-declaration line. Third, a
spec-less probe note recording plainly that this phase has no specification document and that the
deterministic edge-coverage probe was skipped at planning time because the phase had no requirement
identifiers to probe — the identifiers were minted during that same planning run — so the phase's
must-have prohibitions were authored by recall and carry no automated check path.

If any entry still has no disposition, record it as outstanding with the reason and report it in the
SUMMARY as a deviation. Do not omit it and do not invent a closure.

Commit as `docs(36.1): complete the closure table (D-20)`. Stage the evidence file by explicit path.
  </action>
  <verify>
    <automated>EV=.planning/phases/36.1-deferred-items-closure/36.1-EVIDENCE.md; test "$(grep -c 'pending' "$EV")" = 0 &amp;&amp; grep -q 'covered-by-row' "$EV" &amp;&amp; grep -q 'closed-by-drift' "$EV" &amp;&amp; grep -q 're-homed' "$EV" &amp;&amp; grep -q 'row 38' "$EV"</automated>
  </verify>
  <acceptance_criteria>
    - The closure table has the same seventeen lines the skeleton seeded, plus one line per extra row any earlier plan reported; no line was removed.
    - No cell in the table still reads as pending.
    - Every line carries a disposition drawn from the five words in use, and every line whose disposition is fixed or waived names a ledger row id.
    - The two lines mapping to rows 36 and 37 carry the two re-run commands with the output observed on this phase's closing commit — a zero warning count and a successful strict per-crate build.
    - The retirement note names the Phase 35 allowlist row it retires and why.
    - The findings section lists every earlier-plan finding or states explicitly that there were none of each kind.
    - The spec-less probe note states that no specification exists, that the edge probe was skipped for want of requirement identifiers at probe time, and that the prohibitions carry no automated check path.
    - `git diff --stat HEAD~1 -- src/ crates/ docs/ tests/ .planning/WINDOWS.md` is empty.
  </acceptance_criteria>
  <done>Every register entry and both todos are traceable from one table to a disposition, a row, a commit and a command — which is what the next phase verifies this one by.</done>
</task>

<task type="auto">
  <name>Task 3: Run the closing gate sequence and record it</name>
  <files>.planning/phases/36.1-deferred-items-closure/36.1-EVIDENCE.md, .planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-13-closing-gates.txt</files>
  <precondition>All of this phase's source, documentation, wiring and corpus commits exist on this branch, and nothing is left unstaged — the gates must measure the closing commit, not a working tree.</precondition>
  <read_first>
    - .planning/phases/36.1-deferred-items-closure/36.1-CONTEXT.md decision D-25 — the gate list and the order, which is part of the decision
    - Makefile — the code-quality target's dependency list as it now reads with the new examples gate, the shell-lint target, the security targets and the API-baseline target
    - .github/workflows/docs.yml — the documentation build sequence to reproduce, in order, including the preprocessor install and the link-check policy
    - .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-EVIDENCE.md, its measurement section — the house pattern for recording a figure beside its capture file rather than pasting long output inline
    - .planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-01-all-features-sweep.txt — the earlier full feature-union result this run is compared against
  </read_first>
  <action>
Run the closing gates in the decided order, on the closing commit, capturing every output to
`.planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-13-closing-gates.txt` with each
command line and its exit status:

  1. The code-quality target, which now runs formatting, linting, shell linting, checking, the
     rustdoc bar and the new public-API examples gate.
  2. The shell linter, run after staging any changed shell file — it scans tracked files only, so an
     unstaged change reads as clean.
  3. The security targets.
  4. The workspace test run.
  5. The workspace test run under the full feature union without fail-fast. Compare the failing-target
     count against the earlier capture from plan 36.1-01 and record any difference plainly.
  6. The documentation build sequence from the documentation workflow, in order, and then confirm the
     documentation tree is clean afterwards.
  7. The API baseline check, last. The expected result is no change: everything this phase touched is
     a documentation comment, a test body, a build or workflow line, a crate-visible function or
     planning corpus.

Then add a closing-measurement section to the evidence file recording each gate's name, its exit
status and the one figure that matters for it — the rustdoc warning count, the examples-gate totals,
the test pass and fail counts for both runs, the broken-link count, and the API baseline verdict —
each citing the capture file rather than pasting long output inline.

If any gate fails, record it as failing with its output and stop. Do not re-run a gate selectively, do
not filter its output, and do not fix an unrelated defect inside this plan to make a gate pass: report
it so the phase can be replanned. A recorded green that was not observed is worse than a recorded
failure.

Commit as `docs(36.1): record the closing gate results (D-25)`. Stage the evidence file and the
capture by explicit path.
  </action>
  <verify>
    <automated>test -s .planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-13-closing-gates.txt &amp;&amp; make api-surface &amp;&amp; bash scripts/check-public-api-examples.sh &amp;&amp; test -z "$(git status --porcelain -- docs)"</automated>
  </verify>
  <acceptance_criteria>
    - The capture file records all seven gates in the decided order, each with its command line and exit status.
    - `make api-surface` exits 0 and reports no difference against the committed baseline.
    - `bash scripts/check-public-api-examples.sh` exits 0 at 101 satisfied, 0 missing, 0 singular.
    - The workspace test run and the full feature-union run each record their pass and fail counts, and the feature-union run's failing-target count is compared against the plan 36.1-01 capture with any difference stated.
    - The documentation build records a zero broken-link count and `git status --porcelain -- docs` prints nothing afterwards.
    - The evidence file's closing-measurement section names every gate with its figure and cites a capture file for each.
    - Any failing gate is recorded verbatim with its output, and the SUMMARY reports it as a deviation rather than a pass.
    - `git diff --stat HEAD~1 -- src/ crates/ docs/ tests/ Makefile .github/` is empty.
  </acceptance_criteria>
  <done>The closing commit is measured by every gate the phase promised, in the promised order, with the outputs on disk rather than asserted.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Created by **this plan**:
- `WINDOWS.md` rows 39 through 50 (twelve new ledger rows), plus any extra row an earlier plan
  reported, and the resolution of row 38
- The completed closure table, the retirement note, the findings section and the spec-less probe note
  in `.planning/phases/36.1-deferred-items-closure/36.1-EVIDENCE.md`
- `.planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-13-closing-gates.txt` (new file)

No Rust symbol, no source file, no public API item is created or modified by this plan.

New symbols created elsewhere in Phase 36.1 (so drift verification does not flag them): the
`pub(crate)` visibility of `ToolResultFormatter::sanitize_tool_text` and the `fail_run_`-prefixed
pinning test (36.1-02); nineteen `# Examples` doc sections (36.1-05 through 36.1-11); the
`make check-api-examples` target, the hook id `check-api-examples` and the CI lint step named "Check
public-API `# Examples` headings" (36.1-11); the files `36.1-ENTRY-POINTS.md`, `36.1-EVIDENCE.md`,
`COVERAGE.md` and `36.1-CI-EVIDENCE.md` (the last created by 36.1-14); requirement ids `CURR-16`
through `CURR-21`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ledger file → tool-managed state | the ledger recomputes its own counts and a hand edit desynchronises them |
| ledger open count → release tooling | the shipping and milestone-closing commands read this number |
| closure table → downstream phases | the next phase and this phase's own verification read it by identifier |
| local gate run → recorded evidence | a figure written down without being observed is indistinguishable from one that was |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36.1-54 | Tampering | `.planning/WINDOWS.md` | high | mitigate | The ledger carries recomputed counts and rejects a file whose frontmatter and rows disagree, so a hand edit can corrupt it outright. Every row is created and every status moved through the ledger command line; prohibitions forbid hand edits and deletions, and acceptance asserts the diff is tool-shaped only (D-00b). |
| T-36.1-55 | Repudiation | a fixed row with no citable commit | high | mitigate | The fixed subcommand records no reason, so a row minted before its fixing commit exists can never be traced afterwards. The precondition requires every cited commit to exist, and acceptance asserts every fixed row's description carries a short SHA and a plan number (D-18). |
| T-36.1-56 | Repudiation | a register entry with no closure line | high | mitigate | An entry omitted from the table breaks the chain the next phase verifies by. No line may be removed, an entry with no disposition is recorded as outstanding, and acceptance asserts no cell still reads as pending (D-20). |
| T-36.1-57 | Spoofing | the closing gate record | high | mitigate | A recorded green that was not observed is worse than a recorded failure, because it survives into the release record. The action forbids selective re-runs, output filtering and opportunistic fixes, and requires a failing gate to be recorded and reported as a deviation (D-25). |
| T-36.1-58 | Spoofing | rows 36 and 37 | medium | mitigate | Marking them closed on the strength of an earlier phase's word would leave two unverified claims in the release record. Their two verification commands are re-run on this phase's closing commit and the observed output is recorded (D-21). |
| T-36.1-59 | Elevation of Privilege | the public API baseline | medium | mitigate | A crate-visible widening or an accidental public item would move the surface and disturb the next phase's compatibility gates. The API baseline check runs last and acceptance requires no difference (D-25, CURR-21). |
| T-36.1-SC | Tampering | package installs | low | accept | This plan runs no package-manager install. The gate sequence builds and tests with the lockfile already committed; any lockfile change would surface as a failing gate rather than a silent one. |
</threat_model>

<verification>
- The ledger's open count is zero, its total is 50 or more, and every new row carries a disposition with either a commit SHA in its description or an owner and date in its reason.
- Row 38 reads fixed and no row duplicates it.
- The closure table has no pending cell, uses the five dispositions consistently, and carries the retirement note, the findings section and the spec-less probe note.
- All seven closing gates ran in the decided order with their outputs captured, and the API baseline is unchanged.
- `git status --porcelain -- docs` is clean after the documentation build.
- No source file, page, Makefile, hook or workflow is modified by this plan.
- The plan's SUMMARY carries a `## Self-Check` section.
</verification>

<success_criteria>
- ROADMAP SC4 is closed: the ledger and the registers agree, every open entry has exactly one row with a disposition and a reason, and the open count is zero.
- ROADMAP SC6 is measured: every closing gate ran green on the closing commit with its output on disk, and the public surface did not move.
- The next phase can verify this one by identifier from a single table.
</success_criteria>

<output>
Create `.planning/phases/36.1-deferred-items-closure/36.1-13-SUMMARY.md` when done, recording: the commit SHAs, the final ledger totals with the id range minted and the exact row count, the disposition of each of the twelve rows plus row 38, the re-run output for rows 36 and 37, the closure table's line count with any outstanding line named, each closing gate's exit status and headline figure, the API baseline verdict, and a `## Self-Check` section.
</output>
