# Phase 36.1 Deferred Items Closure — CI Evidence Record (plan 36.1-14)

**Phase:** 36.1-deferred-items-closure
**Branch:** `feature/phase-33`, upstream `origin/feature/phase-33`, sitting many commits ahead of
it (this is a routine update of an already-published branch, not a first push).
**Head SHA at seed time:** `6735bc64627d2d17f1844792561af65fb8e6ea4d` — the closing commit of
plan 36.1-13, carrying every plan of this phase (36.1-01 through 36.1-13, including the ledger
reconciliation and closing-gate capture) plus this file's own seeding commit landing on top.
**Head SHA at the recorded CI run:** `6fe5b70a1ff973e088a58184f57d5401c9f97895` — the maintainer
pushed `feature/phase-33` to this exact commit (`origin/feature/phase-33` reads the same SHA). It
adds only two commits on top of the seed-time head above: this file's own seeding commit
(`4046b98a`, `docs(36.1): seed the CI evidence record for the new required-check step (D-12)`) and
the orchestrator's checkpoint commit (`6fe5b70a`, `docs(state): record phase 36.1 checkpoint
(maintainer push pending)`, touching only `.planning/STATE.md`). Neither commit touches `src/`,
`crates/`, `docs/`, `tests/`, `Makefile` or `.github/`, so the local closing figures captured at
`6735bc64` remain valid evidence for the pushed tree the run below actually measured.
**Written:** 2026-09-18. **Updated:** 2026-09-18, with the real CI run recorded below.

## Why this file exists

Plan 36.1-11 added a new step to the CI `lint` job (`Code Quality`): `Check public-API `# Examples`
headings`, running `./scripts/check-public-api-examples.sh` in gate mode. The `lint` job is a
required check. Per D-12 (this phase) and the pattern two prior phases set (`33-CI-EVIDENCE.md`,
`36-CI-EVIDENCE.md`), a local measurement is not evidence that this step passes on the runner: it
says nothing about whether the step was spelled correctly in the workflow, whether the runner
resolves the script and its dependencies, or whether the job's pre-existing steps still pass
beside the new one. Only a real, observed run on the pushed branch answers that. This file is
seeded with the local closing-gate figures below and a run table whose cells are `pending` until a
real run is read and recorded — per this plan's own prohibitions, no cell is pre-filled or guessed.

---

## Local figures (from the closing-gate capture, D-25)

Every figure below was captured by plan 36.1-13 on the closing commit stack (`6dd9164c`,
`56434114` and the two commits after it) and re-cited here verbatim from
`36.1-evidence/36.1-13-closing-gates.txt` and `36.1-EVIDENCE.md`'s Closing-Measurement Section —
not re-run by this plan.

| # | Figure | Value | Gate that produced it |
|---|--------|-------|------------------------|
| 1 | Rustdoc warning count (`cargo doc --workspace --no-deps`, the `lint` job's `Check documentation` step, byte-identical invocation) | **0** `warning:` lines | `make clean-code` → `doc-check` (Gate 1) |
| 2 | Public-API `# Examples` gate totals (`scripts/check-public-api-examples.sh`, the new `Check public-API `# Examples` headings` step, byte-identical invocation) | **101/101 OK, 0 MISSING, 0 SINGULAR** — "All 101 D-05 public API entry points carry a plural '# Examples' heading." | `make clean-code` → `check-api-examples` (Gate 1) |
| 3 | Workspace test pass/fail (`cargo test --workspace`, the `test` job's `Run unit tests` step) | First run: `979 passed; 3 failed` (three 30s-timeout panics, environmental resource contention, files outside this phase's `files_modified`). Second, full unfiltered re-run: `982 passed; 0 failed` | Gate 4 |
| 4 | Full feature-union test pass/fail (`cargo test --workspace --all-features --no-fail-fast`, D-16; not run in the CI `test` job today per `ci.yml:504/507/789` — no `--all-features` test job exists) | 65 `test result: ok` lines, **0 failed**, matching plan 36.1-01's entry baseline exactly | Gate 5 |
| 5 | Broken-link count (mdBook sequence, `mdbook build docs/` with the linkcheck backend) | **0** broken links ("No broken links found") | Gate 6 |
| 6 | API surface baseline verdict (`make api-surface`) | **unchanged** — 3959 items, byte-identical to the committed baseline | Gate 7 |
| 7 | Doctest pass/fail (`cargo test --workspace --doc`, the `test` job's `Run doc tests` step — not separately re-captured by Gate 4/5 above, which run the non-doc test binaries; cited here for completeness since this file's run table tracks it) | Not separately re-measured by plan 36.1-13's closing sweep (Gates 4-5 above are `cargo test --workspace[--all-features]`, which include lib/integration targets but doctests run under a dedicated `--doc` invocation in CI's `test` job) | n/a — CI-only figure for this row; see finding below |

**Finding (recorded, not reconciled):** row 7 has no local figure to compare against — plan
36.1-13's closing-gate sequence (D-25) does not list a standalone `cargo test --workspace --doc`
invocation among its seven gates, and Phase 36's own `36-CI-EVIDENCE.md` recorded doctests
(462 passed / 0 failed / 210 ignored) as a separate, explicitly-run local gate that this phase's
closing sequence did not repeat. This is not a defect in the closing-gate run — D-25 named its
seven gates deliberately and doctests were not one of them — but it means the `Run doc tests` row
in the table below has no local baseline to agree or disagree with; its CI figure is recorded on
its own merits once observed.

---

## Run table (D-12) — recorded, real pushed-branch run

The maintainer pushed `feature/phase-33` to `6fe5b70a1ff973e088a58184f57d5401c9f97895`
(`origin/feature/phase-33` confirmed to read the same SHA). The push triggered
`.github/workflows/ci.yml` run **35329107346** —
https://github.com/DF3NDR/paladin-dev-env/actions/runs/35329107346 (workflow `ci.yml`, run number
352, event `push`, head SHA as above).

Every cell below was read live via `gh run view 35329107346 --repo DF3NDR/paladin-dev-env --json
jobs` and cross-checked against the raw per-job logs (`gh api
repos/DF3NDR/paladin-dev-env/actions/jobs/<id>/logs`, ANSI escapes stripped) — the exact log lines
quoted in the Notes column were read directly out of the log text, not assumed from the step's
green checkmark alone.

| Job | Job ID | Step | Run ID | Conclusion | Notes |
|---|---|---|---|---|---|
| `lint` (`Code Quality`) | `105549054274` | `Check formatting` | 35329107346 | **success** | Ran to completion before the documentation steps; job overall `success`. |
| `lint` (`Code Quality`) | `105549054274` | `Run clippy` | 35329107346 | **success** | Ran to completion before the documentation steps; job overall `success`. |
| `lint` (`Code Quality`) | `105549054274` | `Check documentation` | 35329107346 | **success** | Raw log (step starting line 1550) runs `cargo doc --workspace --no-deps 2>&1 \| tee /tmp/doc-output.txt && ! grep -q "warning:" /tmp/doc-output.txt` to `Finished`/`Generated` with **zero** `warning:` lines — matches local figure 1 (0 warnings) exactly. |
| `lint` (`Code Quality`) | `105549054274` | `Check documentation (all features, -D warnings)` | 35329107346 | **success** | Raw log (step starting line 1691) runs `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --all-features --no-deps` to `Finished`/`Generated` with no error. |
| `lint` (`Code Quality`) | `105549054274` | `Check public-API `# Examples` headings` | 35329107346 | **success** | **The step D-12 makes a required check — this is the phase's actual acceptance evidence.** Raw log line 1784: `All 101 D-05 public API entry points carry a plural '# Examples' heading.` — matches local figure 2 (101/101 OK, 0 MISSING, 0 SINGULAR) exactly, verbatim. |
| `test` (`Unit Tests (stable)`) | `105549054279` | `Run unit tests` | 35329107346 | **success** | Raw log (step group `cargo test --workspace --lib --bins`, starting line 381) shows `test result: ok. 982 passed; 0 failed; 0 ignored` for the workspace aggregate — matches local figure 3's second, full unfiltered re-run (982 passed / 0 failed) exactly; the CI run never hit the transient 30s-timeout flake the first local run did. `Unit Tests (beta)` (job `105549054378`) ran the identical step with the same `success` conclusion. |
| `test` (`Unit Tests (stable)`) | `105549054279` | `Run doc tests` | 35329107346 | **success** | `cargo test --workspace --doc`'s 13 per-crate `test result:` lines (step group starting line 5135) sum to **480 passed / 0 failed / 210 ignored** (`paladin` 154/0/18, `paladin_core` 95/0/38, `paladin_battalion` 59/0/52, `paladin_content` 0/0/0, `paladin_doc_examples` 0/0/0, `paladin_eval` 4/0/2, `paladin_herald` 0/0/6, `paladin_llm` 8/0/0, `paladin_memory` 12/0/0, `paladin_notifications` 0/0/0, `paladin_ports` 148/0/94, `paladin_storage` 0/0/0, `paladin_web` 0/0/0) — summed by hand from each crate's line. `Unit Tests (beta)` (job `105549054378`) ran the identical step with the same `success` conclusion and the same per-crate figures. |

The row for `Check public-API `# Examples` headings` is the step D-12 makes a required check —
this is the phase's actual acceptance evidence and the reason this file exists. The two
documentation steps and the formatting/clippy steps in the same `lint` job are recorded alongside
it because they run in the same job and their conclusions confirm the new step was not added to a
job that had itself regressed. The `test` job's two steps are recorded because they are the CI
figures row 3's transient-flake finding and row 7's doctest gap need a real observation to close.

**Job-level conclusions confirming the above:** `Code Quality` → `success`; `Unit Tests (stable)`
→ `success`; `Unit Tests (beta)` → `success`.

### Other jobs on the same run (context, not part of the seeded table)

Every other job on run 35329107346 also concluded `success` or a by-design `skipped`, and no job
failed: `API Surface Tracking` (`105549054068`) `success` — agrees with the local figure 6
(unchanged, 3959 items) exactly; `Coverage` (`105549054368`) `success`, reporting `Lines:
111771/123587 = 90.44%`, `Functions: 11785/14096 = 83.61%` (above the ADR-0006 82% floor; this
devcontainer has no Docker and cannot measure coverage locally, so this figure is recorded here on
its own merits for Phase 37 SC2, not compared against a local baseline this phase never captured);
`Integration Tests`, `Example Muster (Feature Matrix)`, `Security Audit`, `License & Dependency
Policy`, all eleven `Crate Isolation` jobs, `Semver Checks (vs v0.9.0)`, `MSRV (Rust 1.88)`: all
`success`. `Benchmark Regression Signal (Non-Blocking)`, `Publish Dry Run`, `End-to-End Tests`:
`skipped` by design (non-blocking / tag-gated / own trigger condition), not a failure. The only job
not yet `completed` at read time (2026-09-18, ~09:45 UTC) was `Docker Build`
(`105552779823`, `in_progress`) — not one of the jobs this table's rows verify.

---

## Comparison section (D-25 local figures vs the run 35329107346 CI figures)

**Rule:** where a CI figure and a local figure disagree, the CI figure is authoritative and the
disagreement is recorded here as a finding — never reconciled by adjusting the local record in
`36.1-EVIDENCE.md` or the table above.

| # | Figure | Local (D-25) | CI (run 35329107346) | Verdict |
|---|--------|--------------|------------------------|---------|
| 1 | Rustdoc warning count | 0 `warning:` lines | 0 `warning:` lines (`Check documentation` step) | **Agree.** |
| 2 | Public-API `# Examples` gate | 101/101 OK, 0 MISSING, 0 SINGULAR | `All 101 D-05 public API entry points carry a plural '# Examples' heading.` | **Agree, verbatim.** |
| 3 | Workspace test pass/fail | First run: 979 passed / 3 failed (transient 30s-timeout flake); second, full re-run: 982 passed / 0 failed | `Run unit tests`: 982 passed / 0 failed | **Agree with the local re-run; the CI run is taken as authoritative and confirms the first local run's 3 failures were the documented transient flake, not a real defect.** Recorded as a finding per this section's own rule: the local record is left as observed (both the flake and the clean re-run), not adjusted. |
| 4 | Full feature-union test pass/fail | 65 `test result: ok` lines, 0 failed | Not run in the CI `test` job (no `--all-features` test job exists per `ci.yml:504/507/789`, as noted at seed time) | **No CI figure to compare — unchanged from the seeded finding.** |
| 5 | Broken-link count | 0 broken links | Not a job on this run (mdBook linkcheck is not part of `ci.yml`'s `lint`/`test` jobs) | **No CI figure to compare — local-only gate.** |
| 6 | API surface baseline verdict | unchanged, 3959 items | `API Surface Tracking` job: `success` (unchanged) | **Agree.** |
| 7 | Doctest pass/fail | No local baseline (D-25's seven gates did not include a standalone `cargo test --workspace --doc` run; see the seeded finding above) | `Run doc tests`: 480 passed / 0 failed / 210 ignored | **No local figure to compare against — recorded on its own merits, per the seeded finding.** Note for a future reader: this total (480 passed) differs from Phase 36's own CI-observed doctest total (462 passed / 0 failed / 210 ignored, `36-CI-EVIDENCE.md`) on the same 13 crates and the same `ignored` count; the extra 18 passing doctests are additional `# Examples` sections this phase (36.1-05 through 36.1-11) added since Phase 36 closed. This is not a Phase 36.1 vs Phase 36 disagreement — the two runs measure different trees at different points in time — and is stated here only so the delta is not mistaken for an inconsistency. |

**No disagreement requiring reconciliation was found.** Row 3's transient local failure is
explained by the CI figure (authoritative, clean), not contradicted by it; rows 4 and 5 have no CI
counterpart to compare (recorded as such, not treated as a gap in this run); row 7 has no local
counterpart (recorded on its own merits, with the Phase 36 doctest-count delta noted for context
only).

---

*Phase: 36.1-deferred-items-closure*
*Written: 2026-09-18. Updated: 2026-09-18 with the real CI run.*
*Status: local figures captured (D-25); CI run 35329107346 read live via `gh run view` and
`gh api …/jobs/<id>/logs` — the lint job's five steps (formatting, clippy, both documentation
steps, and the new public-API `# Examples` heading step) and the test job's two steps (unit tests,
doc tests) all concluded `success` on the pushed head `6fe5b70a1ff973e088a58184f57d5401c9f97895`,
with every measured figure agreeing with the local closing measurement except row 7 (doctests),
which has no local baseline and is recorded on its own merits. No job on the run failed.*
