---
phase: 36.1-deferred-items-closure
verified: 2026-09-18T10:16:57Z
status: passed
score: 6/6 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: passed
  previous_score: 6/6 must-haves verified
  gaps_closed: []
  gaps_remaining: []
  regressions: []
---

# Phase 36.1: Deferred Items Closure Verification Report

**Phase Goal:** Every deferred item Phases 30-35 recorded and left unowned is either closed in
the tree or explicitly dispositioned before v0.10.0 ships — the per-phase `deferred-items.md`
registers (31, 32, 34, 35), the two open `WINDOWS.md` rows (#36, #37) and the two
`todos/pending/` files are walked item by item, each one is fixed, waived with a written reason,
or re-homed to a named owner, and the `WINDOWS.md` ledger is brought back into agreement with the
registers so `/gsd-complete-milestone` sees the whole picture rather than two rows.
**Verified:** 2026-09-18T10:16:57Z (re-verification, first pass 2026-09-18T09:59:06Z)
**Status:** passed
**Re-verification:** Yes — after code-review fixes (`36.1-REVIEW.md` → `36.1-REVIEW-FIX.md`)

## Re-verification after code-review fixes

The initial verification pass (2026-09-18T09:59:06Z, recorded below unchanged) found `status:
passed`, 6/6 must-haves, no gaps, no human-verification items. Since then, four commits fixed the
two findings (0 critical, 2 warnings, 1 info) raised by `36.1-REVIEW.md` (committed `539fccf2`):

- `40669bf1` — fix(36.1): convert `paladin-ports` doctests to `?` per WR-01 (11 port-trait files)
- `7df4fbd8` — fix(36.1): convert facade service doctests to `?` per WR-01 (`assistant/service.rs`,
  `run/inspector.rs`)
- `0bb4e795` — fix(36.1): correct garrison troubleshooting text in `docs/src/appendix/cli-configuration.md`
  (WR-02)
- `93369b0a` — docs(36.1): correct stale unit/integration test file counts in
  `docs/src/contributing/testing-guide.md` (IN-01, bonus fix, in scope per orchestrator note)

**Scope confirmation:** `git diff --stat 539fccf2..HEAD` shows exactly the 15 files
`36.1-REVIEW-FIX.md` claims (11 `paladin-ports` port files, 2 facade service files, 2 docs pages)
— 77 insertions / 60 deletions, all doc-comment/prose edits. No production `.rs` logic outside
doc comments changed; no new files added or removed. This directly confirms the fixer's own scope
claim rather than trusting it.

**Re-checked items (all independently re-run against the live tree, not taken from
`36.1-REVIEW-FIX.md`'s recorded output):**

| Check | Command | Result |
| --- | --- | --- |
| All 19 named doctests still compile/pass, none `ignore`-fenced, none reverted to `.unwrap()` | `cargo test -p paladin-ports --doc` | `148 passed; 0 failed; 94 ignored` (the 94 ignored are pre-existing, unrelated to the 11 touched files) |
| Facade service doctest 1 | `cargo test -p paladin-ai --doc AssistantService` | `1 passed; 0 failed` |
| Facade service doctest 2 | `cargo test -p paladin-ai --doc RunInspectorService` | `1 passed; 0 failed` |
| No `rust,ignore`/`` ```ignore `` fence added to any of the 13 touched doctest files | `grep -n ignore` on each of the 13 files | Only two unrelated prose hits (`run_repository_port.rs:340`, `waypoint_port.rs:412` — plain English "ignored", not a code fence) |
| Doctest body actually uses `?`/`ok_or`, not `.unwrap()`, on the demonstrated call flow | Read `waypoint_port.rs` lines 160-286 and `inspector.rs` lines 76-112 directly | Confirmed: `store.save(&waypoint).await?`, `.latest(&thread).await?.ok_or(...)?` in `waypoint_port.rs`; `service.inspect(&thread_id).await.err().ok_or(...)?` in `inspector.rs`. The only remaining `.unwrap()` calls in these files are inside the mock `InMemoryWaypoints` impl's `Mutex::lock()` calls (lines 192/203/216/242) — implementation detail of the throwaway fake backend, not the demonstrated example flow, exactly as `36.1-REVIEW-FIX.md` claimed |
| Examples gate still 101/101 OK | `bash scripts/check-public-api-examples.sh` | "All 101 D-05 public API entry points carry a plural '# Examples' heading." |
| `cargo doc --workspace --no-deps` still 0 warnings | `cargo doc --workspace --no-deps 2>&1` | Finished cleanly, no `warning:` lines in output |
| `make api-surface` still unchanged | `make api-surface` | "✅ API surface unchanged" (3959 items — same count as the first verification pass) |
| Docs gate sequence still green | `mdbook-mermaid install docs/` → `mdbook build docs/` → `scripts/check-doc-examples.sh` → `scripts/check-doc-config.sh` | mermaid installed; `mdbook build`: "No broken links found" (only pre-existing, unrelated fragment-resolution warnings); `check-doc-examples.sh`: "All included examples compile.", "README Quick Example is in sync.", "0 checked, 622 skipped, 0 failed"; `check-doc-config.sh`: "151 YAML block(s) checked, 0 failed" |
| `docs/` working tree clean after the gate run | `git status --porcelain -- docs` | Empty — no stray build artifacts |
| Corrected Garrison troubleshooting text matches the real code path | Read `docs/src/appendix/cli-configuration.md` lines 401-416 against `src/application/cli/config/loader.rs:113-129` and `GarrisonConfig` in `src/application/cli/config/paladin_config.rs:136-144` | Confirmed exactly: absent `garrison:` block → `instantiate_garrison` returns `None` (Paladin runs without memory); `garrison:` present but `type` missing/blank → `garrison_type: String` has no `#[serde(default)]`, so deserialization fails before the Paladin starts (startup error, not silent memory loss) — matches the doc's two-state correction verbatim |
| IN-01 bonus fix counts are correct | `ls tests/unit \| wc -l` = 29, `ls tests/integration \| wc -l` = 61, vs. doc's "22 more unit test files" / "56 more integration test files" | 29 − 7 named = 22; 61 − 5 named = 56 — both match |

**Regressions:** None found. `make api-surface` reports the identical 3959-item count as the
first pass, confirming the WR-01 doctest rewrites did not touch any port/service signature. No
previously-verified truth, artifact, or key link changed status.

**Not re-run (per orchestrator instruction — `make test` is the orchestrator's own gate, and the
long D-25 closing-gate sequence — full `clean-code`, `security`, full-workspace `cargo test
--workspace[--all-features]` — was already captured with full transparency in the first pass and
is unaffected by doc-comment-only changes):** the full workspace test suite, `make clean-code`,
`make security`. The scope confirmation above (doc-comment/prose diff only, no production logic
touched) is the basis for not re-running these.

**Verdict:** All must-haves the four fix commits touch remain verified against the live tree.
Nothing regressed. `status: passed` is confirmed to still hold.

---

## Initial verification (2026-09-18T09:59:06Z) — unchanged, carried forward

All six ROADMAP Success Criteria were independently re-verified against the live tree (not
against SUMMARY.md claims), including running the actual `cargo test`/`cargo doc`/CI commands
this phase's own evidence cites, rather than trusting the recorded output alone.

### Observable Truths

| # | Truth (ROADMAP SC) | Status | Evidence |
| --- | --- | --- | --- |
| 1 | SC1/CURR-16 — the six `docs/src` prose defects are closed on the page | ✓ VERIFIED | `contributing-providers.md` lines 272/367 read `use paladin_llm::myprovider::*;` (no `infrastructure::adapters::llm` hits); `testing-guide.md`'s `tests/` tree matches `ls -p tests` exactly (`config.test.yml` at repo root, `cli/`, `fixtures/`, `repository/`, `scripts/` all present); `cli-configuration.md` Garrison/Arsenal entries point at real `instantiate_garrison`/`instantiate_arsenal` in `src/application/cli/config/loader.rs` (confirmed those functions exist) and `grep -n 'verify no TODO'` returns 0; `grep -rnw OpenAiAdapter docs/src` returns 0; `cicd.md` carries 9 "Illustrative only" captions (1 Kubernetes deploy + 8 Best Practices fragments) |
| 2 | SC2/CURR-17 — the `# Examples` apparatus is wired, refrozen at 101, all 19 fixed | ✓ VERIFIED | `bash scripts/check-public-api-examples.sh` exits 0, "All 101 D-05 public API entry points carry a plural '# Examples' heading."; independently ran `cargo test -p paladin-ports --doc` and `cargo test -p paladin-ai --doc` — all 12 Port doctests and all 7 Service doctests for the 19 named entry points show `... ok` (none `ignored`, none `rust,ignore`); `36.1-ENTRY-POINTS.md` exists with the 101-row snapshot; gate wired in `Makefile` (`check-api-examples`, `clean-code` depends on it), `.pre-commit-config.yaml` (`check-api-examples` at `stages: [pre-push]`), and `ci.yml:73-74` (`Check public-API `# Examples` headings`); independently confirmed via `gh api .../jobs/105549054274` that this exact step ran and concluded `success` on CI run 35329107346; PROJECT.md carries 2 dated `Corrected 2026-09-18` in-place corrections (62/8/`paladin-llm` figures); `ci.yml:549` comment ("62 .rs files... 8 declared") matches live `find examples -name '*.rs' \| wc -l` = 62 and `grep -c '^\[\[example\]\]' Cargo.toml` = 8 |
| 3 | SC3/CURR-18 — `cli_isolation_test.rs::test_cli_feature_is_not_default` no longer fails under `--all-features` | ✓ VERIFIED | Ran `cargo test --test cli_isolation --all-features` directly: `test result: ok. 9 passed; 0 failed`. Source (`tests/cli_isolation_test.rs:158-177`) confirms the rewrite reads `CARGO_MANIFEST_DIR/Cargo.toml` via the `toml` crate and asserts `cli` is absent from `features.default`, exactly matching D-15's specified shape — not a `cfg`-based check that would still be fooled by `--all-features` |
| 4 | SC4/CURR-19 — `WINDOWS.md` rows 36-38 fixed, every open register entry has a row, ledger agrees with registers | ✓ VERIFIED | `gsd-tools windows status`: `open_count: 0`, `total_count: 55`. Rows 36-55 individually enumerated and cross-checked against `36.1-EVIDENCE.md`'s closure table 1:1 — every row's kind/phase/status/reason matches the table exactly, including row 39's disclosed accidental-mint waiver and rows 52-55's four D-07 fallback waivers. Row 38 (`ToolErrorMode::FailRun` raw-error leak) confirmed `fixed`; the fix itself is present in the code at both call sites (see truth 6) |
| 5 | SC5/CURR-20 — both `todos/pending/` items dispositioned with owner + re-check date, not left undated | ✓ VERIFIED | Both files retain `owner: repo maintainer`, `deferred_past: v0.10.0`, `recheck_by: 2026-10-16`, `dispositioned_by: Phase 36.1 (2026-09-18)` frontmatter plus a `## Disposition (Phase 36.1, 2026-09-18)` section; `gsd-tools list-todos` still surfaces both 2 files |
| 6 | SC6/CURR-21 — closing gates green, surface unchanged, CHANGELOG carries the bullets, ADR-0033 amended | ✓ VERIFIED | Independently re-ran `make api-surface`: "✅ API surface unchanged" (3959 items). `CHANGELOG.md` `[0.10.0]` `### Fixed` carries the D-03 bullet ("A failed-tool error's reason is now redacted and bounded..."); `### Documentation` carries the phase-summary bullet (six pages, 19 examples, new gate, ADR-0033 amendment). `0033-cargo-doc-warning-bar.md` carries the full 2026-09-18 amendment listing all eight suppressions; independently confirmed all eight `#![allow(rustdoc::...)]` attributes exist verbatim at the cited file:line (`src/lib.rs:117-119`, `crates/paladin-llm/src/lib.rs:49`, `crates/paladin-ports/src/lib.rs:51-52`, `crates/paladin-storage/src/lib.rs:19`, `crates/paladin-notifications/src/lib.rs:14`) — no `.rs` file changed for D-29. Other closing gates (`clean-code`, `security`, `cargo test --workspace[--all-features]`, mdBook) taken from `36.1-EVIDENCE.md`'s verbatim capture file plus the independently re-confirmed CI run (see truth 2) rather than re-run in full here (per orchestrator guidance to avoid re-running the long gates) |

**Score:** 6/6 truths verified (0 present, behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `tests/cli_isolation_test.rs` | rewritten guard reads manifest, not `cfg` | ✓ VERIFIED | Lines 158-177 match D-15 exactly; behaviorally re-run, 9/9 pass under `--all-features` |
| `src/application/services/paladin/paladin_execution_service.rs` | both `FailRun` arms sanitized | ✓ VERIFIED | Lines 1648-1653 (handoff arm) and 1771-1776 (regular arm) both call `ToolResultFormatter::sanitize_tool_text`; pinning test re-run and passes |
| `src/infrastructure/adapters/arsenal/tool_result_formatter.rs` | `sanitize_tool_text` widened to `pub(crate)` | ✓ VERIFIED | Line 280: `pub(crate) fn sanitize_tool_text` — confirmed not `pub`, `make api-surface` confirms unchanged |
| `docs/src/contributing/contributing-providers.md` | relocated adapter path | ✓ VERIFIED | See truth 1 |
| `docs/src/contributing/testing-guide.md` | corrected `tests/` tree | ✓ VERIFIED | See truth 1; re-verified counts after IN-01 fix (see re-verification section above) |
| `docs/src/appendix/cli-configuration.md` | live-wiring troubleshooting entries | ✓ VERIFIED | See truth 1; re-verified against source after WR-02 fix (see re-verification section above) |
| `docs/src/deployment/cicd.md` | illustrative captions | ✓ VERIFIED | 9 caption hits |
| `.planning/PROJECT.md` | dated corrections at both sites | ✓ VERIFIED | 2 `Corrected 2026-09-18` hits |
| `.planning/phases/36.1-deferred-items-closure/36.1-ENTRY-POINTS.md` | 101-row snapshot | ✓ VERIFIED | Exists, header + table present |
| `Makefile`, `.pre-commit-config.yaml`, `.github/workflows/ci.yml` | three-place gate wiring | ✓ VERIFIED | All three confirmed; CI run confirms the step executes |
| `.planning/decisions/0033-cargo-doc-warning-bar.md` | dated amendment | ✓ VERIFIED | Full 8-attribute table present, matches source exactly |
| `.planning/WINDOWS.md` | rows 36-55 dispositioned | ✓ VERIFIED | `open_count: 0`; all rows match closure table |
| `.planning/todos/pending/2026-08-13-*.md`, `2026-09-13-*.md` | dispositioned in place | ✓ VERIFIED | Both retain frontmatter + Disposition section |
| `CHANGELOG.md` | Fixed + Documentation bullets | ✓ VERIFIED | Both present under `[0.10.0]` |
| 19 doc-comment sites (12 Port traits, 7 Services) | plural `# Examples` heading + compiling, non-ignored doctest, using `?` per project convention | ✓ VERIFIED | All 19 individually located and their doctests run: all `ok`; the 13 touched by the WR-01 fix independently re-confirmed to use `?`/`ok_or` on the demonstrated call flow, not `.unwrap()` |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| `ci.yml` `lint` job | `scripts/check-public-api-examples.sh` | new step "Check public-API `# Examples` headings" | ✓ WIRED | Confirmed present in workflow file AND confirmed to have actually run and concluded `success` on live CI run 35329107346, job 105549054274, independently queried via `gh api` |
| `Makefile` `clean-code` | `check-api-examples` target | dependency list | ✓ WIRED | `clean-code: fmt lint lint-shell check doc-check check-api-examples` |
| `.pre-commit-config.yaml` | `check-api-examples` | pre-push hook entry | ✓ WIRED | Entry present with `make check-api-examples`, `stages: [pre-push]` |
| `cli-configuration.md` prose | `src/application/cli/config/loader.rs` | named functions `instantiate_garrison`/`instantiate_arsenal` | ✓ WIRED | Both functions confirmed to exist at the named location; the WR-02 correction re-verified against the exact `instantiate_garrison`/`GarrisonConfig` source in the re-verification pass |
| `paladin_execution_service.rs` `FailRun` arms | `tool_result_formatter.rs::sanitize_tool_text` | direct call | ✓ WIRED | Confirmed at both call sites (lines 1650, 1774) |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| CURR-16 | 36.1-03, 36.1-04 | Six `docs/src` prose defects closed | ✓ SATISFIED | Truth 1 |
| CURR-17 | 36.1-05 through 36.1-12 | `# Examples` apparatus wired, refrozen, 19 fixed | ✓ SATISFIED | Truth 2 |
| CURR-18 | 36.1-01 | `cli_isolation` no longer fails under `--all-features` | ✓ SATISFIED | Truth 3 |
| CURR-19 | 36.1-02, 36.1-13 | Ledger rows 36-38 + all register entries closed | ✓ SATISFIED | Truth 4 |
| CURR-20 | 36.1-12 | Both todos dispositioned with owner/re-check | ✓ SATISFIED | Truth 5 |
| CURR-21 | 36.1-12, 36.1-13, 36.1-14 | Closing gates green, surface unchanged, CHANGELOG, ADR-0033 | ✓ SATISFIED | Truth 6 |

All six requirement IDs declared in plan frontmatter (spread across the 14 plans) match exactly
the six IDs REQUIREMENTS.md maps to Phase 36.1 — no orphaned requirements found.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| `cli_isolation` passes under `--all-features` | `cargo test --test cli_isolation --all-features` | `9 passed; 0 failed` | ✓ PASS |
| D-03 pinning test passes | `cargo test -p paladin-ai --lib fail_run_redacts_a_secret_in_the_reason` | `1 passed; 0 failed` | ✓ PASS |
| All 12 Port `# Examples` doctests compile and pass | `cargo test -p paladin-ports --doc` | `148 passed; 0 failed` (target names for all 12 ports individually confirmed `ok`); re-confirmed in re-verification pass at 148/0 | ✓ PASS |
| All 7 Service `# Examples` doctests compile and pass | `cargo test -p paladin-ai --doc` | `154 passed; 0 failed` (all 7 named services confirmed `ok`); the 2 touched by WR-01 re-confirmed individually (`AssistantService`, `RunInspectorService`) at 1/0 each | ✓ PASS |
| Examples gate script | `bash scripts/check-public-api-examples.sh` | exit 0, "All 101... carry a plural '# Examples' heading." | ✓ PASS |
| API surface unchanged | `make api-surface` | "API surface unchanged" (3959 items); re-confirmed identical count in re-verification pass | ✓ PASS |
| CI required step actually ran | `gh api repos/.../jobs/105549054274` | `Check public-API `# Examples` headings` → `success` | ✓ PASS |
| CI run overall required-job health | `gh run view 35329107346 --json jobs` | `Code Quality`, `Unit Tests (stable/beta)`, `API Surface Tracking`, `Coverage` all `success`; only non-required `Docker Build` still `in_progress` | ✓ PASS |
| All commits cited in the closure table exist | `git cat-file -e <sha>` for 17 distinct SHAs | all present | ✓ PASS |
| Ledger agrees with the closure table | `gsd-tools windows status` rows 36-55 vs `36.1-EVIDENCE.md` table | 1:1 match | ✓ PASS |

### Anti-Patterns Found

None. Scanned all directly-modified first-party files (`tests/cli_isolation_test.rs`,
`paladin_execution_service.rs`, `tool_result_formatter.rs`, the six `docs/src` pages,
`PROJECT.md`, `check-public-api-examples.sh`, `Makefile`, `.pre-commit-config.yaml`, `ci.yml`)
for `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` markers. The only `TBD` hits are pre-existing,
unrelated CI gate logic (`ci.yml:472/475`, a MIGRATION.md "no TBD" check) and a PROJECT.md
sentence describing that same pre-existing gate — neither introduced by this phase, neither an
unresolved debt marker in phase-modified code.

The re-verification pass additionally scanned the 15 files touched by the four fix commits for
the same markers plus stub patterns (`return null`, hardcoded empty returns, `console.log`-only
bodies) — none found; the diff is entirely doc-comment/prose text plus the `?`/`ok_or` doctest
conversions.

One minor honesty note (not a gap): plan 36.1-02's SUMMARY discloses that only the regular
Arsenal-tool `FailRun` arm got a direct secret-pinning test; the handoff arm's coverage rests on
a "source-level guarantee" (identical `sanitize_tool_text` call, confirmed by direct code
inspection at both call sites) rather than a second behavioral test with an injected credential.
This was explicitly pre-authorized by the plan's own Task 2 action text ("if the handoff arm
cannot be driven from this module without adding production scaffolding, do not add
scaffolding... rely on the source-level guarantee") and is disclosed, not hidden. The underlying
truth — both arms route through the sanitizer — is directly code-verified regardless. This note
is unaffected by the code-review fixes (no file it concerns was touched) and is carried forward
unchanged.

### Human Verification Required

None. Every must-have in this phase resolves to a command-line-checkable artifact, a running
test, or a live CI job status, all of which were independently reproduced during both the
initial verification pass and this re-verification pass.

### Gaps Summary

No gaps found, in either the initial pass or this re-verification. All six ROADMAP Success
Criteria and all six CURR-16..21 requirement IDs are directly, behaviorally verified against the
live tree — not merely cited from SUMMARY.md or REVIEW-FIX.md. The re-verification pass
independently re-ran every check the four fix commits could plausibly affect (doctests, the
examples gate, `cargo doc` warnings, `make api-surface`, the full docs gate sequence, and a direct
source-vs-prose comparison for the corrected Garrison troubleshooting text) rather than trusting
`36.1-REVIEW-FIX.md`'s own recorded output, and found no regressions. Per orchestrator guidance,
the long D-25 closing gates (full `make clean-code`, `make security`, full-workspace `cargo test
--workspace[--all-features]`) were not re-run in this pass — `make test` is the orchestrator's own
gate, and the four fix commits are doc-comment/prose-only (confirmed via `git diff --stat
539fccf2..HEAD`), so they cannot affect those gates' outcome.

---

_Verified: 2026-09-18T10:16:57Z (re-verification; initial pass 2026-09-18T09:59:06Z)_
_Verifier: Claude (gsd-verifier)_
