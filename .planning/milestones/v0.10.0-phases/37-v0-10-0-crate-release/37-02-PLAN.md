---
phase: 37-v0-10-0-crate-release
plan: 02
type: execute
wave: 2
depends_on: [37-01]
files_modified:
  - .planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md
autonomous: true
requirements: [SHIP-05]
must_haves:
  truths:
    - "Six of D-06's seven re-seal gates (rows 1, 2, 3, 4, 5, 7) are re-run on the phase's local head SHA and each is recorded in `37-CI-EVIDENCE.md` with its command, the SHA it ran on, and its verbatim result."
    - "Every count-shaped gate is asserted on the captured number, never on a command's exit code: the placeholder-marker and changelog greps compare a captured value to zero, and the two frozen compatibility test targets assert their selected test counts (9 and 7) so a zero-selecting filter cannot pass."
    - "`cargo semver-checks` is re-run for exactly the eleven pre-existing publishable crates against the `0.9.0` baseline, with `paladin-eval` excluded for the recorded reason that it has no published baseline."
    - "A failing gate stops the plan at a blocking checkpoint with the command, SHA and failing output recorded, and nothing is fixed — not even a one-line documentation or citation change (D-14)."
  artifacts:
    - path: ".planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md"
      provides: "Local sweep rows for D-06 gate rows 1, 2, 3, 4, 5 and 7"
      contains: "## Local sweep"
  key_links:
    - from: ".planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md"
      to: ".cargo/semver-checks-allowlist.toml"
      via: "gate row 1's allowlist set-equality check (`make check-migration-allowlist`) compares MIGRATION.md §9.2 against this file row-for-row"
      pattern: "check-migration-allowlist"
  prohibitions:
    - statement: "MUST NOT silently fix, work around, or re-run past a red gate; a red gate is recorded with its command, SHA and failing output and stops the phase at a blocking checkpoint (D-14, D-15)."
      status: unverified
      verification: judgment
    - statement: "MUST NOT record any gate as green that was not actually run on the stated SHA; a gate this devcontainer cannot measure is labelled CI-attributed, never a local pass (D-00e, D-09)."
      status: unverified
      verification: judgment
---

<objective>
Re-run six of D-06's seven release gates locally on this phase's final pre-merge tree and record
each one in `37-CI-EVIDENCE.md`'s Local sweep, in the shape `29`/`33-CI-EVIDENCE.md` established.

Purpose: SC1 requires the Phase 29 gate set re-run green on the final commit. Phases 29 and 33
ran this identical gate set on this identical tree shape, so every command is already proven —
this plan is evidence capture, not tool building. The seventh gate (`make publish-dry-run`) is
plan 37-03's, alone, because it is the disk-hungry one.
Output: Local sweep rows for gate rows 1, 2, 3, 4, 5 and 7, each with command + SHA + result +
verdict.

Governing constraints: this plan runs sequentially on the main checkout with hooks ON; the phase
prohibitions P1-P5 in `37-01-PLAN.md`'s `must_haves.prohibitions` apply in full. **D-14 is the
operating rule of this plan**: any red gate is recorded and the plan halts for the maintainer. The
agent fixes nothing, and does not re-run a locally-red gate hoping for a different answer (D-15's
flake exception is scoped to CI infrastructure only, never to a local run).
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-v0-10-0-crate-release/37-CONTEXT.md
@.planning/phases/37-v0-10-0-crate-release/37-RESEARCH.md
@.planning/phases/37-v0-10-0-crate-release/37-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate rows 1 and 7 — offline register guards and the changelog completeness reading</name>
  <precondition>`/workspace` has at least 20 GiB free (`df -BG --output=avail /workspace`) and `git status --porcelain` is empty — both recorded by plan 37-01 Task 1</precondition>
  <files>.planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md` — append to the existing `## Local sweep` table, continuing the row numbering from row 1
    - `.planning/phases/37-v0-10-0-crate-release/37-RESEARCH.md` Q5 rows 1 and 7 plus the "Bonus finding" on `make check-gates`, and Pitfall 4 (the zero-count exit-code trap, with the exact capture-and-compare idiom)
    - `.project/v0.10.0/09-program-acceptance-audit.md` lines 1418-1425 and 1499-1510 — §11's own recorded readings for these two gates, the numbers this sweep is compared against
    - `Makefile` lines 200-222 — the `check-migration-allowlist` target and the seven-target `check-gates` bundle
  </read_first>
  <action>
Record the gate-row-1 and gate-row-7 readings. Every count-shaped check captures the number into a
variable and compares the value; none of them relies on an exit code.

Gate row 1 (register integrity):
- `make check-migration-allowlist` — expect the §9.2 register and the `cargo semver-checks`
  allowlist to be set-equal in both directions. §11 recorded 15 pairs; record whatever this run
  prints, and if the pair count differs from 15, record the delta as a finding (a pair count can
  legitimately change; a set-equality failure cannot — that is red).
- `make check-gates` — the seven-target offline bundle (changelog presence, crate names, advisory
  register, workflow suppressions, workflow triggers, CodeQL dismissals, migration allowlist).
  Record each target's verdict. A failure of `check-changelogs` is red: it is the machine check
  behind gate row 7's "every publishable crate's changelog carries this version".
- The `MIGRATION.md` placeholder-marker count is already Local sweep row 1 (plan 37-01); cross
  reference it rather than re-running it.

Gate row 7 (changelog completeness) — split deliberately into hard assertions and recorded
readings, so the executor never has to invent a verdict:
- **Hard assertions (a failure is red, D-14 stop):** the count of unshipped-section headings in
  root `CHANGELOG.md` is zero; the `[0.10.0]` heading occurs exactly once; the count of the
  withheld next-version string across `CHANGELOG.md` and `MIGRATION.md` is zero; and
  `make check-changelogs` (run above) is green. The exact grep expressions for the first and
  third are in `37-RESEARCH.md` Q5 row 7 and §11 of the corpus audit — reuse them verbatim.
- **Recorded readings (a zero is a finding, not a red gate):** topic counts inside the `[0.10.0]`
  section for the subject matter added since §11's re-seal — Phases 34-36.1 are documentation
  phases, so count `mdBook`, `rustdoc`, `examples`, `intra-doc` — and re-read §11's own three
  topics (`rag`, `TokenUsage`, `Commissary`) to confirm they have not regressed to zero. Record
  every number. **A zero on a documentation-phase topic is recorded as a carried finding under
  D-00d and is explicitly not a red gate**: D-06 gate row 7 asks that the `[0.10.0]` section be
  complete, and the binding machine checks for that are `make check-changelogs` and
  `scripts/check-release-consistency.sh` clause 2 (a version heading per publishable crate) —
  both hard-asserted above. Do not edit `CHANGELOG.md` either way (D-14).
- Record each reading as its own numbered Local sweep row with the command, the head SHA from
  plan 37-01's provenance block, the verbatim result and the verdict. Commit with the plain-git
  form and a >= 600 s timeout.
  </action>
  <acceptance_criteria>
    - `37-CI-EVIDENCE.md`'s Local sweep contains rows for `make check-migration-allowlist` and `make check-gates`, each with a verdict
    - The Local sweep records the unshipped-heading count as `0`, the `[0.10.0]` heading occurrence as `1`, and the withheld-next-version count as `0`
    - The Local sweep records at least seven topic counts for the `[0.10.0]` section (four documentation-phase topics plus §11's three), each as an integer
    - Any zero topic count appears as a bullet in `## Findings carried forward (D-00d)` and is labelled not-a-gate-failure
    - `git diff HEAD~1 --numstat` shows no file outside `.planning/` changed, and `CHANGELOG.md` and `MIGRATION.md` are unmodified in the working tree (`git status --porcelain CHANGELOG.md MIGRATION.md` empty)
  </acceptance_criteria>
  <verify>
    <automated>make check-gates && U=$(grep -c '^## \[Unreleased\]' CHANGELOG.md; true); test "$U" = "0" && H=$(grep -c '^## \[0.10.0\]' CHANGELOG.md; true); test "$H" = "1" && N=$(cat CHANGELOG.md MIGRATION.md | grep -c 'v0[.]11[.]0'; true); test "$N" = "0" && test -z "$(git status --porcelain CHANGELOG.md MIGRATION.md)"</automated>
  </verify>
  <done>Gate rows 1 and 7 are recorded in the Local sweep with hard assertions green and every topic reading captured as a number; any zero reading is a carried finding, not a fix.</done>
</task>

<task type="auto">
  <name>Task 2: Gate rows 2 and 3 — the two frozen backward-compatibility test targets, with count assertions</name>
  <precondition>`/workspace` has at least 20 GiB free (`df -BG --output=avail /workspace`)</precondition>
  <files>.planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/37-v0-10-0-crate-release/37-RESEARCH.md` Q5 rows 2 and 3 (exact commands, prior counts, where each also runs in CI)
    - `.project/v0.10.0/09-program-acceptance-audit.md` lines 1427-1435 — §11's own recorded 9-and-7 counts and the note that the OpenAPI count rose from 6 to 7 legitimately
    - `tests/integration/v0_9_config_boot_test.rs` — what the frozen v0.9 config boot asserts
    - `crates/paladin-web/tests/openapi_golden_v0_9.rs` — the path-restricted golden diff with `$ref` closure
  </read_first>
  <action>
Re-run SHIP-02's two frozen proofs and record them. Both are count-asserted, because a cargo test
filter that selects zero tests still exits 0 — the pass condition is the printed count, not the
exit status.

- Gate row 2: the frozen v0.9 config boot target under the `web-server` feature. Assert the
  printed result line reports 9 passed and 0 failed. A count other than 9 is recorded verbatim:
  a *higher* count is recorded as a finding (a test was added since §11) and is not itself red; a
  *lower* count, or any failure, is red — D-14 stop.
- Gate row 3: the `paladin-web` OpenAPI golden-diff target. Assert 7 passed and 0 failed, with the
  same higher/lower rule (§11 already recorded the legitimate 6→7 rise since Phase 29).
- Capture each command's full `test result:` line verbatim into the Local sweep row, with the head
  SHA. Note in each row that the same target also runs in CI (row 2 inside the platform-API E2E
  job, which independently asserts at least 9 tests are selected; row 3 inside the normal
  `paladin-web` test sweep), so the pre-merge CI run in plan 37-07 re-proves both.
- Package-name trap: this workspace's crates are `paladin-ai` (root) and `paladin-ai-core`;
  `-p paladin` and `-p paladin-core` do not exist and exit 101. Use the names exactly as written
  in Q5.
- Commit with the plain-git form and a >= 600 s timeout.
  </action>
  <acceptance_criteria>
    - Both test commands exit 0 and their verbatim `test result:` lines are recorded in the Local sweep
    - The recorded line for gate row 2 contains `9 passed` and `0 failed`; the recorded line for gate row 3 contains `7 passed` and `0 failed` (or a documented higher count recorded as a finding)
    - Each row names the head SHA the target ran on and states where the same target runs in CI
    - No source file is modified: `git status --porcelain -- crates src tests` is empty
  </acceptance_criteria>
  <verify>
    <automated>cargo test --features web-server --test v0_9_config_boot > /tmp/g2.txt 2>&1 && grep -q '9 passed' /tmp/g2.txt && grep -q '0 failed' /tmp/g2.txt && cargo test -p paladin-web --test openapi_golden_v0_9 > /tmp/g3.txt 2>&1 && grep -q '7 passed' /tmp/g3.txt && grep -q '0 failed' /tmp/g3.txt && test -z "$(git status --porcelain -- crates src tests)"</automated>
  </verify>
  <done>Gate rows 2 and 3 are green with their selected-test counts asserted (not merely exit codes) and recorded verbatim in the Local sweep.</done>
</task>

<task type="auto">
  <name>Task 3: Gate rows 4 and 5 — semver-checks for the eleven baselined crates, and the MSRV floor</name>
  <precondition>`/workspace` has at least 20 GiB free (`df -BG --output=avail /workspace`)</precondition>
  <files>.planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/37-v0-10-0-crate-release/37-RESEARCH.md` Q5 rows 4 and 5 (exact commands, the eleven-package list, the CI job definitions, prior runtimes)
    - `.project/v0.10.0/09-program-acceptance-audit.md` lines 1437-1457 — §11's own 11/11 result shape (`major change` / `0 checks: 0 pass, 254 skip` / `no semver update required`) and the MSRV timing
    - `.github/workflows/ci.yml` lines 262-370 — the `msrv` job's exact flag set and the `semver` job's package list with its `paladin-eval` exclusion comment
  </read_first>
  <action>
Re-run the API-break and MSRV gates and record them.

- Gate row 4: `cargo semver-checks check-release --package <pkg> --default-features
  --baseline-version 0.9.0` for exactly the eleven crates the CI `semver` job names —
  `paladin-ai`, `paladin-ai-core`, `paladin-ports`, `paladin-battalion`, `paladin-herald`,
  `paladin-llm`, `paladin-memory`, `paladin-storage`, `paladin-notifications`, `paladin-content`,
  `paladin-web`. `paladin-eval` is excluded, and the row must state why in one clause: it has no
  published `0.9.0` baseline, so the tool would error on a nonexistent baseline (this is the same
  underlying fact D-17 exists to close, and the exclusion is CI's own, not this phase's choice).
  Record the result shape per crate and the 11/11 tally. At this version boundary the expected
  shape is a recognised major change with zero checks evaluated — record it exactly as printed
  rather than re-interpreting it; §11 explains why (no lint covers an inherent method's or free
  function's signature change).
- Gate row 5: the MSRV floor — `cargo check` over the whole workspace with all features and all
  targets, under toolchain 1.88 via `RUSTUP_TOOLCHAIN=1.88`. Match CI's own flag set (CI does not
  pass `--locked`; note in the row whether you did and why, so the row is comparable to §11's).
  Record the `Finished` line, the wall time, and that warnings were zero (CI treats this job's
  warnings as a failure).
- Record both as numbered Local sweep rows with the head SHA. Commit with the plain-git form and a
  >= 600 s timeout.
  </action>
  <acceptance_criteria>
    - The Local sweep records eleven `cargo semver-checks` invocations by package name with an 11/11 tally and the per-crate result shape as printed
    - The `paladin-eval` exclusion is recorded with the no-published-baseline reason
    - The Local sweep records the MSRV command with `RUSTUP_TOOLCHAIN=1.88`, its `Finished` line, and a zero-warning statement
    - No source file, manifest or lockfile is modified: `git status --porcelain -- crates src Cargo.toml Cargo.lock` is empty
  </acceptance_criteria>
  <verify>
    <automated>env RUSTUP_TOOLCHAIN=1.88 cargo check --workspace --all-features --all-targets && for p in paladin-ai paladin-ai-core paladin-ports paladin-battalion paladin-herald paladin-llm paladin-memory paladin-storage paladin-notifications paladin-content paladin-web; do cargo semver-checks check-release --package "$p" --default-features --baseline-version 0.9.0 || exit 1; done; test -z "$(git status --porcelain -- crates src Cargo.toml Cargo.lock)"</automated>
  </verify>
  <done>Gate rows 4 and 5 are green — eleven semver checks against the `0.9.0` baseline with the exclusion reasoned, and the MSRV floor clean with zero warnings — both recorded in the Local sweep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local gate output → evidence record | Command output is transcribed verbatim into an artifact a human later signs; a summarised or optimistic transcription would launder an unproven claim into release authority |
| `cargo semver-checks` → published `0.9.0` baseline | The tool fetches the prior published crate from crates.io read-only to diff against; a missing baseline is an error, never a silent skip |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-08 | Repudiation | Local sweep rows for gate rows 1-5, 7 | high | mitigate | Every row carries command + head SHA + verbatim result + verdict; count-shaped gates assert the captured number, never the exit code, so neither a zero-count grep nor a zero-selecting test filter can be recorded as a pass. |
| T-37-09 | Tampering | A red gate quietly repaired to keep the sweep green | high | mitigate | D-14 is this plan's operating rule: a red gate halts the plan with the command, SHA and failing output recorded, and the agent changes nothing — no documentation, citation or allowlist edit. D-15's flake exception does not apply to a local run. |
| T-37-04 | Repudiation | Gate rows whose prior-phase counts differ | medium | mitigate | A count that differs from §11's recorded reading is transcribed with the delta and classified by the pre-declared higher/lower rule, not by executor judgment at the keyboard. |
</threat_model>

<artifacts_this_phase_produces>
See `37-01-PLAN.md` → "Artifacts this phase produces" for the phase-wide list. This plan adds no
new file or identifier of its own; it appends numbered rows to the existing
`.planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md`. No code symbols, CLI flags or
struct fields are created anywhere in this phase, and no file under `src/` or `crates/*/src/` is
modified.
</artifacts_this_phase_produces>

<verification>
- Six of D-06's seven gate rows are recorded with command + SHA + verbatim result + verdict
- Every count-shaped assertion compares a captured value (zero-count greps, selected-test counts)
- The eleven-crate semver list matches CI's `semver` job exactly, with the `paladin-eval` exclusion reasoned in the row
- No file outside `.planning/` is modified by this plan's three commits
</verification>

<success_criteria>
- `make check-gates` exits 0 and its per-target verdicts are recorded
- The unshipped-heading count is `0`, the `[0.10.0]` heading count is `1`, and the withheld-next-version count is `0`
- Both frozen compatibility targets pass with their counts asserted (9 and 7)
- 11/11 `cargo semver-checks` invocations pass; the MSRV check exits 0 with zero warnings
- `git status --porcelain -- crates src tests Cargo.toml Cargo.lock` is empty after every task
</success_criteria>

<output>
Create `.planning/phases/37-v0-10-0-crate-release/37-02-SUMMARY.md` when done. If any gate went
red, the SUMMARY records the command, the SHA, the failing output, and the fact that nothing was
fixed — and the plan halts for the maintainer rather than continuing to plan 37-03.
</output>
