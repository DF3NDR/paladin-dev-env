---
phase: 37.1-v0-10-1-patch-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/37.1-v0-10-1-patch-release/37.1-CI-EVIDENCE.md
  - .planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md
autonomous: true
requirements: [SHIP-06]
must_haves:
  truths:
    - "Work for this phase lives on a new branch `release/v0.10.1` cut from the merge commit `1d4a9724` (`origin/main`), and every `.planning/`-only commit that was local to `feature/phase-33` is cherry-picked onto it in oldest-to-newest order (D-06)."
    - "The cherry-pick range is resolved live from git at execution time as `1bb94063..<tip of feature/phase-33>`; no fixed commit count is used or asserted anywhere, because the count moved from 14 (CONTEXT.md D-06) to 16 (RESEARCH.md) to 19 (planning time) and will move again."
    - "`feature/phase-33` is retired at the moment the new branch exists: it is never pushed, never rewritten, never reset, and gains no commit after the range endpoint this plan captured; any commit that lands on it after that point is recorded as a D-00d finding and its content re-applied on `release/v0.10.1`."
    - "Outside `.planning/`, `release/v0.10.1`'s tree is byte-identical to `origin/main` at the moment the cherry-pick completes — the release PR's diff is therefore exactly what this phase changes plus the planning record (D-06, D-07)."
    - "A dated old-SHA to new-SHA map for every cherry-picked commit is appended to `37-CI-EVIDENCE.md`, so the in-file citations there (`9154aba7`, `e463936a`, `4e48d55f`, `d969100c`, `cc1e56a7`, …) stay resolvable after the rewrite (D-06)."
    - "`37.1-CI-EVIDENCE.md` exists in the three-table house form (Local sweep, CI-run, Registry) and is this phase's only new evidence file; `37-CI-EVIDENCE.md` receives dated additions only and no existing line of it is edited (D-11, D-00d)."
  artifacts:
    - path: ".planning/phases/37.1-v0-10-1-patch-release/37.1-CI-EVIDENCE.md"
      provides: "This phase's CI evidence file in the three-table house form, plus its Provenance block and Findings section"
      contains: "## Local sweep"
    - path: ".planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md"
      provides: "A dated old-to-new SHA map for the cherry-picked commits and a one-line forward pointer to this phase"
      contains: "SHA map"
  key_links:
    - from: "the merge commit `1d4a9724` on `origin/main`"
      to: "the branch `release/v0.10.1`"
      via: "the branch is cut from that exact commit, so the release PR's base is the tree that tag `v0.10.0` was cut on"
      pattern: "1d4a9724"
    - from: ".planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md"
      to: ".planning/phases/37.1-v0-10-1-patch-release/37.1-CI-EVIDENCE.md"
      via: "a dated forward-pointer line plus the old-to-new SHA map keeps Phase 37's citations resolvable across the rewrite"
      pattern: "37.1-CI-EVIDENCE"
  prohibitions:
    - statement: "MUST NOT push, rewrite, reset, rebase or delete `feature/phase-33` — the commits are carried by cherry-pick and the old branch is left exactly as it is for the maintainer to delete (D-06)."
      status: unverified
      verification: judgment
    - statement: "MUST NOT pass `--no-verify` to any git commit in this plan: the pre-commit `cargo-fmt`/`cargo-clippy` hooks are `always_run: true` and run in full on every docs-only commit by design; budget the wall-clock time instead of bypassing the hook."
      status: unverified
      verification: judgment
    - statement: "MUST NOT hard-code a commit count for the cherry-pick range, assert one, or write one into any evidence file — the range is live."
      status: unverified
      verification: judgment
---

<objective>
Stand up the release branch and this phase's evidence file, carrying the whole local planning
record forward without stranding a citation.

Purpose: D-06 puts this phase's work on `release/v0.10.1` cut from the merge commit tag `v0.10.0`
was cut on, so the release PR's diff is exactly what changed since `v0.10.0`. The local-only
`.planning/` commits on `feature/phase-33` have to come with it, and their SHAs change when they
do — which is why the old-to-new map is part of this plan and not an afterthought. D-11 opens this
phase's own CI-evidence file rather than extending Phase 37's, which stays sealed at the SHAs it
verified and receives dated additions only.
Output: the `release/v0.10.1` branch carrying the cherry-picked planning record, a new
`37.1-CI-EVIDENCE.md` in house form, and two dated additions to `37-CI-EVIDENCE.md`.

Governing constraints: D-00d (findings recorded, never fixed silently; amend-at-source with dated
additions), D-06 (branch mechanics), D-11 (evidence files), D-08 stage 1 (build wave — red is
expected and fixed inside the six success criteria).

**Spec-less probe fallback — SKIPPED, visibly.** No `-SPEC.md` exists for this phase, so both
`## Edge Coverage` and `## Prohibitions` are absent (`EDGE_ABSENT=1`, `PROHIB_ABSENT=1`). The
fallback derives predicates from requirement TEXT, and this phase's only requirement ID
(`SHIP-06`) does not exist in `REQUIREMENTS.md` yet — it is minted by this phase (plan 37.1-15,
D-02). There was therefore nothing to probe and **no probe-derived edge or prohibition predicates
were generated for this phase**. Every plan's `must_haves.truths` is derived instead from the six
ROADMAP success criteria and the locked CONTEXT.md decisions, with maintainer-gated and
post-release truths carried as `verification: backstop` markers.

**Capability detectors — both ran, neither fired.** `api-coverage` reported `detected: false`
(this phase integrates no external API or SDK; the crates.io reads are read-only registry probes,
not an integration), so no `COVERAGE.md` is written and none is fabricated. `assumption-delta`
reported `detected: false` (no singular-to-plural, required-to-optional or derived-to-chosen
transition in scope). The schema-push gate found no ORM/schema files anywhere in scope and is
skipped silently.

**ORCHESTRATOR NOTE — progress tracking moves branch here.** Addressed to the
`/gsd-execute-phase` orchestrator, not to this plan's executor. This plan's Task 1 changes the
checked-out branch from `feature/phase-33` to `release/v0.10.1`. Every progress-tracking commit
from wave 1 onward — including this wave's — must land on `release/v0.10.1`, never on
`feature/phase-33`, which is retired the moment Task 1 completes. Do not create a tracking commit
between this plan's dispatch and Task 1's completion.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/37.1-v0-10-1-patch-release/37.1-CONTEXT.md
@.planning/phases/37.1-v0-10-1-patch-release/37.1-RESEARCH.md
@.planning/phases/37.1-v0-10-1-patch-release/37.1-PATTERNS.md
@.planning/phases/37.1-v0-10-1-patch-release/37.1-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Warm the clippy hook, cut `release/v0.10.1` from `1d4a9724`, and cherry-pick the live planning range</name>
  <precondition>`git rev-parse 1d4a9724^{commit}` resolves and equals `origin/main` after `git fetch origin`; `git merge-base --is-ancestor 1bb94063 origin/main` succeeds — both are the premises D-06 rests on and RESEARCH.md confirmed live</precondition>
  <files>(no tracked file is modified by this task — it creates a git branch and replays commits)</files>
  <read_first>
    - `.planning/phases/37.1-v0-10-1-patch-release/37.1-CONTEXT.md` — D-06 in full (branch name, base commit, cherry-pick order, the accepted SHA change, the retirement of `feature/phase-33`), and D-00d
    - `.planning/phases/37.1-v0-10-1-patch-release/37.1-RESEARCH.md` § "Branch Mechanics (D-06) — CORRECTION, not confirmation" — the live-range finding, the tree-identity confirmation, the no-merge-commits check, and the `always_run: true` pre-commit hook finding
    - `.pre-commit-config.yaml` — the `cargo-fmt` and `cargo-clippy` hook declarations, to see for yourself that `always_run: true` overrides the `types: [rust]` filter
    - `git log --oneline 1bb94063..refs/heads/feature/phase-33` and `git status --porcelain` — the actual range and working-tree state at execution time, which is the only figure that counts
  </read_first>
  <action>
Cut the release branch and replay the planning record onto it, resolving the range live.

1. Run one full `cargo clippy --workspace --all-targets --all-features` warm-up before anything
   else. The pre-commit hooks that run a full workspace format-and-lint on every commit are
   declared unconditional on purpose; a cold target directory turns each replayed commit into a
   cold lint. Warming once keeps the rest of the sequence incremental. Do not bypass the hook.
2. `git fetch origin --tags`. Assert `origin/main` resolves to `1d4a9724cc219b85856a23012543458d62559e47`
   and record the actual value either way — if `main` has moved, stop and record it rather than
   cutting from a different base, because every SHA in this phase's evidence is anchored to it.
3. **Capture the range endpoint before changing branches.** Resolve the current tip of
   `refs/heads/feature/phase-33` into a variable and record that SHA in the session. The endpoint
   is a resolved ref, never the literal token `HEAD`, because `HEAD` moves as soon as step 4 runs.
4. Create `release/v0.10.1` at `1d4a9724` and check it out. Use that branch name exactly.
5. Cherry-pick the range from `1bb94063` to the captured endpoint in one range invocation. A range
   cherry-pick applies oldest-to-newest automatically, which is the order D-06 asks for. No `-m`
   flag is needed or permitted: RESEARCH.md verified every commit in the range has exactly one
   parent. If the range's content has grown since planning, that is expected — replay whatever is
   actually there.
6. If any commit in the range touches a path outside `.planning/`, stop and record it: D-06's whole
   premise is that these commits are planning-only and the tree is otherwise identical to
   `origin/main`.
7. If `git status --porcelain` still shows uncommitted `.planning/` changes after the replay (the
   Phase 37.1 ROADMAP/STATE insertion, if it was never committed), commit them here on
   `release/v0.10.1` with a conventional-commit message.
8. Assert the tree-identity claim directly: the diff between `origin/main` and the new branch,
   excluding `.planning/`, must be empty.
9. Assert nothing was stranded: the commit range from the captured endpoint to
   `refs/heads/feature/phase-33` must be empty. If it is not — the realistic cause is an
   orchestrator progress-tracking commit landing on the old branch — record each stray SHA as a
   D-00d finding and re-apply its content on `release/v0.10.1`; never push, reset or rewrite the
   old branch.
10. Do not push anything. Plan 37.1-08 owns the first push, after the re-seal.
  </action>
  <acceptance_criteria>
    - `git branch --show-current` reports `release/v0.10.1`
    - `git merge-base --is-ancestor 1d4a9724 HEAD` succeeds — the branch descends from the recorded base commit
    - `git diff --name-only origin/main..HEAD -- . ':!.planning'` produces no output
    - `git log --format=%P 1d4a9724..HEAD` contains no line with two SHAs — no merge commit was introduced by the replay
    - `git rev-list --count <captured-endpoint>..refs/heads/feature/phase-33` is 0, or every SHA in that range is recorded as a D-00d finding and its content re-applied on `release/v0.10.1`
    - `git status --porcelain -- .planning` is empty — nothing planning-related is left uncommitted
    - `git rev-parse refs/heads/feature/phase-33` still resolves to the captured endpoint (or to the endpoint plus only the reconciled strays) — the old branch was not rewritten
    - No commit in this task was made with a verification-skipping flag; the session records a successful clippy warm-up before the replay
  </acceptance_criteria>
  <verify>
    <automated>test "$(git branch --show-current)" = "release/v0.10.1" && git merge-base --is-ancestor 1d4a9724 HEAD && test -z "$(git diff --name-only origin/main..HEAD -- . ':!.planning')" && test -z "$(git log --format=%P 1d4a9724..HEAD | awk 'NF>1')" && test -z "$(git status --porcelain -- .planning)"</automated>
  </verify>
  <done>`release/v0.10.1` exists at the recorded base commit, carries every planning commit that was local to `feature/phase-33` in order, is byte-identical to `origin/main` outside `.planning/`, and the old branch was left untouched with nothing stranded on it.</done>
</task>

<task type="auto">
  <name>Task 2: Open `37.1-CI-EVIDENCE.md` in house form and append the dated SHA map and forward pointer to Phase 37's evidence</name>
  <files>
    .planning/phases/37.1-v0-10-1-patch-release/37.1-CI-EVIDENCE.md,
    .planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md
  </files>
  <read_first>
    - `.planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md` — the three-table house form to copy (its `## Provenance`, `## Local sweep`, `## CI-run table`, `## Registry verification (D-08)` and `## Findings carried forward (D-00d)` headings), and the citations near its D-16 diagnosis sections that the SHA map must keep resolvable
    - `.planning/phases/37.1-v0-10-1-patch-release/37.1-PATTERNS.md` § "`37.1-CI-EVIDENCE.md` (NEW)" — the explicit instruction to copy the three-table form and append the SHA map
    - `.planning/phases/37.1-v0-10-1-patch-release/37.1-RESEARCH.md` § Summary and § "Branch Mechanics" — the two corrections this task records
    - `.planning/phases/37.1-v0-10-1-patch-release/37.1-CONTEXT.md` — D-11 (new evidence file, dated additions only to the old one) and D-00d (amend at source)
  </read_first>
  <action>
Create this phase's evidence file and make Phase 37's file survive the rewrite.

1. Create `37.1-CI-EVIDENCE.md` with the same top-level structure Phase 37's file uses: a
   `## Provenance` block (branch name, base commit, the replayed range endpoints, the toolchain
   versions this phase measures on), an empty `## Local sweep` table, an empty `## CI-run table`,
   an empty `## Registry verification (D-00h)` table and a `## Findings carried forward (D-00d)`
   section. Later plans fill the rows; this task establishes the headings so nothing is invented
   ad hoc later.
2. Record two corrections in the Findings section as dated entries, each naming the CONTEXT.md
   statement it corrects and the evidence that overturns it:
   - CONTEXT.md D-06's commit figure was already stale when RESEARCH.md re-measured it, and had
     moved again by planning time. The correction is not a new number — it is that the range is
     resolved live from git and no count is ever asserted. Record the endpoints and the count
     actually replayed by Task 1 as an observation, explicitly labelled as a point-in-time reading
     rather than a contract.
   - The existing CI `publish-dry-run` job in `.github/workflows/ci.yml` is gated to push events on
     the default branch and has never run on a pull request. This is worse than the "green and
     blind" framing Phase 37 used: pre-merge, it did not gate at all. Record it here so plan
     37.1-03's CI-wiring choice has a written basis, and state that satisfying the new gate by
     loosening that job's trigger is ruled out.
3. Record the spec-less probe skip as a dated finding: no `-SPEC.md` exists for this phase, so no
   edge-coverage or prohibition predicates were probe-derived, and the `must_haves.truths` across
   this phase's plans are derived from the six ROADMAP success criteria and the locked decisions
   instead. Record it as a visible skip, not as an absence.
4. Record that both `plan:pre` capability detectors ran and neither fired, with the reason for
   each — so a later reader sees a measured negative rather than an omission.
5. Append to `37-CI-EVIDENCE.md`, at the end, a dated section carrying the old-to-new SHA map: one
   row per replayed commit with its old SHA, its new SHA and its subject, plus one sentence saying
   why the SHAs changed and that the old branch was never pushed. Add one dated forward-pointer
   line naming `.planning/phases/37.1-v0-10-1-patch-release/37.1-CI-EVIDENCE.md` as where this
   phase's evidence lives. Edit no existing line of that file — append only.
6. Commit both files on `release/v0.10.1` with a conventional-commit message. Allow a generous
   timeout: the unconditional pre-commit lint hooks run in full.
  </action>
  <acceptance_criteria>
    - `.planning/phases/37.1-v0-10-1-patch-release/37.1-CI-EVIDENCE.md` exists and contains the headings `## Provenance`, `## Local sweep`, `## CI-run table`, `## Registry verification (D-00h)` and `## Findings carried forward (D-00d)`
    - The Provenance block names `release/v0.10.1`, the base commit `1d4a9724`, and both replay-range endpoint SHAs
    - The Findings section contains a dated entry for the live-range correction and a dated entry for the pull-request-blind dry-run job, each naming the CONTEXT.md statement it corrects
    - The Findings section contains a dated entry recording the spec-less probe skip and a dated entry recording that both capability detectors ran and returned negative
    - `37-CI-EVIDENCE.md` gained a dated SHA-map section with one row per replayed commit and a dated forward-pointer line naming this phase's evidence file
    - `git diff 1d4a9724 -- .planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md | grep -c '^-[^-]'` is 0 — the additions to Phase 37's evidence file removed no existing line
    - Every old SHA cited in `37-CI-EVIDENCE.md` prose that belongs to the replayed range appears in the SHA map's old-SHA column
  </acceptance_criteria>
  <verify>
    <automated>E=.planning/phases/37.1-v0-10-1-patch-release/37.1-CI-EVIDENCE.md && test -f "$E" && grep -q '^## Provenance' "$E" && grep -q '^## Local sweep' "$E" && grep -q '^## CI-run table' "$E" && grep -q '^## Findings carried forward' "$E" && grep -q 'release/v0.10.1' "$E" && grep -q '1d4a9724' "$E" && grep -qi 'sha map' .planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md && grep -q '37.1-CI-EVIDENCE' .planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md && test "$(git diff 1d4a9724 -- .planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md | grep -c '^-[^-]')" = "0"</automated>
  </verify>
  <done>This phase has its own evidence file in house form with the two research corrections, the probe skip and the detector results recorded as dated findings; Phase 37's evidence file carries an append-only SHA map and forward pointer, with no existing line touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `feature/phase-33` (local, unpushed) → `release/v0.10.1` (the branch that becomes the release) | Every commit crossing this boundary is replayed with a new SHA; a stranded or silently dropped commit means the release PR's diff is not what the record says it is |
| `origin/main` at `1d4a9724` → the release branch's base | The base commit anchors every SHA in this phase's evidence; a moved base invalidates the whole evidence chain |
| Phase 37's sealed evidence file → this phase's dated additions | Phase 37's file is sealed at the SHAs it verified; an edit rather than an append would rewrite a verified record |

## STRIDE Threat Register (ASVS L1; block on `high`)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37.1-01 | Tampering | The replayed planning record on `release/v0.10.1` | high | mitigate | Task 1 resolves the range live from a captured ref, asserts no merge commit was introduced, asserts the tree is identical to `origin/main` outside `.planning/`, and asserts the source branch range is empty afterward; any stray is recorded as a D-00d finding and re-applied rather than dropped. |
| T-37.1-02 | Repudiation | Citations in `37-CI-EVIDENCE.md` broken by the SHA rewrite | high | mitigate | Task 2 appends a dated old-to-new SHA map covering every replayed commit, and an acceptance criterion asserts every cited old SHA from the range appears in the map. |
| T-37.1-03 | Tampering | Phase 37's sealed evidence file | medium | mitigate | Additions are append-only; an acceptance criterion counts removed lines in the diff against the base commit and requires zero. |
| T-37.1-04 | Elevation of Privilege | Bypassing the repository's own commit-time lint gate | medium | mitigate | A prohibition forbids verification-skipping commit flags; the mitigation for hook cost is a single clippy warm-up, recorded in the session, not a bypass. |
| T-37.1-SC | Tampering | Package-manager installs | low | accept | This plan installs no package from any registry (no `cargo add`, no `npm`/`pip`/`cargo install`). RESEARCH.md § Package Legitimacy Audit records the phase-wide negative: no manifest dependency table gains a new crate name anywhere in this phase's scope, so no legitimacy checkpoint is required. |
</threat_model>

<artifacts_this_phase_produces>
The phase-wide list. Later plans reference this section rather than restating it.

**New files**
- `scripts/check-publish-order.sh` — the SC3 publish-order gate script (plan 37.1-02)
- `tests/scripts/check-publish-order_test.sh` — its committed regression harness (plan 37.1-02)
- `tests/scripts/fixtures/check-publish-order/` — the captured `cargo metadata` fixtures, including the defective `1d4a9724` shape (plan 37.1-02)
- `.planning/phases/37.1-v0-10-1-patch-release/37.1-CI-EVIDENCE.md` — this phase's evidence file (this plan)
- `.planning/phases/37.1-v0-10-1-patch-release/.continue-here.md` — the D-00i resume file, rewritten at each hard stop (plans 37.1-08, -10, -11, -12)
- `.planning/phases/37.1-v0-10-1-patch-release/37.1-READY-TO-CLOSE.md` — the close hand-off (plan 37.1-16)
- `.planning/phases/37-v0-10-0-crate-release/37-08-SUMMARY.md` — Phase 37's missing plan summary, ending at the D-16 hard stop (plan 37.1-15)

**New identifiers**
- Makefile target `check-publish-order`, added to the `check-gates` prerequisite list (plan 37.1-03)
- CI step `Check publish order` inside the existing `cargo-deny` / "License & Dependency Policy" job of `.github/workflows/ci.yml` (plan 37.1-03)
- Section `## 13. Re-seal for the v0.10.1 release (Phase 37.1, SHIP-06)` in `.project/v0.10.0/09-program-acceptance-audit.md`, ending in a fresh unticked sign-off box (plan 37.1-07)
- Requirement `SHIP-06` in `.planning/REQUIREMENTS.md` (plan 37.1-15)
- Git branch `release/v0.10.1` (this plan)
- Git branch `chore/37.1-close` (plan 37.1-12)

**Deliberately absent: no new `pub` Rust items.** This phase modifies no file under `src/` or
`crates/*/src/`. The public API surface does not move, so the pre-push `api-surface` gate must stay
green **without a baseline refresh** — `.project/current-exports.txt` is not regenerated by any
plan in this phase, and a diff against it is an acceptance criterion of the re-seal (plan 37.1-07),
not a file to update.
</artifacts_this_phase_produces>

<verification>
- The checked-out branch is `release/v0.10.1`, descended from `1d4a9724`, and identical to `origin/main` outside `.planning/`
- The replay introduced no merge commit and stranded nothing on `feature/phase-33`
- `37.1-CI-EVIDENCE.md` exists in the three-table house form with the Provenance block filled
- Both research corrections, the probe skip and the detector results are recorded as dated findings
- `37-CI-EVIDENCE.md` gained an append-only dated SHA map and forward pointer, with no line removed
</verification>

<success_criteria>
- `git branch --show-current` is `release/v0.10.1` and `git merge-base --is-ancestor 1d4a9724 HEAD` succeeds
- `git diff --name-only origin/main..HEAD -- . ':!.planning'` is empty
- No commit count is asserted anywhere in this plan's output; the range is recorded as endpoints
- `37.1-CI-EVIDENCE.md` carries all five house headings and the two dated corrections
- The diff of `37-CI-EVIDENCE.md` against `1d4a9724` removes zero lines
</success_criteria>

<output>
Create `.planning/phases/37.1-v0-10-1-patch-release/37.1-01-SUMMARY.md` on `release/v0.10.1` when
done. Record the base commit, both replay-range endpoint SHAs, the number of commits actually
replayed (as a point-in-time observation, explicitly not a contract), and whether any stray commit
had to be reconciled. Plans 37.1-08 and 37.1-12 both cite the base commit.
</output>
