---
phase: 37.1
slug: v0-10-1-patch-release
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: validated
nyquist_compliant: true
wave_0_complete: true
created: 2026-09-19
validated: 2026-09-23
---

# Phase 37.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Seeded by `/gsd-plan-phase 37.1` from `37.1-RESEARCH.md` § Validation Architecture.
>
> This phase's "tests" are deterministic evidence checks over release-mechanics artifacts plus the
> existing workspace gates: the only source-tree changes are two shell scripts with their harnesses, one
> manifest's dev-dependency table, the version bump, and `Makefile`/`ci.yml` wiring. Every map row below
> is the task's own `<verify><automated>` seed, re-run verbatim on `chore/37.1-post-close` at `6a2d25ea`
> on 2026-09-23; the 19 rows marked *rescoped* were time-bound to a branch, an open PR, an unpushed tag,
> a pre-fix red or a resume file that has since been consumed, and were pinned to the durable artifact
> instead (see the audit section at the end). Every `grep` alternation uses `-E 'a|b'` and literal
> table pipes are matched as `[|]`, so the commands survive the `\|` table escaping intact.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Bash shell-script regression harness (`tests/scripts/*_test.sh`, run by `make test-shell-guards`) for the two scripts this phase touched; evidence checks (`grep`/`jq`/`git`/`gh`/`curl`) over `37.1-CI-EVIDENCE.md`, git objects, GitHub and the crates.io sparse index; the existing workspace gates for the bump |
| **Config file** | none dedicated — `Makefile` targets `check-publish-order`, `check-gates`, `test-shell-guards`, `release-check`, `publish-dry-run` are the config surface |
| **Quick run command** | `./tests/scripts/check-publish-order_test.sh && ./tests/scripts/create-or-reuse-release_test.sh` (~3 s, 9 + 21 assertions) |
| **Full suite command** | `make test-shell-guards && make check-gates && make check-release-consistency RELEASE_TAG=v0.10.1 && make publish-dry-run` plus the eight D-09 gate commands in `37.1-07-01` |
| **Estimated runtime** | ~3 s quick · ~75 s `test-shell-guards` · ~8.5 min for `37.1-04-02` (package + deny + semver-checks + lint + surface) · ~7 min for `37.1-07-01` (MSRV check, two compat targets, 11 semver-checks, `publish-dry-run`) · ~4 min for `37.1-07-02` (`clean-code` + `security`) — measured 2026-09-23 warm |

---

## Sampling Rate

- **After every task commit:** Run the relevant single-file shell test (`./tests/scripts/create-or-reuse-release_test.sh` or the new `./tests/scripts/check-publish-order_test.sh`), plus `cargo package -p paladin-battalion --no-verify` for SC1 tasks
- **After every plan wave:** Run `make test-shell-guards && make check-gates && make release-check`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Phase gate (re-seal, D-09/D-10):** the full eight-row `## 13.` table, all locally runnable except the CI-only 82% coverage floor (D-00e — no Docker in this devcontainer), before the maintainer ticks §13
- **Max feedback latency:** 30 seconds for the quick command

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 37.1-01-01 | 01 | 1 | SHIP-06 | T-37.1-01 | Planning record replayed onto a branch rooted at the sealed merge commit, linear, with nothing outside `.planning/` changed | integration | `git merge-base --is-ancestor 1d4a9724 f3fc061d && test "$(git rev-list --count --merges 1d4a9724..f3fc061d)" = "0" && test "$(git diff --name-only 1d4a9724 8b5df4d8 -- . ':!.planning' \| wc -l \| tr -d ' ')" = "0" && SM=$(grep -cE '^[\|] [0-9a-f]{8} [\|] [0-9a-f]{8} [\|]' .planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md; true); test "$SM" = "24" && test -z "$(git ls-remote --heads origin release/v0.10.1)"` | ✅ | ✅ green (rescoped + re-run 2026-09-23) |
| 37.1-01-02 | 01 | 1 | SHIP-06 | T-37.1-02, T-37.1-03 | Evidence file opened in house form; the sealed Phase 37 file gains a SHA map additions-only (diff pinned to the plan's own commit) | integration | `E=.planning/phases/37.1-v0-10-1-patch-release/37.1-CI-EVIDENCE.md && test -f "$E" && grep -q '^## Provenance' "$E" && grep -q '^## Local sweep' "$E" && grep -q '^## CI-run table' "$E" && grep -q '^## Findings carried forward' "$E" && grep -q 'release/v0.10.1' "$E" && grep -q '1d4a9724' "$E" && grep -qi 'sha map' .planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md && grep -q '37.1-CI-EVIDENCE' .planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md && test "$(git diff 7a8c7cc6~1 7a8c7cc6 -- .planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md \| grep -c '^-[^-]')" = "0"` | ✅ | ✅ green (rescoped + re-run 2026-09-23) |
| 37.1-02-01 | 02 | 2 | SHIP-06 | T-37.1-07 | Fixtures are real `cargo metadata` captures (2 forward edges on `1d4a9724`, 0 after the fix) and the harness runs green | unit | `F=tests/scripts/fixtures/check-publish-order && jq -e . "$F/metadata-1d4a9724.json" >/dev/null && jq -e . "$F/metadata-fixed.json" >/dev/null && test "$(jq '[.packages[] \| select(.name=="paladin-battalion") \| .dependencies[] \| select(.kind=="dev" and .path != null and .req != "*")] \| length' "$F/metadata-1d4a9724.json")" = "2" && test "$(jq '[.packages[] \| select(.name=="paladin-battalion") \| .dependencies[] \| select(.kind=="dev" and .path != null and .req != "*")] \| length' "$F/metadata-fixed.json")" = "0" && test "$(jq '[.packages[] \| select(.name=="paladin-battalion") \| .dependencies[] \| select(.kind=="dev" and .path != null)] \| length' "$F/metadata-fixed.json")" = "2" && test -x tests/scripts/check-publish-order_test.sh && ./tests/scripts/check-publish-order_test.sh >/dev/null` | ✅ | ✅ green (rescoped + re-run 2026-09-23) |
| 37.1-02-02 | 02 | 2 | SHIP-06 | T-37.1-05, T-37.1-06 | Gate has no hard-coded crate list, names every offending edge, fails hard on missing input, is deterministic | unit | `G=./scripts/check-publish-order.sh && test -x "$G" && grep -q 'PUBLISH_CRATES_LIB_ONLY' "$G" && test "$(grep -cE '^[[:space:]]*paladin-[a-z-]+[[:space:]]*$' "$G")" = "0" && ./tests/scripts/check-publish-order_test.sh && F=tests/scripts/fixtures/check-publish-order && OUT=$("$G" --metadata-json "$F/metadata-1d4a9724.json" 2>&1 \|\| true) && grep -q 'paladin-battalion' <<<"$OUT" && grep -q 'paladin-llm' <<<"$OUT" && grep -q 'paladin-storage' <<<"$OUT" && ! "$G" --metadata-json "$F/metadata-1d4a9724.json" >/dev/null 2>&1 && "$G" --metadata-json "$F/metadata-fixed.json" >/dev/null && ! "$G" --metadata-json /nonexistent/path.json >/dev/null 2>&1 && A=$("$G" --metadata-json "$F/metadata-fixed.json" 2>&1) && B=$("$G" --metadata-json "$F/metadata-fixed.json" 2>&1) && test "$A" = "$B"` | ✅ | ✅ green (re-run 2026-09-23) |
| 37.1-02-03 | 02 | 2 | SHIP-06 | T-37.1-07 | The red-on-`1d4a9724` proof is recorded with the worktree capture named | integration | `E=.planning/phases/37.1-v0-10-1-patch-release/37.1-CI-EVIDENCE.md && grep -q 'check-publish-order' "$E" && grep -q '1d4a9724' "$E" && grep -q 'paladin-battalion' "$E" && grep -q 'paladin-llm' "$E" && grep -q 'paladin-storage' "$E" && grep -qi 'worktree' "$E" && grep -qi 'diagnosis' "$E" && test -z "$(git status --porcelain)"` | ✅ | ✅ green (re-run 2026-09-23) |
| 37.1-03-01 | 03 | 3 | SHIP-06 | T-37.1-12 | `check-publish-order` is a `check-gates` prerequisite beside the seven prior guards; all eight green on the fixed tree | gate | `test "$(grep -c '^check-publish-order:' Makefile)" = "1" && grep '^check-gates:' Makefile \| grep -q 'check-publish-order' && for t in check-changelogs check-crate-names check-advisory-register check-workflow-suppressions check-workflow-triggers check-codeql-dismissals check-migration-allowlist; do grep '^check-gates:' Makefile \| grep -q "$t" \|\| exit 1; done && make check-publish-order >/dev/null 2>&1 && make check-gates >/dev/null 2>&1 && for t in check-changelogs check-crate-names check-advisory-register check-workflow-suppressions check-workflow-triggers check-codeql-dismissals check-migration-allowlist; do make "$t" >/dev/null 2>&1 \|\| exit 1; done` | ✅ | ✅ green (rescoped + re-run 2026-09-23) |
| 37.1-03-02 | 03 | 3 | SHIP-06 | T-37.1-09, T-37.1-11 | CI step lives in a job that runs on `pull_request`; no trigger-surface line changed; dry-run blindness recorded | integration | `test "$(grep -c 'check-publish-order.sh' .github/workflows/ci.yml)" = "1" && python3 -c "import yaml,sys; d=yaml.safe_load(open('.github/workflows/ci.yml')); j=[k for k,v in d['jobs'].items() if v.get('name')=='License & Dependency Policy']; sys.exit(0 if j and any('check-publish-order.sh' in str(s.get('run','')) for s in d['jobs'][j[0]]['steps']) else 1)" && ./scripts/check-workflow-triggers.sh >/dev/null && test -z "$(git diff .github/workflows/ci.yml \| grep '^-[^-]' \| grep -E 'github.event_name\|github.ref\|^-on:')" && grep -qiE 'publish dry run\|publish-dry-run' .planning/phases/37.1-v0-10-1-patch-release/37.1-CI-EVIDENCE.md` | ✅ | ✅ green (re-run 2026-09-23) |
| 37.1-03-03 | 03 | 3 | SHIP-06 | T-37.1-10 | Both shell files tracked before lint; suite discovers the new test through the existing glob | gate | `git ls-files --error-unmatch scripts/check-publish-order.sh tests/scripts/check-publish-order_test.sh >/dev/null && make lint-shell >/dev/null && make test-shell-guards >/dev/null && grep -q 'lint-shell' .planning/phases/37.1-v0-10-1-patch-release/37.1-CI-EVIDENCE.md && grep -q 'test-shell-guards' .planning/phases/37.1-v0-10-1-patch-release/37.1-CI-EVIDENCE.md && grep -q 'check-gates' .planning/phases/37.1-v0-10-1-patch-release/37.1-CI-EVIDENCE.md && test -z "$(git status --porcelain)"` | ✅ | ✅ green (re-run 2026-09-23) |
| 37.1-04-01 | 04 | 4 | SHIP-06 | T-37.1-14 | Dev-dependencies path-only with rationale comments intact; publish loop untouched | integration | `M=crates/paladin-battalion/Cargo.toml && test "$(grep -c 'paladin-storage = { path = "../paladin-storage" }' "$M")" = "1" && grep 'paladin-llm' "$M" \| grep -q 'path = "../paladin-llm"' && test "$(cargo metadata --format-version 1 --no-deps \| jq '[.packages[] \| select(.name=="paladin-battalion") \| .dependencies[] \| select(.kind=="dev" and .path != null and .req != "*")] \| length')" = "0" && test "$(cargo metadata --format-version 1 --no-deps \| jq '[.packages[] \| select(.name=="paladin-battalion") \| .dependencies[] \| select(.kind=="dev" and .path != null)] \| length')" = "2" && test "$(grep -c 'ADR-0031' "$M")" -ge 1 && test "$(grep -c 'No cycle' "$M")" -ge 2 && test "$(grep -c 'check-publish-order' "$M")" -ge 1 && test -z "$(git diff --stat scripts/publish-crates.sh)"` | ✅ | ✅ green (re-run 2026-09-23) |
| 37.1-04-02 | 04 | 4 | SHIP-06 | T-37.1-13, T-37.1-15 | Gate, packager, deny, semver-checks, lint and API-surface all green with the baseline unmoved | gate | `./scripts/check-publish-order.sh && make check-gates >/dev/null && cargo package -p paladin-battalion --no-verify >/dev/null 2>&1 && cargo deny check >/dev/null 2>&1 && cargo semver-checks check-release --package paladin-battalion --default-features --baseline-version 0.9.0 >/dev/null 2>&1 && cargo fmt --all --check >/dev/null 2>&1 && make lint lint-shell check doc-check check-api-examples >/dev/null 2>&1 && make api-surface >/dev/null && test -z "$(git status --porcelain .project/current-exports.txt)" && grep -q 'cargo package -p paladin-battalion' .planning/phases/37.1-v0-10-1-patch-release/37.1-CI-EVIDENCE.md && grep -q 'api-surface' .planning/phases/37.1-v0-10-1-patch-release/37.1-CI-EVIDENCE.md` | ✅ | ✅ green (re-run 2026-09-23) |
| 37.1-04-03 | 04 | 4 | SHIP-06 | T-37.1-16 | Remaining versioned dev-dependency edges recorded as a finding, not changed | integration | `E=.planning/phases/37.1-v0-10-1-patch-release/37.1-CI-EVIDENCE.md && grep -q 'paladin-herald' "$E" && grep -q 'paladin-eval' "$E" && grep -qi 'deferred' "$E" && test -z "$(git status --porcelain)"` | ✅ | ✅ green (re-run 2026-09-23) |
| 37.1-05-01 | 05 | 5 | SHIP-06 | T-37.1-18 | The ≥200 KB regression case exists and passes; its pre-fix exit-141 red is on record in the evidence file | unit | `T=tests/scripts/create-or-reuse-release_test.sh && grep -qi 'pipe buffer' "$T" && OUT=$(./"$T" 2>&1) && grep -q '21 assertion(s) passed' <<<"$OUT" && F=$(grep -c 'FAIL' <<<"$OUT"; true); test "$F" = "0" && grep -q '141' .planning/phases/37.1-v0-10-1-patch-release/37.1-CI-EVIDENCE.md` | ✅ | ✅ green (rescoped + re-run 2026-09-23) |
| 37.1-05-02 | 05 | 5 | SHIP-06 | T-37.1-17, T-37.1-19 | Status line extracted by parameter expansion, no pipe; shell suites green | gate | `S=scripts/create-or-reuse-release.sh && test "$(awk '/_cor_gh_call\(\)/,/^}/' "$S" \| grep -c 'head')" = "0" && test "$(awk '/_cor_gh_call\(\)/,/^}/' "$S" \| grep -c 'status_line')" -ge 3 && ./tests/scripts/create-or-reuse-release_test.sh && make lint-shell >/dev/null && make test-shell-guards >/dev/null` | ✅ | ✅ green (re-run 2026-09-23) |
| 37.1-05-03 | 05 | 5 | SHIP-06 | T-37.1-20 | Three same-shape sites swept, recorded, and left untouched | integration | `E=.planning/phases/37.1-v0-10-1-patch-release/37.1-CI-EVIDENCE.md && grep -q 'check-deprecations.sh' "$E" && grep -q 'check-doc-examples.sh' "$E" && grep -q 'check-public-api-examples.sh' "$E" && grep -qi 'overturn' "$E" && test -z "$(git diff --stat scripts/check-deprecations.sh scripts/check-doc-examples.sh scripts/check-public-api-examples.sh)" && test -z "$(git status --porcelain)"` | ✅ | ✅ green (re-run 2026-09-23) |
| 37.1-06-01 | 06 | 6 | SHIP-06 | T-37.1-21, T-37.1-23 | Every manifest at `0.10.1` (comments excluded), pin operator kept, lockfile regenerated in the bump commit | integration | `test "$(grep -hv '^[[:space:]]*#' Cargo.toml crates/*/Cargo.toml \| grep -c '0\.10\.0'; true)" = "0" && test "$(grep -c '"0\.10\.1"' Cargo.toml)" -ge 14 && test "$(grep -c '"=0\.10\.1"' crates/paladin-ports/Cargo.toml)" = "1" && test "$(cargo metadata --format-version 1 --no-deps \| jq -r '[.packages[] \| select(.version != "0.10.1")] \| length')" = "0" && test -n "$(git diff --stat cb73f26e 88d174de -- Cargo.lock)" && test -z "$(git diff --stat cb73f26e 88d174de -- MIGRATION.md .cargo/semver-checks-allowlist.toml CHANGELOG.md)"` | ✅ | ✅ green (rescoped + re-run 2026-09-23) |
| 37.1-06-02 | 06 | 6 | SHIP-06 | T-37.1-22, T-37.1-24 | Twelve `[0.10.1]` sections above the previous release; migration title kept; allowlist untouched | integration | `test "$(grep -c '^## \[0\.10\.1\]' CHANGELOG.md)" = "1" && for f in crates/*/CHANGELOG.md; do test "$(grep -c '^## \[0\.10\.1\]' "$f")" = "1" \|\| exit 1; done && N1=$(grep -n '^## \[0\.10\.1\]' CHANGELOG.md \| cut -d: -f1) && N0=$(grep -n '^## \[0\.10\.0\]' CHANGELOG.md \| cut -d: -f1) && test "$N1" -lt "$N0" && test "$(head -1 MIGRATION.md)" = "$(git show 1d4a9724:MIGRATION.md \| head -1)" && grep -q '0\.10\.1' MIGRATION.md && test -z "$(git diff --stat 1d4a9724 -- .cargo/semver-checks-allowlist.toml)" && make check-changelogs >/dev/null` | ✅ | ✅ green (re-run 2026-09-23) |
| 37.1-06-03 | 06 | 6 | SHIP-06 | T-37.1-25 | Release-consistency, composite, publish-order and surface gates green; three no-work findings recorded | gate | `make check-release-consistency RELEASE_TAG=v0.10.1 >/dev/null && make check-gates >/dev/null && ./scripts/check-publish-order.sh >/dev/null && make api-surface >/dev/null && test -z "$(git status --porcelain .project/current-exports.txt)" && E=.planning/phases/37.1-v0-10-1-patch-release/37.1-CI-EVIDENCE.md && grep -q 'semver-checks-allowlist' "$E" && grep -q 'MIGRATION.md' "$E" && grep -q 'doc-examples' "$E" && grep -q 'development-setup' "$E" && test -z "$(git status --porcelain)"` | ✅ | ✅ green (re-run 2026-09-23) |
| 37.1-07-01 | 07 | 7 | SHIP-06 | T-37.1-28 | All eight D-09 gates actually run on the candidate head, with selected-test counts asserted | gate | `test -z "$(git status --porcelain)" && test "$(grep -c 'TBD' MIGRATION.md)" = "0" && make check-migration-allowlist >/dev/null && make check-gates >/dev/null && make check-publish-order >/dev/null && test "$(grep -c '## \[0\.10\.2\]' CHANGELOG.md)" = "0" && test "$(grep -c '## \[0\.11\.0\]' CHANGELOG.md)" = "0" && env RUSTUP_TOOLCHAIN=1.88 cargo check --workspace --all-features --all-targets >/dev/null 2>&1 && RB=$(cargo test --features web-server --test v0_9_config_boot 2>&1) && printf '%s
' "$RB" \| grep -qE 'test result: ok\. [1-9][0-9]* passed' && ! printf '%s
' "$RB" \| grep -qE 'test result: ok\. 0 passed' && RG=$(cargo test -p paladin-web --test openapi_golden_v0_9 2>&1) && printf '%s
' "$RG" \| grep -qE 'test result: ok\. [1-9][0-9]* passed' && ! printf '%s
' "$RG" \| grep -qE 'test result: ok\. 0 passed' && SV=$(cargo metadata --format-version 1 --no-deps \| jq -r '.packages[] \| select(.publish == null) \| .name' \| grep -v '^paladin-eval$') && test "$(printf '%s
' "$SV" \| wc -l \| tr -d ' ')" = "11" && ( for P in $SV; do cargo semver-checks check-release --package "$P" --default-features --baseline-version 0.9.0 >/dev/null 2>&1 \|\| exit 1; done ) && make publish-dry-run >/dev/null && E=.planning/phases/37.1-v0-10-1-patch-release/37.1-CI-EVIDENCE.md && grep -q 'v0_9_config_boot' "$E" && grep -q 'openapi_golden_v0_9' "$E" && grep -q 'semver-checks' "$E" && grep -q 'publish-dry-run' "$E" && grep -q 'check-publish-order' "$E"` | ✅ | ✅ green (re-run 2026-09-23) |
| 37.1-07-02 | 07 | 7 | SHIP-06 | T-37.1-29 | House sweep green; the 82% floor recorded as CI-attributed, never approximated locally | gate | `make api-surface >/dev/null && test -z "$(git status --porcelain .project/current-exports.txt)" && make clean-code >/dev/null && make security >/dev/null && E=.planning/phases/37.1-v0-10-1-patch-release/37.1-CI-EVIDENCE.md && grep -q 'api-surface' "$E" && grep -q '82' "$E" && grep -qi 'coverage' "$E" && test -z "$(git status --porcelain)"` | ✅ | ✅ green (re-run 2026-09-23) |
| 37.1-07-03 | 07 | 7 | SHIP-06 | T-37.1-26, T-37.1-27 | Section 13 appended additions-only with a fresh unticked box; no box state changed by the agent (pinned to the append commit) | integration | `A=.project/v0.10.0/09-program-acceptance-audit.md && test "$(grep -c '^## 13\.' "$A")" = "1" && test "$(grep -c '^## 11\.' "$A")" = "1" && test "$(grep -c '^## 12\.' "$A")" = "1" && test "$(git diff 1d4a9724 d73c242b -- "$A" \| grep -c '^-[^-]')" = "0" && test "$(git show d73c242b:$A \| grep -c '^- \[x\]')" = "$(git show 1d4a9724:$A \| grep -c '^- \[x\]')" && grep -q 'v0.10.1' "$A" && git show d73c242b:$A \| awk '/^## 13\./,0' \| grep -q '^- \[ \]' && P=.planning/phases/29-program-gates-release/29-ACCEPTANCE-AUDIT.md && grep -q '13' "$P" && grep -q '37.1-CI-EVIDENCE' "$P"` | ✅ | ✅ green (rescoped + re-run 2026-09-23) |
| 37.1-08-01 | 08 | 8 | SHIP-06 | T-37.1-31, T-37.1-35 | Exactly one release PR, merged by the maintainer as a merge commit; the release branch consumed | integration | `M=$(gh pr view 56 --json state,mergeCommit,headRefName --jq '"\(.state) \(.mergeCommit.oid) \(.headRefName)"'); test "$M" = "MERGED f7dae2676580786e9541fefee8f787623b46c70f release/v0.10.1" && E=.planning/phases/37.1-v0-10-1-patch-release/37.1-CI-EVIDENCE.md && grep -qE 'pull/56\|PR #56' "$E" && test -z "$(git ls-remote --heads origin release/v0.10.1)"` | ✅ | ✅ green (rescoped + re-run 2026-09-23) |
| 37.1-08-02 | 08 | 8 | SHIP-06 | T-37.1-33, T-37.1-34 | Resume file named the resume condition and the coverage job; no watcher parked | integration | `C=.planning/phases/37.1-v0-10-1-patch-release/.continue-here.md; git show 3dda866f:"$C" \| grep -qi 'resume condition' && git show 3dda866f:"$C" \| grep -qi 'coverage' && git merge-base --is-ancestor 3dda866f f3fc061d` | ✅ | ✅ green (rescoped + re-run 2026-09-23) |
| 37.1-09-01 | 09 | 9 | SHIP-06 | T-37.1-37, T-37.1-38, T-37.1-39 | Every PR-head run recorded per job; publish-order execution proven in the CI log; coverage figure measured | integration | `E=.planning/phases/37.1-v0-10-1-patch-release/37.1-CI-EVIDENCE.md; grep -q '^## CI-run table' "$E" && grep -qi 'coverage' "$E" && grep -q 'check-publish-order' "$E" && grep -qiE 'required (check\|context)' "$E" && test -z "$(git status --porcelain)" && test "$(git rev-parse v0.10.1^{commit})" = "f7dae2676580786e9541fefee8f787623b46c70f"` | ✅ | ✅ green (rescoped + re-run 2026-09-23) |
| 37.1-09-02 | 09 | 9 | SHIP-06 | T-37.1-36 | Section 13 rows filled with the sign-off box untouched at the pre-tick head | integration | `A=.project/v0.10.0/09-program-acceptance-audit.md; B=$(git show 1d4a9724:$A \| grep -c '^- \[x\]'); P=$(git show 529e7078~1:$A \| grep -c '^- \[x\]'); test "$P" = "$B" && test "$(git show 529e7078~1:$A \| awk '/^## 13\./,0' \| grep -c '^- \[ \]')" -ge 1 && git merge-base --is-ancestor 529e7078 f7dae2676580786e9541fefee8f787623b46c70f` | ✅ | ✅ green (rescoped + re-run 2026-09-23) |
| 37.1-09-03 | 09 | 9 | SHIP-06 | T-37.1-36 | Only the maintainer ticks the sign-off box | manual | — (manual-only; see table below) | n/a | ✅ green (manual, observed 2026-09-23) |
| 37.1-10-01 | 10 | 10 | SHIP-06 | T-37.1-41, T-37.1-43 | Tick read from the remote at the PR head; exactly one more ticked box than the base | integration | `R=529e7078; B=$(git show 1d4a9724:.project/v0.10.0/09-program-acceptance-audit.md \| grep -c '^- \[x\]'); N=$(git show "$R":.project/v0.10.0/09-program-acceptance-audit.md \| grep -c '^- \[x\]'); test "$N" = "$((B+1))" && git show "$R":.project/v0.10.0/09-program-acceptance-audit.md \| awk '/^## 13\./,0' \| grep -q '^- \[x\]' && grep -q "$(git rev-parse $R)" .planning/phases/37.1-v0-10-1-patch-release/37.1-CI-EVIDENCE.md` | ✅ | ✅ green (rescoped + re-run 2026-09-23) |
| 37.1-10-02 | 10 | 10 | SHIP-06 | T-37.1-44 | Second resume file names the wait without a watcher | integration | `C=.planning/phases/37.1-v0-10-1-patch-release/.continue-here.md; git show b13a8553:"$C" \| grep -qi 'resume condition' && git show b13a8553:"$C" \| grep -qiE 'no watcher\|not wait\|no polling' && git merge-base --is-ancestor 529e7078 b13a8553` | ✅ | ✅ green (rescoped + re-run 2026-09-23) |
| 37.1-11-01 | 11 | 11 | SHIP-06 | T-37.1-47 | Tick-commit checks recorded before any tag existed; tag now on the merge commit | integration | `E=.planning/phases/37.1-v0-10-1-patch-release/37.1-CI-EVIDENCE.md; grep -q f3fc061d "$E" && grep -qiE 'required (check\|context)' "$E" && test "$(git rev-parse v0.10.1^{commit})" = "f7dae2676580786e9541fefee8f787623b46c70f"` | ✅ | ✅ green (rescoped + re-run 2026-09-23) |
| 37.1-11-02 | 11 | 11 | SHIP-06 | T-37.1-45 | One-way-door decision is the maintainer's | manual | — (manual-only; see table below) | n/a | ✅ green (manual, observed 2026-09-23) |
| 37.1-11-03 | 11 | 11 | SHIP-06 | T-37.1-45, T-37.1-46, T-37.1-48 | Merge commit and annotated tag are the maintainer's acts; no rehearsal tag | manual | — (manual-only; see table below) | n/a | ✅ green (manual, observed 2026-09-23) |
| 37.1-11-04 | 11 | 11 | SHIP-06 | T-37.1-50 | Post-tag resume file names read-only follow-up and the yank rule; no poller | integration | `C=.planning/phases/37.1-v0-10-1-patch-release/.continue-here.md; V=$(git show f6b74c70:"$C"); grep -qi 'resume condition' <<<"$V" && grep -qi 'merge commit' <<<"$V" && grep -qi 'read-only' <<<"$V" && grep -qi 'yank' <<<"$V" && grep -qiE 'no watcher\|no polling\|not wait' <<<"$V"` | ✅ | ✅ green (rescoped + re-run 2026-09-23) |
| 37.1-12-01 | 12 | 12 | SHIP-06 | T-37.1-51 | Tag commit is a two-parent merge contained in `main`, recorded in the resume file | integration | `git fetch origin --tags >/dev/null 2>&1; TC=$(git rev-parse v0.10.1^{commit}); git merge-base --is-ancestor "$TC" origin/main && test "$(git rev-list --parents -n 1 "$TC" \| wc -w)" = "3" && test "$(git cat-file -t v0.10.1)" = "tag" && git show f6b74c70:.planning/phases/37.1-v0-10-1-patch-release/.continue-here.md \| grep -q "$TC"` | ✅ | ✅ green (rescoped + re-run 2026-09-23) |
| 37.1-12-02 | 12 | 12 | SHIP-06 | T-37.1-54 | Close-out branch cut from the tagged `main`; no stray commit after the tick | integration | `git merge-base --is-ancestor v0.10.1^{commit} HEAD && git merge-base --is-ancestor dce653aa e282a264 && test -f .planning/phases/37.1-v0-10-1-patch-release/37.1-10-SUMMARY.md && test -f .planning/phases/37.1-v0-10-1-patch-release/37.1-11-SUMMARY.md && test -z "$(git status --porcelain -- .planning/phases/37.1-v0-10-1-patch-release)" && { S=0; if git rev-parse --verify --quiet refs/heads/release/v0.10.1 >/dev/null; then S=$(git rev-list --count f3fc061d..refs/heads/release/v0.10.1); fi; OK=0; if test "$S" = "0"; then OK=1; fi; if grep -qi 'stray' .planning/phases/37.1-v0-10-1-patch-release/37.1-CI-EVIDENCE.md; then OK=1; fi; test "$OK" = "1"; }` | ✅ | ✅ green (rescoped + re-run 2026-09-23) |
| 37.1-12-03 | 12 | 12 | SHIP-06 | T-37.1-52, T-37.1-53 | Publish health read from the publish job's own conclusion and table; every crate probed on the index | integration | `git merge-base --is-ancestor v0.10.1^{commit} HEAD && E=.planning/phases/37.1-v0-10-1-patch-release/37.1-CI-EVIDENCE.md && grep -q 'publish-crates' "$E" && grep -q 'release.yml' "$E" && grep -q 'verify-tag-source' "$E" && grep -q "$(git rev-parse v0.10.1^{commit})" "$E" && test "$(grep -c 'paladin-' "$E")" -ge 12 && RID=$(gh run list --workflow release.yml --branch v0.10.1 --limit 1 --json databaseId --jq '.[0].databaseId' 2>/dev/null) && test -n "$RID" && grep -q "$RID" "$E" && PJC=$(gh run view "$RID" --json jobs --jq '[.jobs[] \| select(.name \| test("crates\.io"))][0].conclusion' 2>/dev/null) && test -n "$PJC" && test "$PJC" != "null" && grep -i 'publish' "$E" \| grep -qi "$PJC" && CR=$(cargo metadata --format-version 1 --no-deps \| jq -r '.packages[] \| select(.publish == null) \| .name') && test "$(printf '%s
' "$CR" \| wc -l \| tr -d ' ')" -ge 12 && ( for X in $CR; do grep -q "$X" "$E" \|\| exit 1; curl -sf -H 'User-Agent: paladin-release-verify (github.com/DF3NDR/paladin-dev-env)' "https://index.crates.io/pa/la/$X" >/dev/null \|\| exit 1; done ) && test -z "$(git status --porcelain)"` | ✅ | ✅ green (rescoped + re-run 2026-09-23) |
| 37.1-13-01 | 13 | 13 | SHIP-06 | T-37.1-56, T-37.1-57 | Registry rows ≥ the live derived crate count, which is non-zero | integration | `E=.planning/phases/37.1-v0-10-1-patch-release/37.1-CI-EVIDENCE.md && N=$(cargo metadata --format-version 1 --no-deps \| jq -r '[.packages[] \| select(.publish == null)] \| length') && test "$N" -gt 0 && grep -q '^## Registry verification' "$E" && R=$(awk '/^## Registry verification/,/^## [^R]/' "$E" \| grep -c '[\|] paladin-') && test "$R" -ge "$N" && grep -q '0\.10\.1' "$E"` | ✅ | ✅ green (re-run 2026-09-23) |
| 37.1-13-02 | 13 | 13 | SHIP-06 | T-37.1-58, T-37.1-59 | `trustpub_data` observed unauthenticated; carried findings consolidated | integration | `E=.planning/phases/37.1-v0-10-1-patch-release/37.1-CI-EVIDENCE.md && grep -q 'trustpub_data' "$E" && grep -q 'paladin-eval' "$E" && grep -qiE 'unauthenticated\|no credential' "$E" && grep -q 'check-deprecations.sh' "$E" && grep -q 'semver-checks-allowlist' "$E" && test -z "$(git status --porcelain)"` | ✅ | ✅ green (re-run 2026-09-23) |
| 37.1-14-01 | 14 | 14 | SHIP-06 | T-37.1-60, T-37.1-61, T-37.1-62 | Yank performed by the maintainer only, after every crate resolved at `0.10.1` | manual | — (manual-only; see table below) | n/a | ✅ green (manual, observed 2026-09-23) |
| 37.1-14-02 | 14 | 14 | SHIP-06 | T-37.1-63 | One register row per yanked crate, additions-only | integration | `D=docs/src/appendix/release-recovery.md; E=.planning/phases/37.1-v0-10-1-patch-release/37.1-CI-EVIDENCE.md; grep -q 'Yank register' "$D" && test "$(git diff v0.10.1 -- "$D" \| grep -c '^-[^-]')" = "0" && { R=$(git diff v0.10.1 -- "$D" \| grep -c '^+\| 0\.10\.0'); L=$(grep -ciE 'leave them\|left in place' "$E"); test "$R" = "3" \|\| test "$L" -ge 1; } && grep -q 'paladin-ai-core' "$E" && grep -q 'paladin-herald' "$E" && test -z "$(git status --porcelain)"` | ✅ | ✅ green (re-run 2026-09-23) |
| 37.1-14-03 | 14 | 14 | SHIP-06 | T-37.1-64, T-37.1-65 | Release body prepended, never replaced; tag never moved | manual | — (manual-only; see table below) | n/a | ✅ green (manual, observed 2026-09-23) |
| 37.1-14-04 | 14 | 14 | SHIP-06 | T-37.1-64, T-37.1-65 | Pre-release flip and banner recorded; `v0.10.0` tag still resolves | integration | `E=.planning/phases/37.1-v0-10-1-patch-release/37.1-CI-EVIDENCE.md; git fetch origin --tags >/dev/null 2>&1; grep -qiE 'prerelease\|pre-release' "$E" && grep -q 'v0.10.0' "$E" && git rev-parse v0.10.0^{commit} >/dev/null && test -z "$(git status --porcelain)"` | ✅ | ✅ green (re-run 2026-09-23) |
| 37.1-15-01 | 15 | 15 | SHIP-06 | T-37.1-69 | Milestone record names both tags additions-only | integration | `M=.planning/MILESTONES.md && grep -q 'v0.10.1' "$M" && grep -q 'v0.10.0' "$M" && grep -qi 'registry-verified' "$M" && grep -q '37.1-CI-EVIDENCE' "$M" && grep -qiE 'trusted publishing\|trustpub' "$M" && grep -qi 'yank' "$M" && test "$(git diff v0.10.1 -- "$M" \| grep -c '^-[^-]')" = "0"` | ✅ | ✅ green (re-run 2026-09-23) |
| 37.1-15-02 | 15 | 15 | SHIP-06 | T-37.1-66, T-37.1-68 | `SHIP-06` minted; `SHIP-05` never ticked; state front matter intact | integration | `R=.planning/REQUIREMENTS.md && test "$(grep -c 'SHIP-06' "$R")" -ge 2 && test "$(grep -c '\[x\] \*\*SHIP-05\*\*' "$R")" = "0" && grep -qi 'superseded' "$R" && grep -q 'SHIP-06' .planning/ROADMAP.md && for k in gsd_state_version milestone current_phase status; do grep -q "^$k:" .planning/STATE.md \|\| exit 1; done` | ✅ | ✅ green (re-run 2026-09-23) |
| 37.1-15-03 | 15 | 15 | SHIP-06 | T-37.1-67 | Superseded plans marked, not deleted; Phase 37 summary written | integration | `P=.planning/phases/37-v0-10-0-crate-release && test -f "$P/37-08-SUMMARY.md" && for n in 09 10 11; do grep -qi 'superseded' "$P/37-$n-PLAN.md" \|\| exit 1; done && for n in 09 10 11; do test "$(git diff v0.10.1 -- "$P/37-$n-PLAN.md" \| grep -c '^-[^-]')" = "0" \|\| exit 1; done && grep -qi 'superseded' .planning/ROADMAP.md && test "$(grep -c 're-planned against what this phase ships' .planning/ROADMAP.md)" = "0" && for k in gsd_state_version milestone current_phase status; do grep -q "^$k:" .planning/STATE.md \|\| exit 1; done && test -z "$(git status --porcelain)"` | ✅ | ✅ green (re-run 2026-09-23) |
| 37.1-16-01 | 16 | 16 | SHIP-06 | T-37.1-74 | Ready-to-close names preconditions and evidence for all six criteria | integration | `F=.planning/phases/37.1-v0-10-1-patch-release/37.1-READY-TO-CLOSE.md && test -f "$F" && grep -qi 'precondition' "$F" && grep -q '37.1-CI-EVIDENCE' "$F" && grep -qiE 'api-surface\|public-api\|public API' "$F" && test "$(grep -cE '^#{2,3} ' "$F")" -ge 6` | ✅ | ✅ green (re-run 2026-09-23) |
| 37.1-16-02 | 16 | 16 | SHIP-06 | T-37.1-71, T-37.1-72 | Docs-only close-out PR merged by the maintainer; no released file changed | integration | `BAD=$(git diff --name-only v0.10.1..e282a264 \| grep -Ev '^(\.planning/\|\.project/\|docs/)' \| grep -v '^$' \| wc -l \| tr -d ' '); test "$BAD" = "0" && M=$(gh pr view 57 --json state,mergeCommit --jq '"\(.state) \(.mergeCommit.oid)"'); test "$M" = "MERGED e282a264368af37d02029d80b4a93bfdc9c640e4" && grep -qE 'pull/57\|PR #57' .planning/phases/37.1-v0-10-1-patch-release/37.1-CI-EVIDENCE.md` | ✅ | ✅ green (rescoped + re-run 2026-09-23) |
| 37.1-16-03 | 16 | 16 | SHIP-06 | T-37.1-71, T-37.1-73 | Close-out merge and milestone close are the maintainer's acts | manual | — (manual-only; see table below) | n/a | ✅ green (manual, observed 2026-09-23) |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky. 40 automated rows + 6 maintainer-only checkpoints = 46 tasks across 16 plans; no row pending.*

### Success-criteria → test map (from RESEARCH.md)

| SC | Behavior | Test Type | Automated Command | File Exists? |
|----|----------|-----------|-------------------|--------------|
| SC1 | `paladin-battalion` packages/publishes cleanly at its `CRATES` position | integration (shell) | `cargo package -p paladin-battalion --no-verify` + the new SC3 gate as permanent regression | ✅ cargo / ✅ gate (`scripts/check-publish-order.sh`) |
| SC2 | `create-or-reuse-release.sh` survives a response larger than the pipe buffer | unit (shell, stubbed `gh`) | `./tests/scripts/create-or-reuse-release_test.sh` | ✅ harness / ✅ assertion (case 9, 200 000 chars) |
| SC3 | Publish-order gate fails on the `1d4a9724` shape, passes on the fixed shape, wired into make + CI on `pull_request` | unit (shell) + CI wiring | `./tests/scripts/check-publish-order_test.sh` | ✅ `tests/scripts/check-publish-order_test.sh` (9 assertions) |
| SC4 (bump) | All 13 manifests + changelogs + `MIGRATION.md` read `0.10.1` consistently | integration (shell) | `make check-gates` (bundles `check-changelogs`) + the RESEARCH.md grep sweep | ✅ |
| SC4 (re-seal) | Eight-row `## 13.` table green at `0.10.1` | integration | the eight commands in RESEARCH.md § Phase 29 Gate Re-Seal | ✅ (coverage row CI-only) |
| SC5 | All 12 crates resolve at `0.10.1`; `paladin-eval` shows non-null `trustpub_data` | manual/scripted read-only | `curl` against `index.crates.io` and `crates.io/api/v1/crates/<name>` | ✅ observed (37.1-12-03, 37.1-13-01/02) |
| SC6 | Yank-register row per orphan; MILESTONES.md / GitHub Release / Phase 37 plans tell the true story | manual (maintainer-gated, D-00b) + docs | `37.1-14-02`, `37.1-14-04`, `37.1-15-01..03` | ✅ observed |

---

## Wave 0 Requirements

- [x] `scripts/check-publish-order.sh` (plan 37.1-02 Task 2) — the SC3 gate script itself; not a test file, but the thing the test exercises
- [x] `tests/scripts/check-publish-order_test.sh` — new file covering SC3 (red on the `1d4a9724` shape, green on the fixed shape)
- [x] One new large-body assertion inside the existing `tests/scripts/create-or-reuse-release_test.sh` — covers SC2; harness and conventions already exist, only the assertion is new (payload ≥200 KB, not "just over 64 KiB" — RESEARCH.md measured ~70 KB as non-triggering, 100 KB+ as reliable)

*Everything else needed for validation — `cargo package`, `cargo deny check`, `cargo semver-checks`, `make check-gates`, `make publish-dry-run`, the sparse-index and `trustpub_data` curl probes — already exists and was exercised directly during research.*

---

## Manual-Only Verifications

All six were observed 2026-09-23 (UAT tests 3, 4, 5, 6, 8, 10; `37.1-VERIFICATION.md` § Prohibitions confirmed the actor of every act as the maintainer via `git log`, `gh pr view` and tag metadata). Two were resolved by deviation rather than at a presented checkpoint — the merge and the tag (37.1-11-02/03) were performed directly in session — and are recorded verbatim in `37.1-CI-EVIDENCE.md`.

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Maintainer ticks the `## 13.` sign-off box | SHIP-06 / D-00a, D-10 | Only the maintainer ticks a sign-off box — never an agent, under any mode | Agent appends the eight-row table with evidence, pushes, confirms PR CI green, then stops at a blocking checkpoint |
| Release PR merge to `main` (merge commit) | SHIP-06 / D-00b, D-00c | Irreversible, maintainer-only act | Agent hands over the exact command set and stops |
| Annotated `v0.10.1` tag push on the merge commit | SHIP-06 / D-00b, D-00c | Irreversible — triggers the crates.io publish | Agent verifies `ci.yml` green on the merge commit, hands over the exact `git tag -a` / `git push` commands, stops |
| `cargo yank --version 0.10.0 <crate>` ×3 | SHIP-06 / D-01 | Maintainer-only act; must happen only AFTER the index shows every crate at `0.10.1`, `yanked = false` | Agent verifies the index read-only first, hands over the three exact commands at a blocking checkpoint, then re-verifies `yanked = true` read-only |
| `gh release edit v0.10.0 --prerelease` + banner notice | SHIP-06 / D-03 | Outward-facing edit of a published object | Agent drafts the notice and exact command; runs it only if the maintainer says so at that checkpoint |
| `release.yml` run green for `v0.10.1` | SHIP-06 / SC5 | Runs only on a real tag; cannot be dry-run-dispatched before the ref exists (D-00k) | Read-only observation after the maintainer pushes the tag; a red run means diagnose read-only and hard-stop (D-00j) |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or are listed in the Manual-Only table above (40 + 6)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify (longest manual run is two: 37.1-11-02/03)
- [x] Wave 0 covers all MISSING references (all three Wave 0 files exist and run green)
- [x] No watch-mode flags
- [x] Feedback latency < 30 s for the quick command; the three heavy gate rows are minutes by design and were run serialized
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** validated 2026-09-23 by `/gsd-validate-phase 37.1`

---

## Validation Audit 2026-09-23

| Metric | Count |
|--------|-------|
| Gaps found | 19 (all seed drift, none a missing test) |
| Resolved | 19 (command corrections in place, no auditor spawn) |
| Escalated | 0 |

All 40 automated seeds were re-run verbatim, serialized, on `chore/37.1-post-close` at `6a2d25ea`:
21 green as written (including the three heavy gate rows — `37.1-04-02` 509 s, `37.1-07-01` 410 s,
`37.1-07-02` 235 s — and the shell suites, 54/54 assertions), 19 red. Every red was a seed bound to a
moment the phase has since moved past, not a regression; each was rescoped to the durable artifact and
re-run green:

- **Consumed branches / open PR / unpushed tag** — `37.1-01-01`, `37.1-08-01`, `37.1-08-02`, `37.1-09-01`,
  `37.1-09-02`, `37.1-10-01`, `37.1-10-02`, `37.1-11-01`, `37.1-12-02`, `37.1-12-03`, `37.1-16-02` asserted
  `git branch --show-current` = `release/v0.10.1` / `chore/37.1-close`, `origin/release/v0.10.1` = HEAD, an
  *open* PR, or `git ls-remote --tags v0.10.1` empty. Both branches were consumed by their merges (remote
  refs deleted) and the tag exists. Pinned to the tick commit `f3fc061d`, the first tick `529e7078` (and its
  parent for the pre-tick box count), PR #56/#57 `MERGED` with merge commits `f7dae267`/`e282a264`, and
  `v0.10.1^{commit}` = `f7dae267`.
- **Consumed resume file** — `37.1-08-02`, `37.1-10-02`, `37.1-11-04`, `37.1-12-01` read
  `.continue-here.md`, removed at phase close. Pinned to the commits that carried each version
  (`3dda866f`, `b13a8553`, `f6b74c70`) via `git show <sha>:<path>`.
- **TDD pre-fix reds** — `37.1-02-01`, `37.1-03-01`, `37.1-05-01` asserted the harness/gate *fails*
  before the fix. Rescoped to the post-fix green plus the evidence record of the red (`exit 141`).
- **Diff anchors** — `37.1-01-02` (`git diff 1d4a9724`, 2 pre-existing Phase 37 placeholder-fill
  removals, recorded finding) pinned to plan 01's own commit `7a8c7cc6`; `37.1-06-01` (`HEAD~1`) pinned to
  the bump commit range `cb73f26e..88d174de`; `37.1-07-03` (unticked §13 box, ticked count = base) pinned to
  the §13 append commit `d73c242b` — both boxes have since been ticked by the maintainer (`529e7078`,
  `f3fc061d`), which `37.1-10-01` asserts.
- **Comment lines** — `37.1-06-01`'s "no `0.10.0` in any manifest" now excludes `#` comment lines: the
  four remaining mentions are plan 37.1-04's rationale comments in `paladin-battalion/Cargo.toml`.
- **Table-safety** — nine seeds using BRE `\|` alternation were converted to `grep -E 'a|b'`, and two
  matching a literal table pipe now use `[|]`, so the commands are unambiguous inside this file's own
  table cells.

Manual-only rows observed 2026-09-23: the §13/§14 sign-off ticks are the maintainer's commits
(`529e7078`, `f3fc061d`), the merge (`f7dae267`) and annotated tag are the maintainer's acts recorded
verbatim as N/A-by-deviation checkpoints, the three yanks were the maintainer's own (`yanked: true`
re-read), the `v0.10.0` release edit ran only on the maintainer's "Run it", and PR #57 was merged by the
maintainer ("Merged"). The milestone close remains the maintainer's act.
