---
phase: 22-battlefield-state-superstep-engine
plan: 04
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - MIGRATION.md
  - README.md
  - Cargo.toml
  - crates/paladin-core/Cargo.toml
  - crates/paladin-ports/Cargo.toml
  - crates/paladin-battalion/Cargo.toml
  - crates/paladin-herald/Cargo.toml
  - crates/paladin-llm/Cargo.toml
  - crates/paladin-memory/Cargo.toml
  - crates/paladin-storage/Cargo.toml
  - crates/paladin-notifications/Cargo.toml
  - crates/paladin-content/Cargo.toml
  - crates/paladin-web/Cargo.toml
  - .github/workflows/ci.yml
  - .cargo/semver-checks-allowlist.toml
autonomous: true
requirements: [ENG-08]

must_haves:
  truths:
    - "MIGRATION.md exists at the repository root titled for the v0.9.0 to v0.10.0 upgrade and contains every heading 9.1 through 9.8 from the program overview section 9"
    - "MIGRATION.md 9.1 carries the pre-populated M-B-01, M-B-02 and M-B-03 rows verbatim in scope, and 9.2 carries every register row listed in the program overview 9.2 table"
    - "Sections owned by later epics may read TBD, but every item this phase touches is filled in: the uuid v7 feature and the postgres feature in 9.3, the waypoints migration in 9.4, and the new engine and retention config plus their APP_ environment variables in 9.5"
    - "The cargo semver-checks allowlist contains exactly the per-item entries present in MIGRATION.md 9.2 — set equality in both directions, no blanket ignore, and no allowlist entry without a matching register row"
    - "A semver CI job and an MSRV CI job both exist in .github/workflows/ci.yml, both are reached by the pull_request trigger, and neither has a needs edge on the other"
    - "The semver job pins its comparison to the published 0.9.0 baseline explicitly rather than relying on the tool's latest-published default, so its verdict does not drift when a crate is published out of band"
    - "The semver job addresses crates by their published package names, not their crates/ directory names, so it cannot silently match zero packages and report success"
    - "Re-running the semver and MSRV jobs on an unchanged commit produces the identical verdict, and a cancelled or re-run job leaves no repository state behind — neither job writes a baseline or artifact into the tree"
    - "Every publishable crate manifest declares rust-version 1.85 and the README MSRV badge agrees with it"
  prohibitions:
    - statement: "MUST NOT let automatic per-superstep persistence of full Battlefield snapshots — including raw LLM prompts and model outputs — reach users as an invisible default; MIGRATION.md must state plainly what a Waypoint stores, that it is written on every superstep, where it is written, and which retention and durability knobs bound it, in the document an upgrading v0.9 user is directed to read"
      status: resolved
  artifacts:
    - path: "MIGRATION.md"
      provides: "the program's living upgrade document with the full section 9 skeleton"
      contains: "## 9.2"
    - path: ".cargo/semver-checks-allowlist.toml"
      provides: "per-item deliberate-breaking allowlist mirroring MIGRATION.md 9.2"
    - path: ".github/workflows/ci.yml"
      provides: "the semver and msrv jobs"
      contains: "cargo-semver-checks"
  key_links:
    - from: ".cargo/semver-checks-allowlist.toml"
      to: "MIGRATION.md"
      via: "every allowlist entry must have a matching 9.2 register row (X-10.5), asserted by a CI step"
      pattern: "semver-checks-allowlist"
    - from: "README.md"
      to: "MIGRATION.md"
      via: "upgrade link so a v0.9 user reaches the document"
      pattern: "MIGRATION.md"
---

<objective>
Ship the program scaffolding ENG-08 mandates for the first epic: the root `MIGRATION.md` with the
full program-overview section 9 skeleton and its pre-populated entries, plus the two new CI jobs
(`cargo semver-checks` against the published v0.9.0 crates, and an MSRV job pinned to Rust 1.85)
running on every PR.

Purpose: X-10 requires every pre-existing public type touched by this program to be registered
*before* it is modified, and X-11 requires the MSRV job to land in the first epic that adds a
dependency. Both gates are worthless if they arrive after the code they govern.

Output: `MIGRATION.md`, a per-item semver allowlist, `rust-version` on every publishable crate, and
two new CI jobs.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.project/v0.10.0/00-program-overview.md
@.project/v0.10.0/01-battlefield-state-and-execution-engine.md
@.planning/phases/22-battlefield-state-superstep-engine/22-CONTEXT.md
@.planning/phases/22-battlefield-state-superstep-engine/22-RESEARCH.md
@.planning/phases/22-battlefield-state-superstep-engine/22-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MIGRATION.md with the full section 9 skeleton and pre-populated content</name>
  <read_first>
    - .project/v0.10.0/00-program-overview.md (section 9 in full — 9.1 through 9.8 is the verbatim source of this file's structure and of the M-B-01..03 and 9.2 register rows; section 7 BUG-01; X-10 and X-11 for the rules the register enforces)
    - .project/v0.10.0/01-battlefield-state-and-execution-engine.md (acceptance criterion 8 — the per-epic versioning gate this file satisfies)
    - .planning/phases/22-battlefield-state-superstep-engine/22-CONTEXT.md (D-08: created this phase, TBD acceptable only in later-epic sections; D-02 and D-03 name the 9.3/9.4 entries this phase must fill)
    - README.md (the badge block at the top and the existing document links, for where the MIGRATION.md pointer goes)
    - .planning/phases/22-battlefield-state-superstep-engine/22-01-SUMMARY.md (which public items the tracer actually created, so the 9.2 register records reality rather than the plan's guess)
  </read_first>
  <files>MIGRATION.md, README.md</files>
  <action>
    Create `MIGRATION.md` at the repository root titled "Upgrading from v0.9.0 to v0.10.0" with all
    eight headings `## 9.1` through `## 9.8` using the program overview's own section names
    (Behavioral changes; Rust API changes; Toolchain and dependencies; Persistence and schema
    migrations; Configuration and environment; HTTP API; Deprecations; Upgrade checklist).

    Section 9.1: reproduce the M-B-01, M-B-02 and M-B-03 rows from the overview's table with all
    four columns (ID, Change, Who is affected, Required user action). These describe work owned by
    later phases (BUG-01 by Phase 23, graceful shutdown by Phase 24, tool error mode by Phase 26);
    carry the row content across now and mark the "worked before/after example" sub-items TBD with
    the owning requirement id, since D-08 permits TBD only in later-epic-owned content.

    Add one further 9.1 entry owned by THIS phase, describing what automatic checkpointing changes
    for an upgrading user: that the new `WarEngine` writes one Waypoint per superstep containing a
    full `Battlefield` snapshot — which includes whatever a workflow places in shared state,
    including raw model prompts and outputs — that this is on by default for graphs run through the
    new engine, which backend receives it, and which knobs bound it (`WaypointDurability`,
    `WaypointRetentionConfig`). State plainly that legacy Formation/Phalanx/Campaign execution is
    unchanged and writes no Waypoints, so a v0.9 workflow gains no new persistence until it opts in.

    Section 9.2: reproduce the overview's eleven-row register table with its stated columns (Crate,
    Type/Trait, Change, Mitigation, Deliberate-breaking?, Requirement ID). Fill the Mitigation and
    Deliberate columns for any row this phase actually touched and mark the rest TBD with the owning
    requirement id. Add a row for every pre-existing public type Plan 22-01 modified — read
    22-01-SUMMARY.md rather than assuming; if the tracer only added new modules and touched no
    pre-existing public type, state that explicitly in a note under the table so the verification
    pass sees a deliberate zero rather than an omission.

    Section 9.3: record the `uuid` `v7` feature addition on the existing pinned 1.8.0 workspace
    dependency (D-03) and the `postgres` cargo feature on `paladin-storage` plus its facade
    passthrough (D-01), each with a one-line description of what it pulls in. Record the final MSRV
    as 1.85 and confirm the default feature set of `paladin-ai` gained no heavyweight dependency
    (X-11.4) — back that with the `cargo tree -e features` command used to check it.

    Section 9.4: record the `waypoints` table migration file name, that it targets SQLite and
    Postgres, whether it runs automatically at startup or must be applied manually, its
    reversibility, and storage-growth guidance pointing at `WaypointRetentionConfig`. Include the
    explicit statement the overview requires: existing Citadel JSON/SQLite state files remain
    readable unchanged. Also record the stored graph-fingerprint string format decided at Plan
    22-01's checkpoint, since it is part of the persisted payload.

    Section 9.5: record the new engine and Waypoint retention config structs and every new `APP_`
    environment variable with its default, plus the overview's required claim that all new
    subsystems are disabled by default so a v0.9 configuration file boots v0.10 with identical
    behavior.

    Sections 9.6, 9.7 and 9.8: create the headings with TBD plus the owning phase, since the HTTP
    surface (Phase 27), deprecations and the operator checklist are later-epic content. Phase 29 /
    SHIP-01 clears every remaining TBD.

    In `README.md`, add a link to `MIGRATION.md` in the same area as the other document links. The
    mdBook "Upgrading" page the program definition-of-done also requires is Phase 29's scope — do
    not add it here, and say so in a one-line note at the top of MIGRATION.md so the gap is recorded
    rather than forgotten.
  </action>
  <acceptance_criteria>
    - `MIGRATION.md` exists at the repository root and its first heading names the v0.9.0 to v0.10.0 upgrade
    - `grep -c '^## 9\.' MIGRATION.md` is `8`
    - `MIGRATION.md` contains `M-B-01`, `M-B-02` and `M-B-03`
    - `MIGRATION.md` section 9.2 table has at least 11 data rows (the overview's pre-populated register), each naming a crate and a type or trait
    - `MIGRATION.md` contains `uuid`, `v7`, `postgres`, `waypoints` and `WaypointRetentionConfig`
    - `MIGRATION.md` 9.1 contains an entry naming `Waypoint` and stating what a Waypoint stores and that it is written every superstep
    - Every `TBD` in MIGRATION.md is followed on the same line by an owning requirement or phase identifier: `grep -n 'TBD' MIGRATION.md | grep -vE '(ENG|CF|HITL|FT|RT|PLAT|OBS|SHIP)-' | wc -l` is `0`
    - `README.md` contains a link whose target is `MIGRATION.md`
  </acceptance_criteria>
  <verify>
    <automated>test -f MIGRATION.md && [ "$(grep -c '^## 9\.' MIGRATION.md)" = "8" ] && grep -q 'M-B-01' MIGRATION.md && grep -q 'M-B-03' MIGRATION.md && grep -q 'WaypointRetentionConfig' MIGRATION.md && [ "$(grep -n 'TBD' MIGRATION.md | grep -vE '(ENG|CF|HITL|FT|RT|PLAT|OBS|SHIP)-' | wc -l)" = "0" ] && grep -q 'MIGRATION.md' README.md</automated>
  </verify>
  <done>MIGRATION.md exists with all eight section-9 headings, the pre-populated behavioral and register rows, this phase's dependency/migration/config entries filled in, every remaining TBD attributed to an owner, and a README link.</done>
</task>

<task type="auto">
  <name>Task 2: Declare MSRV 1.85 across the workspace and add the MSRV CI job</name>
  <read_first>
    - .project/v0.10.0/00-program-overview.md (X-11.1 the CI job requirement, X-11.2 the stop-and-flag path when a dependency cannot be satisfied at MSRV)
    - .planning/phases/22-battlefield-state-superstep-engine/22-CONTEXT.md (D-07: dedicated job pinned to the 1.85 toolchain, full workspace, --all-features; the pinned dev toolchain stays 1.97.1)
    - .planning/phases/22-battlefield-state-superstep-engine/22-RESEARCH.md (Summary point 2: no [workspace.package] table exists today and each crate declares its package fields independently; Open Question 1 recommends per-crate rust-version over introducing a workspace table; Environment Availability row for the 1.85 toolchain)
    - rust-toolchain.toml (this file overrides whatever toolchain an action installed — read the CI note in its header)
    - .github/workflows/ci.yml lines 235-270 (the existing `test` job, which is the in-tree precedent for opting out of rust-toolchain.toml via RUSTUP_TOOLCHAIN)
    - README.md (the MSRV badge already reads 1.85 but no crate declares rust-version — this task makes the badge true)
    - Cargo.toml (the root [package] block)
  </read_first>
  <files>Cargo.toml, crates/paladin-core/Cargo.toml, crates/paladin-ports/Cargo.toml, crates/paladin-battalion/Cargo.toml, crates/paladin-herald/Cargo.toml, crates/paladin-llm/Cargo.toml, crates/paladin-memory/Cargo.toml, crates/paladin-storage/Cargo.toml, crates/paladin-notifications/Cargo.toml, crates/paladin-content/Cargo.toml, crates/paladin-web/Cargo.toml, README.md, .github/workflows/ci.yml</files>
  <action>
    Add `rust-version = "1.85"` to the `[package]` block of each publishable crate manifest
    individually — the root `Cargo.toml` (`paladin-ai`) plus the ten crates listed in
    `<files>` — rather than introducing a `[workspace.package]` inheritance table. RESEARCH.md
    Open Question 1 recommends the per-crate form because a workspace table would sweep unrelated
    metadata (license, authors, repository) into inheritance, which X-03's stop-and-flag rule would
    read as an out-of-scope refactor. Place the field immediately after `edition` in each block.

    Add a new job to `.github/workflows/ci.yml` named `msrv` with display name "MSRV (Rust 1.85)".
    It runs on `ubuntu-latest`, checks out, installs the 1.85 toolchain via `dtolnay/rust-toolchain`
    pinned to that version, sets `RUSTUP_TOOLCHAIN: "1.85"` as a job-level env var so
    `rust-toolchain.toml`'s 1.97.1 pin does not override it (the existing `test` job at line 246
    documents exactly this opt-out mechanism), restores the `Swatinem/rust-cache` with a cache key
    distinct from the other jobs, and runs `cargo check --workspace --all-features --all-targets`.
    Give it no `needs:` edge — it must run in parallel with the existing jobs and share no writable
    state with them.

    If the workspace does not build at 1.85, this is the X-11.2 stop-and-flag path, not a silent
    bump: do NOT raise `rust-version` past 1.85 and do NOT relax `--all-features`. Instead identify
    the specific dependency and version that fails, try an older compatible version of it first,
    record the finding and the alternatives considered in MIGRATION.md section 9.3, and halt with a
    checkpoint carrying that evidence for a human decision.

    Confirm the README MSRV badge reads 1.85 and matches the declared `rust-version`; correct it if
    it drifts.
  </action>
  <acceptance_criteria>
    - `grep -l 'rust-version = "1.85"' Cargo.toml crates/*/Cargo.toml | wc -l` is `11`
    - `.github/workflows/ci.yml` contains a job key `msrv:` and the string `1.85`
    - The msrv job has no `needs:` key
    - The msrv job sets `RUSTUP_TOOLCHAIN` so `rust-toolchain.toml` cannot override the pinned toolchain
    - The msrv job's command includes `--workspace` and `--all-features`
    - `README.md` MSRV badge text matches the declared rust-version
    - `actionlint .github/workflows/ci.yml` exits 0 (the repo already runs actionlint as a CI job)
    - `cargo metadata --format-version 1 --no-deps` reports a non-null `rust_version` for all 11 publishable packages
  </acceptance_criteria>
  <verify>
    <automated>[ "$(grep -l 'rust-version = \"1.85\"' Cargo.toml crates/*/Cargo.toml | wc -l)" = "11" ] && grep -q '^  msrv:' .github/workflows/ci.yml && cargo metadata --format-version 1 --no-deps | grep -c '"rust_version":"1.85"' && cargo check --workspace --all-features</automated>
  </verify>
  <done>All eleven publishable crates declare MSRV 1.85, the README badge is backed by manifest data, and a dedicated parallel MSRV CI job builds the full workspace with all features on the 1.85 toolchain.</done>
</task>

<task type="auto">
  <name>Task 3: Add the semver CI job with a per-item allowlist mirroring MIGRATION.md 9.2</name>
  <read_first>
    - .project/v0.10.0/00-program-overview.md (X-10.5: the job must pass, the only permitted exceptions are the register's deliberate-breaking entries, each suppressed by an explicit per-item allowlist checked into the repo, not a blanket ignore)
    - .planning/phases/22-battlefield-state-superstep-engine/22-CONTEXT.md (D-06: against the published v0.9.0 crates.io versions of every publishable crate, allowlist entries must mirror MIGRATION.md 9.2 exactly)
    - .planning/phases/22-battlefield-state-superstep-engine/22-RESEARCH.md (Pitfall 1: the tool's default baseline is "latest published", not 0.9.0 — pass the baseline explicitly; Pitfall 2: package names differ from directory names, and the verified published names are listed there; Standard Stack note on pinning the action to a tagged release rather than a moving ref)
    - .github/workflows/ci.yml lines 193-235 (the `api-surface` job — the closest in-tree analog for a tool-install-then-check job)
    - MIGRATION.md (the 9.2 register the allowlist must mirror, written in Task 1)
  </read_first>
  <files>.github/workflows/ci.yml, .cargo/semver-checks-allowlist.toml</files>
  <action>
    Create `.cargo/semver-checks-allowlist.toml` holding one explicit entry per deliberate-breaking
    change, keyed by published package name and the specific lint being suppressed, each carrying a
    comment naming the MIGRATION.md 9.2 row it mirrors and the requirement id. If this phase
    introduces no deliberate-breaking change, the file exists with a header comment and zero
    entries — an empty per-item list is the correct state and is what makes a later blanket
    suppression visible in review.

    Add a job to `.github/workflows/ci.yml` named `semver` with display name "Semver Checks
    (vs v0.9.0)". It runs on `ubuntu-latest`, checks out with full history, installs a stable
    toolchain, restores `Swatinem/rust-cache` with its own cache key, and runs `cargo semver-checks`
    against every publishable crate. Two things the tool's defaults get wrong here and that this
    job must override:

    - Baseline. Pass the 0.9.0 baseline explicitly rather than relying on the tool's
      "latest published version" default, so a future out-of-band patch release cannot silently move
      the comparison point away from what D-06 specifies.
    - Package identity. Address crates by their published package names, not their `crates/`
      directory names. The published names are `paladin-ai-core`, `paladin-ports`,
      `paladin-battalion`, `paladin-herald`, `paladin-llm`, `paladin-memory`, `paladin-storage`,
      `paladin-notifications`, `paladin-content`, `paladin-web` and `paladin-ai` — note that
      `crates/paladin-core/` publishes as `paladin-ai-core`. A `--package` flag written against the
      directory name matches nothing and produces a green job with no diagnostics.

    Pin the installation to a tagged release (either a `cargo install cargo-semver-checks --locked`
    step or the maintained action at a tagged major version) rather than a moving branch ref. Give
    the job no `needs:` edge and no step that writes a baseline file into the working tree — the
    job must be re-runnable on an unchanged commit with an identical verdict and leave nothing
    behind.

    Add a final step to the job that enforces X-10.5's set equality: every allowlist entry must
    correspond to a MIGRATION.md 9.2 register row and every register row marked deliberate-breaking
    must have an allowlist entry. Implement it as a small shell step that extracts both key sets and
    diffs them, failing the job on any asymmetry. Do not swallow errors: read both files directly and
    let a missing file fail the step rather than defaulting either side to an empty set.
  </action>
  <acceptance_criteria>
    - `.cargo/semver-checks-allowlist.toml` exists and its header comment names MIGRATION.md 9.2 as its source of truth
    - `.github/workflows/ci.yml` contains a job key `semver:` whose steps reference `cargo-semver-checks`
    - The semver job passes an explicit `0.9.0` baseline rather than relying on the tool default
    - The semver job references published package names: it contains `paladin-ai-core` and does not address any crate by a `crates/` directory path
    - The semver job has no `needs:` key and writes no file into the repository working tree
    - The allowlist-vs-register consistency step exists and fails on asymmetry in either direction
    - No blanket suppression is present: `grep -nE '^\s*(ignore|allow)\s*=\s*\["?\*' .cargo/semver-checks-allowlist.toml | wc -l` is `0`
    - `actionlint .github/workflows/ci.yml` exits 0
    - `cargo semver-checks check-release --baseline-version 0.9.0 --workspace` run locally reports no unregistered breaking change (or reports only entries present in the allowlist)
  </acceptance_criteria>
  <verify>
    <automated>test -f .cargo/semver-checks-allowlist.toml && grep -q '^  semver:' .github/workflows/ci.yml && grep -q 'paladin-ai-core' .github/workflows/ci.yml && grep -q '0.9.0' .github/workflows/ci.yml && [ "$(grep -nE '^\s*(ignore|allow)\s*=\s*\[\"?\*' .cargo/semver-checks-allowlist.toml | wc -l)" = "0" ] && actionlint .github/workflows/ci.yml</automated>
  </verify>
  <done>A semver CI job runs on every PR against the explicit v0.9.0 baseline, addresses crates by published package name, carries a per-item allowlist kept set-equal to MIGRATION.md 9.2 by a failing CI step, and holds no blanket suppression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR author -> CI gate configuration | A contributor can weaken a gate by editing the allowlist or the workflow in the same PR that needs the weakening |
| crates.io -> semver baseline | An externally published version determines what the gate compares against |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-10 | Tampering | `.cargo/semver-checks-allowlist.toml` | high | mitigate | Per-item entries only, with a CI step asserting set equality against MIGRATION.md 9.2 in both directions and an acceptance check rejecting any wildcard suppression, so a blanket ignore cannot be slipped in as a one-line diff |
| T-22-11 | Tampering | semver baseline resolution | high | mitigate | The 0.9.0 baseline is passed explicitly rather than resolved as "latest published", so publishing an out-of-band version cannot silently move the comparison point and green a real break (RESEARCH.md Pitfall 1) |
| T-22-12 | Repudiation | a semver job matching zero packages and reporting success | high | mitigate | Crates are addressed by published package name (`paladin-ai-core`, not the `paladin-core` directory), the exact failure mode RESEARCH.md Pitfall 2 documents; an acceptance check asserts the published name appears in the workflow |
| T-22-SC | Tampering | `cargo install cargo-semver-checks` in CI | high | mitigate | Installed from crates.io at a pinned tagged release rather than a moving ref; the tool is a CI-only install and is not a workspace dependency. RESEARCH.md's Package Legitimacy Audit verdict for it is OK with no `[ASSUMED]`/`[SUS]`/`[SLOP]` rows, so no legitimacy checkpoint is required |
</threat_model>

## Artifacts this phase produces

Artifacts created by this plan (new in this phase — not pre-existing surface):

- `MIGRATION.md` (repository root)
- `.cargo/semver-checks-allowlist.toml`
- CI job names: `semver` ("Semver Checks (vs v0.9.0)"), `msrv` ("MSRV (Rust 1.85)")
- `rust-version` key on all eleven publishable crate manifests

<verification>
- `cargo check --workspace --all-features` on the 1.85 toolchain succeeds
- `actionlint .github/workflows/ci.yml` clean
- `cargo semver-checks check-release --baseline-version 0.9.0 --workspace` reports no unregistered break
- `cargo test --workspace --lib` still green (manifest edits changed no code)
- The allowlist-vs-register consistency step passes
</verification>

<success_criteria>
ENG-08 is satisfied: `MIGRATION.md` exists with the full section 9 skeleton and this phase's entries
filled in, a per-item semver allowlist is kept set-equal to its register by CI, and both the
`semver` and `msrv` jobs run on every PR with no `needs` edge and no repository side effects.
</success_criteria>

<output>
Create `.planning/phases/22-battlefield-state-superstep-engine/22-04-SUMMARY.md` when done.
</output>
