---
phase: 22.1-engine-readiness-defect-and-msrv-follow-up
plan: 02
type: execute
wave: 2
depends_on: [22.1-01]
files_modified:
  - crates/paladin-battalion/src/engine/mod.rs
  - crates/paladin-battalion/src/engine/graph.rs
  - crates/paladin-battalion/src/engine/superstep.rs
  - .project/current-exports.txt
autonomous: true
requirements: [ENG-02]

must_haves:
  truths:
    - "A run that is about to report Completed while a non-dead, eligible node still holds an unconsumed fired incoming edge fails with a typed EngineError naming every such node, instead of returning Completed (D-04)."
    - "A graph shape whose nodes can never receive a fired edge from outside their own component — and that is not exempted by a declared runtime-entry marker — is rejected by WarGraph::validate with a typed EngineError naming every offending node (D-03's guard)."
    - "A deferred (aggregate-after-all-branches) node that is starved by the same cycle-bootstrap mechanism is released too, so D-04's check cannot fire on a legitimately-declared aggregator (D-02a covers every declared node, deferred included)."
    - "Every pre-existing engine test passes with its assertions unmodified, including the four validate-ordering tests that assert which error a caller sees first (D-02d)."
    - "Determinism holds over the new cycle-bootstrap shapes: 20 randomized scheduling iterations produce byte-identical Battlefields and identical node execution counts (ENG-FR-04/08, D-02c)."
    - "E2E-1 (tests/integration/e2e_crash_resume_test.rs) passes without any assertion or fixture change (D-02d)."
  artifacts:
    - "crates/paladin-battalion/src/engine/mod.rs — EngineError::UnschedulableCycle and EngineError::StarvedNodeAtCompletion variants, each with full rustdoc in UnreachableNode's voice."
    - "crates/paladin-battalion/src/engine/graph.rs — private WarGraph::unschedulable_unfed_nodes() fixpoint plus the validate() clause that consumes it."
    - "crates/paladin-battalion/src/engine/superstep.rs — the run-end truthful-outcome check on the Completed branch, and the deferred-node starvation tier."
    - ".project/current-exports.txt — regenerated API-surface baseline covering the two new variants."
  key_links:
    - "superstep::run's Completed branch -> Frontier::node_edge_summary: the run-end check must key on the same fresh-fire definition the release uses, or the two can disagree and the check becomes either a false alarm or a no-op."
    - "WarGraph::validate -> validate_eligible_set -> the new unschedulable-cycle clause: ordering is load-bearing — the new clause runs last so every existing ordering test keeps seeing its current error."
    - "EngineError variants -> .project/current-exports.txt -> the api-surface CI job: an unregenerated baseline turns that job red."
  prohibitions:
    - statement: "The engine MUST NOT report RunOutcome::Completed while a node in the eligible set that received a fired incoming edge has never executed — silence about work that never happened is the failure mode this phase exists to end, and a documented limitation is not an acceptable substitute for a typed error."
      status: unverified
      verification: flagged-unverified
---

<objective>
Make BUG-03's class of lie structurally loud: add the validate-time guard for graph shapes the
starvation release still cannot schedule (D-03), the independent run-end truthful-outcome check
(D-04), and the determinism/regression coverage the new frontier shapes need (D-02c/d).

Purpose: Plan 22.1-01 fixed the mechanism. This plan makes a future regression of that mechanism
fail loudly rather than silently — the same two-layer shape BUG-02 established (reject early where
provable, fail typed at the boundary otherwise).

Output: two typed `EngineError` variants, a validate-time fixpoint guard, a run-end check on the
`Completed` branch, a deferred-node starvation tier, determinism coverage, and a regenerated
API-surface baseline.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22.1-engine-readiness-defect-and-msrv-follow-up/22.1-CONTEXT.md
@.planning/phases/22.1-engine-readiness-defect-and-msrv-follow-up/22.1-PATTERNS.md
@.planning/phases/22.1-engine-readiness-defect-and-msrv-follow-up/22.1-01-SUMMARY.md
@crates/paladin-battalion/src/engine/mod.rs
@crates/paladin-battalion/src/engine/graph.rs
</context>

<!-- planner-discipline-allow: BUG-03 -->

## Artifacts this phase produces

| Kind | Symbol / path | Plan |
|------|---------------|------|
| enum variant | `EngineError::UnschedulableCycle { nodes: Vec<NodeId>, reason: String }` | 22.1-02 |
| enum variant | `EngineError::StarvedNodeAtCompletion { nodes: Vec<NodeId>, reason: String }` | 22.1-02 |
| private fn | `WarGraph::unschedulable_unfed_nodes` | 22.1-02 |
| private fn | `WarGraph::validate_schedulable` (the `validate()` clause calling the fixpoint) | 22.1-02 |
| private fn | `starved_at_completion` (the run-end check helper in `superstep.rs`) | 22.1-02 |
| test fn | `unschedulable_unfed_nodes_is_empty_for_a_cycle_fed_from_entry` | 22.1-02 |
| test fn | `unschedulable_unfed_nodes_names_every_node_of_an_externally_unfed_cycle` | 22.1-02 |
| test fn | `unschedulable_unfed_nodes_exempts_runtime_entry_marked_nodes` | 22.1-02 |
| test fn | `completion_check_names_every_node_holding_an_unconsumed_fired_edge` | 22.1-02 |
| test fn | `deferred_aggregator_starved_by_a_cycle_is_still_released` | 22.1-02 |
| test fn | `eng_fr_08_determinism_over_twenty_randomized_iterations_for_cycle_bootstrap_shapes` | 22.1-02 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Typed guard for graph shapes the release cannot schedule</name>
  <files>crates/paladin-battalion/src/engine/mod.rs, crates/paladin-battalion/src/engine/graph.rs</files>
  <read_first>
    - `crates/paladin-battalion/src/engine/mod.rs` — the `EngineError` enum (`#[derive(Debug, Error)]` + `#[non_exhaustive]`, ~line 137). Read `UnreachableNode` (~line 170-194) and `VanguardNodeMissing` (~line 259-268) in full: they are the field-shape and rustdoc-voice templates (`nodes: Vec<NodeId>` + `reason: String`, and a doc paragraph naming which call site produces the error and why it exists instead of a silent failure).
    - `crates/paladin-battalion/src/engine/graph.rs` — the `WarGraph` struct fields (~113-131: `nodes`, `node_order`, `defer_flags`, `dynamic_targets`, `edges`, `schema`, `entry`, `limits`), `validate()` (~line 278) and its clause ordering, and `validate_eligible_set()` (~321-396) — the worklist-fixpoint and "collect every offender, not just the first" discipline to copy.
    - `crates/paladin-battalion/src/engine/graph.rs` test module — `validate_accepts_two_node_cycle`, `validate_accepts_self_loop`, `validate_rejects_self_loop_only_stranded_node_naming_it`, `validate_prefers_limit_error_over_unreachable_node`, `validate_prefers_unknown_node_error_over_unreachable_node`, `validate_prefers_custom_dispatch_error_over_unreachable_node`. These are the ordering assertions that must keep passing byte-identical.
    - `crates/paladin-battalion/src/engine/superstep.rs` — the starvation release landed by Plan 22.1-01, so the guard's definition of "cannot be released" matches the release's actual rule.
    - `.planning/phases/22.1-engine-readiness-defect-and-msrv-follow-up/22.1-PATTERNS.md` — the `EngineError` variant template and the `validate_eligible_set` analog block.
  </read_first>
  <behavior>
    - `unschedulable_unfed_nodes_is_empty_for_a_cycle_fed_from_entry`: for `entry -> a`, `a -> b`, `b -> a` with only `entry` declared as entry, the fixpoint returns an empty vector — this shape IS schedulable via the starvation release and must never be rejected.
    - `unschedulable_unfed_nodes_names_every_node_of_an_externally_unfed_cycle`: for a graph with an entry node plus a disjoint 2-cycle `x -> y`, `y -> x` where neither `x` nor `y` is an entry node and no edge enters the pair from outside, the fixpoint returns `[x, y]` in `node_order` order.
    - `unschedulable_unfed_nodes_exempts_runtime_entry_marked_nodes`: the same graph with `x` and `y` marked via `mark_dynamic_target` returns an empty vector — the declared runtime-entry escape hatch is preserved (ENG-FR-02a note b).
    - `validate()` on the externally-unfed-cycle graph still returns `EngineError::UnreachableNode` (not the new variant), because the eligible-set clause runs first — asserted so the clause ordering is pinned by a test rather than by comment.
  </behavior>
  <action>
Add the two typed variants and the validate-time guard. Variant names below are fixed so the rest
of the phase can reference them; messages and rustdoc wording are Claude's discretion within
`UnreachableNode`'s established voice.

1. In `crates/paladin-battalion/src/engine/mod.rs`, add to `EngineError` (the enum already carries
   `#[non_exhaustive]`, so no per-variant annotation is needed — X-10 compliance is automatic):
   - `UnschedulableCycle { nodes: Vec<NodeId>, reason: String }` — produced by
     `WarGraph::validate`. Its rustdoc must state: which call site produces it; that it names EVERY
     offending node in one error in registration order (never HashMap order), mirroring
     `UnreachableNode`; that it exists so a graph the frontier can never bootstrap fails before any
     node executes rather than running to a false `Completed`; and, honestly, that it is checked
     LAST among `validate`'s clauses, so for a graph with no declared runtime-entry marker the
     eligible-set clause reports first and this clause is defence-in-depth against a future
     relaxation of that clause or a misapplied runtime-entry marker.
   - `StarvedNodeAtCompletion { nodes: Vec<NodeId>, reason: String }` — produced by
     `superstep::run` (wired in Task 2). Its rustdoc must state that it is the run-end
     truthful-outcome check: the engine refused to report `Completed` because a non-dead eligible
     node still held an unconsumed fired incoming edge, i.e. the scheduler's own invariant broke.
     Keep the two variants distinct: a validate-time "this shape cannot be scheduled" failure and
     a run-end "the engine's own invariant broke" failure are different failure classes and must
     not share one message.
2. In `crates/paladin-battalion/src/engine/graph.rs`, add a private
   `fn unschedulable_unfed_nodes(&self) -> Vec<NodeId>` computing this fixpoint:
   - Seed `unfed` with every declared node that is NOT in `self.entry` and has at least one
     incoming edge.
   - Repeat until no change: remove from `unfed` any node that has an incoming edge whose `from`
     is not itself in `unfed` — such a node can receive a fired edge from outside the set and is
     therefore reachable by the starvation release.
   - Remove every node carrying a declared runtime-entry marker (today: membership in
     `self.dynamic_targets`), documenting that ENG-FR-02a's worker-template and Route-target
     exemptions join this list when those features land in Phases 23/25.
   - Return the survivors in `self.node_order` order.
3. Add a private `fn validate_schedulable(&self) -> Result<(), EngineError>` that returns
   `UnschedulableCycle` naming the survivors when the vector is non-empty, with a `reason` that
   explains the rule and names the two fixes (feed the component from an entry-reachable node, or
   declare the entry point of the component as a runtime-entry target). Call it from `validate()`
   AFTER `self.validate_eligible_set()` — this ordering is required by D-02(d): every existing
   `validate_prefers_*` test and `validate_rejects_self_loop_only_stranded_node_naming_it` must
   keep seeing exactly the error they assert today. Document the ordering choice on `validate()`
   in the same style its existing "checked last" note uses for the eligible-set clause.
4. Add the four tests from `<behavior>` to `graph.rs`'s existing `#[cfg(test)] mod tests`, using
   the fixture idioms already in that module.

Do not change `validate_eligible_set`, do not reorder any existing `validate()` clause, and do not
touch `EngineError::GraphMismatch` or `ResumeOptions::allow_graph_change` semantics (D-19).
  </action>
  <verify>
    <automated>cargo test -p paladin-battalion --lib engine::graph</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'UnschedulableCycle' crates/paladin-battalion/src/engine/mod.rs` returns at least 2 (variant declaration plus its `#[error(...)]` context), and `grep -c 'StarvedNodeAtCompletion' crates/paladin-battalion/src/engine/mod.rs` returns at least 1.
    - `grep -c 'fn unschedulable_unfed_nodes' crates/paladin-battalion/src/engine/graph.rs` returns 1 and `grep -c 'fn validate_schedulable' crates/paladin-battalion/src/engine/graph.rs` returns 1.
    - `cargo test -p paladin-battalion --lib engine::graph` exits 0 with `0 failed`.
    - `cargo test -p paladin-battalion --lib engine::graph::tests::validate_prefers_limit_error_over_unreachable_node engine::graph::tests::validate_prefers_unknown_node_error_over_unreachable_node engine::graph::tests::validate_prefers_custom_dispatch_error_over_unreachable_node engine::graph::tests::validate_rejects_self_loop_only_stranded_node_naming_it` exits 0, and `git diff` shows no change inside those four test bodies.
    - `cargo test -p paladin-battalion --lib engine::graph::tests::validate_accepts_two_node_cycle` exits 0 with its body unchanged.
    - `cargo clippy -p paladin-battalion --all-targets -- -D warnings` exits 0 and `cargo fmt --check` exits 0.
  </acceptance_criteria>
  <done>Both typed variants exist with `UnreachableNode`-grade rustdoc, `validate()` rejects externally-unfed components after the eligible-set clause, the runtime-entry escape hatch is preserved, and every pre-existing validate-ordering assertion still holds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run-end truthful-outcome check and the deferred-node starvation tier</name>
  <files>crates/paladin-battalion/src/engine/superstep.rs, .project/current-exports.txt</files>
  <read_first>
    - `crates/paladin-battalion/src/engine/superstep.rs` — the `run` loop's completion branch (`let status = if next_vanguard.is_empty() { WaypointStatus::Completed } ...` and the `return Ok(RunOutcome::Completed { .. })` immediately after `persist_waypoint`, ~lines 514-539), plus the early "Vanguard empty at superstep 0" path near ~line 182-199. Both are places `Completed` is decided.
    - `crates/paladin-battalion/src/engine/superstep.rs` — `Frontier::node_edge_summary` and the starvation release landed by Plan 22.1-01, and `compute_next_vanguard`'s current tier order.
    - `crates/paladin-battalion/src/engine/mod.rs` — `EngineError::StarvedNodeAtCompletion` from Task 1, and `RunOutcome`.
    - `crates/paladin-battalion/src/engine/test_support.rs` — `CountingFunctionNode`, `RecordingWaypointStore`, `run_default`.
    - `scripts/extract-public-api.sh` — the exact invocation the `api-surface` CI job uses to regenerate `.project/current-exports.txt`.
  </read_first>
  <behavior>
    - `completion_check_names_every_node_holding_an_unconsumed_fired_edge`: calling the run-end check helper directly against a hand-constructed `Frontier` state where a non-dead node holds a fresh `Fired` incoming edge and has never executed returns that node (and every other such node) in `node_order` order; against the state at the end of a normal completed run it returns empty.
    - `deferred_aggregator_starved_by_a_cycle_is_still_released`: a graph where a `defer`-marked aggregator sits inside a cycle fed from outside runs the aggregator at least once and reports `Completed`, not `StarvedNodeAtCompletion`.
    - Every existing `engine::superstep` test that asserts `matches!(outcome, RunOutcome::Completed { .. })` still passes — the check must not fire on any legitimate completion.
  </behavior>
  <action>
1. Add a private helper in `superstep.rs` — `starved_at_completion(graph, frontier) -> Vec<NodeId>`
   — returning, in `graph.node_order()` order, every node that is not in `frontier.dead`, is
   declared in the graph, and whose `Frontier::node_edge_summary` reports at least one fresh fired
   incoming edge (fired at or after the node's `last_executed` threshold). This is D-04's
   belt-and-braces condition: such a node has work waiting that the scheduler never dispatched.
2. Wire it into BOTH places `run` decides `Completed`: the mid-loop branch where
   `next_vanguard.is_empty()`, and the early return for a Vanguard that is empty at entry. When the
   helper returns a non-empty vector, return
   `Err(EngineError::StarvedNodeAtCompletion { nodes, reason })` instead of
   `Ok(RunOutcome::Completed { .. })`, with a `reason` naming the invariant that broke (a node in
   the eligible set held an unconsumed fired incoming edge while the Vanguard was empty) and
   pointing at ENG-FR-06a. Persist the Waypoint exactly as the surrounding code already does before
   returning, so the failure is still checkpointed — do not skip persistence to take the error path.
3. Close the one legitimate way the check could fire on correct input: a `defer`-marked node that is
   starved by the cycle-bootstrap mechanism is skipped by Plan 22.1-01's release (which excludes
   deferred nodes so an aggregator still waits for released cycle nodes, per CONTEXT.md's
   recommended ordering). Add a final tier to `compute_next_vanguard`: after the existing defer
   release, if it too is empty, apply the SAME starvation rule to `defer`-marked nodes, ordered by
   `node_order`. Update `compute_next_vanguard`'s rustdoc to describe all four tiers in order.
4. Add the two tests from `<behavior>` to the `engine::superstep` test module.
5. Regenerate the API-surface baseline for the two new variants added in Task 1:
   `./scripts/extract-public-api.sh .project/current-exports.txt` (D-20). Commit the regenerated
   file with the code change so the `api-surface` CI job stays green.

The check is independent of the release mechanism on purpose (D-04) — do not implement it by
calling `compute_next_vanguard` again, and do not weaken it to "warn"; a typed error is the
requirement.
  </action>
  <verify>
    <automated>cargo test -p paladin-battalion --lib engine::superstep && cargo test --test e2e_crash_resume_test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'fn starved_at_completion' crates/paladin-battalion/src/engine/superstep.rs` returns 1.
    - `grep -c 'StarvedNodeAtCompletion' crates/paladin-battalion/src/engine/superstep.rs` returns at least 2 (both `Completed` decision sites are guarded).
    - `cargo test -p paladin-battalion --lib engine::superstep` exits 0 with `0 failed` and `0 ignored`.
    - `cargo test -p paladin-battalion --lib engine::superstep::tests::completion_check_names_every_node_holding_an_unconsumed_fired_edge` and `...::deferred_aggregator_starved_by_a_cycle_is_still_released` each exit 0 reporting `1 passed`.
    - `cargo test --test e2e_crash_resume_test` exits 0, and `git diff -- tests/integration/e2e_crash_resume_test.rs` is empty for this task.
    - `./scripts/extract-public-api.sh /tmp/exports-check.txt && diff -q /tmp/exports-check.txt .project/current-exports.txt` exits 0 (baseline is current).
    - `cargo clippy -p paladin-battalion --all-targets -- -D warnings` and `cargo fmt --check` both exit 0.
  </acceptance_criteria>
  <done>Both `Completed` decision sites fail with a typed error rather than lying, a starved deferred aggregator is released rather than reported, E2E-1 passes untouched, and the API-surface baseline matches the new public surface.</done>
</task>

<task type="auto">
  <name>Task 3: Determinism coverage over the new frontier shapes</name>
  <files>crates/paladin-battalion/src/engine/superstep.rs</files>
  <read_first>
    - `crates/paladin-battalion/src/engine/superstep.rs` — the existing determinism test `eng_fr_08_determinism_over_twenty_randomized_scheduling_iterations` (~line 2047) in full, including its seed loop (`for seed in 0..20u64`), its `shuffle_seeded(&mut entry_ids, seed)` perturbation and the byte-comparison it makes across iterations. Copy this harness shape rather than inventing a new one.
    - `crates/paladin-battalion/src/engine/test_support.rs` — `shuffle_seeded` (~line 309) and `YieldingNode` (~line 276) and the rationale comment explaining why a per-node yield count is needed for a determinism assertion to mean anything.
    - The two cycle-bootstrap fixtures added by Plan 22.1-01 (`self_looping_node_fed_by_upstream_edge_can_never_take_first_turn`, `cycle_node_fed_from_outside_the_cycle_takes_its_first_turn`) — reuse their graph construction.
  </read_first>
  <action>
Add `eng_fr_08_determinism_over_twenty_randomized_iterations_for_cycle_bootstrap_shapes` next to
the existing determinism test, built from the existing harness (do not rebuild it, D-02c /
RESEARCH.md "extend, don't rebuild"). For each of the two cycle-bootstrap shapes from Plan
22.1-01 — the self-loop-fed-from-upstream shape and the `entry -> a -> b -> a` shape — run 20
seeded iterations, perturbing spawn order with `shuffle_seeded` exactly as the existing test does,
and assert across all 20 iterations that: the serialized final `Battlefield` is byte-identical to
iteration 0's; the per-node `run_count()` values are identical; and the `RunOutcome` discriminant
is identical. Use `YieldingNode`-backed nodes where the existing harness does, so the assertion is
not true only by accident of single-threaded ordering.

Keep the new test in the same `#[cfg(test)] mod tests` block and follow the existing test's naming
and comment conventions. Do not modify the existing determinism test.
  </action>
  <verify>
    <automated>cargo test -p paladin-battalion --lib engine::superstep::tests::eng_fr_08_determinism_over_twenty_randomized_iterations_for_cycle_bootstrap_shapes</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p paladin-battalion --lib engine::superstep::tests::eng_fr_08_determinism_over_twenty_randomized_iterations_for_cycle_bootstrap_shapes` exits 0 reporting `1 passed`.
    - `grep -c '0\.\.20u64' crates/paladin-battalion/src/engine/superstep.rs` returns at least 2 (the pre-existing determinism test plus the new one), confirming the 20-iteration floor is met by construction.
    - `git diff -- crates/paladin-battalion/src/engine/superstep.rs` shows no change inside `eng_fr_08_determinism_over_twenty_randomized_scheduling_iterations`.
    - `cargo test -p paladin-battalion` exits 0 with `0 failed`.
    - `cargo fmt --check` and `cargo clippy -p paladin-battalion --all-targets -- -D warnings` both exit 0.
  </acceptance_criteria>
  <done>Twenty randomized scheduling iterations over both new cycle-bootstrap shapes produce byte-identical Battlefields and identical execution counts, with the pre-existing determinism test untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller -> `WarGraph::validate` | An untrusted-to-the-engine graph definition crosses here; the guard rejects shapes that cannot be honestly executed before any node runs. |
| caller -> `RunOutcome` | The outcome value is the trust surface; `StarvedNodeAtCompletion` converts a silent lie into a typed failure. |
| in-process only | No network, filesystem, credential or external-input surface is touched by this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22.1-04 | Tampering (of the truthful-outcome contract) | `superstep::run` `Completed` decision sites | high | mitigate | `starved_at_completion` guards both decision sites and returns `EngineError::StarvedNodeAtCompletion` naming every offending node (Task 2) — an independent net, not a re-run of the release logic. |
| T-22.1-05 | Spoofing (a graph claiming to be executable when it is not) | `WarGraph::validate` | medium | mitigate | The `unschedulable_unfed_nodes` fixpoint rejects externally-unfed components with a typed error naming every node (Task 1), preserving the declared runtime-entry escape hatch rather than silently accepting or silently rejecting it. |
| T-22.1-06 | Information Disclosure | `EngineError` message payloads | low | accept | Both new variants carry only `NodeId`s and a static explanatory `reason` — graph-structure identifiers the caller already authored. No Battlefield values, prompts, model names or credentials are interpolated into either message. |
| T-22.1-07 | Denial of Service | the deferred-node starvation tier | medium | mitigate | The tier is gated behind all three earlier tiers being empty and only releases nodes already holding a fresh fired edge; `EngineLimits::max_supersteps` / `max_node_visits` remain the termination bound, exercised by the pre-existing self-loop limit tests that must stay green. |
| T-22.1-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no packages and does not modify `Cargo.toml` or `Cargo.lock`. `22.1-RESEARCH.md` §Package Legitimacy Audit records seven `OK` verdicts with no `[ASSUMED]`/`[SUS]`/`[SLOP]` package for the phase. |
</threat_model>

<verification>
- `cargo test -p paladin-battalion` — 0 failed, 0 ignored.
- `cargo test --test e2e_crash_resume_test` — green, fixture untouched.
- `cargo test --workspace` — green.
- `cargo fmt --check`, `cargo clippy --workspace --all-targets --all-features -- -D warnings` — clean.
- `./scripts/extract-public-api.sh` output matches the committed `.project/current-exports.txt`.
</verification>

<success_criteria>
`validate()` rejects unschedulable shapes with a typed error naming every node while preserving
every existing clause-ordering assertion; both `Completed` decision sites fail typed rather than
lying; a starved deferred aggregator is released; 20 randomized iterations over the new shapes are
byte-identical; and the API-surface baseline is regenerated.
</success_criteria>

<output>
Create `.planning/phases/22.1-engine-readiness-defect-and-msrv-follow-up/22.1-02-SUMMARY.md` when done
</output>
