---
phase: 22.1-engine-readiness-defect-and-msrv-follow-up
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - Cargo.lock
  - crates/paladin-core/Cargo.toml
  - crates/paladin-ports/Cargo.toml
  - crates/paladin-battalion/Cargo.toml
  - crates/paladin-herald/Cargo.toml
  - crates/paladin-llm/Cargo.toml
  - crates/paladin-memory/Cargo.toml
  - crates/paladin-storage/Cargo.toml
  - crates/paladin-notifications/Cargo.toml
  - crates/paladin-content/Cargo.toml
  - crates/paladin-web/Cargo.toml
  - .github/workflows/ci.yml
  - README.md
  - MIGRATION.md
  - CHANGELOG.md
  - docs/src/contributing/development-setup.md
  - docs/src/architecture/overview.md
  - docs/src/api-reference/stable-api.md
autonomous: true
requirements: [ENG-05]

must_haves:
  truths:
    - "The workspace declares one MSRV floor of 1.88 in exactly one place (workspace.package.rust-version), and all ten crate manifests inherit it via rust-version.workspace = true (D-10)."
    - "cargo +1.88 check --workspace --all-features --all-targets --locked succeeds against the committed lockfile — the floor is measured, not declared (D-07)."
    - "make security passes with zero vulnerabilities and no new entry in .cargo/audit.toml, deny.toml or SECURITY-EXCEPTIONS.md — RUSTSEC-2026-0009 is cleared by upgrading time to >= 0.3.47, not by an exception (D-07/D-08)."
    - "scripts/check-advisory-register.sh exits 0 against the restored lockfile."
    - "The MSRV figure agrees across every location that states it: root Cargo.toml, the ten crate manifests, the ci.yml msrv job, README.md's badge and prerequisites, MIGRATION.md §9.3, CHANGELOG.md and the three mdBook pages — with no location left stating a superseded figure (D-10)."
    - "[workspace] resolver = 3 is set and changes nothing in the committed lockfile beyond the D-08 restore (D-09)."
    - "cargo tree -e features -p paladin-ai under default features gains no entry relative to the pre-change tree, and the result is recorded in MIGRATION.md §9.3 (D-11, X-11.4)."
    - "The rmcp = \"=2.1.0\" exact pin from Phase 12.1 is unchanged and the transport-child-process feature is still enabled — no shipped Arsenal capability is removed (D-06, X-03)."
  artifacts:
    - "Cargo.toml — [workspace] resolver = \"3\", a [workspace.package] table declaring rust-version = \"1.88\"."
    - "Cargo.lock — the pre-44e13fbd resolution, re-resolved under resolver 3."
    - "Ten crate manifests carrying rust-version.workspace = true."
    - ".github/workflows/ci.yml — the msrv job retargeted to 1.88 (job name, RUSTUP_TOOLCHAIN, toolchain action, cache key, comment block)."
    - "MIGRATION.md §9.3 — the measured bump, its dependency chain, and the two rejected alternatives."
    - "CHANGELOG.md [Unreleased] ### Changed — the MSRV bump entry."
  key_links:
    - "rmcp's transport-child-process feature -> process-wrap ^9.0 -> rustc >= 1.87: the chain that forces the floor above 1.85; breaking it by moving the rmcp pin is rejected by D-06."
    - "time >= 0.3.47 (RUSTSEC-2026-0009 fix) -> rustc >= 1.88: the chain that sets 1.88 rather than 1.87, and the reason no new security exception is needed."
    - "workspace.package.rust-version -> the ten crate manifests -> the ci.yml msrv job -> README badge: the single-source chain X-11.2 requires; a divergence in any link is what made the 1.85 figure false."
    - "resolver = 3 -> future cargo update: the recurrence guard that stops a transitive dependency from silently re-resolving above the declared floor."
  prohibitions:
    - statement: "A security advisory that a version upgrade can clear MUST NOT be silenced by adding an entry to .cargo/audit.toml, deny.toml or SECURITY-EXCEPTIONS.md — the register exists for advisories with no fix available, and using it for a fixable one converts a known-good upgrade path into permanent accepted risk."
      status: unverified
      verification: flagged-unverified
---

<objective>
Enact the X-11.2 stop-and-flag decision: raise the declared workspace MSRV floor to a measured
Rust 1.88, restore the lockfile resolution that the 1.85 floor forced 22-17 to downgrade, adopt the
MSRV-aware resolver as the recurrence guard, and reconcile every MSRV figure in the tree to the one
true number.

Purpose: the workspace advertises a floor that no configuration can actually satisfy — the
`=2.1.0`-pinned `rmcp`'s `transport-child-process` feature reaches `process-wrap ^9.0`, whose every
release needs rustc >= 1.86, and 22-17's hand-downgraded lockfile pinned `time` back to 0.3.45,
leaving RUSTSEC-2026-0009 open. 1.88 is the lowest floor that satisfies both without a new security
exception (D-06/D-07), measured green in research.

Output: one declared floor, a restored lockfile, a green `msrv` job target, and every document that
states an MSRV figure agreeing with it.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22.1-engine-readiness-defect-and-msrv-follow-up/22.1-CONTEXT.md
@.planning/phases/22.1-engine-readiness-defect-and-msrv-follow-up/22.1-RESEARCH.md
@.planning/decisions/0024-rustsec-exception-governance.md
@.planning/decisions/0036-audit-suppression-single-source-topology.md
@Cargo.toml
</context>

## Artifacts this phase produces

| Kind | Symbol / path | Plan |
|------|---------------|------|
| manifest table | `[workspace.package]` in root `Cargo.toml` (new table carrying the inherited `rust-version`) | 22.1-04 |
| manifest key | `rust-version.workspace = true` in each of the ten crate manifests | 22.1-04 |
| CI job identity | the `msrv` job's new name, `RUSTUP_TOOLCHAIN` value, toolchain action version and cache key | 22.1-04 |
| doc section | the replacement MSRV bullet in `MIGRATION.md` §9.3 | 22.1-04 |
| doc section | the `[Unreleased] ### Changed` MSRV entry in `CHANGELOG.md` | 22.1-04 |

<tasks>

<task type="auto">
  <name>Task 1: Restore the lockfile, adopt resolver 3, and declare the measured 1.88 floor</name>
  <precondition>Commit 44e13fbd is present in this clone (`git cat-file -e 44e13fbd^:Cargo.lock` exits 0) and `rustup` is on PATH.</precondition>
  <reversibility rating="costly">Published crates advertise the floor; lowering it later means re-pinning every dependency that has since moved (carried from CONTEXT.md D-06/D-07, where the decision is already made — no gate is inserted).</reversibility>
  <files>Cargo.toml, Cargo.lock, crates/paladin-core/Cargo.toml, crates/paladin-ports/Cargo.toml, crates/paladin-battalion/Cargo.toml, crates/paladin-herald/Cargo.toml, crates/paladin-llm/Cargo.toml, crates/paladin-memory/Cargo.toml, crates/paladin-storage/Cargo.toml, crates/paladin-notifications/Cargo.toml, crates/paladin-content/Cargo.toml, crates/paladin-web/Cargo.toml</files>
  <read_first>
    - `Cargo.toml` — the `[workspace]` table (`members`, `exclude`, `resolver` on line 10), `[workspace.dependencies]` (note the two `rmcp` blocks around lines 93 and 186 — these are NOT changed), and the `[package]` table's `rust-version` key on line 43.
    - `crates/paladin-core/Cargo.toml` line 5 (and the identical line 5 in the other nine crate manifests) — the per-crate `rust-version` key being replaced by workspace inheritance.
    - `git show 44e13fbd --stat` and `git show 44e13fbd` — the 20 lockfile entries 22-17 downgraded and the `bs58` entry it dropped, so the restore is understood rather than blind.
    - `.cargo/audit.toml`, `deny.toml`, `SECURITY-EXCEPTIONS.md` and `scripts/check-advisory-register.sh` — the governed advisory register this task must leave untouched and still-passing.
    - `.planning/phases/22.1-engine-readiness-defect-and-msrv-follow-up/22.1-RESEARCH.md` §"D-07's measurement, reproducible verbatim" — the exact command sequence already proven green, and §"Common Pitfalls" 3 and 4.
  </read_first>
  <action>
1. Restore the pre-pin-back resolution as the baseline (D-08): write `git show 44e13fbd^:Cargo.lock`
   over `Cargo.lock`. Do NOT layer new per-crate pins on top of 22-17's downgrades — reverting is
   the instruction, and research measured the restored resolution green at 1.88.
2. In the root `Cargo.toml`, set the `[workspace]` table's `resolver` value to `"3"` (D-09), and
   introduce a `[workspace.package]` table declaring `rust-version = "1.88"` as the single source
   of the floor (D-10). Point the root `[package]`'s own floor at it with
   `rust-version.workspace = true`, and make the same substitution on line 5 of each of the ten
   crate manifests listed in `<files>`. Leave every other key in every manifest untouched — in
   particular both `rmcp = "=2.1.0"` blocks and the `transport-child-process` feature stay exactly
   as they are (D-06; removing a shipped Arsenal capability is forbidden by X-03).
3. Re-resolve under the new resolver without changing anything else: run `cargo metadata --locked`
   first; if it fails because the resolver change moved something, run `cargo update --workspace`
   once and inspect the diff. Keeping or dropping resolution-only differences such as the `bs58`
   entry is Claude's discretion — the constraint is "no advisory, and no package requiring
   rustc > 1.88", not byte-identity with the pre-`44e13fbd` file.
4. Install and measure at the floor (D-07's measurement gate): `rustup toolchain install 1.88.0
   --profile minimal`, then `cargo +1.88 check --workspace --all-features --all-targets --locked`.
   If any crate needs a rustc newer than 1.88, hold that crate at its newest release requiring
   <= 1.88 via the resolver, provided that release carries no advisory. If no such resolution
   exists, STOP and surface a fresh X-11.2 stop-and-flag with the measured number — do not silently
   pick a higher floor.
5. Confirm the advisory position (D-08, RESEARCH.md Pitfall 4): run `make security`, which invokes
   both `cargo-audit` and `cargo-deny` — do not infer `cargo deny`'s result from `cargo audit`
   alone — and run `./scripts/check-advisory-register.sh`. Add NO entry to `.cargo/audit.toml`,
   `deny.toml` or `SECURITY-EXCEPTIONS.md`. If an advisory appears that an upgrade within the
   <= 1.88 constraint cannot clear, STOP and surface it rather than registering an exception.
6. Do not touch `rust-toolchain.toml` (the pinned 1.97.1 dev toolchain) or any Docker builder
   image — neither is affected by the floor change (D-10).
  </action>
  <verify>
    <automated>cargo +1.88 check --workspace --all-features --all-targets --locked && make security && ./scripts/check-advisory-register.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'resolver = "3"' Cargo.toml` returns 1 and `grep -c 'rust-version = "1.88"' Cargo.toml` returns 1.
    - `grep -c 'rust-version = "1.85"' Cargo.toml` returns 0 and `grep -rl 'rust-version = "1.85"' crates/ | wc -l` returns 0.
    - `grep -rhc 'rust-version.workspace = true' crates/*/Cargo.toml | paste -sd+ - | bc` returns 10.
    - `grep -c 'rmcp = "=2.1.0"' Cargo.toml` returns the same count as before this task (2), and `grep -c 'transport-child-process' Cargo.toml` is unchanged.
    - `cargo +1.88 check --workspace --all-features --all-targets --locked` exits 0.
    - `cargo metadata --locked --all-features --format-version 1 > /dev/null` exits 0.
    - No resolved package declares a rustc requirement above the floor: `cargo metadata --locked --all-features --format-version 1 | jq -r '[.packages[] | select(.rust_version != null) | select((.rust_version | split(".") | .[0] | tonumber) > 1 or ((.rust_version | split(".") | .[0] | tonumber) == 1 and (.rust_version | split(".") | .[1] | tonumber) > 88))] | length'` prints `0`.
    - `make security` exits 0 and its `cargo audit` leg reports no `vulnerability found` line.
    - `./scripts/check-advisory-register.sh` exits 0.
    - `git status --porcelain .cargo/audit.toml deny.toml SECURITY-EXCEPTIONS.md rust-toolchain.toml` produces no output (none of the four was modified).
    - `cargo test --workspace` exits 0 with `0 failed`.
  </acceptance_criteria>
  <done>The floor is declared once at 1.88, inherited by all ten crates, measured green by a real 1.88 compile of the full workspace at all features and all targets, and the advisory register is clean and untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Retarget the msrv CI job and reconcile every MSRV figure in the tree</name>
  <files>.github/workflows/ci.yml, README.md, MIGRATION.md, CHANGELOG.md, docs/src/contributing/development-setup.md, docs/src/architecture/overview.md, docs/src/api-reference/stable-api.md</files>
  <read_first>
    - `.github/workflows/ci.yml` lines 234-263 — the `msrv` job in full: the comment block above it (which currently justifies the floor by quoting the old declared value), the job `name`, the `RUSTUP_TOOLCHAIN` env value, the `dtolnay/rust-toolchain@` step, the `Swatinem/rust-cache` `key`, and the `cargo check` step.
    - `README.md` line 8 (the MSRV badge URL) and line 101 (the prerequisites sentence).
    - `MIGRATION.md` §9.3 — the first bullet, which currently states the floor needed no bump, plus the surrounding bullets' attribution style ("Landed in Plan 22-06, Task 2") to mirror.
    - `CHANGELOG.md` — the `[Unreleased]` section and its existing `### Added` subsection, so the new `### Changed` subsection is placed and formatted consistently.
    - `docs/src/contributing/development-setup.md` line 28, `docs/src/architecture/overview.md` line 240, and `docs/src/api-reference/stable-api.md` lines 137 and 453 — the four mdBook MSRV statements, two of which currently state a figure that disagrees with the rest of the tree entirely.
    - `.project/v0.10.0/00-program-overview.md` §3 X-11 (rules 1-4, and §11.2's procedure) and §9.3's required MIGRATION content — the governing rules for what the bullet must contain.
  </read_first>
  <action>
Every edit below lands in the SAME commit as Task 1's manifest change (X-11.2: the floor is applied
in one place and the badge moves with it).

1. `.github/workflows/ci.yml` `msrv` job — update, values only, same block: the job `name` so it
   states the new floor; the `RUSTUP_TOOLCHAIN` env value to `"1.88"`; the toolchain action to
   `dtolnay/rust-toolchain@1.88`; the `Swatinem/rust-cache` `key` to `msrv-1.88`; and the comment
   block above the job so it quotes the new declared value and still explains why
   `RUSTUP_TOOLCHAIN` is needed against the pinned dev toolchain. Keep the floating-patch behaviour
   of the action version as-is — it is the existing accepted pattern (RESEARCH.md Pitfall 3); do
   not pin a micro-version in CI.
2. `README.md` — line 8's badge URL segment becomes `rust-1.88%2B`, and line 101's prerequisites
   sentence states Rust >= 1.88 (edition 2024 is unchanged, ADR-0009).
3. `MIGRATION.md` §9.3 — replace the first bullet with the measured position. It must state the
   chain, not a slogan: the STDIO MCP transport requires `rmcp` at its exact `=2.1.0` pin, which
   with `transport-child-process` requires `process-wrap ^9.0`, whose releases require rustc 1.86
   or 1.87; and `time` >= 0.3.47 (the RUSTSEC-2026-0009 fix) requires rustc 1.88 — hence 1.88. Name
   the two rejected alternatives and why: 1.87 plus a new RUSTSEC-2026-0009 exception (rejected —
   adds to the exception sprawl ADR-0024 governs, for a floor one release lower), and 1.86 with
   `process-wrap` held at an exact `.0` pin (rejected — a transitive `.0` pin plus the same
   exception). State that this is an X-11.2 stop-and-flag resolution and a documented minor-version
   change, that the prior figure was derived from edition 2024's minimum and never measured, and
   that this is the first MSRV figure in the project's history with a passing measurement behind
   it. Add the X-11.4 default-feature-set confirmation (D-11): run `cargo tree -e features -p
   paladin-ai` under default features before and after the change and record that the default set
   did not grow, in the same style Phase 22 recorded it.
4. `CHANGELOG.md` — add a `### Changed` subsection under `[Unreleased]` with the MSRV bump entry:
   the new floor, the two-link dependency chain that forces it, and a pointer to MIGRATION.md §9.3.
5. mdBook — reconcile all four statements to the one true figure: the prerequisites line in
   `docs/src/contributing/development-setup.md`, the stack-table cell in
   `docs/src/architecture/overview.md`, and the two statements in
   `docs/src/api-reference/stable-api.md` (lines 137 and 453) that currently claim a figure
   disagreeing with the entire rest of the tree. Leave the surrounding policy sentence on line 453
   about minor-version bumps intact; only the figure changes.

Do not create an entry in `.cargo/audit.toml`, `deny.toml` or `SECURITY-EXCEPTIONS.md`, and do not
add a MIGRATION.md §9.2 X-10 register row — no public type signature changes in this plan.
  </action>
  <verify>
    <automated>grep -c 'dtolnay/rust-toolchain@1.88' .github/workflows/ci.yml && grep -c 'rust-1.88%2B' README.md && grep -lc '1\.88' MIGRATION.md CHANGELOG.md docs/src/contributing/development-setup.md docs/src/architecture/overview.md docs/src/api-reference/stable-api.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'dtolnay/rust-toolchain@1.88' .github/workflows/ci.yml` returns 1, and `grep -c 'dtolnay/rust-toolchain@1.85' .github/workflows/ci.yml` returns 0.
    - `grep -c 'msrv-1.88' .github/workflows/ci.yml` returns 1 and `grep -c 'RUSTUP_TOOLCHAIN: "1.88"' .github/workflows/ci.yml` returns 1.
    - `grep -c 'rust-1.88%2B' README.md` returns 1.
    - `grep -c 'Final MSRV: 1.85' MIGRATION.md` returns 0, and the replacement §9.3 bullet contains all of `process-wrap`, `RUSTSEC-2026-0009`, `X-11.2` and `paladin-ai`.
    - `grep -c '^### Changed' CHANGELOG.md` returns at least 1, and the `[Unreleased]` section contains a line matching `1.88`.
    - `grep -c '1.93.1' docs/src/api-reference/stable-api.md` returns 0.
    - `grep -c 'MSRV 1.85' docs/src/architecture/overview.md` returns 0.
    - Every one of `README.md`, `MIGRATION.md`, `CHANGELOG.md`, `docs/src/contributing/development-setup.md`, `docs/src/architecture/overview.md`, `docs/src/api-reference/stable-api.md` matches `1.88` at least once: `grep -l '1\.88' README.md MIGRATION.md CHANGELOG.md docs/src/contributing/development-setup.md docs/src/architecture/overview.md docs/src/api-reference/stable-api.md | wc -l` returns 6.
    - `git status --porcelain .cargo/audit.toml deny.toml SECURITY-EXCEPTIONS.md` produces no output.
    - `git log -1 --stat` shows the manifest change from Task 1 and these documentation changes in one commit (X-11.2: floor and badge move together).
  </acceptance_criteria>
  <done>The `msrv` CI job targets the new floor, README/MIGRATION/CHANGELOG/mdBook all state the same figure, no stale or contradictory MSRV number remains anywhere in the tree, and nothing was added to the advisory register.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crates.io -> `Cargo.lock` -> the build | Third-party code crosses into every artifact this project ships. The lockfile resolution is the control point; an outdated floor is what blocked the security-relevant `time` patch. |
| repository -> CI runner | `ci.yml` selects the toolchain the floor is measured against; a job that installs a different version than the manifests declare measures nothing. |
| advisory register (`.cargo/audit.toml`, `deny.toml`, `SECURITY-EXCEPTIONS.md`) | The governed suppression surface. Adding to it converts a fixable advisory into permanently accepted risk. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22.1-12 | Denial of Service | `time` < 0.3.47 in the committed lockfile (RUSTSEC-2026-0009) | high | mitigate | Restore the pre-`44e13fbd` resolution so `time` resolves >= 0.3.47, and prove it with `make security` (both `cargo-audit` and `cargo-deny`) plus `scripts/check-advisory-register.sh` — with no new register entry (Task 1, D-07/D-08). |
| T-22.1-13 | Tampering (silent dependency drift below a security-driven floor) | future `cargo update` runs | medium | mitigate | `[workspace] resolver = "3"` makes version selection MSRV-aware, so a transitive dependency cannot silently re-resolve above the declared floor; the `msrv` CI job remains the gate (Task 1, D-09). |
| T-22.1-14 | Repudiation (a declared floor nobody measured) | `workspace.package.rust-version` vs reality | medium | mitigate | The floor is asserted by a real `cargo +1.88 check --workspace --all-features --all-targets --locked` compile and by a `cargo metadata` scan proving no resolved package requires a newer rustc (Task 1 acceptance criteria), not by declaration. |
| T-22.1-15 | Elevation of Privilege (removing a shipped capability to dodge the floor) | `rmcp` `transport-child-process` / Arsenal STDIO adapter | medium | mitigate | The `=2.1.0` pin and the feature are asserted unchanged by acceptance criteria; X-03 forbids removing a shipped capability, and D-06 rejects moving the pin on measured evidence. |
| T-22.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new package is introduced — the lockfile restore re-selects versions of already-present dependencies. `22.1-RESEARCH.md` §Package Legitimacy Audit checked the seven security-relevant crates in the MSRV chain (`time`, `process-wrap`, `darling`, `serde_with`, `tonic`, `idna_adapter`, `home`) via `gsd-tools query package-legitimacy check`: all `OK`, none `[ASSUMED]`/`[SUS]`/`[SLOP]`, so no legitimacy checkpoint is required. `make security` re-verifies the resolved graph at execution time. |
</threat_model>

<verification>
- `cargo +1.88 check --workspace --all-features --all-targets --locked` — exits 0.
- `cargo test --workspace` — 0 failed.
- `make security` — `cargo-audit` and `cargo-deny` both clean, no `vulnerability found` line.
- `./scripts/check-advisory-register.sh` — exits 0.
- `cargo fmt --check` and `cargo clippy --workspace --all-targets --all-features -- -D warnings` — clean.
- No modification to `.cargo/audit.toml`, `deny.toml`, `SECURITY-EXCEPTIONS.md` or `rust-toolchain.toml`.
</verification>

<success_criteria>
One declared MSRV floor of 1.88, inherited by all ten crate manifests, proven by a real full-feature
compile at that toolchain; the restored lockfile clears RUSTSEC-2026-0009 with no new security
exception; `resolver = "3"` guards against recurrence; and every MSRV figure in CI, README,
MIGRATION.md, CHANGELOG.md and mdBook agrees.
</success_criteria>

<output>
Create `.planning/phases/22.1-engine-readiness-defect-and-msrv-follow-up/22.1-04-SUMMARY.md` when done
</output>
