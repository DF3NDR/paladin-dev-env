---
phase: 22.1-engine-readiness-defect-and-msrv-follow-up
plan: 05
subsystem: infra
tags: [ci, github-actions, postgres, gap-closure, evidence]

# Dependency graph
requires:
  - phase: 22.1-engine-readiness-defect-and-msrv-follow-up
    provides: "22.1-01 (readiness fix), 22.1-02 (typed guards), 22.1-03 (fingerprint completeness), 22.1-04 (MSRV floor raised to 1.88) — every change this evidence capture must sit on top of, per D-14"
provides:
  - "22.1-CI-EVIDENCE.md — a whole-workflow-run success (run 33706948313) on the branch head, superseding the previously-cited run whose overall conclusion was cancelled"
  - "G-22-1's UAT resolution field extended with the pointer to the new evidence"
affects: [ship, 22-battlefield-state-superstep-engine]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Whole-run evidence capture: run-level conclusion checked independently of job-level conclusion, since a green job can sit inside a cancelled/failed overall run (D-12)"

key-files:
  created:
    - .planning/phases/22.1-engine-readiness-defect-and-msrv-follow-up/22.1-CI-EVIDENCE.md
  modified:
    - .planning/phases/22-battlefield-state-superstep-engine/22-UAT.md

key-decisions:
  - "No push was needed: origin/feature/phase-22 and this worktree's base were already both 7c77b0e2 at capture time (the prior wave's merge had already pushed every commit from plans 22.1-01 through 22.1-04), and CI had already been triggered against that head"
  - "Two ci.yml runs existed on the head SHA (33706948313 createdAt 02:15:29Z, 33706946100 createdAt 02:15:27Z), both success; per the plan's ordering rule (no tie — 2s apart) exactly one is named as the selected run: 33706948313, the later by createdAt. The sibling is recorded, not silently dropped."
  - "22.1-CI-EVIDENCE.md's 'Selected run' section was placed before the 'Why the previously-cited evidence did not meet the bar' section specifically so the plan's own automated verify script (grep -oE '[0-9]{8,}' | head -1) picks up the correct run id rather than the superseded run's job id mentioned earlier in a narrative paragraph"
  - "G-22-1's resolution field was extended by appending one sentence to the existing (already-open, never-closed) double-quoted YAML scalar and adding a closing quote — this both preserves 22-17's original text verbatim and makes the field a well-formed single scalar, an improvement over its pre-existing unterminated-quote state shared with G-22-2's resolution field"

requirements-completed: []

coverage:
  - id: D1
    description: "A ci.yml run on the branch head, after every other Phase 22.1 change landed, whose overall conclusion is success (not a green job inside a cancelled/failed run) — the Postgres, MSRV, Security Audit and License & Dependency Policy jobs are all success in that run"
    requirement: "ENG-05"
    verification:
      - kind: other
        ref: "gh run view 33706948313 --json conclusion --jq .conclusion (== success); gh run view 33706948313 --json jobs --jq per-job conclusion for Postgres Waypoint Contract Suite/MSRV (Rust 1.88)/Security Audit/License & Dependency Policy (all == success)"
        status: pass
    human_judgment: false
  - id: D2
    description: "All four required Postgres log facts are quoted verbatim with timestamps in 22.1-CI-EVIDENCE.md: container healthy, pg_isready accepting connections, test result 26 passed/0 failed matching the module's independently-recomputed declared count, and the skip-detection line"
    requirement: "ENG-05"
    verification:
      - kind: other
        ref: "gh run view --job 100497999877 --log (source of all four quotes); grep -cE 'test result: ok\\. [0-9]+ passed; 0 failed' and grep -c 'exercised the live server'/'accepting connections' against 22.1-CI-EVIDENCE.md all >= 1"
        status: pass
    human_judgment: false
  - id: D3
    description: "22-UAT.md's G-22-1 resolution field carries a pointer to 22.1-CI-EVIDENCE.md and the new run id, with the historical 22-17 text preserved and every other gap entry/field untouched"
    requirement: "ENG-05"
    verification:
      - kind: other
        ref: "grep -c '22.1-CI-EVIDENCE.md' 22-UAT.md (==1); grep -c '100440861780' 22-UAT.md (>=1, original text preserved); git diff -U0 22-UAT.md | grep -c '^-[^-]' (==1, single line replaced); grep -c 'gap_id:' unchanged at 3"
        status: pass
    human_judgment: false
  - id: D4
    description: "A human confirms the closing evidence meets its own bar (Task 3, blocking checkpoint) before G-22-1 is sealed"
    human_judgment: true
    rationale: "Task 3 is an explicit checkpoint:human-verify with gate=\"blocking\" per the plan; per the checkpoint protocol this cannot be auto-approved even under auto-mode, and this SUMMARY is being committed checkpoint-pending, before that confirmation exists"

duration: ~1h40min (includes ~90min waiting on two CI runs to complete)
completed: 2026-09-03
status: checkpoint-pending
---

# Phase 22.1 Plan 05: G-22-1 closing evidence — whole-workflow-run success Summary

**Captured a fully green `ci.yml` run (33706948313) on the Phase 22.1 branch head as G-22-1's closing evidence, superseding the previously-cited run whose overall conclusion was `cancelled` and which carried three failed required jobs — Tasks 1 and 2 complete and committed; Task 3 (blocking human-verify checkpoint) is pending.**

## Performance

- **Duration:** ~1h40min (dominated by waiting for two already-queued `ci.yml` runs to complete, ~75 and ~100 minutes respectively)
- **Started:** 2026-09-03T02:15Z (approx, runs were already queued at plan start)
- **Completed (Tasks 1-2):** 2026-09-03T03:56Z
- **Tasks:** 2 of 3 complete (Task 3 is the blocking checkpoint this plan stops at)
- **Files modified:** 2 (1 created, 1 modified)

## Accomplishments

- Confirmed no push was needed: `origin/feature/phase-22` and this worktree's base were both already `7c77b0e22246e8c1207386bb0548f1ede19ee4ee` — the prior wave's merge had already put every Phase 22.1 commit (plans 22.1-01 through 22.1-04) on the branch head, and CI had already been triggered against it.
- Watched both queued `ci.yml` runs on that head SHA to completion via `gh run watch --exit-status` (backgrounded due to CI duration): run `33706948313` (createdAt `2026-09-03T02:15:29Z`) and its sibling `33706946100` (createdAt `2026-09-03T02:15:27Z`) — both completed with overall conclusion `success`. Neither superseded the other; the `ci.yml` concurrency group's cancellation did not fire between this pair.
- Selected `33706948313` as the evidence run per the plan's ordering rule (no tie — 2-second `createdAt` difference; the later run is named). Confirmed job-level: `Postgres Waypoint Contract Suite (live server)`, `MSRV (Rust 1.88)`, `Security Audit`, and `License & Dependency Policy` are all `success` — the same three job names that failed in the previously-cited run `33688238662` are now all green on this commit.
- Extracted all four required Postgres log facts verbatim with timestamps from job `100497999877`'s log: container health (step `Start postgres-test and wait for healthy`, `success`), `pg_isready`'s `accepting connections` line, `test result: ok. 26 passed; 0 failed`, and the skip-detection step's `All waypoint::postgres tests exercised the live server.` line. Independently re-ran `grep -cE '^\s*#\[tokio::test\]' crates/paladin-storage/src/waypoint/postgres.rs` against this worktree's copy of the file (== 26), confirming the CI job's own declared-count check (`Declared tests: 26, passed: 26`) against a second, independent source rather than trusting the job's assertion alone.
- Wrote `22.1-CI-EVIDENCE.md` in the `18-CODEQL-EVIDENCE.md` format: run id/URL, run-level conclusion, head SHA, timestamps, per-job conclusion table, the four quoted log facts in fenced blocks, and a "why the previously-cited evidence did not meet the bar" section naming run `33688238662`, its `cancelled` conclusion, and its three failed jobs by name.
- Appended a single sentence to G-22-1's `resolution` field in `22-UAT.md`, preserving the original 22-17 text verbatim, pointing at the new run and `22.1-CI-EVIDENCE.md`. `git diff` confirms the change is confined to exactly that one line; every other field and every other gap entry is untouched.
- No `.github/` file was edited during this capture (`git status --porcelain -- .github/` empty at every checkpoint) — no ruleset or workflow change, per ADR-0044.

## Task Commits

Each task was committed atomically:

1. **Task 1: Capture the whole-run evidence and author 22.1-CI-EVIDENCE.md** - `dd26ee7a` (docs)
2. **Task 2: Point G-22-1's UAT record at the closing evidence** - `79fe89e1` (docs)

**Plan metadata:** commit pending (this SUMMARY.md, checkpoint-pending)

**Task 3 (checkpoint:human-verify, gate="blocking") is NOT yet executed.** The plan stops here per its own execution flow — a fresh continuation cannot resume Task 1/2 (already committed) but must present Task 3's verification steps to a human.

## Files Created/Modified

- `.planning/phases/22.1-engine-readiness-defect-and-msrv-follow-up/22.1-CI-EVIDENCE.md` - new evidence document: run 33706948313, per-job conclusions, four quoted log facts, and the shortfall analysis of the previously-cited run
- `.planning/phases/22-battlefield-state-superstep-engine/22-UAT.md` - G-22-1's `resolution` field extended by one sentence, original text preserved

## Decisions Made

See `key-decisions` in frontmatter above. Summarized:
- No push was needed (base already matched origin); this is recorded explicitly in the evidence file rather than left implicit, since the plan's Task 1 step 1 assumed a push would be required.
- The evidence file's section order was chosen deliberately so the plan's own automated verify script (which takes the first 8+-digit number in the file) resolves to the correct run id, not an earlier-mentioned job id from the narrative "why the prior evidence fell short" section.
- The two sibling CI runs on the same head SHA did not tie on `createdAt` (2-second gap), so exactly one is named as primary per the ordering rule, with the sibling recorded rather than dropped.
- G-22-1's resolution field's pre-existing unterminated double-quote (shared with G-22-2's resolution field, a pre-existing quirk in this document) was closed off as part of the append, making the field a well-formed single YAML scalar per the plan's explicit acceptance criterion, without touching any other text in the field.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Reordered 22.1-CI-EVIDENCE.md's sections so the automated verify script resolves the correct run id**
- **Found during:** Task 1, immediately after first authoring the file
- **Issue:** The plan's own `<verify>` automated check does `grep -oE '[0-9]{8,}' <file> | head -1` to extract "the run id" and query it via `gh run view`. The first draft of the file led with a "Why the previously-cited evidence did not meet the bar" section naming job id `100440861780` before the "Selected run" section naming the actual evidence run `33706948313` — so the verify script would have resolved the wrong id (a job id, queried as if it were a run id) and failed every subsequent check.
- **Fix:** Moved the "Selected run" section (which states the run id, URL, head SHA, conclusion, and timestamps) to immediately follow the document's opening paragraph, before the "Why the previously-cited evidence did not meet the bar" section. Re-ran the full acceptance-criteria grep/gh sequence from the plan's `<verify>` and `<acceptance_criteria>` blocks after the reorder — all pass.
- **Files modified:** `.planning/phases/22.1-engine-readiness-defect-and-msrv-follow-up/22.1-CI-EVIDENCE.md`
- **Verification:** `grep -oE '[0-9]{8,}' 22.1-CI-EVIDENCE.md | head -1` returns `33706948313`; `gh run view 33706948313 --json conclusion --jq .conclusion` returns `success`; the four named-job conclusion greps all return `success`.
- **Committed in:** `dd26ee7a` (Task 1 commit)

**2. [Rule 1 - Bug] Closed G-22-1's pre-existing unterminated resolution-field quote when appending**
- **Found during:** Task 2
- **Issue:** The plan's acceptance criteria require "the field a valid single YAML scalar" after the edit. The existing `resolution:` field for G-22-1 (and, separately, G-22-2) opened with `"` but was never closed with a matching `"` — a pre-existing defect in the document (confirmed against `sed -n '51p' | cat -A`), unlike G-22-3's resolution field which does close correctly.
- **Fix:** Appended the required sentence and closed the field with a trailing `"`, making it well-formed rather than replicating the pre-existing defect forward. The original 22-17 text is preserved character-for-character up to the append point; `git diff` confirms exactly one line changed.
- **Files modified:** `.planning/phases/22-battlefield-state-superstep-engine/22-UAT.md`
- **Verification:** `git diff -U0 22-UAT.md | grep -c '^-[^-]'` returns 1 (single line replaced, nothing else deleted); `grep -c 'gap_id:'` unchanged at 3.
- **Committed in:** `79fe89e1` (Task 2 commit)

---

**Total deviations:** 2 (both Rule 1 auto-fixes)
**Impact on plan:** Both are necessary corrections that make the plan's own literal acceptance criteria pass. No scope creep — no file outside the plan's `files_modified` list was touched, and G-22-2's identically-shaped unterminated quote was left alone (out of this plan's scope; not renamed, not touched).

## Issues Encountered

- Both `ci.yml` runs on the branch head were already queued (not yet started) when this plan began execution, and each took roughly 75-100 minutes wall-clock to complete — well beyond the Bash tool's default and even extended timeouts. Resolved by using `gh run watch --exit-status` in `run_in_background` mode and waiting for the harness's completion notification rather than polling in a loop, per this environment's guidance on long-running background tasks.
- The two sibling runs (`33706948313`, `33706946100`) both reaching `success` independently — rather than one being cancelled by the `concurrency` group as the plan's narrative anticipated — was unexpected but not disqualifying: it is a stronger signal (two independent green whole-runs on the same commit) than the plan's ordering-rule edge case anticipated, and is recorded plainly in the evidence file rather than treated as an anomaly to explain away.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

**This plan is NOT complete.** Task 3 — a `checkpoint:human-verify` with `gate="blocking"` — requires explicit human confirmation that the captured evidence meets its own bar (D-12's whole-run-success standard) before G-22-1 can be sealed. Tasks 1 and 2 are committed and their acceptance criteria independently verified; Task 3's six verification steps (from the plan) are ready for a human to run:

1. Read `22.1-CI-EVIDENCE.md`.
2. `gh run view 33706948313 --json conclusion,headSha,createdAt --jq '{conclusion, headSha, createdAt}'` — confirm `conclusion` is `success` and `headSha` matches the file.
3. `gh run view 33706948313 --json jobs --jq '[.jobs[] | {name, conclusion}]'` — confirm Postgres, MSRV, Security Audit, License & Dependency Policy are all `success`.
4. Spot-check one quoted log fact against `gh run view --job 100497999877 --log | grep -F '<quoted fragment>'`.
5. Confirm `git log --oneline` shows the commits from plans 22.1-01 through 22.1-04 at or before the recorded SHA.
6. Confirm `git status --porcelain` shows only the two planning files touched (no `.github/` change).

No blockers to the checkpoint itself — all six verification steps are independently reproducible from the evidence already committed.

## Self-Check: PASSED

- `.planning/phases/22.1-engine-readiness-defect-and-msrv-follow-up/22.1-CI-EVIDENCE.md` confirmed present on disk (`test -s` passed during Task 1 verification).
- `git log --oneline -5` confirms both task commit hashes (`dd26ee7a`, `79fe89e1`) present.
- `gh run view 33706948313` re-confirmed reachable and `success` at time of writing this summary.

---
*Phase: 22.1-engine-readiness-defect-and-msrv-follow-up*
*Completed (Tasks 1-2; Task 3 pending): 2026-09-03*
