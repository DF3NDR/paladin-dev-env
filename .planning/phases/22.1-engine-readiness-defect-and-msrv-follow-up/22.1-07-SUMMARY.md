---
phase: 22.1-engine-readiness-defect-and-msrv-follow-up
plan: 07
subsystem: ci-evidence
tags: [ci, evidence, postgres, msrv, security-audit, license-policy, gh-cli]

# Dependency graph
requires:
  - phase: 22.1-engine-readiness-defect-and-msrv-follow-up
    provides: "Plan 22.1-06's BUG-04 fix commits (test(22.1-06) c4dc9859, fix(22.1-06) 9cf69ee0/acc07c5e) and the two new frontier round-trip contract clauses that raised waypoint::postgres's declared #[tokio::test] count from 26 to 28"
provides:
  - "A second, dated whole-run ci.yml success capture (run 33758221386, head 4885380c) in 22.1-CI-EVIDENCE.md, re-meeting D-12's bar on the head that carries the BUG-04 fix, per D-25"
  - "D-20 program-gate confirmation (msrv 1.88, semver-checks vs 0.9.0, api-surface, Security Audit, License & Dependency Policy all success) on the phase's final commit"
affects: []

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Run-level gh run view --json conclusion resolved BEFORE job-level gh run view --json jobs, per D-12/D-25 -- a green job inside a non-success run is not evidence, so the whole-run verdict gates before any job detail is read"
    - "Append-only evidence file: a dated ## Re-capture after <defect> (<date>) section is appended below the original capture, never replacing it, so an evidence file that spans two heads for two reasons preserves both as durable proof"

key-files:
  created: []
  modified:
    - .planning/phases/22.1-engine-readiness-defect-and-msrv-follow-up/22.1-CI-EVIDENCE.md

key-decisions:
  - "Selected run 33758221386 over its sibling 33758215860 (both success, both on head 4885380c, 4-second createdAt gap) per the plan's ordering rule -- no tie, so exactly one run is named rather than recording both"
  - "Corrected an initial guess mid-task: git log --oneline <sha> | grep -c 'fix(22.1-06)' returns 2 not because of a docs-commit text match, but because Plan 22.1-06 genuinely committed two fix(22.1-06) commits (9cf69ee0 the BUG-04 fix, acc07c5e the post-Task-3 engine_benchmarks.rs mechanical fix) -- verified by reading the actual matched lines before writing the explanation into the evidence file, rather than leaving an unverified guess in a durable record"

patterns-established: []

requirements-completed: [ENG-05]

coverage:
  - id: D25-recapture
    description: "A ci.yml run whose overall conclusion is success, on the branch head containing Plan 22.1-06's BUG-04 fix, with Postgres/MSRV/Security Audit/License & Dependency Policy all success and the four Postgres log facts quoted verbatim, appended below the original capture without modifying it"
    requirement: "ENG-05"
    verification:
      - kind: other
        ref: "gh run view 33758221386 --json conclusion,jobs (run 33758221386, head 4885380cf46a903a57423e48300404a2ed6e2445)"
        status: pass
    human_judgment: false
  - id: D25-declared-count
    description: "The quoted test-result line's passed count (28) equals the job's own declared-count step's EXPECTED (28) and this worktree's independently re-run grep -cE '^\\s*#\\[tokio::test\\]' crates/paladin-storage/src/waypoint/postgres.rs (28) -- up from the first capture's 26, proving Plan 22.1-06's two new clauses ran against the live server"
    requirement: "ENG-05"
    verification:
      - kind: other
        ref: "gh run view --job 100657744879 --log; grep -cE '^\\s*#\\[tokio::test\\]' crates/paladin-storage/src/waypoint/postgres.rs"
        status: pass
    human_judgment: false
  - id: D25-append-only
    description: "The first capture's section (run 33706948313, head 7c77b0e2) is preserved byte-for-byte; the edit is a pure append with 0 lines removed above the new section"
    requirement: "ENG-05"
    verification:
      - kind: other
        ref: "git diff -U0 22.1-CI-EVIDENCE.md | grep -c '^-[^-]' == 0"
        status: pass
    human_judgment: false

duration: ~1h38min
completed: 2026-09-03
status: complete
---

# Phase 22.1 Plan 07: Re-capture CI evidence on the BUG-04-fix head Summary

**Resolved and verified `ci.yml` run `33758221386` on branch head `4885380c` (the head carrying Plan 22.1-06's BUG-04 fix) at run-level `success`, with the four named jobs and all five D-20 program gates green, and appended a dated `## Re-capture after BUG-04 (2026-09-03)` section to `22.1-CI-EVIDENCE.md` quoting the four Postgres log facts verbatim at the new declared-vs-passed count of 28 -- while leaving the original run's section (`33706948313`, head `7c77b0e2`) byte-for-byte intact as G-22-1's original closing evidence.**

## Performance

- **Duration:** ~1h38min (dominated by waiting ~93 minutes for the orchestrator-triggered `ci.yml` run to complete, largely the Docker Build job)
- **Started:** ~2026-09-03T12:57Z (orchestrator's push completed just before this plan's precondition check)
- **Completed:** 2026-09-03T14:34Z
- **Tasks:** 2 (Task 1 investigation-only, no file changes; Task 2 the single commit)
- **Files modified:** 1

## Accomplishments

- Verified the precondition: `gh auth status` exits 0, the working tree was clean, and `origin/feature/phase-22` (`4885380cf46a903a57423e48300404a2ed6e2445`) matched this worktree's base and contained both `test(22.1-06)` (`c4dc9859`, `7325451a`) and `fix(22.1-06)` (`9cf69ee0`, `acc07c5e`) commits, confirmed via `git merge-base --is-ancestor` (exit 0) and `git log --oneline | grep`.
- Resolved two `ci.yml` runs on the target head (`33758221386` createdAt `12:57:16Z`, `33758215860` createdAt `12:57:12Z`) and, per the plan's ordering rule, selected the later one -- `33758221386` -- since the pair does not tie (4-second gap).
- Confirmed the selected run's overall conclusion is `success` at the **run level** first (`gh run view --json conclusion`), before reading job-level detail, matching D-12/D-25's rule that a green job inside a non-`success` run is not evidence.
- Confirmed at the job level: `Postgres Waypoint Contract Suite (live server)`, `MSRV (Rust 1.88)`, `Security Audit`, and `License & Dependency Policy` are all `success`; recorded all 33 job conclusions (31 `success`, 2 `skipped` -- `Publish Dry Run` and `End-to-End Tests`, the same two skipped in the first capture).
- Extracted and quoted verbatim the four required Postgres log facts from job `100657744879`: (a) the container-healthy line with the step's own `success` conclusion, (b) the `pg_isready` accepting-connections line, (c) `test result: ok. 28 passed; 0 failed` alongside the job's own `Declared tests: 28, passed: 28` line, plus an independent `grep -cE` re-run in this worktree confirming 28, and (d) the skip-detection step's `All waypoint::postgres tests exercised the live server.` line.
- Also confirmed the five D-20 program gates (MSRV 1.88, `cargo semver-checks` vs 0.9.0, API Surface Tracking, Security Audit, License & Dependency Policy) are all `success` in the same run, satisfying D-25's statement that this capture also closes D-20's "gates green on the phase's final commit" requirement.
- Appended the `## Re-capture after BUG-04 (2026-09-03)` section to `22.1-CI-EVIDENCE.md`, mirroring the first capture's layout (Selected run / Run selection (ordering) / Per-job conclusion / four Postgres log facts / D-20 program-gate confirmation / What this evidence proves and does not / No ruleset or workflow edit) as a pure append -- `git diff -U0` confirms 0 lines removed above the new section, and the original run id `33706948313` and head SHA `7c77b0e2` remain present unmodified.

## Task Commits

Task 1 (resolve and verify the run) produced no file changes -- it is pure investigation via `gh` and `grep` against already-committed state, so nothing was staged or committed for it. Task 2's file write is the plan's single commit:

1. **Task 2: append the dated re-capture section to 22.1-CI-EVIDENCE.md** - `abfd1189` (docs)

**Plan metadata:** commit pending (SUMMARY.md, this file)

## Files Created/Modified

- `.planning/phases/22.1-engine-readiness-defect-and-msrv-follow-up/22.1-CI-EVIDENCE.md` - appended `## Re-capture after BUG-04 (2026-09-03)` with the new run's selection, per-job table, four quoted Postgres log facts at the 28-declared count, the D-20 program-gate confirmation subsection, and the append-only verification note; nothing above the new section was touched

## Decisions Made

See `key-decisions` in frontmatter above. Summarized:
- Selected run `33758221386` over sibling `33758215860` per the ordering rule (no tie, later `createdAt` wins).
- Caught and corrected an unverified guess before it landed in the durable evidence record: the `fix(22.1-06)` grep-count-2 explanation was checked against the actual matched commit lines (two genuine fix commits, not a docs-commit text match) before being written into `22.1-CI-EVIDENCE.md`.

## Deviations from Plan

None - plan executed exactly as written. Task 1's precondition, verification, and acceptance criteria were all met without requiring any auto-fix; Task 2's action items and acceptance criteria were satisfied by the single append edit.

## Issues Encountered

One self-correction, not a deviation from the plan: my first draft of the "Selected run" ancestry explanation in the evidence file guessed the `grep -c 'fix(22.1-06)'` count of 2 came from a docs commit's text reference. Before finalizing, I ran `git log --oneline <sha> | grep 'fix(22.1-06)'` to see the actual matched lines and found two genuine `fix(22.1-06)` commits instead -- corrected the evidence file's explanation to the verified fact before committing, per this project's evidence-integrity standard (quote/verify, never paraphrase-and-assume in a durable record).

## User Setup Required

None. The CI run was already triggered by the orchestrator's push before this plan started; this plan only waited for and read the resulting run, per its own precondition and D-25's instruction that this plan does not push.

## Next Phase Readiness

- G-22-1's closing evidence now exists on both the pre-BUG-04 head (`7c77b0e2`, run `33706948313`) and the post-BUG-04 head (`4885380c`, run `33758221386`) that the phase actually ships, satisfying D-14's "evidence sits on the head the phase ships" rule for the final commit.
- D-20's "program gates green on the phase's final commit" requirement is now evidenced by the same run: MSRV 1.88, semver-checks vs 0.9.0, api-surface, Security Audit, and License & Dependency Policy are all `success` on `4885380c`.
- No blockers. This was the phase's last plan (D-14/D-25); `22.1-CI-EVIDENCE.md` is ready to serve as the phase's closing evidence artifact.

## Self-Check: PASSED

`22.1-CI-EVIDENCE.md` confirmed present on disk with the new section (`grep -c 'Re-capture after BUG-04'` returns 1); commit `abfd1189` confirmed present in `git log --oneline`.

---
*Phase: 22.1-engine-readiness-defect-and-msrv-follow-up*
*Completed: 2026-09-03*
