# G-22-1 Closing Evidence — Whole-Workflow-Run Success

Phase: 22.1-engine-readiness-defect-and-msrv-follow-up
Requirement: ENG-05
Gap closed: G-22-1 (`.planning/phases/22-battlefield-state-superstep-engine/22-UAT.md`)

This document is the phase's evidence record (D-13), following the
`18-CODEQL-EVIDENCE.md` precedent: run id, job id, commit SHA, timestamps, and
log facts quoted verbatim rather than paraphrased. It records the evidence bar
D-12 restated: a `ci.yml` run whose **overall** conclusion is `success` — not a
green job inside a run that was superseded and cancelled — captured after every
other Phase 22.1 change (D-01…D-19) landed on the branch head.

## Selected run

- **Run id:** `33706948313`
- **URL:** https://github.com/DF3NDR/paladin-dev-env/actions/runs/33706948313
- **Workflow:** `ci.yml`
- **Trigger ref:** `feature/phase-22`
- **Head SHA:** `7c77b0e22246e8c1207386bb0548f1ede19ee4ee` — an ancestor-or-equal
  of the branch head containing the commits from plans 22.1-01
  (`4df90c6c`, `48050d17`, `b617aade`) through 22.1-04 (`5de05320`, `97818458`),
  confirmed via `git merge-base --is-ancestor 7c77b0e2... HEAD` (exit 0).
- **Overall conclusion:** `success`
- **Created:** 2026-09-03T02:15:29Z
- **Updated (completed):** 2026-09-03T03:30:34Z

## Why the previously-cited evidence did not meet the bar

Checkpoint 22-17 closed G-22-1 on a job (`Postgres Waypoint Contract Suite (live
server)`, `success`, id `100440861780`) inside run **`33688238662`** (commit
`2b2bc1d5`). That job was green, but the **run's own overall conclusion was
`cancelled`** — superseded by a later push under `ci.yml`'s `concurrency` group
(line 26) — and three required jobs inside that same run had already failed
before the supersession: `MSRV (Rust 1.85)`, `Security Audit`, and `License &
Dependency Policy`. A green job inside a run whose overall verdict is not
`success` is not evidence that the workflow, as a whole, passed on that commit;
D-12 restates the bar as a whole-run `success` for exactly this reason.

## Run selection (ordering)

Per D-14, this capture happens only after every other Phase 22.1 change is on the
branch head. At capture time, `origin/feature/phase-22` and this worktree's base
were both `7c77b0e22246e8c1207386bb0548f1ede19ee4ee` (containing the commits from
plans 22.1-01 through 22.1-04 plus the wave-tracking commits), so no push was
needed — CI had already been triggered against this head by the prior wave's
merge/push.

Two `ci.yml` runs exist on this head SHA (`gh run list --workflow ci.yml --branch
feature/phase-22 --json databaseId,headSha,createdAt,conclusion`):

| run_id | headSha | createdAt | conclusion |
|---|---|---|---|
| 33706948313 | `7c77b0e22246e8c1207386bb0548f1ede19ee4ee` | 2026-09-03T02:15:29Z | success |
| 33706946100 | `7c77b0e22246e8c1207386bb0548f1ede19ee4ee` | 2026-09-03T02:15:27Z | success |

Both completed with overall conclusion `success` — the `ci.yml` concurrency
group's cancellation did not fire between this pair (they were triggered by
different event types racing within 2 seconds; neither superseded the other).
The two `createdAt` values do **not** tie (2-second difference), so per the
ordering rule exactly one run is named: **`33706948313`**, the later of the two
by `createdAt`. Both runs having reached `success` independently is a stronger
signal than either alone, but only the selected run's job/log detail is quoted
below, per the plan's single-run naming rule.

## Per-job conclusion (at least the four named jobs)

| Job | conclusion | job id |
|---|---|---|
| Postgres Waypoint Contract Suite (live server) | success | `100497999877` |
| MSRV (Rust 1.88) | success | `100497999687` |
| Security Audit | success | `100497999723` |
| License & Dependency Policy | success | `100497999693` |

(All 30 non-skipped jobs in this run are `success`; `End-to-End Tests` and
`Publish Dry Run` are `skipped` by their own conditional triggers, unaffected by
this evidence.)

## The four required Postgres log facts (quoted verbatim, `gh run view --job 100497999877 --log`)

**(a) Container health** — step `Start postgres-test and wait for healthy`
(`conclusion: success`, started `2026-09-03T02:30:17Z`, completed
`2026-09-03T02:30:33Z`), the anchored `-qx healthy` wait loop's own runtime echo:

```
2026-09-03T02:30:26.9393451Z Waiting for postgres-test to report healthy...
```

The step's own recorded `conclusion` is `success` — the `timeout 180 sh -c
'until ... grep -qx healthy; do sleep 3; done'` loop exited 0 before the 180s
timeout, i.e. Docker reported the container's health status as exactly
`healthy` (anchored, not merely containing "healthy" — `unhealthy` would not
match `-x`).

**(b) `pg_isready` accepting connections:**

```
2026-09-03T02:30:33.1064924Z /var/run/postgresql:5432 - accepting connections
```

**(c) Test result line, with the declared-count check quoted alongside it:**

```
2026-09-03T02:31:26.6236442Z test result: ok. 26 passed; 0 failed; 0 ignored; 0 measured; 39 filtered out; finished in 8.49s
```

```
2026-09-03T02:31:26.6849303Z Declared tests: 26, passed: 26
```

The job's own "Fail if the suite selected fewer tests than it declares" step
(`conclusion: success`) computes `EXPECTED` from
`grep -cE '^\s*#\[tokio::test\]' crates/paladin-storage/src/waypoint/postgres.rs`
against the exact commit this run tested. Independently re-run against this
worktree's copy of the same file at the same head SHA:

```
$ grep -cE '^\s*#\[tokio::test\]' crates/paladin-storage/src/waypoint/postgres.rs
26
```

26 (declared) == 26 (passed) == 26 (independently recomputed) — the equality is
auditable, not asserted.

**(d) Skip-detection line** — step `Fail if the suite took the SKIP path`
(`conclusion: success`):

```
2026-09-03T02:31:26.6682018Z All waypoint::postgres tests exercised the live server.
```

Its presence (rather than the step's `::error::` branch, which prints
`"waypoint::postgres took the SKIP path — the live server was NOT exercised."`
and would have failed the step) means the env-gated suite did not take its SKIP
path: `WAYPOINT_POSTGRES_TEST_URL` was set and every test ran against the live
Postgres container this job started.

## What this evidence proves, and what it does not

**Proves:** the `waypoint::postgres` contract suite (26 of 26 declared tests)
executed against a live Postgres server, inside a `ci.yml` run whose *overall*
conclusion was `success`, on a commit containing every other Phase 22.1 change
(the readiness fix and its typed guards, the fingerprint completeness fix, and
the MSRV floor raise to 1.88 that this run's own green `MSRV (Rust 1.88)` job
confirms). The three jobs that failed in the previously-cited run — MSRV,
Security Audit, License & Dependency Policy — are all `success` here, on the
same commit.

**Does not prove:** anything about runs on any other commit, or about this
commit's behavior under conditions this suite does not exercise (e.g.
concurrent writers, network partition between the runner and the container).
G-22-1's truth is scoped to "passes the shared Tier 2 contract suite against a
real Postgres server, identically to SQLite/InMemory" — this evidence is scoped
identically.

## No ruleset or workflow edit

`git status --porcelain` during this capture shows no change under `.github/` —
no ruleset or workflow file was edited to produce this evidence, per ADR-0044
(`postgres-integration` and `msrv` are already in the required-check set).

## Re-capture after BUG-04 (2026-09-03)

Plan 22.1-05's checkpoint promoted BUG-04 into Phase 22.1's scope, which moved the phase's final
commit past the head the capture above recorded (`7c77b0e2`, run `33706948313`). D-14's rule —
the evidence must sit on the head the phase ships — applies to the new head exactly as it applied
to the old one, so D-25 requires the same bar re-met here. The capture above is **not superseded**
as G-22-1's closing evidence; it is supplemented. G-22-1's truth was proven on the head that closed
it, and this section proves the same truth again on the head that now closes the phase, after
Plan 22.1-06 fixed BUG-04 (resume dropping pre-crash frontier state) and added two shared contract
clauses to the `waypoint::postgres` suite.

### Selected run

- **Run id:** `33758221386`
- **URL:** https://github.com/DF3NDR/paladin-dev-env/actions/runs/33758221386
- **Workflow:** `ci.yml`
- **Trigger ref:** `feature/phase-22`
- **Head SHA:** `4885380cf46a903a57423e48300404a2ed6e2445` — the branch head at capture time
  (`origin/feature/phase-22` == this worktree's base), containing Plan 22.1-06's fix commits.
  Confirmed by two commands run in this worktree:

  ```
  $ git merge-base --is-ancestor 4885380cf46a903a57423e48300404a2ed6e2445 HEAD; echo $?
  0
  $ git log --oneline 4885380cf46a903a57423e48300404a2ed6e2445 | grep -c 'fix(22.1-06)'
  2
  ```

  (The count is 2 because Plan 22.1-06 committed two genuine `fix(22.1-06)` commits, not one:
  `9cf69ee0` — restore the Frontier on resume from a persisted snapshot (the BUG-04 fix itself) —
  and `acc07c5e` — the post-Task-3 mechanical fix updating `engine_benchmarks.rs`'s
  `Waypoint::new_root` call site for the new `frontier` parameter, discovered only when the plan's
  own full `cargo clippy --workspace --all-targets --all-features -- -D warnings` was run, per
  22.1-06-SUMMARY.md's Task Commits list. Both matches confirm the fix is present.
  `git log --oneline 4885380cf46a903a57423e48300404a2ed6e2445 | grep -c 'test(22.1-06)'` likewise
  returns 2, matching `c4dc9859` (the RED reproduction test) and `7325451a` (the frontier
  round-trip contract-clause tests).)
- **Overall conclusion:** `success`
- **Created:** 2026-09-03T12:57:16Z
- **Updated (completed):** 2026-09-03T14:30:37Z

### Run selection (ordering)

Two `ci.yml` runs exist on this head SHA (`gh run list --workflow ci.yml --branch
feature/phase-22 --json databaseId,headSha,createdAt,conclusion`), both triggered by the
orchestrator's push at 2026-09-03T12:57Z:

| run_id | headSha | createdAt | conclusion |
|---|---|---|---|
| 33758221386 | `4885380cf46a903a57423e48300404a2ed6e2445` | 2026-09-03T12:57:16Z | success |
| 33758215860 | `4885380cf46a903a57423e48300404a2ed6e2445` | 2026-09-03T12:57:12Z | success |

The two `createdAt` values do **not** tie (4-second difference), so per the ordering rule exactly
one run is named: **`33758221386`**, the later of the two by `createdAt`. Both runs independently
reached overall conclusion `success`; only the selected run's job/log detail is quoted below, per
the plan's single-run naming rule.

### Per-job conclusion

| Job | conclusion | job id |
|---|---|---|
| Postgres Waypoint Contract Suite (live server) | success | `100657744879` |
| MSRV (Rust 1.88) | success | `100657745143` |
| Security Audit | success | `100657744854` |
| License & Dependency Policy | success | `100657744522` |

(All 31 non-skipped jobs in this run are `success`, out of 33 total; `Publish Dry Run` and
`End-to-End Tests` are `skipped` by their own conditional triggers, unaffected by this evidence —
the same two jobs, and only those two, were skipped in the first capture.)

### The four required Postgres log facts (quoted verbatim, `gh run view --job 100657744879 --log`)

**(a) Container health** — step `Start postgres-test and wait for healthy`
(`conclusion: success`, started `2026-09-03T13:13:24Z`, completed
`2026-09-03T13:13:37Z`), the anchored `-qx healthy` wait loop's own runtime echo:

```
2026-09-03T13:13:30.9545979Z Waiting for postgres-test to report healthy...
```

The step's own recorded `conclusion` is `success` — the `timeout 180 sh -c
'until ... grep -qx healthy; do sleep 3; done'` loop exited 0 before the 180s
timeout, i.e. Docker reported the container's health status as exactly
`healthy` (anchored, not merely containing "healthy" — `unhealthy` would not
match `-x`).

**(b) `pg_isready` accepting connections:**

```
2026-09-03T13:13:37.0770480Z /var/run/postgresql:5432 - accepting connections
```

**(c) Test result line, with the declared-count check quoted alongside it:**

```
2026-09-03T13:14:31.9793807Z test result: ok. 28 passed; 0 failed; 0 ignored; 0 measured; 41 filtered out; finished in 8.73s
```

```
2026-09-03T13:14:32.0378345Z Declared tests: 28, passed: 28
```

The job's own "Fail if the suite selected fewer tests than it declares" step
(`conclusion: success`, `100657744879`) computes `EXPECTED` from
`grep -cE '^\s*#\[tokio::test\]' crates/paladin-storage/src/waypoint/postgres.rs`
against the exact commit this run tested. Independently re-run against this
worktree's copy of the same file at the same head SHA:

```
$ grep -cE '^\s*#\[tokio::test\]' crates/paladin-storage/src/waypoint/postgres.rs
28
```

28 (declared) == 28 (passed) == 28 (independently recomputed) — the equality is
auditable, not asserted. The declared count rose from the first capture's 26 to 28 because
Plan 22.1-06 added two shared contract clauses
(`frontier_survives_save_latest_and_get_round_trip`,
`pre_bug_04_payload_without_frontier_loads_with_an_empty_snapshot`) to the `WaypointPort` contract
suite, each wired with its own `#[tokio::test]` wrapper in `postgres.rs`. The increase itself is
the proof those two clauses executed against the live server here — they cannot execute anywhere
else, since the shared suite runs identically against `InMemory`/`SQLite`/`Postgres` and only the
Postgres backend takes this job's live-server path rather than the local SKIP path.

**(d) Skip-detection line** — step `Fail if the suite took the SKIP path`
(`conclusion: success`, `100657744879`):

```
2026-09-03T13:14:32.0193306Z All waypoint::postgres tests exercised the live server.
```

Its presence (rather than the step's `::error::` branch, which prints
`"waypoint::postgres took the SKIP path — the live server was NOT exercised."`
and would have failed the step) means the env-gated suite did not take its SKIP
path: `WAYPOINT_POSTGRES_TEST_URL` was set and every test, including the two new contract clauses,
ran against the live Postgres container this job started.

### D-20 program-gate confirmation on the phase's final commit

| Program gate | Job (this run) | Conclusion |
|---|---|---|
| MSRV floor (Rust 1.88) | `MSRV (Rust 1.88)` (`100657745143`) | success |
| `cargo semver-checks` vs 0.9.0 | `Semver Checks (vs v0.9.0)` (`100657745015`) | success |
| API surface tracking | `API Surface Tracking` (`100657744804`) | success |
| Security Audit | `Security Audit` (`100657744854`) | success |
| License & Dependency Policy | `License & Dependency Policy` (`100657744522`) | success |

Per D-25, this run is the evidence for D-20's requirement that the program gates are green on the
phase's final commit — all five gates above are `success` in run `33758221386`, on head
`4885380cf46a903a57423e48300404a2ed6e2445`.

### What this evidence proves, and what it does not

**Proves:** the `waypoint::postgres` contract suite (28 of 28 declared tests, including the two
new frontier round-trip clauses Plan 22.1-06 added) executed against a live Postgres server,
inside a `ci.yml` run whose *overall* conclusion was `success`, on a commit containing every other
Phase 22.1 change through Plan 22.1-06 — the BUG-04 resume-frontier fix, the readiness fix and its
typed guards, the fingerprint completeness fix, and the MSRV floor raise to 1.88 that this run's
own green `MSRV (Rust 1.88)` job confirms again. The five D-20 program gates (MSRV, semver-checks,
api-surface, Security Audit, License & Dependency Policy) are all `success` here, on the same
commit.

**Does not prove:** anything about runs on any other commit, or about this commit's behavior under
conditions this suite does not exercise (e.g. concurrent writers, network partition between the
runner and the container). G-22-1's truth is scoped to "passes the shared Tier 2 contract suite
against a real Postgres server, identically to SQLite/InMemory" — this evidence is scoped
identically, now including the two BUG-04-motivated frontier clauses.

### No ruleset or workflow edit

`git status --porcelain` during this capture is empty — no change anywhere in the worktree,
including nothing under `.github/`. No ruleset or workflow file was edited to produce this
evidence, per ADR-0044 (`postgres-integration` and `msrv` are already in the required-check set).
