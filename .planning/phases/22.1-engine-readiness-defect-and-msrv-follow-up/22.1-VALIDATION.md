---
phase: 22.1
slug: engine-readiness-defect-and-msrv-follow-up
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: validated
nyquist_compliant: true
wave_0_complete: true
created: 2026-09-02
validated: 2026-09-04
---

# Phase 22.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Seeded by plan-phase from `22.1-RESEARCH.md` §Validation Architecture; the planner assigned task IDs
> and `/gsd-validate-phase` filled status on 2026-09-04 (see Validation Audit below).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `cargo test` (workspace default); `#[tokio::test]` for async engine tests; inline `#[cfg(test)] mod tests` in `superstep.rs` / `graph.rs` / `mod.rs` / `waypoint.rs`; shared `WaypointPort` contract clauses in `crates/paladin-storage/src/waypoint/contract_tests.rs` wrapped once per backend |
| **Config file** | none dedicated — `Cargo.toml` `[dev-dependencies]` already wired |
| **Quick run command** | `cargo test -p paladin-battalion --lib engine` |
| **Full suite command** | `cargo test --workspace` then `cargo fmt --check` and `cargo clippy --workspace --all-targets --all-features -- -D warnings` |
| **Estimated runtime** | ~60 seconds quick (warm cache); ~5–8 minutes full workspace (warm cache) |

---

## Sampling Rate

- **After every task commit:** Run `cargo test -p paladin-battalion --lib engine`
- **After every plan wave:** Run `cargo test --workspace`, `cargo fmt --check`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`
- **Before `/gsd-verify-work`:** Full suite green; CI `msrv` job green at the 1.88 floor; `semver` job green vs 0.9.0; `make security` clean; `postgres-integration` job green inside an overall-`success` run (D-12)
- **Max feedback latency:** 90 seconds (quick command)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 22.1-01 T1 | 22.1-01 | 1 | ENG-02 | T-22.1-01 | A cycle node fed from outside the cycle executes at least once before `Completed` (BUG-03) | unit, RED-first (`self_looping_node_fed_by_upstream_edge_can_never_take_first_turn` un-ignored, D-05(i); `cycle_node_fed_from_outside_the_cycle_takes_its_first_turn`, D-05(ii)) + join-semantics canaries (`diamond_join_executes_target_exactly_once`, `false_branch_is_proven_not_firing_and_join_still_runs_once`) | `cargo test -p paladin-battalion --lib engine::superstep` | ✅ | ✅ green |
| 22.1-01 T2 | 22.1-01 | 1 | ENG-02 | — | N/A | doc/register grep (BUG-03 / ENG-FR-06a in the `.project/v0.10.0` trio; absent from MIGRATION.md / CHANGELOG.md) | `grep -c 'BUG-03' .project/v0.10.0/00-program-overview.md .project/v0.10.0/08-traceability-matrix.md && grep -c 'ENG-FR-06a' .project/v0.10.0/01-battlefield-state-and-execution-engine.md` | ✅ | ✅ green |
| 22.1-02 T1 | 22.1-02 | 2 | ENG-02 | — | An externally-unfed cycle is rejected at `validate()` with `EngineError::UnschedulableCycle` naming every node, ordered after the eligible-set clause (D-03) | unit (`unschedulable_unfed_nodes_*` ×3, `validate_prefers_unreachable_node_error_over_unschedulable_cycle`) | `cargo test -p paladin-battalion --lib engine::graph` | ✅ | ✅ green |
| 22.1-02 T2 | 22.1-02 | 2 | ENG-02 | — | `run()` cannot report `Completed` over a non-dead node holding an unconsumed fired edge — `EngineError::StarvedNodeAtCompletion` at both decision sites (D-04); starved defer-marked aggregator released (tier 4) | unit (`completion_check_names_every_node_holding_an_unconsumed_fired_edge`, `deferred_aggregator_starved_by_a_cycle_is_still_released`) + regression (E2E-1) | `cargo test -p paladin-battalion --lib engine::superstep && cargo test --test e2e_crash_resume` | ✅ | ✅ green |
| 22.1-02 T3 | 22.1-02 | 2 | ENG-02 (ENG-FR-08) | — | N/A | unit (20-iteration determinism over both cycle-bootstrap shapes) | `cargo test -p paladin-battalion --lib engine::superstep::tests::eng_fr_08_determinism_over_twenty_randomized_iterations_for_cycle_bootstrap_shapes` | ✅ | ✅ green |
| 22.1-03 T1 | 22.1-03 | 3 | ENG-02 (ENG-FR-14) | — | Resume cannot silently proceed on a scheduling-relevant graph change: entry set, `defer_flags`, `dynamic_targets`, edge condition, `DispatchRule`, Paladin `output_field` each move the fingerprint; prompt/model/`InputMapping`/`EngineLimits` provably do not (D-15/D-16) | unit (six `fingerprint_changes_when_*` difference tests + `fingerprint_is_unchanged_by_prompt_model_input_mapping_and_limits`) | `cargo test -p paladin-battalion --lib engine::graph` | ✅ | ✅ green |
| 22.1-03 T2 | 22.1-03 | 3 | ENG-02 (ENG-FR-14) | — | A canonicalization change fails CI visibly | unit (golden hex `fingerprint_golden_hex_pins_canonical_bytes`, D-17) | `cargo test -p paladin-battalion --lib engine::graph::tests::fingerprint` | ✅ | ✅ green |
| 22.1-03 T3 | 22.1-03 | 3 | ENG-02 | — | N/A | doc (D-18 resume rustdoc, D-05(iii) fixture-comment sweep; comment-only diff) + regression (E2E-1 unmodified, `cargo doc`) | `cargo test -p paladin-battalion && cargo test --test e2e_crash_resume && cargo doc -p paladin-battalion --no-deps` | ✅ | ✅ green |
| 22.1-04 T1+T2 | 22.1-04 | 1 | ENG-05 | T-22.1-SC | No new security exception; RUSTSEC-2026-0009 cleared by the lockfile restore (`time >= 0.3.47`), not allowlisted | toolchain + security (`cargo +1.88 check --locked`, `make security`, `scripts/check-advisory-register.sh`) + grep (every MSRV figure reads 1.88; `rmcp = "=2.1.0"` pin unchanged); guarded continuously by the CI `msrv` job | `cargo +1.88 check --workspace --all-features --all-targets --locked && make security && ./scripts/check-advisory-register.sh` then `grep -c 'dtolnay/rust-toolchain@1.88' .github/workflows/ci.yml && grep -c 'rust-1.88%2B' README.md` | ✅ | ✅ green |
| 22.1-05 T1 | 22.1-05 | 4 | ENG-05 (G-22-1) | T-22.1-16 | A gap is closed only on a run whose OVERALL conclusion is `success` (D-12), never a green job inside a cancelled/failed run | integration (CI, read via `gh`): run 33706948313 on head 7c77b0e2, run-level then job-level, four Postgres log facts at 26 declared/26 passed | `RUN=$(grep -oE '[0-9]{8,}' 22.1-CI-EVIDENCE.md \| head -1) && gh run view "$RUN" --json conclusion --jq .conclusion \| grep -qx success && gh run view "$RUN" --json jobs --jq '[.jobs[] \| select(.name \| test("Postgres\|MSRV\|Security Audit\|License")) \| .conclusion] \| unique \| .[]' \| grep -qx success` | ✅ | ✅ green |
| 22.1-05 T2 | 22.1-05 | 4 | ENG-05 (G-22-1) | — | N/A | grep (22-UAT.md G-22-1 resolution points at the evidence; original 22-17 text preserved) | `grep -c '22.1-CI-EVIDENCE.md' .planning/phases/22-battlefield-state-superstep-engine/22-UAT.md` | ✅ | ✅ green |
| 22.1-05 T3 | 22.1-05 | 4 | ENG-05 (G-22-1) | T-22.1-16 | A human confirms the closing evidence meets its own bar before G-22-1 is sealed | `checkpoint:human-verify` (gate `blocking`) — see Manual-Only Verifications | — (approved by the developer 2026-09-03; recorded in 22.1-05-SUMMARY.md D4 and 22.1-UAT.md test 1) | n/a | ✅ approved |
| 22.1-06 T1 | 22.1-06 | 5 | ENG-04 | T-22.1-20, T-22.1-21 | A resumed run cannot report `Completed` over a node whose only fired incoming edge fired before the interruption; restored edge state is keyed by `(from, to, condition)` identity, never by index (BUG-04) | unit, RED-first (`resume_restores_pre_crash_edge_resolutions_and_executes_the_pending_join`, `resume_after_a_join_shape_crash_matches_the_control_run_superstep_for_superstep`, `resume_with_allow_graph_change_drops_unknown_snapshot_edges_and_starts_new_edges_pending`) + core (`frontier_snapshot_edges_serialize_in_sorted_identity_order`, `waypoint_payload_without_frontier_field_deserializes_with_an_empty_snapshot`) + regression (E2E-1, determinism harness) | `cargo test -p paladin-battalion --lib engine && cargo test -p paladin-ai-core --lib platform::container::waypoint && cargo test --test e2e_crash_resume` | ✅ | ✅ green |
| 22.1-06 T2 | 22.1-06 | 5 | ENG-04 | T-22.1-22 | A Waypoint written before the `frontier` field existed still loads; the new payload survives all three backends (D-23, additive `#[serde(default)]`) | contract suite (`frontier_survives_save_latest_and_get_round_trip`, `pre_bug_04_payload_without_frontier_loads_with_an_empty_snapshot`): InMemory + SQLite executed locally; Postgres compiled and selected locally (`SKIP` path, no Docker), executed live in CI run 33758221386 at 28 declared/28 passed | `cargo test -p paladin-storage --features sqlite,postgres --lib waypoint::` | ✅ | ✅ green |
| 22.1-06 T3 | 22.1-06 | 5 | ENG-04 | — | N/A | doc/register grep (BUG-04 / ENG-FR-12a) + API-surface baseline | `grep -c 'BUG-04' .project/v0.10.0/00-program-overview.md .project/v0.10.0/08-traceability-matrix.md && grep -c 'ENG-FR-12a' .project/v0.10.0/01-battlefield-state-and-execution-engine.md && ./scripts/check-api-surface.sh .project/current-exports.txt` | ✅ | ✅ green |
| 22.1-07 T1 | 22.1-07 | 6 | ENG-05 (D-20, D-25) | T-22.1-25, T-22.1-26 | Evidence sits on the head that carries the BUG-04 fix; run-level `success` is resolved BEFORE job-level detail is read; declared-vs-passed equality at the new count proves the two new Postgres clauses ran live | integration (CI, read via `gh`): run 33758221386 on head 4885380c, four named jobs + five D-20 program gates all `success` | `gh run view 33758221386 --json conclusion --jq .conclusion \| grep -qx success && gh run view 33758221386 --json jobs --jq '[.jobs[] \| select(.name \| test("Postgres Waypoint Contract Suite\|MSRV\|Security Audit\|License & Dependency Policy")) \| .conclusion] \| unique \| .[]' \| grep -qx success` | ✅ | ✅ green |
| 22.1-07 T2 | 22.1-07 | 6 | ENG-05 (D-25) | — | A skipped suite cannot present as an executed one; the re-capture is append-only | grep (four Postgres log facts, `Re-capture after BUG-04`, `D-20 program-gate confirmation`, original run id preserved) | `EV=22.1-CI-EVIDENCE.md; test -s "$EV" && grep -q 'Re-capture after BUG-04' "$EV" && grep -q '33706948313' "$EV" && grep -qE 'test result: ok\. [0-9]+ passed; 0 failed' "$EV" && grep -q 'exercised the live server' "$EV" && grep -q 'accepting connections' "$EV" && grep -q 'D-20 program-gate confirmation' "$EV"` | ✅ | ✅ green |
| 22.1-REVIEW-FIX CR-01 | post-phase (22.1-REVIEW.md) | — | ENG-02 (ENG-FR-14) | — | Two distinct graphs can never share a fingerprint via a delimiter split: length-prefixed canonical encoding, `GRAPH_FINGERPRINT_VERSION` bumped `v1` → `v2` so stored fingerprints mismatch on resume rather than being reinterpreted | unit (`fingerprint_distinguishes_length_prefix_collision_two_nodes_vs_one`, `fingerprint_distinguishes_length_prefix_collision_edge_split`; golden literals re-pinned) | `cargo test -p paladin-battalion --lib engine::graph::tests::fingerprint` | ✅ | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

**Post-review non-gap, recorded for completeness:** 22.1-REVIEW.md WR-01 replaced a `.expect()` on the
superstep semaphore acquire with a `match` that routes `Err` through `NodeRunOutcome::Failed`. The
error arm has no dedicated test because the semaphore is constructed locally and never exposed for
closing, so the path is unreachable through any public API; the fix removes the latent panic, and
existing `engine::superstep` tests cover the unchanged `Ok` arm. Not a validation gap.

---

## Wave 0 Requirements

- [x] `crates/paladin-battalion/src/engine/superstep.rs` — RED test for the general cycle-bootstrap case (D-05(ii)); the existing repro un-ignored with no assertion changes (D-05(i)) — landed `e4400ee8` (red) → `a64e5b0d` (green)
- [x] `crates/paladin-battalion/src/engine/graph.rs` — golden fingerprint hex test plus six difference-case tests (D-17) — landed `5789aaf6`; golden re-pinned to the `v2` encoding in `02eaedf1` (REVIEW-FIX CR-01)
- [x] `.planning/phases/22.1-engine-readiness-defect-and-msrv-follow-up/22.1-CI-EVIDENCE.md` — evidence artifact (D-13), authored `dd26ee7a`; re-captured on the post-BUG-04 head as a dated appended section (D-25) in `abfd1189`
- [x] `crates/paladin-battalion/src/engine/mod.rs` — RED reproduction of the BUG-04 resume divergence (D-24), the conditional `c -> d` variant of the D-24 join shape — landed `c4dc9859` (red, observed `left: 0, right: 1`) → `9cf69ee0` (green)
- [x] `crates/paladin-storage/src/waypoint/contract_tests.rs` — the frontier round-trip and pre-BUG-04 compatibility clauses (D-23), wrapped once per backend — landed `7325451a`
- Framework install: none — `cargo test` / `tokio::test` already fully wired

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| A human confirms G-22-1's closing evidence meets its own bar before the gap is sealed (22.1-05 T3, `checkpoint:human-verify`, gate `blocking`) | ENG-05 (G-22-1) | The plan deliberately requires a human judgment on top of the automated run-level/job-level `gh` checks (T-22.1-16: repudiation of a gap closed on evidence that does not meet its bar). The automated checks in rows 22.1-05 T1 and 22.1-07 T1 above are the primary verification; this row is the governance sign-off, not a substitute for them. **Approved by the developer on 2026-09-03** (22.1-05-SUMMARY.md D4; 22.1-UAT.md test 1). | Re-run only if the evidence is re-captured on a new head: `gh run view <run> --json conclusion,headSha`, `gh run view <run> --json jobs`, then `gh run view --job <postgres job id> --log` and quote container healthy, `pg_isready` accepting connections, `test result: ok. N passed; 0 failed` with N equal to the job's `Declared tests:` line, and the skip-detection line. |

Note on the Postgres contract clauses (22.1-06 T2): they are automated, not manual — the `SKIP` path
runs locally because this devcontainer has no Docker, and the live execution is proven by the
`postgres-integration` CI job, whose result is read by the automated `gh` commands above.

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies (the single `checkpoint:human-verify` task, 22.1-05 T3, is a governance gate layered over automated checks and is approved)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify (only 22.1-05 T3 lacks one)
- [x] Wave 0 covers all MISSING references (all five Wave 0 items landed; no reference remains missing)
- [x] No watch-mode flags
- [x] Feedback latency < 90s (`cargo test -p paladin-battalion --lib engine` completes in well under a minute on a warm cache)
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-09-04

---

## Validation Audit 2026-09-04

| Metric | Count |
|--------|-------|
| Requirements audited | 3 (ENG-02, ENG-04, ENG-05) |
| Tasks mapped | 18 (17 plan tasks across 7 plans + 1 post-review fix) |
| Gaps found | 0 |
| Resolved | 0 |
| Escalated | 0 |
| Manual-only | 1 (blocking human checkpoint, approved 2026-09-03) |

**Method.** Every task's `<automated>` verify block from the seven PLAN files was cross-referenced
against the SUMMARY `coverage:` blocks, 22.1-VERIFICATION.md (18/18 must-haves), 22.1-UAT.md (29/29),
and 22.1-REVIEW-FIX.md. Each named test function was confirmed present in the tree by `grep`, no
`#[ignore]` remains under `crates/paladin-battalion/src/engine/`, and the suites were re-run on the
audited HEAD (`a47c7670`):

| Suite | Command | Result |
|-------|---------|--------|
| Engine (superstep, graph, mod) | `cargo test -p paladin-battalion --lib engine` | 236 passed, 0 failed |
| Core Waypoint / FrontierSnapshot | `cargo test -p paladin-ai-core --lib waypoint` | 29 passed, 0 failed |
| WaypointPort contract, InMemory | `cargo test -p paladin-storage --lib waypoint::in_memory` | 30 passed, 0 failed |
| WaypointPort contract, SQLite | `cargo test -p paladin-storage --lib --features sqlite waypoint::sqlite` | 34 passed, 0 failed |
| WaypointPort contract, Postgres (SKIP path, no Docker) | `cargo test -p paladin-storage --lib --features postgres waypoint::postgres` | 32 passed, 0 failed |
| E2E-1 crash/resume | `cargo test --test e2e_crash_resume` | 27 passed, 0 failed |

Counts are higher than the figures recorded at phase verification on 2026-09-03 (engine 128, core
waypoint 19, InMemory 26, SQLite 30, Postgres 28) because Phase 23 has since landed further tests on
the same branch; every Phase 22.1 test named in the map above is among those passing.

The grep-based verifies (register trio, MSRV figures, 22-UAT.md pointer, evidence-file facts) were
re-run and pass. Both evidence runs were re-confirmed live: `gh run view 33706948313` → `success` on
`7c77b0e2`; `gh run view 33758221386` → `success` on `4885380c` with the four named jobs `success`,
and `4885380c` is an ancestor of HEAD.

**Verdict:** Nyquist-compliant. All three requirements have automated verification at every task;
no test generation was required and no auditor was spawned.
