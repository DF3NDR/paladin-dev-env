---
phase: 22.1
slug: engine-readiness-defect-and-msrv-follow-up
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-09-02
---

# Phase 22.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Seeded by plan-phase from `22.1-RESEARCH.md` §Validation Architecture; the planner assigns task IDs
> and `/gsd-validate-phase` fills status.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `cargo test` (workspace default); `#[tokio::test]` for async engine tests; inline `#[cfg(test)] mod tests` in `superstep.rs` / `graph.rs` / `mod.rs` |
| **Config file** | none dedicated — `Cargo.toml` `[dev-dependencies]` already wired |
| **Quick run command** | `cargo test -p paladin-battalion --lib engine::superstep engine::graph` |
| **Full suite command** | `cargo test --workspace` then `cargo fmt --check` and `cargo clippy --workspace --all-targets --all-features -- -D warnings` |
| **Estimated runtime** | ~60 seconds quick; ~5–8 minutes full workspace (warm cache) |

---

## Sampling Rate

- **After every task commit:** Run `cargo test -p paladin-battalion --lib engine::superstep engine::graph`
- **After every plan wave:** Run `cargo test --workspace`, `cargo fmt --check`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`
- **Before `/gsd-verify-work`:** Full suite green; CI `msrv` job green at the new 1.88 floor; `semver` job green vs 0.9.0; `make security` clean; `postgres-integration` job green inside an overall-`success` run (D-12)
- **Max feedback latency:** 90 seconds (quick command)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD (planner) | TBD | TBD | ENG-02 | — | N/A | unit (un-ignore existing repro, D-05(i)) | `cargo test -p paladin-battalion --lib engine::superstep self_looping_node_fed_by_upstream_edge_can_never_take_first_turn` | ✅ (currently `#[ignore]`d) | ⬜ pending |
| TBD (planner) | TBD | TBD | ENG-02 | — | N/A | unit, RED-first (general cycle bootstrap `entry -> a -> b -> a`, D-05(ii)) | `cargo test -p paladin-battalion --lib engine::superstep` | ❌ W0 | ⬜ pending |
| TBD (planner) | TBD | TBD | ENG-02 | — | N/A | regression (diamond join, not-firing branch, defer release, determinism ≥20 iterations, E2E-1 golden) | `cargo test -p paladin-battalion --lib engine::superstep` and `cargo test --test e2e_crash_resume` | ✅ | ⬜ pending |
| TBD (planner) | TBD | TBD | ENG-02 | — | N/A | unit (typed starvation error, D-03 guard / D-04 check) | `cargo test -p paladin-battalion --lib engine` | ❌ W0 | ⬜ pending |
| TBD (planner) | TBD | TBD | ENG-02 (ENG-FR-14) | — | Resume cannot silently proceed on a scheduling-relevant graph change | unit (golden fingerprint hex + 6 difference cases, D-17) | `cargo test -p paladin-battalion --lib engine::graph` | ❌ W0 | ⬜ pending |
| TBD (planner) | TBD | TBD | ENG-05 | — | No new security exception; RUSTSEC-2026-0009 cleared by lockfile restore | CI + local (`cargo audit`, `cargo deny check`, `cargo +1.88 check --workspace --all-features --all-targets --locked`) | `make security` | ✅ | ⬜ pending |
| TBD (planner) | TBD | TBD | ENG-05 | — | N/A | integration (CI): whole-run `success` with Postgres job `success` and the four log facts | `gh run view <run> --json conclusion,jobs` and `gh run view --job <id> --log` | ✅ (job exists) | ⬜ pending |
| 22.1-06 T1 | 22.1-06 | 5 | ENG-04 | T-22.1-20, T-22.1-21 | A resumed run cannot report Completed over a node whose only fired incoming edge fired before the interruption; restored edge state is keyed by identity, never by index | unit, RED-first (D-24 join shape, conditional `c -> d` variant) + regression (E2E-1, determinism harness) | `cargo test -p paladin-battalion --lib engine && cargo test --test e2e_crash_resume` | ❌ W0 | ⬜ pending |
| 22.1-06 T2 | 22.1-06 | 5 | ENG-04 | T-22.1-22 | A Waypoint written before the field existed still loads; the new payload survives all three backends | contract suite (InMemory + SQLite local; Postgres live in CI) | `cargo test -p paladin-storage --lib waypoint::` and `cargo test -p paladin-storage --features postgres --lib waypoint::postgres` | ❌ W0 | ⬜ pending |
| 22.1-06 T3 | 22.1-06 | 5 | ENG-04 | — | N/A | doc/register + API-surface baseline | `./scripts/check-api-surface.sh .project/current-exports.txt` | ✅ | ⬜ pending |
| 22.1-07 T1/T2 | 22.1-07 | 6 | ENG-05 | T-22.1-25, T-22.1-26 | Evidence sits on the head that carries the BUG-04 fix; a skipped suite cannot present as an executed one | integration (CI): whole-run `success`, four log facts, declared-vs-passed equality at the new count | `gh run view <run> --json conclusion,jobs` and `gh run view --job <id> --log` | ✅ (job exists) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `crates/paladin-battalion/src/engine/superstep.rs` — RED test for the general cycle-bootstrap case (D-05(ii)); the existing repro is un-ignored with no assertion changes (D-05(i))
- [ ] `crates/paladin-battalion/src/engine/graph.rs` — golden fingerprint hex test plus six difference-case tests (D-17)
- [ ] `.planning/phases/22.1-engine-readiness-defect-and-msrv-follow-up/22.1-CI-EVIDENCE.md` — evidence artifact (D-13), authored in the final wave; re-captured on the post-BUG-04 head as a dated appended section (D-25, plan 22.1-07)
- [ ] `crates/paladin-battalion/src/engine/mod.rs` — RED reproduction of the BUG-04 resume divergence (D-24), the conditional `c -> d` variant of the D-24 join shape
- [ ] `crates/paladin-storage/src/waypoint/contract_tests.rs` — the frontier round-trip and pre-BUG-04 compatibility clauses (D-23), wrapped once per backend
- Framework install: none — `cargo test` / `tokio::test` already fully wired

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Whole-workflow-run `success` on the post-MSRV branch head with the four Postgres log facts | ENG-05 (G-22-1) | Executes only in GitHub Actions; cannot run in the devcontainer (no docker-in-docker) | `gh run list --workflow ci.yml --branch feature/phase-22 --limit 3`, then `gh run view <run> --json conclusion,jobs`, then `gh run view --job <postgres job id> --log` and quote: container healthy, `pg_isready` accepting connections, `test result: ok. N passed; 0 failed`, skip-detection line |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 90s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
