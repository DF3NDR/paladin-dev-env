---
phase: 22.1-engine-readiness-defect-and-msrv-follow-up
verified: 2026-09-03T14:50:36Z
status: passed
score: 18/18 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 22.1: Engine readiness defect and MSRV follow-up Verification Report

**Phase Goal:** Close the items Phase 22's gap-closure checkpoint (22-17) could not settle
in-repo: (1) fix the frontier readiness defect (BUG-03) so a cycle-bootstrap node executes at
least once before `Completed`; (2) decide and enact the MSRV position (X-11.2 stop-and-flag,
raise the floor); (3) confirm a fully green `postgres-integration` CI run and record it as
G-22-1's closing evidence; (4) close 22-REVIEW.md CR-01 (`WarGraph::fingerprint()` coverage
gap); (5) fix BUG-04 — resume rebuilt the Frontier from scratch, losing pre-crash edge
resolutions — persist the frontier on the Waypoint, test-first, register as BUG-04 /
ENG-FR-12a, and re-capture CI evidence on the final head.

**Verified:** 2026-09-03T14:50:36Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | BUG-03: a cycle node fed from outside the cycle (self-loop and general `entry->a->b->a` shape) executes at least once before `Completed` | ✓ VERIFIED | `starved_release`/`starved_nodes` in `superstep.rs:1069-1131`, wired into `compute_next_vanguard`'s tier 2; both reproduction tests pass: `cargo test -p paladin-battalion --lib engine::superstep` → `self_looping_node_fed_by_upstream_edge_can_never_take_first_turn` and `cycle_node_fed_from_outside_the_cycle_takes_its_first_turn` both `ok`, neither carries `#[ignore]`. TDD order confirmed: `e4400ee8` (test, red) → `a64e5b0d` (fix, green). |
| 2 | Starvation release engages only when normal-ready AND defer-release passes are both empty; diamond joins still wait for every live incoming edge | ✓ VERIFIED | `compute_next_vanguard` (superstep.rs:1034-1067) tries normal-ready, then `starved_release`, then defer-ready, then `starved_deferred_release`, each gated on the prior being empty. `diamond_join_executes_target_exactly_once` and `deferred_aggregator_starved_by_a_cycle_is_still_released` pass. |
| 3 | No new persisted engine state introduced by the starvation release | ✓ VERIFIED | `starved_nodes` (superstep.rs:1109) reads only `frontier.edge_state`/`frontier.dead`/`frontier.last_executed`, all rebuilt within a run — rustdoc states this explicitly (D-03) and no new field was added to any persisted struct for BUG-03. |
| 4 | Truthful-outcome guard: a run about to report `Completed` while a non-dead eligible node holds an unconsumed fired edge fails with a typed `EngineError` instead | ✓ VERIFIED | `EngineError::StarvedNodeAtCompletion` (mod.rs:343), checked at both `Completed` decision sites in `superstep.rs:208,591`; `completion_check_names_every_node_holding_an_unconsumed_fired_edge` test passes. |
| 5 | A graph shape the starvation release still cannot schedule (unfed cycle, no runtime-entry marker) is rejected at `validate()` with a typed error naming every offending node | ✓ VERIFIED | `EngineError::UnschedulableCycle` (mod.rs:306), `WarGraph::unschedulable_unfed_nodes()`/`validate()` clause (graph.rs:418-500). Tests `unschedulable_unfed_nodes_names_every_node_of_an_externally_unfed_cycle`, `unschedulable_unfed_nodes_is_empty_for_a_cycle_fed_from_entry`, `unschedulable_unfed_nodes_exempts_runtime_entry_marked_nodes`, `validate_prefers_unreachable_node_error_over_unschedulable_cycle` all pass — ordering preserved. |
| 6 | A deferred aggregator starved by the same cycle-bootstrap mechanism is released too (D-02a) | ✓ VERIFIED | `starved_deferred_release`/tier 4 (superstep.rs:1117-1131); `deferred_aggregator_starved_by_a_cycle_is_still_released` test passes. |
| 7 | Determinism holds over new cycle-bootstrap shapes (20 randomized iterations, byte-identical Battlefields) | ✓ VERIFIED | `eng_fr_08_determinism_over_twenty_randomized_iterations_for_cycle_bootstrap_shapes` test passes (part of the 27/27 `engine::superstep` run). |
| 8 | E2E-1 (`tests/integration/e2e_crash_resume_test.rs`) passes without any assertion or fixture change | ✓ VERIFIED | `cargo test -p paladin-ai --test e2e_crash_resume` → `e2e_1_crash_resume_matches_control_run_with_no_reexecution ... ok`; git diff shows no plan touched this file except through the shared engine signature, and the test itself is untouched per plan scope. |
| 9 | `WarGraph::fingerprint()` changes when entry set, `defer_flags`, `dynamic_targets`, an edge condition, a `DispatchRule`, or a Paladin's `output_field` changes; unchanged by prompt/model/InputMapping/EngineLimits | ✓ VERIFIED | `graph.rs:548-618` hashes `entry_ids`, `defer_ids`, `dynamic_target_ids`, serde-canonical edge conditions/dispatch rules, and `output_field`. Tests `fingerprint_changes_when_entry_set_changes`, `..._deferred`, `..._dynamic_target`, `..._output_field_changes`, `..._dispatch_rule_changes`, `..._edge_condition_changes`, and `fingerprint_is_unchanged_by_prompt_model_input_mapping_and_limits` all pass (36/36 `engine::graph` tests green). |
| 10 | Canonical bytes contain no `Debug`-derived text; a golden test pins the exact fingerprint hex | ✓ VERIFIED | `canonical_edge_condition` (serde-based, imported from paladin-core) used at graph.rs:590 instead of `format!("{:?}", ...)`; `fingerprint_golden_hex_pins_canonical_bytes` test passes. |
| 11 | Fingerprint output independent of insertion/HashMap order | ✓ VERIFIED | `fingerprint_is_unchanged_by_insertion_order` test passes; all new collections (`entry_ids`, `defer_ids`, `dynamic_target_ids`) are sorted before hashing (graph.rs:560-568). |
| 12 | Resume-safety rustdoc reworded away from "structurally impossible"; `GraphMismatch`/`allow_graph_change` semantics unchanged | ✓ VERIFIED | `grep "structurally impossible"` in `engine/mod.rs` returns no matches; `resume_with_options` rustdoc (mod.rs:520-567) now states what the fingerprint check detects and cross-references the frontier restoration. `resume_allow_graph_change_*` tests pass unmodified in behavior. |
| 13 | Workspace MSRV floor declared once (`workspace.package.rust-version = "1.88"`), all ten crates inherit via `rust-version.workspace = true`; figure agrees everywhere (README, ci.yml, MIGRATION.md §9.3, CHANGELOG, mdBook) | ✓ VERIFIED | `Cargo.toml:18` declares `rust-version = "1.88"`; all 10 crate manifests use `rust-version.workspace = true`; `ci.yml` msrv job retargeted (`MSRV (Rust 1.88)`, `RUSTUP_TOOLCHAIN: "1.88"`, `dtolnay/rust-toolchain@1.88`, `msrv-1.88` cache key); README badge/prerequisites, MIGRATION.md §9.3, CHANGELOG.md, and all three mdBook pages all read 1.88 with no stray 1.85/1.93.1 remaining. |
| 14 | `resolver = "3"` set, changes nothing beyond the D-08 lockfile restore | ✓ VERIFIED | `Cargo.toml:10` `resolver = "3"`. |
| 15 | `make security` / `cargo audit` / `cargo deny` clean; RUSTSEC-2026-0009 cleared by upgrade, not a new exception; `scripts/check-advisory-register.sh` exits 0 | ✓ VERIFIED | Independently re-run: `cargo audit` shows 0 vulnerabilities (only pre-existing allowed warnings); `cargo deny check` → "advisories ok, bans ok, licenses ok, sources ok"; `scripts/check-advisory-register.sh` → "11 register row(s) checked ... all clauses satisfied"; `.cargo/audit.toml`'s ignore list has no RUSTSEC-2026-0009 entry. |
| 16 | G-22-1 closing evidence: a `ci.yml` run whose OVERALL conclusion is `success` (not a green job inside a cancelled/failed run), on a head containing plans 22.1-01..04, with the four Postgres log facts quoted verbatim and MSRV/Security Audit/License jobs green | ✓ VERIFIED | `22.1-CI-EVIDENCE.md` records run `33706948313` (head `7c77b0e2`); independently re-confirmed live via `gh run view` for the BUG-04 re-capture run `33758221386` (head `4885380c`) — `conclusion: success`, and the four named jobs (Postgres, MSRV, Security Audit, License & Dependency Policy) all `success`. `git merge-base --is-ancestor 4885380c... HEAD` exits 0. |
| 17 | BUG-04: resume restores the pre-crash `Frontier` via a persisted `FrontierSnapshot`, additive `#[serde(default)]` on `Waypoint`; survives round-trip on all three backends; pre-BUG-04 payloads still load with an empty snapshot | ✓ VERIFIED | `Waypoint::frontier: FrontierSnapshot` with `#[serde(default)]` (waypoint.rs:364-365); `resume_with_options` passes `Some(latest.frontier)` into `superstep::run`, which seeds `Frontier::for_run`/`from_snapshot`; every `build_waypoint` call site passes `frontier.snapshot(graph)`. Contract tests `frontier_survives_save_latest_and_get_round_trip` and `pre_bug_04_payload_without_frontier_loads_with_an_empty_snapshot` pass on InMemory (26/26) and SQLite (30/30 with `--features sqlite`); Postgres proven via independently-reconfirmed CI run 33758221386 (28 declared == 28 passed, up from 26, the +2 being exactly these two new clauses). TDD order confirmed: `c4dc9859` (test, red) → `9cf69ee0` (fix, green). Divergence reproduction `resume_restores_pre_crash_edge_resolutions_and_executes_the_pending_join` and superstep-equivalence test both pass (part of 128/128 `engine::` test run). |
| 18 | BUG-03 and BUG-04 registered in the `.project/` trio (overview §7, traceability matrix, PRD FR clauses ENG-FR-06a / ENG-FR-12a); requirements ENG-02, ENG-04, ENG-05 all traced to plans in this phase | ✓ VERIFIED | Both bugs present in `00-program-overview.md` §7, `08-traceability-matrix.md`, and `01-battlefield-state-and-execution-engine.md` (ENG-FR-06a, ENG-FR-12a clauses). `requirements:` fields across the seven plans cover ENG-02 (01/02/03), ENG-04 (06), ENG-05 (04/05/07) — no orphaned requirement. |

**Score:** 18/18 truths verified (0 present-but-behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `crates/paladin-battalion/src/engine/superstep.rs` | Starvation-release tiers, run-end check, un-ignored + new tests | ✓ VERIFIED | Present, substantive, wired; 27/27 `engine::superstep` tests pass. |
| `crates/paladin-battalion/src/engine/graph.rs` | `unschedulable_unfed_nodes`, extended `fingerprint()`, golden + difference tests | ✓ VERIFIED | Present, substantive, wired; 36/36 `engine::graph` tests pass. |
| `crates/paladin-battalion/src/engine/mod.rs` | `UnschedulableCycle`/`StarvedNodeAtCompletion` variants, reworded resume rustdoc, BUG-04 divergence tests | ✓ VERIFIED | Present, substantive, wired; 128/128 `engine::` tests pass. |
| `crates/paladin-core/src/platform/container/waypoint.rs` | `FrontierSnapshot`, `Waypoint::frontier` field | ✓ VERIFIED | Present, substantive; 19/19 `paladin-core` waypoint tests pass, including `frontier_snapshot_edges_serialize_in_sorted_identity_order` and `waypoint_payload_without_frontier_field_deserializes_with_an_empty_snapshot`. |
| `crates/paladin-storage/src/waypoint/{contract_tests,in_memory,sqlite,postgres}.rs` | Two new frontier contract clauses wired on all three backends | ✓ VERIFIED | InMemory 26/26, SQLite (`--features sqlite`) 30/30 pass locally; Postgres confirmed via re-verified live CI run (28/28, +2 over the pre-BUG-04 26). |
| `Cargo.toml` + 10 crate manifests | `resolver = "3"`, `rust-version = "1.88"`, workspace inheritance | ✓ VERIFIED | Confirmed by direct grep of all manifests. |
| `.github/workflows/ci.yml` | msrv job retargeted to 1.88 | ✓ VERIFIED | Job name/toolchain/cache key/comment all read 1.88. |
| `README.md`, `MIGRATION.md`, `CHANGELOG.md`, mdBook x3 | MSRV figure reconciled to 1.88 everywhere | ✓ VERIFIED | grep confirms no stray old figure remains. |
| `.planning/phases/22.1-.../22.1-CI-EVIDENCE.md` | Original + re-capture sections, both whole-run `success` | ✓ VERIFIED | Both sections present; both runs independently re-confirmed live via `gh run view`. |
| `.planning/phases/22-.../22-UAT.md` | G-22-1 resolution pointer to new evidence | ✓ VERIFIED | `resolution:` field for G-22-1 references both `22.1-CI-EVIDENCE.md` captures and both run ids. |
| `.project/v0.10.0/00-program-overview.md`, `08-traceability-matrix.md`, `01-battlefield-state-and-execution-engine.md` | BUG-03/BUG-04 register entries, ENG-FR-06a/ENG-FR-12a clauses | ✓ VERIFIED | All three files carry matching entries for both defects. |
| `.project/current-exports.txt` | Regenerated baseline | ✓ VERIFIED | Regenerated (git history shows commit `a24b0455`); content diff is timestamp-only because the `engine` module is not re-exported through the `paladin` facade — self-reported and independently confirmed (no `EngineError`/`engine` symbols appear in the tracked surface at all, pre- or post-phase). |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `compute_next_vanguard` | `Frontier::node_edge_summary` | shared edge-resolution classification | ✓ WIRED | `starved_nodes` calls `frontier.node_edge_summary(graph, node)` — same helper `is_ready` uses. |
| `superstep::run`'s `Completed` branch | `starved_at_completion` (D-04 check) | run-end truthful-outcome guard | ✓ WIRED | Called at both Completed decision sites (superstep.rs:208, 591). |
| `WarGraph::validate` | `unschedulable_unfed_nodes` | validate-time guard, ordered after eligible-set clause | ✓ WIRED | `validate_prefers_unreachable_node_error_over_unschedulable_cycle` proves ordering. |
| `WarEngine::resume_with_options` | `superstep::run` | `Some(latest.frontier)` seeds `Frontier::for_run`/`from_snapshot` | ✓ WIRED | mod.rs:623 passes `Some(latest.frontier)`; superstep.rs:200/258 call `Frontier::for_run`. |
| `Frontier::snapshot` | `build_waypoint` → `Waypoint.frontier` | every `build_waypoint` call site | ✓ WIRED | All 8 call sites in superstep.rs pass `frontier.snapshot(graph)` (verified by grep at lines 230,249,283,311,350,525,555,613,639). |
| `canonical_edge_condition` | `WarGraph::fingerprint` AND `FrontierEdgeState.condition` | one canonicalization, two consumers | ✓ WIRED | Both import and use the same `paladin_core::...::canonical_edge_condition` function. |
| `EngineError` variants | `.project/current-exports.txt` | api-surface CI job | ✓ WIRED (no-op, documented) | Confirmed engine module isn't part of the tracked facade surface; regeneration ran and produced a timestamp-only diff, consistent with that fact. |

### Requirements Coverage

| Requirement | Source Plan(s) | Description | Status | Evidence |
|-------------|-----------------|--------------|--------|----------|
| ENG-02 | 22.1-01, 22.1-02, 22.1-03 | Cyclic superstep execution with truthful outcomes | ✓ SATISFIED | BUG-03 fixed test-first with two-layer typed defense (validate-time + run-end); fingerprint completeness (CR-01) closed. |
| ENG-04 | 22.1-06 | resume() restores state, superstep-for-superstep equivalence to control run | ✓ SATISFIED | BUG-04 fixed test-first; `FrontierSnapshot` persisted and restored on all three backends; divergence reproduction and equivalence tests both pass. |
| ENG-05 | 22.1-04, 22.1-05, 22.1-07 | Three Waypoint backends, Postgres suite proven to execute; MSRV position | ✓ SATISFIED | MSRV raised to measured 1.88 with lockfile restore and resolver 3; whole-run green CI evidence captured twice (original + BUG-04 re-capture), both independently re-confirmed live via `gh`. |

No orphaned requirements: REQUIREMENTS.md's ENG-02/ENG-04/ENG-05 are each claimed by at least one plan's `requirements:` frontmatter field.

### Anti-Patterns Found

None. Scanned all nine files modified across the seven plans (`superstep.rs`, `graph.rs`, `mod.rs`, `waypoint.rs`, `contract_tests.rs`, `in_memory.rs`, `sqlite.rs`, `postgres.rs`) for `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER`/"not yet implemented" — zero matches.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| BUG-03 reproductions pass, un-ignored | `cargo test -p paladin-battalion --lib engine::superstep` | 27/27 passed | ✓ PASS |
| Full engine module (includes BUG-04 tests) | `cargo test -p paladin-battalion --lib engine::` | 128/128 passed | ✓ PASS |
| E2E-1 unmodified and green | `cargo test -p paladin-ai --test e2e_crash_resume` | 27/27 passed (incl. `e2e_1_crash_resume_matches_control_run_with_no_reexecution`) | ✓ PASS |
| Full paladin-battalion crate | `cargo test -p paladin-battalion --lib` | 347/347 passed | ✓ PASS |
| paladin-core waypoint module | `cargo test -p paladin-ai-core --lib waypoint` | 19/19 passed | ✓ PASS |
| Waypoint contract suite, InMemory | `cargo test -p paladin-storage --lib waypoint::in_memory` | 26/26 passed | ✓ PASS |
| Waypoint contract suite, SQLite | `cargo test -p paladin-storage --lib --features sqlite waypoint::sqlite` | 30/30 passed | ✓ PASS |
| Waypoint contract suite, Postgres | live CI (Docker unavailable locally) | 28/28 passed, run 33758221386 re-confirmed via `gh run view` | ✓ PASS |
| `cargo fmt --check` | — | clean | ✓ PASS |
| `cargo clippy --workspace --all-targets --all-features -- -D warnings` | — | clean | ✓ PASS |
| `cargo audit` | — | 0 vulnerabilities | ✓ PASS |
| `cargo deny check` | — | advisories/bans/licenses/sources all ok | ✓ PASS |
| `scripts/check-advisory-register.sh` | — | exit 0, "11 register row(s)... all clauses satisfied" | ✓ PASS |
| TDD ordering, BUG-03 | `git log` | `e4400ee8` (red) precedes `a64e5b0d` (green) | ✓ PASS |
| TDD ordering, BUG-04 | `git log` | `c4dc9859` (red) precedes `9cf69ee0` (green) | ✓ PASS |
| CI evidence run (BUG-04 re-capture) is real and green | `gh run view 33758221386 --json conclusion,headSha,status` | `success`, matches recorded head SHA | ✓ PASS |
| Recorded head is ancestor of current HEAD and contains the fix | `git merge-base --is-ancestor 4885380c... HEAD` | exit 0 | ✓ PASS |

### Human Verification Required

None. All must-haves resolved to VERIFIED with direct codebase and independently re-run evidence (including live re-confirmation of the cited CI run via `gh`, since Docker/Postgres is unavailable in this environment).

### Gaps Summary

No gaps found. All five phase-goal items are independently confirmed in the tree:

1. BUG-03 (cycle-bootstrap starvation) is fixed test-first with a three-tier release mechanism, a validate-time guard, and a run-end truthful-outcome check — all green, TDD order confirmed by git history.
2. MSRV floor raised to a measured 1.88 in exactly one place, propagated consistently to all documentation surfaces, with the pre-existing lockfile pin-backs reverted and `resolver = "3"` adopted as a recurrence guard; `RUSTSEC-2026-0009` cleared by upgrade rather than a new exception.
3. G-22-1 closing evidence captured twice (once on the pre-BUG-04 head, once re-captured after BUG-04 landed) as whole-run `success` CI runs — independently re-confirmed live against GitHub, not merely read from the evidence file.
4. CR-01 (fingerprint under-coverage) closed: `entry`/`defer_flags`/`dynamic_targets`/`output_field`/`DispatchRule` all folded into the hash via serde-canonical (not `Debug`) bytes, pinned by a golden test plus six difference tests, and the "structurally impossible" resume claim reworded.
5. BUG-04 (resume discarding the pre-crash Frontier) fixed test-first via a persisted, additive `FrontierSnapshot` on `Waypoint`, restored on resume and proven to round-trip on all three `WaypointPort` backends (InMemory/SQLite locally, Postgres via re-confirmed live CI), registered as ENG-FR-12a, with CI evidence re-captured on the final head.

---

_Verified: 2026-09-03T14:50:36Z_
_Verifier: Claude (gsd-verifier)_
