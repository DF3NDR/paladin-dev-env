---
phase: 23-control-flow-dynamic-routing-fan-out-subgraphs
plan: 11
type: execute
wave: 8
depends_on: [23-06, 23-09]
files_modified:
  - tests/integration/e2e_muster_defer_order_test.rs
  - crates/paladin-battalion/src/engine/superstep.rs
  - Cargo.toml
autonomous: true
requirements: [CF-03]

must_haves:
  truths:
    - "Program scenario E2E-3's muster/defer/order half passes as an integration test: a planner node musters 5 workers, a deferred aggregation node runs exactly once after all 5 resolve, and the Battlefield's list-dispatch field holds exactly 5 worker results in deterministic task_key order (PRD acceptance 2, D-17)."
    - "The recovering-worker half of E2E-3 is exercised with a manually-succeeding-on-attempt-N mock rather than a real Aegis retry policy, at a clearly marked seam naming Phase 25 / FT-FR-06 as the owner of the retry half (D-17)."
    - "A 50-task muster runs under #[tokio::test(flavor = \"multi_thread\")] with exact-count assertions and an explicit timeout guard, following the X-05 house pattern (PRD §4 item 8, X-05)."
    - "The stress test asserts exact counts, not lower bounds: exactly 50 worker executions, exactly 50 results in the aggregated field, exactly one aggregator execution, and all 50 keys distinct and in order."
    - "E2E-1 (tests/integration/e2e_crash_resume_test.rs) remains byte-identical and green — the golden this phase must not move."
  artifacts:
    - "tests/integration/e2e_muster_defer_order_test.rs — the E2E-3 muster/defer/order integration test with the marked Phase 25 seam."
    - "crates/paladin-battalion/src/engine/superstep.rs — the 50-task multi-thread stress test with exact counts and a timeout guard."
    - "Cargo.toml — the [[test]] target for the new integration test."
  key_links:
    - "the planner's NextStep::Muster -> the worker template -> the deferred aggregator: the three-stage shape E2E-3 exists to prove end-to-end, exercised through the real engine rather than through unit fixtures."
    - "FaultyPaladinPort's attempt counter -> the marked Phase 25 seam: the recovering worker succeeds by mock scripting now; when FT-FR-06 lands, the same test's seam is where the Aegis retry policy replaces the scripting."
    - "the multi-thread stress test -> the parallelism semaphore and the task_key-ordered merge: 50 concurrent tasks is where a merge-order or isolation defect that 3 tasks hides becomes visible."
---

<objective>
Prove the phase's map-reduce capability end-to-end: land program scenario E2E-3's muster / defer /
ordering half as an integration test, and the PRD's 50-task multi-thread stress test with exact
counts and a timeout guard.

Purpose: every Muster property so far is asserted at the unit boundary. E2E-3 is the program-level
acceptance scenario, and the 50-task stress run is where a concurrency or merge-ordering defect that
three tasks hide becomes visible. Both are named acceptance criteria — PRD 02 §3 item 2 and §4 item
8 — not optional extras.

Output: `tests/integration/e2e_muster_defer_order_test.rs` with a clearly marked seam for Phase 25's
retry half, and a `multi_thread` 50-task stress test in the engine's own test module.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-control-flow-dynamic-routing-fan-out-subgraphs/23-CONTEXT.md
@.planning/phases/23-control-flow-dynamic-routing-fan-out-subgraphs/23-RESEARCH.md
@.planning/phases/23-control-flow-dynamic-routing-fan-out-subgraphs/23-PATTERNS.md
@.project/v0.10.0/00-program-overview.md
@.project/v0.10.0/02-control-flow-routing-fanout-subgraphs.md
</context>

## Artifacts this phase produces

| Kind | Symbol / path | Plan |
|------|---------------|------|
| test file | `tests/integration/e2e_muster_defer_order_test.rs` | 23-11 |
| cargo target | `[[test]] name = "e2e_muster_defer_order"` | 23-11 |
| test fn | the 50-task `#[tokio::test(flavor = "multi_thread")]` stress test | 23-11 |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: E2E-3's muster / defer / ordering half as an integration test</name>
  <files>tests/integration/e2e_muster_defer_order_test.rs, Cargo.toml</files>
  <read_first>
    - `tests/integration/e2e_crash_resume_test.rs` — read the whole file: its top doc comment naming E2E-1 and explaining its simulated-crash rationale, its import block, how it builds a graph with `NodeSpec`/`EdgeSpec`/`InputMapping`, how it wires a `WaypointPort` backend, and how it compares against a control run. This is the structural template.
    - `tests/helpers/mod.rs` and the `FaultyPaladinPort` fixture it exports — specifically that its `fail_until_attempt` counter is GLOBAL across every Paladin executed through one port instance (not per-Paladin) and that `fail_paladin(name)` is additive and chainable. The test must be designed around that shared-counter semantics, which is a recorded project finding.
    - `.project/v0.10.0/00-program-overview.md` §6 — E2E-3's full text, so the test's doc comment and assertions match the scenario as specified rather than a paraphrase.
    - `.project/v0.10.0/02-control-flow-routing-fanout-subgraphs.md` §3 acceptance criterion 2 — including the explicit statement that the retry half is Doc 04's and the muster/defer/order half must pass now with a manually-succeeding-on-attempt-N mock.
    - `crates/paladin-battalion/src/engine/graph.rs` — `add_worker_template`, `add_deferred_node`, and the list-dispatch `DispatchRule` the aggregated field uses.
    - `Cargo.toml` — the `[[test]]` block shape (the `e2e_crash_resume` entry at lines ~258-261).
  </read_first>
  <behavior>
    - `planner_musters_five_workers_and_the_deferred_aggregator_runs_once` — the E2E-3 core: a
      planner node whose output drives a five-task Muster, one worker template, and a `defer: true`
      aggregator downstream. Assert exactly 5 worker executions, exactly 1 aggregator execution, and
      that the aggregator's superstep index is strictly greater than the workers'.
    - `aggregated_results_are_exactly_five_in_task_key_order` — the list-dispatch field holds exactly
      5 entries whose order matches the lexicographic `task_key` order, not the completion order.
    - `one_worker_recovers_by_manual_attempt_scripting` — one worker is scripted to fail on its first
      attempts and succeed on attempt N through the mock, standing in for the Aegis retry half; the
      run still produces 5 results. This test carries the marked Phase 25 seam.
    - `run_completes_with_a_single_superstep_complete_waypoint_per_superstep` — the ENG-FR-11
      invariant still holds alongside the muster's progress Waypoints (superstep-complete Waypoints
      are counted separately from `Running` progress ones).
  </behavior>
  <action>
1. Create `tests/integration/e2e_muster_defer_order_test.rs` modeled structurally on
   `e2e_crash_resume_test.rs`: the same import-block shape and a top doc comment that names program
   scenario E2E-3 exactly as that file names E2E-1, quotes which half this test covers, and states
   why the retry half is scripted rather than real.

2. Build the E2E-3 graph: a planner node whose output determines five tasks, one worker template
   registered with `add_worker_template`, and a `defer: true` aggregator registered with
   `add_deferred_node` downstream of the worker template, writing into a list-dispatch Battlefield
   field. Keep it Tier 1 — mocks plus an in-memory or SQLite Waypoint backend, no Docker (D-30).

3. Script the recovering worker with a manually-succeeding-on-attempt-N mock. Design around
   `FaultyPaladinPort`'s GLOBAL attempt counter rather than assuming per-Paladin counting — a test
   written against per-Paladin semantics will pass or fail for the wrong reason.

4. Mark the Phase 25 seam explicitly and unmistakably: a clearly delimited comment block at the
   scripting site naming FT-FR-06 / Phase 25 as the owner of the retry half, stating exactly which
   assertions become the retry policy's responsibility when it lands, and stating that the mock
   scripting is to be REPLACED there rather than kept alongside. A future reader must not have to
   guess which part of this test is a stand-in.

5. Add the `[[test]]` entry to root `Cargo.toml` in the same shape as `e2e_crash_resume`
   (`name` + `path`, no `required-features`).

6. Assert every count exactly — 5 workers, 1 aggregator, 5 results — never with a lower bound or a
   "greater than" comparison. An exact count is what makes a duplicate or dropped task visible.
  </action>
  <verify>
    <automated>cargo test --test e2e_muster_defer_order</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test e2e_muster_defer_order` exits 0 with `0 failed`, and the run reports at least 4 passing tests.
    - `cargo test --test e2e_muster_defer_order planner_musters_five_workers_and_the_deferred_aggregator_runs_once` exits 0 and reports `1 passed`.
    - `cargo test --test e2e_muster_defer_order aggregated_results_are_exactly_five_in_task_key_order` exits 0 and reports `1 passed`.
    - `grep -c 'E2E-3' tests/integration/e2e_muster_defer_order_test.rs` returns at least 1, and `grep -c 'FT-FR-06' tests/integration/e2e_muster_defer_order_test.rs` returns at least 1 (the marked Phase 25 seam).
    - `grep -c 'e2e_muster_defer_order' Cargo.toml` returns 2 (the `name` and `path` lines), and that `[[test]]` block contains no `required-features` key.
    - `grep -c 'assert_eq!' tests/integration/e2e_muster_defer_order_test.rs` returns at least 5, and `grep -c 'assert!(.*>=' tests/integration/e2e_muster_defer_order_test.rs` returns 0 for any count assertion (exact counts only).
    - `cargo test --test e2e_crash_resume_test` exits 0 and `git diff HEAD -- tests/integration/e2e_crash_resume_test.rs` is empty (E2E-1 unmoved).
    - `cargo fmt --check` exits 0 and `cargo clippy --workspace --all-targets --all-features -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>E2E-3's muster/defer/order half passes end-to-end with exactly 5 workers, one deferred aggregator run and 5 ordered results, with the retry half scripted at an unmistakably marked Phase 25 seam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The 50-task multi-thread muster stress test</name>
  <files>crates/paladin-battalion/src/engine/superstep.rs</files>
  <read_first>
    - `src/application/services/orchestration/listener.rs` — the X-05 house pattern for a concurrency stress test: `#[tokio::test(flavor = "multi_thread")]`, exact-count assertions and an explicit timeout guard. Copy that structure rather than inventing one.
    - `crates/paladin-battalion/src/engine/superstep.rs` — the existing test module's fixtures and the seeded-shuffle determinism tests Plan 23-05 extended; the parallelism semaphore and the muster dispatch path, which is what this test exercises under real contention.
    - `crates/paladin-battalion/src/engine/graph.rs` — `EngineLimits::max_muster_tasks`'s default of 100, so a 50-task muster sits inside the limit by design and the test is not accidentally asserting the limit path.
    - `.project/v0.10.0/02-control-flow-routing-fanout-subgraphs.md` §4 item 8 — "Multi-thread stress: 50-task muster, exact counts, timeout guard".
    - `.project/v0.10.0/00-program-overview.md` X-05 — the cross-cutting requirement this test satisfies for the phase.
  </read_first>
  <behavior>
    - `fifty_task_muster_runs_to_completion_under_multi_thread` — `#[tokio::test(flavor = "multi_thread")]`
      wrapping the whole run in an explicit timeout guard that fails the test rather than hanging.
      Assert exactly 50 worker executions, exactly 50 entries in the aggregated field, exactly 1
      aggregator execution, all 50 `task_key`s distinct, and the aggregated order equal to the sorted
      key order.
    - `fifty_task_muster_is_deterministic_across_repeats` — a small number of repeats (at least 3, to
      keep the suite fast while still exercising real thread interleaving) each producing a
      byte-identical final Battlefield.
  </behavior>
  <action>
1. Add the stress test to `engine::superstep`'s existing test module (RESEARCH's placement
   recommendation), following `listener.rs`'s X-05 house pattern exactly: the `multi_thread` flavor
   attribute, an explicit timeout guard wrapping the run so a deadlock fails loudly instead of
   hanging the suite, and exact-count assertions.

2. Size the muster at 50 tasks — inside the default `max_muster_tasks` of 100, so the test exercises
   concurrency rather than the limit rejection path (Plan 23-05 owns the limit tests).

3. Assert exact counts on every dimension: worker executions, aggregated entries, aggregator
   executions, and distinct keys. Also assert the aggregated ORDER equals the sorted key order — at
   50 tasks with real thread interleaving, a merge-order defect that three tasks hide becomes
   reliably visible.

4. Keep the test's runtime modest: use lightweight function-node workers rather than mock-Paladin
   round trips, and keep the repeat count small. The engine's unit suite is the phase's per-task
   sampling command, so a slow test here degrades the whole feedback loop.

5. Do not mark the test `#[ignore]`. X-05 requires it to run in the default suite; if it is too slow
   to run by default, the test is doing too much and should be trimmed rather than hidden.
  </action>
  <verify>
    <automated>cargo test -p paladin-battalion --lib engine::superstep::tests::fifty_task_muster_runs_to_completion_under_multi_thread</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p paladin-battalion --lib engine::superstep::tests::fifty_task_muster_runs_to_completion_under_multi_thread` exits 0 and reports `1 passed`.
    - `cargo test -p paladin-battalion --lib engine::superstep::tests::fifty_task_muster_is_deterministic_across_repeats` exits 0 and reports `1 passed`.
    - `grep -c 'flavor = "multi_thread"' crates/paladin-battalion/src/engine/superstep.rs` returns at least 1 more than at HEAD.
    - The stress test's body contains an explicit timeout guard — verified by `grep -c 'timeout' crates/paladin-battalion/src/engine/superstep.rs` returning at least 1 more than at HEAD.
    - `cargo test -p paladin-battalion --lib engine::superstep` exits 0 with `0 failed` and `0 ignored` (the stress test is not `#[ignore]`d).
    - `cargo test -p paladin-battalion --lib` completes in under 120 seconds on a normal developer machine, measured with `time`.
    - `cargo fmt --check` exits 0 and `cargo clippy --workspace --all-targets --all-features -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>A 50-task muster runs under a multi-thread runtime with an explicit timeout guard, asserting exact worker, result and aggregator counts plus deterministic key ordering across repeats, and it runs in the default suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> engine under real concurrency | The stress test is the only place the muster path is exercised under genuine multi-thread contention; it is the harness that would surface an isolation or ordering defect the unit tests cannot. |
| in-process only | No network, filesystem, credential or external-service surface is added by this plan; both tests are Tier 1 (mocks plus an in-memory or SQLite backend). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-45 | Tampering (of aggregated results under real concurrency) | the muster merge path at 50 tasks | high | mitigate | Exact-count assertions on workers, results and aggregator runs plus a sorted-order assertion, repeated across at least three multi-thread runs — a dropped, duplicated or mis-ordered task fails the test rather than passing on a lower-bound comparison. |
| T-23-46 | Denial of Service (a hung test masking a deadlock) | the stress test itself | medium | mitigate | An explicit timeout guard wraps the run per the X-05 house pattern, so a deadlock in the muster dispatch fails the suite loudly instead of hanging CI; the test is not `#[ignore]`d, so it runs by default. |
| T-23-47 | Repudiation (a stand-in mistaken for the real thing) | the scripted recovering worker | medium | mitigate | The Phase 25 seam is a clearly delimited comment block naming FT-FR-06 and stating which assertions become the retry policy's responsibility, asserted present by a grep criterion — so a later reader cannot mistake scripted success for a working retry policy. |
| T-23-SC | Tampering | npm/pip/cargo installs | high | accept | This plan adds a `[[test]]` target to `Cargo.toml` but no dependency; the `[dependencies]`/`[dev-dependencies]` tables and `Cargo.lock` are untouched. `23-RESEARCH.md` §Package Legitimacy Audit records zero external package additions for the phase. |
</threat_model>

<verification>
- `cargo test --test e2e_muster_defer_order` — 0 failed.
- `cargo test -p paladin-battalion --lib engine::superstep` — 0 failed, 0 ignored.
- `cargo test --test e2e_crash_resume_test` — green, with the file unmodified.
- `cargo test --workspace` — no regression.
- `cargo fmt --check` and `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean.
</verification>

<success_criteria>
E2E-3's muster/defer/order half passes as an integration test with exactly 5 workers, one deferred
aggregator run and 5 results in `task_key` order, with the retry half scripted at a marked Phase 25
seam; and a 50-task multi-thread stress test runs in the default suite with exact counts, a timeout
guard and deterministic ordering across repeats — while E2E-1 stays byte-identical and green.
</success_criteria>

<output>
Create `.planning/phases/23-control-flow-dynamic-routing-fan-out-subgraphs/23-11-SUMMARY.md` when done
</output>
