---
phase: 24-pause-resume-history-graceful-shutdown
plan: 12
type: execute
wave: 10
depends_on: ["24-11", "24-05", "24-07"]
files_modified:
  - docs/src/user-guides/parley-and-chronicle.md
  - docs/src/SUMMARY.md
  - MIGRATION.md
  - CHANGELOG.md
  - .project/v0.10.0/08-traceability-matrix.md
autonomous: true
requirements: [HITL-01, HITL-02, HITL-03, HITL-04, HITL-05]

must_haves:
  truths:
    - "A new mdBook page docs/src/user-guides/parley-and-chronicle.md teaches the approval gate, resume, partial answers, expiry, history, replay and fork, graceful shutdown from the embedder's side, and the paladin-notifications composition example PRD 03 section 6 requires — and is wired into SUMMARY.md after the control-flow page (D-30)."
    - "The page warns graph authors never to template a secret into a Gate payload, and documents the interim any-authenticated-role access posture on the thread routes with PLAT-06 named as its successor."
    - "MIGRATION.md section 9.2 resolves the Waypoint row and carries a deliberate-zero note for every other type this phase touched, plus the new ParleyPort trait as new (D-29)."
    - "MIGRATION.md section 9.6's endpoint list is filled with the three thread paths; the golden openapi diff stays SHIP-02's scope (D-27, D-29)."
    - "CHANGELOG.md's Unreleased section records the phase's user-visible changes (D-30)."
    - "The traceability matrix rows G-05, G-06, G-09, G-15 and G-26 carry this phase's concrete test anchors (D-29)."
    - "cargo semver-checks against 0.9.0, the MSRV check at 1.88, make security, cargo clippy with warnings denied, cargo doc with no new broken intra-doc links, and workspace line coverage at or above 82 percent are all green on the phase's final commit (D-29, ADR-0006)."
    - "The Postgres Tier 2 contract cases are recorded as CI-only evidence routed to the postgres-integration job, never as locally passed (D-28)."
  artifacts:
    - docs/src/user-guides/parley-and-chronicle.md
    - "docs/src/SUMMARY.md (the new page entry)"
    - "MIGRATION.md (sections 9.2 and 9.6)"
    - "CHANGELOG.md (Unreleased entries)"
    - ".project/v0.10.0/08-traceability-matrix.md (five rows with test anchors)"
  key_links:
    - "docs/src/SUMMARY.md entry -> the new page -> mdBook build"
    - "every new public API -> its doc test -> cargo test --doc"
    - "phase final commit -> semver-checks, msrv, make security, clippy, coverage gates"
  prohibitions:
    - statement: "The Postgres contract-suite cases are never recorded as passed from a local run; Docker is unavailable in this devcontainer and the evidence is CI's postgres-integration job."
      status: recalled
      verification: flagged-unverified
    - statement: "The mdBook page tells graph authors never to place a secret in a Gate payload template, because the state endpoint returns that payload to any authenticated caller."
      status: recalled
      verification: flagged-unverified
    - statement: "No Snyk scan step is added or recorded as required; Snyk was evaluated and removed for having no Rust coverage."
      status: recalled
      verification: flagged-unverified
    - statement: "MIGRATION.md sections owned by later phases (9.6's golden diff, 9.8's checklist finalisation) are left as their owners' TBDs rather than filled here."
      status: recalled
      verification: flagged-unverified
---

<objective>
Close the phase: teach the whole feature in one mdBook page, pay the program registrations the phase
owes (`MIGRATION.md` §9.2 and §9.6, the traceability anchors, the changelog), and gather the gate
evidence the phase seals against.

Purpose: X-08's documentation obligation, D-29's registration obligation, and the definition of done
every HITL requirement shares (the X-10/X-11 versioning gate).

Output: the Parley and Chronicle user guide, filled migration sections, changelog entries, five
traceability rows, and a recorded gate-evidence run.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/24-pause-resume-history-graceful-shutdown/24-CONTEXT.md
@.planning/phases/24-pause-resume-history-graceful-shutdown/24-05-SUMMARY.md
@.planning/phases/24-pause-resume-history-graceful-shutdown/24-07-SUMMARY.md
@.planning/phases/24-pause-resume-history-graceful-shutdown/24-11-SUMMARY.md
@.project/v0.10.0/03-pause-resume-history-shutdown.md
@.project/v0.10.0/08-traceability-matrix.md
@.planning/decisions/0006-coverage-gate.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The Parley and Chronicle user guide</name>
  <files>docs/src/user-guides/parley-and-chronicle.md, docs/src/SUMMARY.md</files>
  <read_first>
    - docs/src/SUMMARY.md (the control-flow page entry at ~line 23 — the new page goes immediately after it)
    - docs/src/user-guides/ (a sibling page, for the established heading structure, code-block style and cross-link conventions)
    - .project/v0.10.0/03-pause-resume-history-shutdown.md (section 6 — the notification composition example this page must carry)
    - crates/paladin-battalion/src/engine/graph.rs (NodeSpec::Gate and GateRequestTemplate as shipped, so every snippet compiles against the real API)
    - crates/paladin-battalion/src/engine/mod.rs (resume_with, replay, fork, with_shutdown_grace signatures)
    - src/application/services/chronicle.rs (ChronicleService's three methods)
    - crates/paladin-notifications/ (the existing notification port and adapter surface the composition example uses)
    - .github/instructions/security.instructions.md (the credential-handling rules the security warning restates)
  </read_first>
  <action>
Create `docs/src/user-guides/parley-and-chronicle.md` and wire it into `docs/src/SUMMARY.md`
immediately after the control-flow page entry (~line 23), matching the surrounding entry format.

Cover, in this order (D-30): building an approval gate as one `NodeSpec::Gate` plus two conditional
edges; raising a parley from a Paladin node through the structured directive envelope and reading
the answer back through the parley placeholder namespace; calling `resume_with` and what each typed
error means; partial answers and how to inspect a partially answered suspension; the two `on_expire`
policies and how a defaulted response is marked; the Chronicle — `history`, `inspect`,
`latest_on_branch`, `replay` and `fork`-with-edit, including the byte-identical mainline invariant
and the branch-scoped subgraph child rule; graceful shutdown from the embedder's side —
`with_shutdown_grace`, the `ShutdownCoordinator`, what a `Skipped` node means on a Halted Waypoint,
and that resume re-runs it exactly once; and the HTTP surface with its 202-then-poll shape.

Include the `paladin-notifications` composition example PRD 03 section 6 requires: notifying a human
that a parley is waiting is application code composed from the existing notification port, not a new
port. Show the composition concretely.

Two warnings the page must carry explicitly, because they are the phase's accepted-risk record:
never template a secret or credential into a Gate's payload, because `GET /v1/threads/{id}/state`
returns that payload to any authenticated caller; and thread routes are currently open to any
authenticated role, with admin and writer scopes deferred to PLAT-06 in Phase 27 — a documented
interim posture, not an oversight.

Every snippet must compile against the shipped API. Keep the ubiquitous language throughout: Parley,
Gate, Chronicle, Waypoint, Thread, Vanguard, Battlefield, WarGraph, WarEngine, Directive.
  </action>
  <verify>
    <automated>grep -c 'parley-and-chronicle' docs/src/SUMMARY.md &amp;&amp; cargo test --workspace --doc</automated>
  </verify>
  <acceptance_criteria>
    - `docs/src/user-guides/parley-and-chronicle.md` exists
    - `docs/src/SUMMARY.md` contains an entry for `user-guides/parley-and-chronicle.md` positioned after the control-flow page entry
    - The page contains sections covering the approval gate, resume, partial answers, expiry, history, replay, fork, graceful shutdown and the HTTP surface
    - The page contains a `paladin-notifications` composition example
    - The page contains the secret-in-Gate-payload warning and the any-authenticated-role interim posture note naming PLAT-06
    - `cargo test --workspace --doc` exits 0
    - `cargo doc --no-deps --workspace` exits 0 with no new broken intra-doc link warning
    - `mdbook build docs` succeeds if mdbook is available; otherwise the page's links resolve against `docs/src/SUMMARY.md`
  </acceptance_criteria>
  <done>One page teaches the whole feature with compiling snippets, the required notification example, and both accepted-risk warnings.</done>
</task>

<task type="auto">
  <name>Task 2: `MIGRATION.md` sections 9.2 and 9.6, and the changelog</name>
  <files>MIGRATION.md, CHANGELOG.md</files>
  <read_first>
    - MIGRATION.md (read in full — section 9.2's X-10 register table starting ~line 74 including the `Waypoint` row at ~line 90 and the Phase 23 deliberate-zero note at ~line 94 whose form this follows; section 9.6 at ~line 139 with its owner split; note sections 9.1, 9.5 and 9.8 were filled in plan 24-09 and are not touched here)
    - CHANGELOG.md (the Unreleased section and its entry conventions)
    - .planning/phases/24-pause-resume-history-graceful-shutdown/24-01-PLAN.md (the "Artifacts this phase produces" list — the authoritative inventory of types this phase touched)
    - crates/paladin-web/openapi.json (the three thread paths as regenerated in plan 24-11)
  </read_first>
  <action>
Fill `MIGRATION.md` section 9.2 (D-29). Resolve the `Waypoint` row at ~line 90 with the shipped
outcome: `WaypointStatus::AwaitingInput` now carries `parleys` and `responses`, landed by HITL-01 in
Phase 24. Then add a deliberate-zero note, in the same form as the Phase 23 note at ~line 94, for
every other type this phase touched that was absent at v0.9.0 and therefore needs no migration
entry: `RunOutcome`, `EngineError`, `NodeSpec`, `NodeContext`, `NodeOutcomeKind`, `WaypointSummary`,
`ThreadApiState`, plus `TraceEvent` recorded as untouched. Record the new `ParleyPort` trait as new.

Fill section 9.6's endpoint list with the three thread paths — `GET /v1/threads/{id}/state`,
`POST /v1/threads/{id}/resume` and `GET /v1/threads/{id}/history` — with their status sets, taking
the shapes from the regenerated `crates/paladin-web/openapi.json`. Leave the golden `openapi.json`
diff proof as SHIP-02's Phase 29 `TBD`; that half of the section is not this phase's to close.

Do not touch sections 9.1, 9.5 or 9.8: plan 24-09 filled this phase's part of them, and the rest
belongs to SHIP-01 in Phase 29.

Add the phase's user-visible changes to `CHANGELOG.md`'s `[Unreleased]` section following its
existing entry conventions: parley suspension and the `Gate` node, typed resume with expiry
policies, the Chronicle's `replay`/`fork` and `fork_of` lineage, graceful shutdown with its two env
vars, and the three thread endpoints. Note the graph fingerprint bump to `v4` and what it means for
threads suspended under `v3`.
  </action>
  <verify>
    <automated>grep -c 'ParleyPort' MIGRATION.md &amp;&amp; grep -c '/v1/threads/' MIGRATION.md &amp;&amp; grep -c 'Unreleased' CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `MIGRATION.md` section 9.2's `Waypoint` row names `AwaitingInput` with `parleys` and `responses` and attributes it to HITL-01, Phase 24
    - `MIGRATION.md` section 9.2 contains a deliberate-zero note listing `RunOutcome`, `EngineError`, `NodeSpec`, `NodeContext`, `NodeOutcomeKind`, `WaypointSummary` and `ThreadApiState`
    - `MIGRATION.md` contains `ParleyPort` recorded as a new trait
    - `MIGRATION.md` section 9.6 lists all three `/v1/threads/` paths
    - `MIGRATION.md` section 9.6 still records the golden diff as SHIP-02's Phase 29 item
    - `MIGRATION.md` sections 9.1, 9.5 and 9.8 are unchanged by this task
    - `CHANGELOG.md`'s `[Unreleased]` section contains entries for the Gate node, resume, Chronicle fork, graceful shutdown and the thread endpoints, and notes the `v4` fingerprint bump
  </acceptance_criteria>
  <done>The program's type register, endpoint list and changelog record everything this phase shipped, and nothing another phase owns.</done>
</task>

<task type="auto">
  <name>Task 3: Traceability anchors and the phase gate evidence run</name>
  <files>.project/v0.10.0/08-traceability-matrix.md</files>
  <read_first>
    - .project/v0.10.0/08-traceability-matrix.md (rows G-05, G-06, G-09, G-15 and G-26, and the protocol section describing how a row's test anchor is written)
    - .planning/phases/24-pause-resume-history-graceful-shutdown/24-05-SUMMARY.md (the E2E-2, multi-parley and stress test names)
    - .planning/phases/24-pause-resume-history-graceful-shutdown/24-06-SUMMARY.md (the contract-suite case names and the Postgres CI routing record)
    - .planning/phases/24-pause-resume-history-graceful-shutdown/24-07-SUMMARY.md (the replay/fork/immutability test names)
    - Makefile (the security, coverage and test targets; the exact invocation for each gate)
    - .planning/decisions/0006-coverage-gate.md (the 82 percent workspace line-coverage floor)
    - .github/instructions/security.instructions.md (what make security covers and what it does not; the manual credential-handling review that remains the primary control)
  </read_first>
  <action>
Fill the test anchors for traceability-matrix rows G-05, G-06, G-09, G-15 and G-26 in
`.project/v0.10.0/08-traceability-matrix.md`, naming the concrete test functions and files this
phase landed, in the form the matrix's own protocol section specifies. Take the names from the
plan summaries rather than guessing them.

Run and record the phase gate evidence (D-29): `cargo semver-checks` against 0.9.0, the MSRV check
at 1.88, `make security` (cargo-audit plus cargo-deny), `cargo clippy --workspace -- -D warnings`,
`cargo doc --no-deps --workspace` with no new broken intra-doc links, and workspace line coverage at
or above the 82 percent floor. Record each command and its result in the plan summary.

Perform the manual credential-handling review `.github/instructions/security.instructions.md`
requires for this phase's new surfaces: confirm no log statement interpolates an API key, that no
config type carrying a credential is `Debug`-formatted outward (the `WaypointStoreConfig` postgres
variant carries an env-var name, not a url), and that the thread endpoints' responses and error
envelopes carry no credential. Record the review outcome. Add no Snyk step: Snyk was evaluated and
removed for having no Rust coverage, and a phase must not be recorded as blocked on one.

Record the Postgres Tier 2 contract cases as CI-only evidence routed to the `postgres-integration`
job. Docker is unavailable in this devcontainer, so they must not be recorded as locally passed —
this is the carried concern from the Phase 23 close and the D-28 rule.
  </action>
  <verify>
    <automated>cargo clippy --workspace -- -D warnings &amp;&amp; cargo test --workspace &amp;&amp; make security</automated>
  </verify>
  <acceptance_criteria>
    - `.project/v0.10.0/08-traceability-matrix.md` rows G-05, G-06, G-09, G-15 and G-26 each name a concrete test function and file that exists in the tree
    - `cargo test --workspace` exits 0
    - `cargo clippy --workspace -- -D warnings` exits 0
    - `cargo fmt --check` exits 0
    - `make security` exits 0
    - `cargo doc --no-deps --workspace` exits 0 with no new broken intra-doc link warning
    - Workspace line coverage is at or above 82 percent, with the measured figure recorded in the plan summary
    - `cargo semver-checks` against 0.9.0 has been run and its output recorded in the plan summary
    - The plan summary records the manual credential-handling review outcome and adds no Snyk step
    - The plan summary records the Postgres contract cases as routed to CI's `postgres-integration` job and NOT verified locally
  </acceptance_criteria>
  <done>Every HITL requirement has a findable test anchor, and every phase gate has recorded evidence — with the Postgres half honestly attributed to CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation -> graph author behaviour | What the guide teaches determines whether authors put secrets in a Gate payload |
| evidence record -> phase seal | A gate recorded as passed that was not actually run is a false assurance |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-50 | Information Disclosure | Graph authors templating credentials into a Gate payload that the state endpoint returns | high | mitigate | The mdBook page carries an explicit warning against it, placed with the Gate authoring instructions rather than buried in a security appendix |
| T-24-51 | Repudiation | A Postgres contract case recorded as passed when Docker was unavailable | high | mitigate | The plan summary records those cases as routed to CI's `postgres-integration` job and explicitly not verified locally, matching the Phase 23 carried-concern precedent |
| T-24-52 | Information Disclosure | A credential reaching a log line, error envelope or `Debug` output on the new surfaces | high | mitigate | The manual credential-handling review from `security.instructions.md` is performed against this phase's new surfaces and its outcome recorded; `WaypointStoreConfig`'s postgres variant carries an env-var name rather than a url by construction |
| T-24-53 | Spoofing | A security scan reading as coverage it does not provide | medium | mitigate | No Snyk step is added; the record states plainly that `make security` scans dependencies and clippy is a lint, with the manual review as the primary control for first-party credential handling |
| T-24-SC | Tampering | package-manager installs | low | accept | No new package is installed by this phase; `make security` runs cargo-audit and cargo-deny over the unchanged dependency set |
</threat_model>

<verification>
- `cargo test --workspace` green (Tier 1); Postgres Tier 2 routed to CI
- `cargo fmt --check`, `cargo clippy --workspace -- -D warnings`, `make security` green
- `cargo doc --no-deps --workspace` green with no new broken intra-doc links
- Workspace line coverage at or above 82 percent (ADR-0006)
- `cargo semver-checks` against 0.9.0 run and recorded
</verification>

<success_criteria>
The feature is teachable from one page, the program's registers and changelog record exactly what
shipped, every requirement has a findable test anchor, and every gate has recorded evidence with the
Postgres half honestly attributed to CI.
</success_criteria>

## Artifacts this phase produces

New files created by this plan (excluded from drift verification):
`docs/src/user-guides/parley-and-chronicle.md` and its `docs/src/SUMMARY.md` entry. The full
phase-wide artifact list is in `24-01-PLAN.md`.

<output>
Create `.planning/phases/24-pause-resume-history-graceful-shutdown/24-12-SUMMARY.md` when done
</output>
