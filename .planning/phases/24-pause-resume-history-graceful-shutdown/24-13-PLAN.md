---
phase: 24-pause-resume-history-graceful-shutdown
plan: 13
type: execute
wave: 11
depends_on: ["24-12"]
files_modified:
  - MIGRATION.md
  - CHANGELOG.md
  - docs/src/user-guides/parley-and-chronicle.md
  - crates/paladin-web/src/openapi.rs
autonomous: true
gap_closure: true
requirements: [HITL-05]

must_haves:
  truths:
    - "MIGRATION.md §9.6's status-code row for POST /v1/threads/{id}/resume lists `403` alongside `202`/`400`/`401`/`404`/`409`/`501`, matching crates/paladin-web/openapi.json's response set for that path exactly, and a one-line note names the require_admin gate and PLAT-06 as its successor."
    - "CHANGELOG.md's [Unreleased] HITL-05 entry states that POST /v1/threads/{id}/resume requires an admin-role credential (403 for a non-admin caller) pending PLAT-06, and that GET /v1/threads/{id}/state and GET /v1/threads/{id}/history remain open to any authenticated role."
    - "docs/src/user-guides/parley-and-chronicle.md's authorization callout and HTTP-surface section describe the as-shipped posture — admin-gated resume, any-authenticated-role reads — with PLAT-06 named as the successor, and `mdbook build docs/` exits 0 with no broken links."
    - "The shipped control is untouched by this plan: crate::agent_auth::require_admin is still applied to resume_thread in crates/paladin-web/src/thread_controller.rs, and post_resume_with_non_admin_role_is_403, post_resume_with_admin_role_is_202 and get_thread_state_with_non_admin_role_is_200_not_403 all still pass."
    - "openapi::tests::openapi_thread_paths_document_every_status asserts `403` on the resume path, so a future regeneration that drops the admin-gate's documented status fails a test instead of drifting silently."
    - "When more than one node raises a parley in the same superstep, the parleys merge into ONE AwaitingInput Waypoint carrying all of them rather than colliding or producing one Waypoint per node — asserted by tests/integration/multi_parley_suspension_test.rs (31 passed, independently re-run in 24-VERIFICATION.md)."
    - "The parley list persisted on an AwaitingInput Waypoint has a specified, stable order keyed by node id — asserted by multi_parley_list_order_is_stable_by_node_id (tests/integration/multi_parley_suspension_test.rs:337)."
    - "replay and fork run twice from the same Waypoint each produce a new branch and never mutate the original chain — asserted by the byte-identical-mainline invariant and fork_does_not_touch_mainline_child_waypoints in the subgraph_formation_in_campaign suite."
    - "Ten suspended threads resumed concurrently each produce exactly their own outcome with no cross-thread interference — asserted by tests/integration/parley_resume_stress_test.rs (X-05, 29 passed)."
    - "ShutdownCoordinator::cancel_and_wait with zero registered runs returns immediately rather than waiting out the grace — asserted by cancel_and_wait_with_zero_registered_runs_returns_immediately (crates/paladin-battalion/src/engine/shutdown.rs:427)."
    - statement: "A superstep that raises no ParleyRequest persists no AwaitingInput Waypoint, and a single-parley superstep persists exactly one AwaitingInput Waypoint carrying exactly that one request."
      verification: backstop
    - statement: "The order in which over-grace Skipped nodes are re-listed in a Halted Waypoint's vanguard is specified and stable across a persist/reload cycle."
      verification: backstop
  artifacts:
    - "MIGRATION.md (§9.6 resume status-code row plus the authorization note)"
    - "CHANGELOG.md ([Unreleased] HITL-05 entry)"
    - "docs/src/user-guides/parley-and-chronicle.md (HTTP-surface section and authorization callout)"
    - "crates/paladin-web/src/openapi.rs (403 added to the resume status assertion in openapi_thread_paths_document_every_status)"
  key_links:
    - "crates/paladin-web/src/thread_controller.rs module rustdoc (\"Authorization (narrows D-24 pending PLAT-06)\") -> the wording used in all three documentation files"
    - "crates/paladin-web/openapi.json resume path responses (already carries 403) -> MIGRATION.md §9.6 row -> openapi_thread_paths_document_every_status assertion"
    - "docs/src/SUMMARY.md entry -> docs/src/user-guides/parley-and-chronicle.md -> mdbook build docs/"
  prohibitions:
    - statement: "Documentation drift is never closed by weakening the shipped control: the admin gate on POST /v1/threads/{id}/resume is not removed, relaxed, or made configurable in order to make the pre-fix documentation true again."
      status: recalled
      verification: flagged-unverified
    - statement: "The documentation never describes the interim admin gate as per-thread ownership or as the final authorization model; it is an interim narrowing of D-24 whose successor is PLAT-06, and every credential configured for the resume route is described as admin-equivalent until then."
      status: recalled
      verification: flagged-unverified
    - statement: "No gate evidence (mdbook build, cargo test, cargo fmt, cargo clippy) is recorded in the SUMMARY as passed unless the command was actually run in this session and its real output observed."
      status: recalled
      verification: flagged-unverified
---

<objective>
Close the single gap in 24-VERIFICATION.md: the phase's three closing documentation deliverables
still describe the pre-CR-01 "any authenticated role" posture for `POST /v1/threads/{id}/resume`,
which commit `00b1e552` narrowed to an admin-gated route after plan 24-12's doc commits
(`86a4c85b`, `c81805c8`) landed.

Purpose: the phase's own security-relevant documentation currently contradicts its shipped code.
`crates/paladin-web/src/thread_controller.rs`'s module rustdoc is the source of truth for the
narrowing; `crates/paladin-web/openapi.json` already carries the `403`. The three human-facing
records (CHANGELOG, the MIGRATION §9.6 status registry, the mdBook user guide) do not.

Output: MIGRATION.md §9.6 lists `403` on the resume row with a note naming the gate and PLAT-06;
CHANGELOG.md's `[Unreleased]` HITL-05 entry states the admin requirement and that reads stay
any-role; the mdBook guide's HTTP-surface section and authorization callout describe the shipped
posture; and the openapi drift-guard test asserts `403` so this cannot silently regress.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/24-pause-resume-history-graceful-shutdown/24-VERIFICATION.md
@.planning/phases/24-pause-resume-history-graceful-shutdown/24-REVIEW-FIX.md
@.planning/phases/24-pause-resume-history-graceful-shutdown/24-12-SUMMARY.md
</context>

<scope_note>
This plan touches exactly the three files named in 24-VERIFICATION.md's `gaps[0].artifacts`, plus
one test-only assertion in `crates/paladin-web/src/openapi.rs` that makes the MIGRATION §9.6 row
mechanically self-guarding.

Deliberately **out of scope**, considered and excluded:

- `.project/v0.10.0/08-traceability-matrix.md` row G-26 does not list CR-01's three role tests as
  anchors. It states nothing false about the authorization posture, so it is not part of this gap;
  adding anchors there is a traceability improvement, not gap closure.
- `README.md:164`'s "empty ⇒ any authenticated caller" line is about per-agent `allowed_roles` on
  `/v1/agents/*`, not thread routes. Accurate, untouched.
- A repo-wide sweep for the stale claim was run during planning
  (`grep -rn "any authenticated\|regardless of role" --include=*.md --include=*.rs`, excluding
  `.planning/` and `target/`): outside the three gap files, every remaining hit is either
  `/v1/agents/*` documentation or `thread_controller.rs`'s own already-correct rustdoc.
</scope_note>

<flagged_assumptions>
Two spec-less edge-probe rows classified `unclassified` and therefore remain **unresolved**; they
are surfaced here rather than silently dropped, and are NOT turned into engineering tasks in this
docs gap-closure plan:

- **HITL-02, unclassified** — "review manually". The probe could not classify an edge category for
  `resume_with`'s typed validation matrix. The behaviour it would probe is already covered by the
  shipped validation matrix (`crates/paladin-battalion/src/engine/mod.rs`) and E2E-2, both re-run
  clean in 24-VERIFICATION.md, but no probe-derived predicate was authored for it.
- **HITL-05, unclassified** — "review manually". Same disposition for the thread HTTP surface; the
  route-level behaviour is covered by `thread_controller.rs`'s test module and the two openapi
  drift-guard tests, but no probe-derived predicate was authored.

Canon-referral breadcrumb (prohibition recall stage 2): IDOR / broken access control (OWASP A01)
and "never put credentials in documentation" are canon security controls — referred, not minted as
project prohibitions.
</flagged_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: MIGRATION.md §9.6 registers the 403, and the openapi drift guard asserts it</name>
  <files>MIGRATION.md, crates/paladin-web/src/openapi.rs</files>
  <read_first>
    - MIGRATION.md lines 190-219 (all of §9.6 as written: the intro paragraph, the three-row status-code table, the 202-then-poll paragraph, and the "Still owed, not this phase's scope" paragraph which already names PLAT-06)
    - crates/paladin-web/src/thread_controller.rs lines 1-48 (module rustdoc; the "## Authorization (narrows D-24 pending PLAT-06)" section is the source of truth for the wording and the rationale)
    - crates/paladin-web/src/openapi.rs lines 138-205 (openapi_thread_paths_document_every_status — the per-path status assertions, including the resume path's status array and the 409 dual-code description check)
    - crates/paladin-web/openapi.json (the /v1/threads/{id}/resume post.responses keys — already 202/400/401/403/404/409/501 after CR-01's UPDATE_OPENAPI=1 regeneration)
    - .planning/phases/24-pause-resume-history-graceful-shutdown/24-VERIFICATION.md (gaps[0].missing item 2 — the exact edit this task owes)
  </read_first>
  <action>
Edit MIGRATION.md §9.6's status-code table row for `POST` `/v1/threads/{id}/resume` (currently
line 202) so the status list reads `202`, `400`, `401`, `403`, `404`, `409` (keeping the existing
parenthetical naming the two distinct envelope codes `thread_not_awaiting_input` and
`graph_not_registered` verbatim), `501`. Insert `403` between `401` and `404` so the list stays in
ascending numeric order. Leave the `GET .../state` and `GET .../history` rows byte-identical —
those two routes are unchanged by CR-01.

Then add one short paragraph immediately after the table (before the existing paragraph that starts
with the 202-then-poll explanation) recording, in the same register the rest of §9.6 uses: that
`POST .../resume` additionally requires an admin-role credential via
`crate::agent_auth::require_admin`, returning `403` for an authenticated non-admin caller; that this
narrows D-24's "authenticated callers, any role" default for the one mutating route only (CR-01,
`24-REVIEW.md`, commit `00b1e552`), because neither `ThreadId` nor `Waypoint` carries an
owner/principal to scope against yet; that `GET .../state` and `GET .../history` remain
authenticated-any-role; that every credential configured for the resume route must be treated as
admin-equivalent until PLAT-06 (Phase 27) supplies real per-thread ownership scoping; and that
`crates/paladin-web/src/thread_controller.rs`'s module rustdoc carries the full rationale. Do not
edit the "Still owed, not this phase's scope" paragraph (lines 214-218) — it already assigns
admin/writer scopes on these routes to PLAT-01…PLAT-06, Phase 27, and stays correct.

In `crates/paladin-web/src/openapi.rs`, inside `openapi_thread_paths_document_every_status`, add
`"403"` to the status array iterated for the resume path so the assertion set becomes
`["202", "400", "401", "403", "404", "409", "501"]`. Change nothing else in that test — the 409
dual-code description assertion, the state-path array and the history-path array stay as they are.
Update the test's own rustdoc sentence so it also mentions the admin gate's `403`, matching the
existing style of that doc comment. This is a test-module-only edit: no production code path in
this crate changes.
  </action>
  <verify>
    <automated>grep '^| `POST` | `/v1/threads/{id}/resume` |' MIGRATION.md | grep -c '`403`'  # expect 1</automated>
    <automated>cargo test -p paladin-web --all-features --lib openapi::tests::openapi_thread_paths_document_every_status  # expect 1 passed</automated>
  </verify>
  <acceptance_criteria>
    - `grep '^| `POST` | `/v1/threads/{id}/resume` |' MIGRATION.md | grep -c '`403`'` prints `1`
    - `grep -c 'require_admin' MIGRATION.md` prints at least `1`, and `grep -n 'require_admin' MIGRATION.md` shows the hit inside §9.6 (between the `## 9.6 HTTP API` heading at line ~190 and the `## 9.7 Deprecations` heading)
    - `grep -n 'PLAT-06' MIGRATION.md` shows at least two hits in §9.6 (the new note plus the pre-existing "Still owed" paragraph)
    - The `GET` rows of the §9.6 table are unchanged: `grep -c '| `GET` | `/v1/threads/{id}/state` | `200`, `400`, `401`, `404`, `501` |' MIGRATION.md` prints `1`
    - `cargo test -p paladin-web --all-features --lib openapi::tests::openapi_thread_paths_document_every_status` exits 0 (`1 passed`)
    - The §9.6 status list and the spec agree: the seven statuses asserted in `openapi_thread_paths_document_every_status` for the resume path are exactly the seven listed in the MIGRATION §9.6 resume row
    - `git diff --stat crates/paladin-web/src/openapi.rs` shows only the test module touched; `cargo clippy -p paladin-web --all-targets --all-features -- -D warnings` exits 0
  </acceptance_criteria>
  <done>MIGRATION.md §9.6 registers the admin gate's `403` for POST resume with a note naming `require_admin` and PLAT-06, and the openapi drift-guard test fails if a future regeneration drops that `403`.</done>
</task>

<task type="auto">
  <name>Task 2: CHANGELOG.md's Unreleased HITL-05 entry states the shipped posture</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md lines 100-127 (the `### Added` HITL-04 and HITL-05 bullets in `[Unreleased]`; the HITL-05 bullet runs 117-126 and is the entry to amend)
    - CHANGELOG.md lines 128-160 (the `[0.9.0]` section, for the file's own section conventions — note that `### Security` there records changes relative to a *released* version)
    - crates/paladin-web/src/thread_controller.rs lines 30-48 (the "Authorization (narrows D-24 pending PLAT-06)" rustdoc — the wording to mirror)
    - MIGRATION.md §9.6 as amended by Task 1 (keep the CHANGELOG's phrasing consistent with the registry note)
    - .planning/phases/24-pause-resume-history-graceful-shutdown/24-VERIFICATION.md (gaps[0].missing item 1)
  </read_first>
  <action>
Amend the existing `[Unreleased]` → `### Added` bullet "**Threads over HTTP (HITL-05).**"
(lines 117-126). Keep every existing claim that is still true — the three route paths, the 202-then-
poll shape, the pagination limits, the `ParleyPort` backing with zero `paladin-battalion`
dependency, `WaypointStoreConfig` / `APP_WAYPOINT_STORE_BACKEND` and the `501 not_implemented`
default, the regenerated `openapi.json` and the untouched `/v1/agents/*` paths.

Add to that bullet, after the sentence naming the three routes and their shared authentication
middleware, an explicit statement that `POST /v1/threads/{id}/resume` additionally requires an
admin-role credential and answers `403` to an authenticated non-admin caller, while
`GET /v1/threads/{id}/state` and `GET /v1/threads/{id}/history` remain reachable by any
authenticated role; and that this is an interim narrowing of the phase's own D-24 decision, applied
because no per-thread ownership exists to scope against yet, with PLAT-06 (Phase 27) named as the
successor that replaces it. Point the reader at `MIGRATION.md` §9.6 for the status-code registry,
matching the cross-reference style the HITL-04 bullet already uses for §9.1 M-B-02.

Do **not** open a new `### Security` subsection under `[Unreleased]`: these routes have never
appeared in a tagged release, so relative to every released version this is the initial, admin-gated
shape of a new endpoint rather than a security change to shipped behaviour. Keep the amendment
inside the single HITL-05 bullet; do not reorder or reflow the neighbouring bullets. Keep the file's
existing prose width and em-dash style.
  </action>
  <verify>
    <automated>sed -n '/^- \*\*Threads over HTTP (HITL-05)/,/^- \|^## /p' CHANGELOG.md | grep -c '403'  # expect at least 1</automated>
    <automated>sed -n '/^- \*\*Threads over HTTP (HITL-05)/,/^- \|^## /p' CHANGELOG.md | grep -ci 'admin'  # expect at least 1</automated>
  </verify>
  <acceptance_criteria>
    - The HITL-05 bullet region (from the line beginning `- **Threads over HTTP (HITL-05).**` to the next top-level bullet or heading) contains all of: `403`, an admin-role requirement for `POST /v1/threads/{id}/resume`, an explicit statement that `GET .../state` and `GET .../history` stay open to any authenticated role, and the string `PLAT-06`
    - `grep -c '^## \[Unreleased\]' CHANGELOG.md` prints `1` and `grep -c '^### Security' CHANGELOG.md` is unchanged from its pre-edit value (no new Security subsection added under Unreleased)
    - The HITL-05 bullet still contains its pre-existing claims: `APP_WAYPOINT_STORE_BACKEND`, `501 not_implemented`, `202 Accepted`, `ParleyPort`
    - `git diff --stat CHANGELOG.md` shows changes confined to the `[Unreleased]` section (no line below the `## [0.9.0] - 2026-09-01` heading is modified)
  </acceptance_criteria>
  <done>An integrator reading CHANGELOG.md's Unreleased entry learns that resume is admin-gated with a 403 for non-admin callers, that the two reads are not, and where the successor scope work lives.</done>
</task>

<task type="auto">
  <name>Task 3: the mdBook user guide describes the as-shipped authorization posture</name>
  <files>docs/src/user-guides/parley-and-chronicle.md</files>
  <read_first>
    - docs/src/user-guides/parley-and-chronicle.md lines 260-310 (the whole "## The HTTP Surface" section: the intro sentence at 275-276, the three-row route table at 278-282, the 202-then-poll paragraph at 284-292, the secret-in-Gate-payload blockquote at 294-298, and the "Interim authorization posture" blockquote at 300-303)
    - crates/paladin-web/src/thread_controller.rs lines 30-48 (module rustdoc "Authorization (narrows D-24 pending PLAT-06)" — the authoritative wording, including that every credential configured for the resume route must be treated as admin-equivalent until PLAT-06)
    - crates/paladin-web/src/thread_controller.rs lines 465-530 and 590-605 (get_thread_state, resume_thread and get_thread_history handler rustdocs — which route carries which posture)
    - MIGRATION.md §9.6 as amended by Task 1 (keep the guide's phrasing consistent)
    - .planning/phases/24-pause-resume-history-graceful-shutdown/24-VERIFICATION.md (gaps[0].missing item 3)
  </read_first>
  <action>
Three edits inside "## The HTTP Surface", in the shipped ubiquitous language (Parley, Gate,
Chronicle, Waypoint, Thread):

First, the section's opening sentence (lines 275-276) currently presents all three routes as sitting
behind one shared authentication middleware. Extend it so it also states that the one mutating
route, `POST /v1/threads/{id}/resume`, additionally requires an admin-role credential.

Second, the route table's `POST /v1/threads/{id}/resume` row (line 281): add that an authenticated
caller without the admin role gets `403`, keeping the existing `202 Accepted { thread_id, state_url }`
description and the request-body shape intact. Leave the two `GET` rows unchanged.

Third, rewrite the body of the "**Interim authorization posture.**" blockquote (lines 300-303) so it
records the posture that actually shipped: the two read routes (`GET .../state`, `GET .../history`)
accept any authenticated caller, while answering an approval gate through `POST .../resume` requires
an admin-role credential and answers `403` otherwise; this narrows the phase's own D-24 decision for
the mutating route only, applied because neither `ThreadId` nor `Waypoint` carries an owner or
principal to scope against yet (CR-01, commit `00b1e552`, rationale recorded in
`thread_controller.rs`'s module rustdoc); every credential configured for the resume route must be
treated as admin-equivalent until Phase 27's `PLAT-06` supplies real per-thread ownership scoping.
Keep the blockquote's "documented, accepted interim posture, not an oversight" framing and keep the
`PLAT-06` reference — only the described posture changes, not the callout's purpose. Keep the
blockquote formatting (`> ` prefix, bold lead-in).

Leave the preceding "**Never template a secret or credential into a Gate's payload.**" blockquote
(lines 294-298) exactly as it is: it is about `GET /v1/threads/{id}/state`, which is still reachable
by any authenticated caller, so its warning remains true as written and its wording is the phase's
accepted-risk record (T-24-45).

Change nothing outside "## The HTTP Surface"; do not touch `docs/src/SUMMARY.md`, whose entry for
this page is already correct.
  </action>
  <verify>
    <automated>mdbook build docs/  # expect exit 0, "No broken links found"</automated>
    <automated>! grep -q 'no admin/writer scope distinction' docs/src/user-guides/parley-and-chronicle.md  # stale clause gone</automated>
  </verify>
  <acceptance_criteria>
    - `mdbook build docs/` exits 0 and its output ends with `No broken links found`
    - `! grep -q 'no admin/writer scope distinction' docs/src/user-guides/parley-and-chronicle.md` exits 0 (the stale role-blind clause is gone)
    - The "Interim authorization posture" blockquote still exists (`grep -c 'Interim authorization posture' docs/src/user-guides/parley-and-chronicle.md` prints `1`) and its body contains `403`, an admin-role requirement for resume, and `PLAT-06`
    - The secret-in-payload blockquote is byte-identical: `grep -c 'Never template a secret or credential' docs/src/user-guides/parley-and-chronicle.md` prints `1` and `git diff docs/src/user-guides/parley-and-chronicle.md` shows no change on lines 294-298
    - `git diff --stat docs/src/SUMMARY.md` shows no change
    - `git diff docs/src/user-guides/parley-and-chronicle.md` shows changes only within the "## The HTTP Surface" section
  </acceptance_criteria>
  <done>A reader of the Parley &amp; Chronicle guide learns the shipped posture: admin-gated resume with a 403 for non-admin callers, any-authenticated-role reads, PLAT-06 as the successor — and the book still builds link-clean.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
No new code symbols. This plan produces documentation content and one test-only assertion:

| Artifact | Kind | Note |
|---|---|---|
| MIGRATION.md §9.6 resume row — added `403` status literal | doc row | ascending order, between `401` and `404` |
| MIGRATION.md §9.6 — new authorization paragraph | doc paragraph | names `require_admin`, CR-01/`00b1e552`, PLAT-06 |
| CHANGELOG.md `[Unreleased]` → `### Added` → HITL-05 bullet — amended sentences | doc prose | no new section, no new bullet |
| docs/src/user-guides/parley-and-chronicle.md — HTTP-surface intro sentence, resume table row, "Interim authorization posture" blockquote body | doc prose | no new page, no new heading, no SUMMARY.md change |
| `"403"` added to the resume status array in `openapi::tests::openapi_thread_paths_document_every_status` | existing test, one literal | inside `#[cfg(test)] mod tests`; no new test fn, no new public item |

No new decorators, classes, functions, CLI flags, struct fields, env vars, routes, or files are
created by this plan. Every identifier it names (`require_admin`, `resume_thread`, `ParleyPort`,
`APP_WAYPOINT_STORE_BACKEND`, `openapi_thread_paths_document_every_status`) already exists in the
tree as of commit `19d2e820`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator/integrator → published docs | An operator issues credentials and wires clients from CHANGELOG / MIGRATION §9.6 / the mdBook guide; a doc that misstates the posture is acted on as if true |
| HTTP client → `/v1/threads/*` | Untrusted request crosses here; `require_authentication` + (on resume only) `require_admin` are the controls — unchanged by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-13-01 | Information Disclosure | Phase-closing docs (CHANGELOG.md, MIGRATION.md §9.6, docs/src/user-guides/parley-and-chronicle.md) | medium | mitigate | All three files restate the exact shipped posture (admin-gated resume with `403`; any-authenticated-role reads); each task's acceptance criteria gate on greps for `403`/admin/`PLAT-06` plus `mdbook build docs/` |
| T-24-13-02 | Elevation of Privilege | `POST /v1/threads/{id}/resume` (`resume_thread`) | high | mitigate | This plan changes no handler code. The verification block re-asserts `require_admin` on `resume_thread` and re-runs `post_resume_with_non_admin_role_is_403` / `post_resume_with_admin_role_is_202` / `get_thread_state_with_non_admin_role_is_200_not_403`, so a documentation edit cannot silently accompany a relaxation of the control (prohibition 1) |
| T-24-13-03 | Information Disclosure | Gate `payload_template` returned verbatim by `GET /v1/threads/{id}/state` to any authenticated caller | medium | accept | Unchanged accepted interim posture (D-24, T-24-45/T-24-46). The existing secret-in-payload warning blockquote is kept byte-identical by Task 3's acceptance criteria; PLAT-06 remains the named successor |
| T-24-13-04 | Tampering | `crates/paladin-web/src/openapi.rs` drift-guard test | low | mitigate | The only code edit is one status literal inside `#[cfg(test)] mod tests`; `git diff --stat` confined to the test module and `cargo clippy -p paladin-web --all-targets --all-features -- -D warnings` green are acceptance criteria |
| T-24-13-05 | Repudiation | The phase's documentary record vs. its shipped code | medium | mitigate | `openapi_thread_paths_document_every_status` now asserts the `403`, so the registry claim in MIGRATION §9.6 is machine-checked rather than prose-only, and a future `UPDATE_OPENAPI=1` regeneration that drops it fails CI |
| T-24-13-SC | Tampering | npm/pip/cargo package installs | low | accept | No install task exists in this plan: no dependency is added, `Cargo.toml` files are untouched, and no package-manager command runs — the Package Legitimacy Gate has nothing to audit here |
</threat_model>

<verification>
Run from the repository root after all three tasks:

1. `grep '^| `POST` | `/v1/threads/{id}/resume` |' MIGRATION.md | grep -c '`403`'` → `1`
2. `sed -n '/^- \*\*Threads over HTTP (HITL-05)/,/^- \|^## /p' CHANGELOG.md | grep -c '403'` → ≥ `1`
3. `! grep -q 'no admin/writer scope distinction' docs/src/user-guides/parley-and-chronicle.md` → exit 0
4. `mdbook build docs/` → exit 0, `No broken links found`
5. `cargo test -p paladin-web --all-features --lib openapi::tests` → all pass (including
   `openapi_lists_the_three_thread_paths` and `openapi_pre_existing_agent_paths_are_unchanged`)
6. Control-unchanged guard (T-24-13-02): `grep -n 'require_admin' crates/paladin-web/src/thread_controller.rs`
   still shows the gate applied in `resume_thread`, and
   `cargo test -p paladin-web --all-features --lib thread_controller::tests::post_resume_with_non_admin_role_is_403 thread_controller::tests::post_resume_with_admin_role_is_202 thread_controller::tests::get_thread_state_with_non_admin_role_is_200_not_403`
   → `3 passed`
7. Project gates on the final commit: `cargo fmt --all --check`, then
   `cargo clippy --workspace --all-targets --features web-server -- -D warnings` → both clean
8. `git diff --stat` touches only the four files in `files_modified`
</verification>

<success_criteria>
- 24-VERIFICATION.md's single gap is closed: all three named documentation artifacts state the
  as-shipped authorization posture for `POST /v1/threads/{id}/resume`, and none of them still
  asserts or implies the pre-fix any-authenticated-role posture for that route
- The MIGRATION §9.6 status registry matches `crates/paladin-web/openapi.json`'s resume response
  set exactly, and a test enforces that agreement
- The shipped admin gate and the three role tests are provably unchanged by this plan
- `mdbook build docs/`, `cargo fmt --all --check` and
  `cargo clippy --workspace --all-targets --features web-server -- -D warnings` are green on the
  final commit
</success_criteria>

<output>
Create `.planning/phases/24-pause-resume-history-graceful-shutdown/24-13-SUMMARY.md` when done.

Record verbatim command output for every gate listed in `<verification>` — a gate is reported as
passed only if it was actually run in the executing session (prohibition 3).
</output>
