---
phase: 25-node-level-fault-tolerance
plan: 07
type: execute
wave: 3
depends_on: ["25-01", "25-03"]
files_modified:
  - crates/paladin-core/src/platform/container/waypoint.rs
  - crates/paladin-ports/src/output/trace_sink_port.rs
  - crates/paladin-battalion/src/engine/superstep.rs
  - crates/paladin-battalion/src/engine/mod.rs
  - crates/paladin-battalion/src/engine/test_support.rs
  - crates/paladin-storage/src/waypoint/contract_tests.rs
autonomous: true
requirements: [FT-01, FT-02]

must_haves:
  truths:
    - "Every attempt of one node in one superstep reads an identical Battlefield snapshot (call-recording mock asserts identical inputs per attempt) and only the succeeding attempt's delta merges - a failed attempt's delta never reaches the Battlefield."
    - "AttemptRecord history is ordered by attempt number ascending and AllProvidersFailed.attempts follows chain order; NodeError JSON has a stable serde field order so error_field comparisons are deterministic."
    - "NodeExecutionRecord gains #[serde(default)] attempts: Vec<AttemptRecord> holding FAILED attempts only, and #[serde(default)] cache_hit: bool; attempt keeps recording the SUCCEEDING attempt number (D-16, FT-FR-03)."
    - "TraceEvent::NodeStarted and NodeFinished gain attempt: u32 and NodeFinished gains cache_hit: bool, emitted once per attempt so PRD 07's span-per-attempt shape needs no rename in Phase 28 (D-16)."
    - "BATTLEFIELD_SCHEMA_VERSION is NOT bumped: every addition is #[serde(default)] and follows the Phase 22.1/24 additive-field precedent set by visit_counts, frontier and fork_of."
    - "WaypointStatus::Failed gains an additive #[serde(default)] node_error: Option<NodeError> beside the existing error: String display line and failed_node, with no reshape; it is None for pre-Aegis and engine-limit failures (D-08)."
    - "EngineError gains a structured NodeFailed(NodeError) used by the exhausted-failure path, RunOutcome::Failed exposes the same NodeError, and From<EngineError> for BattalionError maps it to BattalionError::Node(NodeError); any stringly node-failure arm on that path is retired and retained unconstructed (D-08, X-06)."
    - "A Paladin-node failure becomes NodeErrorSource::Paladin and an LLM failure becomes NodeErrorSource::Llm through From conversions at the engine boundary, replacing plan 25-01's temporary Function mapping (D-07)."
    - "Retries are per task inside a Muster, keyed (node_id, task_key), inside the task's own spawned future - siblings never wait for or re-run because of another task's retries (D-17, FT-FR-06)."
    - "No Waypoint is written between attempts; Phase 23's intra-superstep muster-progress Waypoints record only COMPLETED tasks, and a resume re-executes an interrupted node from attempt 1 (D-17, FT-FR-07)."
    - "A NextStep::Parley Directive is a success and is never retried; the post-resume re-run of a parleying node starts at attempt 1 (D-17)."
  artifacts:
    - "crates/paladin-core/src/platform/container/waypoint.rs (NodeExecutionRecord.attempts + cache_hit, WaypointStatus::Failed.node_error)"
    - "crates/paladin-ports/src/output/trace_sink_port.rs (attempt + cache_hit on the node events)"
    - "crates/paladin-battalion/src/engine/mod.rs (EngineError::NodeFailed, RunOutcome::Failed carrying NodeError, From<EngineError> for BattalionError)"
    - "crates/paladin-storage/src/waypoint/contract_tests.rs (three-backend round-trip for the new Failed payload)"
  key_links:
    - "retry loop failed attempt -> AttemptRecord -> NodeExecutionRecord.attempts -> build_waypoint -> WaypointPort::save"
    - "exhausted retry -> NodeError -> EngineError::NodeFailed -> RunOutcome::Failed and WaypointStatus::Failed.node_error -> From<EngineError> for BattalionError -> BattalionError::Node"
    - "muster task dispatch keyed (node_id, task_key) -> its own retry loop inside its own spawned future -> the aggregation the task contributes to"
  prohibitions:
    - statement: "No Waypoint is persisted between two attempts of the same node; retry never becomes a durable checkpoint boundary."
      status: recalled
      verification: flagged-unverified
    - statement: "A node failure reaching a Waypoint or a RunOutcome is never represented only as a bare string; the structured NodeError travels with it."
      status: recalled
      verification: flagged-unverified
    - statement: "One mustered task's retries never delay, re-run or cancel a sibling task."
      status: recalled
      verification: flagged-unverified
    - statement: "A Parley directive is never treated as an attempt failure and never consumes a retry budget."
      status: recalled
      verification: flagged-unverified
---

<objective>
Expand the proven retry slice into full FT-02 observability and failure semantics: `AttemptRecord`
history and `cache_hit` on the execution record, `attempt`/`cache_hit` on the node trace events, the
structured `NodeError` surfacing on `WaypointStatus::Failed`, `EngineError::NodeFailed`,
`RunOutcome::Failed` and `BattalionError::Node`, and the per-task retry semantics inside a Muster
with their Waypoint and Parley interactions.

Purpose: FT-FR-03's attempt history, FT-FR-06's per-task Muster retry and FT-FR-07's no-Waypoint-
between-attempts rule are the parts an operator and a resume both depend on. They are also what plan
25-12's E2E-3 replacement asserts.

Output: two additive persisted fields, two extended trace events, one structured failure path end to
end, and per-task Muster retry.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-node-level-fault-tolerance/25-CONTEXT.md
@.planning/phases/25-node-level-fault-tolerance/25-RESEARCH.md
@.planning/phases/25-node-level-fault-tolerance/25-PATTERNS.md
@.project/v0.10.0/04-fault-tolerance.md
@.project/v0.10.0/07-observability-tooling.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: AttemptRecord history and cache_hit on the record; attempt on the trace events</name>
  <files>crates/paladin-core/src/platform/container/waypoint.rs, crates/paladin-ports/src/output/trace_sink_port.rs, crates/paladin-battalion/src/engine/superstep.rs, crates/paladin-storage/src/waypoint/contract_tests.rs</files>
  <read_first>
    - crates/paladin-core/src/platform/container/waypoint.rs (NodeExecutionRecord at lines 533-552 including the existing `attempt: u32` field whose rustdoc says it is populated meaningfully once per-node retry lands; NodeOutcomeKind at 501; Waypoint.schema_version at ~618; the existing `#[serde(default)]` additive-field precedent already in this file, and the strip-key/round-trip test style used for `checkpoint_ns`)
    - crates/paladin-core/src/platform/container/node_error.rs (AttemptRecord and NodeError landed by plan 25-01)
    - crates/paladin-ports/src/output/trace_sink_port.rs (TraceEvent at line 66 with NodeStarted/NodeFinished payloads and the enum's existing non-exhaustive marking; TraceSink at 144)
    - crates/paladin-battalion/src/engine/superstep.rs (the retry loop landed by plan 25-01 around the spawned closure at 1515-1599, the NodeStarted/NodeFinished emits inside it, and where NodeExecutionRecord is constructed after the join)
    - crates/paladin-storage/src/waypoint/contract_tests.rs (the three-backend contract-case shape a new round-trip case joins)
    - .planning/phases/22.1-engine-readiness-defect-and-msrv-follow-up/22.1-CONTEXT.md (D-21…D-25 — the additive `#[serde(default)]` Waypoint field precedent and its contract round-trip requirement)
    - .planning/phases/24-pause-resume-history-graceful-shutdown/24-CONTEXT.md (the Claude's-discretion TraceEvent resolution D-16 departs from, and why this phase's own FRs justify the departure)
  </read_first>
  <behavior>
    - Test 1 (`failed_attempts_are_recorded_in_order`): a node that fails twice then succeeds records `attempt: 3` and exactly two `AttemptRecord`s whose `attempt` fields are `1` then `2`, ascending, each carrying its own `duration_ms` and `NodeError`.
    - Test 2 (`a_node_that_succeeds_first_time_records_an_empty_attempts_list`): `attempts` is empty and `attempt` is `1`.
    - Test 3 (`node_events_are_emitted_once_per_attempt_with_the_attempt_number`): a recording `TraceSink` observes `NodeStarted { attempt: 1 }`, `NodeFinished { attempt: 1 }`, `NodeStarted { attempt: 2 }`, `NodeFinished { attempt: 2 }` in that order for a node that took two attempts.
    - Test 4 (`cache_hit_defaults_to_false_on_every_record_and_event`): with no cache configured, every record and every `NodeFinished` carries `cache_hit: false`.
    - Test 5 (`record_round_trips_with_the_new_fields_and_without_them`): a `NodeExecutionRecord` JSON payload written before these fields existed (both keys stripped) deserialises with `attempts` empty and `cache_hit` false; a payload with them round-trips equal.
    - Test 6 (`battlefield_schema_version_is_unchanged`): `BATTLEFIELD_SCHEMA_VERSION` holds the same value it holds on `main`.
    - Test 7 (contract case, three backends) (`waypoint_with_attempt_history_round_trips`): a Waypoint carrying a record with two `AttemptRecord`s saves and loads equal on InMemory, SQLite and (CI-only) Postgres.
  </behavior>
  <action>
Write the failing tests first, commit RED, then implement and commit GREEN.

Add two additive fields to `NodeExecutionRecord` in `waypoint.rs`, both `#[serde(default)]`:
`attempts: Vec<AttemptRecord>` holding **failed attempts only** (a node that succeeds on attempt 1
has an empty list) and `cache_hit: bool`. Replace `attempt`'s placeholder rustdoc with the real
contract: it records the succeeding attempt number. Do NOT bump `BATTLEFIELD_SCHEMA_VERSION` — every
addition here is `#[serde(default)]`, which is exactly the shape of the `visit_counts`/`frontier`/
`fork_of` precedent that did not bump it, and bumping would force every schema-compatibility check to
be revisited for a purely additive change (X-04 does not require it).

Add `attempt: u32` to `TraceEvent::NodeStarted` and `NodeFinished`, and `cache_hit: bool` to
`NodeFinished`, in `trace_sink_port.rs`. The enum is already non-exhaustive, so this is payload
growth on a new-in-0.10 type — a deliberate-zero sentence in plan 25-14's note, not a §9.2 row. Match
PRD 07 §2's field names exactly so Phase 28 renames nothing; add no other PRD 07 payload field.

In `superstep.rs`, move the `NodeStarted`/`NodeFinished` emits **inside** the attempt loop so they
fire once per attempt carrying that attempt's number, and accumulate an `AttemptRecord` for each
failed attempt (its number, `started_at`, `duration_ms` and the `NodeError` built for it) into a
vector that is attached to the record after the loop. Order the vector by attempt number ascending —
it is built in order, so assert rather than sort, and pin the ordering with Test 1. Set `cache_hit`
to `false` at every construction site here; plan 25-13 is the only plan that sets it `true`.

Add the three-backend contract case to `crates/paladin-storage/src/waypoint/contract_tests.rs`
following the existing case shape. The Postgres tier runs only in the Docker-gated CI job — record in
the SUMMARY that its evidence is CI-only, never claimed as locally passed.
  </action>
  <verify>
    <automated>cargo test -p paladin-core failed_attempts_are_recorded_in_order &amp;&amp; cargo test -p paladin-core record_round_trips_with_the_new_fields_and_without_them &amp;&amp; cargo test -p paladin-core battlefield_schema_version_is_unchanged &amp;&amp; cargo test -p paladin-battalion node_events_are_emitted_once_per_attempt_with_the_attempt_number &amp;&amp; cargo test -p paladin-storage waypoint_with_attempt_history_round_trips</automated>
  </verify>
  <acceptance_criteria>
    - `crates/paladin-core/src/platform/container/waypoint.rs` contains `pub attempts: Vec<AttemptRecord>` and `pub cache_hit: bool`
    - Both new record fields carry the serde default: `grep -B1 'pub attempts: Vec<AttemptRecord>' crates/paladin-core/src/platform/container/waypoint.rs | grep -q 'serde(default'`
    - `crates/paladin-ports/src/output/trace_sink_port.rs` contains `attempt: u32` on both `NodeStarted` and `NodeFinished`, and `cache_hit: bool` on `NodeFinished`
    - `BATTLEFIELD_SCHEMA_VERSION` is unchanged: `git diff HEAD~1 -- crates/paladin-core/src/platform/container/battlefield.rs | grep -c 'BATTLEFIELD_SCHEMA_VERSION'` is `0`
    - No other PRD 07 payload field was added: `! grep -qE 'EdgeEvaluated|SupersetStarted|token_usage' crates/paladin-ports/src/output/trace_sink_port.rs`
    - `cargo test -p paladin-core failed_attempts_are_recorded_in_order` exits 0 and asserts the two `AttemptRecord`s are numbered 1 then 2
    - `cargo test -p paladin-battalion node_events_are_emitted_once_per_attempt_with_the_attempt_number` exits 0 and asserts the four-event order
    - `cargo test -p paladin-core record_round_trips_with_the_new_fields_and_without_them` exits 0
    - `cargo test -p paladin-storage waypoint_with_attempt_history_round_trips` exits 0 with the Postgres tier reporting SKIP locally
    - `cargo build --workspace --all-features --all-targets` exits 0
  </acceptance_criteria>
  <done>Failed attempts are recorded in ascending order on the execution record, node trace events fire once per attempt carrying the attempt number, the additions are purely additive with no schema-version bump, and a Waypoint carrying attempt history round-trips on every backend.</done>
</task>

<task type="auto">
  <name>Task 2: The structured failure path — Failed Waypoint, EngineError::NodeFailed, BattalionError::Node</name>
  <files>crates/paladin-core/src/platform/container/waypoint.rs, crates/paladin-battalion/src/engine/mod.rs, crates/paladin-battalion/src/engine/superstep.rs, crates/paladin-storage/src/waypoint/contract_tests.rs</files>
  <read_first>
    - crates/paladin-core/src/platform/container/waypoint.rs (WaypointStatus::Failed at lines 562-567 with its `error: String` and `failed_node: NodeId` fields — the display line that must stay)
    - crates/paladin-battalion/src/engine/mod.rs (EngineError at line 154 and every existing stringly node-failure arm on the exhausted-failure path; RunOutcome at line 109 and its `Failed` variant; whether a `From<EngineError> for BattalionError` impl exists anywhere in the crate)
    - crates/paladin-battalion/src/engine/superstep.rs (the `NodeFailure` enum at line 208 with its `Node(StateNodeError)`, `DirectiveParse` and Battalion-child arms; the `node_failure` first-wins bookkeeping at lines 1605-1925; `build_waypoint` at ~2989 and `persist_waypoint` at ~3042)
    - crates/paladin-core/src/platform/container/node_error.rs (NodeError, NodeErrorSource's Paladin/Llm/Function/Timeout/Cancelled arms)
    - crates/paladin-battalion/src/llm_failure.rs (the conversion helper landed by plan 25-06 — the engine-side `NodeErrorSource::Llm` conversion should agree with it rather than re-deriving a second mapping)
    - crates/paladin-core/src/platform/container/battalion/mod.rs (BattalionError::Node landed by plan 25-02)
  </read_first>
  <behavior>
    - Test 1 (`exhausted_retry_writes_a_failed_waypoint_carrying_the_structured_error`): a node whose retries exhaust produces a `WaypointStatus::Failed { error, failed_node, node_error: Some(ne) }` where `ne.node_id` is the failing node, `ne.attempt` is the last attempt number and `ne.transience` matches the source error.
    - Test 2 (`the_display_line_on_a_failed_waypoint_is_unchanged`): the `error: String` field still holds the same human-readable line it held before this change for the same failure.
    - Test 3 (`pre_aegis_and_limit_failures_carry_none`): a node failure with no Aegis, and an `EngineError::NodeVisitLimitExceeded`, both produce `node_error: None`.
    - Test 4 (`run_outcome_failed_exposes_the_same_node_error`): `RunOutcome::Failed` carries the identical `NodeError` the Waypoint recorded.
    - Test 5 (`engine_error_node_failed_maps_to_battalion_error_node`): `BattalionError::from(EngineError::NodeFailed(ne))` is `BattalionError::Node(ne)`.
    - Test 6 (`a_paladin_node_failure_becomes_node_error_source_paladin`): a `PaladinPort::execute` error surfaces as `NodeErrorSource::Paladin { kind, status, provider, message }`, and an underlying `PaladinError::LlmFailure` surfaces as `NodeErrorSource::Llm` carrying the same status and provider — not as `Function`.
    - Test 7 (`node_error_json_field_order_is_stable`): serialising the same `NodeError` twice produces byte-identical JSON with keys in declaration order.
    - Test 8 (contract case, three backends) (`failed_waypoint_with_node_error_round_trips`): a `Failed` Waypoint with `node_error: Some(..)` and one with `node_error: None` (key absent) both save and load equal.
  </behavior>
  <action>
Write the failing tests first, commit RED, then implement and commit GREEN.

Add `#[serde(default)] node_error: Option<NodeError>` to `WaypointStatus::Failed` beside the existing
`error` and `failed_node` — an additive field on an existing variant, the same technique Task 1 used,
with **no reshape**. Keep `error: String` as the display line: it is what a human reads and what
existing consumers parse, so its content must not change.

Add `EngineError::NodeFailed(NodeError)` and use it on the exhausted-failure path. Retire any stringly
node-failure arm that path used, retaining the variant unconstructed with a rustdoc line recording
what superseded it (X-03, X-06). Extend `RunOutcome::Failed` to expose the same `NodeError`, and add
`impl From<EngineError> for BattalionError` if the crate does not already have one, mapping
`NodeFailed(ne)` to `BattalionError::Node(ne)` and every other variant to whatever the existing
conversion (or the closest existing mapping) produces — do not change any other variant's mapping.

Replace plan 25-01's temporary "map every failure through `NodeErrorSource::Function`" with the real
conversions at the engine boundary: `NodeFailure::Node(StateNodeError)` -> `Function { message }`;
a Paladin-node `PaladinError` -> `Paladin { kind, status, provider, message }` where `kind` is the
variant name and `status`/`provider` come from `LlmFailure`'s typed fields when the error is one;
a `PaladinError::LlmFailure` specifically -> `Llm { .. }` so an LLM failure is distinguishable from
any other Paladin failure; a cancellation -> `Cancelled`. `Timeout(TimeoutKind)` is plan 25-09's.
Keep this mapping consistent with `llm_failure::to_paladin_error` rather than deriving a second one —
if a shared function is cleaner, put it beside that helper.

`node_error` must be `None` for pre-Aegis failures and engine-limit failures, so a reader can tell a
policy-driven node failure from a limit breach without inspecting the message.

Add the two contract cases to `crates/paladin-storage/src/waypoint/contract_tests.rs`, including the
absent-key case, so an old `Failed` payload is proven to still deserialise.
  </action>
  <verify>
    <automated>cargo test -p paladin-battalion exhausted_retry_writes_a_failed_waypoint_carrying_the_structured_error &amp;&amp; cargo test -p paladin-battalion engine_error_node_failed_maps_to_battalion_error_node &amp;&amp; cargo test -p paladin-battalion a_paladin_node_failure_becomes_node_error_source_paladin &amp;&amp; cargo test -p paladin-core node_error_json_field_order_is_stable &amp;&amp; cargo test -p paladin-storage failed_waypoint_with_node_error_round_trips</automated>
  </verify>
  <acceptance_criteria>
    - `crates/paladin-core/src/platform/container/waypoint.rs` contains `node_error: Option<NodeError>` inside `WaypointStatus`
    - The `Failed` variant still declares both original fields: `grep -A4 'Failed {' crates/paladin-core/src/platform/container/waypoint.rs | grep -q 'failed_node'`
    - `crates/paladin-battalion/src/engine/mod.rs` contains `NodeFailed(NodeError)` and `impl From<EngineError> for BattalionError`
    - `cargo test -p paladin-battalion the_display_line_on_a_failed_waypoint_is_unchanged` exits 0
    - `cargo test -p paladin-battalion pre_aegis_and_limit_failures_carry_none` exits 0
    - `cargo test -p paladin-battalion run_outcome_failed_exposes_the_same_node_error` exits 0
    - `cargo test -p paladin-core node_error_json_field_order_is_stable` exits 0
    - `cargo test -p paladin-storage failed_waypoint_with_node_error_round_trips` exits 0, including the absent-key case
    - `cargo build --workspace --all-features --all-targets` exits 0
  </acceptance_criteria>
  <done>An exhausted node failure travels as a structured `NodeError` from the retry loop through the Waypoint, the `RunOutcome` and `BattalionError`, the human-readable display line is unchanged, and an old `Failed` payload still deserialises.</done>
</task>

<task type="auto">
  <name>Task 3: Per-task retry inside a Muster, and the Waypoint and Parley interactions</name>
  <files>crates/paladin-battalion/src/engine/superstep.rs, crates/paladin-battalion/src/engine/test_support.rs</files>
  <read_first>
    - crates/paladin-battalion/src/engine/superstep.rs (the Muster task dispatch path: how a `NextStep::Muster(tasks)` Directive spawns one future per task, how `MusterContext { payload, task_key }` reaches each task's `NodeContext`, how results are collected and ordered by `task_key`, and where Phase 23's intra-superstep muster-progress Waypoints are written)
    - crates/paladin-core/src/platform/container/directive.rs (MusterTask at line 96, MusterContext at 117, NextStep::Parley)
    - .planning/phases/23-control-flow-dynamic-routing-fan-out-subgraphs/23-CONTEXT.md (D-14 — MusterProgress is a stored payload contract from v0.10.0 onward, so its shape must not change here)
    - .planning/phases/24-pause-resume-history-graceful-shutdown/24-CONTEXT.md (D-07/D-08 — the post-resume re-run of a parleying node and `ctx.parley_response()`)
    - crates/paladin-battalion/src/engine/test_support.rs (RecordingWaypointStore and the recording node doubles plan 25-01 added)
  </read_first>
  <behavior>
    - Test 1 (`one_mustered_task_retries_without_re_running_siblings`): five tasks, one of which fails twice then succeeds; each sibling executes exactly once, the failing task executes three times, and the aggregation sees five results.
    - Test 2 (`sibling_tasks_do_not_wait_for_a_retrying_task_to_finish`): under a paused clock, sibling tasks complete while the failing task is still in backoff — their completion timestamps precede the retrying task's final attempt.
    - Test 3 (`each_muster_task_has_its_own_attempt_counter`): two tasks that each fail once record `attempt: 2` independently; neither observes the other's count.
    - Test 4 (`no_waypoint_is_written_between_attempts`): a `RecordingWaypointStore` records zero saves between a node's attempt 1 failure and its attempt 2 start.
    - Test 5 (`muster_progress_waypoints_record_only_completed_tasks`): a progress Waypoint written while one task is mid-retry lists only the tasks that finished, never the retrying one.
    - Test 6 (`resume_re_executes_an_interrupted_node_from_attempt_one`): a run interrupted mid-retry, resumed from its last Waypoint, starts the interrupted node at `attempt: 1`.
    - Test 7 (`a_parley_directive_is_never_retried`): a node returning `NextStep::Parley` under a retry policy suspends the run and executes exactly once; the port/node call count is 1.
    - Test 8 (`post_resume_rerun_of_a_parleying_node_starts_at_attempt_one`): after `resume_with`, the re-run node's `NodeExecutionRecord.attempt` is `1`.
  </behavior>
  <action>
Write the failing tests first, commit RED, then implement and commit GREEN.

Move the retry loop into each Muster task's own spawned future, keyed `(node_id, task_key)`, so a
task retries in place inside the same superstep without any coordination with its siblings (D-17,
FT-FR-06). The worker node's resolved `Aegis` comes from the worker template's own entry in the
sidecar (or the graph default), and each task carries its own attempt counter and its own
`AttemptRecord` vector — the counters must not be shared through any captured state. Do not change
task ordering, the `task_key` sort of results, or the aggregation contract.

Assert rather than assume the Waypoint rules: no Waypoint is written between attempts, and Phase 23's
intra-superstep muster-progress Waypoints continue to record only *completed* tasks — their
`MusterProgress` payload is a stored contract from v0.10.0 onward (Phase 23 D-14) and must not change
shape here. A resume therefore re-executes an interrupted node from attempt 1; document that on the
retry loop and pin it with Test 6.

Treat a `NextStep::Parley` Directive as a **success**: it leaves the retry loop immediately, consumes
no retry budget and is never classified by the predicate. The post-resume re-run is a fresh attempt 1
with `ctx.parley_response()` set — that is the Phase 24 D-07/D-08 path, unchanged; this task only
proves the retry loop does not interfere with it.

Add whatever recording doubles Tests 1-3 need to `engine/test_support.rs` (a per-task-keyed
fail-then-succeed worker with an execution counter per `task_key`), following the existing
`CountingFunctionNode` style. Use the paused-clock idiom plan 25-01 established for Test 2.
  </action>
  <verify>
    <automated>cargo test -p paladin-battalion one_mustered_task_retries_without_re_running_siblings &amp;&amp; cargo test -p paladin-battalion each_muster_task_has_its_own_attempt_counter &amp;&amp; cargo test -p paladin-battalion no_waypoint_is_written_between_attempts &amp;&amp; cargo test -p paladin-battalion muster_progress_waypoints_record_only_completed_tasks &amp;&amp; cargo test -p paladin-battalion a_parley_directive_is_never_retried &amp;&amp; cargo test -p paladin-battalion resume_re_executes_an_interrupted_node_from_attempt_one</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p paladin-battalion one_mustered_task_retries_without_re_running_siblings` exits 0 and asserts exactly one execution per sibling and three for the failing task
    - `cargo test -p paladin-battalion sibling_tasks_do_not_wait_for_a_retrying_task_to_finish` exits 0
    - `cargo test -p paladin-battalion each_muster_task_has_its_own_attempt_counter` exits 0
    - `cargo test -p paladin-battalion no_waypoint_is_written_between_attempts` exits 0 and asserts a save count of zero between attempts
    - `cargo test -p paladin-battalion muster_progress_waypoints_record_only_completed_tasks` exits 0
    - `cargo test -p paladin-battalion a_parley_directive_is_never_retried` exits 0 and asserts a call count of 1
    - `cargo test -p paladin-battalion post_resume_rerun_of_a_parleying_node_starts_at_attempt_one` exits 0
    - The stored muster-progress payload shape is unchanged: `git diff HEAD~3 -- crates/paladin-core/src/platform/container/waypoint.rs | grep -c 'MusterProgress'` is `0`
    - `cargo test --workspace --lib` exits 0
    - `cargo clippy --workspace --all-targets --all-features -- -D warnings` exits 0
    - `cargo fmt --check` exits 0
  </acceptance_criteria>
  <done>A mustered task retries inside its own future without touching its siblings, no Waypoint separates two attempts, a resume restarts an interrupted node at attempt 1, and a Parley never consumes a retry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `NodeError` -> persisted `Waypoint` | A structured failure summary containing provider-influenced text becomes durable state readable by any process with store access |
| concurrent Muster tasks -> shared engine state | Independent retrying futures run in parallel over the same superstep snapshot |
| stored `Failed` payload -> a reader written before `node_error` existed | An older consumer must still deserialise the payload |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-28 | Information Disclosure | `NodeError` persisted on a Failed Waypoint | high | mitigate | Every message reaching a `NodeError` was redacted and bounded upstream by `map_http_status` (plan 25-05) before entering `LlmFailure` (plan 25-06); this plan adds no new unredacted path, and `error_field`/Waypoint payloads inherit M-B-04's raw-content warning (D-34) |
| T-25-29 | Tampering | one Muster task's retry state leaking into a sibling | high | mitigate | Attempt counters and `AttemptRecord` vectors live inside each task's own spawned future keyed `(node_id, task_key)` with no shared captured state (D-17), pinned by `each_muster_task_has_its_own_attempt_counter` |
| T-25-30 | Tampering | a retry becoming a durable checkpoint boundary | medium | mitigate | No Waypoint is written between attempts and muster-progress Waypoints record only completed tasks (D-17, FT-FR-07), pinned by `no_waypoint_is_written_between_attempts` and `muster_progress_waypoints_record_only_completed_tasks`; a resume therefore cannot continue a partially attempted node |
| T-25-31 | Denial of Service | a Parley consuming a retry budget or being re-raised | medium | mitigate | A `NextStep::Parley` leaves the retry loop as a success and is never classified by the predicate (D-17), pinned by `a_parley_directive_is_never_retried` |
| T-25-32 | Repudiation | an older reader failing on the new `Failed` payload | medium | mitigate | `node_error` is additive and `#[serde(default)]` with no reshape (D-08, Phase 22.1 D-21…D-25), proven by the absent-key contract case on all three backends |
| T-25-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package: RESEARCH.md's Package Legitimacy Audit records zero proposed packages, verified by direct `Cargo.toml` read |
</threat_model>

<verification>
- `cargo test -p paladin-core`, `cargo test -p paladin-battalion`, `cargo test -p paladin-storage` green
- `cargo test -p paladin-storage waypoint` green with the Postgres tier reporting SKIP locally (Docker absent — CI-only evidence)
- `cargo build --workspace --all-features --all-targets` green
- `cargo fmt --check` and `cargo clippy --workspace --all-targets --all-features -- -D warnings` green
- No `unwrap()`/`expect()`/`panic!` introduced in library code
</verification>

<success_criteria>
Failed attempts are recorded in order with per-attempt trace events; an exhausted failure travels as a
structured `NodeError` through the Waypoint, the `RunOutcome` and `BattalionError` without losing its
display line; a mustered task retries inside its own future without disturbing siblings; and no
Waypoint separates two attempts, so a resume restarts an interrupted node at attempt 1.
</success_criteria>

## Artifacts this phase produces

New symbols and files created by Phase 25 (excluded from drift verification — none exist in the tree
before this phase):

- **Types:** `Transience`, `NodeError` (core `node_error`), `NodeErrorSource`, `TimeoutKind`,
  `AttemptRecord`, `Aegis`, `RetryPolicy` (core `aegis`), `RetryPredicate`, `TimeoutPolicy`,
  `ErrorHandlerSpec`, `CachePolicy`, `CacheKeySpec`, `CacheMarker`, `CachedDelta`, `StateNodeError`,
  `EngineRegistries`, `RetryPredicateEvaluator`, `RetryPredicateRegistry`, `ErrorHandler`,
  `ErrorHandlerRegistry`, `HeartbeatHandle`, `NodeCachePort`, `NodeCacheKey`, `NodeCacheError`,
  `InMemoryNodeCache`, `RedisNodeCache`, `RedisNodeCacheConfig`, `NodeCacheConfig`,
  `NodeCacheBackend`, `FallbackLlmAdapter`, `RecordingNodeCache`
- **Enum variants:** `PaladinError::LlmFailure`, `LlmError::ProviderError`,
  `LlmError::AllProvidersFailed`, `BattalionError::Node`, `TraceEvent::FallbackHop`, and the
  `EngineError` variants `NodeFailed`, `RunTimeoutExceeded`, `AegisOnUndeclaredNode`,
  `AegisUnsupportedForNodeKind`, `UnregisteredRetryPredicate`, `UnregisteredErrorHandler`,
  `RouteTargetUnknown`, `RouteTargetIsWorkerTemplate`, `RouteErrorFieldUndeclared`,
  `RouteErrorFieldDispatchInvalid`, `AbsorbDeltaSchemaInvalid`, `HandlerNotAllowedOnWorkerTemplate`,
  `MusterHandlerMustBeDeltaOnly`, `RetryPolicyInvalid`, `TimeoutPolicyInvalid`,
  `CachePolicyWithoutCacheBackend`, `CachePolicyOnDeniedField`
- **Struct fields:** `NodeExecutionRecord.attempts`, `NodeExecutionRecord.cache_hit`,
  `WaypointStatus::Failed.node_error`, `FieldSpec.cache`, `PaladinResult.served_by`,
  `NodeContext.attempt`, `TraceEvent::NodeStarted.attempt`, `TraceEvent::NodeFinished.attempt`,
  `TraceEvent::NodeFinished.cache_hit`, `WarGraph`'s aegis/default_aegis sidecar
- **Methods:** `PaladinError::transience`, `LlmError::transience`, `WarGraph::set_aegis`,
  `WarGraph::with_default_aegis`, `WarGraph::aegis_for`, `WarEngine::with_retry_predicate`,
  `WarEngine::with_error_handler`, `WarEngine::with_node_cache`, `PaladinPort::execute_observed`,
  `NodeContext::heartbeat`, `map_http_status`, `llm_failure::to_paladin_error`,
  `FaultyPaladinPort::fail_paladin_until_attempt`
- **Constants:** `GRAPH_FINGERPRINT_VERSION` value `v5`, `NODE_CACHE_SCHEMA_VERSION`
- **Cargo features:** `redis-cache` on `paladin-storage` plus the facade passthrough
- **Env vars:** the seven `APP_NODE_CACHE_*` variables
- **New files:** `crates/paladin-core/src/platform/container/{transience,node_error,aegis,node_cache}.rs`,
  `crates/paladin-ports/src/output/node_cache_port.rs`, `crates/paladin-llm/src/http_status.rs`,
  `crates/paladin-llm/src/fallback.rs`, `crates/paladin-battalion/src/engine/{retry,registries}.rs`,
  `crates/paladin-battalion/src/{error_handler,retry_predicate,llm_failure}.rs`,
  `crates/paladin-storage/src/node_cache/{mod,in_memory,redis,contract_tests}.rs`,
  `src/config/node_cache.rs`, `tests/integration/e2e_compensation_chain_test.rs`,
  `tests/integration/aegis_retry_stress_test.rs`, `docs/src/user-guides/fault-tolerance.md`

<output>
Create `.planning/phases/25-node-level-fault-tolerance/25-07-SUMMARY.md` when done
</output>
