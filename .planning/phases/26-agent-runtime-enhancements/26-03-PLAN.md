---
phase: 26-agent-runtime-enhancements
plan: 03
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - crates/paladin-ports/src/output/llm_port.rs
  - crates/paladin-ports/src/output/vision_llm_port.rs
  - crates/paladin-ports/Cargo.toml
  - crates/paladin-llm/src/compat/engine.rs
  - crates/paladin-llm/src/fallback.rs
  - crates/paladin-llm/src/mock.rs
  - crates/paladin-llm/src/llm_analysis_service.rs
  - crates/paladin-llm/src/anthropic/vision.rs
  - crates/paladin-llm/src/gemini/adapter.rs
  - crates/paladin-llm/src/grok/adapter.rs
  - crates/paladin-llm/src/kimi/adapter.rs
  - crates/paladin-llm/src/ollama/adapter.rs
  - crates/paladin-llm/src/openai/adapter.rs
  - crates/paladin-llm/src/openai_compatible/adapter.rs
  - crates/paladin-llm/src/qwen/adapter.rs
  - crates/paladin-llm/benches/llm_serialization_benchmarks.rs
  - crates/paladin-llm/examples/live_vendor_smoke.rs
  - crates/paladin-battalion/src/commander.rs
  - crates/paladin-battalion/src/grove_service.rs
  - crates/paladin-battalion/src/llm_decision.rs
  - crates/paladin-memory/src/services/memory_extraction_service.rs
  - src/application/services/paladin/paladin_execution_service.rs
  - src/application/services/paladin/planning_service.rs
  - src/application/services/paladin/prompt_generation_service.rs
  - src/application/services/paladin/temperature_service.rs
  - tests/helpers/mock_llm_adapter.rs
  - tests/integration/anthropic_provider_test.rs
  - tests/integration/cli_real_providers_test.rs
  - tests/integration/deepseek_provider_test.rs
  - tests/integration/llm_live_api_tests.rs
  - tests/integration/ollama_docker_test.rs
  - tests/integration/openai_content_analysis_integration_test.rs
  - tests/integration/openai_provider_test.rs
  - tests/integration/provider_switching_test.rs
  - tests/integration/vision_integration_test.rs
  - tests/unit/llm/anthropic_adapter_test.rs
  - tests/unit/llm/deepseek_adapter_test.rs
  - tests/unit/mock_llm_adapter_test.rs
  - .cargo/semver-checks-allowlist.toml
  - MIGRATION.md
autonomous: false
requirements: [RT-05]

must_haves:
  truths:
    - "LlmRequest gains pub response_format: Option<ResponseFormat> with #[serde(default)], is marked #[non_exhaustive], and gains LlmRequest::new(model, prompt) plus chainable with_attachments / with_stream / with_metadata / with_response_format -- all doc-tested (D-28, RT-FR-17, X-10.3)."
    - "ResponseFormat is #[non_exhaustive] with exactly JsonObject and JsonSchema { name: String, schema: serde_json::Value, strict: bool } (D-28)."
    - "Every one of the 37 in-tree LlmRequest struct-literal files migrates to the constructor in the SAME commit as the #[non_exhaustive] attribute, so the workspace compiles at every commit boundary (D-28)."
    - "The MIGRATION.md 9.2 LlmRequest row is resolved Y (struct_marked_non_exhaustive), the matching .cargo/semver-checks-allowlist.toml entry is added and the crates/paladin-ports/Cargo.toml lint suppression is applied -- all three in the same commit, the D-07/D-17/D-28 X-10 treatment (D-28, D-37, X-10.1, X-10.5)."
    - "Option (a) was chosen over option (b) deliberately and the reason is recorded: LlmRequest has no Default, so there is no functional-update site to preserve -- every downstream literal breaks under both options and only (a) makes the NEXT field free, which is why this differs from Phase 25 D-26 (D-28)."
    - "No ProviderCapabilities field is added: a structured-output capability flag is a Deferred Idea, not an RT FR, and adding it would be a second X-10.3 event for nothing (D-28)."
    - "No adapter reads response_format in this plan -- the field is inert on the wire until plan 26-06 wires the four native paths, pinned by an all-adapters-ignore-it test (X-03 additive)."
    - "EDGE(builder idempotency): LlmRequest::new produces a fresh id per call, and chaining the same with_* method twice keeps the last value rather than accumulating (D-28)."
  artifacts:
    - "crates/paladin-ports/src/output/llm_port.rs (LlmRequest::new + four with_* builders, ResponseFormat)"
    - ".cargo/semver-checks-allowlist.toml (the LlmRequest entry)"
    - "MIGRATION.md section 9.2 (the LlmRequest row resolved Y)"
  key_links:
    - "LlmRequest::new(model, prompt) -> with_response_format(ResponseFormat) -> LlmPort::generate -> (plan 26-06) the provider's native JSON mode"
    - "MIGRATION.md 9.2 LlmRequest row (Y) <-> .cargo/semver-checks-allowlist.toml entry <-> crates/paladin-ports/Cargo.toml lint suppression -- the CI semver job checks set-equality in both directions"
  prohibitions:
    - statement: "ProviderCapabilities gains no field in this phase; no adapter capability flag changes value."
      status: recalled
      verification: flagged-unverified
    - statement: "LlmRequest gains no tools field and no adapter is taught to emit function_call -- ADR-0042's deferred capability keeps its trigger unchanged."
      status: recalled
      verification: flagged-unverified
    - statement: "No LlmRequest struct literal is left behind: the non-exhaustive attribute and the 36 call-site migrations land in one commit, never split across two."
      status: recalled
      verification: flagged-unverified
    - statement: "The semver allowlist is never satisfied by a blanket wildcard; the entry names one crate and one specific lint."
      status: recalled
      verification: flagged-unverified
---

<objective>
Give `LlmRequest` the constructor and builder it has never had, add the additive
`response_format: Option<ResponseFormat>` field RT-FR-17 needs, mark the struct `#[non_exhaustive]`,
migrate all 37 in-tree construction files to the constructor, and discharge the X-10 register
obligation for the change in the same commit.

Purpose: `LlmRequest` is a pre-existing public struct with six `pub` fields, **no** `Default` and no
constructor, built by full struct literal in 37 files. Adding a field breaks every one of them under
either X-10.3 option, so this is the change that must land as one atomic, compiler-verified sweep —
and it must land before plan 26-06 can wire a single provider's native JSON mode.

Output: `LlmRequest::new` + four chainable builders + `ResponseFormat` in
`crates/paladin-ports/src/output/llm_port.rs`; 36 migrated call-site files; the resolved
`MIGRATION.md` §9.2 row, the allowlist entry and the per-crate lint suppression.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-agent-runtime-enhancements/26-CONTEXT.md
@.planning/phases/26-agent-runtime-enhancements/26-RESEARCH.md
@.planning/phases/26-agent-runtime-enhancements/26-PATTERNS.md
@.project/v0.10.0/05-agent-runtime-enhancements.md
@.project/v0.10.0/00-program-overview.md
@MIGRATION.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm the one-way LlmRequest public contract before it is written</name>
  <decision>Apply X-10.3 option (a) to `LlmRequest`: add `response_format: Option&lt;ResponseFormat&gt;` with `#[serde(default)]`, mark the struct `#[non_exhaustive]`, add `LlmRequest::new(model, prompt)` plus four chainable `with_*` builders, migrate all 37 construction files in the same commit, and record the row as deliberate-breaking `Y` with an allowlist entry (D-28).</decision>
  <context>This is one of the phase's three one-way decisions. Once v0.10.0 ships, `#[non_exhaustive]` on a published struct and the `new` + `with_*` construction contract are permanent public API: downstream crates will be written against them and removing either is a breaking change in a later release. D-28 carries its own warning — PRD 05 §2.4 assumed this could be a quiet additive field, but `LlmRequest` has no `Default`, so there is no functional-update escape hatch and **both** X-10.3 options break every downstream literal. Option (a) at least makes the next field free. This deliberately diverges from Phase 25 D-26, which chose option (b) for a type that did have a `Default`. Getting this wrong is not a refactor later; it is a semver event.</context>
  <options>
    <option id="option-a-as-locked">
      <name>Proceed exactly as D-28 locks it — option (a), non-exhaustive plus constructor (recommended)</name>
      <pros>The next field added to `LlmRequest` by any later phase costs nothing; the constructor becomes the only construction path, so a future field cannot be silently forgotten at a call site; matches the D-07/D-17 treatment so the phase has one X-10 mechanism rather than three; the 37-site migration is mechanical and compiler-verified.</pros>
      <cons>One deliberate-breaking `Y` row and one allowlist entry; every downstream crate constructing `LlmRequest` by literal must switch to the builder at the v0.10.0 upgrade; the 36-file diff is large.</cons>
    </option>
    <option id="option-b-field-only">
      <name>Option (b) — add the field, record deliberate-breaking, do NOT mark non-exhaustive</name>
      <pros>Slightly smaller conceptual change (destructuring and exhaustive construction keep working); mirrors Phase 25 D-26's precedent literally.</pros>
      <cons>Every downstream literal still breaks, because there is no `Default` to fall back on — the compatibility cost is paid without buying anything; the next field breaks everyone again; D-28's own analysis calls this strictly worse here.</cons>
    </option>
  </options>
  <resume-signal>Select: option-a-as-locked, option-b-field-only, or describe a different treatment</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LlmRequest constructor, builders, ResponseFormat and the X-10 register entry</name>
  <reversibility rating="one-way">D-28: a non-exhaustive attribute on a published struct plus a `new`/`with_*` construction contract become permanent public API the moment v0.10.0 ships; reversing either is a breaking change in a later release.</reversibility>
  <files>crates/paladin-ports/src/output/llm_port.rs, crates/paladin-ports/Cargo.toml, .cargo/semver-checks-allowlist.toml, MIGRATION.md</files>
  <read_first>
    - crates/paladin-ports/src/output/llm_port.rs — `LlmRequest` :626-641 (the six pub fields `id: Uuid`, `model: String`, `prompt: PromptItem`, `attachments: Vec&lt;ContentItem&gt;`, `stream: bool`, `metadata: HashMap&lt;String, String&gt;`), `LlmResponse` :705, `FinishReason` :795, `ProviderCapabilities` :942 (the type D-28 explicitly does NOT extend), `LlmError` :300 with its existing non-exhaustive attribute and `transience()`
    - crates/paladin-core/src/platform/container/execution_result.rs — the module doc at lines 38-46 explaining why `PaladinResult` stays exhaustive while a sibling does not; this is the house's own written reasoning register the new rustdoc should match
    - .cargo/semver-checks-allowlist.toml — the whole file: the entry schema (`crate`, `lint`, `migration_row`, `requirement_id`, `justification`), the set-equality rule with §9.2, and the existing `paladin-web` entry as the worked example
    - MIGRATION.md §9.2 — the `LlmRequest` row at line 134 currently reading TBD with owner RT-0x, Phase 26, and the Phase 23/24/25 rows above it for the exact cell format
    - crates/paladin-ports/Cargo.toml — where the `[package.metadata.cargo_semver_checks.lints]` section goes; if it does not exist yet, follow the convention Phase 25 used in whichever crate first needed it
    - src/application/services/paladin/paladin_builder.rs — `PaladinBuilder` :77 for the fluent-builder idiom (`mut self -> Self`, one field write)
    - .planning/phases/25-node-level-fault-tolerance/25-CONTEXT.md D-26 — the option-(b) reasoning D-28 distinguishes itself from, so the justification written into the allowlist is accurate rather than approximate
  </read_first>
  <behavior>
    - Test 1 (`llm_request_new_sets_documented_defaults`): `LlmRequest::new("gpt-4", prompt)` produces a fresh id, the given model and prompt, empty attachments, `stream: false`, empty metadata and `response_format: None`.
    - Test 2 (`llm_request_new_generates_a_fresh_id_each_call`): two `new` calls with identical arguments produce different ids.
    - Test 3 (`builder_methods_chain_and_last_write_wins`): `LlmRequest::new(..).with_stream(true).with_response_format(ResponseFormat::JsonObject).with_stream(false)` yields `stream == false` with the response format still set — chaining the same setter twice replaces rather than accumulates.
    - Test 4 (`response_format_round_trips_through_serde_and_defaults_to_none`): a JSON document for `LlmRequest` without a `response_format` key deserializes with `response_format: None`; a document carrying a json-schema-shaped value round-trips to `ResponseFormat::JsonSchema { .. }` and back unchanged.
    - Test 5 (`response_format_carries_both_variants`): both `ResponseFormat::JsonObject` and `ResponseFormat::JsonSchema { name, schema, strict }` survive a serde round trip with equality.
    - Test 6 (`provider_capabilities_gained_no_field`): the test constructs `ProviderCapabilities` exhaustively by struct literal, so the test stops compiling the moment a field is added — the compile-time guard for the prohibition.
  </behavior>
  <action>
Write the failing tests first, commit RED, then implement and commit GREEN.

**`crates/paladin-ports/src/output/llm_port.rs`.** Declare a non-exhaustive
`pub enum ResponseFormat` with exactly two variants, `JsonObject` and
`JsonSchema { name: String, schema: serde_json::Value, strict: bool }`, deriving
`Debug, Clone, PartialEq, Serialize, Deserialize` with `#[serde(rename_all = "snake_case")]`, placed
beside `LlmRequest`. Its rustdoc states that it is a request **hint**: an adapter with no native
mode ignores it harmlessly and correctness never depends on it, because the caller also appends a
schema-conformance instruction block (D-27's belt-and-braces rule). Name Anthropic as the adapter
with no native mode (D-28) and point at the per-provider table the guide gains in plan 26-21.

Add `pub response_format: Option<ResponseFormat>` to `LlmRequest` with `#[serde(default)]`, mark
`LlmRequest` non-exhaustive, and add `LlmRequest::new(model: impl Into<String>, prompt: PromptItem)
-> Self` producing a fresh `Uuid`, no attachments, `stream: false`, empty metadata and
`response_format: None`, plus the chainable `with_attachments(Vec<ContentItem>)`,
`with_stream(bool)`, `with_metadata(HashMap<String, String>)` and
`with_response_format(ResponseFormat)` — each `mut self -> Self`, one field write, last-write-wins,
following `PaladinBuilder`'s idiom. All five carry a rustdoc doc test; `new`'s doc test shows every
field's default value explicitly, which is the shape D-28's `<specifics>` entry asks for. Extend the
`LlmRequest` type-level rustdoc with the X-10 note this release owes: what was added, that the
struct is now non-exhaustive, and that construction goes through `new` plus the `with_*` chain.

Do **not** add a field to `ProviderCapabilities` and do not change any adapter's declared
capabilities (D-28, ADR-0042).

**`crates/paladin-ports/Cargo.toml`.** Add the `[package.metadata.cargo_semver_checks.lints]`
suppression for the `struct_marked_non_exhaustive` lint. Read how Phase 25 applied the equivalent
suppression before writing it so the key shape matches exactly.

**`.cargo/semver-checks-allowlist.toml`.** Add one `[[entry]]` with `crate = "paladin-ports"`,
`lint = "struct_marked_non_exhaustive"`, `migration_row = "paladin-ports | LlmRequest"`,
`requirement_id = "RT-05"`, and a one-line justification stating D-28's reason verbatim: `LlmRequest`
has no `Default`, so no functional-update site exists to preserve; every downstream literal breaks
under either option and only option (a) makes the next field free. One crate, one lint — never a
wildcard.

**`MIGRATION.md` §9.2.** Resolve the `LlmRequest` row at line 134: Mitigation cell records the
non-exhaustive attribute plus the doc-tested `LlmRequest::new` builder; Deliberate-breaking cell is
`Y` with the same one-line justification; Requirement cell is `RT-FR-17`. Use the published package
name in the Crate cell per the Plan 25-14 correction. Touch no other row — the `PaladinError` and
`PaladinPort` row extensions belong to plan 26-21 (D-37).
  </action>
  <verify>
    <automated>cargo check --workspace --all-targets --all-features &amp;&amp; cargo test -p paladin-ports --lib llm_request_new_sets_documented_defaults &amp;&amp; cargo test -p paladin-ports --lib builder_methods_chain_and_last_write_wins &amp;&amp; cargo test -p paladin-ports --lib response_format_round_trips_through_serde_and_defaults_to_none &amp;&amp; cargo test -p paladin-ports --doc</automated>
  </verify>
  <acceptance_criteria>
    - `crates/paladin-ports/src/output/llm_port.rs` contains `pub enum ResponseFormat`
    - `crates/paladin-ports/src/output/llm_port.rs` contains `pub fn new(`, `pub fn with_attachments(`, `pub fn with_stream(`, `pub fn with_metadata(` and `pub fn with_response_format(`
    - The struct carries the attribute: `grep -B3 'pub struct LlmRequest' crates/paladin-ports/src/output/llm_port.rs | grep -q non_exhaustive`
    - The new field defaults on deserialize: `grep -B1 'pub response_format' crates/paladin-ports/src/output/llm_port.rs | grep -q 'serde(default)'`
    - `.cargo/semver-checks-allowlist.toml` contains both `struct_marked_non_exhaustive` and `paladin-ports`
    - The MIGRATION row is resolved: `grep 'LlmRequest' MIGRATION.md | grep -c TBD` is `0`
    - `crates/paladin-ports/Cargo.toml` contains `cargo_semver_checks`
    - `cargo test -p paladin-ports --lib provider_capabilities_gained_no_field` exits 0 (it is a compile-time guard — an added field makes it fail to build)
    - `cargo test -p paladin-ports --doc` exits 0 and `new`'s doc test shows all defaults
    - `cargo test -p paladin-ports --lib response_format_round_trips_through_serde_and_defaults_to_none` exits 0
  </acceptance_criteria>
  <done>`LlmRequest` has a doc-tested constructor and four chainable builders, carries the additive `response_format` field and the non-exhaustive attribute, and the §9.2 row, the allowlist entry and the per-crate lint suppression all agree — in one commit.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate all 36 remaining LlmRequest construction files to the constructor</name>
  <files>crates/paladin-llm/src/compat/engine.rs, crates/paladin-llm/src/fallback.rs, crates/paladin-llm/src/mock.rs, crates/paladin-llm/src/llm_analysis_service.rs, crates/paladin-llm/src/anthropic/vision.rs, crates/paladin-llm/src/gemini/adapter.rs, crates/paladin-llm/src/grok/adapter.rs, crates/paladin-llm/src/kimi/adapter.rs, crates/paladin-llm/src/ollama/adapter.rs, crates/paladin-llm/src/openai/adapter.rs, crates/paladin-llm/src/openai_compatible/adapter.rs, crates/paladin-llm/src/qwen/adapter.rs, crates/paladin-llm/benches/llm_serialization_benchmarks.rs, crates/paladin-llm/examples/live_vendor_smoke.rs, crates/paladin-battalion/src/commander.rs, crates/paladin-battalion/src/grove_service.rs, crates/paladin-battalion/src/llm_decision.rs, crates/paladin-memory/src/services/memory_extraction_service.rs, crates/paladin-ports/src/output/vision_llm_port.rs, src/application/services/paladin/paladin_execution_service.rs, src/application/services/paladin/planning_service.rs, src/application/services/paladin/prompt_generation_service.rs, src/application/services/paladin/temperature_service.rs, tests/helpers/mock_llm_adapter.rs, tests/integration/anthropic_provider_test.rs, tests/integration/cli_real_providers_test.rs, tests/integration/deepseek_provider_test.rs, tests/integration/llm_live_api_tests.rs, tests/integration/ollama_docker_test.rs, tests/integration/openai_content_analysis_integration_test.rs, tests/integration/openai_provider_test.rs, tests/integration/provider_switching_test.rs, tests/integration/vision_integration_test.rs, tests/unit/llm/anthropic_adapter_test.rs, tests/unit/llm/deepseek_adapter_test.rs, tests/unit/mock_llm_adapter_test.rs</files>
  <read_first>
    - crates/paladin-ports/src/output/llm_port.rs (the constructor and builders written in Task 2 — the exact target shape)
    - The authoritative site list, regenerated rather than trusted: `grep -rln "LlmRequest {" --include=*.rs .` currently returns exactly 37 files, one of which is `llm_port.rs` itself
    - crates/paladin-llm/src/mock.rs — `MockLlmAdapter` :79 with `with_responses` :92, `with_error` :106, `with_error_then_response` :156, `with_stream_items` :182, `call_count` :196; this file both constructs and records requests, so read it in full before editing
    - crates/paladin-llm/src/compat/engine.rs — `CompatRequestParameters` :106-134 and `build_request`, the single site where plan 26-06's one change later serves five presets
    - tests/helpers/mock_llm_adapter.rs — the root test helper the 20 `tests/` sites share
  </read_first>
  <action>
This is one mechanical, uniform, compiler-verified rewrite — not thirty-six independent edits. It
touches 36 files because the type has 37 construction sites, and the compiler proves the sweep is
complete: with the non-exhaustive attribute applied in Task 2's commit, any missed literal outside
`paladin-ports` is a hard compile error, so `cargo check --workspace --all-targets --all-features`
is the completeness oracle rather than a manual count.

Regenerate the site list first with `grep -rln "LlmRequest {" --include=*.rs .` rather than trusting
this plan's list. If the count is not 37, stop and report the delta before editing anything.

At each site, replace the full struct literal with `LlmRequest::new(<model>, <prompt>)` followed by
only the `with_*` calls whose value differs from the constructor's default: drop
`id: Uuid::new_v4()` (the constructor supplies it) and drop any field set to its default
(`attachments: vec![]`, `stream: false`, `metadata: HashMap::new()`). Where a site sets `id` to a
specific pinned value, preserve that behaviour without adding anything to the public API for it —
either restructure the assertion so it does not depend on the id, or use a test-visible path — and
list every site that needed this in the SUMMARY.

Preserve behaviour exactly. This task changes construction syntax and nothing else: no default
changes, no field value changes, no relaxed assertions. `crates/paladin-llm/src/mock.rs` keeps its
recorded-request semantics identical — teaching the mock to record `response_format` is plan
26-06's task, not this one.

Two files need care beyond mechanical replacement. `crates/paladin-llm/benches/llm_serialization_benchmarks.rs`
is only compiled under `--all-targets`; compile it, never run it, because the repo's
`benches/config_benchmarks.rs` panics at startup on a missing `llm.anthropic.api_key` setting and a
bare `cargo bench` would take it down with it. `crates/paladin-llm/examples/live_vendor_smoke.rs` is
an example: compiled, not run.

Finish by adding one inertness test, `all_adapters_ignore_response_format_until_wired`: build a
request with `with_response_format(ResponseFormat::JsonObject)`, send it through `MockLlmAdapter`,
and assert the call succeeds and the response is identical to the same request without the field.
Its doc comment names plan 26-06 as the plan that makes the field observable on the wire, so the
test reads as a deliberate staging boundary rather than an oversight.
  </action>
  <verify>
    <automated>cargo check --workspace --all-targets --all-features &amp;&amp; test "$(grep -rln 'LlmRequest {' --include=*.rs . | wc -l)" -le 1 &amp;&amp; cargo test --workspace --tests all_adapters_ignore_response_format_until_wired &amp;&amp; cargo test -p paladin-llm --all-features --lib</automated>
  </verify>
  <acceptance_criteria>
    - Every construction site is migrated: `grep -rln 'LlmRequest {' --include=*.rs . | wc -l` is `1` or `0` (only the declaration site in `llm_port.rs` may remain)
    - `cargo check --workspace --all-targets --all-features` exits 0 — the completeness oracle for the sweep
    - `cargo test -p paladin-llm --all-features --lib` exits 0 with no test removed or skipped relative to the previous commit
    - `cargo test --workspace --tests all_adapters_ignore_response_format_until_wired` exits 0 and selects at least 1 test
    - `crates/paladin-llm/src/mock.rs` still records requests the same way: `git diff HEAD~1 -- crates/paladin-llm/src/mock.rs | grep -c '^[+-].*fn call_count'` is `0` (the recorder itself is untouched)
    - `cargo test -p paladin-battalion --lib` and `cargo test -p paladin-memory --all-features --lib` exit 0
    - `cargo clippy --workspace --all-targets --all-features -- -D warnings` exits 0
    - `cargo fmt --check` exits 0
  </acceptance_criteria>
  <done>Zero `LlmRequest` struct literals remain outside the declaration site, the whole workspace compiles under `--all-targets --all-features`, every pre-existing test still passes unchanged, and the new field is proven inert until plan 26-06 wires it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller-authored JSON schema → `LlmRequest.response_format` → provider request body | Caller-supplied schema text crosses into an outbound HTTP body |
| downstream crate → `LlmRequest` construction | A published construction contract that other crates compile against |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-09 | Information Disclosure | a `response_format` schema logged alongside a request body | low | mitigate | Schemas are caller-authored and are never logged with request bodies; `LlmRequest`'s derived `Debug` carries no credential (none flows through it) and the redaction rule for provider bodies is unchanged (D-41). The rustdoc on `ResponseFormat` states that a schema is caller data, not secret material, so a future logging change has a written expectation to violate. |
| T-26-17 | Tampering | a missed `LlmRequest` construction site silently keeping a stale default | medium | mitigate | The non-exhaustive attribute makes any missed literal a hard compile error outside `paladin-ports`; `cargo check --workspace --all-targets --all-features` is the completeness oracle and the acceptance criteria assert the residual literal count is at most 1 |
| T-26-18 | Repudiation | an allowlist entry with no matching register row | medium | mitigate | The CI `semver` job checks set-equality between `.cargo/semver-checks-allowlist.toml` and `MIGRATION.md` §9.2's `Y` rows in both directions; this plan writes both in the same commit, and the acceptance criteria assert the TBD is gone |
| T-26-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package; 26-RESEARCH.md's Package Legitimacy Audit records two `OK` packages, neither added here |
</threat_model>

<verification>
- `cargo check --workspace --all-targets --all-features` green — the completeness oracle for the 36-file sweep
- `cargo test -p paladin-ports --lib` and `--doc` green
- `cargo test -p paladin-llm --all-features --lib`, `cargo test -p paladin-battalion --lib`, `cargo test -p paladin-memory --all-features --lib` green with no test removed
- `cargo semver-checks` reports only the allowlisted `struct_marked_non_exhaustive` finding for `paladin-ports`
- `cargo fmt --check` and `cargo clippy --workspace --all-targets --all-features -- -D warnings` green
</verification>

<success_criteria>
`LlmRequest` is constructed exclusively through a doc-tested `new` plus four chainable builders,
carries `response_format: Option<ResponseFormat>` with `#[serde(default)]` and the
`#[non_exhaustive]` attribute, all 36 downstream construction files are migrated in the same commit,
and the §9.2 row, the semver allowlist entry and the per-crate lint suppression agree exactly.
</success_criteria>

## Artifacts this phase produces

New symbols and files created by Phase 26 (excluded from drift verification — none exist in the tree
before this phase):

- **Traits:** `ExecutionMiddleware`, `TokenCounterPort`, `VaultPort`, `StructuredExecutorPort`,
  `StructuredExecutorExt`, `StructuredSchema`, `ConformanceFixture`
- **Types:** `MiddlewareFlow`, `ToolFlow`, `FinalResult`, `ModelCallContext`, `ToolCallContext`,
  `ToolCallKind`, `LlmResponseView`, `PromptAssembly`, `PromptSection`, `SectionPlacement`,
  `ModelCallLimit`, `TokenBudget`, `ToolCallLimit`, `Guardrail`, `GuardrailRule`, `HistoryTrimmer`,
  `SummarizationMiddleware`, `VaultRecallMiddleware`, `ModelRetryMiddleware`,
  `ModelFallbackMiddleware`, `ToolCallProtocolMiddleware`, `FinishOnPlainAnswerMiddleware`,
  `AgentRuntimeConfig` and its twelve sub-structs, `ToolErrorMode`, `HeuristicTokenCounter`,
  `Namespace`, `VaultRecord`, `ScoredVaultRecord`, `Page`, `VaultError`, `InMemoryVault`,
  `SqliteVault`, `SemanticVault`, `ConfinedVault`, `RunScope`, `InProcessArsenal`,
  `CompositeArsenalPort`, `VaultTools`, `Structured<T>`, `StructuredOptions`, `SchemaRef`,
  `ShapeError`, `TypedSchema<T>`, `ResponseFormat`, `ReasoningAgent`, `ReasoningAgentOptions`
- **Macros:** `llm_conformance_suite!`
- **Functions:** `extract_json`, `shape_check`, `render_instruction_block`, `run_structured`,
  `reasoning_agent`, `redact_secret_patterns`, `AgentRuntimeConfig::build_chain`,
  `ModelFallbackConfig::resolve_chain`, `RetryPredicate::admits`, `GarrisonEntry::summary`,
  `LlmRequest::new` and its four `with_*` builders, `ToolResultFormatter::format_error`,
  `Namespace::new`/`parse`/`is_prefix_of`
- **Enum variants:** `StopReason::CallLimit`, `StopReason::TokenBudget`,
  `PaladinError::StructuredOutputInvalid`, `PaladinError::GuardrailTripped`,
  `PaladinError::ArmamentFailed`, and the new structured-output `EngineError` variants
- **Struct fields:** `GarrisonEntry.is_summary`, `LlmRequest.response_format`,
  `NodeSpec::Paladin.output_schema`, `NodeContext.vault`, `ModelCallContext.llm_override`,
  `ModelCallContext.retry_policy`
- **Methods on pre-existing types:** the six new `PaladinExecutionService::with_*` /
  `execute_scoped` methods, `PaladinPort::execute_scoped`, `WarEngine::with_vault` /
  `with_structured_executor` / `with_output_schema`, `NodeContext::vault`,
  `NodeSpec::with_output_schema`
- **Constants:** `GRAPH_FINGERPRINT_VERSION` value `v6`, `GUARDRAIL_PATTERN_SIZE_LIMIT_BYTES`
- **Cargo:** `schemars = "1.2"` as a direct facade dependency (no new lockfile package, no new feature)
- **Env vars:** the `APP_AGENT_RUNTIME_<SECTION>_<FIELD>` family
- **Migrations:** `002_add_garrison_is_summary.sql`, `003_create_vault_tables.sql` (both in
  `crates/paladin-memory/migrations/`)
- **New files:** the `src/application/services/paladin/middleware/` module tree,
  `src/application/services/paladin/{structured,vault_confined}.rs`,
  `src/application/services/arsenal/{in_process_arsenal,composite_arsenal,vault_tools}.rs`,
  `src/config/agent_runtime.rs`, `src/presets/mod.rs`,
  `crates/paladin-core/src/platform/container/{vault,structured,run_scope}.rs`,
  `crates/paladin-ports/src/output/{vault_port,token_counter_port,structured_executor_port}.rs`,
  `crates/paladin-memory/src/vault/`, `crates/paladin-memory/src/token_counter/`,
  `crates/paladin-memory/src/migrations.rs`, `crates/paladin-llm/src/conformance.rs`,
  `crates/doc-examples/src/agent_runtime.rs`, `docs/src/user-guides/agent-runtime.md`, and the four
  new `tests/integration/` files
- **Removed:** the root `migrations/` copy and its two `Dockerfile` `COPY` lines (D-17)

<output>
Create `.planning/phases/26-agent-runtime-enhancements/26-03-SUMMARY.md` when done
</output>
