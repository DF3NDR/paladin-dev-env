---
phase: 26-agent-runtime-enhancements
plan: 21
type: execute
wave: 14
depends_on: ["26-14", "26-18", "26-20"]
files_modified:
  - docs/src/user-guides/agent-runtime.md
  - docs/src/SUMMARY.md
  - docs/src/user-guides/tool-integration.md
  - docs/src/user-guides/garrison-memory.md
  - MIGRATION.md
  - CHANGELOG.md
  - .project/current-exports.txt
  - .project/v0.10.0/08-traceability-matrix.md
  - .planning/phases/26-agent-runtime-enhancements/26-VALIDATION.md
autonomous: false
requirements: [RT-01, RT-02, RT-03, RT-04, RT-05, RT-06, RT-07]

must_haves:
  truths:
    - "One new mdBook page docs/src/user-guides/agent-runtime.md is registered after the fault-tolerance page in docs/src/SUMMARY.md:25, in the parley-and-chronicle.md / fault-tolerance.md shape, with {{#include}} anchors from crates/doc-examples/src/agent_runtime.rs (D-38, X-08)."
    - "The guide carries all six tables and sections D-38 names: the NodeInterceptor-vs-ExecutionMiddleware two-layer table (D-05), the Vault/Garrison/Waypoint table (D-19), the per-provider native-JSON-mode table (D-28), the Ollama recipe pointer (D-32), the reasoning_agent example (D-35) and the tool-call protocol (D-36)."
    - "tool-integration.md gains a pointer to the protocol and to InProcessArsenal; garrison-memory.md gains a paragraph on summaries; every new public item has rustdoc and a doc test; cargo doc builds with no new broken intra-doc links (D-38, X-08)."
    - "MIGRATION.md 9.2 is complete for this phase: the three RT-owned Y rows (StopReason, LlmRequest, GarrisonEntry) are already resolved by their owning plans, and this plan EXTENDS the PaladinError row's Change cell (StructuredOutputInvalid, GuardrailTripped, ArmamentFailed) and the PaladinPort row (execute_scoped, N), lists the four new traits for completeness (N/A, new), and writes one deliberate-zero note per new-in-0.10 type touched (D-37)."
    - "The deliberate-zero notes cover NodeSpec::Paladin.output_schema, NodeContext.vault, the new EngineError variants, WarGraph builders plus fingerprint v6, DirectiveParser internals and MockLlmAdapter's recorded response_format -- the Phase 23/24/25 note form (D-37)."
    - "MIGRATION 9.3 records schemars as a facade dependency adding no lockfile package with MSRV 1.88 proven by the msrv job, and no new cargo feature (tiktoken counter under content-processing, SqliteVault under sqlite, SemanticVault ungated); 9.4 records 002 and 003 with the embedded-migrator change and the root migrations/ removal; 9.5 records AgentRuntimeConfig and every APP_AGENT_RUNTIME_* variable, all inert by default; 9.1 M-B-03 is per D-33 (D-37)."
    - "Gate evidence green on the phase's final commit: cargo semver-checks vs 0.9.0, the msrv job at 1.88, make security, cargo clippy -- -D warnings, coverage >= 82 percent (ADR-0006), AND ./scripts/extract-public-api.sh .project/current-exports.txt followed by ./scripts/check-api-surface.sh .project/current-exports.txt with the export file REGENERATED -- the Phase 25 carried concern that left the api-surface job red for two phases (D-37, STATE.md 2026-09-06)."
    - "The semver allowlist and MIGRATION 9.2's Y rows are set-equal in both directions -- exactly three entries for this phase (D-37, X-10.5)."
    - "CHANGELOG.md [Unreleased] records the phase; 08-traceability-matrix.md rows G-13 (RT-FR-09), G-16 to G-19, G-21 and G-22 gain test anchors (D-37)."
    - "26-VALIDATION.md is filled from the written plans -- Test Infrastructure, Sampling Rate, the Per-Task Verification Map across all 21 plans, Wave 0 requirements and Manual-Only Verifications -- and its frontmatter is set status: validated, nyquist_compliant: true only if every task in every plan has an automated verify (D-39)."
    - "The security posture D-41 describes is verified as landed, not assumed: namespace traversal closed by construction with attack tests, recalled content and tool errors framed as data, redact-then-bound everywhere, Guardrail patterns bounded, no secret in AgentRuntimeConfig, R-23-01 re-listed as accepted, and the hanging-middleware bound stated rather than solved."
  artifacts:
    - docs/src/user-guides/agent-runtime.md
    - "MIGRATION.md sections 9.1 through 9.5, complete for this phase"
    - .project/current-exports.txt
    - .planning/phases/26-agent-runtime-enhancements/26-VALIDATION.md
  key_links:
    - "crates/doc-examples/src/agent_runtime.rs ANCHOR regions -> {{#include}} in docs/src/user-guides/agent-runtime.md -> compile-verified by cargo check -p paladin-doc-examples"
    - "MIGRATION.md 9.2 Y rows <-> .cargo/semver-checks-allowlist.toml entries <-> per-crate lint suppressions -> the CI semver job's set-equality check"
    - "scripts/extract-public-api.sh -> .project/current-exports.txt -> scripts/check-api-surface.sh -> the api-surface CI job (ci.yml:193-216)"
  prohibitions:
    - statement: "No TBD owned by this phase is left in MIGRATION.md; TBDs owned by Phase 27, 28 or 29 are left untouched."
      status: recalled
      verification: flagged-unverified
    - statement: "The semver allowlist is never widened beyond the three entries this phase's Y rows justify."
      status: recalled
      verification: flagged-unverified
    - statement: "A Docker-only, Qdrant-backed or live-Ollama result is never recorded as passing locally; those tiers are routed to CI or UAT."
      status: recalled
      verification: flagged-unverified
    - statement: "nyquist_compliant is never set true while any task in any plan lacks an automated verify command."
      status: recalled
      verification: flagged-unverified
    - statement: "The api-surface export file is regenerated in this phase; leaving it stale is what left the job red for two phases and must not recur."
      status: recalled
      verification: flagged-unverified
---

<objective>
Close the phase: write the one new guide page with all six tables, complete every `MIGRATION.md`
section this phase owns, regenerate the public-API export file the `api-surface` job has been red on
for two phases, fill `26-VALIDATION.md` from the twenty plans that precede this one, and produce the
gate evidence — with a human confirming the parts a command cannot.

Purpose: the program's Definition of Done treats a stale or incomplete `MIGRATION.md` as a
program-level failure, and STATE.md records an explicit instruction from the Phase 25 close: add
`scripts/check-api-surface.sh` to the gate list from Phase 26 on. This plan is where both become
true.

Output: `docs/src/user-guides/agent-runtime.md` and its registration; `MIGRATION.md` §§9.1–9.5
complete for this phase; a regenerated `.project/current-exports.txt`; `CHANGELOG.md`;
`08-traceability-matrix.md` anchors; a filled `26-VALIDATION.md`; the gate evidence block in the
SUMMARY.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-agent-runtime-enhancements/26-CONTEXT.md
@.planning/phases/26-agent-runtime-enhancements/26-RESEARCH.md
@.planning/phases/26-agent-runtime-enhancements/26-VALIDATION.md
@.project/v0.10.0/05-agent-runtime-enhancements.md
@.project/v0.10.0/00-program-overview.md
@MIGRATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The agent-runtime guide and the docs sweep</name>
  <files>docs/src/user-guides/agent-runtime.md, docs/src/SUMMARY.md, docs/src/user-guides/tool-integration.md, docs/src/user-guides/garrison-memory.md</files>
  <read_first>
    - docs/src/user-guides/fault-tolerance.md — the whole page: its structure, its `{{#include}}` usage against `crates/doc-examples/src/fault_tolerance.rs`, its table style and its length; this is the shape to follow
    - docs/src/user-guides/parley-and-chronicle.md — the second instance of the same shape, for what varies between pages
    - docs/src/SUMMARY.md:25 — the slot immediately after the fault-tolerance page where the new entry goes
    - crates/doc-examples/src/agent_runtime.rs — the `// ANCHOR:` regions plan 26-20 created, which this page includes rather than duplicates
    - crates/paladin-battalion/src/engine/hooks.rs and crates/paladin-ports/src/output/paladin_port.rs — the two-layer rustdoc plan 26-01 wrote; the guide's table is the prose version of the same content and must not contradict it
    - crates/paladin-ports/src/output/vault_port.rs — the Vault/Garrison/Waypoint table already on the trait, which the guide's version must match
    - .planning/phases/26-agent-runtime-enhancements/COVERAGE.md — the decided per-provider `response_format` matrix, the source for the guide's native-JSON-mode table
    - docs/src/getting-started/configuration.md — the Ollama recipe plan 26-14 wrote; the guide links to it rather than repeating it
    - docs/src/user-guides/tool-integration.md and docs/src/user-guides/garrison-memory.md — where the two pointer paragraphs go
  </read_first>
  <action>
Create `docs/src/user-guides/agent-runtime.md`, titled "Agent Runtime: Middleware, Context
Management, Vault Memory, Structured Output and the Reasoning Agent", and register it in
`docs/src/SUMMARY.md` immediately after the fault-tolerance page (:25). Follow
`fault-tolerance.md`'s structure and length — this is a user guide, not a specification restatement.

Six things must be in it (D-38), each with exactly one written home:

1. **The two-layer table** (D-05): `NodeInterceptor` wraps the whole node, once per Aegis attempt,
   inside the Aegis; `ExecutionMiddleware` runs inside the reasoning loop, per model call and per
   tool call. Rows for scope, frequency, owning crate and what each can do. It must agree with the
   rustdoc plan 26-01 put on both traits — read both before writing.
2. **The Vault / Garrison / Waypoint table** (D-19): the same five columns as the `VaultPort`
   rustdoc — scope, lifetime, addressed by, who writes, typical content.
3. **The per-provider native-JSON-mode table** (D-28): which providers use a native
   `response_format` and in which form, and that Anthropic has none and relies on the instruction
   block. Take the rows from `COVERAGE.md` so the guide and the coverage matrix cannot drift.
4. **The Ollama recipe** (D-32): a short pointer to
   `docs/src/getting-started/configuration.md`, not a copy.
5. **The `reasoning_agent` example** (D-35): `{{#include}}` the `reasoning_agent` ANCHOR region from
   `crates/doc-examples/src/agent_runtime.rs`. Never paste the code — the include is what keeps the
   guide compiling in CI.
6. **The tool-call protocol** (D-36): the documented envelope, that it is prompt-level rather than
   wire-level, that ADR-0042's native capability stays deferred, and that both protocol middlewares
   are opt-in.

Also cover, briefly: how to install a chain (`with_middleware` / `with_middleware_chain` /
`build_chain`), that every built-in is inert by default, and the security framing a user needs —
recalled Vault content and fed-back tool errors are data, not instructions, and anything an agent
`vault_put`s inherits M-B-04's raw-content warning (D-41).

Then the two pointer edits: `docs/src/user-guides/tool-integration.md` gains a paragraph pointing at
the tool-call protocol and at `InProcessArsenal` as the way to give an agent an executable tool;
`docs/src/user-guides/garrison-memory.md` gains a paragraph on summaries — what `is_summary` means,
that summaries compound latest-wins, and that old summaries remain in the store by design because
`GarrisonPort` has no delete.

Finally, sweep the rustdoc: every new public item from all twenty preceding plans has rustdoc and a
doc test using `?`. `cargo doc --workspace --no-deps` must build with no **new** broken intra-doc
link — note the pre-existing `qdrant --all-features` rustdoc break from `22-deferred-items.md` item 1
as out of scope rather than fixing it here.
  </action>
  <verify>
    <automated>cargo doc --workspace --no-deps &amp;&amp; cargo check -p paladin-doc-examples &amp;&amp; grep -q 'agent-runtime.md' docs/src/SUMMARY.md &amp;&amp; grep -q '{{#include' docs/src/user-guides/agent-runtime.md &amp;&amp; cargo test -p paladin-ai --doc</automated>
  </verify>
  <acceptance_criteria>
    - `docs/src/user-guides/agent-runtime.md` exists and is registered: `grep -c 'agent-runtime.md' docs/src/SUMMARY.md` is `1`
    - All six required subjects are present: `grep -c 'NodeInterceptor' docs/src/user-guides/agent-runtime.md` is at least `1`; likewise for `Waypoint`, `response_format`, `ollama`, `reasoning_agent` and `ADR-0042`
    - The example is included, not pasted: `grep -c '{{#include' docs/src/user-guides/agent-runtime.md` is at least `1` and `grep -c 'MockLlmAdapter::with_responses' docs/src/user-guides/agent-runtime.md` is `0`
    - The Ollama recipe is linked, not duplicated: `grep -c 'getting-started/configuration' docs/src/user-guides/agent-runtime.md` is at least `1` and `grep -c 'ollama pull' docs/src/user-guides/agent-runtime.md` is `0`
    - `docs/src/user-guides/tool-integration.md` contains `InProcessArsenal`
    - `docs/src/user-guides/garrison-memory.md` contains `is_summary`
    - `cargo doc --workspace --no-deps` exits 0 with no new broken intra-doc link
    - `cargo check -p paladin-doc-examples` exits 0
    - `cargo test -p paladin-ai --doc` exits 0
  </acceptance_criteria>
  <done>One new guide page carries all six tables and sections with the example included rather than duplicated, the two sibling pages point at the new capabilities, and `cargo doc` builds clean.</done>
</task>

<task type="auto">
  <name>Task 2: MIGRATION.md completion, CHANGELOG, traceability anchors and the regenerated export file</name>
  <files>MIGRATION.md, CHANGELOG.md, .project/current-exports.txt, .project/v0.10.0/08-traceability-matrix.md</files>
  <read_first>
    - MIGRATION.md — the whole file: §9.1's M-B-03 row (rewritten by plan 26-19), §9.2's rows including the three now-resolved RT rows and the `PaladinError` / `PaladinPort` rows to extend, the Phase 23/24/25 deliberate-zero notes (the form to copy), and §§9.3 (line 153), 9.4 (163) and 9.5 (174)
    - .cargo/semver-checks-allowlist.toml — the three entries plans 26-03, 26-05 and 26-07 added, for the set-equality check against §9.2's `Y` rows
    - scripts/extract-public-api.sh and scripts/check-api-surface.sh — how the export file is generated and compared, and the exact invocation `.github/workflows/ci.yml:193-216` uses
    - .project/current-exports.txt — the file last regenerated at plan 23-12, which Phases 24 and 25 left 123 items stale before `0e5c106c` fixed it; this phase adds more
    - .project/v0.10.0/08-traceability-matrix.md — rows G-13 (RT-FR-09), G-16 … G-19, G-21 and G-22, which need test anchors
    - CHANGELOG.md — the `[Unreleased]` section and the entry style prior phases used
    - .planning/STATE.md — the Phase 25 close-out carried concern instructing that `scripts/check-api-surface.sh` join the gate list from Phase 26 on
    - Every `26-*-SUMMARY.md` produced by plans 26-01 … 26-20 — the source of truth for what actually landed, rather than what this plan assumes landed
  </read_first>
  <action>
**`MIGRATION.md` §9.2.** The three `Y` rows (`StopReason`, `LlmRequest`, `GarrisonEntry`) were
resolved by their owning plans; verify each is present and correct rather than rewriting it. Then do
this plan's own share:

- Extend the `PaladinError` row's Change cell with the three variants this phase added:
  `StructuredOutputInvalid`, `GuardrailTripped`, `ArmamentFailed`. The enum is already
  non-exhaustive, so this is a Change-cell extension and **not** a new row (D-09, D-26, D-33).
- Extend the `PaladinPort` row with the `execute_scoped` default-method line marked `N` — a
  defaulted method is not breaking (D-21).
- List the four new traits for completeness with `N/A, new`: `ExecutionMiddleware`,
  `TokenCounterPort`, `VaultPort`, `StructuredExecutorPort`.
- Write one **deliberate-zero note**, in the Phase 23/24/25 form, for every new-in-0.10 type this
  phase touched: `NodeSpec::Paladin.output_schema`, `NodeContext.vault`, the new `EngineError`
  variants, the `WarGraph` builders plus fingerprint `v6`, `DirectiveParser` internals, and
  `MockLlmAdapter`'s recorded `response_format` (D-37).

**§9.3.** Record `schemars` as a new **direct facade** dependency at `"1.2"`, already resolved at
`1.2.1` in `Cargo.lock` via `rmcp 2.1.0`, so **no new package** enters the graph; MSRV stays 1.88 and
the `msrv` job (`ci.yml:251`) is the proof (X-11.1). Record that **no new cargo feature** was
introduced: the tiktoken counter rides `content-processing`, `SqliteVault` rides `sqlite`, and
`SemanticVault` is ungated.

**§9.4.** Record `002_add_garrison_is_summary.sql` and `003_create_vault_tables.sql`: backend
SQLite (`paladin-memory`), applied automatically at adapter construction, no down migration,
negligible growth, and the two structural facts — the embedded compile-time migrator replaced the
CWD-relative runtime path, and the root `migrations/` copy plus its two `Dockerfile` `COPY` lines
were removed. Restate that existing Citadel JSON/SQLite state files remain readable unchanged.

**§9.5.** Record `AgentRuntimeConfig`, its twelve sub-structs and every `APP_AGENT_RUNTIME_*`
variable with its default, and the claim that all of it is inert by default — backed by plan 26-02's
`default_agent_runtime_config_is_inert` and plan 26-20's `build_chain_on_a_default_config_returns_an_empty_chain`.

**§9.1.** Confirm M-B-03 reads as plan 26-19 rewrote it (D-33) and carries no TBD.

Leave every TBD owned by Phase 27, 28 or 29 exactly as it is.

**`.project/current-exports.txt`.** Run `./scripts/extract-public-api.sh .project/current-exports.txt`
and then `./scripts/check-api-surface.sh .project/current-exports.txt`. This is the Phase 25 carried
concern: the file was last regenerated at plan 23-12, two phases added 123 public items without
regenerating it, and the `api-surface` job was red for two phases as a result. Regenerating it here,
and running the check, is a named deliverable — not a side effect.

**`CHANGELOG.md`.** Add the phase's entry under `[Unreleased]` in the established style: the
middleware chain, the built-ins, context management, the Vault, structured output, the conformance
close-out and the preset, plus the three X-10 changes with their mitigations.

**`08-traceability-matrix.md`.** Add test anchors to rows G-13 (RT-FR-09), G-16 … G-19, G-21 and
G-22, naming the actual test functions the twenty preceding plans created. Read the SUMMARYs for the
real names rather than guessing — an anchor that names a test that does not exist is worse than no
anchor.
  </action>
  <verify>
    <automated>test "$(grep -c 'TBD' MIGRATION.md)" = "$(git show HEAD~1:MIGRATION.md 2>/dev/null | grep -c 'TBD' || echo 999)" -o true; ./scripts/extract-public-api.sh .project/current-exports.txt &amp;&amp; ./scripts/check-api-surface.sh .project/current-exports.txt &amp;&amp; test "$(grep -c '^\[\[entry\]\]' .cargo/semver-checks-allowlist.toml)" -ge 3 &amp;&amp; grep -q 'schemars' MIGRATION.md &amp;&amp; grep -q '003_create_vault_tables.sql' MIGRATION.md &amp;&amp; grep -q 'APP_AGENT_RUNTIME' MIGRATION.md</automated>
  </verify>
  <acceptance_criteria>
    - No RT-owned TBD remains: `grep -n 'TBD' MIGRATION.md | grep -ci 'RT-0\|Phase 26'` is `0`
    - The `PaladinError` row lists all three new variants: `grep -c 'StructuredOutputInvalid' MIGRATION.md` is at least `1`, likewise `GuardrailTripped` and `ArmamentFailed`
    - The `PaladinPort` row records the defaulted method: `grep -c 'execute_scoped' MIGRATION.md` is at least `1`
    - The four new traits are listed: `grep -c 'ExecutionMiddleware\|TokenCounterPort\|VaultPort\|StructuredExecutorPort' MIGRATION.md` is at least `4`
    - Deliberate-zero notes cover all six new-in-0.10 items: `grep -c 'output_schema\|NodeContext.vault\|EngineError\|fingerprint\|DirectiveParser\|MockLlmAdapter' MIGRATION.md` is at least `6`
    - §9.3 records the dependency and the MSRV: `grep -c 'schemars' MIGRATION.md` is at least `1` and `grep -c '1.88' MIGRATION.md` is at least `1`
    - §9.4 records both migrations: `grep -c '002_add_garrison_is_summary.sql' MIGRATION.md` and `grep -c '003_create_vault_tables.sql' MIGRATION.md` are each at least `1`
    - §9.5 records the config surface: `grep -c 'APP_AGENT_RUNTIME' MIGRATION.md` is at least `1`
    - `./scripts/check-api-surface.sh .project/current-exports.txt` exits 0 with the file regenerated in this commit: `git diff --name-only HEAD~1 -- .project/current-exports.txt | wc -l` is `1`
    - The allowlist holds exactly the three entries this phase justifies: `grep -c '^\[\[entry\]\]' .cargo/semver-checks-allowlist.toml` equals the count of `Y` rows in §9.2 attributable to Phase 26 plus any pre-existing entries
    - `CHANGELOG.md` `[Unreleased]` mentions the middleware chain, the Vault and structured output
    - `.project/v0.10.0/08-traceability-matrix.md` rows G-13, G-16, G-17, G-18, G-19, G-21 and G-22 each name at least one real test function
  </acceptance_criteria>
  <done>Every `MIGRATION.md` section this phase owns is complete with no RT-owned TBD, the semver allowlist and §9.2's `Y` rows agree, the public-API export file is regenerated and its check passes, and the traceability rows name tests that actually exist.</done>
</task>

<task type="auto">
  <name>Task 3: Gate evidence, the security-posture verification and the validation strategy</name>
  <files>.planning/phases/26-agent-runtime-enhancements/26-VALIDATION.md</files>
  <read_first>
    - .planning/phases/26-agent-runtime-enhancements/26-VALIDATION.md — the seeded template's five sections to fill: Test Infrastructure, Sampling Rate, the Per-Task Verification Map, Wave 0 Requirements and Manual-Only Verifications
    - Every `.planning/phases/26-agent-runtime-enhancements/26-*-PLAN.md` — the source of the Per-Task Verification Map: each task's id, plan, wave, requirement, threat ref and `<automated>` command
    - .planning/phases/26-agent-runtime-enhancements/26-RESEARCH.md "Validation Architecture" — the framework table, the quick and full commands, the sampling rate and the Wave 0 gap list already drafted there
    - .planning/phases/25-node-level-fault-tolerance/25-VALIDATION.md — a filled example from the previous phase, for the level of detail expected
    - .planning/phases/26-agent-runtime-enhancements/26-CONTEXT.md D-37, D-39 and D-41 — the gate list, the test tiers and the security posture this task verifies rather than restates
    - .github/workflows/ci.yml — the `api-surface` :193, `msrv` :251, `semver` :303, `ollama-integration` :748 and `coverage` :979 jobs, so the evidence block names what actually runs
    - .planning/phases/25-node-level-fault-tolerance/25-SECURITY.md — the shape of a phase security record, for the posture verification block
  </read_first>
  <action>
**Fill `26-VALIDATION.md`.** Replace every placeholder with the real values:

- *Test Infrastructure*: framework `cargo test` (no config file); quick run
  `cargo test -p <touched-crate> --lib`; full suite `make test-all`; note the house rules that make a
  command valid here — the core package is `paladin-ai-core` (not `paladin-core`),
  `cargo test --workspace --tests <filter>` rather than `--all-targets <filter>` because the latter
  builds `benches/config_benchmarks.rs` which panics at startup, feature-gated tests need
  `--all-features` or explicit features, and a filter that selects zero tests exits 0 and proves
  nothing.
- *Sampling Rate*: per task commit, per wave, before verification — taken from 26-RESEARCH's
  Validation Architecture section.
- *Per-Task Verification Map*: one row per task across all 21 plans, with its plan, wave,
  requirement, threat ref, expected secure behaviour, test type, the exact `<automated>` command and
  whether the test file exists yet. Read the plans for these; do not invent rows.
- *Wave 0 Requirements*: the new test surfaces 26-RESEARCH lists — the middleware module tree, the
  vault contract suite, `conformance.rs`, `doc-examples/src/agent_runtime.rs`, and the four new
  `tests/integration/` files — each mapped to the plan that creates it.
- *Manual-Only Verifications*: the Qdrant-backed `SemanticVault` tier (UAT, D-24) and the live Ollama
  tier (CI `ollama-integration`, D-32), each with why it is manual and how to run it. Nothing else
  should be here — everything RT adds is Tier 1 (D-39).
- Set the frontmatter `status: validated` and `nyquist_compliant: true` **only if** every task in
  every plan has an `<automated>` command and no three consecutive tasks lack one. If any task does
  not, leave `nyquist_compliant: false`, list the gaps, and say so in the SUMMARY.

**Produce the gate evidence** in the SUMMARY, running each and recording the result verbatim
(D-37):

- `cargo semver-checks` against v0.9.0 — only the three allowlisted findings
- the `msrv` job's command at Rust 1.88
- `make security` (cargo-audit + cargo-deny)
- `cargo clippy --workspace --all-targets --all-features -- -D warnings`
- coverage ≥ 82 % via `cargo llvm-cov --fail-under-lines 82` (ADR-0006)
- `./scripts/extract-public-api.sh .project/current-exports.txt` and
  `./scripts/check-api-surface.sh .project/current-exports.txt`

Note the two known local conditions rather than working around them: `cargo test --workspace
--all-features` always fails `cli_isolation::test_cli_feature_is_not_default` by design (it requires
`cli` off), and Docker is unavailable, so the Qdrant and live-Ollama tiers are read from their CI
jobs and never marked passed locally.

**Verify the security posture** D-41 describes, item by item, and record it as a block in the
SUMMARY — verified, not restated: namespace traversal closed by construction with the attack tests
from plans 26-04, 26-13 and 26-16; recalled Vault content and fed-back tool errors framed as data
with named constants; redact-then-bound on every model-facing and error-facing string (plans 26-14,
26-19); `Guardrail` patterns compiled under an explicit size bound (plan 26-08); no secret in
`AgentRuntimeConfig` and provider names only in `ModelFallbackConfig` (plans 26-02, 26-10);
`response_format` schemas never logged with request bodies; Vault values size-bounded and JSON-only;
R-23-01 (hanging `EdgeConditionEvaluator`) re-listed as accepted; and the hanging-middleware bound
(node Aegis `run_timeout` / the service's per-run timeout) stated rather than solved.
  </action>
  <verify>
    <automated>cargo clippy --workspace --all-targets --all-features -- -D warnings &amp;&amp; cargo fmt --check &amp;&amp; make security &amp;&amp; ./scripts/check-api-surface.sh .project/current-exports.txt &amp;&amp; grep -q 'nyquist_compliant' .planning/phases/26-agent-runtime-enhancements/26-VALIDATION.md &amp;&amp; test "$(grep -c '{command}' .planning/phases/26-agent-runtime-enhancements/26-VALIDATION.md)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `26-VALIDATION.md` has no template placeholders left: `grep -c '{command}\|{N}\|{path or\|REQ-{XX}\|{behavior}' .planning/phases/26-agent-runtime-enhancements/26-VALIDATION.md` is `0`
    - The Per-Task Verification Map covers every plan: `grep -c '| 26-' .planning/phases/26-agent-runtime-enhancements/26-VALIDATION.md` is at least `21`
    - Manual-only rows are exactly the two CI/UAT tiers: `grep -c 'Qdrant\|Ollama' .planning/phases/26-agent-runtime-enhancements/26-VALIDATION.md` is at least `2`
    - The frontmatter is set honestly: `status:` is `validated` and `nyquist_compliant:` is `true` only if the SUMMARY records zero tasks without an automated verify
    - `cargo clippy --workspace --all-targets --all-features -- -D warnings` exits 0
    - `cargo fmt --check` exits 0
    - `make security` exits 0
    - `./scripts/check-api-surface.sh .project/current-exports.txt` exits 0
    - The SUMMARY records `cargo semver-checks` output showing only allowlisted findings
    - The SUMMARY records a coverage figure ≥ 82 % from `cargo llvm-cov`
    - The SUMMARY's security block addresses all nine D-41 items with evidence (a test name, a file, or an explicit acceptance)
  </acceptance_criteria>
  <done>The validation strategy is filled from the real plans with no placeholders, every gate has recorded evidence including the previously-red `api-surface` check, and the D-41 security posture is verified item by item rather than restated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Confirm the documentation, the migration register and the gate evidence before sealing</name>
  <what-built>The `agent-runtime.md` guide with all six tables; `MIGRATION.md` §§9.1–9.5 complete for Phase 26 including the three resolved `Y` rows, the two extended rows, four new-trait listings and six deliberate-zero notes; a regenerated `.project/current-exports.txt` with a passing `check-api-surface.sh`; `CHANGELOG.md`; traceability anchors on G-13 and G-16…G-22; a filled `26-VALIDATION.md`; and the gate-evidence and security-posture blocks in the SUMMARY.</what-built>
  <how-to-verify>
    1. Read `docs/src/user-guides/agent-runtime.md` end to end. Confirm all six subjects are present, the `reasoning_agent` example is an `{{#include}}` rather than pasted code, and the Ollama section links rather than duplicates. Build the book locally if convenient.
    2. Read `MIGRATION.md` §§9.1–9.5. Confirm no RT-owned `TBD` remains, the three `Y` rows match `.cargo/semver-checks-allowlist.toml` exactly in both directions, and every new-in-0.10 type touched has a deliberate-zero note.
    3. Confirm `.project/current-exports.txt` was regenerated **in this phase** (`git log -1 --stat -- .project/current-exports.txt`) and that `./scripts/check-api-surface.sh .project/current-exports.txt` exits 0 — this is the Phase 25 carried concern and the reason the `api-surface` job was red for two phases.
    4. Read the gate-evidence block in `26-21-SUMMARY.md`. Confirm every gate has a recorded result: `cargo semver-checks` (only allowlisted findings), `msrv` at 1.88, `make security`, clippy, coverage ≥ 82 %, and the api-surface pair. Confirm the Qdrant and live-Ollama tiers are recorded as CI/UAT-routed, not as local passes.
    5. Read the security-posture block. Confirm each of D-41's nine items carries evidence — a test name, a file, or an explicit acceptance — rather than a restatement of the decision.
    6. Read `26-VALIDATION.md`. Confirm `nyquist_compliant` matches reality: if any task in any plan lacks an automated verify, it must be `false` with the gaps listed.
  </how-to-verify>
  <resume-signal>Type "approved" to seal the phase's documentation and gate evidence, or describe what is missing</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the phase's own claims → the release record | `MIGRATION.md`, the changelog and the traceability matrix are what a downstream user and the verification pass rely on |
| the semver allowlist → the CI gate | An allowlist entry suppresses a real API-compatibility finding |
| the public-API export file → the `api-surface` job | A stale export file makes a green job meaningless |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-66 | Repudiation | an incomplete or stale `MIGRATION.md` | high | mitigate | Every section this phase owns is completed here and the acceptance criteria grep for each obligation individually; the program overview treats a stale `MIGRATION.md` as a program-level failure, and TBDs owned by Phases 27-29 are explicitly left alone rather than silently absorbed |
| T-26-67 | Repudiation | a semver allowlist wider than its justification | high | mitigate | The CI `semver` job checks set-equality between the allowlist and §9.2's `Y` rows in both directions; this plan verifies exactly the three entries plans 26-03, 26-05 and 26-07 added and adds none of its own (X-10.5) |
| T-26-68 | Repudiation | a green `api-surface` job proving nothing | high | mitigate | The export file is **regenerated** in this commit and the check run against it, with an acceptance criterion asserting the file appears in this commit's diff — the exact failure that left the job red for two phases after plan 23-12 (STATE.md, D-37) |
| T-26-69 | Repudiation | a traceability anchor naming a test that does not exist | medium | mitigate | The anchors are read from the twenty preceding SUMMARYs rather than guessed, and the acceptance criteria require each of the seven rows to name at least one real test function |
| T-26-70 | Repudiation | claiming a tier passed that could not run locally | medium | mitigate | Docker is unavailable in this devcontainer: the Qdrant `SemanticVault` tier is UAT-routed and the live Ollama tier is CI-only, both recorded as such in `26-VALIDATION.md`'s Manual-Only section and in the SUMMARY, never as local passes (D-24, D-32, D-39) |
| T-26-05 | Denial of Service | a hanging `ExecutionMiddleware` hook | medium | accept | Re-listed here as the phase's one accepted security item alongside R-23-01: a hook is bounded only by the node's Aegis `run_timeout` and the service's per-run timeout, and a per-hook timeout is a named later decision (D-41) |
| T-26-SC | Tampering | cargo installs across the phase | high | mitigate | One direct dependency was added across the whole phase — `schemars = "1.2"`, verdict `OK` in 26-RESEARCH.md's Package Legitimacy Audit, already resolved at `1.2.1` via `rmcp 2.1.0`, adding no lockfile package. No `[ASSUMED]`, `[SUS]` or `[SLOP]` package exists, so no legitimacy checkpoint was required. `make security` (cargo-audit + cargo-deny) is in this plan's gate evidence. |
</threat_model>

<verification>
- `cargo doc --workspace --no-deps` green with no new broken intra-doc links
- `cargo check -p paladin-doc-examples` green
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` and `cargo fmt --check` green
- `make security` green
- `cargo semver-checks` against v0.9.0 reporting only the three allowlisted findings
- The `msrv` job's command green at Rust 1.88
- `cargo llvm-cov --fail-under-lines 82` green (ADR-0006)
- `./scripts/extract-public-api.sh .project/current-exports.txt` then
  `./scripts/check-api-surface.sh .project/current-exports.txt` green, with the export file
  regenerated in this commit
- Known local conditions recorded, not worked around: `cli_isolation::test_cli_feature_is_not_default`
  fails under `--all-features` by design; Docker-dependent tiers are read from CI
</verification>

<success_criteria>
The phase is documented in one new guide page carrying all six required tables with its example
included rather than duplicated; `MIGRATION.md` §§9.1–9.5 are complete for Phase 26 with no RT-owned
TBD and a semver allowlist that matches §9.2 exactly; the public-API export file is regenerated and
its check passes, closing the Phase 25 carried concern; `26-VALIDATION.md` is filled from the real
plans with an honest `nyquist_compliant` flag; and every gate plus every D-41 security item has
recorded evidence, confirmed by a human.
</success_criteria>

## Artifacts this phase produces

New symbols and files created by Phase 26 (excluded from drift verification — none exist in the tree
before this phase):

- **Traits:** `ExecutionMiddleware`, `TokenCounterPort`, `VaultPort`, `StructuredExecutorPort`,
  `StructuredExecutorExt`, `StructuredSchema`, `ConformanceFixture`
- **Types:** `MiddlewareFlow`, `ToolFlow`, `FinalResult`, `ModelCallContext`, `ToolCallContext`,
  `ToolCallKind`, `LlmResponseView`, `PromptAssembly`, `PromptSection`, `SectionPlacement`,
  `ModelCallLimit`, `TokenBudget`, `ToolCallLimit`, `Guardrail`, `GuardrailRule`, `GuardrailTarget`,
  `GuardrailMatcher`, `GuardrailAction`, `HistoryTrimmer`, `SummarizationMiddleware`,
  `VaultRecallMiddleware`, `ModelRetryMiddleware`, `ModelFallbackMiddleware`,
  `ToolCallProtocolMiddleware`, `FinishOnPlainAnswerMiddleware`, `AgentRuntimeConfig` and its twelve
  sub-structs, `AgentRuntimeDeps`, `ToolErrorMode`, `HeuristicTokenCounter`, `Namespace`,
  `VaultRecord`, `ScoredVaultRecord`, `Page`, `VaultError`, `InMemoryVault`, `SqliteVault`,
  `SemanticVault`, `ConfinedVault`, `RunScope`, `InProcessArsenal`, `CompositeArsenalPort`,
  `VaultTools`, `Structured<T>`, `StructuredOptions`, `SchemaRef`, `ShapeError`, `TypedSchema<T>`,
  `ResponseFormat`, `ReasoningAgent`, `ReasoningAgentOptions`, `Wire`
- **Macros:** `llm_conformance_suite!`
- **Functions:** `extract_json`, `shape_check`, `render_instruction_block`, `run_structured`,
  `reasoning_agent`, `redact_secret_patterns`, `AgentRuntimeConfig::build_chain`,
  `ModelFallbackConfig::resolve_chain`, `RetryPredicate::admits`, `ModelCallContext::effective_llm`,
  `GarrisonEntry::summary`, `LlmRequest::new` and its four `with_*` builders,
  `ToolResultFormatter::format_error`, `Namespace::new`/`parse`/`is_prefix_of`,
  `paladin_memory::migrations::run_migrations`
- **Enum variants:** `StopReason::CallLimit`, `StopReason::TokenBudget`,
  `PaladinError::StructuredOutputInvalid`, `PaladinError::GuardrailTripped`,
  `PaladinError::ArmamentFailed`, and the `EngineError` variants `StructuredExecutorMissing`,
  `UnregisteredOutputSchema`, `OutputSchemaWithStructuredDirective`, `OutputSchemaFieldNotJson`
- **Struct fields:** `GarrisonEntry.is_summary`, `LlmRequest.response_format`,
  `NodeSpec::Paladin.output_schema`, `NodeContext.vault`, `ModelCallContext.llm_override`,
  `ModelCallContext.retry_policy`
- **Methods on pre-existing types:** the six new `PaladinExecutionService::with_*` /
  `execute_scoped` methods, `PaladinPort::execute_scoped`, `WarEngine::with_vault` /
  `with_structured_executor` / `with_output_schema`, `NodeContext::vault`,
  `NodeSpec::with_output_schema`, `impl TokenCounterPort for TiktokenCounter`,
  `impl StructuredExecutorPort for PaladinExecutionService`
- **Constants:** `GRAPH_FINGERPRINT_VERSION` value `v6`, `GUARDRAIL_PATTERN_SIZE_LIMIT_BYTES`,
  `DEFAULT_MAX_VALUE_BYTES`, `paladin_memory::migrations::MIGRATOR`
- **Cargo:** `schemars = "1.2"` as a direct facade dependency (no new lockfile package, no new feature)
- **Env vars:** the `APP_AGENT_RUNTIME_<SECTION>_<FIELD>` family
- **Migrations:** `002_add_garrison_is_summary.sql`, `003_create_vault_tables.sql`
- **New files:** the `src/application/services/paladin/middleware/` module tree,
  `src/application/services/paladin/{structured,vault_confined}.rs`,
  `src/application/services/arsenal/{in_process_arsenal,composite_arsenal,vault_tools}.rs`,
  `src/config/agent_runtime.rs`, `src/presets/mod.rs`,
  `crates/paladin-core/src/platform/container/{vault,structured,run_scope}.rs`,
  `crates/paladin-ports/src/output/{vault_port,token_counter_port,structured_executor_port}.rs`,
  `crates/paladin-memory/src/vault/`, `crates/paladin-memory/src/token_counter/`,
  `crates/paladin-memory/src/migrations.rs`, `crates/paladin-llm/src/conformance.rs`,
  `crates/doc-examples/src/agent_runtime.rs`, `docs/src/user-guides/agent-runtime.md`, and the four
  new `tests/integration/` files
- **Removed:** the root `migrations/` copy and its two `Dockerfile` `COPY` lines (D-17)

<output>
Create `.planning/phases/26-agent-runtime-enhancements/26-21-SUMMARY.md` when done
</output>
