---
phase: 27-platform-api
plan: 06
type: execute
wave: 2
depends_on: []              # no code dependency; wave 2 by file ownership (sole src/config/ writer of the phase, sequenced after the tracer wave)
files_modified:
  - src/config/run_store.rs
  - src/config/run_queue.rs
  - src/config/run_worker.rs
  - src/config/run_stream.rs
  - src/config/assistants.rs
  - src/config/schedules.rs
  - src/config/webhooks.rs
  - src/config/mod.rs
autonomous: true
requirements: [PLAT-01, PLAT-02, PLAT-03, PLAT-04, PLAT-05]

must_haves:
  truths:
    - "Every new subsystem config struct defaults to OFF or to today's behaviour: `RunStoreConfig` → `Disabled`, `RunQueueConfig` → `InMemory`, `SchedulesConfig.enabled` → `false`, `WebhooksConfig.allow_private` → `false`; a v0.9 config file that never mentions them boots v0.10 with no run server wired (D-50, X-09)."
    - "Each struct implements `Default`, `validate() -> Result<(), String>` and `EnvOverridable`, mirroring `src/config/waypoint_store.rs` field-for-field; Postgres/Redis variants carry the NAME of the env var holding the URL, never the URL (D-50)."
    - "`RunWorkerConfig` exposes `lease_seconds` (default 60) and no separate heartbeat knob — the heartbeat is derived as lease/4 by the worker (D-10)."
    - "`Settings` (`src/config/settings.rs`) is not touched; the structs are read by `paladin-server` via `Default` + `apply_env_overrides()`, the Phase 24 precedent."
  artifacts:
    - path: "src/config/run_store.rs"
      provides: "RunStoreConfig, RunStoreBackend"
      contains: "pub enum RunStoreBackend"
    - path: "src/config/run_worker.rs"
      provides: "RunWorkerConfig"
      contains: "lease_seconds"
    - path: "src/config/webhooks.rs"
      provides: "WebhooksConfig"
      contains: "allow_private"
    - path: "src/config/mod.rs"
      provides: "module declarations + re-exports for the seven structs"
      contains: "pub mod run_store"
  key_links:
    - from: "src/config/run_store.rs"
      to: "src/config/env_utils.rs"
      via: "impl EnvOverridable + read_env"
      pattern: "impl EnvOverridable for RunStoreConfig"
---

<objective>
Land the seven X-09 config structs the platform API is gated by — `run_store`, `run_queue`,
`run_worker`, `run_stream`, `assistants`, `schedules`, `webhooks` — every one defaulting to disabled
or to today's behaviour, so SHIP-02's "v0.9 config boots v0.10 with all new subsystems off" is true by
construction (D-50).

Purpose: the server wiring (27-17) and every service option struct convert from these; landing them
early and alone keeps `src/config/mod.rs` a single-writer file across the phase.

Output: seven files under `src/config/` plus the `mod.rs` declarations and re-exports. §9.5 rows are
written by 27-17 when the env vars are actually read at startup.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-platform-api/27-CONTEXT.md
@.project/v0.10.0/00-program-overview.md
@CLAUDE.md

<interfaces>
From src/config/waypoint_store.rs (the template, read in full):
```rust
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(tag = "backend", rename_all = "snake_case")]
pub enum WaypointStoreBackend { Disabled, Sqlite { path: String }, Postgres { url_env: String } }
pub struct WaypointStoreConfig { pub backend: WaypointStoreBackend }
impl Default for WaypointStoreConfig { .. Disabled .. }
impl WaypointStoreConfig { pub fn validate(&self) -> Result<(), String> { .. } }
impl EnvOverridable for WaypointStoreConfig { fn apply_env_overrides(&mut self) { read_env::<String>("APP_WAYPOINT_STORE_BACKEND") .. } }
```
From src/config/env_utils.rs: `pub trait EnvOverridable { fn apply_env_overrides(&mut self); }`,
`pub fn read_env<T: FromStr>(var: &str) -> Option<T>`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `run_store`, `run_queue`, `run_worker`, `run_stream`</name>
  <files>src/config/run_store.rs, src/config/run_queue.rs, src/config/run_worker.rs, src/config/run_stream.rs, src/config/mod.rs</files>
  <read_first>
    - `src/config/waypoint_store.rs` (whole file, including its test module) — copy its shape,
      `validate()` messages, env-override parsing and tests.
    - `src/config/node_cache.rs` — the second tagged-enum precedent (Redis `url_env`).
    - `src/config/mod.rs` — declaration order and the `pub use` block.
    - `.planning/phases/27-platform-api/27-CONTEXT.md` D-10, D-15, D-26, D-50.
  </read_first>
  <behavior>
    - `RunStoreConfig::default().backend == RunStoreBackend::Disabled`; `validate()` rejects an empty
      sqlite path and an unset `url_env`; `APP_RUN_STORE_BACKEND=sqlite` + `APP_RUN_STORE_PATH` /
      `=postgres` + `APP_RUN_STORE_URL_ENV` override.
    - `RunQueueConfig::default().backend == RunQueueBackend::InMemory`; `Redis { url_env, key_prefix }`
      via `APP_RUN_QUEUE_BACKEND=redis`, `APP_RUN_QUEUE_URL_ENV`, `APP_RUN_QUEUE_KEY_PREFIX` (default
      `paladin:run_queue`); `validate()` rejects an unset `url_env`.
    - `RunWorkerConfig { concurrency: 4, lease_seconds: 60, min_probe_interval_ms: 1000 }` by default;
      `validate()` rejects `concurrency == 0`, `lease_seconds < 4` (the quarter must be ≥ 1 s) and
      `min_probe_interval_ms == 0`; env `APP_RUN_WORKER_CONCURRENCY`, `APP_RUN_WORKER_LEASE_SECONDS`,
      `APP_RUN_WORKER_MIN_PROBE_INTERVAL_MS`. No heartbeat field exists.
    - `RunStreamConfig { poll_interval_ms: 1000 }`; `validate()` rejects 0; env
      `APP_RUN_STREAM_POLL_INTERVAL_MS`.
    - A YAML/JSON document omitting every field deserialises to the defaults (`#[serde(default)]`).
  </behavior>
  <action>
    Create `src/config/run_store.rs` (`RunStoreConfig`, `RunStoreBackend { Disabled, Sqlite { path },
    Postgres { url_env } }`), `src/config/run_queue.rs` (`RunQueueConfig`, `RunQueueBackend { InMemory,
    Redis { url_env, key_prefix } }`), `src/config/run_worker.rs` (`RunWorkerConfig`), and
    `src/config/run_stream.rs` (`RunStreamConfig`). Each: module docs stating the disabled/today
    default and why (X-09, D-50), the derive set, `Default` as a manual impl, `validate()`,
    `EnvOverridable`, a doc test on the default (X-02), and a `#[cfg(test)]` module with the same
    test names as `waypoint_store.rs` (`default_is_disabled`, `validate_rejects_*`, `env_overrides_*`,
    `absent_section_deserializes_to_default`). Declare the four modules in `src/config/mod.rs` in
    alphabetical position and add `pub use` lines beside the `WaypointStoreConfig` re-export.
  </action>
  <acceptance_criteria>
    - `cargo test -p paladin-ai --lib config::run_store 2>&1 | grep -qE 'test result: ok\. [1-9][0-9]* passed'` and the same for `config::run_queue`, `config::run_worker`, `config::run_stream`.
    - `grep -c 'heartbeat' src/config/run_worker.rs` counts only rustdoc mentions of the derived ratio — `grep -v '^\s*//' src/config/run_worker.rs | grep -c 'pub heartbeat'` is 0.
    - `grep -c 'url_env' src/config/run_store.rs` is at least 2 and `grep -c 'url:' src/config/run_store.rs` is 0.
    - `grep -c 'pub mod run_store\|pub mod run_queue\|pub mod run_worker\|pub mod run_stream' src/config/mod.rs` is 4.
    - `git diff --stat src/config/settings.rs` is empty.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p paladin-ai --lib config::run_ 2>&amp;1 | grep -qE 'test result: ok\. (1[2-9]|[2-9][0-9]) passed'</automated>
  </verify>
  <done>The four run-side config structs exist, default off/today, validate, and read their env vars.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `assistants`, `schedules`, `webhooks`</name>
  <files>src/config/assistants.rs, src/config/schedules.rs, src/config/webhooks.rs, src/config/mod.rs</files>
  <read_first>
    - `src/config/run_store.rs` (Task 1) — keep the three new files stylistically identical.
    - `.planning/phases/27-platform-api/27-CONTEXT.md` D-32, D-36, D-42, D-43, D-50.
    - PRD 06 PLAT-FR-14 (retry counts, timeout) and PLAT-FR-15 (`webhook_allow_private` semantics).
  </read_first>
  <behavior>
    - `AssistantsConfig { expose_code_registry: true }` by default (D-32 — the one deliberate ON
      default, because it exposes read-only data and PLAT-FR-11's single discovery surface is only true
      when on); env `APP_ASSISTANTS_EXPOSE_CODE_REGISTRY`.
    - `SchedulesConfig { enabled: false, tick_interval_ms: 1000 }`; `validate()` rejects
      `tick_interval_ms == 0`; env `APP_SCHEDULES_ENABLED`, `APP_SCHEDULES_TICK_INTERVAL_MS`.
    - `WebhooksConfig { allow_private: false, max_attempts: 5, timeout_secs: 10 }`; `validate()`
      rejects `max_attempts == 0` and `timeout_secs == 0`; env `APP_WEBHOOKS_ALLOW_PRIVATE`,
      `APP_WEBHOOKS_MAX_ATTEMPTS`, `APP_WEBHOOKS_TIMEOUT_SECS`. Module docs state that `allow_private`
      is the ONLY override of the SSRF guard (D-42) and name the DNS-rebinding limitation.
  </behavior>
  <action>
    Create `src/config/assistants.rs`, `src/config/schedules.rs`, `src/config/webhooks.rs` in the same
    shape as Task 1's files, declare them in `src/config/mod.rs` and re-export. In `webhooks.rs`
    rustdoc, state plainly: rejected target classes (non-http(s), loopback, link-local, RFC1918,
    unique-local, unspecified, `169.254.169.254`), the write-time and send-time application, that
    redirects are never followed, and that resolve-then-connect pinning is not implemented (D-42).
  </action>
  <acceptance_criteria>
    - `cargo test -p paladin-ai --lib config::assistants 2>&1 | grep -qE 'test result: ok\. [1-9][0-9]* passed'`, same for `config::schedules` and `config::webhooks`.
    - `grep -c 'expose_code_registry: true' src/config/assistants.rs` is at least 1.
    - `grep -c 'enabled: false' src/config/schedules.rs` is at least 1.
    - `grep -c 'allow_private: false' src/config/webhooks.rs` is at least 1 and `grep -ci 'rebinding' src/config/webhooks.rs` is at least 1.
    - `grep -c 'pub mod assistants\|pub mod schedules\|pub mod webhooks' src/config/mod.rs` is 3.
    - `cargo clippy -p paladin-ai --all-targets -- -D warnings` and `cargo fmt --check` pass.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p paladin-ai --lib config::assistants 2>&amp;1 | grep -qE 'test result: ok\. [1-9][0-9]* passed' &amp;&amp; cargo test -p paladin-ai --lib config::schedules 2>&amp;1 | grep -qE 'test result: ok\. [1-9][0-9]* passed' &amp;&amp; cargo test -p paladin-ai --lib config::webhooks 2>&amp;1 | grep -qE 'test result: ok\. [1-9][0-9]* passed'</automated>
  </verify>
  <done>All seven X-09 config structs exist with today's-behaviour defaults, validation and env overrides.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Environment / config file → process | Operator-supplied values select backends and security posture (`allow_private`). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-06-01 | Information disclosure | Postgres/Redis URLs in config/`Debug`/logs | medium | mitigate | Backends carry `url_env` (the variable NAME) only; `validate()` checks presence without storing the value — the `waypoint_store.rs` pattern (primary boundary). |
| T-27-06-02 | Elevation of privilege | `allow_private` flipped on by accident | medium | mitigate | Defaults `false`; the env var is parsed as a strict boolean; rustdoc names the consequence. |
| T-27-06-03 | Denial of service | pathological worker/lease/timeout values | low | mitigate | `validate()` rejects zero concurrency, sub-4 s leases, zero intervals and zero attempts/timeouts. |
</threat_model>

<verification>
- `cargo test -p paladin-ai --lib config::` reports a non-zero passing count covering all seven modules.
- `cargo clippy -p paladin-ai --all-targets -- -D warnings`, `cargo fmt --check`.
- `git diff --stat src/config/settings.rs Cargo.lock` is empty.
</verification>

<success_criteria>
- Seven config structs, each `Default` + `validate()` + `EnvOverridable`, each defaulting to off or to today's behaviour.
- No secret ever lives on a config type.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

See the identical section in `27-02-PLAN.md` — the canonical list of every symbol, route, table,
migration, CI job, env var, dependency promotion and doc asset Phase 27 creates. This plan's own
contribution: `src/config/{run_store,run_queue,run_worker,run_stream,assistants,schedules,webhooks}.rs`
(`RunStoreConfig`, `RunStoreBackend`, `RunQueueConfig`, `RunQueueBackend`, `RunWorkerConfig`,
`RunStreamConfig`, `AssistantsConfig`, `SchedulesConfig`, `WebhooksConfig`) and the `APP_RUN_*`,
`APP_ASSISTANTS_*`, `APP_SCHEDULES_*`, `APP_WEBHOOKS_*` env vars.
</artifacts_produced>

<output>
Create `.planning/phases/27-platform-api/27-06-SUMMARY.md` when done.
</output>
