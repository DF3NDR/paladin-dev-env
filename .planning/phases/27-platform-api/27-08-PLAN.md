---
phase: 27-platform-api
plan: 08
type: execute
wave: 3
depends_on: [27-02, 27-04]
files_modified:
  - src/application/services/parley/adapter.rs
  - src/application/services/parley/mod.rs
  - crates/paladin-ports/src/input/parley_port.rs
  - crates/paladin-web/src/thread_controller.rs
  - crates/paladin-web/openapi.json
  - MIGRATION.md
autonomous: true
requirements: [PLAT-03]

must_haves:
  truths:
    - "`POST /v1/threads/{id}/resume` keeps Phase 24's published contract verbatim — `202`, `{ thread_id, state_url }`, 404 `ThreadNotFound`, 409 `ThreadNotAwaitingInput`/`GraphNotRegistered`, 400 for the four validation variants, 501 unwired — and only the mechanism behind `ParleyPort` changes (D-20)."
    - "A complete set of parley responses is validated synchronously through the existing shadow-validation path, then RE-ENQUEUED under the same `run_id` with `attempt` incremented, instead of spawned in-process (PLAT-FR-06, D-19, D-23); the queue message carries the pointer only and the responses ride on the run row (`pending_responses`)."
    - "The resume response gains `run_id`; `ResumeAccepted` and `ResumeAcceptedResponse` are `#[non_exhaustive]` with a construction path preserved, and both are registered in `MIGRATION.md` §9.2 with the endpoint change in §9.6 (D-21)."
    - "Resume never consults the busy-thread index — it updates the existing run row and never inserts (D-19)."
    - "When no run exists for the thread (a Phase 24 thread suspended before the run server existed) the adapter falls back to Phase 24's in-process background continuation and reports `run_id: null`, so the pre-existing behaviour is preserved (X-03)."
  artifacts:
    - path: "src/application/services/parley/adapter.rs"
      provides: "ParleyPortAdapter enqueue path replacing the tokio::spawn continuation"
      contains: "record_resume"
    - path: "crates/paladin-ports/src/input/parley_port.rs"
      provides: "ResumeAccepted with run_id, #[non_exhaustive]"
      contains: "non_exhaustive"
    - path: "crates/paladin-web/src/thread_controller.rs"
      provides: "ResumeAcceptedResponse.run_id"
      contains: "run_id"
  key_links:
    - from: "src/application/services/parley/adapter.rs"
      to: "crates/paladin-ports/src/output/run_queue_port.rs"
      via: "enqueue(QueuedRun { run_id, thread_id, attempt, .. }) after record_resume"
      pattern: "enqueue\\("
    - from: "crates/paladin-web/src/thread_controller.rs"
      to: "crates/paladin-ports/src/input/parley_port.rs"
      via: "ResumeAcceptedResponse::from(ResumeAccepted) copies run_id()"
      pattern: "run_id"
---

<objective>
Replace Phase 24's in-process background continuation with a durable re-enqueue under the same
`run_id`, keeping the published `202` resume contract byte-for-byte and adding `run_id` to the
response so a client can stream or poll the resumed run.

Purpose: PLAT-FR-06 — `POST /threads/{id}/resume` validates then enqueues a resume run reusing the
same `run_id` (`attempt++`). Phase 24 recorded this exact deferral ("PLAT-01…03 replace D-25's
in-process background task under the same 202 contract").

Output: the enqueue path in `ParleyPortAdapter`, `ResumeAccepted.run_id`, `ResumeAcceptedResponse.run_id`,
regenerated `openapi.json`, and `MIGRATION.md` §9.2 + §9.6 rows.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-platform-api/27-CONTEXT.md
@.planning/phases/24-pause-resume-history-graceful-shutdown/24-CONTEXT.md
@.planning/phases/27-platform-api/27-04-SUMMARY.md
@.project/v0.10.0/06-platform-api.md
@CLAUDE.md

<interfaces>
From src/application/services/parley/adapter.rs:96-175 — `ParleyPortAdapter<W>::resume_with`:
`latest` lookup → `registry.resolve(fingerprint)` → `shadow_validate` → `ShadowOutcome::{Rejected,
DelegateSynchronously, Complete}`; the `Complete` arm is the `coordinator.register()` +
`tokio::spawn(engine.resume_with(..))` site this plan converts.
From crates/paladin-ports/src/input/parley_port.rs:88 — `pub struct ResumeAccepted { thread_id }`,
`new(thread_id)`, `thread_id()`; `ParleyError` is already `#[non_exhaustive]`.
From crates/paladin-web/src/thread_controller.rs:360 — `ResumeAcceptedResponse { thread_id: String, state_url: String }`.
From 27-01/27-02: `RunRepositoryPort::active_run_for_thread(thread)`, `record_resume(run_id, responses) -> attempt`;
`RunQueuePort::enqueue(QueuedRun { run_id, thread_id, attempt, enqueued_at })`.
From 27-04's worker: `WorkerDispatch::decide(latest, pending)` → `ResumeWith(responses)` when the
run row carries `pending_responses`, so the worker completes what this adapter enqueues.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `ParleyPortAdapter` enqueues instead of spawning; `ResumeAccepted` gains `run_id`</name>
  <reversibility rating="costly">D-21: `ResumeAccepted`/`ResumeAcceptedResponse` are published shapes; the field and `#[non_exhaustive]` are registered in §9.2 and the response change in §9.6.</reversibility>
  <files>src/application/services/parley/adapter.rs, src/application/services/parley/mod.rs, crates/paladin-ports/src/input/parley_port.rs, crates/paladin-web/src/thread_controller.rs</files>
  <read_first>
    - `src/application/services/parley/adapter.rs` (whole file, including its test module and the
      `ParleyPortAdapter::new` constructor signature) and `parley/mod.rs`.
    - `crates/paladin-ports/src/input/parley_port.rs` lines 80-130 and 230-280.
    - `.planning/phases/24-pause-resume-history-graceful-shutdown/24-CONTEXT.md` D-25 (the status-code
      table that must not change).
    - `.planning/phases/27-platform-api/27-CONTEXT.md` D-19, D-20, D-21, D-22, D-23.
    - `MIGRATION.md` §9.2 (row shape) and §9.6.
  </read_first>
  <behavior>
    - `ShadowOutcome::Complete` with an active `AwaitingInput` run for the thread: the adapter calls
      `repo.record_resume(run_id, responses)` (attempt becomes n+1), then `queue.enqueue(QueuedRun
      { run_id, thread_id, attempt: n+1, .. })`, and returns `ResumeAccepted` with `run_id: Some(run_id)`;
      no `tokio::spawn` happens (a counting mock engine records zero `resume_with` continuation calls).
    - `ShadowOutcome::Complete` with NO run row for the thread: the Phase 24 spawn path runs unchanged
      and `run_id` is `None` (X-03 for pre-run-server threads; rustdoc'd as the legacy fallback).
    - `ShadowOutcome::Rejected` and `DelegateSynchronously` arms are byte-identical in behaviour to
      Phase 24 (their existing tests pass unchanged).
    - `record_resume` failing with `IllegalTransition` (the run is not `AwaitingInput` — e.g. a race
      with cancel) maps to `ParleyError::ThreadNotAwaitingInput { thread, status }`.
    - `resume_reenqueues_same_run_id`: end-to-end over InMemory stores + a real engine + a Gate graph
      (reuse 27-04's harness): submit → worker suspends (`AwaitingInput`, depth 0) → `resume_with`
      through the adapter → depth 1, the queued `run_id` equals the original, `attempt == 2` → worker
      iteration completes the run.
  </behavior>
  <action>
    Extend `ParleyPortAdapter` with two optional collaborators set through builder methods
    `with_run_repository(Arc<dyn RunRepositoryPort>)` and `with_run_queue(Arc<dyn RunQueuePort>)`
    (keeping `new(engine, store, registry, coordinator)` intact — X-03 for existing constructors). In
    the `Complete` arm: if both are set and `active_run_for_thread(thread)` returns a run in
    `AwaitingInput`, take the enqueue path; else the legacy spawn path. In `parley_port.rs` mark
    `ResumeAccepted` `#[non_exhaustive]`, add `run_id: Option<RunId>` with `pub fn run_id(&self) ->
    Option<&RunId>` and `pub fn with_run_id(self, run_id: RunId) -> Self` (construction path preserved
    via `new` + `with_run_id`), and a doc test. Add `#[non_exhaustive]` to `ResumeAcceptedResponse` in
    `thread_controller.rs` with `pub run_id: Option<String>` and a `pub fn new(thread_id, state_url,
    run_id)` constructor (builder-only construction, X-10.3); update the handler's conversion.
    Register `enum_marked_non_exhaustive`/`struct_marked_non_exhaustive` lints are already allowed for
    `paladin-ports`; check `crates/paladin-web/Cargo.toml` for the same metadata and add
    `struct_marked_non_exhaustive = "allow"` there if absent, mirrored in
    `.cargo/semver-checks-allowlist.toml` ONLY if the CI `semver` job's set-equality check requires an
    entry — read that job's script first; if `ResumeAcceptedResponse` is new-in-0.10 (it is: Phase 24)
    no allowlist entry is needed, and the §9.2 row says so explicitly. Write the tests above in
    `adapter.rs`'s test module and the E2E `resume_reenqueues_same_run_id` beside 27-04's harness
    (import it via `super::super::run::worker_tests::harness` if exposed `pub(crate)`, else duplicate
    the minimal graph builder).
  </action>
  <acceptance_criteria>
    - `cargo test -p paladin-ai --lib services::parley 2>&1 | grep -E 'test result: ok\. [1-9][0-9]* passed'` — the pre-existing count plus at least 4 new tests.
    - `cargo test -p paladin-ai --lib resume_reenqueues_same_run_id 2>&1 | grep -qE 'test result: ok\. 1 passed'`.
    - `grep -c 'record_resume' src/application/services/parley/adapter.rs` is at least 1 and `grep -c 'enqueue(' src/application/services/parley/adapter.rs` is at least 1.
    - `grep -B2 'pub struct ResumeAccepted' crates/paladin-ports/src/input/parley_port.rs | grep -c non_exhaustive` is 1.
    - `grep -B3 'pub struct ResumeAcceptedResponse' crates/paladin-web/src/thread_controller.rs | grep -c non_exhaustive` is 1.
    - `cargo test -p paladin-web --lib thread_controller 2>&1 | grep -qE 'test result: ok\. [1-9][0-9]* passed'` (the Phase 24 status-code table tests still pass).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p paladin-ai --lib resume_reenqueues_same_run_id 2>&amp;1 | grep -qE 'test result: ok\. 1 passed' &amp;&amp; cargo test -p paladin-web --lib thread_controller 2>&amp;1 | grep -qE 'test result: ok\. [1-9][0-9]* passed'</automated>
  </verify>
  <done>Resume re-enqueues the same run (`attempt++`) after synchronous validation, the legacy path
  survives for threads without a run, and both published types carry `run_id` non-exhaustively.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate `openapi.json`; register §9.2 and §9.6</name>
  <files>crates/paladin-web/openapi.json, MIGRATION.md</files>
  <read_first>
    - `Makefile` lines 367-371 (`make openapi`) and `crates/paladin-web/src/openapi.rs` 250-280.
    - `MIGRATION.md` §9.2 row format (lines 159-225) including the "new-in-0.10, X-10 does not apply"
      note style at line 192, and §9.6 (310-348).
    - `.planning/phases/27-platform-api/27-CONTEXT.md` D-21, D-48, D-53.
  </read_first>
  <action>
    Run `make openapi` (or `UPDATE_OPENAPI=1 cargo test -p paladin-web --lib
    openapi_matches_committed_baseline`) and commit the regenerated `crates/paladin-web/openapi.json`;
    diff-review it: the ONLY changes must be the `ResumeAcceptedResponse` schema gaining an optional
    nullable `run_id` string — record the diff summary in the SUMMARY (D-48). In `MIGRATION.md` §9.2
    add two rows: `paladin-ports` `ResumeAccepted` (field `run_id: Option<RunId>` + `#[non_exhaustive]`;
    mitigation: `new()` + `with_run_id()` construction path; new-in-0.10 so not deliberate-breaking
    versus v0.9.0 — allowlist entry not required) and `paladin-web` `ResumeAcceptedResponse` (same
    shape, `new()` constructor). In §9.6 add the row: `POST /v1/threads/{id}/resume` response body
    gains `run_id` (nullable — `null` only for threads suspended before a run store was configured),
    status table unchanged, owner PLAT-03.
  </action>
  <acceptance_criteria>
    - `cargo test -p paladin-web --lib openapi_matches_committed_baseline 2>&1 | grep -qE 'test result: ok\. 1 passed'`.
    - `grep -c '"run_id"' crates/paladin-web/openapi.json` is at least 1 within the `ResumeAcceptedResponse` schema (`python3 -c "import json;s=json.load(open('crates/paladin-web/openapi.json'));print('run_id' in s['components']['schemas']['ResumeAcceptedResponse']['properties'])"` prints `True`).
    - `awk '/^## 9.2/,/^## 9.3/' MIGRATION.md | grep -c 'ResumeAccepted'` is at least 2.
    - `awk '/^## 9.6/,/^## 9.7/' MIGRATION.md | grep -c 'run_id'` is at least 1.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p paladin-web --lib openapi_matches_committed_baseline 2>&amp;1 | grep -qE 'test result: ok\. 1 passed' &amp;&amp; awk '/^## 9.2/,/^## 9.3/' MIGRATION.md | grep -q 'ResumeAccepted'</automated>
  </verify>
  <done>The spec reflects the new field and the X-10 register carries both types and the endpoint change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → resume responses | Caller-supplied parley responses are validated before anything is persisted or enqueued. |
| Adapter → run row | Responses are persisted on the run row and later executed by a worker on any instance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-08-01 | Tampering | unvalidated responses reaching the engine | high | mitigate | Phase 24's `shadow_validate` runs synchronously before `record_resume`; the four 400 variants are unchanged. |
| T-27-08-02 | Tampering | resume racing a cancel / double resume | medium | mitigate | `record_resume` is a CAS on `status = 'awaiting_input'`; the loser gets `ThreadNotAwaitingInput` (409). |
| T-27-08-03 | Denial of service | resume creating a second active run on the thread | medium | mitigate | Resume is an `UPDATE` of the existing run — no insert, so the busy index is never a factor (D-19). |
| T-27-08-04 | Information disclosure | `run_id` in the response | low | accept | Run ids are UUID v7 handles to authenticated resources; disclosure to the resuming principal is the feature. |
</threat_model>

<verification>
- `cargo test -p paladin-ai --lib services::parley` and `cargo test -p paladin-web --lib` report non-zero counts.
- `cargo clippy --workspace --all-targets -- -D warnings`, `cargo fmt --check`.
- `git diff --stat crates/paladin-web/openapi.json` shows only the `run_id` addition.
</verification>

<success_criteria>
- Resume is durable (queue), same `run_id`, `attempt++`, contract unchanged, `run_id` exposed and registered.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

See the identical section in `27-02-PLAN.md` — the canonical list of every symbol, route, table,
migration, CI job, env var, dependency promotion and doc asset Phase 27 creates. This plan's own
contribution: `ParleyPortAdapter::with_run_repository`, `ParleyPortAdapter::with_run_queue`,
`ResumeAccepted::run_id`/`with_run_id`, `ResumeAcceptedResponse::run_id`/`new`, and the §9.2/§9.6 rows.
</artifacts_produced>

<output>
Create `.planning/phases/27-platform-api/27-08-SUMMARY.md` when done.
</output>
