---
phase: 27-platform-api
plan: 14
type: execute
wave: 6
depends_on: [27-11, 27-12]
files_modified:
  - crates/paladin-ports/src/input/schedule_admin_port.rs
  - crates/paladin-ports/src/input/mod.rs
  - src/application/services/run/schedule/service.rs
  - src/application/services/run/schedule/admin.rs
  - src/application/services/run/schedule/mod.rs
  - src/application/services/run/schedule/tests.rs
  - crates/paladin-web/src/schedule_controller.rs
  - crates/paladin-web/src/run_controller.rs
  - crates/paladin-web/src/lib.rs
  - crates/paladin-web/openapi.json
autonomous: true
requirements: [PLAT-05, PLAT-06]

must_haves:
  truths:
    - "`POST /schedules` `{ assistant_id, version?, cron, timezone?, input, enabled, thread_strategy?, on_missed?, webhook? }` validates the cron (5 or 6 fields), the timezone (IANA), the assistant reference and the webhook URL (SSRF guard, write time) BEFORE persisting, and computes the first `next_tick`; invalid → 400 with `details` (D-38, D-42)."
    - "`GET /schedules` (paginated), `GET /schedules/{id}` (includes `last_tick`, `next_tick`, `skipped_ticks` so 'why did nothing run' is answerable without a log dive — D-39), `PATCH /schedules/{id}` (partial update; a cron/timezone change recomputes `next_tick`), `DELETE /schedules/{id}` (204)."
    - "Schedule create/patch/delete require `require_admin`; reads require authentication only (D-46). Unwired → 501 naming `schedules.enabled`."
    - "The schedule admin port has no method the router cannot express and every mutating route regenerates `openapi.json` (D-48)."
  artifacts:
    - path: "crates/paladin-ports/src/input/schedule_admin_port.rs"
      provides: "ScheduleAdminPort, CreateRunSchedule, ScheduleAdminError"
      contains: "pub trait ScheduleAdminPort"
    - path: "src/application/services/run/schedule/admin.rs"
      provides: "impl ScheduleAdminPort for ScheduleService (validate → next_tick → persist)"
      contains: "impl ScheduleAdminPort"
    - path: "crates/paladin-web/src/schedule_controller.rs"
      provides: "/v1/schedules routes on RunApiState"
      contains: "patch_schedule"
  key_links:
    - from: "src/application/services/run/schedule/admin.rs"
      to: "src/application/services/run/webhook/ssrf.rs"
      via: "write-time SsrfGuard::check_url on webhook.url"
      pattern: "check_url"
    - from: "crates/paladin-web/src/schedule_controller.rs"
      to: "crates/paladin-ports/src/input/schedule_admin_port.rs"
      via: "RunApiState.schedules: Option<Arc<dyn ScheduleAdminPort>>"
      pattern: "dyn ScheduleAdminPort"
---

<objective>
Expose schedules over HTTP: an admin input port implemented by `ScheduleService`, the five schedule
routes on the shared `RunApiState`, admin scoping, and the write-time SSRF guard on schedule webhooks.

Purpose: the API-managed half of PLAT-FR-13 and part of PLAT-FR-16's "every new endpoint" coverage.

Output: `schedule_admin_port.rs`, `schedule/admin.rs`, `schedule_controller.rs`, `RunApiState.schedules`,
regenerated `openapi.json`.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-platform-api/27-CONTEXT.md
@.planning/phases/27-platform-api/27-11-SUMMARY.md
@.planning/phases/27-platform-api/27-12-SUMMARY.md
@.project/v0.10.0/06-platform-api.md
@CLAUDE.md

<interfaces>
From 27-11: `RunSchedule`, `RunScheduleUpdate`, `ThreadStrategy`, `OnMissed`,
`RunScheduleRepositoryPort { insert, get, list, update, delete, due, claim_tick, increment_skipped }`,
`cron::parse_run_cron(expr, tz) -> Result<RunCron, CronParseError>`, `RunCron::next_after(now)`,
`ScheduleService { repo, submission, options }`.
From 27-12: `AssistantAdminPort::get_version` / `AssistantResolver::resolve` for validating the assistant reference;
`assistant_controller.rs`'s DTO/route/test style on `RunApiState`; `ValidationViolation { path, code, message }`.
From 27-13: `SsrfGuard::check_url`; `RunSubmissionError::WebhookRejected`.
From crates/paladin-web/src/agent_auth.rs:214: `require_admin(&Principal)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `ScheduleAdminPort` and its `ScheduleService` implementation</name>
  <files>crates/paladin-ports/src/input/schedule_admin_port.rs, crates/paladin-ports/src/input/mod.rs, src/application/services/run/schedule/service.rs, src/application/services/run/schedule/admin.rs, src/application/services/run/schedule/mod.rs, src/application/services/run/schedule/tests.rs</files>
  <read_first>
    - `crates/paladin-ports/src/input/assistant_admin_port.rs` (27-12) — the admin-port shape and
      `ValidationViolation`.
    - `src/application/services/run/schedule/{service.rs,tests.rs}` (27-11) — the service to extend.
    - `src/application/services/run/webhook/ssrf.rs` (27-13) — `SsrfGuard::check_url`.
    - `src/application/services/assistant/resolver.rs` (27-12) — resolving `(assistant_id, version?)`.
    - `.planning/phases/27-platform-api/27-CONTEXT.md` D-36, D-38, D-39, D-42.
  </read_first>
  <behavior>
    - `ScheduleAdminPort { create(CreateRunSchedule) -> RunSchedule, get(id) -> Option<RunSchedule>,
      list(limit, cursor) -> RunSchedulePage, patch(id, RunScheduleUpdate) -> RunSchedule, delete(id) -> bool }`
      with `ScheduleAdminError { Invalid { violations: Vec<ValidationViolation> }, NotFound, Backend, NotWired }`.
    - `create`: violations for a bad cron (`path: "/cron"`, code from `CronParseError`), unknown
      timezone (`/timezone`), unknown assistant or version (`/assistant_id`, `/version`), webhook URL
      rejected (`/webhook/url`, code `webhook_url_rejected`), `FixedThread` with an invalid `ThreadId`
      (`/thread_strategy`); on success `next_tick = cron.next_after(now)` and `enabled` as given.
    - `patch`: any change to `cron`/`timezone` recomputes `next_tick` from `now`; `enabled: false` leaves
      `next_tick` in place (a disabled schedule is skipped by `due`); `webhook` re-runs the guard.
    - Tests: each violation path, `create_sets_first_next_tick`, `patch_cron_recomputes_next_tick`,
      `patch_webhook_rejected_by_guard`, `delete_then_get_is_none`.
  </behavior>
  <action>
    Create `crates/paladin-ports/src/input/schedule_admin_port.rs` (declare in `input/mod.rs`) with
    `CreateRunSchedule { assistant_id, version: Option<u32>, cron, timezone: Option<String>, input,
    enabled, thread_strategy: Option<ThreadStrategy>, on_missed: Option<OnMissed>, webhook }`, the trait,
    the error, and a mock module. Create `src/application/services/run/schedule/admin.rs` (declare in
    `schedule/mod.rs`) implementing the port for `ScheduleService` — give the service the extra
    collaborators through `ScheduleService::with_resolver(Arc<dyn AssistantResolver>)` and
    `with_ssrf_guard(SsrfGuard)`; add the tests to `schedule/tests.rs`.
  </action>
  <acceptance_criteria>
    - `cargo test -p paladin-ai --lib services::run::schedule 2>&1 | grep -E 'test result: ok\. [1-9][0-9]* passed'` — 27-11's count plus at least 8.
    - `grep -c 'pub trait ScheduleAdminPort' crates/paladin-ports/src/input/schedule_admin_port.rs` is 1.
    - `grep -c 'check_url' src/application/services/run/schedule/admin.rs` is at least 1.
    - `grep -c 'fn patch_cron_recomputes_next_tick' src/application/services/run/schedule/tests.rs` is 1.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p paladin-ai --lib services::run::schedule 2>&amp;1 | grep -qE 'test result: ok\. (1[4-9]|[2-9][0-9]) passed'</automated>
  </verify>
  <done>Schedules can be created, read, patched and deleted through a core-typed admin port with full
  validation including the write-time SSRF guard.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `/v1/schedules` routes on `RunApiState`; OpenAPI</name>
  <files>crates/paladin-web/src/schedule_controller.rs, crates/paladin-web/src/run_controller.rs, crates/paladin-web/src/lib.rs, crates/paladin-web/openapi.json</files>
  <read_first>
    - `crates/paladin-web/src/assistant_controller.rs` (27-12) — DTO/route/test style to copy exactly.
    - `crates/paladin-web/src/run_controller.rs` — `RunApiState` builder and `run_openapi_router` merge point.
    - `crates/paladin-web/src/thread_controller.rs` 404-413 — pagination query names.
    - `.planning/phases/27-platform-api/27-CONTEXT.md` D-44, D-46, D-47, D-48.
  </read_first>
  <behavior>
    - `POST /v1/schedules` (admin) → 201 `ScheduleResponse` (all `RunSchedule` fields with the webhook
      secret redacted to `"***"` when present); 400 + `details` on violations; `GET /v1/schedules`
      → `{ items, next_cursor }`; `GET /v1/schedules/{id}` → 200/404; `PATCH /v1/schedules/{id}` (admin)
      → 200/400/404; `DELETE /v1/schedules/{id}` (admin) → 204/404; unwired → 501 naming
      `schedules.enabled`; non-admin mutation → 403.
    - `oneshot` tests for each status above (at least 9).
  </behavior>
  <action>
    Add `schedules: Option<Arc<dyn ScheduleAdminPort>>` + `with_schedules` to `RunApiState`. Create
    `crates/paladin-web/src/schedule_controller.rs` (declared in `lib.rs`) with `CreateScheduleRequest`,
    `PatchScheduleRequest` (all optional), `ScheduleResponse`, `ScheduleListResponse`, handlers
    `create_schedule`, `list_schedules`, `get_schedule`, `patch_schedule`, `delete_schedule` with
    `#[utoipa::path]`, and `pub(crate) fn schedule_routes(state) -> OpenApiRouter` merged by
    `run_openapi_router`. Regenerate `openapi.json`; diff-review shows only the schedule paths/schemas.
  </action>
  <acceptance_criteria>
    - `cargo test -p paladin-web --lib schedule_controller 2>&1 | grep -E 'test result: ok\. [9] passed|test result: ok\. [1-9][0-9]+ passed'`.
    - `grep -c 'require_admin' crates/paladin-web/src/schedule_controller.rs` is at least 3.
    - `python3 -c "import json;s=json.load(open('crates/paladin-web/openapi.json'));p=s['paths'];print('/v1/schedules' in p and '/v1/schedules/{schedule_id}' in p and 'patch' in p['/v1/schedules/{schedule_id}'])"` prints `True`.
    - `cargo test -p paladin-web --lib openapi_matches_committed_baseline 2>&1 | grep -qE 'test result: ok\. 1 passed'`.
    - `cargo clippy -p paladin-web --all-targets -- -D warnings` and `cargo fmt --check` pass.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p paladin-web --lib schedule_controller 2>&amp;1 | grep -qE 'test result: ok\. ([9]|[1-9][0-9]) passed' &amp;&amp; cargo test -p paladin-web --lib openapi_matches_committed_baseline 2>&amp;1 | grep -qE 'test result: ok\. 1 passed'</automated>
  </verify>
  <done>Schedules are API-managed with admin scoping, pagination and a regenerated spec.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin HTTP caller → schedule rows | Cron, timezone, input and webhook URL become unattended recurring executions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-14-01 | Elevation of privilege | non-admin creating schedules | high | mitigate | `require_admin` on create/patch/delete (D-46); 403 test. |
| T-27-14-02 | Spoofing / Elevation | SSRF through a schedule webhook | critical | mitigate | Write-time `SsrfGuard::check_url` in `create`/`patch`; send-time guard in 27-13's service. |
| T-27-14-03 | Information disclosure | webhook secret echoed in responses | medium | mitigate | `ScheduleResponse` redacts the secret; test asserts `"***"`. |
| T-27-14-04 | Denial of service | absurd cron frequency | low | accept | See T-27-11-02; operator concern documented in 27-16. |
</threat_model>

<verification>
- `cargo test -p paladin-ai --lib services::run::schedule` and `cargo test -p paladin-web --lib schedule_controller` non-zero.
- `cargo clippy --workspace --all-targets -- -D warnings`, `cargo fmt --check`.
</verification>

<success_criteria>
- Every schedule route exists with auth, admin scoping, pagination and validation, and is in the spec.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

See the identical section in `27-02-PLAN.md` — the canonical list of every symbol, route, table,
migration, CI job, env var, dependency promotion and doc asset Phase 27 creates. This plan's own
contribution: `crates/paladin-ports/src/input/schedule_admin_port.rs` (`ScheduleAdminPort`,
`CreateRunSchedule`, `ScheduleAdminError`), `src/application/services/run/schedule/admin.rs`,
`crates/paladin-web/src/schedule_controller.rs` and its DTOs, the five `/v1/schedules*` routes,
`RunApiState::with_schedules`.
</artifacts_produced>

<output>
Create `.planning/phases/27-platform-api/27-14-SUMMARY.md` when done.
</output>
