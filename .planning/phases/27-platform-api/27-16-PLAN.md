---
phase: 27-platform-api
plan: 16
type: execute
wave: 7
depends_on: [27-05, 27-13, 27-14]
files_modified:
  - docs/src/api-reference/platform-api.md
  - docs/src/SUMMARY.md
  - docs/src/deployment-topologies/queue-worker.md
  - docs/src/deployment/kubernetes.md
  - docs/src/user-guides/parley-and-chronicle.md
  - k8s/server/worker-deployment.yaml
  - k8s/README.md
autonomous: true
requirements: [PLAT-05, PLAT-06]

must_haves:
  truths:
    - "An mdBook page documents the whole platform API: run lifecycle and status machine, every endpoint with its auth tier and pagination, the seven SSE event names and the degraded mode's stated limitation, assistants (append-only, no PUT, synthetic code entries), schedules (5/6-field cron, UTC/IANA, strategies, `on_missed`, `skipped_ticks`), and webhooks (payload, signature verification recipe, retry table, SSRF rejection list, `allow_private`, the DNS-rebinding limitation) (X-08, D-26, D-42)."
    - "The queue/worker topology page and the Kubernetes docs carry a worker-replica example manifest that reads the `APP_RUN_*` env vars, and neither implies the in-process auth token store is multi-replica safe (ADR-0041; PRD 06 §6)."
    - "`mdbook build` succeeds under `warning-policy = \"error\"` — every link on the new/edited pages resolves."
  artifacts:
    - path: "docs/src/api-reference/platform-api.md"
      provides: "the Platform API user/operator documentation"
      contains: "X-Paladin-Signature"
    - path: "k8s/server/worker-deployment.yaml"
      provides: "worker-replica Deployment example"
      contains: "APP_RUN_WORKER_CONCURRENCY"
  key_links:
    - from: "docs/src/deployment-topologies/queue-worker.md"
      to: "k8s/server/worker-deployment.yaml"
      via: "the page embeds/links the worker manifest"
      pattern: "worker-deployment"
---

<objective>
Write the documentation PRD 06 and X-08 owe: one mdBook page for the platform API, the worker-replica
deployment example in `k8s/` and the topology/Kubernetes pages, and cross-links from the Phase 24
parley page. Documentation is a first-class deliverable here because D-26 and D-42 are satisfied by
*naming* limitations plainly.

Purpose: X-08 and PRD 06 §6 (k8s worker-replica example); D-26 (documented degraded mode); D-42
(documented DNS-rebinding limitation); ADR-0041 (do not imply multi-replica auth safety).

Output: `docs/src/api-reference/platform-api.md`, `SUMMARY.md` entry, updated `queue-worker.md`,
`kubernetes.md`, `parley-and-chronicle.md`, `k8s/server/worker-deployment.yaml`, `k8s/README.md`.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-platform-api/27-CONTEXT.md
@.planning/phases/27-platform-api/27-05-SUMMARY.md
@.planning/phases/27-platform-api/27-13-SUMMARY.md
@.planning/phases/27-platform-api/27-14-SUMMARY.md
@.project/v0.10.0/06-platform-api.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: `docs/src/api-reference/platform-api.md` and the SUMMARY entry</name>
  <files>docs/src/api-reference/platform-api.md, docs/src/SUMMARY.md</files>
  <read_first>
    - `docs/src/user-guides/parley-and-chronicle.md` "The HTTP Surface" (line 273+) — the Phase 24
      house style for documenting `/v1` routes and status tables.
    - `docs/src/api-reference/wargraph-doc-schema.md` (27-05) — link target for the Workflow body.
    - `crates/paladin-web/openapi.json` (current) — the authoritative route list; every route documented
      must exist in the spec and vice versa (a grep-based cross-check is in the acceptance criteria).
    - `src/config/{webhooks,run_worker,run_stream,schedules,assistants,run_store,run_queue}.rs` (27-06)
      — env var names and defaults to quote.
    - `.planning/phases/27-platform-api/27-CONTEXT.md` D-02, D-16, D-18, D-25, D-26, D-32, D-38, D-39,
      D-42, D-43, D-46, D-47.
  </read_first>
  <action>
    Write the page with these sections: Overview (the run server, what changed from synchronous
    execute); Runs (status machine diagram in mermaid with the exact D-02 edges, `POST /runs` body,
    202 semantics, `409 thread_busy` incl. `awaiting_input` counting as busy and the resume remedy,
    cancel semantics — `Halted` Waypoint vs `Cancelled` run); Streaming (seven events, payload fields,
    `mode`, `dropped`, 15 s heartbeat, the degraded-mode paragraph verbatim: no ordering guarantee
    relative to the live path, supersteps may coalesce, `done`/`error` always eventually delivered);
    Threads (list/get/history/state/resume/fork/delete); Assistants (envelope shape, kinds, publish
    validation and the `details` violation format, immutability — no PUT, `latest` frozen at submit,
    synthetic `source: "code"` entries and `expose_code_registry`); Schedules (cron forms, timezone,
    strategies, `on_missed`, `skipped_ticks`, restart semantics); Webhooks (payload JSON example,
    events, `X-Paladin-Signature` verification recipe in pseudo-code over the raw bytes, retry table
    1/2/4/8/16 s, dead-letter rules, `GET /runs/{id}/webhook-deliveries`, SSRF rejection list,
    `webhooks.allow_private`, redirects never followed, DNS rebinding limitation); Pagination
    (`limit` 1..=100 default 20, opaque cursor, the cursor-walk caveat about rows inserted after the
    first page); Authentication and scopes (the two tiers); Configuration (every new struct and env var
    with defaults, all off by default); Deployment pointer to the queue/worker topology page. Add the
    page to `docs/src/SUMMARY.md` under "API Reference".
  </action>
  <acceptance_criteria>
    - `grep -c 'platform-api.md' docs/src/SUMMARY.md` is 1.
    - `for r in /v1/runs /v1/runs/{run_id}/stream /v1/runs/{run_id}/cancel /v1/runs/{run_id}/webhook-deliveries /v1/threads/{thread_id}/fork /v1/assistants /v1/schedules; do grep -q "$r" docs/src/api-reference/platform-api.md || echo MISSING $r; done` prints nothing.
    - `grep -ciE 'rebinding' docs/src/api-reference/platform-api.md` is at least 1 and `grep -ci 'coalesce' docs/src/api-reference/platform-api.md` is at least 1.
    - `grep -c 'X-Paladin-Signature' docs/src/api-reference/platform-api.md` is at least 1 and `grep -c 'allow_private' docs/src/api-reference/platform-api.md` is at least 1.
    - `grep -c 'APP_RUN_STORE_BACKEND\|APP_WEBHOOKS_ALLOW_PRIVATE\|APP_SCHEDULES_ENABLED' docs/src/api-reference/platform-api.md` is at least 3.
    - `python3 - <<'EOF'` cross-check exits 0: load `crates/paladin-web/openapi.json`, collect paths starting with `/v1/runs`, `/v1/threads`, `/v1/assistants`, `/v1/schedules`, and assert every one appears verbatim in the page (`EOF`).
    - `command -v mdbook >/dev/null && (cd docs && mdbook build) || echo "mdbook unavailable locally - docs CI verifies"` — record which branch ran.
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'platform-api.md' docs/src/SUMMARY.md &amp;&amp; grep -qi 'rebinding' docs/src/api-reference/platform-api.md &amp;&amp; grep -q 'X-Paladin-Signature' docs/src/api-reference/platform-api.md &amp;&amp; grep -q '/v1/runs/{run_id}/stream' docs/src/api-reference/platform-api.md</automated>
  </verify>
  <done>The platform API is documented end-to-end with every limitation named rather than implied.</done>
</task>

<task type="auto">
  <name>Task 2: Worker-replica example (k8s) and topology/Kubernetes/parley page updates</name>
  <files>docs/src/deployment-topologies/queue-worker.md, docs/src/deployment/kubernetes.md, docs/src/user-guides/parley-and-chronicle.md, k8s/server/worker-deployment.yaml, k8s/README.md</files>
  <read_first>
    - `k8s/server/deployment.yaml`, `k8s/server/configmap.yaml`, `k8s/README.md` — the server
      manifest to derive the worker manifest from (same image, different env/args, no Service).
    - `docs/src/deployment-topologies/queue-worker.md` — "Producer and worker" and "Configuring the
      queue" sections to extend.
    - `docs/src/deployment/kubernetes.md` "Deployment" section (153+).
    - `.planning/decisions/0041-in-process-token-store-single-replica-scope.md` — the caveat to state.
    - `docs/src/user-guides/parley-and-chronicle.md` "The HTTP Surface" — add the `run_id` field on the
      resume response (D-21) and a pointer to the new page.
  </read_first>
  <action>
    Create `k8s/server/worker-deployment.yaml`: a `Deployment` named `paladin-worker` (replicas 2) using
    the same image as the server, env `APP_RUN_STORE_BACKEND=postgres`, `APP_RUN_STORE_URL_ENV=PALADIN_RUN_DB_URL`,
    `APP_RUN_QUEUE_BACKEND=redis`, `APP_RUN_QUEUE_URL_ENV=PALADIN_REDIS_URL`, `APP_RUN_WORKER_CONCURRENCY=4`,
    `APP_RUN_WORKER_LEASE_SECONDS=60`, `APP_SCHEDULES_ENABLED=true` on ONE replica set only (a comment
    explains the tick claim makes multi-replica safe but one is enough), `terminationGracePeriodSeconds`
    at least twice the shutdown grace (MIGRATION §9.8's rule), the secrets referenced from
    `paladin-secrets`. Document in `k8s/README.md`. Extend `queue-worker.md` with a "Run server:
    producer API + worker replicas" section embedding the manifest via a relative link and stating: the
    API replica and worker replicas share the run store and the Redis queue; the auth token store is
    in-process and single-replica-scoped (ADR-0041) — run ONE API replica or terminate auth upstream;
    cancellation is cross-instance via the persisted flag. Add the same manifest reference to
    `kubernetes.md`'s Deployment section. Update `parley-and-chronicle.md`'s resume response example
    with `run_id` and a link to the new page.
  </action>
  <acceptance_criteria>
    - `python3 -c "import yaml; d=list(yaml.safe_load_all(open('k8s/server/worker-deployment.yaml'))); print(all(x['kind']=='Deployment' for x in d if x))"` prints `True`.
    - `grep -c 'APP_RUN_WORKER_CONCURRENCY' k8s/server/worker-deployment.yaml` is at least 1 and `grep -c 'terminationGracePeriodSeconds' k8s/server/worker-deployment.yaml` is 1.
    - `grep -c 'worker-deployment' docs/src/deployment-topologies/queue-worker.md docs/src/deployment/kubernetes.md k8s/README.md` reports at least 1 in each.
    - `grep -ci 'single-replica\|ADR-0041' docs/src/deployment-topologies/queue-worker.md` is at least 1.
    - `grep -c 'run_id' docs/src/user-guides/parley-and-chronicle.md` is at least 1 and `grep -c 'platform-api.md' docs/src/user-guides/parley-and-chronicle.md` is at least 1.
    - `command -v mdbook >/dev/null && (cd docs && mdbook build) || echo "mdbook unavailable locally - docs CI verifies"`.
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "import yaml; d=list(yaml.safe_load_all(open('k8s/server/worker-deployment.yaml'))); assert all(x['kind']=='Deployment' for x in d if x)" &amp;&amp; grep -q 'worker-deployment' docs/src/deployment-topologies/queue-worker.md &amp;&amp; grep -q 'run_id' docs/src/user-guides/parley-and-chronicle.md</automated>
  </verify>
  <done>Operators have a worker-replica manifest and topology guidance that states the auth-store limitation plainly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → operator decisions | A doc that overstates safety (multi-replica auth, SSRF coverage) leads to insecure deployments. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-16-01 | Information disclosure (mis-documentation) | SSRF / DNS-rebinding coverage claims | medium | mitigate | The page names resolve-then-connect pinning as NOT implemented (D-42); grep gate on "rebinding". |
| T-27-16-02 | Spoofing (deployment) | multi-replica API with in-process token store | medium | mitigate | Topology page states ADR-0041's single-replica scope; grep gate. |
| T-27-16-03 | Information disclosure | secrets in example manifests | low | mitigate | Manifest references `paladin-secrets` names only; no literal credentials (grep for `sk-`/`password:` is 0). |
</threat_model>

<verification>
- The docs CI job (`docs.yml`) builds the book; locally `mdbook build` if available.
- YAML validity of the manifest; no literal secrets in `k8s/server/worker-deployment.yaml`.
</verification>

<success_criteria>
- X-08 satisfied for this epic; every documented limitation matches the code's actual behaviour.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

See the identical section in `27-02-PLAN.md` — the canonical list of every symbol, route, table,
migration, CI job, env var, dependency promotion and doc asset Phase 27 creates. This plan's own
contribution: `docs/src/api-reference/platform-api.md`, `k8s/server/worker-deployment.yaml`, and the
edits to `queue-worker.md`, `kubernetes.md`, `parley-and-chronicle.md`, `k8s/README.md`, `SUMMARY.md`.
</artifacts_produced>

<output>
Create `.planning/phases/27-platform-api/27-16-SUMMARY.md` when done.
</output>
