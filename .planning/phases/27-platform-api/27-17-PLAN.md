---
phase: 27-platform-api
plan: 17
type: execute
wave: 8
depends_on: [27-06, 27-15, 27-16]
files_modified:
  - src/infrastructure/web/run_api_wiring.rs
  - src/infrastructure/web/mod.rs
  - src/bin/paladin-server.rs
  - config.example.yml
  - MIGRATION.md
autonomous: true
requirements: [PLAT-01, PLAT-02, PLAT-03, PLAT-04, PLAT-05, PLAT-06]

must_haves:
  truths:
    - "`paladin-server` builds `RunApiState` and `ThreadApiState`'s run fields from the seven config structs read through `Default` + `apply_env_overrides()`; with every struct at its default (`run_store.backend = disabled`) no worker, scheduler or webhook service is spawned and every new route answers 501 `not_implemented` naming the config key to set (D-44, D-50, X-09)."
    - "With `run_store` set (sqlite or postgres) and `run_queue` in_memory or redis, the process spawns the worker pool (`RunWorkerConfig → RunWorkerOptions`, heartbeat derived), the schedule service when `schedules.enabled`, and the webhook delivery service, all registered with the one `ShutdownCoordinator`; `SIGTERM` drains them within the configured grace (D-11, D-13)."
    - "A configured `postgres` run store or `redis` queue on a binary built without `storage-postgres` / `redis-queue` is a startup error naming the missing feature — never a silent fallback (the `build_postgres_thread_state` precedent)."
    - "Code-registered agents (`AgentRegistry`) are exposed to the run pipeline through a `CodeAgentResolver` (cfg `web-server`) so `POST /runs` for a code-registered agent id executes it via `FacadeProvisioner`; `AgentRegistry` itself is untouched (D-32, X-03)."
    - "`MIGRATION.md` §9.5 lists every new config struct, key, env var and default (D-53)."
  artifacts:
    - path: "src/infrastructure/web/run_api_wiring.rs"
      provides: "build_run_api(configs, settings, coordinator, waypoint store, auth, registry) -> RunApiHandles"
      contains: "pub async fn build_run_api"
    - path: "src/bin/paladin-server.rs"
      provides: "run_router merged alongside agent/thread routers; services spawned from config"
      contains: "run_router("
  key_links:
    - from: "src/bin/paladin-server.rs"
      to: "src/infrastructure/web/run_api_wiring.rs"
      via: "build_run_api called after build_thread_state; handles.run_state passed to run_router"
      pattern: "build_run_api"
    - from: "src/infrastructure/web/run_api_wiring.rs"
      to: "src/config/run_worker.rs"
      via: "RunWorkerConfig converted into RunWorkerOptions (lease = lease_seconds, heartbeat derived)"
      pattern: "RunWorkerOptions"
---

<objective>
Wire the whole platform API into `paladin-server` from configuration: stores, queue, worker pool,
scheduler, webhook delivery, resolvers (stored + code-registered), the event bus, and the routers —
all off by default, all registered with the shutdown coordinator, all fail-closed on feature/config
mismatch.

Purpose: every prior plan proved its slice in tests; this plan makes the binary actually serve it,
following the exact `build_thread_state`/`thread_state_over_store` precedent from Phase 24.

Output: `src/infrastructure/web/run_api_wiring.rs`, the `paladin-server.rs` changes, a commented
`config.example.yml` section, and `MIGRATION.md` §9.5.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-platform-api/27-CONTEXT.md
@.planning/phases/27-platform-api/27-06-SUMMARY.md
@.planning/phases/27-platform-api/27-15-SUMMARY.md
@.planning/decisions/0031-extracted-crate-dependency-rule.md
@.planning/decisions/0039-http-topology-no-garrison-no-arsenal.md
@.project/v0.10.0/06-platform-api.md
@CLAUDE.md

<interfaces>
From src/bin/paladin-server.rs: lines 60-140 (settings load, `EngineConfig`/`WaypointStoreConfig`
`apply_env_overrides`, `agent_router(state).merge(thread_router(thread_state))`, docs router merge,
`with_http_layers`), 297-330 (`NoRegisteredGraphsPaladinPort` — for the run pipeline the engine's
`PaladinPort` must be REAL: a facade `PaladinExecutionService`-backed port routed by model, the same
resolution `FacadeProvisioner::from_settings` performs), 328-440 (`build_thread_state`,
`thread_state_over_store`, `build_postgres_thread_state` and its `#[cfg(not(feature))]` twin).
From src/infrastructure/web/facade_provisioner.rs (cfg `web-server`): `FacadeProvisioner::from_settings(&Settings)`,
`provision(&AgentSpec) -> ProvisionedAgent` — reuse for `CodeAgentResolver` and for the model→LLM lookup.
From 27-15: `RunApiState::new().with_run_submission(..).with_run_repository(..).with_run_events(..)
.with_assistants(..).with_schedules(..).with_webhook_deliveries(..).with_code_registry(..).with_expose_code_registry(..).with_auth(..)`;
`ThreadApiState::with_runs(..).with_run_submission(..)`; `ParleyPortAdapter::with_run_repository/with_run_queue` (27-08).
From 27-04/07/10/11/13: `RunWorkerPool::spawn`, `ScheduleService::spawn`, `WebhookDeliveryService::spawn`,
`RunEventBus`, `RunEventStreamService`, `DbCancellationProbe`, `ChainedResolver`, `SsrfGuard`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `build_run_api` — config → stores/queue/services/resolvers/state, fail-closed on feature mismatch</name>
  <files>src/infrastructure/web/run_api_wiring.rs, src/infrastructure/web/mod.rs</files>
  <read_first>
    - `src/bin/paladin-server.rs` 60-140 and 297-440 (read closely; this task mirrors it).
    - `src/infrastructure/web/{mod.rs,facade_provisioner.rs}`.
    - `src/config/{run_store,run_queue,run_worker,run_stream,assistants,schedules,webhooks}.rs` (27-06).
    - `src/application/services/run/{mod.rs,worker.rs,submission.rs,events.rs,cancel.rs}`,
      `run/schedule/service.rs`, `run/webhook/service.rs`, `services/assistant/{service,resolver}.rs`
      — constructor signatures.
    - `.planning/phases/27-platform-api/27-CONTEXT.md` D-11, D-12, D-32, D-44, D-50.
  </read_first>
  <behavior>
    - `build_run_api(RunApiConfigs { run_store, run_queue, run_worker, run_stream, assistants, schedules,
      webhooks }, &Settings, coordinator, Option<Arc<dyn WaypointPort>>, AgentAuthConfig, Arc<AgentRegistry>)
      -> Result<RunApiHandles, Box<dyn Error>>` where `RunApiHandles { run_state: RunApiState, thread_run_submission:
      Option<Arc<dyn RunSubmissionPort>>, run_repository: Option<Arc<dyn RunRepositoryPort>>, parley_extras:
      Option<(Arc<dyn RunRepositoryPort>, Arc<dyn RunQueuePort>)>, tasks: Vec<JoinHandle<()>> }`.
    - `run_store.backend == Disabled` → `RunApiState::new().with_auth(auth)` (+ code registry fields
      so `GET /assistants` can still list synthetic entries when `assistants` is unwired? No — keep it
      501 too; synthetic entries need the admin port wired), no tasks spawned, `parley_extras: None`.
    - `Sqlite { path }` → `SqliteRunRepository`, `SqliteAssistantRepository`, `SqliteRunScheduleRepository`,
      `SqliteWebhookDeliveryRepository` over the same file; `Postgres { url_env }` → the Postgres
      quartet behind `#[cfg(feature = "storage-postgres")]` with the fail-closed twin; queue
      `InMemory` → `InMemoryRunQueue`, `Redis { url_env, key_prefix }` → `RedisRunQueue` behind
      `#[cfg(feature = "redis-queue")]` with a fail-closed twin; a waypoint store is REQUIRED when the
      run store is enabled (startup error naming `waypoint_store.backend`).
    - Engine for the pool: `WarEngine::new(paladin_port, waypoint_store)` where `paladin_port` is the
      facade's real `PaladinPort` built from `Settings` (the same LLM resolution `FacadeProvisioner`
      uses — factor a `fn paladin_port_from_settings(&Settings) -> Arc<dyn PaladinPort>` in
      `facade_provisioner.rs` if none exists), with durability/grace from `EngineConfig`, the
      `DbCancellationProbe`, and the `RunEventBusSink`.
    - Resolvers: `ChainedResolver(StoredAssistantResolver(assistant repo, validator over the server's
      `EngineRegistries`), CodeAgentResolver(registry, provisioner))` where `CodeAgentResolver` (new,
      cfg `web-server`, in `run_api_wiring.rs`) maps an `AgentRegistry` entry to `Runnable::Agent`.
    - Services: `RunWorkerPool::spawn(RunWorkerOptions::from(&run_worker))`, `ScheduleService::spawn`
      only if `schedules.enabled`, `WebhookDeliveryService::spawn` with `WebhookDeliveryOptions::from(&webhooks)`;
      all registered with `coordinator`.
    - Unit tests (in the module, `#[cfg(test)]`): defaults → no tasks + 501 state; sqlite + in_memory
      on temp files → three tasks (worker, webhooks, and schedules when enabled), state fields all
      `Some`; `Postgres`/`Redis` without the feature → `Err` naming the feature (compile the test under
      `#[cfg(not(feature = ..))]`), missing waypoint store → `Err` naming `waypoint_store.backend`.
  </behavior>
  <action>
    Create `src/infrastructure/web/run_api_wiring.rs` (declared `#[cfg(feature = "web-server")] pub mod
    run_api_wiring;` in `src/infrastructure/web/mod.rs`) with `RunApiConfigs`, `RunApiHandles`,
    `CodeAgentResolver`, `build_run_api`, and the two `#[cfg]` twins per optional backend. Implement
    `From<&RunWorkerConfig> for RunWorkerOptions` (lease = `lease_seconds`, `min_probe_interval` from ms),
    `From<&WebhooksConfig> for WebhookDeliveryOptions`, `From<&SchedulesConfig> for ScheduleServiceOptions`
    in this file (keeping `src/config/` free of service types).
  </action>
  <acceptance_criteria>
    - `cargo test -p paladin-ai --features web-server --lib run_api_wiring 2>&1 | grep -E 'test result: ok\. [1-9][0-9]* passed'` — at least 4 tests.
    - `grep -c 'pub async fn build_run_api' src/infrastructure/web/run_api_wiring.rs` is 1.
    - `grep -c 'cfg(feature = "storage-postgres")' src/infrastructure/web/run_api_wiring.rs` is at least 1 and `grep -c 'cfg(feature = "redis-queue")' src/infrastructure/web/run_api_wiring.rs` is at least 1.
    - `grep -c 'NoRegisteredGraphsPaladinPort' src/infrastructure/web/run_api_wiring.rs` is 0 (the run pipeline uses a real port).
    - `grep -v '^\s*//' src/config/*.rs | grep -c 'RunWorkerOptions\|WebhookDeliveryOptions'` is 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p paladin-ai --features web-server --lib run_api_wiring 2>&amp;1 | grep -qE 'test result: ok\. ([4-9]|[1-9][0-9]) passed'</automated>
  </verify>
  <done>One function turns configuration into the fully wired run API, off by default and fail-closed on mismatch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `paladin-server` integration, `config.example.yml`, `MIGRATION.md` §9.5</name>
  <files>src/bin/paladin-server.rs, config.example.yml, MIGRATION.md</files>
  <read_first>
    - `src/bin/paladin-server.rs` 60-140 and its test module (790-913) — `thread_router_is_merged_alongside_agent_router`
      is the test shape to repeat for `run_router`.
    - `config.example.yml` — where the `waypoint_store` example section lives (add the seven new
      sections beside it, all commented with defaults).
    - `MIGRATION.md` §9.5 (255-309) — row shape.
    - `.planning/phases/27-platform-api/27-CONTEXT.md` D-44, D-50, D-53.
  </read_first>
  <behavior>
    - The server reads the seven configs (`Default` + `apply_env_overrides()` + `validate()` with a
      startup error on failure), calls `build_run_api` after `build_thread_state`, threads
      `handles.thread_run_submission`/`run_repository` into `ThreadApiState` via `with_run_submission`/
      `with_runs`, passes `parley_extras` into the `ParleyPortAdapter` (`with_run_repository`/`with_run_queue`),
      merges `run_router(handles.run_state)` alongside the agent and thread routers BEFORE
      `with_http_layers`, and keeps `handles.tasks` until shutdown, where `coordinator.cancel_and_wait(grace)`
      drains them.
    - Tests in the binary's module: `run_router_is_merged_alongside_agent_and_thread_routers`
      (unwired → 501 on `POST /v1/runs`, not 404), `default_config_spawns_no_run_services`
      (`build_run_api` over defaults yields zero tasks), `openapi_json_lists_run_paths` (the served
      `/openapi.json` contains `/v1/runs`).
    - `config.example.yml` gains commented `run_store`, `run_queue`, `run_worker`, `run_stream`,
      `assistants`, `schedules`, `webhooks` sections with defaults and env var names.
    - §9.5 rows: one per struct with keys, env vars, defaults, and "disabled/today by default" — and the
      statement that a v0.9 config boots v0.10 with no run server (SHIP-02's test will assert it).
  </behavior>
  <action>
    Edit `src/bin/paladin-server.rs` as described (follow the existing comment style explaining the
    D-24 merge order). Extend `config.example.yml`. Write the §9.5 rows. Run the binary once locally
    with defaults (`cargo run --bin paladin-server --features web-server` against `config.test.yml`,
    stop with SIGINT) and record in the SUMMARY that startup logs show "run server disabled" and that
    `POST /v1/runs` answers 501.
  </action>
  <acceptance_criteria>
    - `cargo test --bin paladin-server --features web-server 2>&1 | grep -E 'test result: ok\. [1-9][0-9]* passed'` — the pre-existing count plus at least 3.
    - `grep -c 'build_run_api' src/bin/paladin-server.rs` is at least 1 and `grep -c 'run_router(' src/bin/paladin-server.rs` is at least 1.
    - `grep -c 'run_store:\|run_queue:\|run_worker:\|run_stream:\|assistants:\|schedules:\|webhooks:' config.example.yml` is at least 7.
    - `awk '/^## 9.5/,/^## 9.6/' MIGRATION.md | grep -c 'RunStoreConfig\|RunQueueConfig\|RunWorkerConfig\|RunStreamConfig\|AssistantsConfig\|SchedulesConfig\|WebhooksConfig'` is at least 7.
    - `cargo build --bin paladin-server --features web-server,storage-postgres,redis-queue` and `cargo build --bin paladin-server --features web-server` both succeed.
    - `cargo clippy --workspace --all-targets --all-features -- -D warnings` and `cargo fmt --check` pass.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --bin paladin-server --features web-server 2>&amp;1 | grep -qE 'test result: ok\. [1-9][0-9]* passed' &amp;&amp; awk '/^## 9.5/,/^## 9.6/' MIGRATION.md | grep -q 'WebhooksConfig'</automated>
  </verify>
  <done>The binary serves the platform API when configured and behaves exactly as v0.9 when not; §9.5 is filled.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config/env → process capabilities | Enabling stores, queues and outbound webhooks changes the process's attack surface. |
| Server → LLM providers on behalf of stored assistants | The real `PaladinPort` carries provider credentials from `Settings`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-17-01 | Elevation of privilege | new routes reachable without auth | high | mitigate | `run_router` merged before `with_http_layers` and carries the same `require_authentication` layer; binary test asserts 501-not-404 and auth wiring. |
| T-27-17-02 | Information disclosure | connection strings / API keys in logs | high | mitigate | Only `url_env` names are logged; provider keys stay inside `Settings`-built adapters (existing redaction rules); grep gate for `NoRegisteredGraphsPaladinPort` misuse only. |
| T-27-17-03 | Denial of service | services left running after shutdown | medium | mitigate | All tasks registered with the coordinator; drained on SIGTERM within grace. |
| T-27-17-04 | Tampering | silent fallback on missing feature | medium | mitigate | Fail-closed startup errors naming the feature (precedent). |
</threat_model>

<verification>
- `cargo test --bin paladin-server --features web-server` and `cargo test -p paladin-ai --features web-server --lib run_api_wiring` non-zero.
- Both feature combinations build; clippy `--all-features` clean; `cargo fmt --check`.
</verification>

<success_criteria>
- `paladin-server` is a durable run server when configured and byte-for-byte v0.9-shaped when not.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

See the identical section in `27-02-PLAN.md` — the canonical list of every symbol, route, table,
migration, CI job, env var, dependency promotion and doc asset Phase 27 creates. This plan's own
contribution: `src/infrastructure/web/run_api_wiring.rs` (`build_run_api`, `RunApiConfigs`, `RunApiHandles`,
`CodeAgentResolver`, the `From<&…Config>` option conversions), the `paladin-server.rs` wiring, the
`config.example.yml` sections, and the §9.5 rows.
</artifacts_produced>

<output>
Create `.planning/phases/27-platform-api/27-17-SUMMARY.md` when done.
</output>
