---
phase: 27-platform-api
plan: 18
type: execute
wave: 9
depends_on: [27-17]
files_modified:
  - tests/integration/e2e_platform_api_test.rs
  - Cargo.toml
  - .github/workflows/ci.yml
  - scripts/sdk-smoke/run.sh
  - scripts/sdk-smoke/smoke.py
  - scripts/sdk-smoke/smoke.ts
  - scripts/sdk-smoke/package.json
  - scripts/sdk-smoke/tsconfig.json
  - crates/paladin-web/openapi.json
  - .project/current-exports.txt
  - MIGRATION.md
autonomous: false
requirements: [PLAT-01, PLAT-02, PLAT-03, PLAT-04, PLAT-05, PLAT-06]

must_haves:
  truths:
    - "PRD 06 acceptance 1 passes as one integration test: create a workflow assistant with a Gate → submit a run → SSE shows progress → the run suspends `AwaitingInput` → a mockito webhook receives the `awaiting_input` payload with the parley → resume via `POST /threads/{id}/resume` → the run completes → `GET /threads/{id}/history` shows the chain and `POST /threads/{id}/fork` submits a new run."
    - "A `sdk-clients` CI job generates a Python and a TypeScript client from `crates/paladin-web/openapi.json` with one pinned `openapi-generator-cli` image, compiles both, boots `paladin-server` on the all-InMemory/SQLite profile, and smoke-tests list assistants → submit run → poll status from each client; it runs on every PR (D-49, PLAT-FR-17)."
    - "`openapi.json` is diff-reviewed as a whole for the phase: every new path is additive and no pre-existing path or schema changed except the registered `ResumeAcceptedResponse.run_id` (D-48; SHIP-02's golden diff later restricts itself to pre-existing paths)."
    - "`MIGRATION.md` §9.6 lists every new endpoint with its auth tier, and the resume-response field (D-53)."
    - "Workspace line coverage stays at or above the 82% floor under CI's exact invocation — `cargo llvm-cov --workspace --features integration-tests,llm-all --lcov --output-path lcov.info --fail-under-lines 82 -- --test-threads=1` (`scripts/coverage.sh` hard-fails locally without Redis/MinIO) (D-54, X-02)."
    - "`.project/current-exports.txt` is regenerated with `./scripts/extract-public-api.sh` so the `api-surface` CI job passes on the phase's purely additive public-API growth (X-10.1 inventory)."
  prohibitions:
    - statement: "The `sdk-clients` job MUST NOT report green when client generation produced zero source files, when either client failed to compile, or when the smoke server was unreachable — each script asserts a non-empty generated tree, a compile step, and a real 202 from `POST /v1/runs` followed by a terminal status."
      category: transparency
  artifacts:
    - path: "tests/integration/e2e_platform_api_test.rs"
      provides: "the acceptance-1 lifecycle test"
      contains: "awaiting_input"
    - path: ".github/workflows/ci.yml"
      provides: "sdk-clients job"
      contains: "sdk-clients:"
    - path: "scripts/sdk-smoke/smoke.py"
      provides: "Python generated-client smoke"
      contains: "submit"
    - path: "scripts/sdk-smoke/smoke.ts"
      provides: "TypeScript generated-client smoke"
      contains: "submit"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "crates/paladin-web/openapi.json"
      via: "openapi-generator-cli -i crates/paladin-web/openapi.json"
      pattern: "openapi-generator-cli"
    - from: "tests/integration/e2e_platform_api_test.rs"
      to: "src/infrastructure/web/run_api_wiring.rs"
      via: "build_run_api over InMemory queue + SQLite temp files + MockLlmAdapter"
      pattern: "build_run_api"
---

<objective>
Close the phase with its two program-level proofs and its last bookkeeping: the full-lifecycle
integration test that PRD 06 §4 item 1 names, the generated-client CI gate PRD 06 §4 item 7 names,
the phase-wide OpenAPI diff review, the §9.6 endpoint register, and the coverage-floor evidence.

Purpose: everything before this plan proved a slice; this plan proves the system and produces the
evidence the verifier and Phase 29 will read.

Output: `tests/integration/e2e_platform_api_test.rs` (+ `[[test]]` entry), the `sdk-clients` job and
its scripts, the reviewed `openapi.json`, §9.6 rows, and a human checkpoint on the CI evidence.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-platform-api/27-CONTEXT.md
@.planning/phases/27-platform-api/27-RESEARCH.md
@.planning/phases/27-platform-api/27-17-SUMMARY.md
@.project/v0.10.0/06-platform-api.md
@.project/v0.10.0/00-program-overview.md
@CLAUDE.md

<interfaces>
From tests/integration/e2e_approval_gate_test.rs and e2e_crash_resume_test.rs — the root-crate E2E
house style (`[[test]]` entries in `Cargo.toml` at lines 286-320; `paladin::` facade imports; `tempfile`
SQLite stores; `tests/helpers/mock_llm_adapter.rs`).
From 27-17: `build_run_api(RunApiConfigs, &Settings, coordinator, waypoint_store, auth, registry) -> RunApiHandles`.
From 27-15: `run_router`, `thread_router`, DTOs; from 27-10: the SSE framing; from 27-13: the webhook payload keys.
From .github/workflows/ci.yml: the `crate-isolation`/`cli-tests` jobs for a job that builds and runs
the binary; `ubuntu-latest` has Docker (27-RESEARCH.md Environment Availability); `actions/setup-python@v5`
and `actions/setup-node@v4` are available actions.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `e2e_platform_api` — the acceptance-1 lifecycle over the real router</name>
  <files>tests/integration/e2e_platform_api_test.rs, Cargo.toml</files>
  <read_first>
    - `tests/integration/e2e_approval_gate_test.rs` (whole) and `tests/helpers/mock_llm_adapter.rs`.
    - `src/infrastructure/web/run_api_wiring.rs` (27-17) and `src/bin/paladin-server.rs`'s test module
      (how a full router is assembled in-process).
    - `crates/paladin-battalion/tests/fixtures/graph_docs/approval_gate.json` (27-05) — the workflow
      body to publish.
    - `crates/paladin-web/src/run_controller.rs` (SSE framing) and `src/application/services/run/webhook/tests.rs`
      (mockito capture pattern).
    - PRD 06 §4 item 1.
  </read_first>
  <behavior>
    - Setup: `build_run_api` with `run_store = Sqlite(temp)`, `run_queue = InMemory`, worker
      concurrency 1, webhooks enabled with `allow_private = true` (mockito binds 127.0.0.1 — the test
      states this is the one legitimate use of the override), `Settings` pointing at the mock LLM;
      router = `agent_router(..).merge(thread_router(..)).merge(run_router(..))` behind a test API key.
    - Steps, each asserted: (1) `POST /v1/assistants` with `approval_gate.json` as a Workflow body
      → 201 v1; (2) `POST /v1/runs { assistant_id, input, webhook: { url: mockito, secret, events:
      [awaiting_input, completed] } }` → 202; (3) `GET /v1/runs/{id}/stream` read until a `parley`
      event arrives (collect at least one `superstep` before it); (4) `GET /v1/runs/{id}` →
      `awaiting_input`; (5) mockito received one POST whose body has `event == "awaiting_input"`,
      `parleys` non-empty, `attempt == 1`, and whose `X-Paladin-Signature` verifies over the raw body;
      (6) `POST /v1/threads/{id}/resume` with the approval response → 202 with `run_id` equal to the
      original; (7) stream/poll until `done`; `GET /v1/runs/{id}` → `completed`, `attempt == 2`;
      mockito received the `completed` webhook; `GET /v1/runs/{id}/webhook-deliveries` lists two
      `delivered` rows; (8) `GET /v1/threads/{id}/history` has ≥ 3 waypoints with one `AwaitingInput`;
      (9) `POST /v1/threads/{id}/fork { from_waypoint_id: <first>, edit: {} }` → 202 with a new
      `run_id`, and that run reaches a terminal status.
    - Whole test under a 60 s `tokio::time::timeout`; no Docker; runs in the `test` CI job.
  </behavior>
  <action>
    Create `tests/integration/e2e_platform_api_test.rs` and add `[[test]] name = "e2e_platform_api"
    path = "tests/integration/e2e_platform_api_test.rs" required-features = ["web-server"]` to
    `Cargo.toml` beside the other `e2e_*` entries. Drive the router with `tower::util::oneshot`
    (cloned per request) and read the SSE body incrementally with `http_body_util::BodyExt::frame`.
    Verify the signature with the same `hmac` primitive (import `paladin::application::services::run::webhook::sign_webhook_body`).
  </action>
  <acceptance_criteria>
    - `cargo test --features web-server --test e2e_platform_api 2>&1 | grep -qE 'test result: ok\. [1-9] passed'`.
    - `grep -c 'name = "e2e_platform_api"' Cargo.toml` is 1.
    - `grep -c 'awaiting_input' tests/integration/e2e_platform_api_test.rs` is at least 2 and `grep -c 'fork' tests/integration/e2e_platform_api_test.rs` is at least 1.
    - `grep -c 'sign_webhook_body' tests/integration/e2e_platform_api_test.rs` is at least 1.
    - `grep -c 'docker' tests/integration/e2e_platform_api_test.rs` is 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --features web-server --test e2e_platform_api 2>&amp;1 | grep -qE 'test result: ok\. [1-9] passed'</automated>
  </verify>
  <done>Acceptance 1 is one committed, Docker-free integration test that passes locally and in the `test` job.</done>
</task>

<task type="auto">
  <name>Task 2: `sdk-clients` CI job and smoke scripts; phase-wide OpenAPI diff review; §9.6</name>
  <files>.github/workflows/ci.yml, scripts/sdk-smoke/run.sh, scripts/sdk-smoke/smoke.py, scripts/sdk-smoke/smoke.ts, scripts/sdk-smoke/package.json, scripts/sdk-smoke/tsconfig.json, crates/paladin-web/openapi.json, MIGRATION.md</files>
  <read_first>
    - `.github/workflows/ci.yml` — `cli-tests` (1159+) and `crate-isolation` (515+) for build/run-binary
      job shapes; the `redis-queue` job (27-03) for the "cannot go green on nothing" steps.
    - `config.test.yml` and `src/bin/paladin-server.rs` — how to boot the server with a static API key
      and the InMemory/SQLite profile via env (`APP_RUN_STORE_BACKEND=sqlite`, `APP_RUN_STORE_PATH`,
      `APP_RUN_QUEUE_BACKEND=in_memory`, `APP_WAYPOINT_STORE_BACKEND=sqlite`, auth env from Phase 24's docs).
    - `MIGRATION.md` §9.6 (310-348) row shape; `git diff v0.9.0 -- crates/paladin-web/openapi.json`
      is NOT the review (many phases changed it); the review is `git log -p --follow` over this phase's
      commits or a diff against the pre-27-01 commit — find it with `git log --oneline --
      crates/paladin-web/openapi.json`.
    - `.planning/phases/27-platform-api/27-CONTEXT.md` D-48, D-49, D-53.
  </read_first>
  <action>
    Add a `sdk-clients` job (name "Generated SDK Clients (Python + TypeScript) smoke") running on every
    PR/push with no path filter: checkout; `dtolnay/rust-toolchain@stable`; `Swatinem/rust-cache@v2`;
    `actions/setup-python@v5` (3.12) and `actions/setup-node@v4` (20); generate with
    `docker run --rm -v "$PWD:/local" openapitools/openapi-generator-cli:<PINNED_TAG> generate -i
    /local/crates/paladin-web/openapi.json -g python -o /local/target/sdk/python` and `-g
    typescript-fetch -o /local/target/sdk/typescript` — pin `<PINNED_TAG>` to the current stable `v7.x`
    tag (look it up at execution time on Docker Hub and record it in the job as an `env:`; the npm
    package `@openapitools/openapi-generator-cli` is the documented fallback if the pull fails); assert
    each output tree contains at least 20 files (`find … -type f | wc -l`); build the server
    `cargo build --bin paladin-server --features web-server`; `scripts/sdk-smoke/run.sh` boots it on a
    free port with the env profile above and a static API key, waits for `/health`, runs
    `pip install target/sdk/python && python scripts/sdk-smoke/smoke.py` and `cd scripts/sdk-smoke &&
    npm ci && npx tsc -p tsconfig.json && node dist/smoke.js`, then stops the server; each smoke script
    lists assistants (expects 200 and a JSON `items` array), submits a run for a code-registered agent
    id from `config.test.yml` (expects 202 and a `run_id`), polls `GET /runs/{id}` until a terminal
    status or 30 s, and exits non-zero on any deviation. Commit `scripts/sdk-smoke/{run.sh,smoke.py,
    smoke.ts,package.json,tsconfig.json}` (package.json depends only on `typescript` and the
    generated client via a relative path).

    OpenAPI review: produce the diff of `crates/paladin-web/openapi.json` from the commit before 27-01
    to HEAD, confirm every change is an added path/schema or the registered `run_id`, and paste the
    path list into the SUMMARY. Write §9.6: a table of every new endpoint (method, path, auth tier,
    pagination yes/no, plan), plus the `ResumeAcceptedResponse.run_id` line already present.
    Regenerate the public-API baseline with `./scripts/extract-public-api.sh .project/current-exports.txt`
    (cargo-public-api on nightly; the header's tool version must match CI's) and commit it — Phase 27
    adds well over a hundred public items and the `api-surface` job fails without this. Run coverage
    with CI's exact command — `cargo llvm-cov --workspace --features integration-tests,llm-all --lcov
    --output-path lcov.info --fail-under-lines 82 -- --test-threads=1` (30–60 min cold; use
    `nohup … &` plus a foreground `tail --pid` wait, never `scripts/coverage.sh`, which hard-fails
    without Redis/MinIO) — and record the workspace line-coverage figure (≥ 82%) in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `grep -c '^  sdk-clients:$' .github/workflows/ci.yml` is 1 and `grep -c 'openapi-generator-cli' .github/workflows/ci.yml` is at least 2.
    - `grep -c 'wc -l' .github/workflows/ci.yml` increased by at least 2 (the non-empty-tree guards) — `awk '/^  sdk-clients:/,/^  [a-z-]*:$/' .github/workflows/ci.yml | grep -c 'find .* -type f | wc -l'` is at least 2.
    - `test -x scripts/sdk-smoke/run.sh && python3 -m py_compile scripts/sdk-smoke/smoke.py` exits 0.
    - `grep -c 'run_id' scripts/sdk-smoke/smoke.py` is at least 1 and `grep -c 'run_id' scripts/sdk-smoke/smoke.ts` is at least 1.
    - `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0.
    - `awk '/^## 9.6/,/^## 9.7/' MIGRATION.md | grep -c '/v1/'` is at least 22 (every new route).
    - `cargo llvm-cov --workspace --features integration-tests,llm-all --lcov --output-path lcov.info --fail-under-lines 82 -- --test-threads=1` exits 0 and its summary line reports ≥ 82.00% (Tier 1 locally; CI's `coverage` job is the gate).
    - `./scripts/check-api-surface.sh .project/current-exports.txt` exits 0 after the baseline regeneration, and `git diff --stat .project/current-exports.txt` shows additions only (no removed public items — X-03).
  </acceptance_criteria>
  <verify>
    <automated>grep -q '^  sdk-clients:$' .github/workflows/ci.yml &amp;&amp; python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" &amp;&amp; python3 -m py_compile scripts/sdk-smoke/smoke.py &amp;&amp; test "$(awk '/^## 9.6/,/^## 9.7/' MIGRATION.md | grep -c '/v1/')" -ge 22</automated>
  </verify>
  <done>The spec is proven generatable and usable from two languages by a CI gate that cannot pass vacuously; the endpoint register is complete; coverage evidence is recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm the Tier-2 and SDK evidence on CI</name>
  <what-built>The full Platform API: durable runs, worker pool, cancellation, resume, streaming,
  assistants, schedules, webhooks, the HTTP surface, wiring, docs, the acceptance-1 E2E test and the
  `sdk-clients` job. Tier-2 suites (Redis queue, Postgres run/assistant/schedule/webhook repositories)
  and the SDK smoke only run on CI.</what-built>
  <how-to-verify>
    1. Push the phase branch and open (or update) the PR; wait for CI.
    2. Confirm these jobs are green and did NOT take a skip path: `redis-queue` (log contains
       "All run_queue::redis tests exercised the live server"), `postgres-integration` (declared vs
       selected counts equal, covering `waypoint`, `run`, `assistant`, `run_schedule`, `webhook`
       modules), `sdk-clients` (both "generated N files" lines show N ≥ 20; Python and TypeScript smoke
       both print a `run_id` and a terminal status), `coverage` (≥ 82%), `msrv` (1.88), `semver`,
       `api-surface`, `test` (includes `e2e_platform_api`).
    3. Optionally run a manual p99 check per 27-VALIDATION.md's manual row: boot the server on the
       SQLite+InMemory profile and submit 200 runs; record the p99 of `POST /v1/runs`.
    4. Reply with the CI run URL(s) and the job outcomes.
  </how-to-verify>
  <resume-signal>Type "approved" with the CI run URL, or describe which job failed and how.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI → Docker Hub image / npm / pip | A third-party generator image runs in CI over the spec. |
| Generated clients → test server | Smoke scripts hold a static test API key. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-18-01 | Tampering | unpinned generator image | medium | mitigate | Pinned `v7.x` tag recorded in the job; documented npm fallback also pinned. |
| T-27-18-02 | Repudiation | vacuous green SDK job | medium | mitigate | Non-empty tree guards, compile steps, real 202 + terminal status assertions (prohibition P1). |
| T-27-18-03 | Information disclosure | API key in CI logs | low | mitigate | A throwaway static key from `config.test.yml` only; never a real provider key (`LLM_API_KEY=test-key`, the `e2e-tests` job precedent). |
| T-27-18-04 | Tampering | supply-chain on `pip install`/`npm ci` of generated code | high | mitigate | Installs only the generated tree plus `typescript`; no third-party runtime packages beyond the generator's declared deps; `cargo-deny`/`audit` unaffected (no Rust deps added). |
</threat_model>

<verification>
- `cargo test --features web-server --test e2e_platform_api` passes locally.
- CI: `redis-queue`, `postgres-integration`, `sdk-clients`, `coverage`, `msrv`, `semver`, `api-surface`, `test` green with the anti-skip guards satisfied.
- CI's exact `cargo llvm-cov … --fail-under-lines 82` command exits 0 locally; `./scripts/check-api-surface.sh .project/current-exports.txt` exits 0.
</verification>

<success_criteria>
- PRD 06 acceptance criteria 1 and 7 pass; 2, 4, 5, 6 have their CI/UAT evidence; 8 (coverage) and 9 (versioning gate: §9.2–§9.6 filled, `semver` + `msrv` green) are recorded.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

See the identical section in `27-02-PLAN.md` — the canonical list of every symbol, route, table,
migration, CI job, env var, dependency promotion and doc asset Phase 27 creates. This plan's own
contribution: `tests/integration/e2e_platform_api_test.rs` (+ its `[[test]]` entry), the `sdk-clients`
CI job, `scripts/sdk-smoke/{run.sh,smoke.py,smoke.ts,package.json,tsconfig.json}`, and the §9.6 rows.
</artifacts_produced>

<output>
Create `.planning/phases/27-platform-api/27-18-SUMMARY.md` when done.
</output>
