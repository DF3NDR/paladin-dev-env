---
phase: 27-platform-api
plan: 25
type: execute
wave: 3
depends_on: [27-19, 27-20, 27-21, 27-22, 27-23, 27-24]
files_modified:
  - .planning/phases/27-platform-api/27-CI-EVIDENCE.md
autonomous: false
gap_closure: true
requirements: [PLAT-01, PLAT-02, PLAT-03, PLAT-04, PLAT-05, PLAT-06]

must_haves:
  truths:
    - "Every gap this closure set addresses is proven by a live CI run at the SHA that contains all six gap-closure plans — not by a local run, and not by a run at an older SHA (D-51)."
    - "`redis-queue` is green with 0 failed and its log shows the suite exercised a live server rather than taking the self-skip path (PLAT-02)."
    - "`postgres-integration` is green with 0 failed across the waypoint, run, assistant, run_schedule and webhook modules, with declared-vs-selected counts equal (PLAT-01, PLAT-04, PLAT-05)."
    - "`coverage` completes — no exit 101 from `paladin-storage` — and reports at least 82% workspace line coverage, the ADR-0006 floor (PLAT-01, PLAT-02)."
    - "`sdk-clients` is green with both generated-client smokes run: each prints a `run_id` and reports the run reaching exactly `completed` (PLAT-06)."
    - "`api-surface` reports `API surface unchanged` on CI's own toolchain (PLAT-06)."
    - "`e2e-platform-api` is green and its log shows a non-zero passing test count for the `e2e_platform_api` binary (PLAT-03, PLAT-06)."
    - "`test`, `msrv` (1.88) and `semver` are green, and `make security` (cargo-audit + cargo-deny) passes over the changed credential/HTTP code (PLAT-05)."
    - "The evidence is written down: `27-CI-EVIDENCE.md` records the run id, the SHA, and per-job outcome plus the exact log line that proves it, so a later reader can re-check the claim rather than trusting a summary."
  prohibitions:
    - statement: "A Tier-2 suite MUST NOT be recorded as passed on local evidence, on a self-skip path, or on a CI run predating the gap-closure commits (D-51)."
      category: process
    - statement: "A job MUST NOT be recorded green on the basis of an overall conclusion alone when the plan names a specific log string as the proof."
      category: process
    - statement: "The phase MUST NOT be re-verified while any of the six gap-closure plans is unmerged."
      category: process
  artifacts:
    - path: ".planning/phases/27-platform-api/27-CI-EVIDENCE.md"
      provides: "the recorded local sweep and per-job CI evidence table for the gap-closure SHA"
      contains: "run id"
  key_links:
    - from: ".planning/phases/27-platform-api/27-CI-EVIDENCE.md"
      to: ".planning/phases/27-platform-api/27-VERIFICATION.md"
      via: "each of the five recorded gaps maps to the job that now proves it closed"
      pattern: "redis-queue|postgres-integration|sdk-clients|api-surface|e2e-platform-api"
---

<objective>
Close the two `human_verification` items 27-VERIFICATION.md left open, and give the five gaps a single
place where their closure is evidenced.

Every fix in this set lands in code that only a live CI run can exercise: Redis and Postgres suites
need services this devcontainer has no Docker for; `coverage` currently aborts with exit 101 before
computing a percentage; `sdk-clients` needs Java or Docker for the openapi generator; and
`api-surface` only drifts on CI's own nightly. The fixes are proven locally as far as local proof can
go — this plan is where the remaining half of the evidence is collected and written down.

Purpose: PLAT-01 through PLAT-06 — the phase's re-verification depends on evidence that exists only
after a CI run at the closing SHA.

Output: `27-CI-EVIDENCE.md` with the local sweep and the per-job CI results, and a blocking human
checkpoint that cannot be satisfied by a green-looking summary.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-platform-api/27-VERIFICATION.md
@.planning/phases/27-platform-api/27-VALIDATION.md
@.planning/phases/27-platform-api/27-18-SUMMARY.md
@CLAUDE.md

<interfaces>
The failing baseline to compare against is CI run `34222317640` at SHA `1e81939a`: `redis-queue`
13 passed / 3 failed; `postgres-integration` 83 passed / 3 failed; `coverage` exit 101 with no
percentage; `sdk-clients` exit 1; `api-surface` failed; `e2e_platform_api` not run by any job.
`.github/workflows/ci.yml` job display names: `Redis Run Queue Contract Suite (live server)`,
`Postgres Storage Contract Suites (live server)`, `Coverage`, `Generated SDK Clients (Python +
TypeScript) smoke`, `API Surface Tracking`, `Unit Tests`, `MSRV (Rust 1.88)`,
`Semver Checks (vs v0.9.0)`, plus the new `e2e-platform-api` job added by plan 27-24.
Coverage invocation (CI and `scripts/coverage.sh`): `cargo llvm-cov --workspace --features
integration-tests,llm-all --fail-under-lines 82 -- --test-threads=1`. It hard-fails locally without
Redis/MinIO, so it is a CI/UAT command only (27-VALIDATION.md).
Two local traps this plan's sweep must not fall into (27-VALIDATION.md): doc tests do not count toward
`cargo llvm-cov` and are skipped by `--tests`, so they are run separately; and a test filter selecting
zero tests still exits 0, so every recorded command must show a non-zero passing count.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full local gate sweep and open the evidence record</name>
  <files>.planning/phases/27-platform-api/27-CI-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/27-platform-api/27-VERIFICATION.md` — the five gaps, their `missing` lists and
      both `human_verification` items, which this record answers one by one.
    - `.planning/phases/27-platform-api/27-VALIDATION.md` — the sampling rules, the tier rule and the
      two verification traps.
    - The six gap-closure SUMMARYs (`27-19` … `27-24`) — what each plan actually changed and what it
      already proved locally, so the record cites rather than repeats.
    - `CLAUDE.md` — the required gate commands (`cargo test`, `cargo fmt --check`, `cargo clippy -- -D
      warnings`, `make security`) and the 82% coverage floor.
  </read_first>
  <behavior>
    - The sweep runs the whole workspace, not a filtered subset, and each recorded command shows a
      non-zero passing count.
    - Doc tests are run as their own command, because the coverage invocation does not count them.
    - A locally unreachable Redis or Postgres produces a self-skip, which is recorded as "not evidence"
      rather than as a pass.
    - The record names the exact CI jobs and log strings the checkpoint will look for, before the run
      happens, so the bar cannot be lowered after seeing the results.
  </behavior>
  <action>
    Run and record, verbatim (command plus the result line): `cargo test --workspace --lib --bins`,
    `cargo test --workspace --doc`, `cargo test --features web-server --test e2e_platform_api`,
    `cargo fmt --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `make security`, and
    `./scripts/check-api-surface.sh .project/current-exports.txt`. Also re-run each gap-closure plan's
    own headline check (`claim_marker`, `run::` under the sqlite feature, `mock-llm.py --self-test` and
    `smoke.py --self-test`, `bounded_body_`, `lease_heartbeat_with_a_zero_lease`,
    `normalize-api-bounds.py --self-test`).

    Create `.planning/phases/27-platform-api/27-CI-EVIDENCE.md` with: a header naming the phase, the
    branch and the HEAD SHA at the time of writing; a **Local sweep** table (command, result line,
    verdict); a **Tier-2 / CI evidence** table pre-filled with one row per job the checkpoint must
    confirm — `redis-queue`, `postgres-integration`, `coverage`, `sdk-clients`, `api-surface`,
    `e2e-platform-api`, `test`, `msrv`, `semver` — each carrying the gap it closes, the exact log
    string or numeric threshold that counts as proof, and an empty result column; and a
    **Baseline** section quoting run `34222317640`'s failing numbers so the comparison is explicit.
    Leave the CI columns empty — filling them is the checkpoint's job, not this task's.
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/27-platform-api/27-CI-EVIDENCE.md` exits 0.
    - `grep -c 'redis-queue' .planning/phases/27-platform-api/27-CI-EVIDENCE.md` is at least 1, and the same for `postgres-integration`, `coverage`, `sdk-clients`, `api-surface` and `e2e-platform-api`.
    - `grep -c '34222317640' .planning/phases/27-platform-api/27-CI-EVIDENCE.md` is at least 1.
    - `cargo test --workspace --lib --bins 2>&1 | grep -qE 'test result: ok\. [1-9][0-9]* passed'`.
    - `cargo test --workspace --doc 2>&1 | grep -qE 'test result: ok\. [1-9][0-9]* passed'`.
    - `cargo fmt --check` exits 0; `cargo clippy --workspace --all-targets -- -D warnings` exits 0; `make security` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --workspace --lib --bins 2>&amp;1 | grep -qE 'test result: ok\. [1-9][0-9]* passed' &amp;&amp; cargo test --workspace --doc 2>&amp;1 | grep -qE 'test result: ok\. [1-9][0-9]* passed' &amp;&amp; test -f .planning/phases/27-platform-api/27-CI-EVIDENCE.md</automated>
  </verify>
  <done>Every locally provable gate is green and recorded, and the evidence file states the CI bar in advance with empty result columns.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm the gap closure on a live CI run and fill in the evidence</name>
  <what-built>Six gap-closure plans: the Redis claim/nack attempt semantics (27-19), the Postgres run
  timestamp precision contract (27-20), the hermetic `sdk-clients` smoke with an exact success status
  (27-21), the bounded webhook response read and the signing-key failure path (27-22), the heartbeat
  guard and two documented limitations (27-23), and the canonical API-surface baseline plus the new
  `e2e-platform-api` CI job (27-24). Five of the six can only be proven on CI: this devcontainer has no
  Docker, so Redis, Postgres, the coverage run and the openapi generator are all unavailable, and the
  api-surface drift only appears on CI's own nightly.</what-built>
  <how-to-verify>
    1. Push the branch (or update the PR) at the SHA that contains all six gap-closure commits, and
       wait for `.github/workflows/ci.yml` to finish. Record the run id and the SHA.
    2. Confirm each job below is green AND shows its named proof — an overall green conclusion alone
       is not sufficient for the rows that name a log string:
       - `Redis Run Queue Contract Suite (live server)` — 0 failed, 16 clauses, and the log line
         reporting the suite exercised the live server (a self-skip is NOT a pass).
       - `Postgres Storage Contract Suites (live server)` — 0 failed; declared-vs-selected counts equal
         across `waypoint`, `run`, `assistant`, `run_schedule` and `webhook`; in particular the three
         previously failing `run::postgres` clauses and the new
         `postgres_run_timestamps_round_trip_at_microsecond_precision` all pass.
       - `Coverage` — completes with no exit 101 and reports at least 82% workspace line coverage.
         Record the percentage.
       - `Generated SDK Clients (Python + TypeScript) smoke` — both "generated N files" lines show
         N ≥ 20; both smokes print a `run_id`; both report the terminal status `completed`; and the
         TypeScript half actually ran (no `npm ci` lockfile error).
       - `API Surface Tracking` — the log contains `API surface unchanged`.
       - `e2e-platform-api` — green, with a passing test-result line showing a non-zero count.
       - `Unit Tests`, `MSRV (Rust 1.88)`, `Semver Checks (vs v0.9.0)` — green.
    3. Fill the CI columns of `.planning/phases/27-platform-api/27-CI-EVIDENCE.md` with the run id, the
       SHA, and for each job its conclusion plus the quoted log line or number that proves it.
    4. If any job is red, do not approve: report which job, which assertion, and the log excerpt, so
       the failure is planned against rather than re-run hopefully.
  </how-to-verify>
  <resume-signal>Type "approved" with the CI run URL and the coverage percentage, or describe which job failed, with the failing assertion and a log excerpt.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI run → phase verification record | A job's reported conclusion becomes the project's evidence that a gap is closed. |
| Local environment → claimed evidence | A devcontainer without Docker can produce green-looking results that prove nothing about Tier-2 paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-25-01 | Repudiation | evidence attributed to the wrong SHA or an older run | high | mitigate | The record carries the run id and the SHA, and the checkpoint requires the SHA to contain all six gap-closure commits. |
| T-27-25-02 | Repudiation | a self-skipping suite recorded as a pass | high | mitigate | Each Tier-2 row names the log string that distinguishes a live run from a skip; a conclusion alone does not satisfy those rows. |
| T-27-25-03 | Tampering | the bar being lowered after seeing results | medium | mitigate | The thresholds and log strings are written into the evidence file before the run, with empty result columns. |
| T-27-25-04 | Information disclosure | CI logs quoted into the record carrying secrets | medium | mitigate | Only the named result lines and counts are quoted, never environment dumps or raw request/response bodies. |
</threat_model>

<verification>
- Task 1's local sweep, all green, each with a non-zero passing count.
- The checkpoint's nine job rows, each with its named proof, recorded in `27-CI-EVIDENCE.md`.
- After approval, `/gsd-verify-work 27` (or the orchestrator's re-verification) can cite this file
  rather than re-deriving the evidence.
</verification>

<success_criteria>
- All five 27-VERIFICATION.md gaps and both `human_verification` items are answered by a named job on
  a named run at a named SHA, with the proof quoted — and the answer is recorded where the next reader
  will find it.
</success_criteria>

## Artifacts this phase produces

- `.planning/phases/27-platform-api/27-CI-EVIDENCE.md` — new file: the local sweep table, the per-job
  CI evidence table (job, gap closed, required proof, result), and the quoted failing baseline from
  run `34222317640`.

## Edge coverage lift (spec-less probe fallback)

Flagged assumptions carried here (unresolved, surfaced rather than dropped). All six are PLAT-04
(assistant versioning and `WarGraphDoc`) probes; PLAT-04 is untouched by this gap-closure set, and the
phase verification already recorded SC4 and the `assistant::postgres` suite as fully green, so no new
predicate is authored for them:

- **PLAT-04 `boundary`** — thresholds around assistant version numbers (first version, `latest`
  freeze at submit) are exercised by 27-09's `assistant_version_freeze_at_submit`, not re-derived here.
- **PLAT-04 `adjacency`** — two publishes landing on the same version number are governed by the
  `(assistant_id, version)` primary key; not re-derived here.
- **PLAT-04 `empty`** — an empty or single-node `WarGraphDoc` is covered by 27-05's fixture corpus.
- **PLAT-04 `ordering`** — version listing order is fixed by the append-only version key.
- **PLAT-04 `idempotency`** — re-publishing an identical document produces a new immutable version, no
  in-place update (there is no update method on the port).
- **PLAT-04 `concurrency`** — concurrent publish versus submit is proven by 27-09's freeze-at-submit
  multi-thread clause.

<output>
Create `.planning/phases/27-platform-api/27-25-SUMMARY.md` when done.
</output>
