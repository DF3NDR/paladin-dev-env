---
phase: 28-observability-tooling
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/trace.rs
  - src/config/mod.rs
  - src/config/settings.rs
  - src/config/web_server.rs
  - config.example.yml
  - config.test.yml
autonomous: true
requirements: [OBS-01, OBS-02]

must_haves:
  truths:
    - "`TraceConfig` deserializes from an absent `trace:` section (every field `#[serde(default)]`) with `log_sink = true`, `channel_capacity = 1024`, `persist = false`, `state_values = false`, `value_cap_bytes = 256`, `heartbeat_interval_secs = 5` and `otel.enabled = false` — a v0.9 config file boots unchanged (D-36, X-03)."
    - "`TraceConfig::validate()` rejects `channel_capacity == 0`, `value_cap_bytes == 0`, a non-`http(s)` `otel.endpoint` when `otel.enabled`, and `otel.enabled` on a build compiled without the `otel` feature — the last as a typed `TraceConfigError::FeatureNotCompiled { feature: \"otel\" }`, never a silent no-op (D-36)."
    - "`OtelConfig`'s `Debug` output never contains a header value: formatting a config whose `headers` map holds a secret prints a redaction marker and the key names only (D-36, security instructions)."
    - "`PALADIN_TRACE_*` environment overrides apply through `EnvOverridable` for every scalar field, and the deliberate divergence from the workspace's `APP_*` prefix is recorded in the module rustdoc."
    - "`web_server.dev_ui.mermaid_url` defaults to the jsDelivr `mermaid@11` ESM bundle URL and is settable from config, so an air-gapped operator can point it at a local mirror (D-26, D-36)."
    - "`config.example.yml` and `config.test.yml` both carry the full `trace:` section with every default spelled out, and both still deserialize into `Settings`."
  artifacts:
    - path: "src/config/trace.rs"
      provides: "TraceConfig, OtelConfig, TraceConfigError, Default/validate/EnvOverridable impls"
      contains: "pub struct TraceConfig"
    - path: "config.example.yml"
      provides: "documented trace: section with every default"
      contains: "value_cap_bytes"
  key_links:
    - from: "src/config/settings.rs"
      to: "src/config/trace.rs"
      via: "Settings gains a `trace: TraceConfig` section with #[serde(default)] and calls TraceConfig::validate() from Settings::validate()"
      pattern: "trace: TraceConfig"
---

<objective>
Give every tunable this phase introduces a real home: `TraceConfig` (with nested `OtelConfig`) in
`src/config/trace.rs` following the house `Default` + `validate()` + `EnvOverridable` shape, plus
`web_server.dev_ui.mermaid_url`, wired into `Settings` and mirrored in both config YAML files.

Purpose: X-09 and D-36. Every later plan reads these values — the dispatcher capacity (28-01/28-06),
the heartbeat rate limit (28-03), the log-sink default-on switch (28-06), the OTLP endpoint and
headers (28-09), the persistence switch (28-11) and the inspector's Mermaid URL (28-15).

Output: `src/config/trace.rs`, `Settings.trace`, `WebServerConfig.dev_ui`, and the two YAML files.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-observability-tooling/28-CONTEXT.md
@.planning/phases/28-observability-tooling/28-RESEARCH.md
@.planning/phases/28-observability-tooling/28-PATTERNS.md
@.project/v0.10.0/07-observability-tooling.md
@.github/instructions/security.instructions.md
@CLAUDE.md

<interfaces>
House config shape (`src/config/run_stream.rs`, read it in full — it is short): a
`#[derive(Debug, Clone, ..., Serialize, Deserialize)]` struct, a manual `impl Default`, an
`impl X { pub fn validate(&self) -> Result<(), String> }`, and an
`impl EnvOverridable for X { fn apply_env_overrides(&mut self) { if let Some(v) =
read_env::<T>("APP_...") { .. } } }`, with `use crate::config::env_utils::{EnvOverridable, read_env};`.
`Settings::validate()` (`src/config/settings.rs:88`) currently delegates to `agent_runtime.validate()`
and returns `Result<(), String>` — that signature is pre-existing public surface and must not change
(X-03).

`src/config/waypoint_retention.rs` confirms the workspace's env prefix family is `APP_*`
(`APP_WAYPOINT_RETENTION_*`). D-36 nonetheless locks `PALADIN_TRACE_*` for this phase's variables and
D-39 records them in `MIGRATION.md` §9.5 under that name; the locked decision wins and the divergence
is documented rather than silently normalised.

No `ConfigError` enum exists anywhere under `src/config/` — every `validate()` in the tree returns
`Result<(), String>`.

Mermaid ESM URL (28-RESEARCH.md Q11):
`https://cdn.jsdelivr.net/npm/mermaid@11/dist/mermaid.esm.min.mjs`.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end `trace:` config — YAML file to validated `Settings.trace`, one path only</name>
  <files>src/config/trace.rs, src/config/mod.rs, src/config/settings.rs, config.test.yml</files>
  <read_first>
    - `src/config/run_stream.rs` in full — the struct/Default/validate/EnvOverridable/doc-test shape to copy verbatim.
    - `src/config/waypoint_retention.rs` — a nested-section example and the `APP_*` prefix evidence.
    - `src/config/settings.rs` around the section fields and `validate()` (line 88).
    - `src/config/env_utils.rs` — `EnvOverridable` and `read_env`.
    - `config.test.yml` — the tracked file that boots `paladin-server` cleanly.
    - `.planning/phases/28-observability-tooling/28-CONTEXT.md` D-36; `28-PATTERNS.md` `src/config/trace.rs` section.
  </read_first>
  <behavior>
    - `Settings::load_from_file` on a `config.test.yml` carrying a `trace:` section with a single
      overridden field (`channel_capacity: 32`) yields `settings.trace.channel_capacity == 32` and
      house defaults for every other field.
    - `Settings::load_from_file` on the same file with the `trace:` section deleted yields
      `TraceConfig::default()` — the section is optional (X-03 backward compatibility).
    - `Settings::validate()` returns `Err` when `trace.channel_capacity` is 0, with a message naming
      the field.
  </behavior>
  <action>
    Create `src/config/trace.rs` with `pub struct TraceConfig { log_sink: bool, channel_capacity:
    usize, persist: bool, state_values: bool, value_cap_bytes: usize, heartbeat_interval_secs: u64,
    otel: OtelConfig }`, all fields `pub` and `#[serde(default)]`, plus a manual `impl Default`
    returning `true / 1024 / false / false / 256 / 5 / OtelConfig::default()`. Give it a
    `validate(&self) -> Result<(), String>` matching the house signature. Declare `pub mod trace;` in
    `src/config/mod.rs` and re-export `TraceConfig` beside the other config types. Add
    `#[serde(default)] pub trace: TraceConfig` to `Settings` and call `self.trace.validate()?` from
    `Settings::validate()` (keeping its `Result<(), String>` signature — X-03). Add the `trace:`
    section to `config.test.yml` with a non-default `channel_capacity` so the round-trip test has
    something to prove. Write the three `<behavior>` tests in `trace.rs`'s test module plus the
    house-style module doc with a runnable `# Examples` block (doc test) copied in shape from
    `run_stream.rs`.
  </action>
  <acceptance_criteria>
    - `cargo test -p paladin-ai --lib config::trace 2>&1 | grep -qE 'test result: ok\. [1-9][0-9]* passed'`.
    - `grep -c 'pub struct TraceConfig' src/config/trace.rs` is 1 and `grep -c 'pub mod trace;' src/config/mod.rs` is 1.
    - `grep -c 'trace: TraceConfig' src/config/settings.rs` is 1.
    - `grep -c 'trace:' config.test.yml` is at least 1.
    - `cargo test -p paladin-ai --doc config::trace 2>&1 | grep -qE 'test result: ok\. [1-9] passed'`.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p paladin-ai --lib config::trace 2>&amp;1 | grep -qE 'test result: ok\. [1-9][0-9]* passed'</automated>
  </verify>
  <done>A `trace:` section in a config file reaches `Settings.trace` validated, and its absence yields the documented defaults.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `OtelConfig` with redacting `Debug`, the typed feature-not-compiled error, `PALADIN_TRACE_*` overrides</name>
  <files>src/config/trace.rs</files>
  <read_first>
    - `src/config/trace.rs` as written in Task 1.
    - `crates/paladin-core/src/platform/container/heartbeat.rs:106-122` — the manual `Debug` precedent shape.
    - `.github/instructions/security.instructions.md` — the credential-handling rules (no secret in a `Debug` output, no log line interpolating a header value).
    - `.planning/phases/28-observability-tooling/28-CONTEXT.md` D-36; `.project/v0.10.0/00-program-overview.md` X-06, X-09.
  </read_first>
  <behavior>
    - `OtelConfig::default()` is `{ enabled: false, endpoint: "http://localhost:4318/v1/traces", headers: {} (BTreeMap), service_name: "paladin" }`.
    - `otel_debug_redacts_header_values`: `format!("{:?}", cfg)` for a config whose `headers` holds `("authorization", "Bearer sk-secret")` contains `authorization` and does not contain `sk-secret`.
    - `validate` rejects, each with a distinct typed variant: `channel_capacity == 0`; `value_cap_bytes == 0`; `otel.enabled` with an endpoint whose scheme is neither `http` nor `https`; and `otel.enabled` on a build without the `otel` feature.
    - `trace_env_overrides_apply`: setting `PALADIN_TRACE_LOG_SINK=false`, `PALADIN_TRACE_CHANNEL_CAPACITY=64`, `PALADIN_TRACE_PERSIST=true`, `PALADIN_TRACE_STATE_VALUES=true`, `PALADIN_TRACE_VALUE_CAP_BYTES=64`, `PALADIN_TRACE_HEARTBEAT_INTERVAL_SECS=1`, `PALADIN_TRACE_OTEL_ENABLED=true`, `PALADIN_TRACE_OTEL_ENDPOINT`, `PALADIN_TRACE_OTEL_SERVICE_NAME` and calling `apply_env_overrides` changes exactly those fields.
  </behavior>
  <action>
    Add `pub struct OtelConfig { pub enabled: bool, pub endpoint: String, pub headers:
    BTreeMap<String, String>, pub service_name: String }` with a manual `Default` and a
    hand-written `impl fmt::Debug` that prints the header KEYS with each value replaced by a fixed
    redaction marker, following `HeartbeatHandle`'s manual-impl style; derive `Debug` nowhere on this
    type. Add `#[derive(Debug, thiserror::Error)] #[non_exhaustive] pub enum TraceConfigError` with
    `ZeroChannelCapacity`, `ZeroValueCapBytes`, `EndpointNotHttp { scheme: String }` and
    `FeatureNotCompiled { feature: &'static str }` (X-06 — structured fields, no stringly variants).
    Add `pub fn validate_typed(&self) -> Result<(), TraceConfigError>` implementing the four rules,
    with the feature check written as a `cfg!(feature = "otel")` test so the error fires on a build
    that lacks the feature; make the existing house-signature `validate()` a thin
    `self.validate_typed().map_err(|e| e.to_string())` wrapper so `Settings::validate()` composes
    unchanged. Implement `EnvOverridable` for `TraceConfig` reading the nine `PALADIN_TRACE_*`
    variables listed in `<behavior>` through `read_env`, and state in the module rustdoc that this
    phase uses the `PALADIN_TRACE_*` prefix per a locked phase decision even though the rest of
    `src/config/` uses `APP_*`, so a future reader does not treat it as a typo. Write the four
    `<behavior>` tests, serialising the env-var test the same way `waypoint_retention.rs` does.
  </action>
  <acceptance_criteria>
    - `cargo test -p paladin-ai --lib config::trace 2>&1 | grep -qE 'test result: ok\. ([8-9]|[1-9][0-9]) passed'`.
    - `cargo test -p paladin-ai --lib config::trace::tests::otel_debug_redacts_header_values 2>&1 | grep -qE 'test result: ok\. 1 passed'`.
    - `grep -c 'derive' src/config/trace.rs` shows no `Debug` derive on `OtelConfig`: `sed -n '/pub struct OtelConfig/,+0p' src/config/trace.rs` is preceded by no `Debug` in its derive list, and `grep -c 'impl fmt::Debug for OtelConfig' src/config/trace.rs` is 1.
    - `grep -c 'FeatureNotCompiled' src/config/trace.rs` is at least 2 (variant plus the raising site).
    - `grep -c 'PALADIN_TRACE_' src/config/trace.rs` is at least 9.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p paladin-ai --lib config::trace 2>&amp;1 | grep -qE 'test result: ok\. ([8-9]|[1-9][0-9]) passed'</automated>
  </verify>
  <done>OTLP endpoint, headers and service name are configurable, headers never reach a `Debug` output, an impossible feature/config combination is a typed error, and every field has a `PALADIN_TRACE_*` override.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: `web_server.dev_ui.mermaid_url` and the documented YAML sections</name>
  <files>src/config/web_server.rs, config.example.yml, config.test.yml</files>
  <read_first>
    - `src/config/web_server.rs` in full — `WebServerConfig`'s existing sections, `Default`, `validate`, `EnvOverridable`.
    - `config.example.yml` — the commenting style used for every other documented section.
    - `.planning/phases/28-observability-tooling/28-CONTEXT.md` D-26, D-36; `28-UI-SPEC.md` "External asset" note.
  </read_first>
  <behavior>
    - `WebServerConfig::default().dev_ui.mermaid_url` is `https://cdn.jsdelivr.net/npm/mermaid@11/dist/mermaid.esm.min.mjs`.
    - A `web_server: { dev_ui: { mermaid_url: "/static/mermaid.esm.min.mjs" } }` section loads that value; an absent `dev_ui:` section yields the default.
    - `Settings::load_from_file("config.example.yml")` succeeds and `Settings::validate()` returns `Ok` for it.
  </behavior>
  <action>
    Add `pub struct DevUiConfig { pub mermaid_url: String }` to `src/config/web_server.rs` with a
    `Default` returning the jsDelivr ESM URL above, and `#[serde(default)] pub dev_ui: DevUiConfig` on
    `WebServerConfig`; extend that struct's `EnvOverridable` with `PALADIN_TRACE`-family-consistent
    naming for this one field (`APP_WEB_SERVER_DEV_UI_MERMAID_URL`, matching the surrounding
    `web_server` section's existing prefix rather than the trace family — this field belongs to
    `web_server`, not to `trace`). Add the full `trace:` section to `config.example.yml` with every
    field spelled out at its default value and a comment per field (what it does, and for
    `state_values` that values are redacted then capped and never reach the SSE wire), plus the
    `web_server.dev_ui.mermaid_url` line with a comment naming the air-gapped local-mirror use. Mirror
    the same sections into `config.test.yml`. Write the three `<behavior>` tests, the third as a test
    that loads the repository's own `config.example.yml` by path.
  </action>
  <acceptance_criteria>
    - `cargo test -p paladin-ai --lib config::web_server 2>&1 | grep -qE 'test result: ok\. [1-9][0-9]* passed'`.
    - `grep -c 'mermaid_url' src/config/web_server.rs` is at least 2 and `grep -c 'mermaid_url' config.example.yml` is 1.
    - `grep -c 'heartbeat_interval_secs' config.example.yml` is 1 and `grep -c 'value_cap_bytes' config.example.yml` is 1 and `grep -c 'service_name' config.example.yml` is at least 1.
    - `grep -c 'value_cap_bytes' config.test.yml` is 1.
    - A test named `example_config_still_loads_and_validates` exists and passes.
    - `cargo clippy -p paladin-ai --all-targets -- -D warnings` and `cargo fmt --check` pass.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p paladin-ai --lib config::web_server 2>&amp;1 | grep -qE 'test result: ok\. [1-9][0-9]* passed' &amp;&amp; cargo test -p paladin-ai --lib example_config_still_loads_and_validates 2>&amp;1 | grep -qE 'test result: ok\. 1 passed'</automated>
  </verify>
  <done>The inspector's Mermaid source is operator-configurable and both shipped config files document every new key without breaking `Settings` deserialization.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator config file → process memory | OTLP headers are credential-shaped values entering a config struct. |
| Config struct → logs / error messages | A `Debug`-formatted config or a validation error could carry a secret. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-02-01 | Information disclosure | `OtelConfig.headers` in `Debug` output or logs | high | mitigate | Manual `Debug` printing header keys with redacted values; no `Debug` derive on the type; no log line interpolates a header value (D-36). |
| T-28-02-02 | Spoofing | `otel.endpoint` pointing at a non-`http(s)` or malformed destination | medium | mitigate | `validate_typed` rejects any non-`http(s)` scheme when `otel.enabled`; the exporter's own no-redirect client is 28-09's control. |
| T-28-02-03 | Repudiation | `otel.enabled` silently doing nothing on a build without the feature | medium | mitigate | Typed `FeatureNotCompiled` error at validation time, never a silent no-op (D-36). |
| T-28-02-04 | Information disclosure | `state_values` accidentally on in a shipped example config | medium | accept | Default is `false` in both `Default` and both YAML files; the comment states the consequence. Residual risk is an operator's deliberate change. |
</threat_model>

<verification>
- `cargo test -p paladin-ai --lib config::trace` and `config::web_server` green; the doc test for `TraceConfig` green.
- `Settings::load_from_file("config.example.yml")` + `validate()` green.
- `cargo fmt --check`, `cargo clippy --workspace --all-targets -- -D warnings`.
</verification>

<success_criteria>
- X-09 is satisfied for every tunable this phase introduces: `Default`, `validate()`, `EnvOverridable`, doc test, both YAML files.
- A v0.9-era config file with no `trace:` section still loads and validates (X-03).
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

See the identical section in `28-01-PLAN.md` — the canonical list of every symbol, route, table,
migration, feature, env var, CLI subcommand, golden and doc asset Phase 28 creates. This plan's own
contribution: `src/config/trace.rs` (`TraceConfig`, `OtelConfig`, `TraceConfigError` with
`ZeroChannelCapacity`/`ZeroValueCapBytes`/`EndpointNotHttp`/`FeatureNotCompiled`,
`TraceConfig::validate_typed`), `Settings.trace`, `DevUiConfig`/`WebServerConfig.dev_ui`
(`mermaid_url`), the `PALADIN_TRACE_*` env-var family, and the documented `trace:` /
`web_server.dev_ui:` sections in `config.example.yml` and `config.test.yml`.
</artifacts_produced>

<output>
Create `.planning/phases/28-observability-tooling/28-02-SUMMARY.md` when done.
</output>
