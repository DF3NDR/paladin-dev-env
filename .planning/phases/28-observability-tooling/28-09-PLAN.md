---
phase: 28-observability-tooling
plan: 09
type: execute
wave: 4
depends_on: [28-06]
files_modified:
  - src/infrastructure/telemetry/otel_sink.rs
  - src/infrastructure/telemetry/mod.rs
  - Cargo.toml
  - Cargo.lock
  - tests/integration/otel_transport_test.rs
  - tests/integration/mod.rs
  - .github/workflows/feature-flags.yml
autonomous: true
requirements: [OBS-02]

must_haves:
  truths:
    - "The OTel exporter compiles only under the umbrella feature `otel`, which is absent from `default` and from `full`'s explicit member list, following the `storage-postgres` / `redis-cache` precedent and X-11.4."
    - "The span model is derived from the record stream alone with no engine internals: one `run` root span per `thread_id` opened on `RunStarted` with `start_time = at` and closed on `RunFinished` with `end_with_timestamp`, and one child span per `(node_id, attempt)` opened on `NodeStarted` and closed on `NodeFinished` — so retries are sibling spans, not nested ones (D-12, OBS-FR-05)."
    - "Attempt spans carry the attributes `node_id`, `superstep`, `attempt`, `outcome`, `tokens`, `cache_hit` and `muster_task_key`; `EdgeEvaluated`, `DeltaMerged`, `ParleyRaised`, `FallbackHop` and `MiddlewareEvent` are span EVENTS on the enclosing span — the attempt span when a `node_id` is present, otherwise the run span (D-12)."
    - "A record arriving for a span that was never opened (a dropped `NodeStarted`) opens a synthetic span flagged `paladin.trace.partial = true` rather than being discarded — a dropped event degrades the trace, never loses the node (D-12)."
    - "The span-tree shape is asserted in-process against `opentelemetry_sdk`'s in-memory span exporter on a branch + retry + muster fixture: one root, N attempt children, the retried node present as two siblings with `attempt` 1 and 2, and edge and delta events on the right spans (D-13a, PRD 07 acceptance 2)."
    - "The transport is asserted against a hermetic axum stub: a request reaches the stub with `Content-Type: application/x-protobuf`, the configured headers, and a body decoding to a resource carrying the configured `service.name` (D-13b)."
    - "The exporter's HTTP client is built with redirects disabled, so a `3xx` can never forward the configured headers to a different host — the same rule the webhook client and every LLM adapter already follow."
    - "The CI feature-flags matrix gains an `otel` leg building with that feature alone."
  prohibitions:
    - statement: "The OTel exporter MUST NOT log, `Debug`-print, serialize into an error body, or otherwise emit the VALUES of `OtelConfig.headers`, and MUST NOT follow an HTTP redirect that would carry them to a host other than the configured endpoint. (Generic SSRF and injection classes are canon — covered by `/gsd-secure-phase`, `make security` and the manual credential-handling review in `.github/instructions/security.instructions.md`; not minted here.)"
      category: privacy
      status: resolved
      verification: judgment
  artifacts:
    - path: "src/infrastructure/telemetry/otel_sink.rs"
      provides: "OtelTraceSink, the span-tree builder, the no-redirect OTLP client construction"
      contains: "Policy::none()"
    - path: "tests/integration/otel_transport_test.rs"
      provides: "hermetic axum OTLP stub transport assertion"
      contains: "application/x-protobuf"
    - path: ".github/workflows/feature-flags.yml"
      provides: "an otel leg in the feature matrix"
      contains: "otel"
  key_links:
    - from: "src/infrastructure/telemetry/mod.rs"
      to: "src/infrastructure/telemetry/otel_sink.rs"
      via: "build_run_sink adds the OTel sink to the composite when the feature is compiled and otel.enabled is set"
      pattern: "OtelTraceSink"
    - from: "src/infrastructure/telemetry/otel_sink.rs"
      to: "src/config/trace.rs"
      via: "endpoint, headers and service_name come from OtelConfig"
      pattern: "OtelConfig"
---

<objective>
Send the trace stream to a real tracing backend: an `otel`-gated OTLP-over-HTTP exporter that turns
records into a span-per-attempt tree, verified twice — tree shape in process against an in-memory
exporter, transport against a hermetic axum stub.

Purpose: OBS-02 / OBS-FR-05 per D-12 and D-13, and PRD 07 acceptance 2.

Output: `src/infrastructure/telemetry/otel_sink.rs`, the `otel` umbrella feature and its three
optional dependencies, the transport integration test, and the CI feature-flags leg.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-observability-tooling/28-CONTEXT.md
@.planning/phases/28-observability-tooling/28-RESEARCH.md
@.planning/phases/28-observability-tooling/28-PATTERNS.md
@.planning/phases/28-observability-tooling/28-06-SUMMARY.md
@.project/v0.10.0/07-observability-tooling.md
@.github/instructions/security.instructions.md
@CLAUDE.md

<interfaces>
Pinned versions (28-RESEARCH.md Q1, live `cargo info` on 2026-09-08 — re-verify before starting if
more than a few days have passed, the OTel family ships frequent point releases):
`opentelemetry 0.32.0` (Apache-2.0, MSRV 1.75), `opentelemetry_sdk 0.32.1` (Apache-2.0, MSRV 1.75),
`opentelemetry-otlp 0.32.0` (Apache-2.0, MSRV 1.75). All three cleared the package-legitimacy audit
with an `OK` verdict and are already covered by `deny.toml`'s licence allowlist — no `deny.toml` edit.

Dependency lines must be `default-features = false, features = ["http-proto", "reqwest-client",
"trace"]` on `opentelemetry-otlp`: its DEFAULT feature set includes `reqwest-blocking-client`, which
is not the async client D-12 specifies.

`InMemorySpanExporter` lives at `opentelemetry_sdk::testing::trace::InMemorySpanExporter` behind the
`testing` cargo feature — a dev-only need that must not leak into the production `otel` feature's
dependency set.

`SpanBuilder::with_start_time<T: Into<SystemTime>>` and the `Span` trait's required
`end_with_timestamp(&mut self, timestamp: SystemTime)` are both confirmed present at these versions.

No-redirect client (28-PATTERNS.md, vetted against docs.rs):
`reqwest::Client::builder().redirect(reqwest::redirect::Policy::none()).build()` passed through
`opentelemetry_otlp::SpanExporter::builder().with_http().with_endpoint(..).with_http_client(client)`.

Feature-forwarding precedent (root `Cargo.toml:406-464`): `storage-postgres` and `redis-cache` are
declared with an inline comment citing X-11.4 and are explicitly excluded from `full`'s member list at
line 460. `otel` joins that same exclusion list.

CI matrix (28-RESEARCH.md Q15): `.github/workflows/feature-flags.yml:36-99` enumerates 14 legs today;
`otel` is absent and is added as a new `- name:` entry building `--no-default-features --features otel`.

Hermetic axum stub precedent: the Phase 3 `FixtureServer` pattern (a `tokio::spawn`ed axum app on an
ephemeral port, torn down at test end, no Docker).

Docker is unavailable in this devcontainer — a real collector container is explicitly rejected by
D-13; the two-layer verification replaces it.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: One run, one span tree — the `otel` feature, the exporter and the in-memory shape assertion</name>
  <precondition>Network access to crates.io is required to resolve the three new dependencies; if more than a few days have elapsed since 2026-09-08, re-run `cargo info` for each of the three crates and pin the current versions rather than the recorded ones, recording any drift in the summary.</precondition>
  <files>src/infrastructure/telemetry/otel_sink.rs, src/infrastructure/telemetry/mod.rs, Cargo.toml, Cargo.lock</files>
  <read_first>
    - `Cargo.toml` `[features]` around lines 406-464 — the `storage-postgres` and `redis-cache` declarations, their X-11.4 comments, and `full`'s explicit member list at line 460.
    - `src/infrastructure/telemetry/{mod.rs,log_sink.rs}` (from 28-06) — `build_run_sink` and the `TraceSink` impl shape to mirror.
    - `src/config/trace.rs` (from 28-02) — `OtelConfig`'s fields and its redacting `Debug`.
    - `crates/paladin-llm/src/openai/adapter.rs` around line 253 — the house no-redirect client construction.
    - `crates/paladin-core/src/platform/container/trace.rs` — every variant's fields, the span model's only input.
    - `.planning/phases/28-observability-tooling/28-CONTEXT.md` D-12, D-13; `28-RESEARCH.md` Q1 and the OTLP code example.
  </read_first>
  <behavior>
    - `run_start_and_finish_produce_one_root_span`: a record stream of `RunStarted` … `RunFinished`
      produces exactly one root span named for the run, whose start and end timestamps equal the
      records' `at` values rather than wall-clock-at-export.
    - `retried_node_produces_sibling_attempt_spans`: a node with two attempts produces two child spans
      of the root — siblings, not nested — carrying `attempt` 1 and 2 and their own outcomes.
    - `attempt_span_carries_every_attribute`: an attempt span has `node_id`, `superstep`, `attempt`,
      `outcome`, `tokens`, `cache_hit` and `muster_task_key`.
    - `branch_retry_muster_fixture_tree_shape`: the PRD's fixture (a branching, retrying, mustering
      run) yields one root, the expected number of attempt children, edge and delta events on the
      right spans, and the parley event on the run span.
    - `orphan_record_opens_a_partial_span`: a `NodeFinished` whose `NodeStarted` was dropped produces
      a span flagged `paladin.trace.partial = true` rather than being discarded.
    - `otel_sink_absent_without_the_feature`: a default build compiles and `build_run_sink` never
      references the exporter.
  </behavior>
  <action>
    Add `opentelemetry`, `opentelemetry_sdk` and `opentelemetry-otlp` to the root `[dependencies]` as
    `optional = true` with the pinned versions and the explicit feature lists in `<interfaces>`, and
    declare `otel = ["dep:opentelemetry", "dep:opentelemetry_sdk", "dep:opentelemetry-otlp"]` in
    `[features]` with an inline comment citing X-11.4 exactly as `storage-postgres` does; do NOT add it
    to `default` or to `full`'s member list. Add `opentelemetry_sdk` with its `testing` feature to
    `[dev-dependencies]` so the in-memory exporter is available to tests without entering the
    production feature set. Create `src/infrastructure/telemetry/otel_sink.rs` under
    `#[cfg(feature = "otel")]` with `OtelTraceSink` implementing `TraceSink`: it holds a tracer, a map
    from `thread_id` to the open run span and a map from `(thread_id, node_id, attempt)` to the open
    attempt span, and drives the D-12 span model from the record stream alone. Build the OTLP exporter
    with a `reqwest::Client` constructed with `redirect(Policy::none())` and passed through
    `with_http_client`, taking `endpoint`, `headers` and `service.name` from `OtelConfig`; never log or
    format a header value anywhere in the file. Wire the sink into `build_run_sink` behind
    `#[cfg(feature = "otel")]` and the `otel.enabled` flag. Write the six `<behavior>` tests against
    `InMemorySpanExporter`, gated `#[cfg(feature = "otel")]`.
  </action>
  <acceptance_criteria>
    - `cargo test -p paladin-ai --features otel --lib infrastructure::telemetry::otel_sink 2>&1 | grep -qE 'test result: ok\. [1-9][0-9]* passed'`.
    - `grep -c 'Policy::none()' src/infrastructure/telemetry/otel_sink.rs` is 1.
    - `grep -c 'paladin.trace.partial' src/infrastructure/telemetry/otel_sink.rs` is at least 1.
    - `grep -c 'end_with_timestamp' src/infrastructure/telemetry/otel_sink.rs` is at least 1 and `grep -c 'with_start_time' src/infrastructure/telemetry/otel_sink.rs` is at least 1.
    - `grep -c 'reqwest-blocking-client' Cargo.toml` is 0 — the async client is used, and the blocking default is turned off. <!-- planner-discipline-allow: reqwest-blocking-client -->
    - `grep -c '^otel = ' Cargo.toml` is 1 and the `full = [` line does not list `otel`.
    - `cargo build` (default features) succeeds and `cargo build --features otel` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p paladin-ai --features otel --lib infrastructure::telemetry::otel_sink 2>&amp;1 | grep -qE 'test result: ok\. [1-9][0-9]* passed'</automated>
  </verify>
  <done>A real run's records become a correctly-shaped span tree in process, with retries as siblings and dropped events degrading to flagged partial spans rather than vanishing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Transport against a hermetic OTLP stub, and the CI feature leg</name>
  <files>tests/integration/otel_transport_test.rs, tests/integration/mod.rs, .github/workflows/feature-flags.yml</files>
  <read_first>
    - An existing hermetic axum fixture server in the test tree (the Phase 3 `FixtureServer` pattern) — the spawn, ephemeral-port and teardown conventions.
    - `tests/integration/mod.rs` — how integration test modules are declared and feature-gated.
    - `.github/workflows/feature-flags.yml` lines 30-100 — the 14 existing legs and the exact `- name:`/`run:` shape to copy.
    - `src/infrastructure/telemetry/otel_sink.rs` (Task 1).
    - `.planning/phases/28-observability-tooling/28-CONTEXT.md` D-13b, D-40.
  </read_first>
  <behavior>
    - `otlp_export_reaches_the_stub`: an axum stub serving `POST /v1/traces` on an ephemeral port
      receives at least one request after a fixture run is exported and flushed; the request's
      `Content-Type` is `application/x-protobuf`, its headers include every header configured on
      `OtelConfig`, and its body decodes to a resource whose `service.name` is the configured value.
    - `otlp_client_does_not_follow_redirects`: a stub answering `302` with a `Location` pointing at a
      second stub leaves the second stub with zero requests.
    - The test is hermetic — no Docker, no external network, no fixed port — and tears the stub down at
      the end.
    - The CI feature-flags job has an `otel` leg that builds the workspace with that feature alone.
  </behavior>
  <action>
    Create `tests/integration/otel_transport_test.rs` gated `#[cfg(feature = "otel")]`, declaring it in
    `tests/integration/mod.rs` behind the same gate. Spin up an axum app with a `POST /v1/traces`
    handler recording the request's headers and body into an `Arc<Mutex<Vec<_>>>`, bind it to an
    ephemeral port, point `OtelConfig.endpoint` at it with two configured headers and a distinctive
    `service_name`, run a small fixture through the sink, flush the exporter, and assert the three
    clauses. Add the second stub and the `302` case for the redirect assertion. Add an `otel` leg to
    `.github/workflows/feature-flags.yml` matching the surrounding entries' shape, building
    `--no-default-features --features otel`, and run the integration test in the same leg so the
    transport assertion has a CI home. Record in the summary that the leg was added and what it runs.
  </action>
  <acceptance_criteria>
    - `cargo test -p paladin-ai --features otel --test otel_transport_test 2>&1 | grep -qE 'test result: ok\. [1-9] passed'` (or, if the file is compiled through the integration harness module, the equivalent filtered invocation named in the summary).
    - `grep -c 'application/x-protobuf' tests/integration/otel_transport_test.rs` is at least 1.
    - `grep -c 'fn otlp_client_does_not_follow_redirects' tests/integration/otel_transport_test.rs` is 1.
    - `grep -c '127.0.0.1:0\|SocketAddr::from(([0-9]*, 0, 0, 1), 0)' tests/integration/otel_transport_test.rs` is at least 1 — an ephemeral port, not a fixed one.
    - `grep -c 'otel' .github/workflows/feature-flags.yml` is at least 2.
    - `cargo clippy --workspace --all-targets --features otel -- -D warnings` and `cargo fmt --check` pass.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p paladin-ai --features otel --test otel_transport_test 2>&amp;1 | grep -qE 'test result: ok\. [1-9] passed'</automated>
  </verify>
  <done>Spans actually leave the process over OTLP HTTP with the configured headers and service name, redirects cannot carry those headers elsewhere, and CI builds and exercises the feature.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Process → operator-configured OTLP collector endpoint | Outbound HTTP to a configured host carrying configured, credential-shaped headers. |
| `OtelConfig.headers` → HTTP request / logs / errors | Secret-by-assumption values move through the exporter. |
| Trace record attributes → external tracing backend | Node ids, outcomes and costs leave the process. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-09-01 | Information disclosure | Credential header forwarded on a redirect to an attacker-influenced host | high | mitigate | `reqwest::Client::builder().redirect(Policy::none())` passed through `with_http_client`; `otlp_client_does_not_follow_redirects` proves the second host receives nothing. |
| T-28-09-02 | Information disclosure | Header values in logs, `Debug` output or an exporter error | high | mitigate | `OtelConfig`'s manual redacting `Debug` (28-02); no log line or error body in this file interpolates a header value; the manual credential-handling review in `security.instructions.md` covers the file. |
| T-28-09-03 | Spoofing / SSRF-adjacent | A configured endpoint pointing at an internal host | medium | mitigate | `TraceConfig::validate_typed` rejects non-`http(s)` schemes when the exporter is enabled; the endpoint is operator-supplied configuration, not caller-supplied input, so the webhook-grade SSRF guard does not apply — recorded as the reasoned scope boundary. |
| T-28-09-04 | Information disclosure | Span attributes carrying state values | medium | mitigate | Attributes are ids, counts, outcomes and flags; `DeltaMerged` becomes a span event carrying field NAMES; the exporter emits only what the record already holds. |
| T-28-09-05 | Denial of service | Exporter blocking the run when the collector is slow or down | high | mitigate | The sink sits behind the never-awaited bounded dispatcher; an export failure is a diagnostics-only `Ok(())`; the composite isolates it from sibling sinks. |
| T-28-09-06 | Tampering | Supply-chain risk from the three new dependencies | medium | mitigate | All three cleared the package-legitimacy audit with an `OK` verdict, are Apache-2.0 already in `deny.toml`'s allowlist, and clear MSRV 1.88 with headroom; `make security` and the MSRV job run at the phase close-out. |
</threat_model>

<verification>
- `cargo test -p paladin-ai --features otel --lib infrastructure::telemetry::otel_sink` and the transport integration test green.
- `cargo build` with default features green and unchanged in dependency terms; `cargo build --features otel` green.
- `cargo fmt --check`, `cargo clippy --workspace --all-targets -- -D warnings` both with and without `otel`.
</verification>

<success_criteria>
- PRD 07 acceptance 2 holds: the span-tree shape is asserted against a collector stub for a branch + retry + muster fixture, in two layers — shape in process, transport over the wire.
- X-11.4 holds: a default build gains no OTel dependency.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

See the identical section in `28-01-PLAN.md` — the canonical list for Phase 28. This plan's own
contribution: `src/infrastructure/telemetry/otel_sink.rs` (`OtelTraceSink`, the span-per-attempt model,
the `paladin.trace.partial` flag, the no-redirect OTLP client), the `otel` umbrella cargo feature and
its three optional dependencies, `tests/integration/otel_transport_test.rs`, and the `otel` leg in
`.github/workflows/feature-flags.yml`.
</artifacts_produced>

<output>
Create `.planning/phases/28-observability-tooling/28-09-SUMMARY.md` when done.
</output>
