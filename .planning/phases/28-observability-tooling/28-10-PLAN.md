---
phase: 28-observability-tooling
plan: 10
type: execute
wave: 4
depends_on: [28-07]
files_modified:
  - crates/paladin-battalion/src/engine/export/overlay.rs
  - crates/paladin-battalion/src/engine/export/mod.rs
  - crates/paladin-battalion/src/engine/export/mermaid.rs
  - crates/paladin-battalion/src/engine/export/shape.rs
  - crates/paladin-battalion/tests/export_golden.rs
autonomous: true
requirements: [OBS-03]

must_haves:
  truths:
    - "`ExecutionOverlay` carries `visits` (a per-node ordered list of `Visit { superstep, attempt, outcome, duration_ms, tokens, cache_hit }`), `fired_edges`, `evaluated_edges`, a `source` of either Waypoints or Trace, and an `observed_only` flag — all in ordered collections so rendering is deterministic (D-21)."
    - "From Waypoint history alone the overlay is buildable: visits come from each Waypoint's completed node-execution records, fired edges are DERIVED from consecutive completed-to-vanguard transitions that match an edge in the shape, and `evaluated_edges` is empty (D-21)."
    - "From persisted trace records the overlay is exact: visits from `NodeStarted`/`NodeFinished` and both edge sets read directly from `EdgeEvaluated`, with `source = Trace` (D-21)."
    - "`to_mermaid_overlay` colours nodes by their LAST outcome using the `classDef` sets frozen in the UI-SPEC, appends a visit-count badge to nodes visited more than once, appends duration and token figures to labels, renders fired edges bold and evaluated-but-not-fired edges dotted (D-21)."
    - "When no static graph is available for a thread, the overlay renders the observed subgraph from the nodes and derived edges actually seen and the diagram carries the locked title `(observed nodes only — no graph document available)` (D-22)."
    - "Graph resolution order for a thread is: an explicitly supplied graph document, then the run's assistant version's document when the thread belongs to a run, then observed-only (D-22)."
    - "An overlay golden for the branching fixture with a scripted run is committed and checked by the same bless idiom as the static goldens; the trace-sourced and Waypoint-sourced overlays of the same run have distinct goldens so the difference between exact and derived edge data is visible (D-20, D-21)."
    - "A DOT overlay is deliberately not implemented — the PRD names Mermaid for the overlay only, and the omission is recorded rather than silently assumed."
  prohibitions: []
  artifacts:
    - path: "crates/paladin-battalion/src/engine/export/overlay.rs"
      provides: "ExecutionOverlay, Visit, OverlaySource, from_waypoints, from_trace_records, observed_shape"
      contains: "pub struct ExecutionOverlay"
    - path: "crates/paladin-battalion/src/engine/export/mermaid.rs"
      provides: "to_mermaid_overlay"
      contains: "pub fn to_mermaid_overlay"
  key_links:
    - from: "crates/paladin-battalion/src/engine/export/overlay.rs"
      to: "crates/paladin-core/src/platform/container/waypoint.rs"
      via: "the Waypoint-sourced overlay reads each Waypoint's completed node-execution records and vanguard"
      pattern: "NodeExecutionRecord|vanguard"
    - from: "crates/paladin-battalion/src/engine/export/overlay.rs"
      to: "crates/paladin-core/src/platform/container/trace.rs"
      via: "the trace-sourced overlay reads NodeStarted, NodeFinished and EdgeEvaluated records"
      pattern: "EdgeEvaluated"
---

<objective>
Answer "which branch fired and why did node X run 3 times" from data: an `ExecutionOverlay` built from
Waypoint history (always available) or persisted trace records (exact), rendered onto the
`GraphShape` as an annotated Mermaid diagram, with an observed-only fallback when no static graph
exists for the thread.

Purpose: OBS-03 / OBS-FR-09 per D-21 and D-22. The CLI `run export` command (28-13) and the inspector
page's diagram and panels (28-14, 28-15) both render this.

Output: `crates/paladin-battalion/src/engine/export/overlay.rs`, `to_mermaid_overlay`, the
observed-only shape builder, and the overlay goldens.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-observability-tooling/28-CONTEXT.md
@.planning/phases/28-observability-tooling/28-RESEARCH.md
@.planning/phases/28-observability-tooling/28-PATTERNS.md
@.planning/phases/28-observability-tooling/28-UI-SPEC.md
@.planning/phases/28-observability-tooling/28-07-SUMMARY.md
@.project/v0.10.0/07-observability-tooling.md
@CLAUDE.md

<interfaces>
Waypoint sources (`crates/paladin-core/src/platform/container/waypoint.rs`):
`NodeExecutionRecord { node_id, outcome: NodeOutcomeKind, duration_ms, token_count, attempt,
cache_hit, .. }` at lines 526-600; `Waypoint { vanguard, completed, status, graph_fingerprint, .. }`
at 655-677. `WaypointPort::history(thread, limit, before)` at
`crates/paladin-ports/src/output/waypoint_port.rs:212`. A Waypoint carries only a
`graph_fingerprint`, not the graph itself — which is why D-22's resolution order exists.

Trace sources (from 28-01/28-03): `NodeStarted { superstep, node_id, attempt, muster_task_key }`,
`NodeFinished { superstep, node_id, attempt, outcome, duration_ms, token_count, cache_hit }`,
`EdgeEvaluated { from, to, condition_kind, fired }`.

From 28-07: `GraphShape`, `ShapeNode`, `ShapeEdge`, `ShapeKind`, `to_mermaid`, and the golden test
target with its bless idiom.

Outcome colour sets and glyphs are frozen in `28-UI-SPEC.md`'s "Outcome color set" table — five
outcomes (success ✓, failed ✗, parleyed ⏸, skipped ⊘, cache_hit ⚡) with explicit light and dark
fill/stroke/text hex values per set. The overlay's `classDef` block uses those exact values so the
goldens and the inspector page agree.

Locked observed-only diagram title (`28-UI-SPEC.md` copywriting):
`(observed nodes only — no graph document available)`.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: One executed branch, annotated — `ExecutionOverlay` from Waypoints and its Mermaid rendering</name>
  <files>crates/paladin-battalion/src/engine/export/overlay.rs, crates/paladin-battalion/src/engine/export/mod.rs, crates/paladin-battalion/src/engine/export/mermaid.rs</files>
  <read_first>
    - `crates/paladin-core/src/platform/container/waypoint.rs` lines 520-690 — `NodeExecutionRecord`, `Waypoint.vanguard`, `Waypoint.completed`, `NodeOutcomeKind`.
    - `crates/paladin-battalion/src/engine/export/{shape.rs,mermaid.rs}` (from 28-07) — the shape types and the static renderer this extends.
    - `28-UI-SPEC.md` "Outcome color set" and "Node-kind badges" — the exact hex values and glyphs the `classDef` block must use.
    - `.planning/phases/28-observability-tooling/28-CONTEXT.md` D-21, D-22.
  </read_first>
  <behavior>
    - `overlay_from_waypoints_derives_fired_edges`: for a branching run whose Waypoint at superstep 2
      completed node `check` and whose superstep 3 vanguard is `[retry]`, with a `check → retry` edge
      in the shape, `fired_edges` contains that pair and `evaluated_edges` is empty, `source` is
      Waypoints.
    - `overlay_visits_are_ordered_per_node`: a loop node visited in supersteps 2, 4 and 6 has three
      `Visit` entries in that order, each carrying its superstep, attempt and outcome.
    - `overlay_mermaid_colours_by_last_outcome`: a node whose last visit failed renders with the
      failed `classDef`; a node whose last visit succeeded renders with the success `classDef`.
    - `overlay_mermaid_annotates_repeat_visits_and_cost`: a node visited three times carries a visit
      count badge in its label; every visited node's label carries its duration and token figures.
    - `overlay_mermaid_renders_fired_edges_bold`: a fired edge is rendered with the bold link style
      and a non-fired evaluated edge with the dotted style; with a Waypoints source (no
      `evaluated_edges`) only the bold ones appear.
  </behavior>
  <action>
    Create `crates/paladin-battalion/src/engine/export/overlay.rs`, declared in `export/mod.rs`.
    Define `pub struct ExecutionOverlay { visits: BTreeMap<NodeId, Vec<Visit>>, fired_edges:
    BTreeSet<(NodeId, NodeId)>, evaluated_edges: BTreeSet<(NodeId, NodeId)>, source: OverlaySource,
    observed_only: bool }`, `pub struct Visit { superstep, attempt, outcome: NodeOutcomeKind,
    duration_ms, tokens, cache_hit }` and `pub enum OverlaySource { Waypoints, Trace }` — ordered
    collections throughout so rendering is deterministic. Implement
    `ExecutionOverlay::from_waypoints(&[Waypoint], &GraphShape)`: visits from each Waypoint's
    `completed` records in superstep order; fired edges derived by taking each Waypoint's completed
    node set and the NEXT Waypoint's `vanguard`, and adding `(a, c)` when the shape contains that edge;
    `evaluated_edges` left empty; `source = Waypoints`. Add `to_mermaid_overlay(&GraphShape,
    &ExecutionOverlay) -> String` in `mermaid.rs`, reusing the static renderer's node and edge
    emission and layering on: an outcome `classDef` block using the UI-SPEC hex values for the five
    outcomes, a per-node class assignment from that node's LAST visit's outcome, a visit-count badge
    appended to the label of any node with more than one visit, duration and token figures appended to
    every visited node's label, a bold link style for fired edges and a dotted one for evaluated-but-
    not-fired edges. Keep the output byte-deterministic. Write the five `<behavior>` tests.
  </action>
  <acceptance_criteria>
    - `cargo test -p paladin-battalion --lib engine::export::overlay 2>&1 | grep -qE 'test result: ok\. [1-9][0-9]* passed'`.
    - `grep -c 'pub struct ExecutionOverlay' crates/paladin-battalion/src/engine/export/overlay.rs` is 1 and `grep -c 'pub fn from_waypoints' crates/paladin-battalion/src/engine/export/overlay.rs` is 1.
    - `grep -c 'pub fn to_mermaid_overlay' crates/paladin-battalion/src/engine/export/mermaid.rs` is 1.
    - `grep -c 'BTreeMap' crates/paladin-battalion/src/engine/export/overlay.rs` is at least 1 and `grep -c 'BTreeSet' crates/paladin-battalion/src/engine/export/overlay.rs` is at least 1.
    - `grep -c 'fn overlay_from_waypoints_derives_fired_edges' crates/paladin-battalion/src/engine/export/overlay.rs` is 1.
    - `cargo test -p paladin-battalion --doc engine::export 2>&1 | grep -qE 'test result: ok\. [1-9] passed'`.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p paladin-battalion --lib engine::export::overlay 2>&amp;1 | grep -qE 'test result: ok\. [1-9][0-9]* passed'</automated>
  </verify>
  <done>A real branching run's Waypoint history renders as an annotated diagram showing which branch fired, how many times each node ran and what each visit cost — with no persisted trace required.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The exact trace-sourced overlay, the observed-only fallback, and the overlay goldens</name>
  <files>crates/paladin-battalion/src/engine/export/overlay.rs, crates/paladin-battalion/src/engine/export/shape.rs, crates/paladin-battalion/tests/export_golden.rs</files>
  <read_first>
    - `crates/paladin-battalion/src/engine/export/overlay.rs` as written in Task 1.
    - `crates/paladin-core/src/platform/container/trace.rs` — `NodeStarted`, `NodeFinished`, `EdgeEvaluated` field names.
    - `crates/paladin-battalion/tests/export_golden.rs` (from 28-07) — the golden table and bless branch to extend.
    - `28-UI-SPEC.md` copywriting — the locked observed-only diagram title, verbatim.
    - `.planning/phases/28-observability-tooling/28-CONTEXT.md` D-21, D-22.
  </read_first>
  <behavior>
    - `overlay_from_trace_is_exact`: over a record stream containing `EdgeEvaluated` records, both
      `fired_edges` and `evaluated_edges` are populated directly from those records with no
      derivation, and `source` is Trace.
    - `trace_overlay_shows_evaluated_but_not_fired`: an edge evaluated with `fired: false` appears in
      `evaluated_edges` and not in `fired_edges`, and renders dotted.
    - `observed_shape_is_built_when_no_graph_is_available`: `GraphShape::observed(&ExecutionOverlay)`
      builds a shape from only the nodes visited and the edges observed, `observed_only` is true, and
      the rendered diagram's title line is exactly the locked observed-only string.
    - `waypoint_and_trace_overlays_of_the_same_run_differ_only_in_evaluated_edges`: for one scripted
      run, the two overlays agree on visits and fired edges and differ only in `evaluated_edges` and
      `source`.
    - Two overlay goldens are committed for the branching fixture — one Waypoint-sourced, one
      trace-sourced — and checked by the existing golden test with the same bless variable.
  </behavior>
  <action>
    Add `ExecutionOverlay::from_trace_records(&[TraceRecord])` reading visits from paired
    `NodeStarted`/`NodeFinished` records (one visit per attempt) and both edge sets from
    `EdgeEvaluated`, setting `source = Trace`. Add `GraphShape::observed(&ExecutionOverlay)` in
    `shape.rs` building a shape from the visited node ids (kind unknown, rendered without a badge or
    with a neutral one) and the observed edges, setting `observed_only`. Make `to_mermaid_overlay`
    prepend the locked observed-only title line, verbatim, when `overlay.observed_only` is true. Record
    in the module rustdoc that a DOT overlay is deliberately not implemented because the PRD names
    Mermaid for the overlay only, so a future reader does not treat the omission as an oversight.
    Extend `crates/paladin-battalion/tests/export_golden.rs` with a scripted branching run producing
    both a Waypoint history and a trace record stream, and add two golden comparisons —
    `branch_overlay_waypoints.mermaid` and `branch_overlay_trace.mermaid` — through the existing bless
    branch. Write the four `<behavior>` tests.
  </action>
  <acceptance_criteria>
    - `cargo test -p paladin-battalion --lib engine::export::overlay 2>&1 | grep -qE 'test result: ok\. [1-9][0-9]* passed'`.
    - `cargo test -p paladin-battalion --test export_golden 2>&1 | grep -qE 'test result: ok\. [1-9][0-9]* passed'`.
    - `grep -c 'pub fn from_trace_records' crates/paladin-battalion/src/engine/export/overlay.rs` is 1 and `grep -c 'pub fn observed' crates/paladin-battalion/src/engine/export/shape.rs` is 1.
    - `test -s crates/paladin-battalion/tests/golden/export/branch_overlay_waypoints.mermaid` and `test -s crates/paladin-battalion/tests/golden/export/branch_overlay_trace.mermaid`.
    - `grep -c 'observed nodes only' crates/paladin-battalion/src/engine/export/mermaid.rs` is at least 1.
    - `grep -ci 'dot overlay' crates/paladin-battalion/src/engine/export/overlay.rs` is at least 1 — the deliberate omission is recorded.
    - `cargo clippy -p paladin-battalion --all-targets -- -D warnings` and `cargo fmt --check` pass.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p paladin-battalion --lib engine::export::overlay 2>&amp;1 | grep -qE 'test result: ok\. [1-9][0-9]* passed' &amp;&amp; cargo test -p paladin-battalion --test export_golden 2>&amp;1 | grep -qE 'test result: ok\. [1-9][0-9]* passed'</automated>
  </verify>
  <done>Persisted traces give exact fired and evaluated edge sets, a thread with no available graph still renders its observed subgraph with an honest title, and both overlay renderings are frozen by goldens.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Waypoint / trace history → rendered diagram | Execution history, including outcomes and costs, becomes a shareable artifact. |
| Node ids and observed edges → diagram markup | Author-supplied identifiers reach a rendered document. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-10-01 | Information disclosure | Overlay carrying state values into a diagram | medium | mitigate | `Visit` carries superstep, attempt, outcome, duration, tokens and cache flag only — never a field value; `DeltaMerged` is not read by the overlay at all. |
| T-28-10-02 | Repudiation | A derived fired-edge set presented as exact | medium | mitigate | `source` is carried on the overlay and rendered; the Waypoints-sourced case leaves `evaluated_edges` empty rather than guessing, and the inspector labels the missing half (28-15). |
| T-28-10-03 | Tampering | Node id breaking out of a diagram label | medium | mitigate | Labels reuse the static renderer's quoting and escaping, proven by the shared goldens. |
| T-28-10-04 | Repudiation | An observed-only diagram mistaken for the whole graph | medium | mitigate | The locked title line states the limitation on the diagram itself, and `observed_only` is a field consumers can branch on (D-22). |
</threat_model>

<verification>
- `cargo test -p paladin-battalion --lib engine::export` and `--test export_golden` green, now covering twelve golden files.
- `cargo test -p paladin-battalion --doc engine::export` green.
- `cargo fmt --check`, `cargo clippy --workspace --all-targets -- -D warnings`.
</verification>

<success_criteria>
- PRD 07 acceptance 3's overlay clause holds: an execution-overlay golden for the branching fixture exists, in both source modes.
- The OBS-03 acceptance question — which branch fired, and why did node X run three times — is answerable from the overlay data alone.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

See the identical section in `28-01-PLAN.md` — the canonical list for Phase 28. This plan's own
contribution: `crates/paladin-battalion/src/engine/export/overlay.rs` (`ExecutionOverlay`, `Visit`,
`OverlaySource`, `from_waypoints`, `from_trace_records`), `GraphShape::observed`,
`to_mermaid_overlay`, and the two overlay goldens `branch_overlay_waypoints.mermaid` and
`branch_overlay_trace.mermaid`.
</artifacts_produced>

<output>
Create `.planning/phases/28-observability-tooling/28-10-SUMMARY.md` when done.
</output>
