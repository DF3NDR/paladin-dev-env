---
phase: 28-observability-tooling
plan: 11
type: execute
wave: 5
depends_on: [28-04, 28-06]
files_modified:
  - src/application/services/run/events.rs
  - src/application/services/run/worker.rs
  - src/application/services/run/stream_tests.rs
  - crates/paladin-core/src/platform/container/run.rs
  - src/infrastructure/telemetry/persisting_sink.rs
  - src/infrastructure/telemetry/mod.rs
  - crates/paladin-web/openapi.json
autonomous: true
requirements: [OBS-02]

must_haves:
  truths:
    - "The SSE bus has exactly one producer: the worker no longer publishes `parley`, `done` or `error` directly, and every wire event originates from `map_trace_event` — one implementation, two consumers, no second pathway (D-14, OBS-FR-06)."
    - "`map_trace_event` is total over the seven published wire names: `SuperstepStarted → superstep`, `NodeStarted → node_started`, `NodeFinished → node_finished`, `DeltaMerged → state_delta`, `ParleyRaised → parley`, `RunFinished` with a non-failed status `→ done`, `RunFinished` with a failed status `→ error`, and everything else `None`; the existing exactly-four-of-eight unit test becomes exactly-seven-of-twelve (D-14)."
    - "The seven wire names, their payload fields and the OpenAPI schema are unchanged as a published contract — no rename, no removal, no reshaping (27-CONTEXT D-25, X-03)."
    - "`RunStreamEvent.seq` remains the bus's own dense per-run counter starting at 1, and the payload gains an additive `trace_seq` field correlating a wire event with the same record in logs, spans and `run_traces`; degraded-mode events synthesized from Waypoints carry no `trace_seq` (D-15)."
    - "`RunStreamMode` gains a `Replay` variant AND `#[non_exhaustive]` in the same change (X-10.2), and its only two consumers — the worker and the events service — are updated together."
    - "When trace persistence is on and a requested run is terminal or executing elsewhere with rows in `run_traces`, the stream replays those rows through the same `map_trace_event` with `mode: replay`, the original `at` and `trace_seq`, and terminates with `done` or `error` exactly as a live stream does (D-16)."
    - "With no rows — persistence off, or a run older than retention — the stream falls back to today's degraded Waypoint-polling path, unchanged (D-16)."
    - "`PersistingTraceSink` buffers records and flushes on `WaypointSaved`, on `RunFinished`, and whenever the buffer reaches its record threshold; a crash loses at most the un-flushed tail of one superstep, documented as best-effort against the Waypoint's durability (D-17)."
    - "A `run_traces` write failure is logged and never fails, stalls or alters the run — the prohibition recorded in `28-06-PLAN.md` applies to this sink too."
  prohibitions: []
  artifacts:
    - path: "src/infrastructure/telemetry/persisting_sink.rs"
      provides: "PersistingTraceSink with superstep-boundary, terminal and threshold flushes"
      contains: "pub struct PersistingTraceSink"
    - path: "src/application/services/run/events.rs"
      provides: "map_trace_event total over seven wire names; the replay branch in RunEventStreamService"
      contains: "RunStreamMode::Replay"
    - path: "crates/paladin-core/src/platform/container/run.rs"
      provides: "RunStreamMode::Replay and #[non_exhaustive] on RunStreamMode"
      contains: "Replay"
  key_links:
    - from: "src/application/services/run/worker.rs"
      to: "src/application/services/run/events.rs"
      via: "the worker's direct parley/done/error publishes are removed; the bus receives only what the trace sink maps"
      pattern: "map_trace_event|RunEventBusSink"
    - from: "src/infrastructure/telemetry/persisting_sink.rs"
      to: "crates/paladin-ports/src/output/run_trace_port.rs"
      via: "buffered records are appended through RunTracePort on each flush boundary"
      pattern: "RunTracePort|append"
---

<objective>
Collapse the SSE bus onto the single trace pathway and upgrade its degraded mode to full fidelity:
`map_trace_event` becomes total over the seven published wire names, the worker's direct publishes
disappear, the payload gains `trace_seq`, `RunStreamMode` gains `Replay`, and a `PersistingTraceSink`
writes records to `run_traces` at superstep boundaries so a finished run can be replayed exactly.

Purpose: OBS-02 / OBS-FR-06 and FR-07, per D-14, D-15, D-16 and D-17. This is 27-CONTEXT D-25's
"single edit point" being exercised for the first time, and it closes that phase's recorded
correction that today's enum cannot produce all seven wire names.

Output: a one-producer bus, `trace_seq` on the wire, `RunStreamMode::Replay`, the replay branch,
`PersistingTraceSink`, and a re-blessed `openapi.json`.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-observability-tooling/28-CONTEXT.md
@.planning/phases/28-observability-tooling/28-RESEARCH.md
@.planning/phases/28-observability-tooling/28-PATTERNS.md
@.planning/phases/28-observability-tooling/28-04-SUMMARY.md
@.planning/phases/28-observability-tooling/28-06-SUMMARY.md
@.planning/phases/27-platform-api/27-CONTEXT.md
@.project/v0.10.0/07-observability-tooling.md
@CLAUDE.md

<interfaces>
Worker publish sites to remove (28-RESEARCH.md Q8, verified): `src/application/services/run/worker.rs`
lines ~850-903, inside `run_once`'s dispatch-completion handling — a
`match &outcome { AwaitingInput => publish(Parley), Completed => publish(Done), Halted =>
publish(Done), Failed => publish(Error) }`; four outcome arms producing three wire kinds. Separately,
`record_engine_failure` (~1050-1067) has its own `publish(Error)` for an `EngineError` outside normal
outcome reporting — that path has no `RunFinished` record behind it and must be handled deliberately
rather than deleted blindly.

`map_trace_event` (`src/application/services/run/events.rs:87`) currently maps four of eight variants
and reports `node_finished`'s `outcome` as the placeholder string it was forced to use before the
enum carried a real outcome; that placeholder is replaced with `NodeFinished.outcome` now that 28-03
supplies it. `RunEventBusSink` at 267-295, `RunEventStreamService` at 500-560 with the degraded
`DegradedState` unfold, `RUN_EVENT_CHANNEL_CAPACITY = 64`.

`RunStreamMode` (`crates/paladin-core/src/platform/container/run.rs:508-517`) is currently NOT
`#[non_exhaustive]`, unlike `RunStreamEventKind` (465-469) and `RunStreamEvent` (524-527), which both
are — the marker was overlooked when the type was created in Phase 27. Its only consumers anywhere in
the tree are `worker.rs` and `events.rs`, both first-party and both edited by this plan.

OpenAPI (28-RESEARCH.md Q5): `crates/paladin-web/src/openapi.rs:264`'s drift-guard builds the document
from three named router sources; the `sdk-clients` CI job at `.github/workflows/ci.yml:1077-1125`
reads the committed `crates/paladin-web/openapi.json` off disk, so an additive schema change needs
only a re-bless commit and no workflow edit. The bless command is `UPDATE_OPENAPI=1` against
`openapi_matches_committed_baseline` (`Makefile:370`).

From 28-04: `RunTracePort::{append, read, prune_thread}`, `RunTraceError`.
From 28-06: `build_run_sink` and the per-run composite construction site.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: One producer — `map_trace_event` total over seven wire names, worker publishes removed</name>
  <files>src/application/services/run/events.rs, src/application/services/run/worker.rs, src/application/services/run/stream_tests.rs</files>
  <read_first>
    - `src/application/services/run/events.rs` lines 60-160 and 260-300 — `map_trace_event`, its doc comment (including the placeholder-outcome note that this plan resolves) and `RunEventBusSink`.
    - `src/application/services/run/worker.rs` lines 840-910 and 1040-1075 — the four-arm publish match and `record_engine_failure`'s separate publish.
    - `src/application/services/run/stream_tests.rs` — the existing exactly-four-of-eight test and the wire-name assertions.
    - `.planning/phases/27-platform-api/27-CONTEXT.md` D-24, D-25 — the seven frozen wire names and the published payload contract.
    - `.planning/phases/28-observability-tooling/28-CONTEXT.md` D-14, D-15.
  </read_first>
  <behavior>
    - `map_trace_event_covers_exactly_seven_of_twelve`: the existing coverage test is updated to assert
      that exactly seven of the twelve variants map to a wire event and the other five map to `None`,
      naming each.
    - `run_finished_status_splits_done_and_error`: `RunFinished` with `completed`, `halted` and
      `awaiting_input` maps to `done`; with `failed` it maps to `error`.
    - `parley_raised_maps_to_the_parley_wire_name`: a `ParleyRaised` record produces the `parley` wire
      event with its existing published payload fields.
    - `node_finished_reports_the_real_outcome`: the `node_finished` payload's outcome field carries the
      record's `NodeOutcomeKind` rather than the previous placeholder.
    - `worker_publishes_nothing_directly`: a full run through the worker produces the same sequence of
      wire events as before this change, and a source-level check confirms the worker no longer calls
      the bus's publish path for outcome reporting.
    - `engine_failure_still_reaches_the_error_wire_name`: an `EngineError` outside normal outcome
      reporting still produces an `error` wire event — deliberately, through whichever mechanism the
      executor chooses (emitting a `RunFinished { status: failed }` record, or retaining that one
      publish with a comment explaining why it has no trace record behind it) — and the choice is
      recorded in the summary.
    - `wire_payload_carries_trace_seq`: every live event's payload carries `trace_seq` equal to the
      originating record's `seq`, while `RunStreamEvent.seq` remains the bus's own dense counter
      starting at 1.
  </behavior>
  <action>
    Complete `map_trace_event`'s match over the twelve variants so it is total across the seven
    published wire names, splitting `RunFinished` on `status`, mapping `ParleyRaised` to `parley`, and
    replacing the placeholder outcome string with the record's real `NodeOutcomeKind`; keep the
    wildcard arm returning `None`. Update the function's doc comment to remove the note explaining the
    placeholder, since the reason for it is gone. Add `trace_seq` to every mapped payload, taken from
    the record's `seq`, leaving `RunStreamEvent.seq` as the bus's own counter untouched. Remove the
    worker's four-arm outcome publish block, and handle `record_engine_failure`'s separate publish
    deliberately per the `<behavior>` clause. Update `stream_tests.rs`'s coverage test and add the six
    other `<behavior>` tests.
  </action>
  <acceptance_criteria>
    - `cargo test -p paladin-ai --lib services::run::events 2>&1 | grep -qE 'test result: ok\. [1-9][0-9]* passed'`.
    - `cargo test -p paladin-ai --lib services::run::stream_tests 2>&1 | grep -qE 'test result: ok\. [1-9][0-9]* passed'`.
    - `grep -c 'trace_seq' src/application/services/run/events.rs` is at least 2.
    - `grep -c 'fn map_trace_event_covers_exactly_seven_of_twelve' src/application/services/run/stream_tests.rs` is 1.
    - `awk '/fn run_once/,/^}/' src/application/services/run/worker.rs | grep -c 'RunStreamEventKind::Done\|RunStreamEventKind::Parley'` is 0 — the outcome-reporting publishes are gone.
    - `grep -c 'fn engine_failure_still_reaches_the_error_wire_name' src/application/services/run/worker.rs` is 1.
    - `cargo build --workspace --all-targets` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p paladin-ai --lib services::run::events 2>&amp;1 | grep -qE 'test result: ok\. [1-9][0-9]* passed' &amp;&amp; cargo test -p paladin-ai --lib services::run::stream_tests 2>&amp;1 | grep -qE 'test result: ok\. [1-9][0-9]* passed'</automated>
  </verify>
  <done>Every SSE event a client sees originates from one trace record through one mapping function, with the published wire contract unchanged and each event correlatable back to its record.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `PersistingTraceSink` — buffered, superstep-boundary flushes into `run_traces`</name>
  <files>src/infrastructure/telemetry/persisting_sink.rs, src/infrastructure/telemetry/mod.rs, src/application/services/run/worker.rs</files>
  <read_first>
    - `src/infrastructure/telemetry/{mod.rs,log_sink.rs}` (from 28-06) — `build_run_sink` and the `TraceSink` impl shape.
    - `crates/paladin-ports/src/output/run_trace_port.rs` (from 28-04) — `append`'s batch signature and the error type.
    - `src/config/trace.rs` — the `persist` flag.
    - `src/application/services/run/worker.rs` — the per-run composite construction site.
    - `.planning/phases/28-observability-tooling/28-CONTEXT.md` D-17.
  </read_first>
  <behavior>
    - `persisting_sink_flushes_on_waypoint_saved`: records buffered since the last flush are appended
      as one batch when a `WaypointSaved` record arrives, and the buffer is empty afterwards.
    - `persisting_sink_flushes_on_run_finished`: the terminal record triggers a final flush including
      itself.
    - `persisting_sink_flushes_on_threshold`: a long superstep producing more than the configured
      buffer threshold flushes mid-superstep and bounds memory.
    - `persisting_sink_write_failure_does_not_fail_the_run`: a port returning `Err` on `append` is
      logged once and the sink returns `Ok(())`; a run executed with that failing port produces an
      identical `RunOutcome` to the same run with no persisting sink.
    - `persisting_sink_only_attached_when_persist_is_on`: `build_run_sink` includes it only when
      `trace.persist` is set and a `RunTracePort` is available.
    - The sink's rustdoc states plainly that trace persistence is best-effort and the Waypoint is the
      durability truth — a crash loses at most the un-flushed tail of one superstep.
  </behavior>
  <action>
    Create `src/infrastructure/telemetry/persisting_sink.rs` with
    `PersistingTraceSink { port: Arc<dyn RunTracePort>, buffer: Mutex<Vec<TraceRecord>>, threshold:
    usize }` implementing `TraceSink`: push each record into the buffer, then flush when the record is
    a `WaypointSaved` or a `RunFinished`, or when the buffer has reached the threshold. Flush appends
    the whole batch through `RunTracePort::append` and clears the buffer; an error is logged once at
    error level and swallowed into `Ok(())`, matching the port's diagnostics-only contract. Extend
    `build_run_sink` to include the sink when `trace.persist` is set and a port is available, and pass
    the port through from the worker's per-run composition. Document the best-effort contract in the
    module rustdoc. Write the six `<behavior>` tests against an in-memory `RunTracePort` and a
    deliberately failing one.
  </action>
  <acceptance_criteria>
    - `cargo test -p paladin-ai --lib infrastructure::telemetry::persisting_sink 2>&1 | grep -qE 'test result: ok\. [1-9][0-9]* passed'`.
    - `grep -c 'pub struct PersistingTraceSink' src/infrastructure/telemetry/persisting_sink.rs` is 1.
    - `grep -c 'fn persisting_sink_write_failure_does_not_fail_the_run' src/infrastructure/telemetry/persisting_sink.rs` is 1.
    - `grep -ci 'best-effort' src/infrastructure/telemetry/persisting_sink.rs` is at least 1.
    - `grep -c 'PersistingTraceSink' src/infrastructure/telemetry/mod.rs` is at least 1.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p paladin-ai --lib infrastructure::telemetry::persisting_sink 2>&amp;1 | grep -qE 'test result: ok\. [1-9][0-9]* passed'</automated>
  </verify>
  <done>Trace records reach durable storage at superstep boundaries without ever putting a run at risk, and the durability contract is stated where a reader will find it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: `RunStreamMode::Replay` and the full-fidelity replay branch</name>
  <files>crates/paladin-core/src/platform/container/run.rs, src/application/services/run/events.rs, src/application/services/run/stream_tests.rs, crates/paladin-web/openapi.json</files>
  <read_first>
    - `crates/paladin-core/src/platform/container/run.rs` lines 460-575 — `RunStreamEventKind`, `RunStreamMode`, `RunStreamEvent` and which of the three already carry the non-exhaustive marker.
    - `src/application/services/run/events.rs` lines 490-570 — `RunEventStreamService::stream` and the degraded `DegradedState` unfold.
    - `crates/paladin-web/src/openapi.rs` around line 264 — the drift-guard test and its three router sources.
    - `Makefile` line 370 — the OpenAPI bless target.
    - `.planning/phases/28-observability-tooling/28-CONTEXT.md` D-15, D-16; `28-RESEARCH.md` Q5 and Pitfall 1.
  </read_first>
  <behavior>
    - `run_stream_mode_is_non_exhaustive`: the variant addition and the marker land in the same change,
      and both first-party consumers compile.
    - `terminal_run_with_rows_replays`: a finished run with `run_traces` rows streams every mapped
      record with `mode: replay`, the record's original `at` and its `trace_seq`, then terminates with
      `done` (or `error` for a failed run).
    - `replay_and_live_produce_the_same_wire_sequence`: for one run, the live stream and a subsequent
      replay of the same run yield the same ordered sequence of wire kinds and payload fields, apart
      from `mode`.
    - `terminal_run_without_rows_falls_back_to_degraded`: with persistence off, or with a run whose
      rows were pruned, the existing Waypoint-polling degraded path runs unchanged and its synthesized
      events carry no `trace_seq`.
    - `replay_paginates_through_the_port`: a run with more records than one `read` limit is replayed
      completely, in order, with no repeats.
    - The committed `openapi.json` is re-blessed and the `sdk-clients` job's inputs remain valid.
  </behavior>
  <action>
    Add `Replay` to `RunStreamMode` and `#[non_exhaustive]` to the enum in the same change, per X-10.2
    and Pitfall 1 — the type is new-in-0.10 so this is a deliberate-zero migration note rather than a
    register row, but the marker is added regardless. In `RunEventStreamService::stream`, branch
    before the existing live and degraded paths: when `trace.persist` is on and the requested run is
    terminal or executing elsewhere, call `RunTracePort::read` paginating by `after_seq`, map each
    record through the same `map_trace_event`, emit with `mode: replay` and the record's original `at`
    and `trace_seq`, and terminate with the same `done`/`error` logic; when `read` returns nothing,
    fall through to today's degraded unfold with no behavioural change. Re-bless
    `crates/paladin-web/openapi.json` with the documented bless variable and commit the result; if the
    diff is empty because neither the added enum variant nor the additive payload field is reachable
    from the three router sources the drift-guard assembles, record that fact in the summary rather
    than assuming a change was missed. Write the five `<behavior>` tests.
  </action>
  <acceptance_criteria>
    - `cargo test -p paladin-ai --lib services::run::stream_tests 2>&1 | grep -qE 'test result: ok\. [1-9][0-9]* passed'`.
    - `cargo test -p paladin-ai-core --lib platform::container::run 2>&1 | grep -qE 'test result: ok\. [1-9][0-9]* passed'`.
    - `grep -B2 'pub enum RunStreamMode' crates/paladin-core/src/platform/container/run.rs | grep -c 'non_exhaustive'` is 1.
    - `grep -c 'Replay' crates/paladin-core/src/platform/container/run.rs` is at least 1.
    - `grep -c 'fn replay_and_live_produce_the_same_wire_sequence' src/application/services/run/stream_tests.rs` is 1 and `grep -c 'fn terminal_run_without_rows_falls_back_to_degraded' src/application/services/run/stream_tests.rs` is 1.
    - `cargo test -p paladin-web --lib openapi_matches_committed_baseline 2>&1 | grep -qE 'test result: ok\. 1 passed'` — the committed document matches after the re-bless.
    - `cargo clippy --workspace --all-targets -- -D warnings` and `cargo fmt --check` pass.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p paladin-ai --lib services::run::stream_tests 2>&amp;1 | grep -qE 'test result: ok\. [1-9][0-9]* passed' &amp;&amp; cargo test -p paladin-web --lib openapi_matches_committed_baseline 2>&amp;1 | grep -qE 'test result: ok\. 1 passed'</automated>
  </verify>
  <done>A finished run's stream is a faithful replay of what actually happened rather than a Waypoint-derived approximation, and the degraded path is still there for runs with no rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Trace record → SSE wire → API client | Internal execution detail crosses to an authenticated external consumer. |
| `run_traces` rows → replayed stream | Persisted data is served back to a caller. |
| Trace record → persisted row | Observability data becomes durable state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-11-01 | Information disclosure | State field VALUES reaching the SSE wire | high | mitigate | `map_trace_event` emits field NAMES and byte counts for `state_delta` and never a `FieldChange.value`, regardless of the `trace.state_values` config — the Phase 27 prohibition stands unchanged. |
| T-28-11-02 | Information disclosure | Replay serving another tenant's run | high | mitigate | Replay reads through `RunTracePort` scoped by the same `ThreadId` the existing stream endpoint already authorizes; no new authorization surface is introduced and the endpoint's existing auth layers are untouched. |
| T-28-11-03 | Denial of service | Replaying an unbounded run in one response | medium | mitigate | `read` is paginated by `after_seq` with a limit; the replay loop streams incrementally rather than materialising the whole run. |
| T-28-11-04 | Tampering | A persisted-write failure silently changing run behaviour | high | mitigate | The persisting sink swallows errors into a logged `Ok(())` behind the never-awaited dispatcher; `persisting_sink_write_failure_does_not_fail_the_run` asserts outcome equality. |
| T-28-11-05 | Repudiation | A replayed stream indistinguishable from a live one | low | mitigate | Every replayed event carries `mode: replay` and the original `at`, so a consumer can tell the difference. |
</threat_model>

<verification>
- `cargo test -p paladin-ai --lib services::run` green across `events`, `stream_tests` and `worker`; `cargo test -p paladin-ai --lib infrastructure::telemetry` green.
- `cargo test -p paladin-web --lib openapi_matches_committed_baseline` green against the re-blessed document.
- `cargo fmt --check`, `cargo clippy --workspace --all-targets -- -D warnings`.
</verification>

<success_criteria>
- OBS-FR-06 holds by construction: one `TraceSink` implementation, two consumers, no second event pathway.
- OBS-FR-07's consumer half holds: the degraded stream upgrades to full fidelity when persistence is on, and degrades gracefully when it is not.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

See the identical section in `28-01-PLAN.md` — the canonical list for Phase 28. This plan's own
contribution: the total seven-of-twelve `map_trace_event`, the removal of the worker's direct
outcome publishes, the additive `trace_seq` payload field, `RunStreamMode::Replay` plus its
`#[non_exhaustive]` marker, the replay branch in `RunEventStreamService`,
`src/infrastructure/telemetry/persisting_sink.rs` (`PersistingTraceSink`), and the re-blessed
`crates/paladin-web/openapi.json`.
</artifacts_produced>

<output>
Create `.planning/phases/28-observability-tooling/28-11-SUMMARY.md` when done.
</output>
