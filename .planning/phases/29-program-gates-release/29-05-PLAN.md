---
phase: 29-program-gates-release
plan: 05
type: execute
wave: 2
depends_on: [29-01, 29-02, 29-03]
files_modified:
  - MIGRATION.md
  - .github/workflows/ci.yml
autonomous: true
requirements: [SHIP-01, SHIP-02]

must_haves:
  truths:
    - "`MIGRATION.md` contains zero placeholder markers: the header note's two occurrences, §9.5's and §9.8's are all closed, and the rewritten header describes the closed state without reintroducing the literal marker token (D-01, RESEARCH Q11 — the count at HEAD is 4, not 3)."
    - "A new step in the `semver` job fails when `MIGRATION.md` still carries any placeholder marker, so the convention cannot silently return in v0.11 (D-01)."
    - "§9.5's disabled-by-default claim cites the SHIP-02 boot test by its real Cargo target name `v0_9_config_boot` and its file path, in the past tense — the claim is backed, not forthcoming (D-01, D-09)."
    - "§9.6's still-owed golden-diff note is replaced by a citation of `crates/paladin-web/tests/openapi_golden_v0_9.rs` naming what it restricts to and what it normalises, and the same replacement is applied to the Phase 27 note that also defers to SHIP-02 (D-08, D-09)."
    - "§9.7 is confirmed empty at closeout with the confirmation stated as a closed fact rather than as a pending action (D-01)."
    - "§9.8 is one ordered, copy-pasteable operator checklist whose every step names a concrete command or file that exists in the shipped tree — and it names only real `paladin-cli` subcommands: `setup-check`, `maneuver validate`, `graph export`, `eval run`. No `paladin-cli health` and no `graph validate` appear anywhere in it (D-02, RESEARCH Q1)."
    - "The `v0_9_config_boot` target is executed by CI on every PR through a step added to the EXISTING `e2e-platform-api` job (already `--features web-server`), with a guard that fails when the run selects zero tests — no new workflow job is created (D-09 as corrected)."
    - "Both the placeholder gate and the row-level allowlist gate are read-only greps that write nothing into the working tree, so running the `semver` job twice on the same commit reaches the same verdict (edge: idempotency)."
    - statement: "The `semver` job's steps write only to `/tmp` paths and the job carries no `needs:` edge, so two concurrent CI runs on different commits cannot interfere with each other's verdict."
      verification: backstop
  prohibitions:
    - statement: "A placeholder marker MUST NOT be closed by deleting the sentence that carried it. Each of the four occurrences is replaced by the substantive content it was holding a place for — a citation, a checklist, or a confirmed-empty statement — and the resulting section says what is true, not less."
      category: evidence-integrity
      status: resolved
      verification: judgment
  artifacts:
    - path: "MIGRATION.md"
      provides: "Closed §9.5 (boot test cited by target name), §9.6 (golden diff cited by file), §9.7 (confirmed empty), §9.8 (the ordered operator checklist), and a rewritten header describing the closed state"
      contains: "v0_9_config_boot"
    - path: ".github/workflows/ci.yml"
      provides: "The placeholder-marker gate step in the semver job and the v0_9_config_boot step with a zero-tests-selected guard in the existing e2e-platform-api job"
      contains: "v0_9_config_boot"
  key_links:
    - from: "MIGRATION.md"
      to: "tests/integration/v0_9_config_boot_test.rs"
      via: "§9.5's disabled-by-default claim cites the boot test that proves it, by target name and path"
      pattern: "v0_9_config_boot"
    - from: "MIGRATION.md"
      to: "crates/paladin-web/tests/openapi_golden_v0_9.rs"
      via: "§9.6's golden-diff claim cites the path-restricted closure test that proves it"
      pattern: "openapi_golden_v0_9"
    - from: ".github/workflows/ci.yml"
      to: "MIGRATION.md"
      via: "the semver job's new step fails the build while any placeholder marker remains"
      pattern: "MIGRATION.md"
---

<objective>
Close `MIGRATION.md` and turn "no placeholders" from a habit every phase's verification had to
recalibrate around into a CI gate. Four placeholder markers remain — two in the header note describing
the convention itself, one in §9.5 owned by SHIP-02, one in §9.8 owned by SHIP-01. All four are
replaced by the substance they were holding a place for: §9.5 cites the boot test plan 29-01 built,
§9.6 cites the golden diff plan 29-02 built, §9.7 is confirmed empty, §9.8 becomes the ordered
operator checklist, and the header is rewritten in the past tense. Then a `semver`-job step makes the
absence permanent, and a step in the existing `e2e-platform-api` job makes the boot test actually run
in CI.

Purpose: SHIP-01's core deliverable plus the CI gating half of SHIP-02, per D-01, D-02 and D-09. Every
prior phase's VERIFICATION had to note "this marker is owned by a later phase"; a gate is what stops
the pattern recurring in v0.11.

Output: a `MIGRATION.md` whose §9 sections are all substantive, and two new CI steps in existing jobs.

This plan has no `type="tracer"` task: it edits one document and one workflow file with no layered
execution path to trace. The tracer for this phase is plan 29-01's boot test — which this plan is what
finally wires to CI.

**Deviation from D-09's literal wording, recorded.** D-09 says both compat tests are ordinary
`cargo test --workspace` targets that the `test` job gates on every PR. Measured false for the boot
test: the `test` job runs `cargo test --workspace --lib --bins` and `--doc` only
(`.github/workflows/ci.yml:438-441`), which selects no root integration target, and `web-server` is
not a default feature so the target is also skipped by the `integration-tests` job's
`--features integration-tests` sweep. D-09's actual intent — no NEW CI job — is honored by adding a
step to the existing `e2e-platform-api` job, which already runs with `--features web-server` and
already carries the zero-tests-selected guard pattern this step reuses. The openapi golden diff needs
no CI change at all: the per-crate matrix job already runs `cargo test -p paladin-web`.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-program-gates-release/29-CONTEXT.md
@.planning/phases/29-program-gates-release/29-RESEARCH.md
@.planning/phases/29-program-gates-release/29-PATTERNS.md
@.planning/phases/29-program-gates-release/29-01-SUMMARY.md
@.planning/phases/29-program-gates-release/29-02-SUMMARY.md
@.project/v0.10.0/00-program-overview.md
@CLAUDE.md

<assumption_delta_decision>
The deterministic assumption-delta scan fired on this phase with one `chosen`-family signal: the
ROADMAP phase text "M-B-01…03 resolved with **chosen** defaults". Reviewed against the actual scope.

- **Primary noun:** unchanged. The signal points at defaults chosen in Phases 23, 24 and 26
  (`EdgeCondition::Custom` fail-closed, `graceful_shutdown = true`, `tool_error_mode = FeedToModel`)
  and being *recorded* here, not decided here. No configuration decision becomes a modeling decision
  in Phase 29; no identity model, primary key or anchor field moves.
- **Decision:** `no-change`. Phase 29 writes down already-locked defaults and adds no variant, no
  second case and no new parameter.
- **Companion invariant test:** not added, because it already exists. The generalized intent this
  phase encodes — "a v0.9 config boots v0.10 with legacy behavior for every subsystem" — is plan
  29-01's boot test, which goes red the instant a future phase makes a new subsystem default-on.
</assumption_delta_decision>

<interfaces>
Measured against HEAD in this planning session:

- Placeholder-marker occurrences in `MIGRATION.md`: **four**, at lines 7 and 8 (the header note, one
  each), line 318 (§9.5, owner SHIP-02) and line 630 (§9.8, owner SHIP-01). The header currently says,
  across lines 7-8, that every marker below carries the requirement or phase that owns closing it and
  that `SHIP-01` (Phase 29) is responsible for clearing every remaining one before the v0.10.0
  release, per overview §9's living-document contract. The rewrite must keep that citation style while
  describing the closed state, and must not use the marker token itself — the new gate greps the whole
  file, so the header's own prose would re-trip it (RESEARCH anti-pattern 3).
- §9.5 line 318 ends: "This claim will be backed by an integration test that boots the server with the
  v0.9 sample config and asserts feature/config resolution to legacy behavior — …, owner SHIP-02,
  Phase 29." Replace with a past-tense citation of `v0_9_config_boot`, its path
  `tests/integration/v0_9_config_boot_test.rs`, what it asserts at both levels, the fixture it loads,
  and why the frozen `config.example.yml` copy is documentation evidence rather than the loaded file.
- §9.6 has TWO forward references to SHIP-02, both to close: line 431's "**Still owed, not this
  phase's scope:** the golden `openapi.json` diff proof … is **SHIP-02, Phase 29**", and line 574's
  "SHIP-02 (Phase 29) is still the ship-gate golden-diff proof that restricts itself to pre-existing
  paths". Neither carries a marker token, so neither is caught by the new gate — both are nonetheless
  stale the moment plan 29-02 lands.
- §9.7 (line 624) closes with "this section is finalized (or confirmed empty) at closeout, owner
  SHIP-01, Phase 29." No marker token, still a pending action.
- §9.8 (lines 628-630) is the checklist placeholder. Its parenthetical already names the intended
  order — back up state dirs and DBs, apply migrations, update config, adjust termination grace,
  register custom evaluators if used, deploy, verify — and carries two substantive facts to keep:
  `terminationGracePeriodSeconds` at least `60` (2x the 30s default
  `APP_ENGINE_SHUTDOWN_GRACE_SECS`), or at least twice whatever that env var is set to, in every
  Deployment manifest (M-B-02); and evaluator registration via
  `CampaignExecutionService::with_evaluator` on the legacy path or `WarEngine::with_edge_evaluator` on
  the `WarEngine` path (M-B-01, CF-01 Phase 23).
- The real `paladin-cli` surface, verified against the clap derive in `src/bin/paladin-cli.rs` and
  `src/application/cli/commands/graph.rs:42`: `paladin-cli setup-check [--verbose]`,
  `paladin-cli maneuver validate <flow-args>`,
  `paladin-cli graph export --format mermaid|dot (<FILE>|--assistant <id>) [--out <path>]`,
  `paladin-cli eval run <glob> [--repeat N] [--bless] [--live] [--registries <name>]`.
  `GraphCommands` has exactly one variant, `Export`. There is **no** `paladin-cli health` and **no**
  `graph validate` subcommand.
- Migrations run automatically at adapter construction via `sqlx::migrate!` (§9.4), so the
  apply-migrations step is "start the new binary once against a restored backup" plus the Postgres
  note, not a separate migrate command.
- CI step styles to copy: the `semver` job's allowlist step (`.github/workflows/ci.yml:359-409`) for
  the grep gate — `set -euo pipefail`, `test -f` the input first, `::error::` annotation before
  `exit 1`; and the `e2e-platform-api` job (`.github/workflows/ci.yml:1207-1233`) for the test step —
  `set -o pipefail`, `tee` to a log under `/tmp`, then a separate "Fail if the run selected zero tests"
  step that greps `test result: ok\. \K[0-9]+(?= passed)` and fails when the count is absent or below 1.
- The five `--baseline-version 0.9.0` occurrences in the `semver` job stay untouched (RESEARCH
  Pitfall 4). Plan 29-03 already replaced the allowlist step's body with the row-level pair
  comparison; this plan adds a sibling step beside it and must not disturb it.
</interfaces>
</context>

<tasks>

<!-- planner-discipline-allow: paladin-cli health -->
<!-- planner-discipline-allow: graph validate -->
<!-- planner-discipline-allow: baseline-version 0.9.0 -->
<!-- planner-region-allow: TBD -->

<task type="auto">
  <name>Task 1: Close §9.5, §9.6, §9.7 and §9.8, and rewrite the header note as a closed state</name>
  <files>MIGRATION.md</files>
  <read_first>
    - `MIGRATION.md` lines 1-15 (the header note), 316-330 (§9.5's opening claim), 398-440 (§9.6's
      opening and the line-431 still-owed note), 566-580 (the line-574 SHIP-02 reference), and 620-638
      (§9.7 and §9.8 in full).
    - `MIGRATION.md` §9.4 (lines 292-315) — the prose density, the "landed Phase N" provenance style,
      and the `sqlx::migrate!` statement the checklist's migration step depends on. This is the section
      to match in tone and specificity.
    - `.planning/phases/29-program-gates-release/29-01-SUMMARY.md` and `29-02-SUMMARY.md` — the actual
      target names, file paths, test function names and recorded deviations to cite. Cite what the
      summaries say shipped, not what this plan predicted.
    - `src/bin/paladin-cli.rs` (the `Commands` enum) and `src/application/cli/commands/graph.rs`
      around line 42 (`GraphCommands`) — confirm the four real subcommands before writing any of them
      into the checklist.
    - The `k8s/` Deployment manifests — the actual `terminationGracePeriodSeconds` values and file
      names, so the checklist names the real files an operator edits.
    - `.planning/phases/29-program-gates-release/29-CONTEXT.md` D-01 and D-02; `29-RESEARCH.md` Q1 and
      Q11 and the anti-pattern about the header's own prose re-tripping the gate.
  </read_first>
  <action>
    Rewrite the header note (lines 5-9) in the past tense: state that the file was created in Phase 22
    (`ENG-08`) as a living document, appended to by every later epic, and that at the v0.10.0 close
    every section is substantive — each section that once held a marker for a later requirement now
    carries the content that requirement produced. Keep the overview §9 citation. Use "placeholder" or
    "marker" as the noun and do not write the four-letter token itself anywhere in the file, because
    the new CI gate greps the whole file and the header's own prose would re-trip it.

    Replace §9.5's forward-looking sentence with a past-tense citation: name the target
    `v0_9_config_boot`, its path `tests/integration/v0_9_config_boot_test.rs`, both levels it asserts
    (every v0.10 `Settings` field the v0.9 file omits resolving to its own `Default`, and each of the
    nine platform config structs `paladin-server.rs` builds outside `Settings` being inert at
    `::default()` with `apply_env_overrides()` a no-op absent its `APP_*` variables), and the
    behavioral level (the six v0.9 `/v1/agents…` routes plus `/health`, `/ready` and `/openapi.json`
    mounted; every route family new in v0.10 answering `501`). Record the fixture deviation in one
    sentence: the loaded fixture is the frozen `v0.9.0` `config.test.yml`, and the frozen `v0.9.0`
    `config.example.yml` is kept as documentation evidence only because it does not parse at either tag
    (`LlmProviderConfig::api_key` is a required `String` and its `ollama:` block carries none — a
    pre-existing defect, not a v0.10 regression). Add one sentence stating that `501` — not `404` — is
    the documented answer for a disabled platform subsystem, cross-referencing §9.6's own status-code
    tables.

    In §9.6, replace the line-431 still-owed paragraph and the line-574 SHIP-02 sentence with a
    citation of `crates/paladin-web/tests/openapi_golden_v0_9.rs`: what it restricts to (the six paths
    present at `v0.9.0`), what it follows (the transitive `$ref` closure of those operations into
    `components.schemas`, plus `components.securitySchemes` in full), and the single sanctioned
    normalisation (`info.version`, which the release bump regenerates). Name the frozen baseline
    `crates/paladin-web/tests/fixtures/openapi-v0.9.0.json` and note it is gated on every PR by
    `cargo test -p paladin-web`.

    In §9.7, replace the pending-action clause with a closed statement: the section is confirmed empty
    at the v0.10.0 close — no item is marked `#[deprecated]` by any phase in the program — keeping the
    existing per-phase evidence sentences that explain why each phase deliberately added none.

    Replace §9.8 entirely with one ordered, numbered, copy-pasteable operator checklist. Order per the
    overview: (1) back up state directories and databases, naming the concrete paths and backends an
    operator has (waypoint store, run store, Citadel files, plus §9.4's Citadel-files-unchanged
    statement); (2) apply migrations — state that every migration in §9.4 runs automatically at adapter
    construction through `sqlx::migrate!`, so the step is "start the new binary once against a restored
    backup and confirm it comes up", plus the Postgres note §9.4 carries; (3) update config — nothing
    is required, citing §9.5's now test-backed disabled-by-default claim and naming the boot test;
    (4) raise `terminationGracePeriodSeconds` to at least `60`, or at least twice whatever
    `APP_ENGINE_SHUTDOWN_GRACE_SECS` is set to, in every Deployment manifest, naming the actual `k8s/`
    files; (5) register an evaluator for every `EdgeCondition::Custom` name in use, via
    `CampaignExecutionService::with_evaluator` or `WarEngine::with_edge_evaluator`, citing M-B-01;
    (6) deploy; (7) verify with `paladin-cli setup-check --verbose` for environment and connectivity,
    `paladin-cli maneuver validate` for configuration if the deployment uses the Maneuver DSL, and
    `paladin-cli eval run` against a scenario glob for a behavioral post-deploy check. Use only those
    real subcommands: do not write `paladin-cli health` and do not write a `graph validate`
    subcommand — neither exists. Where a "inspect the graph" step is wanted, use `graph export` (the
    one `GraphCommands` variant). Keep it one flat list, not per-subsystem sub-checklists.
  </action>
  <acceptance_criteria>
    <!-- planner-discipline-allow: TBD -->
    - `grep -c 'TBD' MIGRATION.md` is 0.
    - `grep -c 'v0_9_config_boot' MIGRATION.md` is at least 1 and `grep -c 'openapi_golden_v0_9' MIGRATION.md` is at least 1.
    - `grep -c 'openapi-v0.9.0.json' MIGRATION.md` is at least 1.
    - `grep -c 'Still owed' MIGRATION.md` is 0 and `grep -cE 'owner SHIP-0[12], Phase 29' MIGRATION.md` is 0 — no section still defers to this phase.
    - `grep -c 'paladin-cli health' MIGRATION.md` is 0, `grep -c 'graph validate' MIGRATION.md` is 0, and `grep -c 'graph-validate' MIGRATION.md` is 0.
    - `grep -c 'setup-check' MIGRATION.md` is at least 1 and `grep -c 'eval run' MIGRATION.md` is at least 1.
    - `grep -c 'terminationGracePeriodSeconds' MIGRATION.md` is at least 1 and `grep -c 'with_edge_evaluator' MIGRATION.md` is at least 1.
    - §9.8 is an ordered list with at least seven numbered steps: `awk '/^## 9\.8 /,0' MIGRATION.md | grep -cE '^[0-9]+\.'` is at least 7.
    - No §9 section was deleted: `grep -c '^## 9\.[5-8] ' MIGRATION.md` is 4.
    - `MIGRATION.md` grew rather than shrank: `git diff --numstat HEAD~1 -- MIGRATION.md | awk '{print ($1 > $2)}'` prints 1.
    - No `.rs` file changed by this task's commit: `git diff --name-only HEAD~1 | grep -c '\.rs$'` is 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -euo pipefail; test "$(grep -c TBD MIGRATION.md)" -eq 0; grep -q v0_9_config_boot MIGRATION.md; grep -q openapi_golden_v0_9 MIGRATION.md; test "$(grep -c "paladin-cli health" MIGRATION.md)" -eq 0; test "$(grep -c "graph validate" MIGRATION.md)" -eq 0; test "$(grep -c "Still owed" MIGRATION.md)" -eq 0; test "$(awk "/^## 9\.8 /,0" MIGRATION.md | grep -cE "^[0-9]+\.")" -ge 7; test "$(grep -c "^## 9\.[5-8] " MIGRATION.md)" -eq 4'</automated>
  </verify>
  <done>Every §9 section of `MIGRATION.md` says something substantive: the two compat claims cite the tests that prove them, deprecations are confirmed empty, and an operator has one ordered checklist naming only commands that exist.</done>
</task>

<task type="auto">
  <name>Task 2: Add the placeholder-marker gate to the `semver` job and run the boot test in the existing `e2e-platform-api` job</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - `.github/workflows/ci.yml` lines 296-410 — the whole `semver` job as plan 29-03 left it: the
      package list, the five `--baseline-version 0.9.0` occurrences, and the row-level allowlist step
      whose shell conventions this new sibling step copies.
    - `.github/workflows/ci.yml` lines 1195-1240 — the `e2e-platform-api` job in full: its checkout,
      toolchain, cache key, the `cargo test --features web-server --test e2e_platform_api` step with
      `set -o pipefail` and `tee`, and the separate "Fail if the run selected zero tests" step with its
      `grep -oP 'test result: ok\. \K[0-9]+(?= passed)'` guard.
    - `.github/workflows/ci.yml` lines 415-445 — the `test` job, to confirm for the record that it runs
      only `--lib --bins` and `--doc` and therefore does not select a root integration target.
    - `MIGRATION.md` as Task 1 left it — the gate must pass against it on the first run.
    - `.planning/phases/29-program-gates-release/29-01-SUMMARY.md` — the exact target name registered
      in `Cargo.toml` and the number of tests it contains, so the zero-selected guard's threshold is
      right.
  </read_first>
  <action>
    Add a step to the `semver` job, immediately after the row-level allowlist step, named for what it
    enforces (that `MIGRATION.md` carries no unfilled placeholder). Body in the sibling step's style:
    `set -euo pipefail`, `test -f MIGRATION.md` first so a missing file fails rather than reading as an
    empty set, then count occurrences of the marker token in the file and fail with a `::error::`
    annotation naming the file and the offending line numbers when the count is non-zero. Print the
    matching lines before failing so the log names what to fix. Add a leading comment block recording
    why the gate exists — every prior phase's verification had to recalibrate around markers owned by a
    later phase, and this stops the pattern recurring in v0.11 — and warning that any prose describing
    the convention must not contain the token itself.

    Add two steps to the existing `e2e-platform-api` job, after its current pair, in that job's own
    style. The first runs `cargo test --features web-server --test v0_9_config_boot` with
    `set -o pipefail` and `tee` to a distinct log path under `/tmp`. The second is a separate
    zero-selected guard that extracts the passed count from that log with the same `grep -oP 'test
    result: ok\. \K[0-9]+(?= passed)' | tail -1` idiom and fails with a `::error::` annotation when the
    count is absent or below the number of tests `29-01-SUMMARY.md` records — a filter selecting zero
    tests exits 0 in cargo, which is exactly the silent no-op this guard exists to catch. Update the
    job's `name:` so it no longer reads as covering only the PRD 06 lifecycle, and extend its leading
    comment block to record that it now also carries the SHIP-02 boot proof and why: `web-server` is not
    a default feature, so this is the only existing job that can select the target, and D-09 asks for no
    new job.

    Do not add a new workflow job, do not modify the `test` job, and do not touch any
    `--baseline-version 0.9.0` occurrence.
  </action>
  <acceptance_criteria>
    - `grep -c 'v0_9_config_boot' .github/workflows/ci.yml` is at least 2 — the test step and the guard step both name the target.
    - `python3 -c "import yaml; d=yaml.safe_load(open('.github/workflows/ci.yml')); j=d['jobs']; print(len(j['semver']['steps']), len(j['e2e-platform-api']['steps']))"` prints step counts of 6 and 7 respectively (one more `semver` step and two more `e2e-platform-api` steps than before this task), and the file parses as valid YAML.
    - No new job was created: `python3 -c "import yaml; print(len(yaml.safe_load(open('.github/workflows/ci.yml'))['jobs']))"` prints the same number as `git show HEAD~1:.github/workflows/ci.yml | python3 -c "import yaml,sys; print(len(yaml.safe_load(sys.stdin)['jobs']))"`.
    - `grep -c 'baseline-version 0.9.0' .github/workflows/ci.yml` is 5 — unchanged.
    - The `test` job is untouched: `git diff HEAD~1 -- .github/workflows/ci.yml | grep -c 'lib --bins'` is 0.
    - The new gate passes against the current `MIGRATION.md`: reproducing its body locally exits 0.
    - The new gate is proven fail-first: on a scratchpad copy of `MIGRATION.md` with the marker token reinserted once, the same body exits non-zero. Run against a copy; do not modify the tracked file.
    - `grep -c 'selected zero tests' .github/workflows/ci.yml` is at least 2 — the pre-existing guard plus the new one.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -euo pipefail; python3 -c "import yaml; d=yaml.safe_load(open(\".github/workflows/ci.yml\")); j=d[\"jobs\"]; assert len(j[\"semver\"][\"steps\"])==6, len(j[\"semver\"][\"steps\"]); assert len(j[\"e2e-platform-api\"][\"steps\"])==7, len(j[\"e2e-platform-api\"][\"steps\"])"; test "$(grep -c v0_9_config_boot .github/workflows/ci.yml)" -ge 2; test "$(grep -c "baseline-version 0.9.0" .github/workflows/ci.yml)" -eq 5; test "$(grep -c TBD MIGRATION.md)" -eq 0; S="$TMPDIR/mig-probe.md"; cp MIGRATION.md "$S"; printf "\nTBD placeholder probe\n" >> "$S"; if test "$(grep -c TBD "$S")" -eq 0; then echo "gate is not fail-first" >&2; exit 1; fi; rm -f "$S"'</automated>
  </verify>
  <done>`MIGRATION.md` can never silently reacquire a placeholder, and the SHIP-02 boot test runs in CI on every PR with a guard that fails rather than reading green when the filter selects nothing — both without a new workflow job.</done>
</task>

</tasks>

<threat_model>
> Assessed at **ASVS Level 1** (`workflow.security_asvs_level = 1`); **`high`-severity threats block the phase** (`workflow.security_block_on = high`); `medium`/`low` are dispositioned per the register below.
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `MIGRATION.md`'s upgrade instructions → an operator's production cluster | A checklist naming a command that does not exist, or omitting the grace-period change, breaks a real upgrade. |
| A test target's existence → CI's claim that it gates | A registered test nothing runs is worse than no test, because the register reads as coverage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-05-01 | Repudiation | §9.5/§9.6 claiming compatibility with no test behind it | high | mitigate | Both claims are rewritten to cite a real target by name and path, and the acceptance criteria grep for `v0_9_config_boot` and `openapi_golden_v0_9` in the file; Task 2 makes the boot test actually run in CI. |
| T-29-05-02 | Denial of service | §9.8 instructing an operator to run `paladin-cli health` or `graph validate` | high | mitigate | Neither subcommand exists; the acceptance criteria assert both strings are absent from the file, and the checklist names only the four verified subcommands. An upgrade blocked on a nonexistent verification command is an availability failure, not a documentation nit. |
| T-29-05-03 | Denial of service | An operator deploying without raising `terminationGracePeriodSeconds` and losing in-flight runs | high | mitigate | Step 4 of the checklist names the value, the env var it is derived from, and the actual `k8s/` manifests; a criterion asserts the setting is named in the file. |
| T-29-05-04 | Tampering | A future edit reintroducing a placeholder or loosening the gate | medium | mitigate | The gate is a read-only grep in the `semver` job with a fail-first probe in this plan's own verify; the step's comment records why it exists. |
| T-29-05-05 | Repudiation | The new CI step reading green having selected zero tests | high | mitigate | A separate guard step extracts the passed count and fails when absent or below the count `29-01-SUMMARY.md` records — the same failure shape the pre-existing `e2e_platform_api` guard was written for. |
</threat_model>

<verification>
- `grep -c TBD MIGRATION.md` is 0, and the gate's own body exits 0 against the file and non-zero
  against a scratch copy with the token reinserted.
- `.github/workflows/ci.yml` parses as YAML with the same job count as before, one more `semver` step
  and two more `e2e-platform-api` steps.
- `cargo test --features web-server --test v0_9_config_boot` green locally with the count the new CI
  guard's threshold expects.
- `cargo fmt --check` and `cargo clippy --workspace --all-targets -- -D warnings` unaffected (no Rust
  file changed by this plan).
</verification>

<success_criteria>
- SHIP-01's "every §9 section filled with no placeholder" is true and enforced, with §9.2's register
  gate already tightened by plan 29-03.
- SHIP-02's two proofs are both cited from `MIGRATION.md` and both gated by CI on every PR.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

See the identical section in `29-01-PLAN.md` — the canonical list for Phase 29. This plan's own
contribution: `MIGRATION.md`'s rewritten header note and closed §9.5, §9.6, §9.7 and §9.8; the
placeholder-marker gate step in `.github/workflows/ci.yml`'s `semver` job; and the
`v0_9_config_boot` test step plus its zero-tests-selected guard step in the existing
`e2e-platform-api` job. No new Rust symbol, no new dependency, no new workflow job, no production
behavior change.
</artifacts_produced>

<output>
Create `.planning/phases/29-program-gates-release/29-05-SUMMARY.md` when done.
</output>
