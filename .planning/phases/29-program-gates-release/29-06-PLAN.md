---
phase: 29-program-gates-release
plan: 06
type: execute
wave: 3
depends_on: [29-05]
files_modified:
  - docs/src/api-reference/upgrading.md
  - docs/src/SUMMARY.md
  - docs/src/api-reference/migration-guide.md
  - .project/v0.10.0/00-program-overview.md
autonomous: true
requirements: [SHIP-01]

must_haves:
  truths:
    - "`docs/src/api-reference/upgrading.md` exists as a hand-written page — not an `{{#include}}` of `MIGRATION.md` — carrying a two-paragraph orientation, the §9.1 behavioral-change table with one line per M-B-01…04, a mirror of §9.8's operator checklist, and a link to `MIGRATION.md` by repository URL (D-03)."
    - "The page is registered in `docs/src/SUMMARY.md` on the line directly above the existing Migration Guide entry, in the same list style (D-03)."
    - "`docs/src/api-reference/migration-guide.md` gains a short v0.10.0 section at the top pointing to the Upgrading page and `MIGRATION.md`, plus one line noting that the 0.6 through 0.9 changes are recorded in `CHANGELOG.md` (D-03)."
    - "`mdbook build docs/` succeeds with `mdbook-linkcheck` at `warning-policy = \"error\"` — every link on the new page resolves, and no relative repository-root path (`crates/…`, `k8s/…`) is introduced that the linkchecker would reject (D-03)."
    - "`.project/v0.10.0/00-program-overview.md` §4's Directive row carries a one-line errata note stating that `NextStep` has exactly five variants — `Edges`, `Goto`, `Muster`, `End`, `Parley` — and that `Halt` is a run-level outcome concept (`RunOutcome::Halted`, `WaypointStatus::Halted`) reached via cancellation, never a node's routing choice (D-25, RESEARCH Q9)."
    - "The README's MSRV badge, its prerequisites line and `MIGRATION.md` §9.3 all state 1.88, confirmed in this plan and recorded rather than changed (D-25)."
    - "The doc sweep is exactly the bounded list D-25 names. The `Since: v0.10.0` markers on the Phase 22-28 mdBook pages are left as they are, and the pre-existing default-feature `cargo doc` warning set is out of scope here (D-25, RESEARCH Pitfall 7)."
  prohibitions:
    - statement: "The Upgrading page MUST NOT be produced by including `MIGRATION.md` through the mdBook links preprocessor. `MIGRATION.md` carries relative repository-root links that the docs job's linkchecker rejects at `warning-policy = \"error\"`, so an include would fail the docs build; the page is hand-written (D-03)."
      category: build-integrity
      status: resolved
      verification: judgment
    - statement: "The overview §4 errata note MUST NOT rename, restructure or reclassify any ubiquitous-language term. It states a single verified fact about `NextStep`'s variants and leaves the term table otherwise untouched (D-25, X-10)."
      category: scope-integrity
      status: resolved
      verification: judgment
  artifacts:
    - path: "docs/src/api-reference/upgrading.md"
      provides: "The mdBook Upgrading page SHIP-01 requires: orientation, the M-B-01..04 behavioral-change table, a mirror of the §9.8 checklist, and the MIGRATION.md link"
      contains: "M-B-01"
    - path: "docs/src/SUMMARY.md"
      provides: "The Upgrading entry, directly above the Migration Guide entry"
      contains: "api-reference/upgrading.md"
    - path: "docs/src/api-reference/migration-guide.md"
      provides: "A v0.10.0 (from v0.9.x) pointer section at the top, plus the CHANGELOG.md note covering 0.6-0.9"
      contains: "v0.10.0"
    - path: ".project/v0.10.0/00-program-overview.md"
      provides: "The §4 Directive-row errata note reconciling the Halt mention with NextStep's real five variants"
      contains: "RunOutcome::Halted"
  key_links:
    - from: "docs/src/SUMMARY.md"
      to: "docs/src/api-reference/upgrading.md"
      via: "the new SUMMARY entry registers the page in the book, directly above the Migration Guide entry"
      pattern: "api-reference/upgrading.md"
    - from: "docs/src/api-reference/upgrading.md"
      to: "MIGRATION.md"
      via: "the page links the root migration record by repository URL, not by a relative path the linkchecker would follow"
      pattern: "MIGRATION.md"
---

<objective>
Give an upgrading operator a page in the published book. SHIP-01 names an mdBook "Upgrading" page
alongside the README link that already exists; this plan writes it by hand, registers it above the
existing Migration Guide entry, points the historical v0.1→v0.5 guide at it, and lands the one errata
note D-25's bounded doc sweep still owes.

Purpose: SHIP-01's documentation half, per D-03 and D-25. The page is hand-written rather than an
include because `MIGRATION.md`'s relative `crates/…` and `k8s/…` links would fail the docs job's
linkchecker, which runs with `warning-policy = "error"`.

Output: a new book page, a SUMMARY entry, a pointer section on the historical guide, and a one-line
factual correction in the program overview.

This plan has no `type="tracer"` task: it is four documentation edits with no layered execution path.
The tracer for this phase is plan 29-01's boot test.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-program-gates-release/29-CONTEXT.md
@.planning/phases/29-program-gates-release/29-RESEARCH.md
@.planning/phases/29-program-gates-release/29-PATTERNS.md
@.planning/phases/29-program-gates-release/29-05-SUMMARY.md
@CLAUDE.md

<interfaces>
Measured against HEAD in this planning session:

- `docs/src/SUMMARY.md` lines 64-73 are the API Reference list. The Migration Guide entry is
  `- [Migration Guide](api-reference/migration-guide.md)`. The new entry
  `- [Upgrading](api-reference/upgrading.md)` goes on the line directly above it, same list style,
  same indentation.
- `docs/src/api-reference/migration-guide.md` opens with `# Migration Guide`, then "This guide covers
  all breaking changes since v0.1.0 up to the current **v0.5.0** release.", then a
  `## Table of Contents` bullet list, then dated `## Migrating to vX.Y.Z (from …)` sections. It stops
  at v0.5.0 — it is a historical document, which is why D-03 adds a pointer rather than appending the
  v0.10.0 content to it.
- `docs/book.toml` sets `[output.linkcheck] warning-policy = "error"`, and `preprocessor.links` is
  enabled — so an `{{#include ../../../MIGRATION.md}}` would render but its relative repository-root
  links would fail the docs job. This is D-03's rejection reason, and the reason the page links
  `MIGRATION.md` by repository URL.
- `docs/.github/workflows/docs.yml` runs mdbook 0.4.40 with mermaid and mdbook-linkcheck 0.7.7, plus
  `check-doc-examples.sh` and `check-doc-config.sh`. Local `mdbook` 0.4.40 and `mdbook-linkcheck`
  0.7.7 are both present in this devcontainer (verified by running them).
- `MIGRATION.md` §9.1 holds M-B-01 (BUG-01 `EdgeCondition::Custom` fail-closed), M-B-02 (shutdown
  grace default and the `terminationGracePeriodSeconds` requirement), M-B-03 (`tool_error_mode`
  default `FeedToModel`) and M-B-04 (added by `ENG-08` in Phase 22). All four go on the page's table,
  one line each. §9.8 is the checklist to mirror — as plan 29-05 wrote it, so read that file, not this
  plan's description of it.
- `NextStep`'s real definition, verified at
  `crates/paladin-core/src/platform/container/directive.rs:40-89`: five variants — `Edges`,
  `Goto(Vec<NodeId>)`, `Muster(Vec<MusterTask>)`, `End`, `Parley(ParleyRequest)`. There is no `Halt`
  variant. `Halted` exists only as a run outcome: `RunOutcome::Halted { waypoint }` and
  `WaypointStatus::Halted` (`crates/paladin-battalion/src/engine/mod.rs:315` and nearby), reached via
  cancellation (`ENG-FR-23`).
- `.project/v0.10.0/00-program-overview.md:86` is the §4 Directive row, whose text reads that a
  Directive carries "state delta plus optional routing (`Goto`, `End`, `Halt`)". That mention is what
  `23-DISCUSSION-LOG.md` line 133 parked for the Phase 29 doc sweep.
- MSRV agreement, verified: `README.md:8` is the badge `rust-1.88%2B`, `README.md:101` is
  "Prerequisites: Rust ≥ 1.88 (edition 2024)", and `MIGRATION.md` §9.3 states 1.88. All three already
  agree — D-25 asks the audit to RECORD this, not to change anything.
- `README.md:97` already carries the `MIGRATION.md` link SHIP-01 requires
  ("**Upgrading:** [`MIGRATION.md`](MIGRATION.md) …") — verified present, so no README edit is needed
  for the link half of SHIP-01.
- Out of scope here, per D-25 and RESEARCH Pitfall 7: the 72-warning default-feature
  `cargo doc --workspace --no-deps` condition and the pre-existing `qdrant --all-features` rustdoc
  break. Both are recorded by plan 29-07 and plan 29-09, not fixed here.
</interfaces>
</context>

<tasks>

<!-- planner-discipline-allow: {{#include -->
<!-- planner-discipline-allow: api-reference/migration-guide.md -->
<!-- planner-discipline-allow: README.md -->

<task type="auto">
  <name>Task 1: Write the Upgrading page, register it in SUMMARY, and point the historical guide at it</name>
  <files>docs/src/api-reference/upgrading.md, docs/src/SUMMARY.md, docs/src/api-reference/migration-guide.md</files>
  <read_first>
    - `MIGRATION.md` §9.1 in full (lines 11-60) — the four M-B rows, their "Who is affected" and
      "Required user action" columns, which the page's table condenses to one line each.
    - `MIGRATION.md` §9.8 as plan 29-05 wrote it — the checklist to mirror verbatim. Read the file, not
      this plan's summary of it, so the mirror cannot drift.
    - `docs/src/api-reference/migration-guide.md` lines 1-40 — the `#`-title, orientation paragraph,
      table-of-contents and dated-section structure to echo in tone, and where the new v0.10.0 pointer
      section goes relative to the existing table of contents.
    - `docs/src/SUMMARY.md` lines 60-76 — the API Reference list and the exact Migration Guide line.
    - `docs/book.toml` — the `[output.linkcheck]` block, to confirm the policy that forbids an include.
    - `.planning/phases/29-program-gates-release/29-CONTEXT.md` D-03.
  </read_first>
  <action>
    Create `docs/src/api-reference/upgrading.md`. Structure: an `# Upgrading` title; two orientation
    paragraphs saying who this page is for (an operator or library consumer moving a deployment from
    v0.9.x to v0.10.0) and what the authoritative record is (root `MIGRATION.md`, linked by repository
    URL); a `## Behavioral changes` table with one row per M-B-01, M-B-02, M-B-03 and M-B-04, each
    naming the change, who is affected, and the required action, condensed from §9.1 to one line
    apiece; a `## Upgrade checklist` section mirroring §9.8's numbered steps as plan 29-05 wrote them,
    including the real `paladin-cli` subcommands and the `terminationGracePeriodSeconds` value; and a
    closing `## Full migration record` section linking `MIGRATION.md` by repository URL. Do not use
    `{{#include}}`. Do not introduce any relative link that leaves `docs/src/` — every link is either
    an in-book relative link to another `docs/src/` page or an absolute repository URL, because the
    docs job's linkchecker treats a warning as an error.

    In `docs/src/SUMMARY.md`, insert `- [Upgrading](api-reference/upgrading.md)` on the line directly
    above the existing `- [Migration Guide](api-reference/migration-guide.md)` line, matching its
    indentation and list marker exactly. Change nothing else in the file.

    In `docs/src/api-reference/migration-guide.md`, add a short `## Upgrading to v0.10.0 (from v0.9.x)`
    section immediately after the orientation paragraph and before the existing table of contents,
    containing: two or three sentences saying that v0.10.0's upgrade record lives on the Upgrading page
    and in root `MIGRATION.md`, an in-book relative link to `upgrading.md`, a repository-URL link to
    `MIGRATION.md`, and one line noting that the 0.6 through 0.9 changes are recorded in
    `CHANGELOG.md` rather than in this guide. Add the new section to the existing table of contents so
    the page stays internally consistent. Leave every existing dated section untouched — this file is
    the historical v0.1→v0.5 record and is not being rewritten.
  </action>
  <acceptance_criteria>
    - `test -f docs/src/api-reference/upgrading.md` succeeds and the file is at least 40 lines: `test "$(wc -l < docs/src/api-reference/upgrading.md)" -ge 40`.
    - All four behavioral changes are on the page: `for m in M-B-01 M-B-02 M-B-03 M-B-04; do grep -q "$m" docs/src/api-reference/upgrading.md || exit 1; done` exits 0.
    - `grep -c 'terminationGracePeriodSeconds' docs/src/api-reference/upgrading.md` is at least 1 and `grep -c 'setup-check' docs/src/api-reference/upgrading.md` is at least 1 — the checklist mirror carries the concrete value and a real command.
    - `grep -c 'paladin-cli health' docs/src/api-reference/upgrading.md` is 0 and `grep -c 'graph validate' docs/src/api-reference/upgrading.md` is 0.
    - `grep -c '{{#include' docs/src/api-reference/upgrading.md` is 0.
    - The SUMMARY entry sits directly above the Migration Guide entry: `grep -n -A1 'api-reference/upgrading.md' docs/src/SUMMARY.md | grep -c 'api-reference/migration-guide.md'` is 1.
    - `git diff --numstat HEAD~1 -- docs/src/SUMMARY.md | awk '{print $1, $2}'` shows exactly 1 insertion and 0 deletions.
    - `grep -c 'v0.10.0' docs/src/api-reference/migration-guide.md` is at least 2 (the new section heading and its table-of-contents entry) and `grep -c 'CHANGELOG.md' docs/src/api-reference/migration-guide.md` is at least 1.
    - `grep -c 'Migrating to v0.5.0' docs/src/api-reference/migration-guide.md` is at least 1 — no historical section was removed.
    - `mdbook build docs/` exits 0 with no linkcheck error.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -euo pipefail; U=docs/src/api-reference/upgrading.md; test -f "$U"; for m in M-B-01 M-B-02 M-B-03 M-B-04; do grep -q "$m" "$U"; done; test "$(grep -c "{{#include" "$U")" -eq 0; test "$(grep -c "paladin-cli health" "$U")" -eq 0; test "$(grep -n -A1 "api-reference/upgrading.md" docs/src/SUMMARY.md | grep -c "api-reference/migration-guide.md")" -eq 1; grep -q CHANGELOG.md docs/src/api-reference/migration-guide.md; mdbook build docs/ >/dev/null'</automated>
  </verify>
  <done>An operator reading the published book finds an Upgrading page above the Migration Guide that names every behavioral change and the ordered checklist, and the historical guide points forward instead of appearing to stop at v0.5.0.</done>
</task>

<task type="auto">
  <name>Task 2: Land the overview §4 errata note and record the MSRV agreement</name>
  <files>.project/v0.10.0/00-program-overview.md</files>
  <read_first>
    - `.project/v0.10.0/00-program-overview.md` lines 75-93 — §4's term table in full, especially the
      Directive row at line 86 with its `(Goto, End, Halt)` parenthetical, and the surrounding rows'
      formatting so the errata note matches the table's style.
    - `crates/paladin-core/src/platform/container/directive.rs` lines 30-95 — the `NextStep` enum's
      five real variants and the `Directive` struct that carries it.
    - `crates/paladin-battalion/src/engine/mod.rs` around line 315 — `RunOutcome::Halted { waypoint }`
      and the `WaypointStatus::Halted` it pairs with, plus the cancellation path (`ENG-FR-23`) that
      reaches it.
    - `.planning/phases/23-control-flow-dynamic-routing-fan-out-subgraphs/23-DISCUSSION-LOG.md` around
      line 133 — the note that parked this reconciliation for the Phase 29 doc sweep, so the errata
      cites where the question came from.
    - `README.md` lines 8 and 101, and `MIGRATION.md` §9.3 — the three MSRV statements to re-verify.
    - `.planning/phases/29-program-gates-release/29-CONTEXT.md` D-25; `29-RESEARCH.md` Q9.
  </read_first>
  <action>
    Add a one-line errata note to `.project/v0.10.0/00-program-overview.md` §4, attached to the
    Directive row (either as a trailing sentence inside the row's description cell or as a footnote
    immediately beneath the table, whichever keeps the table rendering clean). The note states the
    verified fact: `NextStep` has exactly five variants — `Edges`, `Goto`, `Muster`, `End`, `Parley` —
    and `Halt` is not among them; `Halted` is a run-level outcome (`RunOutcome::Halted`,
    `WaypointStatus::Halted`) reached through cancellation, a distinct concept from a node's routing
    choice. Cite the defining file `crates/paladin-core/src/platform/container/directive.rs` and mark
    the note as an errata recorded at the v0.10.0 close, referencing `23-DISCUSSION-LOG.md`'s parked
    question. Do not restructure §4, do not rename any term, and do not touch any other row — the term
    table's twelve entries are what plan 29-04's ubiquitous-language audit checks against.

    Re-verify the MSRV agreement and record the result in this plan's SUMMARY (not in a new document):
    confirm `README.md`'s badge and prerequisites line and `MIGRATION.md` §9.3 all state 1.88, and
    state that no change was needed. D-25 asks for a recorded confirmation, so the SUMMARY carries the
    three citations and the verdict; plan 29-04's audit section 2 references it.

    Confirm and record — without changing them — that the `Since: v0.10.0` markers on the Phase 22-28
    mdBook pages are correct as they stand, and that the pre-existing default-feature `cargo doc`
    warning condition is outside this bounded sweep (plan 29-07 files it, plan 29-09 records the
    count). Stating both keeps the sweep's boundary explicit rather than leaving two known conditions
    looking overlooked.
  </action>
  <acceptance_criteria>
    - `grep -c 'RunOutcome::Halted' .project/v0.10.0/00-program-overview.md` is at least 1 and `grep -c 'WaypointStatus::Halted' .project/v0.10.0/00-program-overview.md` is at least 1.
    - `grep -c 'directive.rs' .project/v0.10.0/00-program-overview.md` is at least 1 — the note cites the defining file.
    - §4's table still has twelve term rows: `awk '/^## 4\./,/^## 5\./' .project/v0.10.0/00-program-overview.md | grep -cE '^\| \*\*' ` is 12.
    - All twelve terms are still present and unrenamed: `for t in Battlefield Dispatch Superstep Waypoint Thread Directive Muster Parley Vanguard Chronicle Aegis Vault; do grep -q "\*\*$t\*\*" .project/v0.10.0/00-program-overview.md || exit 1; done` exits 0.
    - The edit is confined to §4: `git diff HEAD~1 -- .project/v0.10.0/00-program-overview.md | grep -cE '^\+' ` is at most 4, and `git diff HEAD~1 -- .project/v0.10.0/00-program-overview.md | grep -cE '^-'` is at most 1.
    - MSRV agreement re-verified and unchanged: `grep -c '1.88' README.md` is at least 2, `grep -c '1.88' MIGRATION.md` is at least 1, and `git diff --name-only HEAD~1 | grep -c 'README.md'` is 0.
    - No `.rs` file changed: `git diff --name-only HEAD~1 | grep -c '\.rs$'` is 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -euo pipefail; O=.project/v0.10.0/00-program-overview.md; grep -q "RunOutcome::Halted" "$O"; grep -q "WaypointStatus::Halted" "$O"; grep -q "directive.rs" "$O"; test "$(awk "/^## 4\./,/^## 5\./" "$O" | grep -cE "^\| \*\*")" -eq 12; for t in Battlefield Dispatch Superstep Waypoint Thread Directive Muster Parley Vanguard Chronicle Aegis Vault; do grep -q "\*\*$t\*\*" "$O"; done; test "$(git diff --name-only HEAD~1 | grep -c "README.md")" -eq 0'</automated>
  </verify>
  <done>The overview no longer implies a sixth `NextStep` variant, the correction is one cited line rather than a restructuring, and the MSRV agreement plus the two deliberately-excluded doc conditions are recorded so the sweep's boundary is explicit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Published book page → an operator's upgrade procedure | A checklist mirror that drifts from `MIGRATION.md` §9.8 sends an operator down a stale path. |
| A new page's links → the docs job's linkchecker | `warning-policy = "error"` means one unresolvable link fails the whole docs build. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-06-01 | Denial of service | The docs build failing on a relative repository-root link | high | mitigate | The page is hand-written with no `{{#include}}`, links `MIGRATION.md` by repository URL, and `mdbook build docs/` is an acceptance criterion — the same linkchecker configuration the docs job uses. |
| T-29-06-02 | Repudiation | The page's checklist mirror drifting from §9.8 | medium | mitigate | `read_first` requires reading `MIGRATION.md` §9.8 as plan 29-05 actually wrote it, not this plan's description; criteria assert the concrete grace-period value and a real subcommand appear on the page. |
| T-29-06-03 | Tampering | The errata note growing into a §4 restructuring or a term rename | medium | mitigate | Criteria bound the diff to at most four added and one removed line, assert twelve term rows remain, and assert all twelve term names are unchanged — the same table plan 29-04's language audit checks. |
| T-29-06-04 | Information disclosure | The Upgrading page publishing a credential-shaped example | low | mitigate | The checklist mirror names env var NAMES and file paths only; no example carries a key value, matching the security instructions' credential-handling rule. |
</threat_model>

<verification>
- `mdbook build docs/` exits 0 with mdbook-linkcheck at `warning-policy = "error"`.
- `./scripts/check-doc-examples.sh` and `./scripts/check-doc-config.sh` exit 0 (the two other checks
  the docs job runs).
- `docs/src/SUMMARY.md`'s diff is exactly one added line.
- `.project/v0.10.0/00-program-overview.md` §4 still has twelve term rows with unchanged names.
</verification>

<success_criteria>
- SHIP-01's "linked from the README and the mdBook Upgrading page" is satisfied: the README link was
  already present and verified, and the Upgrading page now exists and is registered in the book.
- D-25's bounded doc sweep is complete, with its two deliberate exclusions recorded rather than left
  looking overlooked.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

See the identical section in `29-01-PLAN.md` — the canonical list for Phase 29. This plan's own
contribution: `docs/src/api-reference/upgrading.md` (new page), the `docs/src/SUMMARY.md` Upgrading
entry, the `docs/src/api-reference/migration-guide.md` v0.10.0 pointer section, and the
`.project/v0.10.0/00-program-overview.md` §4 Directive-row errata note. No new Rust symbol, no new
dependency, no production behavior change.
</artifacts_produced>

<output>
Create `.planning/phases/29-program-gates-release/29-06-SUMMARY.md` when done.
</output>
