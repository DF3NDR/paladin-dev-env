---
phase: 29-program-gates-release
plan: 07
type: execute
wave: 3
depends_on: [29-04, 29-05]
files_modified:
  - .project/v0.10.0/09-program-acceptance-audit.md
  - docs/src/operations/observability.md
autonomous: true
requirements: [SHIP-03]

must_haves:
  truths:
    - "Audit section 6 records the compatibility audit: every §9.2 row checked against `cargo semver-checks` output AND a public-API diff of `.project/current-exports.txt` at HEAD against `git show v0.9.0:.project/current-exports.txt`, with no new tool adopted (D-05)."
    - "Every pre-existing public item whose signature changed between v0.9.0 and HEAD maps to a §9.2 row; every §9.2 `N` row corresponds to a change `cargo semver-checks` classifies as non-breaking; every deliberate-zero note is confirmed by the named type's absence from the v0.9.0 export (D-05)."
    - "Section 6 states explicitly that no pre-existing public trait gained a required method (doc-08 step 6's final clause)."
    - "Audit section 7 confirms `MIGRATION.md` §9.1 holds M-B-01, M-B-02, M-B-03 and M-B-04 and nothing else, and cites the Phase 22 `ENG-08` decision that added M-B-04 rather than treating it as an unexplained fourth entry (D-17, doc-08 step 7)."
    - "Audit section 8 records the toolchain audit: the MSRV job's toolchain and result, the agreement of `rust-version`, the README badge and `MIGRATION.md` §9.3 on 1.88, a `cargo tree -e features` default-feature check showing no new heavyweight dependency, and `cargo deny` plus `cargo audit` results (doc-08 step 8, X-11)."
    - "Audit section 9 records both SHIP-02 proofs as existing and passing, citing `v0_9_config_boot` and `crates/paladin-web/tests/openapi_golden_v0_9.rs` by name with their observed pass counts (doc-08 step 9)."
    - "The Phase 28 tracing-overhead measurement is recorded as an ACCEPTED deviation for v0.10.0 — log sink +22.18%, composite +18.46% against PRD 07 acceptance 6's ≤3% bar — with the reason stated (tracing sinks are opt-in, `trace.state_values` defaults off, and the microbenchmark is all-Function-node with no LLM latency to amortise against) and the re-scoped bar named as the follow-up (D-16)."
    - "`docs/src/operations/observability.md` states the measured overhead figures and now also states the v0.10.0 disposition, so a reader of the published docs learns the number AND that it is an accepted, tracked deviation (D-16)."
    - "A maintainer sign-off section lists the six judgment-tier safety/privacy prohibitions from `28-VERIFICATION.md` and the M-B-04 countersignature as unchecked `- [ ]` boxes with the evidence beside each, left unticked by the executor (D-17)."
    - "The pre-existing default-feature `cargo doc --workspace --no-deps` warning condition is recorded with its measured count as a carried, out-of-scope condition rather than omitted (RESEARCH Pitfall 7, Open Question 2)."
  prohibitions:
    - statement: "A finding MUST NOT be closed by changing production Rust behavior. Allowed fixes remain documentation, test additions, FR-citation doc comments, `MIGRATION.md`/allowlist corrections, CI gate text and changelog text; anything else halts for a recorded human decision (D-12, X-03)."
      category: scope-integrity
      status: resolved
      verification: judgment
    - statement: "The executor MUST NOT tick any maintainer sign-off checkbox. The judgment-tier items exist precisely because an agent's verdict on them is non-authoritative; they are ticked by a human at the phase's UAT step (D-17)."
      category: governance
      status: resolved
      verification: judgment
  artifacts:
    - path: ".project/v0.10.0/09-program-acceptance-audit.md"
      provides: "Audit sections 6-9 filled, the D-16 accepted-deviation record, and the maintainer sign-off checklist left unchecked"
      contains: "22.18"
    - path: "docs/src/operations/observability.md"
      provides: "The Known limitations bullet extended with the v0.10.0 disposition of the overhead measurement"
      contains: "22.18"
  key_links:
    - from: ".project/v0.10.0/09-program-acceptance-audit.md"
      to: ".project/current-exports.txt"
      via: "section 6's public-API diff compares this committed snapshot against its v0.9.0 counterpart"
      pattern: "current-exports.txt"
    - from: ".project/v0.10.0/09-program-acceptance-audit.md"
      to: ".cargo/semver-checks-allowlist.toml"
      via: "section 6 confirms every allowlist entry corresponds to a deliberate-breaking §9.2 row"
      pattern: "semver-checks-allowlist"
---

<objective>
Finish the audit's compatibility, behavioral-change, toolchain and config-compat sections, and record
the two things a human must decide rather than an agent: the Phase 28 tracing-overhead bar that was
measured and failed, and the judgment-tier items whose verdict an agent cannot authoritatively give.

Purpose: SHIP-03's second half, per D-05, D-16 and D-17. Doc-08 step 6 asks for a public-API diff
against v0.9.0; the tree already produces that snapshot, so no new tool is adopted. Steps 7 through 9
are confirmations with citations. The overhead deviation and the six judgment-tier prohibitions are the
phase's honest edges, and recording them is the point.

Output: audit sections 6 through 9 filled, the accepted-deviation record, and an unchecked maintainer
sign-off section that the phase's UAT step consumes.

This plan has no `type="tracer"` task: it produces evidence over already-shipped code. The tracer for
this phase is plan 29-01's boot test.

**D-16 is flagged for developer review.** CONTEXT.md auto-selected acceptance of the overhead failure
plus a re-scoped follow-up bar. The alternative — optimise first — would make this an X-03 production
change and halt the phase. The rating and the reversal cost are recorded on Task 2 so the developer can
overturn it at plan review rather than discovering it at seal time.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-program-gates-release/29-CONTEXT.md
@.planning/phases/29-program-gates-release/29-RESEARCH.md
@.planning/phases/29-program-gates-release/29-PATTERNS.md
@.planning/phases/29-program-gates-release/29-04-SUMMARY.md
@.planning/phases/29-program-gates-release/29-05-SUMMARY.md
@.project/v0.10.0/08-traceability-matrix.md
@CLAUDE.md

<interfaces>
Measured against HEAD in this planning session:

- Doc-08 protocol steps 6-9 (`.project/v0.10.0/08-traceability-matrix.md:103-107`): (6) the
  compatibility audit — `cargo semver-checks` output plus a manual public-API diff, every
  enum-variant/struct-field/trait-method change to a pre-existing type having a §9.2 row with a stated
  mitigation, every allowlist entry corresponding to a deliberate-breaking `Y` row, any unregistered
  change a finding, and no pre-existing public trait gaining a required method; (7) the
  behavioral-change audit — §9.1 holds M-B-01…03 and nothing else, or any additional entry was raised
  as a stop-and-flag with a recorded decision; (8) the toolchain audit — MSRV job green with
  `--all-features`, `rust-version`/README badge/§9.3 agreement, `cargo tree -e features` showing no new
  heavyweight default dependency, `cargo deny`/`cargo audit` green; (9) the config-compat test and the
  `openapi.json` golden diff both existing and passing.
- The public-API snapshot: `.project/current-exports.txt` is committed at HEAD (7901 lines; Phase 28's
  17th plan regenerated it and recorded 3936 items). `git show v0.9.0:.project/current-exports.txt`
  exists and is 3550 lines. That pair IS doc-08 step 6's "manual `cargo public-api`-style diff" — no
  new tool (D-05's rejection of adopting `cargo public-api` as a first-class dependency).
  `scripts/extract-public-api.sh` regenerates the snapshot but needs `cargo-public-api` installed and a
  nightly toolchain; `scripts/check-api-surface.sh` is the drift guard, and the `api-surface` CI job
  keeps the committed file honest on nightly. If `cargo-public-api` is unavailable in the execution
  environment, the committed snapshot is used as current on the strength of that job's most recent
  green run, and the audit says so with the run cited — it does not silently assume currency.
- M-B-04 provenance: §9.1's fourth row was added by `ENG-08` in Phase 22. Doc-08 step 7's wording
  allows an additional entry only if it "was raised as a stop-and-flag item with a recorded decision",
  so section 7 cites the Phase 22 CONTEXT/plan decision that added it and asks the maintainer to
  countersign (D-17) rather than declaring the clause satisfied unilaterally.
- The bench measurement: `.planning/phases/28-observability-tooling/28-BENCH-EVIDENCE.md` records
  `log_sink` +22.18% and `composite` +18.46% against a ~110µs untraced baseline, versus PRD 07
  acceptance 6's ≤3% bar. `28-VERIFICATION.md`'s frontmatter `human_verification` item 1 asks a
  maintainer to choose (a) accept, (b) re-scope to an I/O-bound superstep, or (c) optimise first;
  CONTEXT.md D-16 auto-selected (a) now plus (b) as the recorded follow-up.
  `.planning/STATE.md` additionally records that Phase 28's D-37 was "accepted by maintainer sign-off
  at close-out UAT (2026-09-09)" — cite that, so the audit does not re-ask a question already answered
  once.
- `docs/src/operations/observability.md` ALREADY states the figures honestly, at lines 182-187 under
  `## Known limitations`: the ≤3% bar, `log_sink` +22.18%, `composite` +18.46%, the ~110µs baseline,
  and the words "both genuinely fail the ≤3% bar". D-16's requirement that "the audit confirms the
  number is stated" is therefore a confirmation, not an addition; what is missing is the v0.10.0
  disposition, which this plan adds as one sentence.
- `28-VERIFICATION.md`'s `human_verification` item 2 is the six judgment-tier safety/privacy
  prohibitions, LLM-verified non-authoritatively. Read that frontmatter for the six statements
  verbatim; do not paraphrase them into new wording.
- The `cargo doc` condition, measured live during research: `cargo doc --workspace --no-deps` emits
  **72** `warning:` lines against default features — the exact command the `lint` job's "Check
  documentation" step runs with zero tolerance (`.github/workflows/ci.yml:62-63`) — and 84 against
  `--all-features`. STATE.md's Phase 26 carried concern recorded roughly 60. SHIP-04's requirement text
  asks only that this phase introduce no NEW broken links and that the semver and MSRV jobs be green,
  so this is recorded as a carried condition, not fixed here (D-25 does not list it). Re-measure at
  execution time and record the observed count, not the planning-time one.
- `29-05-SUMMARY.md` carries the recorded MSRV re-verification (README badge, prerequisites line,
  §9.3 all at 1.88) that section 8 cites rather than re-deriving.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill audit section 6 — the compatibility audit, row by row, against semver-checks and the v0.9.0 public-API diff</name>
  <files>.project/v0.10.0/09-program-acceptance-audit.md</files>
  <read_first>
    - `.project/v0.10.0/09-program-acceptance-audit.md` as plans 29-04 and its own Task ordering left
      it — the ten-section skeleton and the per-FR table.
    - `MIGRATION.md` §9.2 in full (lines 159-268) — all 26 data rows, the deliberate-breaking column,
      and the eight "deliberate zero" notes naming types absent at v0.9.0.
    - `.cargo/semver-checks-allowlist.toml` — all nine `[[entry]]` blocks with their `migration_row`
      fields and the lint each suppresses.
    - `crates/paladin-core/Cargo.toml:64-67`, `crates/paladin-ports/Cargo.toml:45-47`,
      `crates/paladin-web/Cargo.toml:75-77`, `Cargo.toml:96-97` — the per-crate
      `[package.metadata.cargo-semver-checks.lints]` suppressions that actually silence the tool, which
      is what section 6 must reconcile the register against.
    - `.project/current-exports.txt` (HEAD) and `git show v0.9.0:.project/current-exports.txt` — the
      two snapshots to diff. Read the diff, not the whole files.
    - `.planning/phases/26-agent-runtime-enhancements/26-21-SUMMARY.md` lines 162-181 — the last full
      semver-checks sweep record (11/11, "assume minor"), the form to repeat.
    - `.planning/decisions/0048-paladin-eval-composition-crate.md` — why `paladin-eval` is outside the
      semver diff and inside the publish order. Check the reasoning against this ADR; do not re-derive
      it.
  </read_first>
  <action>
    Run `cargo semver-checks check-release --package <pkg> --default-features --baseline-version 0.9.0`
    for the eleven baseline crates the `semver` job lists and record each result verbatim in the form
    `26-21-SUMMARY.md` uses. State that `paladin-eval` is excluded and cite ADR-0048 for why (no
    published `0.9.0` baseline to diff against), rather than presenting the exclusion as an oversight.

    Produce the public-API diff: `diff` the committed `.project/current-exports.txt` against
    `git show v0.9.0:.project/current-exports.txt`. If `cargo-public-api` and a nightly toolchain are
    available, first regenerate with `scripts/extract-public-api.sh` and run
    `scripts/check-api-surface.sh` to confirm the committed snapshot is current, recording both results.
    If they are not available, state that plainly and cite the `api-surface` CI job's most recent green
    run as the basis for treating the committed snapshot as current — never assume currency silently.

    Write section 6 as three sub-parts. First, a per-row pass over §9.2's 26 data rows: for each row
    state whether it names a pre-existing type or a new-in-0.10 type, and for pre-existing types whether
    the observed signature change appears in the diff and whether the row states a mitigation. Second,
    the reverse direction: every pre-existing item whose signature changed in the diff must map to a
    §9.2 row — list any that does not as a finding. Third, confirm each of the eight deliberate-zero
    notes by the named type's absence from the v0.9.0 export, showing the grep that proves absence.
    Then state explicitly whether any pre-existing public trait gained a required method, with the
    check used — doc-08 step 6 asks for that specific conclusion and it must appear as its own
    sentence, not be implied by the rows.

    Record the row-count facts as measured, not as CONTEXT.md stated them: §9.2 has 26 data rows (not
    28), and its `Y` markers appear on ten rows that dedupe to nine distinct `crate | type` pairs
    because `paladin-ai | Settings` is deliberately marked twice. Cite plan 29-03's row-level CI gate as
    the durable enforcement of the set equality this section verifies once.

    Set section 6's `Verdict:` from what was observed. Any unregistered change, any row without a
    mitigation, or any trait that gained a required method is a finding with a proposed disposition. If
    closing a finding would need a production code change, stop and return a checkpoint per D-12.
  </action>
  <acceptance_criteria>
    - `cargo semver-checks check-release --package paladin-ai-core --default-features --baseline-version 0.9.0` exits 0, and the audit records a result line for each of the eleven baseline crates: `awk '/^## 6/,/^## 7/' .project/v0.10.0/09-program-acceptance-audit.md | grep -cE 'paladin-(ai|ai-core|ports|battalion|herald|llm|memory|storage|notifications|content|web)'` is at least 11.
    - `grep -c 'ADR-0048' .project/v0.10.0/09-program-acceptance-audit.md` is at least 1 — `paladin-eval`'s exclusion is cited, not unexplained.
    - `grep -c 'current-exports.txt' .project/v0.10.0/09-program-acceptance-audit.md` is at least 1 and the section names both sides of the diff (HEAD and `v0.9.0`).
    - The section records 26 data rows and nine distinct pairs: `awk '/^## 6/,/^## 7/' .project/v0.10.0/09-program-acceptance-audit.md | grep -c '26'` is at least 1 and the same range mentions nine pairs.
    - `awk '/^## 6/,/^## 7/' .project/v0.10.0/09-program-acceptance-audit.md | grep -ci 'required method'` is at least 1 — doc-08 step 6's trait conclusion is stated as its own sentence.
    - `awk '/^## 6/,/^## 7/' .project/v0.10.0/09-program-acceptance-audit.md | grep -c 'Verdict: pending'` is 0.
    - No `.rs` file changed: `git diff --name-only HEAD~1 | grep -c '\.rs$'` is 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -euo pipefail; A=.project/v0.10.0/09-program-acceptance-audit.md; S=$(awk "/^## 6/,/^## 7/" "$A"); printf "%s" "$S" | grep -q "current-exports.txt"; printf "%s" "$S" | grep -qi "required method"; test "$(printf "%s" "$S" | grep -c "Verdict: pending")" -eq 0; grep -q "ADR-0048" "$A"; cargo semver-checks check-release --package paladin-ai-core --default-features --baseline-version 0.9.0 >/dev/null'</automated>
  </verify>
  <done>Every §9.2 row is reconciled in both directions against semver-checks and a real v0.9.0-to-HEAD public-API diff, the eight deliberate zeros are proven by absence, and the trait-method question is answered in its own sentence.</done>
</task>

<task type="auto">
  <name>Task 2: Fill audit sections 7, 8 and 9, record the accepted overhead deviation, and add its disposition to the observability docs</name>
  <reversibility rating="costly">Accepting the +22.18%/+18.46% tracing overhead as a documented v0.10.0 deviation is recorded in the audit, the published docs, the changelog and the defect register together; reversing it means choosing to optimise first, which is an X-03 production change that halts this phase and unwinds four artefacts in step.</reversibility>
  <files>.project/v0.10.0/09-program-acceptance-audit.md, docs/src/operations/observability.md</files>
  <read_first>
    - `.project/v0.10.0/09-program-acceptance-audit.md` as Task 1 left it.
    - `MIGRATION.md` §9.1 in full — the four M-B rows, to confirm the set is exactly M-B-01…04.
    - `.planning/phases/22-*/22-CONTEXT.md` and the Phase 22 plan that added M-B-04 under `ENG-08` —
      the recorded decision section 7 cites for the fourth entry.
    - `.project/v0.10.0/00-program-overview.md` §7 — the BUG-01…04 classification section 7 leans on.
    - `.planning/phases/28-observability-tooling/28-VERIFICATION.md` frontmatter — both
      `human_verification` items, especially the six judgment-tier prohibition statements, verbatim.
    - `.planning/phases/28-observability-tooling/28-BENCH-EVIDENCE.md` — the measurement, the baseline,
      and the benchmark's own shape (all-Function-node), which is the reason the deviation is defensible.
    - `.planning/STATE.md` Phase 28 decisions — the D-37 line recording maintainer sign-off at
      close-out UAT on 2026-09-09.
    - `docs/src/operations/observability.md` lines 175-200 — the existing `## Known limitations`
      section and the overhead bullet as it stands.
    - `.planning/phases/29-program-gates-release/29-05-SUMMARY.md` — the recorded MSRV re-verification
      section 8 cites.
    - `.github/workflows/ci.yml` lines 55-70 (the `lint` job's "Check documentation" step) and 245-260
      (the `msrv` job) — the two conditions section 8 records.
  </read_first>
  <action>
    Fill section 7: confirm `MIGRATION.md` §9.1 holds exactly M-B-01, M-B-02, M-B-03 and M-B-04 and no
    other entry, quoting each row's identifier and one-line summary. For M-B-04, cite the Phase 22
    `ENG-08` decision that added it and state that doc-08 step 7's clause is satisfied by that recorded
    decision — then place the countersignature in the maintainer sign-off section rather than declaring
    the clause closed on the executor's own authority.

    Fill section 8: record the `msrv` job's toolchain and result (the job runs
    `cargo +1.88 check --workspace --all-features --all-targets`; run it locally and record the
    result, or cite the CI run if the toolchain is unavailable). Cite `29-05-SUMMARY.md` for the
    three-way MSRV agreement at 1.88 rather than re-deriving it. Run `cargo tree -e features` for
    default features and record that no new heavyweight dependency entered the default set, naming what
    was compared against. Run `cargo deny check` and `cargo audit` and record their exact outputs
    including any allowed-warning counts. Then record the `cargo doc --workspace --no-deps` condition:
    re-measure the `warning:` line count under default features, record the observed number, state that
    it is the exact command the `lint` job's "Check documentation" step runs with zero tolerance,
    state that it is pre-existing and growing (roughly 60 at Phase 26, 72 measured during this phase's
    research), and state that SHIP-04's requirement text asks only for no NEW broken intra-doc links
    plus green semver and MSRV jobs — so this is a carried condition, named and tracked, not a Phase 29
    fix. Do not present the `lint` job as green when it is not.

    Fill section 9: record both SHIP-02 proofs as existing and passing, citing `v0_9_config_boot`
    (path and observed pass count) and `crates/paladin-web/tests/openapi_golden_v0_9.rs` (path and
    observed pass count), plus the CI steps that now gate them (the `e2e-platform-api` job step plan
    29-05 added, and the per-crate matrix job for `paladin-web`).

    Record the overhead deviation as its own clearly-headed subsection: the bar (PRD 07 acceptance 6,
    ≤3% superstep overhead with tracing on), the measurement (`log_sink` +22.18%, `composite` +18.46%
    against a ~110µs untraced baseline, `28-BENCH-EVIDENCE.md`), the disposition (ACCEPTED for v0.10.0
    as a documented deviation), the reason (tracing sinks are opt-in so no sink means no overhead,
    `trace.state_values` defaults off, and the microbenchmark is all-Function-node with no LLM latency
    to amortise against — so no v0.9 workflow and no default v0.10 workflow pays it), the follow-up
    (the bar re-scoped to an I/O-bound superstep), and the sign-off trail (28-VERIFICATION item 1's
    three options, and STATE.md's record that the maintainer accepted it at Phase 28 close-out UAT on
    2026-09-09). Name the three artefacts that carry the same record elsewhere so a reader can check
    consistency: the published docs bullet (this task), the root changelog's Known limitations entry
    (plan 29-09), and the defect-register row (plan 29-08).

    Add the maintainer sign-off section: the six judgment-tier safety/privacy prohibitions from
    `28-VERIFICATION.md` item 2, quoted verbatim, and the M-B-04 countersignature, each as an unchecked
    `- [ ]` box with its evidence beside it. Leave every box unticked. Do not add a box for anything
    that has a mechanical check — this section is for items where an agent's verdict is
    non-authoritative by design.

    In `docs/src/operations/observability.md`, extend the existing overhead bullet under
    `## Known limitations` with one sentence stating the v0.10.0 disposition: accepted as a documented
    deviation for this release, with the bar re-scoped to an I/O-bound superstep as the follow-up, and
    the reason a default deployment does not pay it. Do not soften or restate the measured figures —
    they are already stated correctly and honestly, and the audit's job here is to confirm that.

    Update `29-ACCEPTANCE-AUDIT.md`'s verdict line to reflect sections 1-9 complete with section 10
    pending (plan 29-09).
  </action>
  <acceptance_criteria>
    - All four behavioral changes are confirmed in section 7: `awk '/^## 7/,/^## 8/' .project/v0.10.0/09-program-acceptance-audit.md | grep -cE 'M-B-0[1-4]'` is at least 4, and the same range cites `ENG-08`.
    - `awk '/^## 8/,/^## 9/' .project/v0.10.0/09-program-acceptance-audit.md | grep -c '1.88'` is at least 1 and the same range records `cargo deny` and `cargo audit` results.
    - The `cargo doc` condition is recorded with a measured count: `grep -cE 'cargo doc --workspace --no-deps' .project/v0.10.0/09-program-acceptance-audit.md` is at least 1 and the audit states a `warning:` line count.
    - `awk '/^## 9/,/^## 10/' .project/v0.10.0/09-program-acceptance-audit.md | grep -c 'v0_9_config_boot'` is at least 1 and the same range names `openapi_golden_v0_9`.
    - The deviation record carries both figures: `grep -c '22.18' .project/v0.10.0/09-program-acceptance-audit.md` is at least 1 and `grep -c '18.46' .project/v0.10.0/09-program-acceptance-audit.md` is at least 1.
    - The sign-off section has exactly seven unchecked boxes and zero checked ones: `grep -c '^- \[ \]' .project/v0.10.0/09-program-acceptance-audit.md` is 7 and `grep -c '^- \[x\]' .project/v0.10.0/09-program-acceptance-audit.md` is 0.
    - Sections 7, 8 and 9 are no longer pending: `awk '/^## 7/,/^## 10/' .project/v0.10.0/09-program-acceptance-audit.md | grep -c 'Verdict: pending'` is 0.
    - `docs/src/operations/observability.md` keeps its figures and gains the disposition: `grep -c '22.18' docs/src/operations/observability.md` is at least 1, `grep -c '18.46' docs/src/operations/observability.md` is at least 1, and `grep -ci 'accepted' docs/src/operations/observability.md` is at least 1.
    - `mdbook build docs/` exits 0.
    - No `.rs` file changed: `git diff --name-only HEAD~1 | grep -c '\.rs$'` is 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -euo pipefail; A=.project/v0.10.0/09-program-acceptance-audit.md; test "$(awk "/^## 7/,/^## 8/" "$A" | grep -cE "M-B-0[1-4]")" -ge 4; grep -q 22.18 "$A"; grep -q 18.46 "$A"; test "$(grep -c "^- \[ \]" "$A")" -eq 7; test "$(grep -c "^- \[x\]" "$A")" -eq 0; test "$(awk "/^## 7/,/^## 10/" "$A" | grep -c "Verdict: pending")" -eq 0; grep -q 22.18 docs/src/operations/observability.md; grep -qi accepted docs/src/operations/observability.md; mdbook build docs/ >/dev/null; test "$(git diff --name-only HEAD~1 | grep -c "\.rs\$")" -eq 0'</automated>
  </verify>
  <done>Sections 7 through 9 are confirmed with citations, the overhead failure is recorded as an accepted and tracked deviation in both the audit and the published docs, and the seven items only a human can sign off sit unticked waiting for the UAT step.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| v0.9.0 public API → v0.10.0 public API | An unregistered signature change breaks a downstream crate at compile time with no migration note. |
| Agent verdict → maintainer sign-off | Judgment-tier safety and privacy items are exactly where an agent's confident-sounding verdict is least trustworthy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-07-01 | Tampering | A public-API change with no §9.2 row and no allowlist entry | high | mitigate | Section 6 reconciles in both directions against a real v0.9.0-to-HEAD export diff plus per-crate semver-checks output; plan 29-03's row-level CI gate makes the set equality durable. |
| T-29-07-02 | Repudiation | An agent ticking a judgment-tier safety or privacy prohibition | high | mitigate | All seven boxes are authored unchecked with evidence beside each; an acceptance criterion asserts zero checked boxes, and the prohibition block states the rule. |
| T-29-07-03 | Repudiation | Recording the `lint` job as green when its documentation step is red | medium | mitigate | Section 8 records the re-measured `warning:` count, names the exact command, and states that SHIP-04's text does not require it to be zero — the condition is tracked rather than papered over. |
| T-29-07-04 | Information disclosure | The audit quoting a credential while citing `cargo audit`/`cargo deny` output | low | mitigate | Recorded output is advisory identifiers and counts; no `.env`, no config value and no response body is quoted, per the security instructions' credential-handling rule. |
| T-29-07-05 | Denial of service | Accepting the tracing overhead without recording that a default deployment does not pay it | medium | accept | Accepted with the reason recorded in four places (audit, published docs, changelog, defect register); tracing sinks are opt-in and `trace.state_values` defaults off, so the exposure is bounded to deployments that deliberately enable a sink. |
</threat_model>

<verification>
- `cargo semver-checks check-release` green for all eleven baseline crates, recorded verbatim.
- `cargo deny check` and `cargo audit` run and recorded with their exact outputs.
- The audit has zero `Verdict: pending` lines in sections 1-9 and exactly seven unchecked sign-off
  boxes with none ticked.
- `mdbook build docs/` exits 0 after the observability edit.
- No `.rs` file is modified by any task in this plan.
</verification>

<success_criteria>
- SHIP-03's ten-step protocol is complete through step 9, with every verdict backed by a command or a
  cited artefact.
- The two things a human must decide are visible and unticked rather than silently resolved: the
  overhead deviation's acceptance trail and the six judgment-tier prohibitions.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

See the identical section in `29-01-PLAN.md` — the canonical list for Phase 29. This plan's own
contribution: audit sections 6, 7, 8 and 9 of
`.project/v0.10.0/09-program-acceptance-audit.md`, its accepted-deviation subsection and its
seven-item maintainer sign-off checklist, and the v0.10.0 disposition sentence in
`docs/src/operations/observability.md`'s Known limitations bullet. No Rust file, no new symbol, no
dependency, no behavior change.
</artifacts_produced>

<output>
Create `.planning/phases/29-program-gates-release/29-07-SUMMARY.md` when done.
</output>
