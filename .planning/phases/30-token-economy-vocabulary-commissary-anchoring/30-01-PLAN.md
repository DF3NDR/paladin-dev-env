---
phase: 30-token-economy-vocabulary-commissary-anchoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/decisions/0049-commissary-design-and-rename.md
  - .planning/decisions/PROMOTION.md
  - .planning/PROJECT.md
  - docs/src/architecture/domain-model.md
  - .github/copilot-instructions.md
  - docs/src/architecture/commissary.md
  - docs/src/SUMMARY.md
autonomous: true
requirements: [VOCAB-01, VOCAB-02, VOCAB-03]

must_haves:
  truths:
    # --- goal-backward (plan objective) ---
    - "A reader of .planning/decisions/ finds one numbered, Accepted, 2026-09-14 ADR (0049) that states what the Commissary does (verify_fits guard + dispense allocator, fail-loud / never-silent), why the capability is no longer called by its retired name, and which candidate names were rejected and why."
    - "A reader of the published mdBook reaches a Commissary page from the Architecture nav and sees only API that crates/paladin-llm/src/services/commissary.rs ships today."
    - "A developer reading any of the three ubiquitous-language lists sees Commissary and the plain-vs-Medieval vocabulary rule (units/measures and technical port traits keep plain industry names; domain roles, places and events get Medieval-Military names)."
    - "mdbook build docs/ exits 0 after the edits, with linkcheck warning-policy = error and no broken links."
    # --- edge-probe lift: VOCAB-01 ---
    - "[edge:VOCAB-01/adjacency] Each of the three vocabulary lists carries exactly one Commissary entry and none contradicts the others: grep -c on the bolded term returns 1 in docs/src/architecture/domain-model.md and 1 in .github/copilot-instructions.md, and .planning/PROJECT.md's Ubiquitous language bullet names Commissary exactly once."
    - "[edge:VOCAB-01/empty] The reserved term is named in prose only and holds no enumerated list row: no table row whose first cell is the bolded reserved officer name exists in docs/src/architecture/domain-model.md or .github/copilot-instructions.md."
    - "[edge:VOCAB-01/encoding] The term is spelled byte-exactly Commissary (capital C, singular) everywhere it is introduced: for each of the three list files the case-sensitive count of Commissary equals the case-insensitive count, so no lowercase or variant spelling exists."
    - "[edge:VOCAB-01/ordering] Adding the term reorders nothing: git diff on docs/src/architecture/domain-model.md and .github/copilot-instructions.md shows added lines only, with zero removals of pre-existing table rows."
    # --- edge-probe lift: VOCAB-02 ---
    - "[edge:VOCAB-02/adjacency] ADR numbers do not collide: ls .planning/decisions/0049-*.md matches exactly one file, the pre-existing in-tree .planning/decisions/0010-milestone-3-epic-numbering.md is unmodified, and ADR-0049 refers to the abandoned-branch record only by its full git show origin/feature/quartermaster-prompt-budgeting path, never as a bare in-tree ADR number."
    - "[edge:VOCAB-02/empty] The rejected-name list is non-empty and complete: ADR-0049 carries all nine rejected candidates (Quartermaster, Convoy, apportion, Provisioner, ProvisioningPlan, Muster, provision(), ContextRation, Allocation), each on its own bullet line with a reason; no bullet is empty."
    - "[edge:VOCAB-02/encoding] Structured entries parse: every content line under ADR-0049's ## Considered Options and ## Code Locations begins with a bullet or numbered marker, never a prose paragraph, per PROMOTION.md's adr-parser.cjs splitEntries contract."
    - "[edge:VOCAB-02/ordering] The seven required H2 headings appear exactly once each, in the prescribed order: Status, Context, Decision, Considered Options, Code Locations, Code Conformance, Downstream Consumers."
  artifacts:
    - ".planning/decisions/0049-commissary-design-and-rename.md (new)"
    - "docs/src/architecture/commissary.md (new)"
    - ".planning/decisions/PROMOTION.md (modified — one index row for 0049, Next free ADR number advanced to 0050, one dated note)"
    - ".planning/PROJECT.md (modified — one Key Decisions row linking ADR-0049; Commissary added to the Constraints Ubiquitous language bullet)"
    - "docs/src/architecture/domain-model.md (modified — vocabulary-rule prose + one Commissary row)"
    - ".github/copilot-instructions.md (modified — one Commissary row in the Naming Convention table)"
    - "docs/src/SUMMARY.md (modified — one nav line under # Architecture)"
  key_links:
    - "docs/src/SUMMARY.md -> architecture/commissary.md — if the nav line is missing or misspelled the page is orphaned and mdbook-linkcheck (warning-policy = error) fails the docs.yml required check."
    - ".planning/PROJECT.md Key Decisions row -> .planning/decisions/0049-commissary-design-and-rename.md — a mistyped relative path leaves the table pointing at nothing."
    - ".planning/decisions/PROMOTION.md Next free ADR number line -> the 0049 file — if the index and the directory disagree the next phase allocates a colliding number."
    - "ADR-0049 ## Code Locations -> crates/paladin-llm/src/services/commissary.rs and the abandoned-branch ADR path — the provenance chain that lets a reader trace the record back to shipped code and to history."
    - "The three vocabulary lists -> each other — none is generated from another, so each edit is an independent diff that must not contradict the other two."
  prohibitions:
    # canon-referral drop: no OWASP / injection / path-traversal / GDPR prohibitions minted —
    # this plan writes prose only, parses no untrusted input and opens no attack surface
    # (30-RESEARCH.md Security Domain).
    - requirement_id: VOCAB-02
      category: transparency
      statement: "ADR-0049 must not present the abandoned-branch prompt-context-budgeting record as a live, in-tree decision: it is cited as history by its full git show branch path, never copied wholesale and never renumbered into the in-tree ADR series (the in-tree 0010 is a different, unrelated decision)."
      status: unresolved
      verification: null
    - requirement_id: VOCAB-03
      category: transparency
      statement: "The mdBook Commissary page must not document any API that crates/paladin-llm/src/services/commissary.rs does not ship today — no invented constructor, builder or convenience method; a reader must not be able to follow the page into a compile error."
      status: unresolved
      verification: null
---

<objective>
Anchor `Commissary` — write its decision record, put the token-economy vocabulary rule where
developers and readers will find it, and give the shipped service a documentation home reachable
from the published architecture nav.

Purpose: `crates/paladin-llm/src/services/commissary.rs` ships today with a facade export, no
in-tree caller, no docs page and a design ADR that exists only on an abandoned branch (milestone
finding F4). This plan closes the documentation half of that gap and freezes the vocabulary rule
the rest of Milestone 13 depends on.

Output: ADR-0049, a new mdBook page, and `Commissary` present in all three ubiquitous-language
lists, with `mdbook build docs/` green.

**Tracer:** Task 1 is this phase's single `type="tracer"` slice (one tracer per phase, per
`references/planner-mvp-mode.md` — plans 30-02 and 30-03 are expansion and carry no tracer of their
own). It carries one term, `Commissary`, end-to-end through every layer this phase touches:
decision record → planning index (`PROMOTION.md`) → planning corpus (`PROJECT.md`) → published
docs (`domain-model.md`) → developer instructions (`copilot-instructions.md`) → the real build
gate (`mdbook build docs/`). If that path works, every remaining artifact in Phase 30 is the same
path again with different content.

**CONTEXT.md decisions honored** (`30-CONTEXT.md` `<decisions>`, locked via the PRD express path): D-01, D-02, D-03, D-04, D-05, D-06, D-17, D-18.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-token-economy-vocabulary-commissary-anchoring/30-CONTEXT.md
@.planning/phases/30-token-economy-vocabulary-commissary-anchoring/30-RESEARCH.md
@.planning/phases/30-token-economy-vocabulary-commissary-anchoring/30-PATTERNS.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Phase 30 is documentation-only. The complete artifact set across all three plans:

| Artifact | Kind | Plan |
|---|---|---|
| `.planning/decisions/0049-commissary-design-and-rename.md` | new ADR | 30-01 |
| `.planning/decisions/0050-treasurer-reservation.md` | new ADR | 30-02 |
| `.planning/decisions/0051-token-economy-versioning-x03-supersession.md` | new ADR | 30-02 |
| `docs/src/architecture/commissary.md` | new mdBook page | 30-01 |
| `docs/src/SUMMARY.md` | one new nav line under `# Architecture` | 30-01 |
| `docs/src/architecture/domain-model.md` | vocabulary-rule prose + one table row | 30-01 |
| `.github/copilot-instructions.md` | one Naming Convention table row | 30-01 |
| `.planning/PROJECT.md` | Ubiquitous-language bullet edit + three Key Decisions rows | 30-01 (1 row), 30-02 (2 rows) |
| `.planning/decisions/PROMOTION.md` | three index rows, `Next free ADR number` advanced 0049 → 0052, dated notes | 30-01, 30-02 |
| `docs/src/getting-started/configuration.md` | one new `## Token Budget Terminology` section | 30-03 |
| `crates/paladin-core/src/platform/container/herald.rs` | rustdoc text only on the `cost_estimate` field, accessor, builder and example | 30-03 |
| `src/lib.rs` | one provenance comment reworded | 30-03 |
| `.project/project-management/paladin-project-plan-final.md` | one historical annotation | 30-03 |

**No new Rust symbols are created by this phase.** No public type, field, accessor, builder
method, trait, config key or function signature is added, removed or renamed anywhere. There is
therefore **no `MIGRATION.md` §9.2 entry and no `cargo semver-checks` allowlist change** in
Phase 30 — stated here deliberately, because every other v0.10.0 phase has had one.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: Commissary end-to-end — ADR-0049, the vocabulary rule, and all three lists</name>
  <reversibility rating="reversible">An ADR is superseded by a later ADR and a doc row is one line; nothing here breaks a contract another system reads.</reversibility>
  <precondition>`git rev-parse --verify origin/feature/quartermaster-prompt-budgeting` succeeds — the abandoned branch's remote-tracking ref must be present to read the historical record via `git show`; if it is absent, run `git fetch origin feature/quartermaster-prompt-budgeting` first. Do NOT check the branch out.</precondition>
  <files>.planning/decisions/0049-commissary-design-and-rename.md, .planning/decisions/PROMOTION.md, .planning/PROJECT.md, docs/src/architecture/domain-model.md, .github/copilot-instructions.md</files>
  <read_first>
    - `.planning/phases/30-token-economy-vocabulary-commissary-anchoring/30-PATTERNS.md` — the exact heading skeleton, row shapes and numbering procedure for every file in this task
    - `.planning/phases/30-token-economy-vocabulary-commissary-anchoring/30-RESEARCH.md` Pattern 1, Pattern 2 and Pattern 4 — the verified ADR source material and the current state of all three lists
    - `.planning/decisions/0048-paladin-eval-composition-crate.md` — the heading shape to copy
    - `.planning/decisions/PROMOTION.md` lines 20-120 (index table, `Next free ADR number`, the dated-note precedents) and lines 205-270 (required heading set, bullet-list contract, Part A procedure)
    - `crates/paladin-llm/src/services/commissary.rs` — module docs (lines 1-60), the public model types (lines 61-300), and `dispense` / `verify_fits` (lines ~440-600). Do not read the test module in this task.
    - `git show origin/feature/quartermaster-prompt-budgeting:.planning/decisions/0010-prompt-context-budgeting.md` — the historical record (168 lines), read via `git show`, never a checkout
    - `git show -s --format=%B 348f5910` and `git show -s --format=%B 35fd8390` — the port commits' own avoided-name lists
    - `docs/src/architecture/domain-model.md` lines 1-35 — the Naming Convention table being extended
    - `.github/copilot-instructions.md` lines 19-40 — the Naming Convention table being extended
    - `.planning/PROJECT.md` lines 1230-1245 (the Constraints "Ubiquitous language" bullet) and lines 1308-1325 (the Key Decisions table header comment and row shape)
  </read_first>
  <action>
Implements CONTEXT.md decisions **D-1** (the vocabulary rule → VOCAB-01) and **D-2** (Commissary anchoring, the ADR half → VOCAB-02).

Write `.planning/decisions/0049-commissary-design-and-rename.md` with title `# ADR-0049: Commissary design, rename rationale, and rejected names`, `## Status` body `Accepted`, `**Date:** 2026-09-14`, and exactly the seven required H2 headings in PROMOTION.md's order: Status, Context, Decision, Considered Options, Code Locations, Code Conformance, Downstream Consumers. `## Considered Options` and `## Code Locations` must be bulleted lists (one physical line per bullet) — a prose paragraph collapses into one opaque blob in `adr-parser.cjs`. `## Code Conformance` reads `conforms`: the ADR records already-shipped state and instructs no code change.

Content to carry, reconstructed (never copied wholesale) from the sources listed in this task's read-first block:
- **Context/Decision:** the two deliberately separate responsibilities — `verify_fits`, a pre-flight GUARD that measures an already-assembled prompt against the provider's declared window minus the caller's reserved completion budget and returns `CommissaryError::ContextOverflow { measured_tokens, allotted_tokens, provider }` rather than trimming; and `dispense`, a bounded ALLOCATOR over fixed material plus a caller-prioritised `Consignment`, returning a `Stockpile` whose retained items are clamped with a visible truncation marker and whose shed items are each recorded in `Stockpile.shed`. State the fail-loud / never-silent stance and name the anti-pattern it rejects: `RagRetrievalService::truncate_to_token_budget`, which drops lowest-scoring items with no marker and no record. State the honesty clause: `TokenCounterPort::count` is infallible and carries no exactness signal, so `is_exact_counter` is an explicit constructor parameter threaded into `Stockpile.exact_tally`. State the refusal-to-invent-a-window rule: construction fails with `UndeclaredContextWindow` unless a window is declared or a fallback is supplied explicitly.
- **Decision (rename):** the capability was re-ported to v0.10.0 under new vocabulary; per D-01/D-02 the port picked verified-free Medieval-Military names confirmed 0/0 by grep across both repos. Cite port commits `348f5910` and `35fd8390` by SHA.
- **Considered Options (the rejected-name list, nine bullets, one reason each):** `Quartermaster` (retired with the abandoned branch; the term is not reintroduced), `Convoy`, `apportion`, `Provisioner`, `ProvisioningPlan`, `Muster`/`muster` (`Muster` collides with the framework's own troop-assembly vocabulary — the `paladin muster` CLI command and `Commands::Muster`), `provision()` (collides with `SandboxPort::provision`, a downstream container-lifecycle method), `ContextRation` and `Allocation` (collide with live downstream domain vocabulary — `allocation` is the downstream Prover's on-chain entitlement invariant). Mark which came from the historical record and which from the port commits.
- **Code Locations (bulleted):** `crates/paladin-llm/src/services/commissary.rs` (the shipped service: `Commissary::new`, `Commissary::from_port`, `Commissary::verify_fits`, `Commissary::dispense`, `Commissary::allotted_tokens`; types `CommissaryPlan`, `Consignment`, `ConsignmentItem`, `DispensedItem`, `ShedItem`, `Stockpile`, `CommissaryError`); `src/lib.rs` (the unconditional facade re-export block); `crates/paladin-ports/src/output/token_counter_port.rs`; and the historical record cited as the runnable command `git show origin/feature/quartermaster-prompt-budgeting:.planning/decisions/0010-prompt-context-budgeting.md` — write the full branch-qualified path so no reader confuses it with the unrelated in-tree ADR of the same number.
- **Downstream Consumers:** Phase 33 (in-tree adoption, COMM-01..03), the new mdBook page, and the downstream Web3 Security Paladin app.
- Write **v0.10.0** as the version identity throughout; never `v0.11.0`.

Then, in the same task:
1. `.planning/decisions/PROMOTION.md` — append one index-table row `| 0049 | `commissary-design-and-rename` | <one-line subject> (Phase 30, plan 30-01) |` immediately after the 0048 row; change the `**Next free ADR number: 0049**` line to `**Next free ADR number: 0050**`; and insert one dated note directly beneath it, dated 2026-09-14, in the style of the 2026-09-09 plan 28-17 note, stating that the line advances by one and that `ls .planning/decisions/0049-*.md` was re-run and confirms the file exists with the expected number, none skipped or reused, and no existing index row renumbered, reworded or reordered. Edit only the index row, the `Next free ADR number` line and the note — a prior phase recorded that editing PROMOTION.md's Part A procedural prose is an unauthorized fourth edit, so leave it alone.
2. `.planning/PROJECT.md` — append one `## Key Decisions` row below the current last row, in the exact shape `| [<title>](.planning/decisions/0049-commissary-design-and-rename.md) (ADR-0049) | <one-sentence rationale citing concrete file evidence> | conforms |`. Preserve the `<!-- LOCKED DECISIONS ... -->` header comment untouched. Separately, in the `## Constraints` "Ubiquitous language" bullet, add `Commissary` to the enumerated term list and state the rule in the same bullet (or the sentence immediately after it): units and measures (`TokenUsage`, `max_tokens`, `max_context_tokens`, `TokenBudget`) and technical port traits (`TokenCounterPort`, `LlmPort`, `EmbeddingPort`) keep plain industry names; domain roles, places and events get Medieval-Military names. The reserved officer name for spend governance is named in the surrounding prose as reserved — it does **not** join the enumerated term list, because it has no shipped type.
3. `docs/src/architecture/domain-model.md` — add the same vocabulary rule as prose in the `## Medieval Military Naming Convention` section (the file today states the convention as a flat mandate with no plain-vs-Medieval split), and add exactly one table row after the `**Citadel**` row: `| **Commissary** | Input-side, per-call window-rationing officer | `Commissary` · `crates/paladin-llm/src/services/commissary.rs` |`. Do not reorder or reword any existing row.
4. `.github/copilot-instructions.md` — add exactly one row to the `## Naming Convention: Medieval Military Theme` table, after the `**Citadel**` row: `| **Commissary** | Input-side, per-call window-rationing officer | `crates/paladin-llm/src/services/commissary.rs` |`. Add that one row only; do NOT reconcile the six other terms this table is missing (Sanctum, Sentinel, Conclave, Council, Grove, Maneuver) — out of scope, RESEARCH.md Pitfall 4. `CLAUDE.md` carries no table of its own (it `@`-imports this file), so do not create a duplicate table there.

Commit as one conventional commit: `docs(30): anchor Commissary with ADR-0049 and the vocabulary rule`. Commit with `git commit -q -m … -- <files>` directly rather than the GSD commit helper (the pre-commit hook runs `cargo fmt --check` plus workspace clippy, ~70-140 s warm, and times the helper out). End the message with the literal trailer line `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`.
  </action>
  <acceptance_criteria>
    - `.planning/decisions/0049-commissary-design-and-rename.md` exists and its `^## ` heading sequence is exactly `## Status`, `## Context`, `## Decision`, `## Considered Options`, `## Code Locations`, `## Code Conformance`, `## Downstream Consumers` — seven headings, that order, no extras.
    - `grep -c 'Accepted' .planning/decisions/0049-commissary-design-and-rename.md` ≥ 1 and the file contains `**Date:** 2026-09-14` and the word `conforms`.
    - The file contains the string `origin/feature/quartermaster-prompt-budgeting:.planning/decisions/0010-prompt-context-budgeting.md` (branch-qualified provenance), and `.planning/decisions/0010-milestone-3-epic-numbering.md` is byte-unchanged (`git diff --name-only` does not list it).
    - All nine rejected candidate names appear in the file, each on a line beginning with `- `.
    - Every content line under `## Considered Options` and under `## Code Locations` begins with `- ` or a digit-dot marker; no prose paragraph appears under either heading.
    - `grep -c '^\*\*Next free ADR number' .planning/decisions/PROMOTION.md` returns `1` and that line reads `**Next free ADR number: 0050**`; `grep -c '^| 0049 |' .planning/decisions/PROMOTION.md` returns `1`.
    - `grep -c '(ADR-0049)' .planning/PROJECT.md` returns `1` and the row links `.planning/decisions/0049-commissary-design-and-rename.md`.
    - `grep -c '\*\*Commissary\*\*' docs/src/architecture/domain-model.md` returns `1`; `grep -c '\*\*Commissary\*\*' .github/copilot-instructions.md` returns `1`; `grep -c 'Commissary' .planning/PROJECT.md` ≥ 1.
    - For each of `docs/src/architecture/domain-model.md`, `.github/copilot-instructions.md` and `.planning/PROJECT.md`: `grep -c 'Commissary' FILE` equals `grep -ci 'commissary' FILE` (no lowercase or variant spelling).
    - No table row whose first cell is the bolded reserved spend-governance officer name exists in `docs/src/architecture/domain-model.md` or `.github/copilot-instructions.md` (the reserved term is prose only). <!-- planner-discipline-allow: Treasurer -->
    - `git diff HEAD~1 -- docs/src/architecture/domain-model.md .github/copilot-instructions.md` shows added lines only; no pre-existing table row is removed or reordered.
    - `git diff --name-only HEAD~1` lists no `.rs` file, no `Cargo.toml`, no `Cargo.lock` and no `MIGRATION.md`. <!-- planner-discipline-allow: MIGRATION.md -->
    - `mdbook build docs/` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>ADR=.planning/decisions/0049-commissary-design-and-rename.md; test -f "$ADR" && [ "$(grep '^## ' "$ADR" | tr '\n' '|')" = "## Status|## Context|## Decision|## Considered Options|## Code Locations|## Code Conformance|## Downstream Consumers|" ] && grep -q '\*\*Date:\*\* 2026-09-14' "$ADR" && grep -q 'conforms' "$ADR" && grep -q 'origin/feature/quartermaster-prompt-budgeting:.planning/decisions/0010-prompt-context-budgeting.md' "$ADR" && [ "$(grep -c '^\*\*Next free ADR number' .planning/decisions/PROMOTION.md)" = "1" ] && grep -q '^\*\*Next free ADR number: 0050\*\*' .planning/decisions/PROMOTION.md && [ "$(grep -c '^| 0049 |' .planning/decisions/PROMOTION.md)" = "1" ] && [ "$(grep -c '(ADR-0049)' .planning/PROJECT.md)" = "1" ] && [ "$(grep -c '\*\*Commissary\*\*' docs/src/architecture/domain-model.md)" = "1" ] && [ "$(grep -c '\*\*Commissary\*\*' .github/copilot-instructions.md)" = "1" ] && for f in docs/src/architecture/domain-model.md .github/copilot-instructions.md .planning/PROJECT.md; do [ "$(grep -c 'Commissary' $f)" = "$(grep -ci 'commissary' $f)" ] || exit 1; done && mdbook build docs/</automated>
  </verify>
  <done>ADR-0049 exists with the seven required headings in order and branch-qualified provenance; PROMOTION.md indexes it and reads `Next free ADR number: 0050`; PROJECT.md links it from Key Decisions and states the vocabulary rule; `Commissary` appears exactly once in each of the three vocabulary lists; `mdbook build docs/` exits 0; the commit touches no `.rs` file.</done>
</task>

<task type="auto">
  <name>Task 2: The Commissary mdBook page, reachable from the architecture nav</name>
  <reversibility rating="reversible">A new page and one nav line; deleting both restores the previous book exactly.</reversibility>
  <files>docs/src/architecture/commissary.md, docs/src/SUMMARY.md</files>
  <read_first>
    - `.planning/phases/30-token-economy-vocabulary-commissary-anchoring/30-PATTERNS.md` — the `docs/src/architecture/commissary.md` section (page shape, the two code blocks to mirror verbatim, the `SUMMARY.md` insertion format)
    - `.planning/phases/30-token-economy-vocabulary-commissary-anchoring/30-RESEARCH.md` Pattern 3 and the Code Examples section — the exact test-sourced usage sketch and the `rust,ignore` rationale
    - `crates/paladin-llm/src/services/commissary.rs` — the `#[cfg(test)] mod tests` helper `commissary()` and `an_over_budget_consignment_sheds_the_lowest_priority_item_first` (lines 631-686), and `the_window_comes_from_the_ports_declared_capabilities` (lines 985-1008). These are the shapes the sketch mirrors.
    - `crates/paladin-llm/src/services/commissary.rs` lines 61-300 — the public model types (`CommissaryPlan`, `ConsignmentItem`, `Consignment`, `DispensedItem`, `ShedItem`, `Stockpile`, `CommissaryError`) so every name on the page is a real one
    - `docs/src/architecture/domain-model.md` — the sibling architecture page whose prose/table structure this page mirrors
    - `docs/src/SUMMARY.md` lines 34-41 — the `# Architecture` nav block being extended
    - `docs/book.toml` — the `[output.linkcheck] warning-policy = "error"` setting and the mermaid preprocessor registration
  </read_first>
  <action>
Implements CONTEXT.md decision **D-2** (Commissary anchoring, the mdBook-page half → VOCAB-03).

Create `docs/src/architecture/commissary.md` with title `# Commissary`, framed as the input-side, per-call window-rationing officer — "what fits in this sortie's pack". Sections:

1. **Concept** — the two separate responsibilities (`verify_fits` guard, `dispense` allocator), the fail-loud / never-silent stance, and the split of duty: the framework owns measurement and enforcement, callers own policy via the per-item `priority`. Note that the service ships in `paladin-llm` and is re-exported unconditionally from the facade.
2. **The model** — a table covering `Consignment`, `ConsignmentItem`, `DispensedItem`, `ShedItem`, `Stockpile` (fields `dispensed`, `shed`, `prompt_tokens`, `allotted_tokens`, `exact_tally`), `CommissaryPlan`, and `CommissaryError` with its five variants (`UndeclaredContextWindow`, `ReservationExceedsWindow`, `FixedMaterialExceedsAllowance`, `ContextOverflow`, `InvalidConfig`). Use only these names — the crate ships exactly this surface, no more.
3. **Flow** — a mermaid diagram (the `mdbook-mermaid` 0.13.0 preprocessor is registered in `book.toml`) showing `Consignment` → `Commissary::dispense` → `Stockpile` split into `dispensed` and `shed`.
4. **Usage sketch** — the two code blocks reproduced from 30-PATTERNS.md / 30-RESEARCH.md, both fenced `rust,ignore`: the construct-and-dispense block mirroring the over-budget shed test, and the `verify_fits` match block showing the `ContextOverflow` arm. Keep the source-citation comment naming the `commissary.rs` line range each block mirrors. Use `rust,ignore` deliberately — `MockCounter` and `capabilities_with_window` are test-only helpers, so a live doctest would require inventing a public substitute, which the phase forbids.
5. **Honesty about exactness** — `is_exact_counter` is supplied by the caller at construction and surfaces as `Stockpile.exact_tally`, so a reader can tell an exact tally from an estimate.
6. **See also** — relative links to `domain-model.md` and `../getting-started/configuration.md` (both are real sibling pages; every intra-book link must resolve under `warning-policy = "error"`).

Then add exactly one nav line to `docs/src/SUMMARY.md` in the `# Architecture` block, immediately after the `- [Domain Model](architecture/domain-model.md)` line, matching the existing format exactly: `- [Commissary](architecture/commissary.md)`.

Do not add a `Bounded Contexts` row in `domain-model.md` — the Naming Convention row added in Task 1 satisfies VOCAB-01, and `Commissary` is a service, not an aggregate root (30-RESEARCH.md Open Question 1).

Commit as `docs(30): add Commissary mdBook page and architecture nav entry`, using `git commit -q -m … -- <files>` directly, ending with the literal trailer `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`.
  </action>
  <acceptance_criteria>
    - `docs/src/architecture/commissary.md` exists and `docs/src/SUMMARY.md` contains exactly one line matching `- [Commissary](architecture/commissary.md)`.
    - `mdbook build docs/` exits 0 and its linkcheck output reports no broken links (`warning-policy = "error"` means any unresolved intra-book link fails the build).
    - Anti-invention gate: every `Commissary::<name>` and `Consignment::<name>` mentioned on the page resolves to a `pub fn <name>` in `crates/paladin-llm/src/services/commissary.rs`; a name with no such function fails the check.
    - Anti-invention gate: every `Commissary*` / `Consignment*` / `Dispensed*` / `Shed*` / `Stockpile` type name mentioned on the page resolves to a `pub struct` or `pub enum` of that exact name in `crates/paladin-llm/src/services/commissary.rs`.
    - Both usage blocks are fenced `rust,ignore` — `grep -c '```rust,ignore' docs/src/architecture/commissary.md` returns `2`.
    - The page contains a `mermaid` fenced block (`grep -c '```mermaid' docs/src/architecture/commissary.md` returns `1`).
    - `grep -rniE 'api[_-]?key|secret|token=|bearer ' docs/src/architecture/commissary.md` finds no credential-shaped string (T-30-01).
    - `git diff --name-only HEAD~1` lists no `.rs` file.
  </acceptance_criteria>
  <verify>
    <automated>P=docs/src/architecture/commissary.md; C=crates/paladin-llm/src/services/commissary.rs; test -f "$P" && [ "$(grep -c '^- \[Commissary\](architecture/commissary.md)$' docs/src/SUMMARY.md)" = "1" ] && [ "$(grep -c '```rust,ignore' "$P")" = "2" ] && [ "$(grep -c '```mermaid' "$P")" = "1" ] && for m in $(grep -oE '\b(Commissary|Consignment)::[a-z_]+' "$P" | sed 's/.*:://' | sort -u); do grep -qE "pub fn $m\b" "$C" || { echo "INVENTED API: $m"; exit 1; }; done && for t in $(grep -oE '\b(Commissary[A-Za-z]*|Consignment[A-Za-z]*|Dispensed[A-Za-z]*|Shed[A-Za-z]*|Stockpile)\b' "$P" | sort -u); do grep -qE "pub (struct|enum) $t\b" "$C" || { echo "INVENTED TYPE: $t"; exit 1; }; done && ! grep -rniE 'api[_-]?key|secret|token=|bearer ' "$P" && mdbook build docs/</automated>
  </verify>
  <done>The Commissary page exists, is linked once from the Architecture nav, contains a mermaid flow diagram and two `rust,ignore` sketches mirrored from real tests, names zero invented API or types, and `mdbook build docs/` exits 0 with no broken links.</done>
</task>

</tasks>

<flagged_assumptions>
## Flagged assumptions (spec-less probe fallback — surfaced, never dropped)

- **VOCAB-03 edge probe returned `unclassified`.** No generic edge category
  (adjacency / empty / encoding / ordering / boundary / precision / idempotency / concurrency)
  maps cleanly onto "an mdBook page is reachable from the nav with the link-check green".
  **Assumption carried:** `mdbook build docs/` exiting 0 under `warning-policy = "error"`, plus the
  two anti-invention gates in Task 2, together form the complete correctness contract for this
  requirement; no further edge predicate is authored. Surfaced here per the no-silent-drop rule.

*No-silent-drop accounting for this plan: 8 probe rows authored into `must_haves.truths`
(VOCAB-01 ×4, VOCAB-02 ×4) + 1 surfaced as a flagged assumption (VOCAB-03) = 9 of the phase's
20 rows. Plan 30-02 carries 6, plan 30-03 carries 5 (4 authored + 1 flagged).*
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| abandoned branch history → live decision record | Text read from `origin/feature/quartermaster-prompt-budgeting` crosses into `.planning/decisions/`, where readers treat it as current. Provenance must survive the crossing. |
| planning corpus → published documentation | `docs/src/**` is built and published by `docs.yml`; anything written there reaches consumers outside this repo. |
| shipped crate API → documentation prose | The mdBook page asserts what the crate offers. Prose that overstates the API misleads a downstream integrator. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-01 | Information Disclosure | new ADR + mdBook page text | low | mitigate | No API key, token, secret or credential-shaped string appears in any new document — Task 2 `<verify>` greps the page for credential shapes; the ADR carries provider *names* only, never keys. |
| T-30-02 | Tampering | `docs/src/architecture/commissary.md` usage sketch | medium | mitigate | The sketch mirrors real `#[cfg(test)]` tests in `commissary.rs`; the two anti-invention gates in Task 2 fail the task if any method or type named on the page is absent from the crate. |
| T-30-03 | Tampering | scope creep from docs into code behaviour | medium | mitigate | Both tasks assert `git diff --name-only HEAD~1` lists no `.rs` file; the phase-level `.rs` diff guard lives in plan 30-03 Task 3. |
| T-30-04 | Repudiation | ADR provenance | low | mitigate | The historical record is cited by its full branch-qualified `git show` path, never renumbered into the in-tree series; the in-tree ADR of the same number must remain byte-unchanged. |
| T-30-SC | Tampering | package-manager installs | low | accept | No dependency is added: this phase changes no `Cargo.toml` or `Cargo.lock` (asserted in both tasks' acceptance criteria), so the package-legitimacy gate has nothing to audit — 30-RESEARCH.md "Package Legitimacy Audit: not applicable". |

Blocking threshold: `high`. No threat in this plan is rated `high`; no blocking checkpoint is
required.
</threat_model>

<verification>
- `mdbook build docs/` exits 0 (linkcheck `warning-policy = "error"`, ~30-60 s).
- ADR-0049 heading sequence is exactly the seven required H2s in PROMOTION.md's order.
- `Commissary` present exactly once in each of the three vocabulary lists, capitalisation-consistent.
- `PROMOTION.md` reads `**Next free ADR number: 0050**` with exactly one such line.
- `git diff --name-only` for this plan's commits lists no `.rs`, `Cargo.toml`, `Cargo.lock` or
  `MIGRATION.md` path. <!-- planner-discipline-allow: MIGRATION.md -->
- `.planning/` phase history under `.planning/phases/` is untouched by this plan.
</verification>

<success_criteria>
- VOCAB-01 satisfied: the vocabulary rule is stated in `.planning/PROJECT.md` and
  `docs/src/architecture/domain-model.md`, and `Commissary` is in the ubiquitous-language list,
  the domain-model table and the developer-facing Naming Convention table.
- VOCAB-02 satisfied: ADR-0049 records the design, the rename rationale and the nine-name rejected
  list, reconstructed from the branch-qualified historical record and the port commits.
- VOCAB-03 satisfied: `docs/src/architecture/commissary.md` exists, is linked from the architecture
  nav, and the book builds with the link-check green.
- Two atomic conventional commits, each ending with the required `Co-Authored-By` trailer.
</success_criteria>

<output>
Create `.planning/phases/30-token-economy-vocabulary-commissary-anchoring/30-01-SUMMARY.md` when done.
</output>
