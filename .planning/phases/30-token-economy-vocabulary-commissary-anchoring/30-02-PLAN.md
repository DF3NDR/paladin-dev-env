---
phase: 30-token-economy-vocabulary-commissary-anchoring
plan: 02
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - .planning/decisions/0050-treasurer-reservation.md
  - .planning/decisions/0051-token-economy-versioning-x03-supersession.md
  - .planning/decisions/PROMOTION.md
  - .planning/PROJECT.md
autonomous: true
requirements: [VOCAB-04, VOCAB-07]

must_haves:
  truths:
    # --- goal-backward (plan objective) ---
    - "A reader of .planning/decisions/ finds a one-page reservation ADR (0050) stating that the spend-governance officer term is reserved, verified 0/0 in-tree by grep, owns cross-run / per-tenant / per-API-key allowances, per-model currency pricing, cost_estimate production and rate pacing, installs rather than replaces the per-run TokenBudget, and is built in Milestone 14."
    - "That same ADR carries the downstream guardrail: the term is a framework-only word and must never be used as an audit-target or fixture domain term in the downstream Web3 Security Paladin app, nor the reverse."
    - "A reader of .planning/decisions/ finds a versioning ADR (0051) recording that Phases 31-33 land as clean breaks inside the untagged v0.10.0, superseding corpus rule X-03 for those three phases only, on the operator's 2026-09-14 decision."
    - "ADR-0051 states that every break in Phases 31-33 still gets a MIGRATION section 9.2 row and a cargo semver-checks allowlist row as documentation for the downstream refactor, never as a compatibility shim."
    - ".planning/PROJECT.md's Key Decisions table links all three of this phase's ADRs, and the X-03 supersession is recorded there as required by VOCAB-07."
    # --- edge-probe lift: VOCAB-04 ---
    - "[edge:VOCAB-04/boundary] The 0/0 in-tree claim holds exactly at zero as a CODE SYMBOL, and the ADR says so in those terms: a symbol-scoped grep across crates and src for struct/enum/trait/mod/fn/impl/use/type declarations of the reserved name, or for its path-qualified form, returns no match — and every occurrence of the bare word anywhere under crates or src is a rustdoc line (a /// or //! line), never code. ADR text lives under .planning/decisions/, outside both greps' scope. One non-doc occurrence would invalidate the reservation."
    - "[derived:VOCAB-04/successor-state] ADR-0050 records the bare authoring-time grep result verbatim AND anticipates the only in-tree prose occurrences this same phase creates: the herald.rs cost_estimate rustdoc written by plan 30-03 under VOCAB-05, which names the reserved role as the field's future producer. A reader re-running the bare grep after Phase 30 closes must not conclude the ADR lied."
    - "[edge:VOCAB-04/precision] ADR-0050 states that the reserved officer installs a per-run TokenBudget and does not replace it, using the word installs, and cites src/application/services/paladin/middleware/limits.rs as the install point."
    - "[edge:VOCAB-04/concurrency] Two ADRs authored in one plan mutate one counter exactly once: .planning/decisions/PROMOTION.md holds exactly one line beginning with the bolded Next free ADR number label, it reads 0052, and a single dated note accounts for the advance by two."
    # --- edge-probe lift: VOCAB-07 ---
    - "[edge:VOCAB-07/adjacency] Three contiguous ADR numbers, none reused: ls of .planning/decisions/0049-*.md, 0050-*.md and 0051-*.md each matches exactly one file, and git diff on PROMOTION.md shows only added index rows plus the single Next free ADR number line change — no pre-existing row renumbered, reworded or reordered."
    - "[edge:VOCAB-07/empty] The supersession scope is exhaustive and bounded: ADR-0051 names Phases 31, 32 and 33 and no others, and states that Phase 30 itself makes no public-API change and therefore registers zero migration-register rows and zero semver allowlist rows."
    - "[edge:VOCAB-07/ordering] The three Key Decisions rows are appended in ascending ADR-number order (0049, 0050, 0051) below the current last row, each in the shape pipe-bracket-title-link-paren-ADR-number pipe rationale pipe conforms, and no pre-existing row is reordered."
  artifacts:
    - ".planning/decisions/0050-treasurer-reservation.md (new)"
    - ".planning/decisions/0051-token-economy-versioning-x03-supersession.md (new)"
    - ".planning/decisions/PROMOTION.md (modified — two index rows, Next free ADR number advanced 0050 → 0052, one dated note)"
    - ".planning/PROJECT.md (modified — two Key Decisions rows, for ADR-0050 and ADR-0051)"
  key_links:
    - "ADR-0050 -> src/application/services/paladin/middleware/limits.rs — the install point that makes installs-not-replaces a checkable claim rather than a slogan."
    - "ADR-0051 -> .project/v0.10.0/00-program-overview.md X-03 — the exact rule being superseded; without the citation a future reader cannot tell what was narrowed."
    - "ADR-0051 -> .project/Milestone_13-Token-Economy/overview/Milestone-13_Token-Economy.md section 5.1 — the clean-break policy the supersession adopts."
    - ".planning/PROJECT.md Key Decisions rows -> the three ADR files — the table links, never restates; a broken relative path orphans the record."
    - ".planning/decisions/PROMOTION.md Next free ADR number line -> the 0050 and 0051 files — index and directory must agree or the next phase allocates a colliding number."
  prohibitions:
    # canon-referral drop: no OWASP / injection / path-traversal / GDPR prohibitions minted —
    # this plan writes planning-record prose only (30-RESEARCH.md Security Domain).
    - requirement_id: VOCAB-04
      category: transparency
      statement: "ADR-0050 must not describe any spend-governance code, crate, module, type, feature flag or config key as existing, partially built, or planned for this phase — the officer name is reserved in writing only, and no artifact bearing it may be created under crates or src."
      status: unresolved
      verification: null
    - requirement_id: VOCAB-07
      category: transparency
      statement: "ADR-0051 must not present the supersession as broader than the operator granted: it covers Phases 31-33 only, and must not be worded so a later reader can read it as a blanket licence to remove public API anywhere else in v0.10.0."
      status: unresolved
      verification: null
---

<objective>
Reserve the spend-governance officer term in writing, and turn the clean-break versioning
assumption that Phases 31-33 rely on into a numbered, citable decision.

Purpose: Phases 31-33 make deliberate breaking changes inside an untagged v0.10.0. Today that
rests on an operator conversation and a milestone-overview paragraph, while the v0.10.0 program
overview's X-03 says removals are not allowed. VOCAB-07 makes the exception an ADR rather than an
assumption. VOCAB-04 reserves the second officer's name before anyone builds against it — and
records the cross-repo guardrail that keeps the framework word out of the downstream app's
audit-target vocabulary.

Output: ADR-0050, ADR-0051, the PROMOTION.md index bookkeeping, and the matching PROJECT.md
Key Decisions rows.

**Tracer:** this phase's single `type="tracer"` slice landed in plan 30-01 Task 1 (one tracer per
phase, per `references/planner-mvp-mode.md`). This plan is expansion along the proven
ADR → index → Key-Decisions path.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-token-economy-vocabulary-commissary-anchoring/30-CONTEXT.md
@.planning/phases/30-token-economy-vocabulary-commissary-anchoring/30-RESEARCH.md
@.planning/phases/30-token-economy-vocabulary-commissary-anchoring/30-PATTERNS.md
@.planning/phases/30-token-economy-vocabulary-commissary-anchoring/30-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

See the full phase-wide table in `30-01-PLAN.md`. This plan owns:

| Artifact | Kind |
|---|---|
| `.planning/decisions/0050-treasurer-reservation.md` | new ADR (one page, reservation shape) |
| `.planning/decisions/0051-token-economy-versioning-x03-supersession.md` | new ADR (policy-exception shape) |
| `.planning/decisions/PROMOTION.md` | two index rows, `Next free ADR number` 0050 → 0052, one dated note |
| `.planning/PROJECT.md` | two Key Decisions rows (ADR-0050, ADR-0051) |

**No new Rust symbols are created by this plan** — and specifically, no module, type, feature flag
or file bearing the reserved officer name may be created anywhere under `crates/` or `src/`. No
`MIGRATION.md` §9.2 entry and no `cargo semver-checks` allowlist change belongs to Phase 30.
<!-- planner-discipline-allow: MIGRATION.md -->
</artifacts_this_phase_produces>

<assumption_delta_decision>
## Assumption-delta checkpoint (advisory)

`gsd-tools query assumption-delta scan 30 --json` returned `detected: true` with a single
`pluralization` signal on the word **"also"**, matched inside VOCAB-07's sentence *"the supersession
is also recorded in `PROJECT.md` Key Decisions"*.

- **Noun now primary:** none. The match is on a discourse adverb, not on a second instance of a
  previously-singular domain noun.
- **Decision:** `no-change`.
- **Rationale:** the only genuine singular→plural shape in this phase is the two-officer model
  (Commissary + the reserved spend-governance officer), and that is a *vocabulary reservation
  recorded in prose*, not a code generalization — no type, trait, collection or config key becomes
  plural, and no code path gains a second implementation. Promoting or adding-alongside would mean
  generalizing a structure that does not exist in the tree (the reserved officer is 0/0 in-tree by
  design).
- **Accepted debt:** none.
- **Suggested (not required) invariant test:** none proposed — there is no runtime structure to
  encode an invariant against in this phase. If Milestone 14 ever builds the reserved officer, the
  invariant worth encoding there is that installing a per-run `TokenBudget` composes with, rather
  than replaces, the existing middleware chain.
</assumption_delta_decision>

<tasks>

<task type="auto">
  <name>Task 1: ADR-0050 — reserve the spend-governance officer term, with the downstream guardrail</name>
  <reversibility rating="reversible">A reservation ADR can be superseded by a later ADR; no code, no contract and no stored data depends on it.</reversibility>
  <files>.planning/decisions/0050-treasurer-reservation.md</files>
  <read_first>
    - `.planning/phases/30-token-economy-vocabulary-commissary-anchoring/30-PATTERNS.md` — the `0050-treasurer-reservation.md` section (reservation/deferral shape, `conforms` verdict, reuse pattern)
    - `.planning/phases/30-token-economy-vocabulary-commissary-anchoring/30-CONTEXT.md` — the Treasurer reservation decision block (D-3 → VOCAB-04) and the locked-by-overview list
    - `.planning/decisions/0042-llm-native-tool-calling-deferred.md` — the closest in-tree shape: a capability recorded as future, not built, with a named trigger and owner
    - `.planning/decisions/0048-paladin-eval-composition-crate.md` — the heading skeleton to copy
    - `.planning/decisions/PROMOTION.md` lines 205-235 — the required heading set and the bullet-list contract for `## Considered Options` / `## Code Locations`
    - `.project/Milestone_14-Treasurer/overview/Milestone-14_Treasurer.md` and `.project/Milestone_14-Treasurer/Epic_1/prd-treasurer-spend-governance.md` — what the reservation promises the officer will own (read to describe it; build none of it)
    - `.project/Milestone_13-Token-Economy/overview/Milestone-13_Token-Economy.md` section 5 item 3 — the exact downstream-guardrail wording (the `GarrisonTreasury` fixture collision)
    - `src/application/services/paladin/middleware/limits.rs` — the existing per-run `TokenBudget` middleware that the reserved officer *installs* rather than replaces
  </read_first>
  <action>
Implements CONTEXT.md decision **D-3** (Treasurer reservation + downstream guardrail → VOCAB-04).

Write `.planning/decisions/0050-treasurer-reservation.md` — one page — with title `# ADR-0050: `Treasurer` reserved for cross-run spend governance`, `## Status` body `Accepted`, `**Date:** 2026-09-14`, and exactly the seven required H2 headings in PROMOTION.md's order. `## Considered Options` and `## Code Locations` are bulleted lists. `## Code Conformance` reads `conforms` — the ADR reserves a word and institutes no code change.

Content:
- **Context:** Milestone 13 splits token work across two officers. `Commissary` (ADR-0049) is the input-side, per-call window-rationing officer. The output-side, cross-run spend-governance role needs a name now so that Phases 31-33 and the downstream app do not invent competing ones. Run `grep -rn Treasurer crates src` at authoring time and record the verbatim command and its 0/0 result in the ADR text, dated 2026-09-14 and qualified by the current commit SHA. <!-- planner-discipline-allow: Treasurer -->
- **Context — the successor state, stated up front so the record does not age into a lie:** the reservation is **0/0 as a code symbol**, permanently. Later in this same phase, plan 30-03 (VOCAB-05) writes the reserved role's name into rustdoc prose on `ExecutionMetadata.cost_estimate` in `crates/paladin-core/src/platform/container/herald.rs`, naming it as that field's future producer. Say this explicitly in the ADR: after Phase 30 closes, the bare grep returns rustdoc lines only, and the durable invariant is the symbol-scoped one — no `struct`, `enum`, `trait`, `mod`, `fn`, `impl`, `use` or `type` declaration of the name and no path-qualified use of it exists anywhere under `crates/` or `src/`, and every occurrence of the bare word there is a `///` or `//!` line.
- **Decision — what the role will own:** cross-run / per-tenant / per-API-key **allowances**; per-model currency **pricing**; production of `ExecutionMetadata.cost_estimate` (`crates/paladin-core/src/platform/container/herald.rs`), which has no in-tree producer today; and rate **pacing**.
- **Decision — what it does NOT do:** it **installs** a per-run `TokenBudget` — the existing middleware at `src/application/services/paladin/middleware/limits.rs`, configured via `agent_runtime.token_budget.*` — rather than replacing it. Use the word *installs*; do not write *replaces* or *manages*.
- **Decision — when:** built in **Milestone 14** (`.project/Milestone_14-Treasurer/`), which hard-depends on Phase 31; not this cycle, not this milestone. Rustdoc and prose elsewhere in the repo point at **Milestone 14**, never at "Epic 5" (that PRD wording predates the Milestone 14 split).
- **Decision — the downstream guardrail:** the term is a **framework-only word**. In the downstream Web3 Security Paladin app it collides with a benchmark fixture, `GarrisonTreasury`, which is an audit-*target* domain term. The framework term must never be used as an audit-target or fixture domain term downstream, and the downstream fixture vocabulary must never be used for the framework role. Record that this is a cross-repo convention documented here, not a lint or CI rule in this repo.
- **Considered Options (bulleted):** `Paymaster` — rejected, a paymaster pays the troops whereas tokens are spent with the provider; `Comptroller` — a collision-free alternative, viable but not chosen; the chosen term with the guardrail accepted; and "reserve nothing, name it when built" — rejected, because the downstream app is already writing spend-adjacent vocabulary. Record that the two-officer model is operator-confirmed 2026-09-14 and locked.
- **Code Locations (bulleted):** `src/application/services/paladin/middleware/limits.rs` (the install point); `src/config/agent_runtime.rs` (`token_budget` config); `crates/paladin-core/src/platform/container/herald.rs` (`cost_estimate`, the field the role will one day produce); `.project/Milestone_14-Treasurer/` (the owning milestone). Each bullet states that the officer itself appears in **none** of them today.
- **Downstream Consumers:** Milestone 14, Phase 31 (its hard prerequisite), and the downstream Web3 Security Paladin app.
- Version identity is **v0.10.0** throughout; never `v0.11.0`.

Create **no** file, module, type, marker trait, feature flag or config key bearing the reserved
name anywhere under `crates/` or `src/` — the 0/0 grep is the exit condition and a scaffolding
instinct would trip it (30-RESEARCH.md Pitfall 1).

Commit as `docs(30): reserve the Treasurer term with ADR-0050`, using `git commit -q -m … -- <files>` directly rather than the GSD commit helper, ending with the literal trailer `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`.
  </action>
  <acceptance_criteria>
    - `.planning/decisions/0050-treasurer-reservation.md` exists; its `^## ` heading sequence is exactly `## Status`, `## Context`, `## Decision`, `## Considered Options`, `## Code Locations`, `## Code Conformance`, `## Downstream Consumers`.
    - The file contains `**Date:** 2026-09-14`, the status word `Accepted`, and the verdict word `conforms`.
    - The file contains the word `installs` and cites `src/application/services/paladin/middleware/limits.rs`.
    - The file contains `Milestone 14` and does NOT contain the string `Epic 5`.
    - The file contains `GarrisonTreasury` (the downstream guardrail) and both rejected alternatives `Paymaster` and `Comptroller`.
    - The file contains `v0.10.0` and does NOT contain `v0.11.0`.
    - Symbol-scoped reservation holds: `grep -rnE '\b(struct|enum|trait|mod|fn|impl|use|type) +Treasurer\b|\bTreasurer::' crates src` returns no match (exit 1) — the reservation adds no in-tree code artifact. <!-- planner-discipline-allow: Treasurer -->
    - Every occurrence of the bare word under `crates`/`src` (zero at this point in the phase; rustdoc-only after plan 30-03) is a `///` or `//!` line: `grep -rn 'Treasurer' crates src | grep -v '///' | grep -v '//!'` produces empty output. <!-- planner-discipline-allow: Treasurer -->
    - The ADR states both the authoring-time bare-grep result and the durable symbol-scoped invariant, and names `herald.rs` as the one place this phase later writes the term into rustdoc prose.
    - Every content line under `## Considered Options` and `## Code Locations` begins with `- ` or a digit-dot marker.
    - `git diff --name-only HEAD~1` lists only `.planning/decisions/0050-treasurer-reservation.md`.
  </acceptance_criteria>
  <verify>
    <automated>A=.planning/decisions/0050-treasurer-reservation.md; test -f "$A" && [ "$(grep '^## ' "$A" | tr '\n' '|')" = "## Status|## Context|## Decision|## Considered Options|## Code Locations|## Code Conformance|## Downstream Consumers|" ] && grep -q '\*\*Date:\*\* 2026-09-14' "$A" && grep -q 'conforms' "$A" && grep -q 'installs' "$A" && grep -q 'src/application/services/paladin/middleware/limits.rs' "$A" && grep -q 'Milestone 14' "$A" && ! grep -q 'Epic 5' "$A" && grep -q 'GarrisonTreasury' "$A" && grep -q 'Paymaster' "$A" && grep -q 'Comptroller' "$A" && grep -q 'v0\.10\.0' "$A" && ! grep -q 'v0\.11\.0' "$A" && grep -q 'herald.rs' "$A" && ! grep -rnE '\b(struct|enum|trait|mod|fn|impl|use|type) +Treasurer\b|\bTreasurer::' crates src && [ -z "$(grep -rn 'Treasurer' crates src | grep -v '///' | grep -v '//!')" ]</automated>
  </verify>
  <done>ADR-0050 exists as a one-page reservation with the seven required headings, records the role's future scope, the installs-not-replaces rule with its install point, the Milestone 14 owner and the downstream guardrail with both rejected alternatives; the reserved name exists nowhere under `crates`/`src` as a code symbol, and the ADR states up front that plan 30-03 will add rustdoc-prose occurrences in `herald.rs`.</done>
</task>

<task type="auto">
  <name>Task 2: ADR-0051 — clean breaks inside the untagged v0.10.0, superseding X-03 for Phases 31-33</name>
  <reversibility rating="costly">Undoing after Phase 31 lands means restoring removed public API across three phases and re-coordinating the downstream submodule refactor — flagged, not gated: the operator made this call on 2026-09-14 and CONTEXT.md records it as locked.</reversibility>
  <files>.planning/decisions/0051-token-economy-versioning-x03-supersession.md</files>
  <read_first>
    - `.planning/phases/30-token-economy-vocabulary-commissary-anchoring/30-PATTERNS.md` — the `0051-...` section (supersession-framing analog, `conforms` verdict)
    - `.planning/phases/30-token-economy-vocabulary-commissary-anchoring/30-CONTEXT.md` — the "Versioning decision ADR (VOCAB-07)" block, which carries the exact scope and the operator's rationale
    - `.project/v0.10.0/00-program-overview.md` line 44 (X-03, the rule being narrowed) and line 58 (X-10, semver hygiene, which keeps applying)
    - `.project/Milestone_13-Token-Economy/overview/Milestone-13_Token-Economy.md` section 5 items 1 and 2 — the clean-break policy and the coordinated pointer bump
    - `.planning/decisions/0048-paladin-eval-composition-crate.md` — heading skeleton and the precedent for an ADR that narrows an existing rule for a scoped set of work
    - `.planning/decisions/PROMOTION.md` lines 205-245 — the required heading set and the supersession mechanism (a superseding ADR carries a `## Supersedes` line when it supersedes another **ADR**)
    - `.planning/ROADMAP.md` — the Phase 31, 32 and 33 entries, to name the scope exactly
  </read_first>
  <action>
Implements the CONTEXT.md "Versioning decision ADR" decision (a roadmap-time addition with no PRD D-number → VOCAB-07), under the CONTEXT.md locked version identity (v0.10.0, Milestone 14 references).

Write `.planning/decisions/0051-token-economy-versioning-x03-supersession.md` with title `# ADR-0051: Token-economy phases land as clean breaks inside the untagged v0.10.0`, `## Status` body `Accepted`, `**Date:** 2026-09-14`, and exactly the seven required H2 headings in PROMOTION.md's order. Do **not** add a `## Supersedes` heading: X-03 is a program-corpus rule, not an ADR, so the supersession is stated inside `## Decision` and the seven-heading set stays intact.

Content:
- **Context:** v0.10.0 corpus rule **X-03** (`.project/v0.10.0/00-program-overview.md` line 44) requires existing public APIs to keep compiling and behaving identically, permits `#[deprecated]` but forbids removals, and declares any other behavioral change a stop-and-flag event. Milestone 13 §5.1 takes the opposite position for the token-economy work: clean break, no deprecation shims, because the framework is pre-1.0 and has exactly one consumer — the downstream Web3 Security Paladin app — which pins its own submodule pointer and adopts the whole milestone in one coordinated step.
- **Decision:** X-03 is **superseded for Phases 31, 32 and 33 only**, on the operator's 2026-09-14 decision. Name those three phases explicitly and state that the exception extends to no other phase, no other milestone, and no other public API in v0.10.0. State the two named breaks the milestone already anticipates: the token-carrier change (Phase 31 / Epic 2) and dropping `Commissary::new`'s `is_exact_counter` argument (Phase 32 / Epic 3).
- **Decision — what still applies:** every break in those phases still gets a `MIGRATION.md` §9.2 row **and** a `cargo semver-checks` allowlist row, row-level set-equal in both directions per Phase 29 D-04 — **as documentation for the downstream refactor, never as a compatibility shim**. X-10 (semver hygiene) is untouched and still governs which additive-looking edits count as breaking. The `0.10.0` tag is cut only after Phase 33 re-seals the Phase 29 release gates (COMM-04); `0.10.0` is bumped on the feature branch with no tag until then (Phase 29 D-18/D-21).
- **Decision — this phase's own position:** Phase 30 changes no public Rust API, so it registers **zero** §9.2 rows and **zero** allowlist rows. Say this explicitly; it is the boundary that keeps the exception from reading as retroactive.
- **Version identity:** write **v0.10.0** everywhere. Milestone 13 §5.2's "cut a v0.11.0 release" wording is superseded by the operator's instruction — note that supersession in one sentence so a reader comparing the two documents is not left guessing.
- **Considered Options (bulleted):** deprecation shims across three phases (rejected — ships cruft while the API is still being shaped, pre-1.0, and the single consumer adopts in lockstep anyway); defer the breaks to a v0.11.0 after tagging v0.10.0 (rejected — the operator's version identity decision puts this work inside v0.10.0); clean break with migration-register documentation (chosen); blanket X-03 waiver for all of v0.10.0 (rejected — far broader than the operator granted).
- **Code Locations (bulleted):** `.project/v0.10.0/00-program-overview.md` (X-03, X-10); `.project/Milestone_13-Token-Economy/overview/Milestone-13_Token-Economy.md` §5; `MIGRATION.md` §9.2 (the register later phases write into — this phase adds no row); the `cargo semver-checks` allowlist (likewise untouched by this phase); `.planning/ROADMAP.md` Phases 31-33. <!-- planner-discipline-allow: MIGRATION.md -->
- **Code Conformance:** `conforms` — this phase makes no public-API change; the ADR records a forward-looking policy exception for later phases.
- **Downstream Consumers:** Phases 31, 32, 33, the Phase 33 release re-seal (COMM-04), and the downstream app's coordinated refactor.

Commit as `docs(30): record the X-03 clean-break supersession as ADR-0051`, using `git commit -q -m … -- <files>` directly, ending with the literal trailer `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`.
  </action>
  <acceptance_criteria>
    - `.planning/decisions/0051-token-economy-versioning-x03-supersession.md` exists; its `^## ` heading sequence is exactly the seven required headings in PROMOTION.md's order (no `## Supersedes`).
    - The file contains `**Date:** 2026-09-14`, `Accepted`, and `conforms`.
    - The file contains `X-03` and cites `.project/v0.10.0/00-program-overview.md`.
    - The file names all three of `Phase 31`, `Phase 32` and `Phase 33` (or the exact form `Phases 31-33` plus each number) and states the exception extends to no other phase.
    - The file contains `v0.10.0` and does NOT contain `v0.11.0` as this work's target — any mention of the superseded `v0.11.0` framing appears only in the one sentence that records it as superseded.
    - The file states that Phase 30 itself registers no migration-register row and no semver allowlist row.
    - Every content line under `## Considered Options` and `## Code Locations` begins with `- ` or a digit-dot marker.
    - `git diff --name-only HEAD~1` lists only `.planning/decisions/0051-token-economy-versioning-x03-supersession.md`; in particular it lists no `.rs` file, no `Cargo.toml`, and no migration register. <!-- planner-discipline-allow: MIGRATION.md -->
  </acceptance_criteria>
  <verify>
    <automated>A=.planning/decisions/0051-token-economy-versioning-x03-supersession.md; test -f "$A" && [ "$(grep '^## ' "$A" | tr '\n' '|')" = "## Status|## Context|## Decision|## Considered Options|## Code Locations|## Code Conformance|## Downstream Consumers|" ] && grep -q '\*\*Date:\*\* 2026-09-14' "$A" && grep -q 'conforms' "$A" && grep -q 'X-03' "$A" && grep -q '00-program-overview.md' "$A" && grep -q '31' "$A" && grep -q '32' "$A" && grep -q '33' "$A" && grep -q 'v0\.10\.0' "$A" && [ "$(git diff --name-only HEAD~1 | grep -c '\.rs$')" = "0" ]</automated>
  </verify>
  <done>ADR-0051 exists with the seven required headings, cites X-03 at its source line, scopes the supersession to Phases 31-33 exactly, preserves the migration-register and allowlist requirement as documentation rather than a shim, and states that Phase 30 itself registers neither.</done>
</task>

<task type="auto">
  <name>Task 3: Index bookkeeping — PROMOTION.md rows and the PROJECT.md Key Decisions rows</name>
  <reversibility rating="reversible">Table rows and a counter line; reverting the commit restores the index exactly.</reversibility>
  <files>.planning/decisions/PROMOTION.md, .planning/PROJECT.md</files>
  <read_first>
    - `.planning/decisions/PROMOTION.md` lines 20-120 — the index table (row shape `| NNNN | slug | subject (Phase N, plan N-NN) |`), the `**Next free ADR number:**` line, and the dated-note precedents (the 2026-08-12 plan 14-07 note is the worked example for an advance-by-more-than-one)
    - `.planning/decisions/PROMOTION.md` lines 255-270 — Part A steps 5 and 6, the two bookkeeping obligations
    - `.planning/PROJECT.md` lines 1308-1325 — the `## Key Decisions` header comment and the exact row shape, plus the current last row (the 30-01 commit already appended the ADR-0049 row; append below it)
    - `.planning/phases/30-token-economy-vocabulary-commissary-anchoring/30-01-SUMMARY.md` — what plan 30-01 already wrote into both files, so this task appends rather than duplicates
    - `.planning/phases/30-token-economy-vocabulary-commissary-anchoring/30-PATTERNS.md` — the Key Decisions row shape and the numbering procedure
  </read_first>
  <action>
Append to `.planning/decisions/PROMOTION.md`'s index table, immediately after the `| 0049 |` row that plan 30-01 added, two rows in the established shape:
- `| 0050 | `treasurer-reservation` | <one-line subject> (Phase 30, plan 30-02) |`
- `| 0051 | `token-economy-versioning-x03-supersession` | <one-line subject> (Phase 30, plan 30-02) |`

Change the `**Next free ADR number: 0050**` line (as left by plan 30-01) to `**Next free ADR number: 0052**`. There must remain exactly **one** line in the file beginning with that bolded label — a prior phase recorded a grep-count-2 mismatch caused by the Part A step-5 procedural sentence that also contains the phrase; that sentence is prose, is correct as written, and must not be edited.

Insert one dated note directly beneath the `Next free ADR number` line, dated 2026-09-14, in the style of the 2026-08-12 plan 14-07 note: the line advances by **two**, from 0050 to 0052, because plan 30-02 authored ADR-0050 and ADR-0051 (plan 30-01 having already advanced 0049 → 0050 for ADR-0049); state that `ls .planning/decisions/00{49,50,51}-*.md` was re-run and confirms all three files exist with contiguous numbers, none skipped or reused, and no existing index row renumbered, reworded or reordered. Do not edit PROMOTION.md's Part A procedural prose.

Then append two rows to `.planning/PROJECT.md`'s `## Key Decisions` table, below the ADR-0049 row plan 30-01 added, in ascending ADR-number order and the exact existing shape `| [<title>](.planning/decisions/00NN-<slug>.md) (ADR-00NN) | <one-sentence rationale citing concrete evidence> | conforms |`:
- ADR-0050 — rationale names the reserved role's future scope, the installs-not-replaces rule, Milestone 14 as owner, and the 0/0 in-tree grep.
- ADR-0051 — rationale names X-03, the Phases 31-33 scope, the operator's 2026-09-14 decision, and that migration-register plus allowlist rows remain required as documentation. This row is what VOCAB-07 means by "the supersession is also recorded in `PROJECT.md` Key Decisions". <!-- planner-discipline-allow: MIGRATION.md -->

Preserve the `<!-- LOCKED DECISIONS ... -->` header comment and every existing row byte-for-byte; reorder nothing.

Commit as `docs(30): index ADR-0050 and ADR-0051 and link them from Key Decisions`, using `git commit -q -m … -- <files>` directly, ending with the literal trailer `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`.
  </action>
  <acceptance_criteria>
    - `grep -c '^\*\*Next free ADR number' .planning/decisions/PROMOTION.md` returns `1` and that line reads `**Next free ADR number: 0052**`.
    - `grep -c '^| 0050 |' .planning/decisions/PROMOTION.md` returns `1` and `grep -c '^| 0051 |' .planning/decisions/PROMOTION.md` returns `1`.
    - PROMOTION.md contains a note dated `2026-09-14` referencing plan `30-02`.
    - `grep -c '(ADR-0050)' .planning/PROJECT.md` returns `1` and `grep -c '(ADR-0051)' .planning/PROJECT.md` returns `1`; each row links the matching file under `.planning/decisions/`.
    - The three Key Decisions rows appear in ascending ADR-number order: the line number of the `(ADR-0049)` row is less than that of `(ADR-0050)`, which is less than that of `(ADR-0051)`.
    - `git diff HEAD~1 -- .planning/decisions/PROMOTION.md` changes exactly one pre-existing line (the `Next free ADR number` line); every other change is an addition.
    - `ls .planning/decisions/0049-*.md .planning/decisions/0050-*.md .planning/decisions/0051-*.md` lists exactly three files.
    - `git diff --name-only HEAD~1` lists no `.rs` file.
  </acceptance_criteria>
  <verify>
    <automated>P=.planning/decisions/PROMOTION.md; J=.planning/PROJECT.md; [ "$(grep -c '^\*\*Next free ADR number' $P)" = "1" ] && grep -q '^\*\*Next free ADR number: 0052\*\*' $P && [ "$(grep -c '^| 0050 |' $P)" = "1" ] && [ "$(grep -c '^| 0051 |' $P)" = "1" ] && grep -q '2026-09-14' $P && [ "$(grep -c '(ADR-0050)' $J)" = "1" ] && [ "$(grep -c '(ADR-0051)' $J)" = "1" ] && a=$(grep -n '(ADR-0049)' $J | head -1 | cut -d: -f1) && b=$(grep -n '(ADR-0050)' $J | head -1 | cut -d: -f1) && c=$(grep -n '(ADR-0051)' $J | head -1 | cut -d: -f1) && [ "$a" -lt "$b" ] && [ "$b" -lt "$c" ] && [ "$(ls .planning/decisions/0049-*.md .planning/decisions/0050-*.md .planning/decisions/0051-*.md | wc -l)" = "3" ] && [ "$(git diff --name-only HEAD~1 | grep -c '\.rs$')" = "0" ]</automated>
  </verify>
  <done>PROMOTION.md indexes all three of this phase's ADRs, reads `Next free ADR number: 0052` on exactly one line, and carries one dated 2026-09-14 note accounting for the advance by two; PROJECT.md's Key Decisions table links all three ADRs in ascending number order with no pre-existing row disturbed.</done>
</task>

</tasks>

<flagged_assumptions>
## Flagged assumptions (spec-less probe fallback)

None originate in this plan — VOCAB-04 and VOCAB-07 both produced classified edge rows, all six of
which are authored into `must_haves.truths` above. The two `unclassified` rows for the phase
(VOCAB-03, VOCAB-06) are surfaced in plans 30-01 and 30-03 respectively.

*No-silent-drop accounting for this plan: 6 probe rows authored (VOCAB-04 ×3, VOCAB-07 ×3),
0 flagged.*
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator conversation → durable policy record | An exception granted verbally becomes a citable ADR that later agents execute against. Over-broad wording grants more than was asked. |
| framework vocabulary → downstream app vocabulary | The reserved officer name crosses into another repo's domain language, where an existing fixture already occupies adjacent ground. |
| planning record → future executor behaviour | Phases 31-33 read ADR-0051 as authorization to remove public API. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-05 | Elevation of Privilege | ADR-0051 scope wording | medium | mitigate | The supersession names Phases 31, 32 and 33 exhaustively and states it extends to no other phase or API; the "blanket X-03 waiver" option is recorded as explicitly rejected in `## Considered Options`, and Task 2's acceptance criteria assert the three phase numbers are present. |
| T-30-06 | Spoofing | reserved-term scaffolding | medium | mitigate | No file, module, type, feature flag or config key bearing the reserved name may exist under `crates/` or `src/`; Task 1's `<verify>` fails if the 0/0 grep finds anything (30-RESEARCH.md Pitfall 1). |
| T-30-07 | Repudiation | ADR-0050's 0/0 claim vs this phase's own later edit | medium | mitigate | VOCAB-05 (plan 30-03, wave 3) writes the reserved name into `herald.rs` rustdoc, which a naive re-run of the bare grep would read as the ADR having lied. ADR-0050 states the authoring-time result, the durable symbol-scoped invariant, and the `herald.rs` successor state up front; plan 30-03 is sequenced after this plan so the record is written before the prose occurrence exists. |
| T-30-01 | Information Disclosure | ADR text | low | mitigate | No API key, token or secret appears in either ADR; provider and fixture *names* only. |
| T-30-03 | Tampering | scope creep into code | medium | mitigate | Every task asserts `git diff --name-only HEAD~1` lists no `.rs` file. |
| T-30-SC | Tampering | package-manager installs | low | accept | No dependency is added; no `Cargo.toml`/`Cargo.lock` change exists in this plan — nothing for the package-legitimacy gate to audit. |

Blocking threshold: `high`. No threat in this plan is rated `high`; no blocking checkpoint is
required. The `costly` reversibility rating on Task 2 is flagged, not gated — CONTEXT.md records
the operator making this exact call on 2026-09-14, so re-asking would gate a decision already made.
</threat_model>

<verification>
- All three ADR files exist with contiguous numbers 0049, 0050, 0051 and the seven required
  headings each, in PROMOTION.md's order.
- The symbol-scoped grep for the reserved officer term across `crates` and `src` returns nothing,
  and any bare-word occurrence there is a `///`/`//!` rustdoc line.
  <!-- planner-discipline-allow: Treasurer -->
- `.planning/decisions/PROMOTION.md` holds exactly one `Next free ADR number` label line, reading
  `0052`, with one dated 2026-09-14 note for this plan.
- `.planning/PROJECT.md` Key Decisions links all three ADRs in ascending order.
- `git diff --name-only` across this plan's commits lists no `.rs` file, no `Cargo.toml` and no
  migration register. <!-- planner-discipline-allow: MIGRATION.md -->
</verification>

<success_criteria>
- VOCAB-04 satisfied: the one-page reservation ADR exists with the role's scope, the
  installs-not-replaces rule and its install point, the Milestone 14 owner, the 0/0 in-tree grep
  recorded verbatim together with the durable symbol-scoped invariant and the successor-state note
  about `herald.rs`, and the downstream guardrail with both rejected alternatives.
- VOCAB-07 satisfied: the versioning ADR records the Phases-31-33-only supersession of X-03 with
  the operator's 2026-09-14 rationale, keeps the migration-register and allowlist rows as
  documentation rather than shims, and is linked from `PROJECT.md` Key Decisions.
- Three atomic conventional commits, each ending with the required `Co-Authored-By` trailer.
</success_criteria>

<output>
Create `.planning/phases/30-token-economy-vocabulary-commissary-anchoring/30-02-SUMMARY.md` when done.
</output>
