---
phase: 34-documentation-currency-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/34-documentation-currency-audit/34-AUDIT.md
  - .planning/phases/34-documentation-currency-audit/34-EVIDENCE.md
  - .planning/phases/34-documentation-currency-audit/34-signals.sh
  - .planning/phases/34-documentation-currency-audit/34-check.sh
  - .planning/phases/34-documentation-currency-audit/COVERAGE.md
autonomous: true
requirements: [CURR-01, CURR-02, CURR-03, CURR-04, CURR-05]
must_haves:
  truths:
    - "The five CURR-01…CURR-05 requirement IDs exist in REQUIREMENTS.md and the ROADMAP Phase 34 Requirements line names them (D-20)."
    - "34-AUDIT.md exists as the single canonical inventory with all seven D-01 sections present in order, and its header records HEAD SHA, date and every tool version (D-01, D-23)."
    - "All 93 docs/src/**/*.md paths are present as seeded rows whose findings cell states the page has not been swept, so absence can never read as checked (D-05, D-00b)."
    - "One fully-worked row exists in each of the three tables, proving the row schema end-to-end before any sweep expands it (D-06, D-13, D-17)."
    - "34-check.sh runs green in seed mode, so every later plan has a mechanical completeness gate (CURR-01…05)."
  artifacts:
    - ".planning/phases/34-documentation-currency-audit/34-AUDIT.md"
    - ".planning/phases/34-documentation-currency-audit/34-EVIDENCE.md"
    - ".planning/phases/34-documentation-currency-audit/34-signals.sh"
    - ".planning/phases/34-documentation-currency-audit/34-check.sh"
    - ".planning/phases/34-documentation-currency-audit/COVERAGE.md"
  key_links:
    - "REQUIREMENTS.md CURR-0n rows ↔ ROADMAP Phase 34 Requirements line ↔ every plan's requirements frontmatter."
    - "34-AUDIT.md table rows ↔ 34-EVIDENCE.md numbered rows by evidence anchor (D-02)."
    - "34-signals.sh nine signal classes ↔ every mdBook row findings cell (D-07)."
  prohibitions:
    - statement: "No file outside .planning/ is created, modified or deleted (ROADMAP SC5, D-22)."
      status: required
      verification: "git status --porcelain -- . ':!.planning' prints nothing, recorded in 34-EVIDENCE.md."
    - statement: "No currency verdict is settled by file existence, modification time or git log (D-00b)."
      status: required
      verification: "Every non-seeded findings cell names a producing command; 34-check.sh rejects a settled verdict whose findings cell is empty."
---

<objective>
Mint the `CURR-*` requirement family and prove the whole audit spine end-to-end on one thin
vertical slice: requirement ID → audit skeleton → one real measured row in each of the three
tables → evidence anchor → mechanical completeness gate → read-only proof.

Purpose: every later plan in this phase appends rows to artifacts whose schema, ID scheme and
verification harness are already proven. If the row schema is wrong, it is wrong after one
commit, not after five sweeps have written 250 rows against it.
Output: `34-AUDIT.md` (skeleton + 93 seeded mdBook rows + 3 worked rows), `34-EVIDENCE.md`,
`34-signals.sh`, `34-check.sh`, `COVERAGE.md`, and the `CURR-01…05` IDs in REQUIREMENTS.md and
ROADMAP.md.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-documentation-currency-audit/34-CONTEXT.md
@.planning/phases/34-documentation-currency-audit/34-RESEARCH.md
@.planning/phases/34-documentation-currency-audit/34-PATTERNS.md

Deliberately NOT referenced: `.planning/STATE.md` (1,108 lines) and `.planning/PROJECT.md`.
Neither is needed to execute this plan and STATE.md alone would consume a quarter of the
executor's context. The Phase 32/33 close notes STATE.md carries (73 warnings, 14 unresolved
links) are superseded by 34-RESEARCH.md's live per-crate measurement anyway.
</context>

<artifacts_this_phase_produces>
Phase 34 writes only inside `.planning/phases/34-documentation-currency-audit/`, plus the two
`.planning/` root files this plan's minting task edits. The complete set:

| Artifact | Created by | Purpose |
|---|---|---|
| `34-AUDIT.md` | 34-01 (skeleton), appended by 34-02…34-09 | The single canonical inventory (D-01), seven sections in D-01 order |
| `34-EVIDENCE.md` | 34-01 (header + table), appended by every later plan | Numbered command → result → verdict record (D-02), the `33-CI-EVIDENCE.md` shape |
| `34-evidence/` | 34-03, 34-06, 34-07, 34-08 | Raw verbatim tool captures too large to inline (D-02 discretion) |
| `deferred-items.md` | 34-09 | Non-documentation findings register (D-19) |
| `34-signals.sh` | 34-01 | Runs the nine D-07 signal classes against one page |
| `34-check.sh` | 34-01 | Completeness / ID-uniqueness / SC5 gate for every plan |
| `COVERAGE.md` | 34-01 | Reasoned no-external-API declaration for the seal-time gate |
| `CURR-01…CURR-05` | 34-01 | Requirement IDs in REQUIREMENTS.md + ROADMAP Phase 34 line |
| `MB-nn` / `RD-nn` / `EX-nn` | 34-03…34-08 (rows), 34-09 (work lists) | The stable per-finding ID scheme (D-03), never renumbered |

No script, log or artifact is written outside `.planning/` — see the prohibitions block.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end audit spine — mint CURR-01…05, build the inventory skeleton, prove one row per table</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/phases/34-documentation-currency-audit/34-AUDIT.md, .planning/phases/34-documentation-currency-audit/34-EVIDENCE.md, .planning/phases/34-documentation-currency-audit/34-signals.sh, .planning/phases/34-documentation-currency-audit/34-check.sh</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — read the `### Commissary In-Tree Adoption (…, epic `COMM`)` section (around line 440), the `## v2 Requirements` header (around line 470), the `## Traceability` table tail (around line 570) and the `**Coverage:**` bullets plus the trailing `*Extended: …*` footer line (around line 590). The new section and rows must copy this exact bullet and row form.
    - `.planning/ROADMAP.md` — the `### Phase 34:` section only; the `**Requirements**:` line is the single line this task edits there.
    - `.planning/phases/34-documentation-currency-audit/34-CONTEXT.md` — D-01 (seven-section order), D-02, D-03 (ID scheme), D-04 (S/M/L), D-05, D-06, D-07 (eight signal classes plus the ninth), D-20 (prefix), D-22, D-23.
    - `.planning/phases/34-documentation-currency-audit/34-PATTERNS.md` — the `16-DOCS-01-VERDICTS.md` header/method/signal-class table excerpts and the `33-CI-EVIDENCE.md` numbered-table excerpt. These are the shapes to reproduce.
    - `.planning/milestones/v0.8.0-phases/16-documentation-currency-the-architecture-gap/16-DOCS-01-VERDICTS.md` — the Method section and one real row, for voice and the producing-command-per-signal discipline.
    - `docs/src/appendix/doc-coverage-report.md` — the known-answer mdBook page (its line 18 asserts the docs build has no warnings).
    - `crates/paladin-memory/src/token_counter/mod.rs` lines 1-10 — the known-answer rustdoc case.
    - `examples/README.md` — the known-answer example case (its MSRV sentence).
    - `Cargo.toml` `[workspace.package]` — the real `rust-version` value to compare the README against.
  </read_first>
  <action>
Do the four steps in order; each is a prerequisite for the next.

STEP 1 — mint the requirement family (D-20).
In `.planning/REQUIREMENTS.md`, insert a new section
`### Documentation Currency Audit (Release Readiness — Phases 34-36, epic \`CURR\`)` under
`## v1 Requirements`, immediately BEFORE the `## v2 Requirements` header (that is, after the
`COMM-04` bullet ends the COMM section). Give it a one-line preamble stating that the prefix also
carries Phases 35-36 per ROADMAP ("assigned at planning under the Phase 34 prefix") and that those
phases mint their own `CURR-nn` numbers when planned. Then five bullets in the file's existing
form — `- [ ] **CURR-0n**: <text>` — each ending with a parenthetical citing the ROADMAP Phase 34
success-criterion number and the CONTEXT.md decision ids it satisfies, in the same style COMM-04
uses for "(PRD R4; roadmap-time addition; SHIP-01…04 re-verification)". The five, one per ROADMAP
success criterion:
CURR-01 — every `.md` under `docs/src/` carries a `current`/`stale`/`missing` verdict against the
Phase 22-33 shipped surface, and every `stale`/`missing` verdict cites the phase and the shipped
item (SC1; D-05, D-06, D-07, D-08, D-09, D-10).
CURR-02 — every `warning:` line from the default-feature `cargo doc` run and every error from the
per-crate `-D warnings --all-features` sweep is enumerated with crate, file and line, with the
`ci.yml` lint-job command quoted verbatim as the bar (SC2; D-00a, D-12, D-13, D-14, D-15).
CURR-03 — every program under `examples/`, every `crates/doc-examples` module and
`crates/paladin-llm/examples/live_vendor_smoke.rs` carries a build status under the CI feature-set
split plus a currency verdict (SC3; D-16, D-17, D-18).
CURR-04 — the inventory is partitioned into sized Phase 35 (`MB-nn`) and Phase 36 (`RD-nn`,
`EX-nn`) work lists, and any finding that is neither documentation nor an example is routed to the
deferred register (SC4; D-03, D-04, D-19, D-21).
CURR-05 — the audit is read-only against the tree: the phase's commits touch only `.planning/`
(SC5; D-00c, D-22, D-23).
In the same file's `## Traceability` table, append five rows after `| COMM-04 | Phase 33 | Complete |`,
each of the form `| CURR-0n | Phase 34 | Pending |`. Update the `**Coverage:**` bullets: v1
requirements total goes from 66 to 71 (add a parenthetical "5 added 2026-09-17 for Phases 34-36"),
and "Mapped to phases" goes from 66 to 71, "Unmapped: 0 ✓" unchanged. Append to the existing
trailing `*Extended: …*` footer line, keeping its current text intact and adding
`; CURR-01…05 added 2026-09-17 for Phase 34 (Release Readiness)`.
In `.planning/ROADMAP.md`, change ONLY the Phase 34 `**Requirements**:` line to
`**Requirements**: CURR-01, CURR-02, CURR-03, CURR-04, CURR-05 (minted at planning 2026-09-17; prefix shared with Phases 35-36)`.
Do not touch the Phase 35 or Phase 36 `**Requirements**:` lines and do not edit any other part of
ROADMAP.md — the orchestrator annotates plan waves there after planning.

STEP 2 — write `34-signals.sh` (the D-07 evidence engine).
A bash script taking one argument, a path under `docs/src/`. It prints a compact block, one
labelled line per signal class, each line carrying the literal command it ran and its result
(hits or the word none). The nine classes, reproducing the `16-DOCS-01-VERDICTS.md` table and
adding D-07's ninth: (1) version strings, `grep -nE 'v?[0-9]+\.[0-9]+\.[0-9]+'` compared against
root `Cargo.toml` `[workspace.package] version`; (2) dependency pins,
`grep -nE '^[a-z-]+ *= *\{? *version'` compared against the owning crate manifest; (3) crate
names, `grep -noE 'paladin-[a-z-]+' | sort -u` compared against `ls crates/`; (4) module/source
paths, `grep -noE '(crates|src)/[A-Za-z0-9_/.-]+\.rs'` each fed to `test -f`; (5) make targets,
`grep -noE 'make [a-z-]+'` compared against `grep -oE '^[a-z-]+:' Makefile`; (6) workflow and job
names, `grep -noE '[a-z-]+\.yml'` compared against `ls .github/workflows/`; (7) error types,
`grep -noE '[A-Z][A-Za-z]*Error(::[A-Za-z]+)?'` compared against a `grep -rn` in `crates/` and
`src/`; (8) feature flags, `grep -noE '"[a-z-]+"' ` narrowed to tokens that also appear under a
`[features]` heading in any workspace manifest; (9) the shipped-surface hit — `grep -nFf` against
a token file the script reads from `34-shipped-tokens.txt` if that file exists, and otherwise
prints `class 9: SKIPPED — 34-shipped-tokens.txt not yet written by plan 34-02`. Class 9 must
degrade to that explicit SKIPPED line rather than failing, because plan 34-02 creates the token
file. The script exits 0 whether or not signals fire — it is a measurement tool, not a gate.

STEP 3 — write `34-AUDIT.md` and `34-EVIDENCE.md`.
`34-AUDIT.md` opens with a Method statement in the `16-DOCS-01-VERDICTS.md` voice: it is the
single D-01 canonical inventory; a verdict is settled by content, never by file existence or
modification time (D-00b); a settled verdict with an empty findings cell is invalid; name which
plan appends which section (34-02 §1, 34-03/04/05 §2, 34-06/07 §3, 34-08 §4, 34-09 §5-§7).
Then a measurement header carrying: the HEAD SHA from `git rev-parse HEAD`, the date, and a
verbatim block of `cargo --version`, `rustc --version`, `mdbook --version`,
`mdbook-linkcheck --version`, `mdbook-mermaid --version` output, each annotated with the pin it is
compared against (`rust-toolchain.toml` `channel`, and `docs.yml`'s `--version` pins for the three
mdBook-family tools). The header also records the D-23 invariance argument explicitly: because
every Phase 34 commit touches only `.planning/` (SC5, proven per commit by the git diff in
STEP 4), the source tree every row measures is identical at every Phase 34 SHA, so a later plan's
`git rev-parse HEAD` differing from this one does not invalidate a row — state that as the
recorded reading of D-23, alongside the rule that if the branch moves for any other reason,
Phases 35/36 re-run the D-12/D-16 commands and diff rather than trust the counts. Record the
toolchain-drift question D-12 leaves open as closed with the RESEARCH.md finding: `rust-toolchain.toml`
overrides the lint job's `dtolnay/rust-toolchain@stable` action, so both environments run the same
version — no drift.
Then the seven D-01 sections as headings, in order: §1 Shipped-surface checklist (D-08); §2 mdBook
verdict table (D-05/D-06/D-07); §3 Rustdoc findings table (D-12/D-13/D-14/D-15); §4 Examples table
(D-16/D-17/D-18); §5 Phase 35 work list (D-21); §6 Phase 36 work list (D-21); §7 Deferred routing
(D-19). Each empty section carries a one-line note naming the plan that fills it.
§2 carries its column header `| # | Page | Verdict | Findings (signal class → cmd → result) | Cites (Phase N — item (REQ)) | MB ID(s) | Size |` and then one seeded row for every path returned
by `find docs/src -name '*.md' | sort` — 93 rows expected, but generate from the live `find`, never
from a copied list. Each seeded row's Verdict cell reads `pending` and its Findings cell reads
exactly the placeholder literal this task's acceptance criteria specify. That placeholder is the
`16-DOCS-01-VERDICTS.md` concurrency rule made mechanical: an unswept page must never be
indistinguishable from a checked one.
§3 carries its column header `| RD ID | Run | Crate | File:line | Kind | Message (verbatim first line) | Location source | Evidence anchor | Size |` and §4 carries
`| EX ID | Program / module | Build invocation | Build status | Currency verdict | Obsolete-API hits | Claimed capability → tree check | Evidence anchor | Size |`, each followed by a one-line note that rows land in plans 34-06/07 and 34-08 respectively.
Also write the ID scheme down once, under the Method statement: `MB-nn` mdBook → Phase 35,
`RD-nn` rustdoc → Phase 36, `EX-nn` examples → Phase 36, numbered in table order, never
renumbered, because Phases 35 and 36 close items by ID (D-03). Record the sizing rubric verbatim
from D-04 (S = one-line or one-link fix, single rustdoc link repair, one rename; M = section
rewrite or new section, rustdoc block on an undocumented item family, signature-level example
update; L = new page, new example, or rewrite of more than half a page or example).
`34-EVIDENCE.md` follows the `33-CI-EVIDENCE.md` shape from 34-PATTERNS.md: a header with phase,
branch, HEAD SHA, date and the same toolchain block, a statement that its verdict column
distinguishes a gate this phase enforces from a baseline it merely carries (D-00c makes this
phase's fix set empty by construction, so almost every row is CARRIED, not a gate), then the
numbered table `| # | Command | Result | Verdict |` with the rows this task produces.

STEP 4 — prove the three row schemas and the read-only gate.
Work one real row per table, choosing the three known-answer cases CONTEXT.md names as method
self-tests, so a wrong schema fails here rather than in a sweep.
mdBook row — `docs/src/appendix/doc-coverage-report.md`. Run `34-signals.sh` against it, then run
the default-feature `cargo doc --workspace --no-deps` command from `ci.yml`'s "Check documentation"
step, teeing to `34-evidence/34-01-cargo-doc-default.txt`, and count its `warning:` lines. The page
asserts the docs build succeeds without warnings; the measured count contradicts it. Record the
row with verdict `stale`, a findings cell naming the signal-class commands actually run and the
`cargo doc` count, the citation, `MB-01`, and size per D-04. This is the first `MB` ID and fixes
the numbering origin.
rustdoc row — the `HeuristicTokenCounter` intra-doc link. Run
`RUSTDOCFLAGS="-D warnings" cargo doc -p paladin-memory --all-features --no-deps`, tee to
`34-evidence/34-01-rustdoc-memory.txt`. This diagnostic has no `-->` span because the link lives in
a `//!` comment, so recover the location with `grep -n` for the quoted snippet against
`crates/paladin-memory/src/token_counter/mod.rs`. The row must end with a real `file:line`
(expected `crates/paladin-memory/src/token_counter/mod.rs:3`, matching `.planning/WINDOWS.md` row
37) and its Location source cell must read `grep recovery`, not `rustdoc -->`. This proves the
P-01 recovery method that plans 34-06 and 34-07 depend on.
examples row — `examples/README.md`. Compare its stated minimum Rust version against
`Cargo.toml` `[workspace.package] rust-version`. Record `EX-01` with the two values and size per
D-04.
Then write `34-check.sh`: a bash gate taking a mode argument, `--seed` or `--final`. In both modes
it asserts (a) every path from `find docs/src -name '*.md'` appears in a §2 row — implement as a
`comm -23` of the sorted find output against the sorted set of page paths extracted from
`34-AUDIT.md`, and require the result to be empty; (b) every `MB-`/`RD-`/`EX-` ID in the file is
unique — `grep -oE '(MB|RD|EX)-[0-9]+' | sort | uniq -d` must be empty; (c) no settled verdict row
(a §2 row whose Verdict cell is `current`, `stale` or `missing`) has a findings cell that is empty
or is the seeded placeholder — this is the mechanical form of D-00b; (d) the SC5 proof,
`git status --porcelain -- . ':!.planning'` and
`git diff --stat $(git merge-base HEAD main)..HEAD -- . ':!.planning'` both empty. In `--final`
mode it additionally asserts (e) no §2 row still carries the seeded placeholder, (f) every
`examples/*.rs` path and every `crates/doc-examples/src/*.rs` path except `lib.rs` appears in a §4
row, and (g) every `MB-`/`RD-`/`EX-` ID that appears in §2/§3/§4 also appears in §5, §6 or
`deferred-items.md`. The script prints one PASS/FAIL line per assertion and exits non-zero if any
assertion fails. Run it in `--seed` mode and require exit 0.
Finally append to `34-EVIDENCE.md` a numbered row for each command this task ran, and the SC5 row
carrying the verbatim (empty) output of the git diff.
  </action>
  <verify>
    <automated>cd /workspace && bash .planning/phases/34-documentation-currency-audit/34-check.sh --seed && grep -c '^- \[ \] \*\*CURR-0' .planning/REQUIREMENTS.md | grep -qx 5 && grep -q 'CURR-01, CURR-02, CURR-03, CURR-04, CURR-05' .planning/ROADMAP.md && test "$(grep -c 'not yet swept' .planning/phases/34-documentation-currency-audit/34-AUDIT.md)" = "$(find docs/src -name '*.md' | wc -l)" && grep -q 'crates/paladin-memory/src/token_counter/mod.rs:3' .planning/phases/34-documentation-currency-audit/34-AUDIT.md && test -z "$(git status --porcelain -- . ':!.planning')" && echo TRACER-OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^- \[ \] \*\*CURR-0' .planning/REQUIREMENTS.md` equals 5.
    - `grep -c '^| CURR-0[0-9] | Phase 34 | Pending |' .planning/REQUIREMENTS.md` equals 5.
    - `grep -n 'CURR-01' .planning/ROADMAP.md` hits the Phase 34 `**Requirements**:` line, and `grep -c 'TBD — assigned at planning; one new prefix' .planning/ROADMAP.md` equals 0.
    - `grep -c 'CURR-01…05 added 2026-09-17' .planning/REQUIREMENTS.md` equals 1 and the pre-existing `*Extended: 2026-09-14 …*` text is still present in the same line.
    - `git diff --stat -- .planning/ROADMAP.md | tail -1` shows exactly 1 file changed with 1 insertion and 1 deletion.
    - `.planning/phases/34-documentation-currency-audit/34-AUDIT.md` contains the seven section headings in D-01 order; `grep -c '^## §' ` (or the equivalent heading form actually used) returns 7.
    - Every seeded §2 row's Verdict cell is exactly `pending` and its Findings cell is exactly the literal `pending — not yet swept (no signal class run)`.
    - `grep -c 'not yet swept' 34-AUDIT.md` equals the live `find docs/src -name '*.md' | wc -l` count (93 at plan time).
    - `comm -23 <(find docs/src -name '*.md' | sort) <(grep -oE 'docs/src/[A-Za-z0-9_./-]+\.md' 34-AUDIT.md | sort -u)` prints nothing.
    - 34-AUDIT.md §2 contains a row for `docs/src/appendix/doc-coverage-report.md` whose Verdict cell is `stale`, whose findings cell names at least three signal-class commands verbatim, and which carries the ID `MB-01`.
    - 34-AUDIT.md §3 contains a row carrying `crates/paladin-memory/src/token_counter/mod.rs:3` and the literal `grep recovery` in its Location source cell.
    - 34-AUDIT.md §4 contains a row for `examples/README.md` naming both the README's stated minimum Rust version and the `Cargo.toml` `[workspace.package] rust-version` value.
    - `bash 34-signals.sh docs/src/introduction.md` exits 0 and prints nine labelled class lines, with class 9 reporting the SKIPPED state.
    - `bash 34-check.sh --seed` exits 0 and prints a PASS line for each of its four seed-mode assertions.
    - `34-EVIDENCE.md` contains a numbered row for every command run by this task, including the SC5 git diff row.
    - `git status --porcelain -- . ':!.planning'` prints nothing.
  </acceptance_criteria>
  <done>CURR-01…05 exist in REQUIREMENTS.md, ROADMAP.md and this plan's frontmatter; 34-AUDIT.md carries the header, the seven sections, 93 seeded mdBook rows and exactly one fully-worked row in each of the three tables; 34-signals.sh and 34-check.sh run green; nothing outside .planning/ changed.</done>
  <reversibility rating="costly">The `MB-nn`/`RD-nn`/`EX-nn` numbering origin fixed here is cited by Phases 35 and 36 (D-03) — renumbering after those phases plan breaks their citations. Not one-way: nothing outside `.planning/` is touched, so the whole artifact set can be rewritten by a later commit.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Record the no-external-API declaration and the spec-less-probe skip</name>
  <files>.planning/phases/34-documentation-currency-audit/COVERAGE.md, .planning/phases/34-documentation-currency-audit/34-AUDIT.md</files>
  <read_first>
    - `.planning/phases/34-documentation-currency-audit/34-AUDIT.md` — as written by Task 1; the Method statement is where the probe-skip note is appended.
    - `.planning/phases/32-unified-token-primitives/COVERAGE.md` if it exists — the prior-phase declaration form. If it does not exist, use the one-line prose form given below.
  </read_first>
  <action>
Write `.planning/phases/34-documentation-currency-audit/COVERAGE.md` containing a title line and a
single prose declaration line beginning `No external API integration:` followed by the reason —
that Phase 34 is a read-only documentation measurement over an already-shipped tree, that the only
APIs it names (the platform API, the provider APIs) are documentation subjects rather than
integration targets, and that the phase adds no dependency, no network call and no new
input-handling code. Both the plan-time detector runs (the api-coverage scan and the
assumption-delta scan) returned `detected: false` on this phase's ROADMAP section; record that
fact in the file so the seal-time re-scan over the PLAN bodies has the reasoned declaration it
needs even if a PLAN body's prose trips the term vocabulary. Keep the file free of any pipe table
— a pipe table in this file parses as a coverage matrix and would be validated as one.
Then append two short recorded-choice notes to `34-AUDIT.md`'s Method statement: (1) the spec-less
probe fallback was skipped this run because no SPEC.md exists for this phase and no requirement
IDs existed at plan time, so the phase's `must_haves` truths were derived directly from the five
ROADMAP success criteria and the CONTEXT.md decisions rather than from probe-derived predicates;
(2) the reversibility reading for D-22 — CONTEXT.md rates it one-way, and the plans carry that
rating on the tasks that implement it, but no blocking human checkpoint is inserted, because the
run is unattended and D-22's enforcement is itself a mechanical recorded `git diff` that every task
verify re-runs. A human gate that cannot be answered would stall the audit without adding any
protection the diff does not already give.
  </action>
  <verify>
    <automated>cd /workspace && test -f .planning/phases/34-documentation-currency-audit/COVERAGE.md && grep -q '^No external API integration:' .planning/phases/34-documentation-currency-audit/COVERAGE.md && test "$(grep -c '^|' .planning/phases/34-documentation-currency-audit/COVERAGE.md)" = "0" && grep -qi 'probe fallback' .planning/phases/34-documentation-currency-audit/34-AUDIT.md && test -z "$(git status --porcelain -- . ':!.planning')" && echo COVERAGE-OK</automated>
  </verify>
  <acceptance_criteria>
    - `COVERAGE.md` exists and contains a line starting with the literal `No external API integration:` followed by a reason clause.
    - `grep -c '^|' COVERAGE.md` equals 0 (no pipe table anywhere in the file).
    - `COVERAGE.md` names both detector results (api-coverage and assumption-delta, both not detected).
    - `34-AUDIT.md` Method section contains the probe-fallback skip note and the D-22 no-checkpoint reasoning.
    - `git status --porcelain -- . ':!.planning'` prints nothing.
  </acceptance_criteria>
  <done>The seal-time api-coverage gate has a reasoned declaration to read, and both recorded choices (probe skip, D-22 checkpoint omission) are visible in the audit itself.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository working tree → git commit | The only place this phase can do harm: a commit that carries a file outside `.planning/` falsifies SC5 and the read-only premise Phases 35-37 plan against |
| build/test tool output → `34-EVIDENCE.md` | Captured command output is committed verbatim; anything the environment leaks into that output is published |
| observation (mtime, git log) → recorded verdict | A verdict asserted without a producing command is indistinguishable on disk from a measured one |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-01 | Tampering | phase commits (`git add`) | high | mitigate | Every task's `<verify>` runs `git status --porcelain -- . ':!.planning'` and requires empty; `34-check.sh` repeats it plus the merge-base `git diff --stat` in both modes (D-22) |
| T-34-02 | Information Disclosure | `34-EVIDENCE.md`, `34-evidence/*.txt` | high | mitigate | Only `cargo`/`mdbook`/`grep` output is teed, never an environment dump; no task exports or echoes a provider key; `live_vendor_smoke` is built, never run (D-16 asks for build status only); before commit, captures are grepped for key-shaped tokens (`sk-`, `_API_KEY=`) and any hit is redacted before the surrounding text is truncated, per the house redaction rule in `crates/paladin-llm/src/redaction.rs` |
| T-34-03 | Repudiation | `34-AUDIT.md` verdict rows | medium | mitigate | `34-check.sh` assertion (c) fails any settled verdict whose findings cell is empty or still the seeded placeholder; the seeded placeholder makes "not looked at" visibly distinct from `current` (D-00b) |
| T-34-04 | Tampering (supply chain) | package installs | low | accept | This phase installs no package — every tool it invokes (`cargo`, `mdbook`, `mdbook-linkcheck`, `mdbook-mermaid`, the `scripts/*.sh` gates) is already present and pinned; no `cargo install`, `npm`, `pip` or `cargo add` appears in any task, so there is no new package to audit |
| T-34-05 | Denial of Service | devcontainer disk / wall clock | low | accept | The per-crate rustdoc sweep and the example builds consume `target/` space and ~10 minutes combined; both are bounded, gitignored, and measured in 34-RESEARCH.md |
</threat_model>

<verification>
- `bash .planning/phases/34-documentation-currency-audit/34-check.sh --seed` exits 0.
- `grep -c '^- \[ \] \*\*CURR-0' .planning/REQUIREMENTS.md` equals 5; five matching Traceability rows exist.
- `grep -n 'CURR-01' .planning/ROADMAP.md` hits only the Phase 34 Requirements line.
- 34-AUDIT.md carries the measurement header, the seven D-01 sections, 93 seeded rows and three worked rows.
- `git status --porcelain -- . ':!.planning'` is empty.
</verification>

<success_criteria>
The audit spine is proven end-to-end on one slice: a requirement ID exists, the inventory file
exists with every section and every page seeded, one measured row in each table demonstrates the
schema (including the P-01 grep-recovered `file:line` that plans 34-06/07 depend on), the
completeness gate runs green, and the read-only proof is recorded. Any later plan that breaks the
schema breaks `34-check.sh`.
</success_criteria>

<output>
Create `.planning/phases/34-documentation-currency-audit/34-01-SUMMARY.md` when done.
</output>
