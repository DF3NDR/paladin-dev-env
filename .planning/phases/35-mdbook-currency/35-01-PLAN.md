---
phase: 35-mdbook-currency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/35-mdbook-currency/deferred-items.md
  - crates/doc-examples/src/superstep_engine.rs
  - crates/doc-examples/src/lib.rs
  - docs/src/user-guides/superstep-engine.md
  - docs/src/SUMMARY.md
autonomous: true
requirements: [CURR-06, CURR-07, CURR-08]
must_haves:
  truths:
    - "D-00g: `.planning/REQUIREMENTS.md` contains one `- [ ] **CURR-0N**:` line for each of CURR-06, CURR-07, CURR-08, CURR-09, CURR-10 inside the existing `### Documentation Currency Audit` block, each citing its ROADMAP Phase 35 success criterion"
    - "D-00g: the ROADMAP Phase 35 `**Requirements**:` line names CURR-06, CURR-07, CURR-08, CURR-09, CURR-10 and no longer reads TBD"
    - "D-07: `docs/src/user-guides/superstep-engine.md` exists, its first line is an H1, and its header carries the literal `**Since:** v0.10.0 (Phase 22)`"
    - "D-07: `docs/src/SUMMARY.md` contains a nav entry pointing at `user-guides/superstep-engine.md` positioned after the `Maneuver Flow DSL` entry and before the `Control Flow` entry"
    - "D-09: `crates/doc-examples/src/superstep_engine.rs` exists with four `// ANCHOR:` regions (`build_graph`, `configure_limits`, `run_engine`, `inspect_waypoints`) and `crates/doc-examples/src/lib.rs` declares `pub mod superstep_engine;`"
    - "D-09: every `{{#include}}` on the engine page resolves to an anchor in `crates/doc-examples/src/superstep_engine.rs` and `scripts/check-doc-examples.sh` exits 0"
    - "D-00e: `mdbook build docs/` exits 0 and its linkcheck backend reports no broken links after the new page and nav entry land"
    - "D-08: the engine page documents WarGraph/Battlefield merge semantics, Waypoint checkpointing with the (ThreadId, WaypointId) addressing scheme, the three WaypointPort backends, EngineConfig/EngineLimits with their APP_ENGINE_* overrides, WaypointRetentionService, and the graph-fingerprint scheme"
    - statement: "D-08: routing, pause/resume, Aegis and middleware appear on the engine page only as links to control-flow.md, parley-and-chronicle.md, fault-tolerance.md and agent-runtime.md, never re-explained"
      verification: backstop
    - statement: "D-27: the phase-local deferred register records the `contributing-providers.md` observation with a proposed classification rather than a silent fix"
      verification: backstop
  artifacts:
    - docs/src/user-guides/superstep-engine.md
    - crates/doc-examples/src/superstep_engine.rs
    - .planning/phases/35-mdbook-currency/deferred-items.md
  key_links:
    - "`docs/src/SUMMARY.md` nav entry -> `user-guides/superstep-engine.md` (MB-30's audit-assigned position; four dependent pages in plans 35-03 and 35-04 link to this exact path)"
    - "engine page `{{#include ../../../crates/doc-examples/src/superstep_engine.rs:<anchor>}}` -> `crates/doc-examples/src/lib.rs` `pub mod superstep_engine;` -> `cargo check -p paladin-doc-examples` Layer 1 gate"
  prohibitions:
    - statement: "D-01: no page under docs/src is deleted and no entry is removed from docs/src/SUMMARY.md"
      status: recalled
      verification: flagged-unverified
    - statement: "D-26: no existing crates/doc-examples module, support.rs, examples/ program or examples/README.md is edited — Phase 35 only adds modules and lib.rs registrations"
      status: recalled
      verification: flagged-unverified
    - statement: "D-00c: nothing under src/ or crates/ other than crates/doc-examples changes in this phase"
      status: recalled
      verification: flagged-unverified
    - statement: "D-10: no shipped docs/src page cites a phase planning document (NN-CONTEXT.md, NN-RESEARCH.md, NN-AUDIT.md) as a reader-facing source; ADR references under .planning/decisions/ stay permitted through the GitHub blob-URL form"
      status: recalled
      verification: flagged-unverified
---

<objective>
Open Phase 35 by minting its requirement IDs and closing MB-30 — the single `missing page` row in
`34-AUDIT.md` §5 and the one row every other plan's cross-links depend on.

Per D-00a the work list is `34-AUDIT.md` §5's 60 `MB-nn` rows and every ID must be closed by exactly
one plan; this phase's MB-ID -> plan map is:

| Plan | Wave | MB IDs |
|---|---|---|
| 35-01 | 1 | MB-30 |
| 35-02 | 2 | MB-19, MB-20, MB-24, MB-27, MB-28 |
| 35-03 | 2 | MB-06, MB-07, MB-18, MB-21, MB-22, MB-23, MB-25, MB-26, MB-29 |
| 35-04 | 2 | MB-02, MB-03, MB-04, MB-05, MB-08, MB-09, MB-10, MB-11, MB-12 |
| 35-05 | 2 | MB-13, MB-14, MB-15, MB-16, MB-17, MB-35 |
| 35-06 | 2 | MB-31, MB-32, MB-33, MB-34, MB-36 |
| 35-07 | 2 | MB-40, MB-41, MB-42, MB-43, MB-44, MB-45, MB-46 |
| 35-08 | 2 | MB-01, MB-37, MB-39, MB-47, MB-54, MB-55, MB-59, MB-60 |
| 35-09 | 2 | MB-38, MB-48, MB-49, MB-50, MB-51, MB-52, MB-53, MB-56, MB-57, MB-58 |
| 35-10 | 3 | none — closure, CHANGELOG, exit greps, evidence |

Sixty IDs, each in exactly one plan (D-00a: none dropped, merged away or absorbed into another ID's
fix). D-00c governs every correction — the shipped tree outranks any document, and nothing under
`src/` or `crates/` other than `crates/doc-examples` changes. D-00d is the vocabulary rule the phase
closes in plan 35-04; D-00h is the anchor/`{{#include}}` mechanism this plan proves end to end;
D-00g is the requirement-prefix rule this plan executes. D-01's three dispositions (correct /
archive / retitle) classify every row; MB-30 is the only `new` page other than D-05's ADR index.

Per D-10 the four pages that point forward to, or omit, the engine gain a link to this page under
their own IDs — MB-22 (`control-flow.md`, plan 35-03), MB-05 (`introduction.md`), MB-09
(`architecture/overview.md`) and MB-11 (`architecture/domain-model.md`) (all plan 35-04) — which is
why MB-30 is written in wave 1 and those four follow in wave 2.

Purpose: give every later plan a landed link target, a proven page->module->nav->gate->commit path,
and the minted `CURR-06…CURR-10` IDs its frontmatter cites.
Output: the new engine guide, its compile-verified `doc-examples` module, the nav insertion, the
five requirement lines, and the phase-local deferred register.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-mdbook-currency/35-CONTEXT.md
@.planning/phases/35-mdbook-currency/35-RESEARCH.md
@.planning/phases/35-mdbook-currency/35-PATTERNS.md
</context>

<artifacts_this_phase_produces>
New symbols created by Phase 35 (all plans) — exclude these from drift verification against the
pre-phase tree:

- `docs/src/user-guides/superstep-engine.md` (new page, this plan)
- `docs/src/contributing/adr-index.md` (new page, plan 35-05)
- `crates/doc-examples/src/superstep_engine.rs` (new module, this plan) with anchors
  `build_graph`, `configure_limits`, `run_engine`, `inspect_waypoints`
- `crates/doc-examples/src/paladin_agents.rs` (plan 35-02) with anchors `build_agent`,
  `attach_garrison`
- `crates/doc-examples/src/arsenal_tools.rs` (plan 35-02) with anchors `custom_armament`,
  `handoffs`
- `crates/doc-examples/src/herald_output.rs` (plan 35-02) with anchor `custom_herald`
- `crates/doc-examples/src/battalion_patterns.rs` (plan 35-02) with anchor `commander`
- `crates/doc-examples/src/sanctum_vector_memory.rs` (plan 35-02) with anchors `rag_retrieve`,
  `rag_format`
- `pub mod superstep_engine;` (this plan) and `pub mod arsenal_tools; pub mod battalion_patterns;
  pub mod herald_output; pub mod paladin_agents; pub mod sanctum_vector_memory;` (plan 35-02) added
  to `crates/doc-examples/src/lib.rs`
- New `docs/src/SUMMARY.md` entries: the engine-guide entry (this plan) and the
  `[Architecture Decisions](contributing/adr-index.md)` entry (plan 35-05); the existing
  `contributing/architecture-decisions.md` entry is retitled "Adapter Development Guide" (plan
  35-05)
- New `CHANGELOG.md` heading `### Documentation` under `## [0.10.0]` (plan 35-10)
- New requirement IDs `CURR-06`, `CURR-07`, `CURR-08`, `CURR-09`, `CURR-10` (this plan)
- `.planning/phases/35-mdbook-currency/deferred-items.md` (this plan) and
  `.planning/phases/35-mdbook-currency/35-EVIDENCE.md` (plan 35-10)
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: Mint CURR-06…CURR-10 and drive one engine-guide slice end to end</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, crates/doc-examples/src/superstep_engine.rs, crates/doc-examples/src/lib.rs, docs/src/user-guides/superstep-engine.md, docs/src/SUMMARY.md</files>
  <precondition>`mdbook`, `mdbook-mermaid` and `mdbook-linkcheck` resolve on PATH at the `docs.yml` pins 0.4.40 / 0.13.0 / 0.7.7; if any is absent or off-pin, install it with `cargo install <tool> --version <pin> --locked` before running the gate.</precondition>
  <reversibility rating="costly">D-07 rates the page path costly: `docs/src/user-guides/superstep-engine.md` becomes a link target on at least four other pages and in the CHANGELOG, so a later rename is a multi-page edit. The path, the nav title and the nav position are fixed by D-07 and are not re-decided here.</reversibility>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 470-500 (the `### Documentation Currency Audit` block that holds CURR-01…CURR-05) and lines 595-615 (the traceability table)
    - `.planning/ROADMAP.md` §"Phase 35: mdBook Currency" — the five success criteria the five new IDs mirror, and the `**Requirements**: TBD` line to replace
    - `.planning/phases/34-documentation-currency-audit/34-AUDIT.md` §5 row 1 (MB-30) and §2 row 94 plus the "Superstep-engine dedicated-page decision" subsection immediately after the §2 table
    - `docs/src/SUMMARY.md` lines 11-33 (the User Guides section)
    - `docs/src/api-reference/platform-api.md` lines 1-6 (the `**Since:**` / `**Crates:**` header form to copy)
    - `docs/src/user-guides/orchestration.md` lines 14-17 (the illustrative-fragment header note wording)
    - `crates/doc-examples/src/fault_tolerance.rs` lines 1-40 (module header, `#![allow(...)]` line, anchor shape, `crate::support::` import style)
    - `crates/doc-examples/src/lib.rs` (the full `pub mod` list)
    - `crates/doc-examples/Cargo.toml` (available dependencies and features)
    - `examples/war_engine_memory_baseline.rs` lines 1-120 (a live, compiling program using the same engine surface — shape cross-check only, not source to copy)
    - `crates/paladin-battalion/src/engine/graph.rs` (`WarGraph`, `NodeSpec`, `EdgeSpec`, `EngineLimits`)
  </read_first>
  <action>
    Two commits, in order.

    First, mint the requirement IDs per D-00g. Append five entries to the existing
    `### Documentation Currency Audit (Release Readiness — Phases 34-36, epic `CURR`)` block in
    `.planning/REQUIREMENTS.md`, immediately after the CURR-05 entry, in the same
    `- [ ] **CURR-0N**: …` shape CURR-01…CURR-05 use, one per ROADMAP Phase 35 success criterion in
    order: CURR-06 = every item in the Phase 34 mdBook work list closed by a page edit or a new
    page with `docs/src/SUMMARY.md` linking each new page at the audit-assigned nav position
    (SC1; D-00a, D-01, D-07); CURR-07 = `mdbook build docs/` with the linkcheck backend passes with
    zero broken links under the exact `docs.yml` command sequence including `mdbook-mermaid install`
    (SC2; D-00e); CURR-08 = no touched page names a type, function, config key, route or CLI flag
    the v0.10.0 tree does not export, snippets meant to run are compile-verified in
    `crates/doc-examples`, illustrative snippets are marked (SC3; D-11, D-12, D-13, D-14, D-20,
    D-22); CURR-09 = the book's vocabulary matches the three ubiquitous-language lists with the
    exit greps empty or allowlisted (SC4; D-00d, D-17, D-18, D-21); CURR-10 = `CHANGELOG.md`
    `[0.10.0]` carries a Documentation entry summarising the pages added and corrected (SC5; D-25).
    Add the five matching rows to the traceability table that ends at the `CURR-05 | Phase 34 |
    Complete` row, each as `| CURR-0N | Phase 35 | Pending |`, update the "Coverage" counts from 71
    to 76 total and 76 mapped, and append a dated line to the closing `*Extended: …*` footer noting
    CURR-06…CURR-10 added 2026-09-17 for Phase 35. Then replace the ROADMAP Phase 35
    `**Requirements**: TBD — assigned at planning under the Phase 34 prefix` line with
    `**Requirements**: CURR-06, CURR-07, CURR-08, CURR-09, CURR-10 (minted at planning 2026-09-17;
    prefix shared with Phases 34 and 36)`. Commit with
    `git add -- .planning/REQUIREMENTS.md .planning/ROADMAP.md && git commit -q -m "docs(35): mint CURR-06..CURR-10 for the mdBook currency phase" -- .planning/REQUIREMENTS.md .planning/ROADMAP.md`.

    Second, scaffold MB-30 as one thin slice through every layer this phase touches. Create
    `crates/doc-examples/src/superstep_engine.rs` following the `fault_tolerance.rs` module shape:
    a `//!` header naming `docs/src/user-guides/superstep-engine.md` as the page it feeds, the same
    `#![allow(unused_variables, unused_imports, dead_code)]` line, mock dependencies pulled from
    `crate::support::` as an ordinary import (never edited — D-26), and one
    `// ANCHOR: build_graph` / `// ANCHOR_END: build_graph` region wrapping a `pub fn` that builds a
    small cyclic `WarGraph` over a `BattlefieldSchema` and returns
    `Result<WarGraph, Box<dyn std::error::Error>>`. Use the crate split RESEARCH.md Pitfall 1
    confirms: `WarEngine`, `WarGraph`, `EngineLimits` and `EngineError` come from
    `paladin_battalion::engine::{self, graph, node}`; `Battlefield`, `BattlefieldSchema`,
    `DispatchRule`, `FieldName`, `FieldSpec`, `StateDelta`, `Directive`, `NodeId` and `ThreadId`
    come from `paladin_core::platform::container::{battlefield, directive, waypoint}`;
    `InMemoryWaypointStore` comes from `paladin_storage::waypoint::in_memory` and needs no
    `Cargo.toml` change (RESEARCH.md Pitfall 4). Register the module by adding
    `pub mod superstep_engine;` to `crates/doc-examples/src/lib.rs`, keeping the existing
    alphabetical order of the non-`support` entries — this is an addition only (D-26).
    Create `docs/src/user-guides/superstep-engine.md` with the H1 `WarEngine: Battlefield State &
    Superstep Execution`, the `**Since:** v0.10.0 (Phase 22)` marker required by D-07 in the
    `platform-api.md` header form, a `**Crates:**` line naming `paladin-battalion` (`WarEngine`,
    `WarGraph`), `paladin-core` (`Battlefield`, `Waypoint`) and `paladin-storage` (the `WaypointPort`
    backends), the `orchestration.md` illustrative-fragment header note with the version reading
    v0.10.0 per D-20, a one-paragraph intro, and one
    `{{#include ../../../crates/doc-examples/src/superstep_engine.rs:build_graph}}` block. Insert
    the nav entry `- [WarEngine: Battlefield State & Superstep Execution](user-guides/superstep-engine.md)`
    into `docs/src/SUMMARY.md` immediately after the `Maneuver Flow DSL` line and before the
    `Control Flow: Dynamic Routing & Subgraphs` line — the position the audit assigned (D-07). Run
    `mdbook-mermaid install docs/`, then `git status --porcelain -- docs` and restore with
    `git checkout -- docs/` if it reports drift (D-00e), then `mdbook build docs/` and
    `./scripts/check-doc-examples.sh`. Commit with
    `git add -- crates/doc-examples/src/superstep_engine.rs crates/doc-examples/src/lib.rs docs/src/user-guides/superstep-engine.md docs/src/SUMMARY.md && git commit -q -m "docs(35): scaffold the WarEngine superstep-engine guide end to end (MB-30)" -- crates/doc-examples/src/superstep_engine.rs crates/doc-examples/src/lib.rs docs/src/user-guides/superstep-engine.md docs/src/SUMMARY.md`,
    allowing a long timeout because the pre-commit hook runs workspace clippy whenever `.rs` files
    are staged (D-24).
  </action>
  <verify>
    <automated>cd /workspace &amp;&amp; grep -cE '^- \[ \] \*\*CURR-(06|07|08|09|10)\*\*' .planning/REQUIREMENTS.md | grep -qx 5 &amp;&amp; grep -q 'Requirements\*\*: CURR-06, CURR-07, CURR-08, CURR-09, CURR-10' .planning/ROADMAP.md &amp;&amp; grep -q 'pub mod superstep_engine;' crates/doc-examples/src/lib.rs &amp;&amp; grep -q 'user-guides/superstep-engine.md' docs/src/SUMMARY.md &amp;&amp; grep -q '\*\*Since:\*\* v0.10.0 (Phase 22)' docs/src/user-guides/superstep-engine.md &amp;&amp; mdbook build docs/ &amp;&amp; ./scripts/check-doc-examples.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '^- \[ \] \*\*CURR-(06|07|08|09|10)\*\*' .planning/REQUIREMENTS.md` returns `5`
    - `grep -c '^| CURR-\(06\|07\|08\|09\|10\) | Phase 35 |' .planning/REQUIREMENTS.md` returns `5`
    - `.planning/ROADMAP.md` contains the literal `**Requirements**: CURR-06, CURR-07, CURR-08, CURR-09, CURR-10` in the Phase 35 section and the string `TBD — assigned at planning under the Phase 34 prefix` no longer appears in that section
    - `crates/doc-examples/src/lib.rs` contains `pub mod superstep_engine;`
    - `crates/doc-examples/src/superstep_engine.rs` contains `// ANCHOR: build_graph` and `// ANCHOR_END: build_graph`
    - `docs/src/user-guides/superstep-engine.md` contains `**Since:** v0.10.0 (Phase 22)` and one `{{#include ../../../crates/doc-examples/src/superstep_engine.rs:build_graph}}` line
    - `awk '/Maneuver Flow DSL/{m=NR} /superstep-engine.md/{s=NR} /Control Flow: Dynamic Routing/{c=NR} END{exit !(m<s && s<c)}' docs/src/SUMMARY.md` exits 0
    - `mdbook build docs/` exits 0 and its output contains `No broken links found`
    - `./scripts/check-doc-examples.sh` exits 0
    - `git log --oneline -1 -- docs/src/user-guides/superstep-engine.md` subject matches `docs(35): .* (MB-30)`
  </acceptance_criteria>
  <done>CURR-06…CURR-10 exist in REQUIREMENTS.md and the ROADMAP, and one end-to-end slice — new doc-examples module, lib.rs registration, new page with a live `{{#include}}`, nav entry, green `docs.yml` gate, committed — is landed.</done>
</task>

<task type="auto">
  <name>Task 2: Complete the engine guide to its D-08 scope with three further anchors</name>
  <files>crates/doc-examples/src/superstep_engine.rs, docs/src/user-guides/superstep-engine.md</files>
  <read_first>
    - `docs/src/user-guides/superstep-engine.md` (the skeleton Task 1 landed)
    - `crates/doc-examples/src/superstep_engine.rs` (the module Task 1 landed)
    - `.planning/phases/34-documentation-currency-audit/34-AUDIT.md` §2 row 94 and the "Superstep-engine dedicated-page decision" subsection (the per-token evidence the page must answer)
    - `crates/paladin-battalion/src/engine/mod.rs` (`WarEngine`, `RunOutcome`, the `EngineError` enum including `RecursionLimitExceeded`)
    - `crates/paladin-battalion/src/engine/graph.rs` (`EngineLimits` fields and defaults)
    - `crates/paladin-core/src/platform/container/battlefield.rs` (`Battlefield`, `BattlefieldSchema`, `DispatchRule`, `FieldSpec`, `StateDelta` merge semantics)
    - `crates/paladin-core/src/platform/container/waypoint.rs` (`Waypoint`, its `vanguard: Vec<NodeId>` field, `ThreadId`, `WaypointId`, `GRAPH_FINGERPRINT_VERSION`)
    - `src/config/engine.rs` (the app-facing `EngineConfig` and its `impl From<EngineConfig> for EngineLimits`, plus the `APP_ENGINE_*` env bindings)
    - `src/application/services/waypoint_retention.rs` and `crates/paladin-storage/src/waypoint/retention.rs` (`WaypointRetentionService` and the `prune()` free function)
    - `crates/paladin-storage/src/waypoint/{in_memory,sqlite,postgres}.rs` (the three `WaypointPort` backends)
    - `docs/src/user-guides/fault-tolerance.md` (the narrative + `{{#include}}` interleaving to mirror)
  </read_first>
  <action>
    Extend `crates/doc-examples/src/superstep_engine.rs` with three further anchor regions so the
    four read top to bottom as one small program per the D-09 narrative: `configure_limits`
    (construct `EngineLimits` naming `max_supersteps`, `max_node_visits`, `run_timeout_secs`,
    `waypoint_durability` and `max_muster_tasks`), `run_engine` (build a `WarEngine` over the graph
    and an `InMemoryWaypointStore`, run it for one `ThreadId`, match on the run outcome), and
    `inspect_waypoints` (read the resulting Waypoint history back out of the store and show the
    `(ThreadId, WaypointId)` addressing). Keep every anchor a `pub fn` returning
    `Result<_, Box<dyn std::error::Error>>` and keep mock dependencies coming from
    `crate::support::` (D-26: `support.rs` is imported, never edited).

    Write the page body to exactly the D-08 scope and no more, one section per bullet, each
    `{{#include}}`ing the matching anchor where a runnable block belongs and using `rust,ignore`
    fences for conceptual fragments (struct shapes, the fingerprint rule) per D-11: (1) `WarGraph`
    and `Battlefield` typed state with superstep merge semantics, naming `BattlefieldSchema`,
    `FieldSpec`, `DispatchRule` and `StateDelta`; (2) `Waypoint` full-snapshot checkpointing after
    every superstep and the `(ThreadId, WaypointId)` addressing scheme, naming the `vanguard`
    field on `Waypoint` (it is a `Vec<NodeId>` field, not a struct — RESEARCH.md's correction to the
    CONTEXT.md prose); (3) the three `WaypointPort` backends at
    `crates/paladin-storage/src/waypoint/{in_memory,sqlite,postgres}.rs`; (4) `EngineConfig` and
    `EngineLimits` with the `max_supersteps`, `max_node_visits`, `run_timeout_secs`,
    `waypoint_durability` and `max_muster_tasks` knobs, their `APP_ENGINE_*` environment overrides
    and `EngineError::RecursionLimitExceeded`, stating that the app-facing `EngineConfig` lives in
    `src/config/engine.rs` and converts into `EngineLimits`; (5) `WaypointRetentionService` from
    `src/application/services/waypoint_retention.rs` wrapping the `prune()` free function in
    `crates/paladin-storage/src/waypoint/retention.rs`; (6) the graph-fingerprint scheme naming
    `GRAPH_FINGERPRINT_VERSION`, its current `v6` value and what bumps it. Close with a
    "Where to go next" section that links `control-flow.md`, `parley-and-chronicle.md`,
    `fault-tolerance.md` and `agent-runtime.md` — D-08 requires routing, pause/resume, Aegis and
    middleware to be linked, never re-explained here. Per D-16 add no dated correction callout.
    Run `mdbook build docs/` and `./scripts/check-doc-examples.sh`, then commit both files in one
    commit subject `docs(35): complete the WarEngine superstep-engine guide (MB-30)` with the plain
    `git add -- <files> && git commit -q -m … -- <files>` form and a long timeout (D-24).
  </action>
  <verify>
    <automated>cd /workspace &amp;&amp; grep -c '^// ANCHOR: ' crates/doc-examples/src/superstep_engine.rs | grep -qx 4 &amp;&amp; grep -c '{{#include ../../../crates/doc-examples/src/superstep_engine.rs:' docs/src/user-guides/superstep-engine.md | grep -qE '^[2-9]$' &amp;&amp; grep -q 'GRAPH_FINGERPRINT_VERSION' docs/src/user-guides/superstep-engine.md &amp;&amp; grep -q 'WaypointRetentionService' docs/src/user-guides/superstep-engine.md &amp;&amp; grep -q 'APP_ENGINE_' docs/src/user-guides/superstep-engine.md &amp;&amp; mdbook build docs/ &amp;&amp; ./scripts/check-doc-examples.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^// ANCHOR: ' crates/doc-examples/src/superstep_engine.rs` returns `4` and the four names are `build_graph`, `configure_limits`, `run_engine`, `inspect_waypoints`
    - `docs/src/user-guides/superstep-engine.md` contains each of the literals `BattlefieldSchema`, `StateDelta`, `WaypointId`, `vanguard`, `max_supersteps`, `max_node_visits`, `run_timeout_secs`, `waypoint_durability`, `max_muster_tasks`, `APP_ENGINE_`, `RecursionLimitExceeded`, `WaypointRetentionService`, `GRAPH_FINGERPRINT_VERSION`
    - `docs/src/user-guides/superstep-engine.md` contains relative links to `control-flow.md`, `parley-and-chronicle.md`, `fault-tolerance.md` and `agent-runtime.md`
    - `grep -cE '^```rust$' docs/src/user-guides/superstep-engine.md` returns `0` — every non-`{{#include}}` Rust fence on the page is `rust,ignore` per D-11(b)
    - `grep -c 'Corrected 2026-09' docs/src/user-guides/superstep-engine.md` returns `0` per D-16
    - `mdbook build docs/` exits 0 with `No broken links found`; `./scripts/check-doc-examples.sh` exits 0
    - `git log --oneline --grep 'MB-30' | wc -l` returns at least 2
  </acceptance_criteria>
  <done>The engine guide covers the full D-08 surface with four compile-verified anchors and links — not re-explanations — for routing, pause/resume, Aegis and middleware.</done>
</task>

<task type="auto">
  <name>Task 3: Seed the phase-local deferred register</name>
  <files>.planning/phases/35-mdbook-currency/deferred-items.md</files>
  <read_first>
    - `.planning/phases/34-documentation-currency-audit/deferred-items.md` (the Phase 34 D-19 register shape to mirror, and the five entries this phase must not absorb)
    - `.planning/phases/35-mdbook-currency/35-RESEARCH.md` Open Question 2 (the `contributing-providers.md` observation)
    - `.planning/phases/34-documentation-currency-audit/34-AUDIT.md` §2 row 52 (`contributing/contributing-providers.md`, verdict `current`)
  </read_first>
  <action>
    Create `.planning/phases/35-mdbook-currency/deferred-items.md` in the Phase 34 register's shape:
    a short header stating it is the Phase 35 phase-local register created under D-26 and D-27, then
    a table with columns Item, Observed at, Why not fixed here, Proposed classification, Owner.
    Seed it with two rows. Row one is the RESEARCH.md Open Question 2 observation:
    `docs/src/contributing/contributing-providers.md` carries the same relocated-LLM-adapter import
    defect that D-13 fixes elsewhere, at its lines 272 and 367, but the page is settled `current` in
    `34-AUDIT.md` §2 row 52 and carries no `MB-nn`; per D-27 it is recorded with a proposed
    classification (a missed `MB-nn` candidate for a later docs pass) and is not fixed in this
    phase. Row two states the standing D-26 rule so later plans have a place to append: any page fix
    that would need an existing `crates/doc-examples` anchor or `support.rs` changed is a Phase 36
    `EX-nn` pointer, recorded here with the page, the anchor and what the page wanted, with the
    include left in place. Add a closing note that plans 35-02 through 35-09 record their own
    observations under a `## Deferred observations` heading in their SUMMARY rather than editing
    this file directly — they run in parallel worktrees — and that plan 35-10 folds those SUMMARY
    sections into this register. Commit with subject
    `docs(35): seed the Phase 35 deferred register (D-26, D-27)`.
  </action>
  <verify>
    <automated>cd /workspace &amp;&amp; test -f .planning/phases/35-mdbook-currency/deferred-items.md &amp;&amp; grep -q 'contributing-providers.md' .planning/phases/35-mdbook-currency/deferred-items.md &amp;&amp; grep -q 'D-26' .planning/phases/35-mdbook-currency/deferred-items.md &amp;&amp; grep -q 'D-27' .planning/phases/35-mdbook-currency/deferred-items.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/35-mdbook-currency/deferred-items.md` exists and contains a markdown table with the header cells Item, Observed at, Why not fixed here, Proposed classification, Owner
    - The register names `docs/src/contributing/contributing-providers.md` with its line numbers 272 and 367 and cites `34-AUDIT.md` §2 row 52 as the reason it is out of the `MB-nn` list
    - The register cites D-26 and D-27 by ID and names Phase 36 as the owner of the `EX-nn` row
    - `git log --oneline -1 -- .planning/phases/35-mdbook-currency/deferred-items.md` subject starts with `docs(35):`
  </acceptance_criteria>
  <done>The phase-local register exists, carries the one observed-but-not-fixed defect, and tells later plans where to record theirs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository -> published book (`docs/book/html/` via GitHub Pages) | Anything written into `docs/src` is published; a secret or an attacker-controlled link crosses here |
| planning corpus -> shipped page | `.planning/` content quoted into a page becomes public |
| workspace -> `crates/doc-examples` | New compile-checked Rust enters the workspace build graph |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-01 | Information Disclosure | `docs/src/user-guides/superstep-engine.md`, `crates/doc-examples/src/superstep_engine.rs` | low | mitigate | New page and module use mock adapters from `crate::support::` and placeholder values only; no API key, endpoint credential or real environment value is written into either file, and the `APP_ENGINE_*` section documents variable names and defaults, never a live value |
| T-35-02 | Tampering | `docs/` working tree after `mdbook-mermaid install docs/` | low | mitigate | D-00e: run `git status --porcelain -- docs` immediately after the mermaid install and restore with `git checkout -- docs/` if it reports drift, so vendored JS cannot be committed unreviewed |
| T-35-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no registry package: `crates/doc-examples` gains a module using existing first-party path dependencies only, and `35-RESEARCH.md` records the Package Legitimacy Audit as not applicable for that reason. The mdBook tool pins are pre-existing `docs.yml` pins reinstalled at fixed versions with `--locked`, not new dependencies |
</threat_model>

<verification>
- `mdbook-mermaid install docs/` leaves `git status --porcelain -- docs` empty (D-00e)
- `mdbook build docs/` exits 0 with `No broken links found`
- `./scripts/check-doc-examples.sh` exits 0 (Layer 1 compiles the new module)
- `./scripts/check-doc-config.sh` exits 0
- `git log --oneline --grep 'MB-30'` lists the scaffold and completion commits
- `grep -rn '23-CONTEXT.md' docs/src/user-guides/superstep-engine.md` returns nothing (D-10)
</verification>

<success_criteria>
- MB-30 is closed: the engine guide exists at the D-07 path, with the D-07 nav title at the
  audit-assigned position, the D-07 `Since` marker, and the full D-08 surface
- Four compile-verified anchors back the page through `crates/doc-examples/src/superstep_engine.rs`
- CURR-06…CURR-10 exist in `.planning/REQUIREMENTS.md` and on the ROADMAP Phase 35 line
- The phase-local deferred register exists with the D-27 observation recorded
- The full `docs.yml` sequence is green on this plan's final commit
</success_criteria>

<output>
Create `.planning/phases/35-mdbook-currency/35-01-SUMMARY.md` when done, including the D-23 closure
table with one row per MB ID this plan closed: `MB-nn | page | disposition (corrected / archived /
new / retitled) | commit | how the §5 finding was addressed`.
</output>
