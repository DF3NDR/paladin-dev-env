---
phase: 35-mdbook-currency
plan: 02
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - crates/doc-examples/src/lib.rs
  - crates/doc-examples/src/paladin_agents.rs
  - crates/doc-examples/src/arsenal_tools.rs
  - crates/doc-examples/src/herald_output.rs
  - crates/doc-examples/src/battalion_patterns.rs
  - crates/doc-examples/src/sanctum_vector_memory.rs
  - docs/src/user-guides/paladin-agents.md
  - docs/src/user-guides/arsenal-tools.md
  - docs/src/user-guides/herald-output.md
  - docs/src/user-guides/battalion-patterns.md
  - docs/src/user-guides/sanctum-vector-memory.md
autonomous: true
requirements: [CURR-06, CURR-07, CURR-08]
must_haves:
  truths:
    - "D-12: `crates/doc-examples/src/lib.rs` declares `pub mod arsenal_tools;`, `pub mod battalion_patterns;`, `pub mod herald_output;`, `pub mod paladin_agents;` and `pub mod sanctum_vector_memory;`, and no pre-existing `pub mod` line is changed or removed"
    - "D-11a: each of the five pages pulls its corrected constructor or method call through a `{{#include ../../../crates/doc-examples/src/<module>.rs:<anchor>}}` block rather than a hand-written fence"
    - "D-11a: `./scripts/check-doc-examples.sh` exits 0, so every anchor these five pages include compiles against the v0.10.0 tree"
    - "MB-27: `docs/src/user-guides/paladin-agents.md` shows the one-argument `InMemoryGarrison::new(config)` constructor and the list-taking `with_handoffs` builder method, and pins `paladin-ai` at 0.10.0"
    - "MB-19: `docs/src/user-guides/arsenal-tools.md` shows the five-field `ArmamentResult` including `call_id` and `execution_time_ms`, reads tool input from the `arguments` map, and shows `with_handoffs`"
    - "MB-24: `docs/src/user-guides/herald-output.md` documents all seven `Herald` trait methods and its custom-Herald sample implements all seven"
    - "MB-20: `docs/src/user-guides/battalion-patterns.md` uses the builder pattern `orchestration.md` uses instead of a hand-written `Commander::new` call, and pins `paladin-ai` at 0.10.0"
    - "MB-28: `docs/src/user-guides/sanctum-vector-memory.md` spells the retrieval service in camel case and documents the Phase 33 RAG surface — the retrieval result type, the shed-item type, the timeout-bounded retrieval call, the token-counter builder method and the omission marker"
    - "D-24: `git log --oneline --grep 'MB-'` shows one commit per page for MB-19, MB-20, MB-24, MB-27 and MB-28"
    - statement: "D-11b: every Rust fence these five pages keep outside an include is corrected against the live tree and marked illustrative"
      verification: backstop
  artifacts:
    - crates/doc-examples/src/paladin_agents.rs
    - crates/doc-examples/src/arsenal_tools.rs
    - crates/doc-examples/src/herald_output.rs
    - crates/doc-examples/src/battalion_patterns.rs
    - crates/doc-examples/src/sanctum_vector_memory.rs
  key_links:
    - "each page's `{{#include}}` -> its new module anchor -> `pub mod` line in `crates/doc-examples/src/lib.rs` -> `cargo check -p paladin-doc-examples` Layer 1 gate"
    - "`crate::support::` mock adapters -> the five new modules (imported as an ordinary dependency, never edited)"
  prohibitions:
    - statement: "D-26: no existing crates/doc-examples module, support.rs, examples/ program or examples/README.md is edited — Phase 35 only adds modules and lib.rs registrations"
      status: recalled
      verification: flagged-unverified
    - statement: "D-01: no page under docs/src is deleted and no entry is removed from docs/src/SUMMARY.md"
      status: recalled
      verification: flagged-unverified
    - statement: "D-00c: nothing under src/ or crates/ other than crates/doc-examples changes in this phase"
      status: recalled
      verification: flagged-unverified
    - statement: "D-16: no new dated correction callout is added to any page"
      status: recalled
      verification: flagged-unverified
---

<objective>
Close the five user-guide rows whose `34-AUDIT.md` §2 finding is signature-level — MB-27, MB-19,
MB-24, MB-20 and MB-28 — by promoting each broken constructor or method call into a new
`crates/doc-examples` module under D-11(a) and D-12, so the page can no longer drift from the API.

This is the only wave-2 plan that touches `crates/doc-examples/src/lib.rs`. D-12's candidate list
also named `hexagonal-design.md` (MB-10) and `design-patterns.md` (MB-12); both are dropped here
under D-12's own "drop a candidate whose fix is a fragment under D-11(b)" rule — MB-10's defect is a
trait-signature excerpt and MB-12's is a constructor line inside a pattern catalogue, neither a
complete flow — so plan 35-04 corrects them in place as marked fragments instead.

D-00h is the mechanism: anchors in a workspace module, reached from the page through
`{{#include}}`, compiled by `scripts/check-doc-examples.sh` Layer 1. D-26 draws the boundary with
Phase 36: this plan only adds modules and `lib.rs` registrations and never edits an existing
module or `support.rs`. D-20 governs the version pins these same pages carry. D-23 requires the
closure table in the SUMMARY and D-24 requires one commit per page.

Purpose: make SC3's "snippets meant to run are compile-verified" true for the five worst offenders.
Output: five new compile-verified modules and five corrected user-guide pages.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-mdbook-currency/35-CONTEXT.md
@.planning/phases/35-mdbook-currency/35-RESEARCH.md
@.planning/phases/35-mdbook-currency/35-PATTERNS.md
@.planning/phases/35-mdbook-currency/35-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
New symbols created by Phase 35 — exclude from drift verification against the pre-phase tree. This
plan creates: `crates/doc-examples/src/paladin_agents.rs` (anchors `build_agent`,
`attach_garrison`), `crates/doc-examples/src/arsenal_tools.rs` (anchors `custom_armament`,
`handoffs`), `crates/doc-examples/src/herald_output.rs` (anchor `custom_herald`),
`crates/doc-examples/src/battalion_patterns.rs` (anchor `commander`),
`crates/doc-examples/src/sanctum_vector_memory.rs` (anchors `rag_retrieve`, `rag_format`), and the
five matching `pub mod` lines in `crates/doc-examples/src/lib.rs`.

Created by sibling plans and referenced here: `docs/src/user-guides/superstep-engine.md` and
`crates/doc-examples/src/superstep_engine.rs` (plan 35-01), `docs/src/contributing/adr-index.md`
(plan 35-05), `CHANGELOG.md` `### Documentation` under `## [0.10.0]` (plan 35-10),
`.planning/phases/35-mdbook-currency/{deferred-items.md,35-EVIDENCE.md}`, requirement IDs
`CURR-06`…`CURR-10`.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: MB-27 — paladin-agents.md end to end through a new module</name>
  <files>crates/doc-examples/src/paladin_agents.rs, crates/doc-examples/src/lib.rs, docs/src/user-guides/paladin-agents.md</files>
  <precondition>The executor's worktree starts with a cold `target/`, so the first `cargo check -p paladin-doc-examples` inside `scripts/check-doc-examples.sh` can run 15-25 minutes; allow that timeout on the first invocation and expect later runs to be warm.</precondition>
  <read_first>
    - `docs/src/user-guides/paladin-agents.md` (the page as it stands, notably line 36's dependency pin, the Memory — Garrison section near line 273 and the Agent Handoffs section near lines 254-255)
    - `.planning/phases/34-documentation-currency-audit/34-AUDIT.md` §2 row 89 (MB-27's finding) and §5 row 6
    - `crates/paladin-memory/src/garrison/in_memory_garrison.rs` around line 79 (`pub fn new(config: GarrisonConfig) -> Self`)
    - `crates/paladin-core/src/platform/container/garrison.rs` (`GarrisonConfig` constructor shape)
    - `src/application/services/paladin/paladin_builder.rs` around line 859 (`with_handoffs`)
    - `crates/doc-examples/src/fault_tolerance.rs` lines 1-40 (module header, allow line, anchor shape, `crate::support::` import style)
    - `crates/doc-examples/src/support.rs` (the mock constructors available — read only, never edited per D-26)
    - `crates/doc-examples/src/lib.rs`
  </read_first>
  <action>
    Create `crates/doc-examples/src/paladin_agents.rs` in the `fault_tolerance.rs` module shape — a
    `//!` header naming `docs/src/user-guides/paladin-agents.md`, the same
    `#![allow(unused_variables, unused_imports, dead_code)]` line, mocks from `crate::support::` —
    with two anchor regions. `build_agent` builds a Paladin through `PaladinBuilder` and returns
    `Result<_, Box<dyn std::error::Error>>`. `attach_garrison` constructs a `GarrisonConfig`, passes
    it to the one-argument `InMemoryGarrison::new(config)` constructor, attaches it to the builder,
    and adds specialists through `with_handoffs`, which takes the whole `Vec<Arc<Paladin>>` at once.
    Add `pub mod paladin_agents;` to `crates/doc-examples/src/lib.rs`, keeping the existing
    alphabetical order of the non-`support` entries.

    On `docs/src/user-guides/paladin-agents.md`, replace the zero-argument Garrison constructor
    sample in the Memory — Garrison section and the per-call chained specialist form in the Agent
    Handoffs section with `{{#include}}` blocks pointing at the two new anchors; the live builder
    method is `with_handoffs`, not a per-call chainable form. Correct the dependency pin at line 36
    from the 0.5.0-era string to the literal `0.10.0` per D-20, and bump any "Every code example
    targets the current vX workspace" sentence on the page to v0.10.0. Leave the `PaladinError`
    variant list and the `JsonHerald` facade import path alone — the audit confirmed both current.
    Per D-16 add no dated callout. Run `mdbook build docs/` and `./scripts/check-doc-examples.sh`,
    then commit all three files in one commit subject
    `docs(35): compile-verify the Paladin agents guide against the live builder API (MB-27)` with
    the plain `git add -- <files> && git commit -q -m … -- <files>` form and a long timeout, because
    the pre-commit hook runs workspace clippy whenever `.rs` files are staged (D-24).
  </action>
  <verify>
    <automated>cd /workspace &amp;&amp; grep -q 'pub mod paladin_agents;' crates/doc-examples/src/lib.rs &amp;&amp; grep -q 'with_handoffs' crates/doc-examples/src/paladin_agents.rs &amp;&amp; grep -q 'InMemoryGarrison::new(' crates/doc-examples/src/paladin_agents.rs &amp;&amp; grep -q '{{#include ../../../crates/doc-examples/src/paladin_agents.rs:' docs/src/user-guides/paladin-agents.md &amp;&amp; ! grep -qE '"0\.[5-9]\.[0-9]+"' docs/src/user-guides/paladin-agents.md &amp;&amp; mdbook build docs/ &amp;&amp; ./scripts/check-doc-examples.sh</automated>
  </verify>
  <acceptance_criteria>
    - `crates/doc-examples/src/paladin_agents.rs` contains `// ANCHOR: build_agent` and `// ANCHOR: attach_garrison` with matching `ANCHOR_END` lines
    - `crates/doc-examples/src/lib.rs` contains `pub mod paladin_agents;` and `git diff HEAD~1 -- crates/doc-examples/src/lib.rs` shows only added lines
    - `docs/src/user-guides/paladin-agents.md` contains at least two `{{#include ../../../crates/doc-examples/src/paladin_agents.rs:` lines
    - `grep -cE '"0\.[5-9]\.[0-9]+"' docs/src/user-guides/paladin-agents.md` returns `0`
    - `grep -c 'with_specialist' docs/src/user-guides/paladin-agents.md` returns `0`
    - `./scripts/check-doc-examples.sh` exits 0 and `mdbook build docs/` exits 0 with `No broken links found`
    - `git log --oneline -1 -- docs/src/user-guides/paladin-agents.md` subject matches `docs\(35\): .*\(MB-27\)`
  </acceptance_criteria>
  <done>MB-27 is closed and the module-plus-include path is proven end to end for the four remaining rows in this plan.</done>
</task>

<task type="auto">
  <name>Task 2: MB-19 and MB-24 — arsenal-tools.md and herald-output.md</name>
  <files>crates/doc-examples/src/arsenal_tools.rs, crates/doc-examples/src/herald_output.rs, crates/doc-examples/src/lib.rs, docs/src/user-guides/arsenal-tools.md, docs/src/user-guides/herald-output.md</files>
  <read_first>
    - `docs/src/user-guides/arsenal-tools.md` (the Custom Armaments and Handoff Tool sections)
    - `docs/src/user-guides/herald-output.md` (the Herald Trait section near lines 100-114 and the Custom Herald Implementation sample)
    - `.planning/phases/34-documentation-currency-audit/34-AUDIT.md` §2 rows 76 and 84
    - `crates/paladin-core/src/platform/container/arsenal/core.rs` lines 36-93 (`ArmamentCall` with its `arguments` map, and the five-field `ArmamentResult`)
    - `crates/paladin-core/src/platform/container/herald.rs` lines 49-153 (the seven-method `Herald` trait)
    - `docs/src/user-guides/output-formatting.md` lines 33-70 (the sibling page that already documents the trait correctly — the target state to agree with)
    - `src/application/services/paladin/paladin_builder.rs` around line 859 (`with_handoffs`)
    - `crates/doc-examples/src/paladin_agents.rs` (the module shape Task 1 landed)
  </read_first>
  <action>
    Create `crates/doc-examples/src/arsenal_tools.rs` with a `custom_armament` anchor that builds an
    `ArmamentResult` with all five live fields — `call_id`, `success`, `output`, `error`,
    `execution_time_ms` — and reads its input from the `arguments` map on `ArmamentCall` rather than
    an `args` field, and a `handoffs` anchor showing `with_handoffs` taking the whole specialist
    list. Create `crates/doc-examples/src/herald_output.rs` with a `custom_herald` anchor
    implementing the `Herald` trait in full: `format_paladin_result`, `format_battalion_result`,
    `format_stream_chunk`, `finalize_stream`, `format_error`, `name` and `mime_type`. Register both
    with `pub mod arsenal_tools;` and `pub mod herald_output;` in `crates/doc-examples/src/lib.rs`,
    additions only.

    On `arsenal-tools.md`, replace the Custom Armaments struct-literal fence and the Handoff Tool
    sample with `{{#include}}` blocks for the two new anchors, and correct any prose naming the old
    per-call chainable specialist method or the non-existent `args` field. On `herald-output.md`,
    rewrite the Herald Trait section to list all seven methods with their live signatures and
    replace the Custom Herald Implementation sample with the `custom_herald` include, so the page
    agrees with `output-formatting.md`; leave the `JsonHerald`, `MarkdownHerald` and `TableHerald`
    constructors and the six-key token-usage JSON sample alone — the audit confirmed all current.
    Per D-11(b) any remaining Rust fence on either page is corrected against the tree and marked
    illustrative; add the `orchestration.md` header note to a page that lacks one. Per D-16 add no
    dated callout. Run `mdbook build docs/` and `./scripts/check-doc-examples.sh`, then make two
    commits per D-24 — one per page, each carrying its own new module — with subjects
    `docs(35): compile-verify the Arsenal tools guide against the live ArmamentResult shape (MB-19)`
    and `docs(35): document the full seven-method Herald trait (MB-24)`.
  </action>
  <verify>
    <automated>cd /workspace &amp;&amp; grep -q 'pub mod arsenal_tools;' crates/doc-examples/src/lib.rs &amp;&amp; grep -q 'pub mod herald_output;' crates/doc-examples/src/lib.rs &amp;&amp; grep -q 'execution_time_ms' crates/doc-examples/src/arsenal_tools.rs &amp;&amp; grep -c 'fn format_paladin_result\|fn format_battalion_result\|fn format_stream_chunk\|fn finalize_stream\|fn format_error\|fn name\|fn mime_type' crates/doc-examples/src/herald_output.rs | grep -qE '^[7-9]$' &amp;&amp; mdbook build docs/ &amp;&amp; ./scripts/check-doc-examples.sh</automated>
  </verify>
  <acceptance_criteria>
    - `crates/doc-examples/src/arsenal_tools.rs` contains `// ANCHOR: custom_armament` and `// ANCHOR: handoffs`; `crates/doc-examples/src/herald_output.rs` contains `// ANCHOR: custom_herald`
    - `crates/doc-examples/src/herald_output.rs` implements all seven trait methods — `format_paladin_result`, `format_battalion_result`, `format_stream_chunk`, `finalize_stream`, `format_error`, `name`, `mime_type`
    - `docs/src/user-guides/arsenal-tools.md` contains `{{#include ../../../crates/doc-examples/src/arsenal_tools.rs:` and `grep -c 'with_specialist' docs/src/user-guides/arsenal-tools.md` returns `0`
    - `docs/src/user-guides/herald-output.md` names all seven method identifiers and contains `{{#include ../../../crates/doc-examples/src/herald_output.rs:custom_herald}}`
    - `./scripts/check-doc-examples.sh` exits 0; `mdbook build docs/` exits 0 with `No broken links found`
    - `git log --oneline --grep 'MB-19' | wc -l` and `git log --oneline --grep 'MB-24' | wc -l` each return at least 1
  </acceptance_criteria>
  <done>MB-19 and MB-24 are closed with compile-verified replacements for the two struct/trait samples the audit found uncompilable.</done>
</task>

<task type="auto">
  <name>Task 3: MB-20 and MB-28 — battalion-patterns.md and sanctum-vector-memory.md</name>
  <files>crates/doc-examples/src/battalion_patterns.rs, crates/doc-examples/src/sanctum_vector_memory.rs, crates/doc-examples/src/lib.rs, docs/src/user-guides/battalion-patterns.md, docs/src/user-guides/sanctum-vector-memory.md</files>
  <read_first>
    - `docs/src/user-guides/battalion-patterns.md` (line 51's dependency pin and the Commander section near lines 265-275)
    - `docs/src/user-guides/sanctum-vector-memory.md` (line 49's service name and the RAG section, lines 215-249)
    - `.planning/phases/34-documentation-currency-audit/34-AUDIT.md` §2 rows 77 and 92
    - `crates/paladin-battalion/src/commander.rs` lines 270-300 and around line 471 (the five-argument constructor and the single-argument execute method) plus the `CommanderBuilder` type
    - `docs/src/user-guides/orchestration.md` lines 155-210 (the builder form this page should adopt) and `crates/doc-examples/src/orchestration.rs` anchor `commander` (read only — D-26 forbids editing it; the new module gets its own anchor)
    - `crates/paladin-memory/src/services/rag_retrieval_service.rs` around line 179 (`RagRetrievalService`, `RagRetrievalResult`, `ShedItem`, `RagRetrievalError`, `retrieve_context_with_timeout`, `with_token_counter`)
    - `src/application/services/paladin/paladin_execution_service.rs` lines 1970-2000 (`format_retrieved_context` and the omission-marker append)
    - `crates/doc-examples/src/paladin_agents.rs` (the module shape from Task 1)
  </read_first>
  <action>
    Create `crates/doc-examples/src/battalion_patterns.rs` with a `commander` anchor that builds a
    Commander through the builder type `orchestration.md` uses, then runs it with the live
    single-argument execute method — the direct constructor takes five positional arguments
    (strategy, paladins, config, aggregator, paladin port) and the page's two-argument form does not
    exist. Create `crates/doc-examples/src/sanctum_vector_memory.rs` with a `rag_retrieve` anchor
    calling the retrieval service (camel-case `RagRetrievalService`, built with the token-counter
    builder method) through its timeout-bounded retrieval entry point and matching on the returned
    result type, and a `rag_format` anchor showing how the execution service renders that result
    into prompt context including the omission marker appended when memories are shed for budget
    reasons. Register both with `pub mod battalion_patterns;` and `pub mod sanctum_vector_memory;`
    in `crates/doc-examples/src/lib.rs`, additions only.

    On `battalion-patterns.md`, replace the Commander construction and execution fences with the new
    include, correct the dependency pin at line 51 to the literal `0.10.0` per D-20, and leave the
    token-usage samples at lines 312-315 alone — the audit confirmed them current. On
    `sanctum-vector-memory.md`, fix the service name at line 49 to its camel-case spelling and
    extend the RAG section so it documents the Phase 33 surface the audit found entirely absent:
    the retrieval result type, the shed-item type, the retrieval error type, the timeout-bounded
    retrieval call, the token-counter builder method, and the omission marker — with the two new
    includes carrying the runnable parts and `rust,ignore` fences for the type shapes per D-11(b).
    Keep the existing attachment pattern description (the builder's Sanctum and embedding-port
    methods still exist) and do not reintroduce the token-counter types Phase 32 deleted. Per D-16
    add no dated callout. Run `mdbook build docs/` and `./scripts/check-doc-examples.sh`, then make
    two commits per D-24 with subjects
    `docs(35): rebuild the battalion-patterns Commander example on the live builder API (MB-20)` and
    `docs(35): document the Phase 33 RAG retrieval surface in the Sanctum guide (MB-28)`.
  </action>
  <verify>
    <automated>cd /workspace &amp;&amp; grep -q 'pub mod battalion_patterns;' crates/doc-examples/src/lib.rs &amp;&amp; grep -q 'pub mod sanctum_vector_memory;' crates/doc-examples/src/lib.rs &amp;&amp; grep -q 'RagRetrievalResult' crates/doc-examples/src/sanctum_vector_memory.rs &amp;&amp; ! grep -q 'RAGRetrievalService' docs/src/user-guides/sanctum-vector-memory.md &amp;&amp; ! grep -qE '"0\.[5-9]\.[0-9]+"' docs/src/user-guides/battalion-patterns.md &amp;&amp; mdbook build docs/ &amp;&amp; ./scripts/check-doc-examples.sh</automated>
  </verify>
  <acceptance_criteria>
    - `crates/doc-examples/src/battalion_patterns.rs` contains `// ANCHOR: commander`; `crates/doc-examples/src/sanctum_vector_memory.rs` contains `// ANCHOR: rag_retrieve` and `// ANCHOR: rag_format`
    - `crates/doc-examples/src/sanctum_vector_memory.rs` names `RagRetrievalResult`, `ShedItem`, `retrieve_context_with_timeout` and `with_token_counter`
    - `docs/src/user-guides/sanctum-vector-memory.md` names `RagRetrievalResult`, `ShedItem`, `RagRetrievalError`, `retrieve_context_with_timeout`, `with_token_counter` and the omission marker, and `grep -c 'RAGRetrievalService' docs/src/user-guides/sanctum-vector-memory.md` returns `0`
    - `grep -cE '\bTokenCounterFactory\b|garrison::TokenCounter\b' docs/src/user-guides/sanctum-vector-memory.md` returns `0` — the Phase 32 deleted types are not reintroduced
    - `grep -cE '"0\.[5-9]\.[0-9]+"' docs/src/user-guides/battalion-patterns.md` returns `0`
    - `docs/src/user-guides/battalion-patterns.md` contains `{{#include ../../../crates/doc-examples/src/battalion_patterns.rs:commander}}`
    - `./scripts/check-doc-examples.sh` exits 0; `mdbook build docs/` exits 0 with `No broken links found`
    - `git log --oneline --grep 'MB-20' | wc -l` and `git log --oneline --grep 'MB-28' | wc -l` each return at least 1
  </acceptance_criteria>
  <done>MB-20 and MB-28 are closed: the Commander example compiles against the live builder API and the Sanctum guide documents the Phase 33 RAG surface the audit found missing.</done>
</task>

</tasks>

<!-- planner-discipline-allow: RAGRetrievalService -->
<!-- planner-discipline-allow: with_specialist -->

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository -> published book (`docs/book/html/`) | Anything written into `docs/src` is published |
| workspace -> `crates/doc-examples` | Five new compile-checked Rust modules enter the workspace build graph |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-03 | Information Disclosure | the five new `crates/doc-examples` modules and the five edited pages | low | mitigate | Every new anchor builds its dependencies from `crate::support::` mocks and literal placeholder strings; no provider API key, endpoint credential or real connection string is written into a module or a page |
| T-35-04 | Tampering | `crates/doc-examples/src/lib.rs` | low | mitigate | D-26 restricts this plan to added `pub mod` lines; the acceptance criterion asserts `git diff` on `lib.rs` shows additions only, so a Phase 36 `EX-nn` edit cannot be smuggled in here |
| T-35-SC | Tampering | npm/pip/cargo installs | low | accept | No registry package is installed: the new modules use existing first-party workspace path dependencies only (`paladin-core`, `paladin-ports`, `paladin-battalion`, `paladin-memory`, `paladin-storage`), and `35-RESEARCH.md` records the Package Legitimacy Audit as not applicable for that reason |
</threat_model>

<verification>
- `./scripts/check-doc-examples.sh` exits 0 — Layer 1 compiles all five new modules
- `mdbook build docs/` exits 0 with `No broken links found`
- `./scripts/check-doc-config.sh` exits 0
- `git diff 35-01..HEAD --name-only -- src/ crates/` lists only paths under `crates/doc-examples/` (D-00c)
- `git log --oneline --grep 'MB-'` shows one commit per page for MB-19, MB-20, MB-24, MB-27, MB-28
</verification>

<success_criteria>
- All five rows closed with compile-verified includes rather than hand-written fences (D-11a, D-12)
- Five new modules registered in `crates/doc-examples/src/lib.rs` by addition only (D-26)
- No existing `doc-examples` module or `support.rs` touched
- The full `docs.yml` sequence green on this plan's final commit
</success_criteria>

<output>
Create `.planning/phases/35-mdbook-currency/35-02-SUMMARY.md` when done, with the D-23 closure table
(`MB-nn | page | disposition | commit | how the §5 finding was addressed`) and, if any observation
was made on a page the audit settled `current`, a `## Deferred observations` section for plan 35-10
to fold into `deferred-items.md` (D-26, D-27).
</output>
