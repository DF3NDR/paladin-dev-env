---
phase: 35-mdbook-currency
plan: 08
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - docs/src/appendix/doc-coverage-report.md
  - docs/src/appendix/user-rest-api.md
  - docs/src/appendix/user-system.md
  - docs/src/appendix/contributing-legacy.md
  - docs/src/appendix/build-baselines.md
  - docs/src/appendix/battalion-benchmarks.md
  - docs/src/appendix/sanctum-benchmarks.md
  - docs/src/appendix/release-automation.md
autonomous: true
requirements: [CURR-06, CURR-07, CURR-08]
must_haves:
  truths:
    - "D-02: each of the five archive-tier pages — doc-coverage-report, build-baselines, user-system, user-rest-api and contributing-legacy — opens with an archive banner immediately after its H1, in the ADR-0047 shape, naming the live source of truth and, where one exists, the disposition record"
    - "D-01: every archive-tier page keeps its path and its `docs/src/SUMMARY.md` entry — archiving records a disposition, it does not destroy the record"
    - "D-02: `docs/src/appendix/user-system.md` and `docs/src/appendix/user-rest-api.md` state plainly that the user CLI subcommand they describe does not exist in the shipped binary while the user domain, service and repository layers do"
    - "D-02: `docs/src/appendix/user-rest-api.md` renders as a page — no unterminated fence and no leaked raw source tail"
    - "D-02: `docs/src/appendix/build-baselines.md`'s current-crate-count framing reads as a dated snapshot, and `docs/src/appendix/contributing-legacy.md` states MSRV 1.88, names the `crates/` workspace and uses the real repository URL"
    - "D-03: `docs/src/appendix/battalion-benchmarks.md`, `docs/src/appendix/sanctum-benchmarks.md` and `docs/src/appendix/release-automation.md` are corrected in place as live reference, not archived"
    - "MB-37: the toolchain line on `docs/src/appendix/battalion-benchmarks.md` states 1.88"
    - "MB-55: `docs/src/appendix/sanctum-benchmarks.md` describes the Qdrant adapter as shipped, not future"
    - "MB-54: `docs/src/appendix/release-automation.md` states all three dependencies of the publish job, including the pre-publish consistency gate"
    - "D-00e: `mdbook build docs/` exits 0 with no broken links after all eight pages are edited"
    - statement: "D-04: the effort stays proportional — a banner is not a rewrite and correct-tier pages are corrected to their findings, not rewritten for style"
      verification: backstop
  artifacts:
    - docs/src/appendix/doc-coverage-report.md
    - docs/src/appendix/user-rest-api.md
  key_links:
    - "each archive banner -> the live page that supersedes it (`contributing/development-setup.md` for the legacy contributing page; `architecture/overview.md` and the Phase 36 rustdoc bar for the coverage report)"
    - "`.planning/decisions/0047-architecture-appendix-disposition.md` -> the banner shape; `.planning/decisions/0033-cargo-doc-warning-bar.md` -> the coverage report's named bar"
    - "`crates/paladin-storage/src/sqlite_user_repository.rs` and `crates/paladin-core/src/platform/manager/user_service.rs` -> the user pages' banners, which name what does exist"
  prohibitions:
    - statement: "D-01: no page under docs/src is deleted and no entry is removed from docs/src/SUMMARY.md"
      status: recalled
      verification: flagged-unverified
    - statement: "D-04: PROJECT.md is not edited and its Milestone 11 appendix non-goal is not reopened"
      status: recalled
      verification: flagged-unverified
    - statement: "D-02: an archive-tier page receives its banner plus only the one-line factual corrections its finding cites, and nothing more"
      status: recalled
      verification: flagged-unverified
    - statement: "D-16: no new dated correction callout is added to any page"
      status: recalled
      verification: flagged-unverified
---

<objective>
Close eight appendix rows — the five archive-tier pages D-02 names (MB-01, MB-39, MB-60, MB-59,
MB-47) and three correct-tier pages (MB-37, MB-55, MB-54).

D-01 defines the dispositions. Archive means the page is a dated snapshot, report or completion
summary whose only value is historical: it gets the ADR-0047 banner naming the live source of truth,
plus the one-line factual corrections its §5 finding cites, and nothing more. No page is deleted and
no entry leaves the nav — archiving records a disposition, it does not destroy the record.

CONTEXT.md's Claude's Discretion asks the planner to settle archive-versus-correct for
`sanctum-benchmarks.md`, `battalion-benchmarks.md` and `release-automation.md`; D-03 already names
all three in correct-tier membership, and a locked decision outranks the discretion note, so all
three are corrected in place. D-04 keeps the effort proportional: the Milestone 11 appendix non-goal
in PROJECT.md is not reopened and PROJECT.md is not edited — ROADMAP Phase 35 SC1 is the binding
statement for these rows, and a banner is not a rewrite.

Purpose: close the appendix's historical tier honestly, so a reader lands on the live page instead
of a superseded snapshot.
Output: five banners with their cited corrections, three in-place corrections, one commit per page
except the two user pages which share one.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-mdbook-currency/35-CONTEXT.md
@.planning/phases/35-mdbook-currency/35-RESEARCH.md
@.planning/phases/35-mdbook-currency/35-PATTERNS.md
@.planning/phases/35-mdbook-currency/35-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
New symbols created by Phase 35 — exclude from drift verification against the pre-phase tree. This
plan creates no new file.

Created by sibling plans and referenced here: `docs/src/user-guides/superstep-engine.md` and
`crates/doc-examples/src/superstep_engine.rs` (plan 35-01); `crates/doc-examples/src/`
`paladin_agents.rs`, `arsenal_tools.rs`, `herald_output.rs`, `battalion_patterns.rs`,
`sanctum_vector_memory.rs` plus their `pub mod` lines (plan 35-02);
`docs/src/contributing/adr-index.md` and the retitled Contributing nav entry (plan 35-05);
`CHANGELOG.md` `### Documentation` under `## [0.10.0]` (plan 35-10);
`.planning/phases/35-mdbook-currency/{deferred-items.md,35-EVIDENCE.md}`; requirement IDs
`CURR-06`…`CURR-10`.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: MB-01 — archive doc-coverage-report.md end to end</name>
  <files>docs/src/appendix/doc-coverage-report.md</files>
  <read_first>
    - `docs/src/appendix/doc-coverage-report.md` (all 36 lines, notably its H1, the crate list and the no-warnings claim at line 18)
    - `docs/src/appendix/design-and-architecture.md` lines 1-9 (the shipped ADR-0047 banner — the exact blockquote shape to reuse)
    - `.planning/phases/34-documentation-currency-audit/34-AUDIT.md` §2 row 25 (MB-01's finding, including the 73-warning measurement) and §5 row 50
    - `.planning/decisions/0033-cargo-doc-warning-bar.md` (the bar the banner points at)
    - `.planning/ROADMAP.md` §"Phase 36" (the phase that owns the live rustdoc measure)
    - `.planning/phases/35-mdbook-currency/35-CONTEXT.md` D-02 (why this page is archived rather than regenerated) and the Specific Ideas banner template
  </read_first>
  <action>
    Insert an archive banner immediately after the H1, in the ADR-0047 blockquote shape read from
    `design-and-architecture.md`: it opens with a bold "Archived — historical document." lead, says
    what the page recorded and as of when, says it is not maintained, names where the current
    maintained equivalent lives, and names the disposition record. For this page the live sources
    are ADR-0033, which ratifies the zero-warning `cargo doc` bar, and Phase 36, which closes the
    measurement; say so plainly, and say that a regenerated report is deliberately deferred until
    that bar is green rather than produced here (D-02).

    Then apply only the one-line factual corrections the finding cites, and nothing more (D-02): the
    page's claim that the documentation build succeeds with no warnings is false against the
    measured result recorded in the audit, so correct that sentence to say the measurement is
    historical and the current figure is tracked by Phase 36; and the nine-crate list is a
    2026-05-28-era inventory missing two library crates and the facade, so either name it as the
    snapshot's inventory or correct it to eleven library crates plus the facade — choose the
    snapshot framing, since the page is archived. Do not restructure or rewrite the page.

    Per D-16 add no dated callout on top of the banner. Run `mdbook build docs/`, then commit with
    `git add -- docs/src/appendix/doc-coverage-report.md && git commit -q -m "docs(35): archive the doc-coverage report behind an ADR-0047 banner (MB-01)" -- docs/src/appendix/doc-coverage-report.md`
    (D-24).
  </action>
  <verify>
    <automated>cd /workspace &amp;&amp; head -6 docs/src/appendix/doc-coverage-report.md | grep -q '^&gt; \*\*Archived' &amp;&amp; grep -q 'ADR-0033' docs/src/appendix/doc-coverage-report.md &amp;&amp; grep -q 'doc-coverage-report.md' docs/src/SUMMARY.md &amp;&amp; mdbook build docs/</automated>
  </verify>
  <acceptance_criteria>
    - Line 1 of `docs/src/appendix/doc-coverage-report.md` is still its H1 and one of lines 2-6 begins a blockquote whose first words are `**Archived — historical document.**`
    - The banner names ADR-0033 and Phase 36 as the live measure
    - The page no longer asserts that the documentation build succeeds with no warnings as a current fact
    - `grep -c 'doc-coverage-report.md' docs/src/SUMMARY.md` returns `1` — the nav entry is untouched
    - `git diff HEAD~1 --stat -- docs/src/appendix/doc-coverage-report.md` shows fewer than 20 changed lines — a banner plus cited corrections, not a rewrite (D-04)
    - `mdbook build docs/` exits 0 with `No broken links found`
    - `git log --oneline -1 -- docs/src/appendix/doc-coverage-report.md` subject matches `docs\(35\): .*\(MB-01\)`
  </acceptance_criteria>
  <done>MB-01 is closed as an archive, and the banner shape plus the proportionality bound are proven for the four remaining archive-tier pages.</done>
</task>

<task type="auto">
  <name>Task 2: MB-59, MB-60, MB-47 and MB-39 — the four remaining archive-tier pages</name>
  <files>docs/src/appendix/user-rest-api.md, docs/src/appendix/user-system.md, docs/src/appendix/contributing-legacy.md, docs/src/appendix/build-baselines.md</files>
  <read_first>
    - `docs/src/appendix/user-rest-api.md` (all 139 lines — it opens with an H1 then raw REST examples and ends mid-Rust-source with an unterminated string)
    - `docs/src/appendix/user-system.md` (all 151 lines, notably the CLI Module Implementation section at lines 37-49 and the clap pin at line 39 the audit confirmed correct)
    - `docs/src/appendix/contributing-legacy.md` (line 25's Rust version, the clone URL at line 32, the Project Structure block at lines 41-50)
    - `docs/src/appendix/build-baselines.md` (line 17 and line 31's toolchain, the M5/M7 comparison table and its current-crate-count framing at line 27)
    - `.planning/phases/34-documentation-currency-audit/34-AUDIT.md` §2 rows 42, 43, 22 and 13
    - `docs/src/appendix/design-and-architecture.md` lines 1-9 (the banner shape)
    - `src/bin/paladin-cli.rs` (the twelve-variant `Commands` enum — there is no user subcommand)
    - `crates/paladin-storage/src/sqlite_user_repository.rs` and `crates/paladin-core/src/platform/manager/user_service.rs` (the layers that do exist)
    - `docs/src/contributing/development-setup.md` (the live page the legacy contributing page points at)
    - `Cargo.toml` line 64 (`repository`) and `rust-version`
  </read_first>
  <action>
    Three commits: the two user pages share one per D-24's tightly-coupled-pair allowance, the other
    two get one each. Each page gets the ADR-0047-shaped banner immediately after its H1 plus only
    the corrections its finding cites (D-02).

    User pages (MB-59 and MB-60, one commit). Both banners state plainly that the user subcommand
    family these pages document does not exist in the shipped binary — the live command enum has
    twelve variants and none of them is a user command — while the user domain, service and
    repository layers named in the read list do exist, and that the web-facing user work remains
    forward scope. On `user-rest-api.md` also repair the structure only as far as needed to render
    as a page: give it a proper H1 if the opening line is not one, terminate the unterminated fence
    at the end of the file, and either close or fence the leaked raw source tail so mdBook renders a
    complete page. Do not rewrite the content. On `user-system.md` leave the clap dependency pin
    alone; the audit confirmed it matches the live manifest exactly. Subject
    `docs(35): archive user-system and user-rest-api behind ADR-0047 banners (MB-59, MB-60)`.

    `contributing-legacy.md` (MB-47). Banner pointing at `contributing/development-setup.md` as the
    live contributing guide. Cited corrections: the Rust version line reads 1.88, the Project
    Structure block names the `crates/` Cargo workspace alongside the `src/` layout it already
    shows, and the placeholder clone URL is replaced with the real repository URL from `Cargo.toml`.
    Subject `docs(35): archive the legacy contributing page and correct its MSRV and layout (MB-47)`.

    `build-baselines.md` (MB-39). Banner naming it a dated Milestone 7 build-time snapshot and
    pointing at `appendix/performance-baseline.md` for current measurements. Cited correction: the
    current-crate-count framing on the comparison table is reworded so it reads as the snapshot's
    own count rather than a claim about today's tree, and the toolchain rows are left as the
    snapshot's own values — they are historical and exempt under D-20, and plan 35-10 records them
    in the D-21 allowlist. Subject
    `docs(35): archive the build baselines snapshot behind a banner (MB-39)`.

    Per D-16 add no dated callout on top of any banner. Run `mdbook build docs/` after the three
    commits.
  </action>
  <verify>
    <automated>cd /workspace &amp;&amp; for f in user-rest-api user-system contributing-legacy build-baselines; do head -8 "docs/src/appendix/$f.md" | grep -q '^&gt; \*\*Archived' || exit 1; grep -q "appendix/$f.md" docs/src/SUMMARY.md || exit 1; done &amp;&amp; grep -q 'development-setup.md' docs/src/appendix/contributing-legacy.md &amp;&amp; grep -q '1.88' docs/src/appendix/contributing-legacy.md &amp;&amp; grep -q 'DF3NDR/paladin-dev-env' docs/src/appendix/contributing-legacy.md &amp;&amp; awk '/^```/{n++} END{exit n%2}' docs/src/appendix/user-rest-api.md &amp;&amp; mdbook build docs/</automated>
  </verify>
  <acceptance_criteria>
    - Each of the four pages has its H1 on line 1 and an `**Archived — historical document.**` blockquote within its first eight lines
    - Each of the four nav entries still resolves — `grep -c 'appendix/<page>.md' docs/src/SUMMARY.md` returns `1` for each
    - Both user-page banners state that no user subcommand exists in the shipped binary and name the user service and repository modules that do exist
    - `awk '/^```/{n++} END{exit n%2}' docs/src/appendix/user-rest-api.md` exits 0 — the fences balance, so the page renders
    - `docs/src/appendix/contributing-legacy.md` contains `1.88`, `crates/`, `https://github.com/DF3NDR/paladin-dev-env` and a link to `contributing/development-setup.md`, and `grep -cE '\b1\.70\b|your-org/paladin' docs/src/appendix/contributing-legacy.md` returns `0`
    - `docs/src/appendix/build-baselines.md` frames its crate-count comparison as the snapshot's own figure and links `appendix/performance-baseline.md`
    - `mdbook build docs/` exits 0 with `No broken links found`
    - `git log --oneline --grep 'MB-39'`, `--grep 'MB-47'` and `--grep 'MB-59'` each return at least one commit
  </acceptance_criteria>
  <done>The archive tier is complete: five banners, every path and nav entry intact, one malformed page now renders.</done>
</task>

<task type="auto">
  <name>Task 3: MB-37, MB-55 and MB-54 — the three correct-tier appendix pages</name>
  <files>docs/src/appendix/battalion-benchmarks.md, docs/src/appendix/sanctum-benchmarks.md, docs/src/appendix/release-automation.md</files>
  <read_first>
    - `docs/src/appendix/battalion-benchmarks.md` line 11 (the toolchain claim) and its dated header
    - `docs/src/appendix/sanctum-benchmarks.md` line 17 and the Qdrant Adapter section at lines 212-219, plus its footer
    - `docs/src/appendix/release-automation.md` the Known operational caveats bullets at lines 149-153
    - `.planning/phases/34-documentation-currency-audit/34-AUDIT.md` §2 rows 9, 37 and 34
    - `Cargo.toml` `rust-version` and `rust-toolchain.toml`
    - `crates/paladin-memory/src/sanctum/qdrant_adapter.rs` and `crates/paladin-memory/Cargo.toml` (the shipped adapter and its feature)
    - `.github/workflows/release.yml` around lines 600-610 (the publish job's `needs` array)
  </read_first>
  <action>
    Three commits, one per page. All three are correct-tier under D-03 — live reference for a
    shipped surface — so they are corrected in place with no banner.

    `battalion-benchmarks.md` (MB-37). Correct the toolchain line to state 1.88, matching
    `Cargo.toml` `rust-version` and `rust-toolchain.toml`. The audit confirmed the benchmark target
    path and the page's own dated header are accurate, and that the measured numbers are a dated
    snapshot whose toolchain drift is expected, so the toolchain-version line is the only genuine
    content error — change nothing else. Subject
    `docs(35): correct the battalion benchmark MSRV line to 1.88 (MB-37)`.

    `sanctum-benchmarks.md` (MB-55). The Qdrant adapter shipped: the adapter module and the `qdrant`
    Cargo feature both exist. Rewrite the future-tense framing in the summary line and in the
    dedicated Qdrant section so both describe the adapter as shipped and say the benchmark numbers
    for it have not been captured yet, which is a different claim from the adapter being
    unimplemented. Leave the benchmark command alone; the audit confirmed the target exists. The
    page's own unfilled footer fields may be left as they are — they are honest about the page's
    state — or filled if the executor has a real value; do not invent one. Subject
    `docs(35): record the Qdrant Sanctum adapter as shipped in the benchmark page (MB-55)`.

    `release-automation.md` (MB-54). Correct the operational caveat that names only two dependencies
    of the publish job: the live job declares three, the third being the pre-publish consistency
    gate. State all three and say what the third gate enforces. The audit reproduced all nine job
    display names exactly and confirmed the operator flow, the branch-bypass description and the
    dry-run sections accurate — change nothing else. Subject
    `docs(35): state the publish job's third dependency, the consistency gate (MB-54)`.

    Per D-16 add no dated callout to any of the three. Run `mdbook build docs/` and
    `./scripts/check-doc-config.sh` after the three commits.
  </action>
  <verify>
    <automated>cd /workspace &amp;&amp; grep -q '1.88' docs/src/appendix/battalion-benchmarks.md &amp;&amp; ! grep -qE '\b1\.85\b' docs/src/appendix/battalion-benchmarks.md &amp;&amp; ! grep -qi 'when the qdrant adapter is implemented' docs/src/appendix/sanctum-benchmarks.md &amp;&amp; grep -q 'check-release-consistency' docs/src/appendix/release-automation.md &amp;&amp; ! grep -rq '^&gt; \*\*Archived' docs/src/appendix/battalion-benchmarks.md docs/src/appendix/sanctum-benchmarks.md docs/src/appendix/release-automation.md &amp;&amp; mdbook build docs/ &amp;&amp; ./scripts/check-doc-config.sh</automated>
  </verify>
  <acceptance_criteria>
    - `docs/src/appendix/battalion-benchmarks.md` states the Rust version as 1.88 and `grep -cE '\b1\.85\b' docs/src/appendix/battalion-benchmarks.md` returns `0`
    - `docs/src/appendix/sanctum-benchmarks.md` describes the Qdrant adapter as shipped, and `grep -ci 'when the qdrant adapter is implemented\|Qdrant Adapter (Future' docs/src/appendix/sanctum-benchmarks.md` returns `0`
    - `docs/src/appendix/release-automation.md` names `test`, `create-release` and `check-release-consistency` as the publish job's dependencies
    - None of the three pages carries an archive banner — they are correct-tier under D-03
    - `mdbook build docs/` exits 0 with `No broken links found`; `./scripts/check-doc-config.sh` exits 0
    - `git log --oneline --grep 'MB-37'`, `--grep 'MB-54'` and `--grep 'MB-55'` each return at least one commit
  </acceptance_criteria>
  <done>MB-37, MB-55 and MB-54 are closed in place, and the archive-versus-correct split recorded in this plan's objective is applied consistently.</done>
</task>

</tasks>

<!-- planner-discipline-allow: your-org/paladin -->

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository -> published book (`docs/book/html/`) | Anything written into `docs/src` is published |
| historical snapshot -> current reader | An unbannered archived page reads as current guidance |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-13 | Repudiation | the five archive-tier pages | medium | mitigate | Each banner names what the page recorded, as of when, that it is not maintained, and where the live source is, so a superseded snapshot — most sharply the coverage report's zero-warning claim and the user pages' non-existent CLI — can no longer read as current assurance. The nav entry and path are preserved so inbound links still resolve to the disposition rather than to a 404 |
| T-35-SC | Tampering | npm/pip/cargo installs | low | accept | Prose-only plan: no package is installed and no Rust file is added or edited, so the supply-chain surface is unchanged |
</threat_model>

<verification>
- Each of the five archive-tier pages carries its banner within the first eight lines and keeps its nav entry (D-01, D-02)
- `mdbook build docs/` exits 0 with `No broken links found` — including the repaired user REST page
- `./scripts/check-doc-config.sh` and `./scripts/check-doc-examples.sh` both exit 0
- `git diff 35-01..HEAD --name-only -- .planning/PROJECT.md` is empty (D-04)
- `git diff 35-01..HEAD --name-only -- src/ crates/` is empty for this plan's commits (D-00c)
</verification>

<success_criteria>
- All eight rows closed: five archived with banners and cited corrections, three corrected in place
- No page deleted, no nav entry removed (D-01)
- Effort stayed proportional — banners plus cited lines, not rewrites (D-04)
- The full `docs.yml` sequence green on this plan's final commit
</success_criteria>

<output>
Create `.planning/phases/35-mdbook-currency/35-08-SUMMARY.md` when done, with the D-23 closure table
(`MB-nn | page | disposition | commit | how the §5 finding was addressed`) — the disposition column
must read `archived` for MB-01, MB-39, MB-47, MB-59 and MB-60 and `corrected` for MB-37, MB-54 and
MB-55 — and, if any observation was made on a page the audit settled `current`, a
`## Deferred observations` section for plan 35-10 to fold into `deferred-items.md` (D-27).
</output>
