---
phase: 35-mdbook-currency
plan: 09
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - docs/src/appendix/minio-file-repository-setup.md
  - docs/src/appendix/redis-queue-adapter-setup.md
  - docs/src/appendix/sanctum-migration.md
  - docs/src/appendix/port-trait-template.md
  - docs/src/appendix/provider-expansion.md
  - docs/src/appendix/sentinel.md
  - docs/src/appendix/council.md
  - docs/src/appendix/battalion-patterns-guide.md
  - docs/src/appendix/integration-tests.md
  - docs/src/appendix/security-scanning.md
autonomous: true
requirements: [CURR-06, CURR-07, CURR-08]
must_haves:
  truths:
    - "D-13: every port import on these pages uses the bare crate path `paladin_ports::output::…`; `grep -rn 'paladin::paladin_ports::' docs/src` returns nothing"
    - "D-13: every LLM adapter import uses a `paladin_llm::` crate path with the correct type spellings `OpenAIAdapter`, `AnthropicAdapter`, `DeepSeekAdapter`; `grep -rn 'paladin::infrastructure::adapters::llm::' docs/src` returns nothing"
    - "D-11c: every corrected sample on these pages is fenced `rust,ignore` and was proved to compile with a throwaway `examples/_scratch.rs` plus `cargo check --example _scratch --features <needed>`, with the command and result recorded in the plan SUMMARY and the scratch file never committed"
    - "MB-48: the council service constructor on `docs/src/appendix/council.md` takes its three live arguments including the registry, and the result fields it names exist on the live result type"
    - "MB-38: every code sample on `docs/src/appendix/battalion-patterns-guide.md` opens with an import that compiles against the facade"
    - "MB-52: `docs/src/appendix/provider-expansion.md`'s provider comparison covers all nine shipped providers and its version line reads 0.10.0"
    - "MB-49: `docs/src/appendix/integration-tests.md`'s inventory covers every file `ls tests/integration/*.rs` returns"
    - "MB-57: `docs/src/appendix/security-scanning.md` states the Snyk disposition as evaluated and removed with zero Rust coverage, carries a CodeQL section stating the advisory-only disposition, and lists every advisory in `.cargo/audit.toml`"
    - "D-00e: `mdbook build docs/` exits 0 with no broken links after all ten pages are edited"
    - statement: "D-03: all ten pages are corrected in place as live reference for a shipped surface — none is archived"
      verification: backstop
  artifacts:
    - docs/src/appendix/security-scanning.md
    - docs/src/appendix/integration-tests.md
  key_links:
    - "`crates/paladin-ports/src/output/*` -> the port imports on five appendix pages (the crate is a top-level sibling, never reached through the facade)"
    - "`crates/paladin-llm/src/{openai,anthropic,deepseek}/adapter.rs` -> the adapter imports on provider-expansion and sentinel (relocated with no facade shim)"
    - "`.github/instructions/security.instructions.md` -> the Snyk and CodeQL wording on `docs/src/appendix/security-scanning.md` (repeated, never re-argued)"
    - "`ls tests/integration/*.rs` -> the inventory table on `docs/src/appendix/integration-tests.md`"
  prohibitions:
    - statement: "D-01: no page under docs/src is deleted and no entry is removed from docs/src/SUMMARY.md"
      status: recalled
      verification: flagged-unverified
    - statement: "D-27: a defect noticed on a page the audit settled `current` is recorded in the phase deferred register with a proposed classification, never fixed silently"
      status: recalled
      verification: flagged-unverified
    - statement: "D-11c: the throwaway scratch example file is never committed"
      status: recalled
      verification: flagged-unverified
    - statement: "D-26: no existing crates/doc-examples module, support.rs, examples/ program or examples/README.md is edited"
      status: recalled
      verification: flagged-unverified
---

<objective>
Close the ten remaining appendix rows — the six that carry the systemic import defects (MB-50,
MB-53, MB-56, MB-51, MB-52, MB-58) and four that carry API-shape or inventory defects (MB-48,
MB-38, MB-49, MB-57). All ten are correct-tier under D-03: each documents a shipped, live surface,
so each is corrected in place, none archived.

D-13 supplies the one import rule, established once and applied identically on every page: the ports
crate is a top-level sibling reached as `paladin_ports::output::…`, never nested under the facade —
the facade re-exports `core::…` and `application::services::…` as compat paths but has no
`paladin_ports` re-export, so the compat pattern does not generalize. The relocated LLM adapters are
reached as `paladin_llm::openai::OpenAIAdapter`, `paladin_llm::anthropic::AnthropicAdapter` and
`paladin_llm::deepseek::DeepSeekAdapter`; note the capital I and A in the OpenAI type name — the
sentinel page has both the wrong module path and the wrong casing on the same lines, and fixing only
the path leaves a second defect behind.

D-11(c) supplies the proof obligation for appendix samples: they are corrected in place and fenced
`rust,ignore`, and the executor proves each corrected sample compiles with the same throwaway
`examples/_scratch.rs` plus `cargo check --example _scratch --features <needed>` the audit used,
records the command and result in the SUMMARY, and never commits the scratch file.

Purpose: remove the last uncompilable imports and the last false inventories from the appendix.
Output: ten corrected pages, one commit per page.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-mdbook-currency/35-CONTEXT.md
@.planning/phases/35-mdbook-currency/35-RESEARCH.md
@.planning/phases/35-mdbook-currency/35-PATTERNS.md
@.planning/phases/35-mdbook-currency/35-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
New symbols created by Phase 35 — exclude from drift verification against the pre-phase tree. This
plan creates no new committed file; `examples/_scratch.rs` is a throwaway compile probe that is
deleted and never committed (D-11c).

Created by sibling plans and referenced here: `docs/src/user-guides/superstep-engine.md` and
`crates/doc-examples/src/superstep_engine.rs` (plan 35-01); `crates/doc-examples/src/`
`paladin_agents.rs`, `arsenal_tools.rs`, `herald_output.rs`, `battalion_patterns.rs`,
`sanctum_vector_memory.rs` plus their `pub mod` lines (plan 35-02);
`docs/src/contributing/adr-index.md` and the retitled Contributing nav entry (plan 35-05);
`CHANGELOG.md` `### Documentation` under `## [0.10.0]` (plan 35-10);
`.planning/phases/35-mdbook-currency/{deferred-items.md,35-EVIDENCE.md}`; requirement IDs
`CURR-06`…`CURR-10`.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: MB-50 — the import rule and the scratch-compile proof, end to end on one page</name>
  <files>docs/src/appendix/minio-file-repository-setup.md</files>
  <precondition>The worktree starts with a cold `target/`, so the first `cargo check --example _scratch --features cli,s3-storage` can run 15-25 minutes; allow that timeout on the first probe and expect later probes to be warm.</precondition>
  <read_first>
    - `docs/src/appendix/minio-file-repository-setup.md` lines 118-125, 259-263, 566-574 and 630-638 (the four broken port imports and the two adapter imports that already compile)
    - `.planning/phases/34-documentation-currency-audit/34-AUDIT.md` §2 row 29 (MB-50's finding, including the exact compiler error) and §5 row 52
    - `crates/paladin-ports/src/output/file_storage_port.rs` (the live trait and options type, and the module path they sit at)
    - `src/lib.rs` (confirm for yourself that the facade re-exports `core::` and `application::services::` but never the ports crate)
    - `src/infrastructure/adapters/file_storage/mod.rs` (the adapter re-export that makes the page's adapter import valid)
    - `.planning/phases/35-mdbook-currency/35-RESEARCH.md` Pitfall 2 (why the compat pattern does not generalize)
  </read_first>
  <action>
    Apply the D-13 import rule to all four broken port imports on the page: the file-storage port
    trait and its options type are reached as `paladin_ports::output::file_storage_port::…`, a bare
    top-level crate path, and the glob form becomes
    `use paladin_ports::output::file_storage_port::*;`. Leave the two MinIO adapter imports alone —
    the audit confirmed the facade re-export makes them compile — and leave the four container-image
    pins alone, already confirmed current. Fence every corrected sample `rust,ignore` per D-11(c)
    and add the illustrative-fragment header note if the page lacks one.

    Prove each corrected sample per D-11(c): write a throwaway `examples/_scratch.rs` containing the
    corrected imports and a trivial `fn main() {}`, run
    `cargo check --example _scratch --features cli,s3-storage`, record the exact command and its
    result line, then delete the file. The scratch file is never committed and never staged — check
    `git status --porcelain -- examples` is clean before committing.

    Per D-16 add no dated callout. Run `mdbook build docs/`, then commit with
    `git add -- docs/src/appendix/minio-file-repository-setup.md && git commit -q -m "docs(35): fix the file-storage port import path (MB-50)" -- docs/src/appendix/minio-file-repository-setup.md`
    (D-24).
  </action>
  <verify>
    <automated>cd /workspace &amp;&amp; ! grep -q 'paladin::paladin_ports::' docs/src/appendix/minio-file-repository-setup.md &amp;&amp; grep -q 'paladin_ports::output::file_storage_port' docs/src/appendix/minio-file-repository-setup.md &amp;&amp; test -z "$(git status --porcelain -- examples)" &amp;&amp; ! test -f examples/_scratch.rs &amp;&amp; mdbook build docs/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'paladin::paladin_ports::' docs/src/appendix/minio-file-repository-setup.md` returns `0`
    - The page's port imports read `paladin_ports::output::file_storage_port::` with no facade prefix, and the adapter imports are unchanged from before the commit
    - `grep -cE '^```rust$' docs/src/appendix/minio-file-repository-setup.md` returns `0` — every Rust fence is marked illustrative
    - `examples/_scratch.rs` does not exist and `git status --porcelain -- examples` is empty
    - The SUMMARY records the literal `cargo check --example _scratch --features cli,s3-storage` command and its result
    - `mdbook build docs/` exits 0 with `No broken links found`
    - `git log --oneline -1 -- docs/src/appendix/minio-file-repository-setup.md` subject matches `docs\(35\): .*\(MB-50\)`
  </acceptance_criteria>
  <done>MB-50 is closed and the D-13 import rule plus the D-11(c) scratch-compile proof are established for the nine remaining pages.</done>
</task>

<task type="auto">
  <name>Task 2: MB-53, MB-56, MB-51, MB-52 and MB-58 — the rest of the import family</name>
  <files>docs/src/appendix/redis-queue-adapter-setup.md, docs/src/appendix/sanctum-migration.md, docs/src/appendix/port-trait-template.md, docs/src/appendix/provider-expansion.md, docs/src/appendix/sentinel.md</files>
  <read_first>
    - `docs/src/appendix/redis-queue-adapter-setup.md` line 101 (the broken port import; lines 100, 133 and 242 already compile)
    - `docs/src/appendix/sanctum-migration.md` lines 241 and 386 (the two broken port imports; lines 169, 240 and 385 already compile)
    - `docs/src/appendix/port-trait-template.md` lines 48 and 60 (the template's own broken example imports)
    - `docs/src/appendix/provider-expansion.md` lines 36-43 (the three-provider comparison table), lines 201, 214 and 234 (the broken adapter imports), line 367 (a broken port import) and lines 559-560 (the date and version footer)
    - `docs/src/appendix/sentinel.md` lines 74, 188 and 216 (the adapter imports carrying both the wrong module path and the wrong type casing)
    - `.planning/phases/34-documentation-currency-audit/34-AUDIT.md` §2 rows 33, 39, 31, 32 and 41
    - `crates/paladin-ports/src/output/{queue_port,sanctum_port,llm_port}.rs`
    - `crates/paladin-llm/src/{openai,anthropic,deepseek}/adapter.rs` (confirm each type name's exact spelling with `grep -n 'pub struct .*Adapter'` before writing it)
    - `src/infrastructure/adapters/llm/mod.rs` (its own doc comment records that the adapters relocated and no shim was kept)
    - `Cargo.toml` `[features]` (the nine `llm-*` provider flags for the comparison table)
  </read_first>
  <action>
    Five commits, one per page. On every page apply the D-13 rule, fence each corrected sample
    `rust,ignore` per D-11(c), and prove it with the throwaway scratch probe described in Task 1
    using whatever feature list that page's imports need — recording the command and result in the
    SUMMARY and deleting the file each time.

    `redis-queue-adapter-setup.md` (MB-53): correct the queue-port import at line 101 to the bare
    crate path. Probe with `--features cli,redis-queue`. Leave the adapter and manager imports alone;
    the audit confirmed all three compile. Subject
    `docs(35): fix the queue port import path (MB-53)`.

    `sanctum-migration.md` (MB-56): correct both port imports to bare crate paths. Probe with the
    feature list the page's adapter imports need. Leave the Qdrant image tags alone — illustrative
    per the audit's own precedent. Subject
    `docs(35): fix the Sanctum port import paths (MB-56)`.

    `port-trait-template.md` (MB-51): correct both example imports in the template so every future
    port author copies a path that compiles. This is a generic template with placeholder
    identifiers, so the probe substitutes a real port for the placeholder to prove the shape.
    Subject `docs(35): fix the port-trait template's example import (MB-51)`.

    `provider-expansion.md` (MB-52): correct the three adapter imports to `paladin_llm::` crate
    paths with the exact type spellings read from the adapter files, and the port import at line 367
    to the bare crate path. Expand the three-provider comparison table to cover all nine shipped
    providers, taking the flag names from the facade `[features]` block; the page's own later
    streamed-usage table already discusses all nine, so the two sections must agree. Correct the
    version footer to 0.10.0 and drop or correct the stale last-updated date per D-20. Subject
    `docs(35): cover all nine providers and fix the adapter import paths (MB-52)`.

    `sentinel.md` (MB-58): correct the adapter imports on all three cited lines — both the module
    path and the type name. Check each spelling against `grep -n 'pub struct .*Adapter'` output
    before writing it; the page's current lowercase rendering of the OpenAI type is itself part of
    the defect. Leave the vision-type and builder imports alone; the audit confirmed both compile.
    Subject `docs(35): fix the relocated LLM adapter imports and type casing (MB-58)`.

    Per D-16 add no dated callout to any of the five. Run `mdbook build docs/` after the five
    commits and confirm `git status --porcelain -- examples` is empty.
  </action>
  <verify>
    <automated>cd /workspace &amp;&amp; ! grep -rq 'paladin::paladin_ports::' docs/src &amp;&amp; ! grep -rq 'paladin::infrastructure::adapters::llm::' docs/src &amp;&amp; ! grep -rq 'OpenAiAdapter' docs/src &amp;&amp; grep -q 'paladin_llm::' docs/src/appendix/sentinel.md &amp;&amp; ! grep -qE '"0\.[0-9]\.[0-9]+"' docs/src/appendix/provider-expansion.md &amp;&amp; for p in kimi qwen grok ollama gemini; do grep -qi "$p" docs/src/appendix/provider-expansion.md || exit 1; done &amp;&amp; test -z "$(git status --porcelain -- examples)" &amp;&amp; mdbook build docs/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc 'paladin::paladin_ports::' docs/src` returns `0` — the D-21 exit grep for this defect is empty book-wide
    - `grep -rc 'paladin::infrastructure::adapters::llm::' docs/src` returns `0` and `grep -rc 'OpenAiAdapter' docs/src` returns `0`
    - `docs/src/appendix/sentinel.md` and `docs/src/appendix/provider-expansion.md` name `OpenAIAdapter`, `AnthropicAdapter` and `DeepSeekAdapter` with the spellings in `crates/paladin-llm/src/*/adapter.rs`
    - `docs/src/appendix/provider-expansion.md`'s comparison table names all nine shipped providers and its version footer reads 0.10.0
    - `examples/_scratch.rs` does not exist and `git status --porcelain -- examples` is empty
    - The SUMMARY records one `cargo check --example _scratch --features …` command and result per page
    - `mdbook build docs/` exits 0 with `No broken links found`
    - `git log --oneline --grep 'MB-51'`, `--grep 'MB-52'`, `--grep 'MB-53'`, `--grep 'MB-56'` and `--grep 'MB-58'` each return at least one commit
  </acceptance_criteria>
  <done>The systemic import defect is gone book-wide: both D-13 exit greps are empty and every corrected sample was compile-probed.</done>
</task>

<task type="auto">
  <name>Task 3: MB-48, MB-38, MB-49 and MB-57 — API shapes, imports and inventories</name>
  <files>docs/src/appendix/council.md, docs/src/appendix/battalion-patterns-guide.md, docs/src/appendix/integration-tests.md, docs/src/appendix/security-scanning.md</files>
  <read_first>
    - `docs/src/appendix/council.md` lines 80-130 (the imports the audit confirmed compile, the two-argument constructor call, and the result-field accesses)
    - `docs/src/appendix/battalion-patterns-guide.md` lines 38-39, 129-130, 227-228 and 349-350 (the four broken opening imports)
    - `docs/src/appendix/integration-tests.md` lines 20-56 (the Main test files table) and its CI service-provisioning section, which the audit confirmed correct
    - `docs/src/appendix/security-scanning.md` lines 10-15 (the tooling table the audit confirmed correct), lines 81-86 (the tracked exceptions) and lines 95-122 (the Snyk section)
    - `.planning/phases/34-documentation-currency-audit/34-AUDIT.md` §2 rows 23, 10, 28 and 40
    - `crates/paladin-battalion/src/council_service.rs` lines 20-90 (the three-argument constructor and the live result type's four fields)
    - `src/core/platform/mod.rs` and `src/lib.rs` (the facade prelude and the item-level battalion re-exports that do compile)
    - `ls tests/integration/*.rs` (the live inventory) and `ls .github/workflows/`
    - `.github/instructions/security.instructions.md` — the Snyk removal record and the "Known gap: no Rust SAST" section (the wording to repeat verbatim)
    - `.cargo/audit.toml` (the full `ignore` array)
  </read_first>
  <action>
    Four commits, one per page. Fence every corrected sample `rust,ignore` and prove it with the
    throwaway scratch probe per D-11(c), recording each command and result in the SUMMARY and
    deleting the file.

    `council.md` (MB-48): correct the council service constructor call to the live three-argument
    form — the paladin port, the garrison port and the registry the page currently omits — and
    correct the result-field accesses to the four fields the live result type actually carries; the
    two field names the page uses exist on neither. Leave the two imports alone; the audit confirmed
    both compile as backward-compatible facade re-exports. Subject
    `docs(35): correct the council service constructor and result fields (MB-48)`.

    `battalion-patterns-guide.md` (MB-38): every one of the four samples opens with an import of a
    facade module that does not exist, so none of them compiles as written. Replace each opening
    import with the facade forms that do compile — the prelude glob, plus item-level imports from
    the battalion container re-export path — checking each against the facade source before writing
    it. Subject `docs(35): fix the battalion patterns guide imports to compiling facade paths (MB-38)`.

    `integration-tests.md` (MB-49): rebuild the Main test files inventory from `ls tests/integration/*.rs`
    so every live file appears, adding the two dozen the audit found missing — the end-to-end
    approval-gate, crash-resume, muster-defer-order and platform-API tests, the RAG-Commissary test,
    the vault-confinement, OTel-transport, config-boot, engine-tracer, waypoint-retention,
    structured-node, golden-bridge, middleware-under-engine, multi-parley, parley-resume,
    provider-switching, Ollama-Docker, reasoning-agent, subgraph-in-campaign, commander-error-paths,
    Aegis-retry, arsenal-bridge and the two battalion herald tests among them — each with a one-line
    purpose. Leave the battalion sub-module table and the CI service-provisioning section alone;
    both were confirmed exact. Subject
    `docs(35): rebuild the integration-test inventory from the live tests directory (MB-49)`.

    `security-scanning.md` (MB-57): rewrite the Snyk section to the measured disposition, worded as
    `.github/instructions/security.instructions.md` words it — evaluated and removed on 2026-08-18,
    zero Rust coverage, with the code scanner returning no findings on a four-class planted probe
    and the dependency scanner reporting no supported target files for a Cargo workspace — and state
    the standing instruction not to reintroduce it. Add a CodeQL section stating the advisory-only
    disposition in the same source's words: evaluated at the tested version, retained advisory-only,
    reports into the code-scanning UI, not pinned in any ruleset, does not gate a merge, with the
    manual credential-handling review remaining the primary control. Do not re-argue either verdict
    from scratch — repeat the source. Correct the tracked-exceptions list so it holds every advisory
    in the `ignore` array of `.cargo/audit.toml`, which carries five, not the two the page lists.
    Leave the tooling table alone; the audit confirmed its job names live. Subject
    `docs(35): record the measured Snyk removal and the advisory-only CodeQL disposition (MB-57)`.

    Per D-16 add no dated callout to any of the four. Run `mdbook build docs/` and
    `./scripts/check-doc-config.sh` after the four commits, and confirm
    `git status --porcelain -- examples` is empty.
  </action>
  <verify>
    <automated>cd /workspace &amp;&amp; ! grep -q 'use paladin::battalion::' docs/src/appendix/battalion-patterns-guide.md &amp;&amp; grep -q 'transcript' docs/src/appendix/council.md &amp;&amp; ! grep -q 'conversation_history' docs/src/appendix/council.md &amp;&amp; LIVE=$(ls tests/integration/*.rs | wc -l) &amp;&amp; DOC=$(grep -oE '[a-z0-9_]+_test\.rs' docs/src/appendix/integration-tests.md | sort -u | wc -l) &amp;&amp; [ "$DOC" -ge $((LIVE - 5)) ] &amp;&amp; grep -qi 'evaluated and removed' docs/src/appendix/security-scanning.md &amp;&amp; grep -qi 'CodeQL' docs/src/appendix/security-scanning.md &amp;&amp; test -z "$(git status --porcelain -- examples)" &amp;&amp; mdbook build docs/ &amp;&amp; ./scripts/check-doc-config.sh</automated>
  </verify>
  <acceptance_criteria>
    - `docs/src/appendix/council.md` shows the three-argument constructor call naming the registry parameter, names `transcript`, `conclusion`, `rounds_completed` and `termination_reason`, and `grep -cE 'conversation_history|final_output' docs/src/appendix/council.md` returns `0`
    - `grep -c 'use paladin::battalion::' docs/src/appendix/battalion-patterns-guide.md` returns `0` and each of the four samples opens with an import proved by the scratch probe
    - The distinct test filenames named on `docs/src/appendix/integration-tests.md` cover every file `ls tests/integration/*.rs` returns
    - `docs/src/appendix/security-scanning.md` states the Snyk disposition as evaluated and removed with zero Rust coverage, carries a CodeQL section stating it does not gate a merge, and lists all five advisories in `.cargo/audit.toml`'s ignore array
    - `examples/_scratch.rs` does not exist and `git status --porcelain -- examples` is empty
    - `mdbook build docs/` exits 0 with `No broken links found`; `./scripts/check-doc-config.sh` exits 0
    - `git log --oneline --grep 'MB-38'`, `--grep 'MB-48'`, `--grep 'MB-49'` and `--grep 'MB-57'` each return at least one commit
  </acceptance_criteria>
  <done>MB-48, MB-38, MB-49 and MB-57 are closed: every sample compiles, the test inventory is complete, and the security page states the measured SAST posture rather than a deferred one.</done>
</task>

</tasks>

<!-- planner-discipline-allow: paladin::paladin_ports:: -->
<!-- planner-discipline-allow: paladin::infrastructure::adapters::llm:: -->
<!-- planner-discipline-allow: OpenAiAdapter -->
<!-- planner-discipline-allow: use paladin::battalion:: -->
<!-- planner-discipline-allow: conversation_history -->

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository -> published book (`docs/book/html/`) | Anything written into `docs/src` is published |
| security posture record -> documentation | The book states which scanners gate a merge; a wrong statement reads as assurance the project does not have |
| local shell -> repository | The throwaway compile probe writes into `examples/` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-14 | Repudiation | `docs/src/appendix/security-scanning.md` | high | mitigate | The page currently records the Snyk decision as deferred with partial coverage, which reads as assurance where the measured result was zero Rust coverage. The rewrite repeats `.github/instructions/security.instructions.md` verbatim — Snyk evaluated and removed, CodeQL retained advisory-only and not merge-gating, manual credential review the primary control — and lists all five allowlisted advisories, so no reader concludes a gate exists that does not |
| T-35-15 | Tampering | `examples/_scratch.rs` | low | mitigate | D-11(c)'s probe file is deleted after each use and never staged; every task asserts `git status --porcelain -- examples` is empty and that the file is absent before committing, so a throwaway probe cannot enter the tree or the `cargo build --examples` CI step |
| T-35-SC | Tampering | npm/pip/cargo installs | low | accept | No registry package is installed: the scratch probe compiles against existing workspace path dependencies with existing feature flags, and no file under `crates/` or `src/` is modified |
</threat_model>

<verification>
- `grep -rn 'paladin::paladin_ports::' docs/src` and `grep -rn 'paladin::infrastructure::adapters::llm::' docs/src` both return nothing (the two D-13 exit greps, D-21)
- `mdbook build docs/` exits 0 with `No broken links found`
- `./scripts/check-doc-config.sh` and `./scripts/check-doc-examples.sh` both exit 0
- `git status --porcelain -- examples` is empty and `examples/_scratch.rs` is absent (D-11c, D-26)
- `git diff 35-01..HEAD --name-only -- src/ crates/` is empty for this plan's commits (D-00c)
- Ten commits, one per page, each naming its MB ID
</verification>

<success_criteria>
- All ten rows closed in place as correct-tier (D-03), one commit per page (D-24)
- Both D-13 exit greps empty book-wide
- Every corrected appendix sample compile-probed and the command recorded (D-11c)
- The full `docs.yml` sequence green on this plan's final commit
</success_criteria>

<output>
Create `.planning/phases/35-mdbook-currency/35-09-SUMMARY.md` when done, with the D-23 closure table
(`MB-nn | page | disposition | commit | how the §5 finding was addressed`), one
`cargo check --example _scratch --features …` command and result per page (D-11c), and, if any
observation was made on a page the audit settled `current`, a `## Deferred observations` section for
plan 35-10 to fold into `deferred-items.md` (D-27).
</output>
