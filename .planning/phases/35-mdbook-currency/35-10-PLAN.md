---
phase: 35-mdbook-currency
plan: 10
type: execute
wave: 3
depends_on: ["35-02", "35-03", "35-04", "35-05", "35-06", "35-07", "35-08", "35-09"]
files_modified:
  - CHANGELOG.md
  - .planning/phases/35-mdbook-currency/35-EVIDENCE.md
  - .planning/phases/35-mdbook-currency/deferred-items.md
autonomous: true
requirements: [CURR-06, CURR-07, CURR-08, CURR-09, CURR-10]
must_haves:
  truths:
    - "D-23: `.planning/phases/35-mdbook-currency/35-EVIDENCE.md` exists and carries the consolidated closure table with one row per MB ID — all sixty — each naming its page, disposition, commit and how the §5 finding was addressed"
    - "D-23: the evidence file records the full `docs.yml` sequence run locally, verbatim, with the three tool versions annotated against their `docs.yml` pins, the `git status --porcelain -- docs` check after the mermaid install, the linkcheck summary line, both scripts' result lines and the `make api-surface` output"
    - "D-21: all six literal exit greps and the Phase 31 ten-page re-check are recorded verbatim in the evidence file, each empty or fully allowlisted, with every allowlist row naming file, line, matched text and why it is historical"
    - "D-25: `CHANGELOG.md` `[0.10.0]` carries a `### Documentation` subsection placed after `### Fixed` and before `### Known limitations`, with one bullet for the new engine guide, one per nav section summarising the corrected pages, and one naming the archived appendix pages and their live replacements"
    - "D-25: no `MB-nn` identifier appears anywhere in `CHANGELOG.md` — they are planning-corpus identifiers"
    - "D-26: `make api-surface` reports no change; the `doc-examples` crate is `publish = false` and absent from the committed baseline, so the new modules do not move the public surface"
    - "D-00a: `git log --oneline --grep 'MB-'` reproduces the closure map mechanically — every one of the sixty IDs appears in at least one commit subject"
    - "D-27: the phase-local deferred register holds every observation the nine page plans recorded, each with a proposed classification and an owner"
    - statement: "D-00b: if the branch moved with a non-`.planning` change since the Phase 34 baseline, the final evidence run re-ran `mdbook build docs/` and `scripts/check-doc-examples.sh` rather than trusting that baseline"
      verification: backstop
    - statement: "D-25: the CHANGELOG bullets read as reader-facing release notes, not as a work-list recital"
      verification: backstop
  artifacts:
    - .planning/phases/35-mdbook-currency/35-EVIDENCE.md
    - CHANGELOG.md
  key_links:
    - "the nine plan SUMMARY closure tables -> the consolidated sixty-row table in `35-EVIDENCE.md` (D-23)"
    - "the nine plan SUMMARY `## Deferred observations` sections -> `.planning/phases/35-mdbook-currency/deferred-items.md` (D-26, D-27)"
    - "`35-EVIDENCE.md`'s final `docs.yml` run -> the docs half of Phase 37's gate re-seal reference"
  prohibitions:
    - statement: "D-25: no MB-nn identifier appears in CHANGELOG.md"
      status: recalled
      verification: flagged-unverified
    - statement: "D-01: no page under docs/src is deleted and no entry is removed from docs/src/SUMMARY.md"
      status: recalled
      verification: flagged-unverified
    - statement: "D-26: no existing crates/doc-examples module, support.rs, examples/ program or examples/README.md is edited; the api-surface baseline is not regenerated"
      status: recalled
      verification: flagged-unverified
    - statement: "D-04: PROJECT.md is not edited and its Milestone 11 appendix non-goal is not reopened"
      status: recalled
      verification: flagged-unverified
---

<objective>
Close the phase: prove every gate green on the final commit, record the evidence, write the
reader-facing CHANGELOG entry, and fold the nine page plans' deferred observations into the
phase-local register.

D-23 defines the evidence file: the `NN-EVIDENCE.md` house pattern, carrying the consolidated
closure table assembled from the nine plan SUMMARYs plus the verbatim gate run. D-21 defines the
exit greps and the allowlist row shape. D-25 defines the CHANGELOG entry and forbids `MB-nn`
identifiers in it. D-26 requires `make api-surface` as the last gate and expects no change, because
the `doc-examples` crate is `publish = false` and has zero matches in the committed export baseline.
D-00b's re-run rule applies here: if the branch has moved with a non-`.planning` change since the
Phase 34 baseline, re-run the build and the doc-examples script rather than trusting that baseline.
D-00e fixes the exact sequence and the mermaid-install cleanliness check. D-04 keeps PROJECT.md
untouched — the CHANGELOG bullet, not PROJECT.md, is where the appendix disposition is stated.

Purpose: turn sixty closed rows into recorded, re-runnable proof and a release note a reader can use.
Output: `35-EVIDENCE.md`, the `[0.10.0]` `### Documentation` subsection, and the completed deferred
register.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-mdbook-currency/35-CONTEXT.md
@.planning/phases/35-mdbook-currency/35-RESEARCH.md
@.planning/phases/35-mdbook-currency/35-01-SUMMARY.md
@.planning/phases/35-mdbook-currency/35-02-SUMMARY.md
@.planning/phases/35-mdbook-currency/35-03-SUMMARY.md
@.planning/phases/35-mdbook-currency/35-04-SUMMARY.md
@.planning/phases/35-mdbook-currency/35-05-SUMMARY.md
@.planning/phases/35-mdbook-currency/35-06-SUMMARY.md
@.planning/phases/35-mdbook-currency/35-07-SUMMARY.md
@.planning/phases/35-mdbook-currency/35-08-SUMMARY.md
@.planning/phases/35-mdbook-currency/35-09-SUMMARY.md
</context>

<artifacts_this_phase_produces>
New symbols created by Phase 35 — exclude from drift verification against the pre-phase tree. This
plan creates `.planning/phases/35-mdbook-currency/35-EVIDENCE.md` and the `CHANGELOG.md` heading
`### Documentation` under `## [0.10.0]`, and completes
`.planning/phases/35-mdbook-currency/deferred-items.md` (seeded by plan 35-01).

Created by sibling plans and referenced here: `docs/src/user-guides/superstep-engine.md` and
`crates/doc-examples/src/superstep_engine.rs` with anchors `build_graph`, `configure_limits`,
`run_engine`, `inspect_waypoints` (plan 35-01); `crates/doc-examples/src/paladin_agents.rs`,
`arsenal_tools.rs`, `herald_output.rs`, `battalion_patterns.rs`, `sanctum_vector_memory.rs` and
their five `pub mod` lines in `crates/doc-examples/src/lib.rs` (plan 35-02);
`docs/src/contributing/adr-index.md`, the new `[Architecture Decisions](contributing/adr-index.md)`
nav entry and the retitled "Adapter Development Guide" nav entry (plan 35-05); requirement IDs
`CURR-06`…`CURR-10` (plan 35-01).
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: Run the full gate and write 35-EVIDENCE.md with the sixty-row closure table</name>
  <files>.planning/phases/35-mdbook-currency/35-EVIDENCE.md</files>
  <precondition>All eight wave-2 plans have merged back and their SUMMARY files are present in `.planning/phases/35-mdbook-currency/`; `mdbook`, `mdbook-mermaid` and `mdbook-linkcheck` resolve on PATH at the `docs.yml` pins 0.4.40 / 0.13.0 / 0.7.7.</precondition>
  <read_first>
    - all nine `.planning/phases/35-mdbook-currency/35-0N-SUMMARY.md` closure tables (the rows this file consolidates)
    - `.planning/phases/34-documentation-currency-audit/34-AUDIT.md` §5 (the authoritative sixty-ID list to reconcile against)
    - `.planning/phases/34-documentation-currency-audit/34-EVIDENCE.md` (the house format for command captures and the Measurement Header shape)
    - `.github/workflows/docs.yml` lines 35-70 (the exact sequence and the three `cargo install --locked` pins to quote)
    - `.planning/phases/35-mdbook-currency/35-CONTEXT.md` D-21 (the exit greps and the allowlist row shape) and D-23 (what the evidence file must carry)
    - `Makefile` around line 378 (the `api-surface` target)
  </read_first>
  <action>
    First honour D-00b: run `git diff --stat ee1fb160f8e743e638b32beb6c4e32be4ede9325..HEAD -- . ':!.planning' ':!docs' ':!CHANGELOG.md'` and record the result. If it shows a change outside this phase's own paths — a Phase 36 source or `doc-examples` edit landing on the branch — say so explicitly in the evidence file and treat every measurement below as re-derived here rather than inherited from the Phase 34 baseline.

    Run the exact `docs.yml` sequence in order and capture every command and its full output
    verbatim: `mdbook-mermaid install docs/`, then `git status --porcelain -- docs` (which must print
    nothing; if it does not, restore with `git checkout -- docs/` and record both the drift and the
    restoration per D-00e), then `mdbook build docs/`, then `./scripts/check-doc-examples.sh`, then
    `./scripts/check-doc-config.sh`. Capture `mdbook --version`, `mdbook-mermaid --version` and
    `mdbook-linkcheck --version` and annotate each against its `docs.yml` pin. Then run
    `make api-surface` and capture its output — D-26 expects no change, since the doc-examples crate
    is `publish = false` and has zero matches in `.project/current-exports.txt`.

    Run the D-21 exit greps and record each command with its verbatim output: the version-pin grep
    `grep -rnE '"0\.[5-9]\.[0-9]+"' docs/src`; the MSRV-drift grep
    `grep -rnE '\b1\.(70|75|85)(\.[0-9]+)?\b' docs/src`; the two import greps
    `grep -rn 'paladin::paladin_ports::' docs/src` and
    `grep -rn 'paladin::infrastructure::adapters::llm::' docs/src`; the vocabulary grep
    `grep -rniE '\bQuartermaster\b' docs/src`; the fabricated-CI-name grep
    `grep -rn 'test\.yml\|build-release' docs/src`; and the Phase 31 re-check, which for each of the
    ten pages on that hit list greps the bare count token and the split usage type and compares, as
    `34-AUDIT.md` §2's own subsection does. Each must be empty or fully allowlisted. Build the
    allowlist table with one row per surviving match in the shape `file:line | matched text | reason`,
    where the reason names why it is historical — a Timeline row, a release-history entry, a dated
    snapshot toolchain. A match with no such reason is a miss, not an allowlist entry: fix the page
    and re-run rather than allowlisting it.

    Write `.planning/phases/35-mdbook-currency/35-EVIDENCE.md` with a Measurement Header (date, HEAD
    SHA, tool versions against pins), the consolidated closure table with one row per MB ID in
    `34-AUDIT.md` §5 order — sixty rows, columns MB ID, page, disposition, commit, how the §5 finding
    was addressed — assembled from the nine SUMMARY tables, then the verbatim gate captures, the
    api-surface capture, and the exit-grep section with its allowlist table. Reconcile the table
    against §5 before committing: if any ID is missing, or any row's disposition disagrees with its
    plan SUMMARY, stop and report rather than papering over it. Commit with subject
    `docs(35): record the Phase 35 gate evidence and the sixty-row closure table`.
  </action>
  <verify>
    <automated>cd /workspace &amp;&amp; test -f .planning/phases/35-mdbook-currency/35-EVIDENCE.md &amp;&amp; for n in $(seq -w 1 60); do grep -q "MB-$n" .planning/phases/35-mdbook-currency/35-EVIDENCE.md || exit 1; done &amp;&amp; grep -q 'No broken links found' .planning/phases/35-mdbook-currency/35-EVIDENCE.md &amp;&amp; grep -q 'api-surface' .planning/phases/35-mdbook-currency/35-EVIDENCE.md &amp;&amp; mdbook build docs/ &amp;&amp; ./scripts/check-doc-examples.sh &amp;&amp; ./scripts/check-doc-config.sh &amp;&amp; make api-surface</automated>
  </verify>
  <acceptance_criteria>
    - `for n in $(seq -w 1 60); do grep -q "MB-$n" .planning/phases/35-mdbook-currency/35-EVIDENCE.md || echo "missing MB-$n"; done` prints nothing — all sixty IDs are in the closure table
    - Every closure row's disposition is one of `corrected`, `archived`, `new` or `retitled` and names a commit SHA or subject
    - The evidence file quotes `mdbook --version`, `mdbook-mermaid --version` and `mdbook-linkcheck --version` and annotates each against its `docs.yml` pin
    - The evidence file contains the verbatim `git status --porcelain -- docs` result taken immediately after `mdbook-mermaid install docs/`
    - The evidence file contains the linkcheck summary line `No broken links found` and both scripts' result lines
    - The evidence file contains the `make api-surface` output and states that no public-surface change occurred
    - All seven D-21 checks appear with their commands and verbatim output; every surviving match has an allowlist row naming file, line, matched text and a historical reason
    - `mdbook build docs/`, `./scripts/check-doc-examples.sh`, `./scripts/check-doc-config.sh` and `make api-surface` all exit 0
  </acceptance_criteria>
  <done>The phase's proof exists as one re-runnable record: sixty closed rows, a green gate sequence and empty-or-allowlisted exit greps.</done>
</task>

<task type="auto">
  <name>Task 2: Write the CHANGELOG [0.10.0] Documentation subsection</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - `CHANGELOG.md` lines 1-60 (the `## [0.10.0]` entry's opening and subsection order) and lines 380-445 (the `### Fixed` and `### Known limitations` subsections between which the new heading goes)
    - `CHANGELOG.md` around line 1062 (the `[0.5.0]` `### Documentation` subsection — the precedent for tone and length)
    - `.planning/phases/35-mdbook-currency/35-EVIDENCE.md` (the closure table this entry summarises)
    - `docs/src/SUMMARY.md` (the nav sections the bullets are organised by)
    - `.planning/phases/35-mdbook-currency/35-CONTEXT.md` D-25 (placement, bullet shape and the identifier prohibition) and D-04 (the appendix disposition statement)
  </read_first>
  <action>
    Insert a `### Documentation` subsection into the `## [0.10.0]` entry, after `### Fixed` and
    before `### Known limitations`, matching the `[0.5.0]` precedent's reader-facing tone. Write one
    bullet for the new superstep-engine guide, naming what it documents — battlefield state,
    superstep execution, waypoint checkpointing, the engine limits and their environment overrides —
    and that it is linked from the User Guides nav. Write one bullet per nav section summarising what
    was corrected there: Getting Started and API Reference (version pins at 0.10.0, MSRV 1.88, the
    feature-flag and crate inventories regenerated from the manifests, the stable-API catalogue
    rerooted on the workspace crate paths), User Guides and Architecture (constructor and trait
    samples compile-verified through the documentation examples crate, the domain model extended with
    the engine's state entities), Deployment and Operations (the CI pages rebuilt on the real job
    inventory, the tracing sections pointed at the shipped OTLP export), Contributing (the adapter
    guide retitled to match its content and a new Architecture Decisions index added), and Appendix
    (the CLI pages rebuilt from live `--help` output, the port and adapter import paths corrected).
    Write one bullet naming the five archived appendix pages and the live page each now points at,
    and say plainly that archiving records a disposition and removes nothing.

    Per D-25 use no `MB-nn` identifier anywhere — they are planning-corpus identifiers, and Phase 36
    will append its own bullets under this same heading. Per D-04 do not edit PROJECT.md; if the
    appendix scope needs stating, state it here in one clause. Commit with
    `git add -- CHANGELOG.md && git commit -q -m "docs(35): add the [0.10.0] Documentation changelog entry" -- CHANGELOG.md`.
  </action>
  <verify>
    <automated>cd /workspace &amp;&amp; awk '/^## \[0\.10\.0\]/{i=1} /^## \[0\.9/{i=0} i &amp;&amp; /^### /{print NR": "$0}' CHANGELOG.md | grep -q 'Documentation' &amp;&amp; ! grep -qE '\bMB-[0-9]{2}\b' CHANGELOG.md &amp;&amp; awk '/^## \[0\.10\.0\]/{i=1} /^## \[0\.9/{i=0} i &amp;&amp; /^### Fixed/{f=NR} i &amp;&amp; /^### Documentation/{d=NR} i &amp;&amp; /^### Known limitations/{k=NR} END{exit !(f&lt;d &amp;&amp; d&lt;k)}' CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `CHANGELOG.md`'s `## [0.10.0]` entry contains a `### Documentation` heading whose line number is greater than that entry's `### Fixed` heading and less than its `### Known limitations` heading
    - The subsection contains a bullet naming the superstep-engine guide, at least five bullets covering the nav sections, and one bullet naming the five archived appendix pages with their live replacements
    - `grep -cE '\bMB-[0-9]{2}\b' CHANGELOG.md` returns `0`
    - `git diff HEAD~1 --name-only` lists only `CHANGELOG.md`
    - `git log --oneline -1 -- CHANGELOG.md` subject starts with `docs(35):`
  </acceptance_criteria>
  <done>The release record carries a reader-facing Documentation entry in the position Phase 36 will append to.</done>
</task>

<task type="auto">
  <name>Task 3: Fold the deferred observations and re-run the gate on the final commit</name>
  <files>.planning/phases/35-mdbook-currency/deferred-items.md, .planning/phases/35-mdbook-currency/35-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/35-mdbook-currency/deferred-items.md` (the register plan 35-01 seeded)
    - every `## Deferred observations` section across the nine plan SUMMARYs
    - `.planning/phases/34-documentation-currency-audit/deferred-items.md` (the five Phase 34 entries this register must not absorb or duplicate)
    - `.planning/phases/35-mdbook-currency/35-EVIDENCE.md` (the file this task appends the final run to)
    - `.planning/phases/35-mdbook-currency/35-CONTEXT.md` D-26 and D-27
  </read_first>
  <action>
    Collect every `## Deferred observations` entry the nine page plans recorded and add one register
    row each, keeping the seeded columns — Item, Observed at, Why not fixed here, Proposed
    classification, Owner. Per D-26 any observation that wants an existing `crates/doc-examples`
    anchor or `support.rs` changed is owned by Phase 36 as an `EX-nn` pointer, with the page, the
    anchor and what the page wanted recorded, and the include left in place. Per D-27 any defect
    noticed on a page the audit settled `current` is recorded with a proposed classification, never
    fixed. Do not absorb, restate or renumber the five entries in the Phase 34 register; link to it
    instead. If no plan recorded an observation, say so explicitly — the seeded rows stand and the
    register states that the nine page plans recorded none.

    Then re-run the full gate on the resulting final commit and append the run to
    `35-EVIDENCE.md` under a clearly labelled final-run heading: the `docs.yml` sequence in order
    with the mermaid-install cleanliness check, `make api-surface`, and the seven D-21 checks, all
    verbatim, so the last recorded run is the one Phase 37 re-seals against. Commit both files with
    subject `docs(35): fold the deferred observations and record the final gate run`.
  </action>
  <verify>
    <automated>cd /workspace &amp;&amp; grep -q 'Owner' .planning/phases/35-mdbook-currency/deferred-items.md &amp;&amp; grep -qi 'final' .planning/phases/35-mdbook-currency/35-EVIDENCE.md &amp;&amp; mdbook-mermaid install docs/ &amp;&amp; test -z "$(git status --porcelain -- docs)" &amp;&amp; mdbook build docs/ &amp;&amp; ./scripts/check-doc-examples.sh &amp;&amp; ./scripts/check-doc-config.sh &amp;&amp; make api-surface &amp;&amp; ! grep -rqiE '\bQuartermaster\b' docs/src &amp;&amp; ! grep -rq 'paladin::paladin_ports::' docs/src &amp;&amp; ! grep -rq 'paladin::infrastructure::adapters::llm::' docs/src</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/35-mdbook-currency/deferred-items.md` holds one row per `## Deferred observations` entry across the nine plan SUMMARYs, or states explicitly that none was recorded
    - Every register row names a proposed classification and an owner, and no row duplicates or renumbers an entry from the Phase 34 register
    - `35-EVIDENCE.md` contains a final-run section with the `docs.yml` sequence, `make api-surface` and the seven D-21 checks, all verbatim and all taken after the last content commit
    - `mdbook-mermaid install docs/` followed by `git status --porcelain -- docs` prints nothing (D-00e)
    - `mdbook build docs/` exits 0 with `No broken links found`; `./scripts/check-doc-examples.sh`, `./scripts/check-doc-config.sh` and `make api-surface` all exit 0
    - `grep -rniE '\bQuartermaster\b' docs/src`, `grep -rn 'paladin::paladin_ports::' docs/src` and `grep -rn 'paladin::infrastructure::adapters::llm::' docs/src` all return no lines
    - `git log --oneline -1` subject starts with `docs(35):`
  </acceptance_criteria>
  <done>The deferred register is complete, and the last recorded gate run is the one Phase 37 re-seals against.</done>
</task>

</tasks>

<!-- planner-discipline-allow: \bQuartermaster\b -->
<!-- planner-discipline-allow: paladin::paladin_ports:: -->
<!-- planner-discipline-allow: paladin::infrastructure::adapters::llm:: -->
<!-- planner-discipline-allow: test\.yml\|build-release -->
<!-- planner-discipline-allow: MB-[0-9]{2} -->
<!-- planner-discipline-allow: "0\.[5-9]\.[0-9]+" -->

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository -> published book (`docs/book/html/`) | The final gate run is the last check before the book publishes |
| repository -> crates.io | `make api-surface` is the gate that proves this phase moved no public surface before Phase 37 publishes |
| planning corpus -> release record | The CHANGELOG is reader-facing; planning identifiers must not leak into it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-16 | Tampering | the public API surface baseline | medium | mitigate | `make api-surface` runs as the last gate and must report no change; the baseline is not regenerated in this phase (D-26), so a doc-examples module that accidentally exported something publishable would fail the gate rather than silently widen the surface Phase 37 publishes |
| T-35-17 | Repudiation | `35-EVIDENCE.md` allowlist table | medium | mitigate | Every allowlist row must name file, line, matched text and a historical reason; a match without such a reason is treated as a miss to fix, not an entry to add, so the exit greps cannot be made to read clean by widening the allowlist |
| T-35-SC | Tampering | npm/pip/cargo installs | low | accept | No registry package is installed. The three mdBook tools are pre-existing `docs.yml` pins installed with `--locked` at fixed versions and their versions are recorded verbatim in the evidence file against those pins |
</threat_model>

<verification>
- The full `docs.yml` sequence green on the final commit, recorded verbatim in `35-EVIDENCE.md`
- `git status --porcelain -- docs` empty immediately after `mdbook-mermaid install docs/` (D-00e)
- `make api-surface` reports no change (D-26)
- All seven D-21 checks empty or fully allowlisted with reasons
- All sixty MB IDs present in the closure table and in `git log --oneline --grep 'MB-'`
- `grep -cE '\bMB-[0-9]{2}\b' CHANGELOG.md` returns `0` (D-25)
- `git diff --name-only 35-01~1..HEAD -- .planning/PROJECT.md` is empty (D-04)
</verification>

<success_criteria>
- `35-EVIDENCE.md` carries the sixty-row closure table, the verbatim gate run and the exit greps with their allowlist (D-21, D-23)
- `CHANGELOG.md` `[0.10.0]` carries the `### Documentation` subsection in the D-25 position with no planning identifiers
- The phase-local deferred register is complete and does not absorb the Phase 34 entries (D-26, D-27)
- `make api-surface` reports no change, as D-26 anticipates
- Phase 35 is ready for `/gsd-verify-work 35`
</success_criteria>

<output>
Create `.planning/phases/35-mdbook-currency/35-10-SUMMARY.md` when done. This plan closes no MB ID of
its own, so its closure table is the consolidated sixty-row table by reference to `35-EVIDENCE.md`;
state explicitly that all sixty IDs reconcile against `34-AUDIT.md` §5 and name any that did not.
</output>