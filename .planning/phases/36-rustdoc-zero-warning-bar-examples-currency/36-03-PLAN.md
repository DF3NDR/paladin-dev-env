---
phase: 36-rustdoc-zero-warning-bar-examples-currency
plan: 03
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - crates/paladin-core/src/platform/container/trace.rs
  - crates/paladin-core/src/platform/container/directive.rs
  - crates/paladin-core/src/platform/container/structured.rs
  - crates/paladin-core/src/platform/container/webhook.rs
  - .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-03-core.txt
autonomous: true
requirements: [CURR-11, CURR-12, CURR-15]
must_haves:
  truths:
    - "`RUSTDOCFLAGS=\"-D warnings\" cargo doc -p paladin-ai-core --all-features --no-deps` exits 0."
    - "A forced re-documentation of `paladin-ai-core` under default features emits zero rustdoc diagnostic lines, down from the 14 the audit and the re-baseline both measured."
    - "All 28 `paladin-ai-core` rows in `34-AUDIT.md` §6 — RD-52 through RD-65 and their followers RD-103 through RD-116 — are closed across the 14 location groups they name."
    - "`cargo check --workspace --all-targets --all-features` exits 0 and `cargo test --workspace --doc` stays green."
    - "`make api-surface` reports the surface unchanged."
  artifacts:
    - "crates/paladin-core/src/platform/container/trace.rs — resolvable links to the trace envelope, its event enum and the field-change type"
    - "crates/paladin-core/src/platform/container/directive.rs — de-linked mention of the engine's state-node entry point"
    - "crates/paladin-core/src/platform/container/structured.rs — resolvable or de-linked JSON-extraction mention"
    - "crates/paladin-core/src/platform/container/webhook.rs — resolvable links to the delivery row type and its schema-version constant"
    - ".planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-03-core.txt"
  key_links:
    - "The trace module's doc header names ten items — the record envelope, the event enum, the field-change type and four event variants — all declared later in the same file; this is the Pitfall 1 bare-shorthand family validated in plan 36-01."
    - "The directive file names an engine type that belongs to paladin-battalion, not to paladin-ai-core; the innermost crate must not gain a dependency to make a doc link resolve — de-link instead."
    - "The webhook file's delivery row type and schema-version constant are both unconditional public items of this same crate, confirmed in 36-RESEARCH.md's Primary sources, so both links resolve with an in-crate path."
  prohibitions:
    - statement: "No private item is widened from pub(crate) to pub to satisfy a documentation link (D-05)."
      status: recalled
      verification: flagged-unverified
    - statement: "No rustdoc lint-suppression attribute is added to silence a private intra-doc link (D-05)."
      status: recalled
      verification: flagged-unverified
    - statement: "No crate gains a new dependency in order to make a doc link resolve — dependencies flow inward only and the core crate imports nothing from application or infrastructure (CLAUDE.md)."
      status: recalled
      verification: flagged-unverified
    - statement: "No new rustdoc prose is written beyond what a link fix requires (D-04)."
      status: recalled
      verification: flagged-unverified
---

<objective>
Close every `paladin-ai-core` rustdoc row — 28 of the 143, in 14 location groups, ten of which sit
in one module doc block.

This plan implements D-00b (the §6 work list and stable IDs), D-00c (a mention of an item the tree
does not expose is corrected toward the tree, never the reverse), D-04 (all rows are link defects,
sized S, no new prose), D-05 (private intra-doc link technique), D-06 (unresolved link technique
including the explicit-path form), D-09 (lead-row discipline), D-10 (per-crate verification as
fixes land), D-24 (closure table), D-26 (one commit per crate) and D-28 (`make api-surface` before
the commit).

Purpose: `paladin-ai-core` is the second-largest block after `paladin-battalion` — 14 of the 65
default-feature diagnostics — and its trace module is the densest single doc block in the phase.
Note the package name is `paladin-ai-core` while the directory is `crates/paladin-core`;
`cargo doc -p paladin-core` is not a valid invocation anywhere in this phase.

Spec-less probe fallback skipped (visible, recorded): Phase 36 has no SPEC and its requirement IDs
were minted by this planning run, so `must_haves` are derived from the five ROADMAP Phase 36
Success Criteria and the CONTEXT decisions.

Output: `paladin-ai-core` documenting clean under both bar commands, in one commit whose subject
cites the RD range.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-CONTEXT.md
@.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-RESEARCH.md
@.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-PATTERNS.md
@.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The trace module's doc header — ten unresolved links (10 groups, 20 rows)</name>
  <files>crates/paladin-core/src/platform/container/trace.rs</files>
  <read_first>
    - crates/paladin-core/src/platform/container/trace.rs lines 1-60 (the whole module doc header carrying all ten cited lines: 3, 6, 17, 21, 22, 25, 36, 37, 44 and 45)
    - crates/paladin-core/src/platform/container/trace.rs lines 300-420 (the `TraceRecord` declaration and the `TraceEvent` enum with its `DeltaMerged`, `NodeProgress` and `ParleyRaised` variants — confirm each variant's exact spelling and each item's real path before writing a link)
    - crates/paladin-core/src/platform/container/trace.rs (locate `FieldChange` and its value field or accessor — the audit cites both the type and its member)
    - crates/paladin-memory/src/token_counter/mod.rs (plan 36-01's committed fix — the validated form for a module doc header linking to an item declared later in the same file)
    - .planning/phases/34-documentation-currency-audit/34-AUDIT.md §3 rows RD-54 through RD-63 (each carries the verbatim first line of its own diagnostic, naming the exact unresolved target)
  </read_first>
  <action>
All ten groups are the D-06 unresolved-link kind, all inside the trace module's own doc header, all
naming items declared later in the same file. The cited lines and their targets are: line 3 names
`TraceEvent`; line 6 names `TraceRecord`; line 17 names `TraceRecord`; line 21 names
`TraceEvent::DeltaMerged`; line 22 names `FieldChange`; line 25 names `FieldChange::value`; line 36
names `TraceEvent::NodeProgress`; line 37 names `TraceEvent::ParleyRaised`; line 44 names
`TraceEvent::NodeProgress`; line 45 names `TraceEvent::ParleyRaised`. Each has exactly one
all-features follower closed by the same source-line fix (RD-105 through RD-114), per D-09.

Apply D-06 to every one: resolve the link to the item's real path with an explicit intra-doc
target, using the same form plan 36-01 validated for a module doc header naming a sibling item.
Before writing each link, confirm from the declarations in this same file that the item and — for
the variant and field references — the exact member name still exist. If a mention names something
the tree no longer exposes under that name, correct the mention to the shipped name per D-00c
using `MIGRATION.md` §9.2 as the rename register, rather than inventing a target.

Where a target turns out to be private rather than merely unresolved, apply D-05 instead: plain
code font or a reword naming the public entry point. Do not widen visibility and do not add a
lint-suppression attribute. Do not rewrite prose beyond what each link change requires (D-04).

Do not commit yet — D-26 requires a single `paladin-ai-core` commit, which Task 2 makes.
  </action>
  <verify>
    <automated>touch crates/paladin-core/src/lib.rs &amp;&amp; cargo doc -p paladin-ai-core --no-deps 2>&amp;1 | tee /tmp/36-03-t1.txt &amp;&amp; test "$(grep -c 'container/trace' /tmp/36-03-t1.txt)" = 0 &amp;&amp; cargo check -p paladin-ai-core --all-features</automated>
  </verify>
  <acceptance_criteria>
    - A forced re-documentation of `paladin-ai-core` under default features produces a capture containing a `Documenting paladin-ai-core` line and zero lines referencing the trace container file.
    - `grep -c '^//!' crates/paladin-core/src/platform/container/trace.rs` is unchanged from its pre-edit value.
    - `grep -rn 'allow(rustdoc::' crates/paladin-core/src` produces no output.
    - `git diff crates/paladin-core/src/platform/container/trace.rs` adds and removes only lines beginning with a doc-comment marker.
    - `cargo check -p paladin-ai-core --all-features` exits 0.
  </acceptance_criteria>
  <done>The trace module's doc header resolves every link it makes; RD-54…RD-63 and their followers RD-105…RD-114 are fixed in the working tree.</done>
</task>

<task type="auto">
  <name>Task 2: directive, structured and webhook containers (4 groups, 8 rows) and the single paladin-ai-core commit</name>
  <files>crates/paladin-core/src/platform/container/directive.rs, crates/paladin-core/src/platform/container/structured.rs, crates/paladin-core/src/platform/container/webhook.rs, .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-03-core.txt</files>
  <read_first>
    - crates/paladin-core/src/platform/container/directive.rs lines 1-30 (the module doc block at line 3 naming the engine's state-node run entry point)
    - crates/paladin-core/src/platform/container/structured.rs lines 1-40 (the doc block at line 13 naming a JSON-extraction helper, and whether that helper exists in this crate at all)
    - crates/paladin-core/src/platform/container/webhook.rs lines 1-60 and 170-200 (the doc block at lines 19-20, the delivery row struct and the schema-version constant it names, and the "no signing key on the row" prohibition recorded in this file's own header, which the wording must not contradict)
    - crates/paladin-core/Cargo.toml (confirms the package name is paladin-ai-core and that this crate depends on neither paladin-battalion nor paladin-ports, so a cross-crate link to the engine's state-node type is not available)
    - crates/paladin-core/src/platform/container/trace.rs (as fixed in Task 1 — the in-plan reference for the link form)
    - .planning/phases/34-documentation-currency-audit/34-AUDIT.md §3 rows RD-52, RD-53, RD-64 and RD-65
  </read_first>
  <action>
`directive.rs:3` — unresolved link to `StateNode::run` (RD-52, follower RD-103). `StateNode` is
paladin-battalion's engine type, and `paladin-ai-core` is the innermost crate: it must not gain a
dependency on an outer crate to make a doc link resolve. Apply D-05's technique in this D-06
situation — put the mention in plain code font, or reword to name the directive contract this crate
actually owns.

`structured.rs:13` — unresolved link to `extract_json` (RD-53, follower RD-104). Determine first
whether that helper exists in this crate and is public: if yes, resolve it with an explicit
in-crate path (D-06); if it is private or lives elsewhere, use plain code font (D-05). Do not widen
its visibility.

`webhook.rs:19` and `:20` — unresolved links to `WebhookDelivery` (RD-64, follower RD-115) and
`WEBHOOK_DELIVERY_SCHEMA_VERSION` (RD-65, follower RD-116). Both are unconditional public items of
this same crate, so both resolve with an explicit in-crate path (D-06). The surrounding prose
records the webhook row's security invariants — do not weaken or restate them; the edit is the link
syntax only.

Then close the crate (D-10, D-26, D-28):
  - Capture the per-crate sweep verbatim into `36-evidence/36-03-core.txt`: a forced
    re-documentation under default features with its diagnostic count, and
    `RUSTDOCFLAGS="-D warnings" cargo doc -p paladin-ai-core --all-features --no-deps` with its
    exit code.
  - Run `cargo fmt --all -- --check`, `cargo check --workspace --all-targets --all-features`,
    `cargo test --workspace --doc` and `make api-surface` (expect no change).
  - Make the single commit per D-26:
    `docs(36): resolve rustdoc links in paladin-ai-core (RD-52…RD-65, RD-103…RD-116)`. Stage with
    `git add -- crates/paladin-core/ .planning/phases/36-*/36-evidence/36-03-core.txt`, never
    `git add .`. Allow a long timeout for the clippy pre-commit hook.
  </action>
  <verify>
    <automated>touch crates/paladin-core/src/lib.rs &amp;&amp; cargo doc -p paladin-ai-core --no-deps 2>&amp;1 | tee /tmp/36-03-core-default.txt &amp;&amp; test "$(grep -c 'warning:' /tmp/36-03-core-default.txt)" = 0 &amp;&amp; RUSTDOCFLAGS="-D warnings" cargo doc -p paladin-ai-core --all-features --no-deps &amp;&amp; cargo fmt --all -- --check &amp;&amp; cargo check --workspace --all-targets --all-features &amp;&amp; cargo test --workspace --doc &amp;&amp; ./scripts/check-api-surface.sh .project/current-exports.txt</automated>
  </verify>
  <acceptance_criteria>
    - `RUSTDOCFLAGS="-D warnings" cargo doc -p paladin-ai-core --all-features --no-deps` exits 0.
    - A forced re-documentation of `paladin-ai-core` under default features produces a capture containing a `Documenting paladin-ai-core` line and zero lines matching `warning:`.
    - `grep -rn 'allow(rustdoc::' crates/paladin-core/src` produces no output.
    - `git diff HEAD~1 -- crates/paladin-core/Cargo.toml` is empty — no dependency was added to make a link resolve.
    - `git diff --stat HEAD~1 -- crates/ src/` lists only paths under `crates/paladin-core/`.
    - `git log -1 --pretty=%s` starts with `docs(36): resolve rustdoc links in paladin-ai-core` and contains `RD-`.
    - `cargo test --workspace --doc` reports 0 failed.
    - `./scripts/check-api-surface.sh .project/current-exports.txt` reports the surface unchanged.
  </acceptance_criteria>
  <done>All 28 `paladin-ai-core` rows are closed, the crate documents clean under both bar commands, and the work is in one commit whose subject cites the RD range.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

This plan creates no new Rust symbols and no new public API — every change is a rewritten
doc-comment line inside an existing `crates/paladin-core` file. SC5 requires `make api-surface` to
report no change.

New file created by this plan:
- `.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-03-core.txt`

New symbols created elsewhere in Phase 36 (listed so drift verification does not flag them):
example targets `token_economy_commissary`, `war_engine_configuration`,
`control_flow_dynamic_routing`, `human_in_the_loop_gate`, `graceful_shutdown`,
`agent_runtime_middleware`, `structured_output_schema`, `sanctum_rag_retrieval`,
`platform_api_client`, `webhook_receiver`, `node_result_cache`, `observability_tracing`,
`observability_otel_export`, `eval_scenarios_demo`; Makefile targets `doc-check` and
`check-examples`; the `.pre-commit-config.yaml` hook id `doc-check`; the CI step name
`Check documentation (all features, -D warnings)`; requirement IDs `CURR-11`…`CURR-15`;
`36-EVIDENCE.md` and `36-CI-EVIDENCE.md`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository → published rustdoc site | the core crate's doc comments are the innermost crate's public contract and render publicly |
| doc-comment edit → hexagonal dependency direction | adding a dependency to make a link resolve would invert the architecture's inward-only rule |
| doc-comment edit → public API baseline | a link "fixed" by widening visibility silently moves the published surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-07 | Tampering | `paladin-ai-core` dependency graph | high | mitigate | The directive container names an item owned by an outer crate; the fix de-links rather than adding a dependency. Acceptance criterion asserts the commit adds no line to that crate's manifest. |
| T-36-08 | Tampering | public API surface | high | mitigate | `make api-surface` runs before the commit and must report no change (D-28); D-05 forbids widening visibility, and the JSON-extraction helper specifically must not gain a public declaration. |
| T-36-09 | Information Disclosure | the webhook container's doc block | medium | mitigate | That container's own header records the "no signing key on the row" prohibition and the signature invariant; this plan changes link syntax only and must not reword, weaken or restate those security statements. The acceptance criterion restricts the diff to doc-comment lines. |
| T-36-SC | Tampering | cargo package installs | low | accept | No package-manager install runs in this plan; `36-RESEARCH.md`'s Package Legitimacy Audit records "not applicable — this phase installs no new external package". |
</threat_model>

<verification>
- `RUSTDOCFLAGS="-D warnings" cargo doc -p paladin-ai-core --all-features --no-deps` exits 0.
- A forced default-feature re-documentation of `paladin-ai-core` emits zero lines matching `warning:`, down from 14.
- `cargo check --workspace --all-targets --all-features` exits 0.
- `cargo test --workspace --doc` reports 0 failed.
- `cargo fmt --all -- --check` and `make api-surface` are green.
- Exactly one commit touches `crates/paladin-core/` and it touches no other crate (D-26), and it adds no line to that crate's manifest.
</verification>

<success_criteria>
- All 14 `paladin-ai-core` location groups are fixed at their cited file:line, closing RD-52…RD-65 and RD-103…RD-116.
- `paladin-ai-core`'s contribution to both bar commands is zero.
- The hexagonal dependency direction is unchanged and `make api-surface` reports no change.
</success_criteria>

<output>
Create `.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-03-SUMMARY.md` when done, carrying the D-24 closure table: `ID | file:line (cited) | file:line (actual, if different) | kind | fix | commit`, with every follower listed on its lead's row.
</output>
