---
phase: 36-rustdoc-zero-warning-bar-examples-currency
plan: 04
type: execute
wave: 3
depends_on: ["36-01", "36-02"]
files_modified:
  - crates/paladin-llm/src/http_status.rs
  - crates/paladin-llm/src/redaction.rs
  - crates/paladin-llm/src/services/commissary.rs
  - crates/paladin-llm/src/compat/engine.rs
  - crates/paladin-llm/src/gemini/adapter.rs
  - crates/paladin-web/src/thread_controller.rs
  - crates/paladin-web/src/dev_ui_controller.rs
  - .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-04-llm-web.txt
autonomous: true
requirements: [CURR-11, CURR-12, CURR-15]
must_haves:
  truths:
    - "`RUSTDOCFLAGS=\"-D warnings\" cargo doc -p paladin-llm --all-features --no-deps` exits 0."
    - "`RUSTDOCFLAGS=\"-D warnings\" cargo doc -p paladin-web --all-features --no-deps` exits 0."
    - "A forced re-documentation of `paladin-llm` and of `paladin-web` under default features emits zero rustdoc diagnostic lines, down from 4 and 3 respectively."
    - "All 13 `paladin-llm` rows (RD-47, RD-48, RD-49, RD-50, RD-117, RD-118, RD-119, RD-120, RD-121, RD-122, RD-123, RD-124, RD-125) and all 11 `paladin-web` rows (RD-07, RD-08, RD-09, RD-129, RD-130, RD-131, RD-132, RD-133, RD-134, RD-135, RD-136) are closed."
    - "Every fix holds under both the default build and the all-features build — twelve of these rows exist only under all features because the doc'd items themselves sit behind the openai-compatible, gemini and dev-ui feature gates."
    - "`cargo check --workspace --all-targets --all-features` exits 0 and `cargo test --workspace --doc` stays green."
    - "`make api-surface` reports the surface unchanged."
  artifacts:
    - "crates/paladin-llm/src/http_status.rs — generic placeholders wrapped in backticks, not HTML-escaped"
    - "crates/paladin-llm/src/redaction.rs and src/services/commissary.rs — de-linked private constant mentions"
    - "crates/paladin-llm/src/compat/engine.rs and src/gemini/adapter.rs — de-linked private mentions under their feature gates"
    - "crates/paladin-web/src/thread_controller.rs — de-linked private mentions"
    - "crates/paladin-web/src/dev_ui_controller.rs — resolvable or de-linked mentions under the dev-ui gate"
    - ".planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-04-llm-web.txt"
  key_links:
    - "The HTTP-status file's lines 6 and 7 emit unclosed-HTML-tag diagnostics because two angle-bracketed placeholders are written bare in a doc comment; the fix is backticks, never an HTML entity (D-08)."
    - "The twelve all-features-only rows here trace to three known feature gates — openai-compatible, gemini and dev-ui — and in every case the doc'd item is itself behind the gate, so D-07's rule that a doc comment may link to a feature-gated item only when the doc'd item is gated by the same feature is satisfiable in place."
    - "The redaction module's doc block states the redact-before-truncate credential invariant the security instructions name; the link fix must not reword that statement."
  prohibitions:
    - statement: "No private item is widened from pub(crate) to pub to satisfy a documentation link (D-05)."
      status: recalled
      verification: flagged-unverified
    - statement: "No rustdoc lint-suppression attribute is added to silence a private intra-doc link (D-05)."
      status: recalled
      verification: flagged-unverified
    - statement: "Unclosed HTML tags are fixed by wrapping the generic in backticks, never by escaping with an HTML entity (D-08)."
      status: recalled
      verification: flagged-unverified
    - statement: "No docsrs conditional-attribute machinery is introduced — a docs.rs metadata build is out of scope for this phase (D-07)."
      status: recalled
      verification: flagged-unverified
---

<objective>
Close every `paladin-llm` and `paladin-web` rustdoc row — 24 of the 143, in 17 location groups,
twelve of which reproduce only under all features.

This plan implements D-00b (the §6 work list and stable IDs), D-04 (all rows are link and HTML
defects, sized S, no new prose), D-05 (private intra-doc link technique), D-06 (unresolved link
technique), D-07 (feature-gated targets — every fix must hold under both the default and the
all-features build, with no docsrs conditional-attribute machinery introduced), D-08 (redundant
explicit links collapse; unclosed HTML tags get backticks), D-09 (lead-row discipline), D-10
(per-crate verification as fixes land), D-24 (closure table), D-26 (one commit per crate) and D-28
(`make api-surface` before each commit).

Purpose: these two crates carry the whole feature-gated remainder of the defect list. Twelve of the
seventeen groups never appear in the default-feature run at all — they surface only in the
per-crate all-features sweep, which is the only complete enumeration (`36-RESEARCH.md` Pitfall 2).
`36-RESEARCH.md`'s Re-baseline delta already traces each of the twelve to a named gate, so there is
no mystery item here.

Scheduling note: the dependency on 36-02 is a worktree-serialization edge, not a code dependency —
each isolated executor worktree builds a cold `target/` at roughly 16 GB and the volume fits at
most two at once, so no wave may hold more than two build-heavy plans. The dependency on 36-01 is
a genuine prerequisite: its tracer task validates the link technique.

Spec-less probe fallback skipped (visible, recorded): Phase 36 has no SPEC and its requirement IDs
were minted by this planning run, so `must_haves` are derived from the five ROADMAP Phase 36
Success Criteria and the CONTEXT decisions.

Output: `paladin-llm` and `paladin-web` documenting clean under both bar commands, in two commits —
one per crate — whose subjects cite their RD ranges.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-CONTEXT.md
@.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-RESEARCH.md
@.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-PATTERNS.md
@.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: paladin-llm — 9 groups, 13 rows across http_status, redaction, commissary, compat engine and the gemini adapter</name>
  <files>crates/paladin-llm/src/http_status.rs, crates/paladin-llm/src/redaction.rs, crates/paladin-llm/src/services/commissary.rs, crates/paladin-llm/src/compat/engine.rs, crates/paladin-llm/src/gemini/adapter.rs, .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-04-llm-web.txt</files>
  <read_first>
    - crates/paladin-llm/src/http_status.rs lines 1-20 (the two doc lines 6 and 7 that write a status placeholder and a body placeholder bare, producing the two unclosed-HTML-tag diagnostics)
    - crates/paladin-llm/src/redaction.rs lines 150-180 (the `redact_secret_patterns` doc block at line 164 and the private `JWT_MIN_SEGMENT_LEN` constant it links to; also the redact-before-truncate invariant this module owns)
    - crates/paladin-llm/src/services/commissary.rs lines 75-105 (the `pessimistic_tokens_per_1000_bytes` doc block at line 89 and the private constant it links to)
    - crates/paladin-llm/src/compat/engine.rs lines 100-130, 190-215 and 1030-1055 (the `temperature`, `redirect_policy` and `available_models` doc blocks and the private `CompatEngine::build_request`, `CompatEngine::map_error` and `classify_fetch_failure` items they link to)
    - crates/paladin-llm/src/gemini/adapter.rs lines 20-70 (the module doc block at line 28 naming `GeminiResponse` and the block at line 59 naming `GeminiAdapter::map_error`)
    - crates/paladin-llm/Cargo.toml (confirms the openai-compatible and gemini feature names that gate the two all-features-only files)
    - .github/instructions/security.instructions.md (the credential-handling rules the redaction module's prose states — the link fix must not weaken them)
    - .planning/phases/34-documentation-currency-audit/34-AUDIT.md §3 rows RD-47, RD-48, RD-49 and RD-50, and the per-crate sweep rows RD-119 through RD-123
  </read_first>
  <action>
`http_status.rs:6` and `:7` — unclosed HTML tag diagnostics (RD-49 and RD-50, followers RD-124 and
RD-125). Two doc lines write angle-bracketed placeholders bare and rustdoc parses them as HTML.
Apply D-08: wrap each placeholder in backticks so it renders as code. Do not escape with an HTML
entity and do not delete the placeholder.

`redaction.rs:164` — the public documentation for `redact_secret_patterns` links to the private
item `JWT_MIN_SEGMENT_LEN` (RD-47, follower RD-117). Apply D-05: plain code font, or reword to name
the public behaviour. The surrounding prose states the redact-before-truncate credential invariant
— change the link syntax only, never that statement.

`services/commissary.rs:89` — the public documentation for `pessimistic_tokens_per_1000_bytes`
links to the private constant of the same name in capitals (RD-48, follower RD-118). Apply D-05.
Keep the Medieval-military vocabulary intact (D-00i): `Commissary` is the input-side per-call
window-rationing officer and `Quartermaster` never appears.

`compat/engine.rs:114`, `:201` and `:1041` — three private intra-doc links (RD-119, RD-120,
RD-121), visible only under all features because the whole module is gated by openai-compatible.
Apply D-05 to each: `temperature` to `CompatEngine::build_request`, `redirect_policy` to
`CompatEngine::map_error`, `available_models` to `classify_fetch_failure`. Because the doc'd item is
itself behind the same gate as the target, D-07's rule is satisfied in place — no gate annotation is
needed and none is added.

`gemini/adapter.rs:28` and `:59` — two private intra-doc links (RD-122, RD-123) behind the gemini
gate. Apply D-05: the module doc's response-type mention and the error-mapping mention both become
plain code font or are reworded toward the public adapter contract.

Then close the crate (D-10, D-26, D-28):
  - Capture verbatim into `36-evidence/36-04-llm-web.txt`: a forced re-documentation of
    `paladin-llm` under default features with its diagnostic count, and
    `RUSTDOCFLAGS="-D warnings" cargo doc -p paladin-llm --all-features --no-deps` with its exit
    code.
  - Run `cargo fmt --all -- --check`, `cargo check --workspace --all-targets --all-features` and
    `make api-surface` (expect no change).
  - Commit per D-26:
    `docs(36): resolve rustdoc links in paladin-llm (RD-47…RD-50, RD-117…RD-125)`. Stage with
    `git add -- crates/paladin-llm/`, never `git add .`. Allow a long timeout for the clippy
    pre-commit hook.
  </action>
  <verify>
    <automated>touch crates/paladin-llm/src/lib.rs &amp;&amp; cargo doc -p paladin-llm --no-deps 2>&amp;1 | tee /tmp/36-04-llm-default.txt &amp;&amp; test "$(grep -c 'warning:' /tmp/36-04-llm-default.txt)" = 0 &amp;&amp; RUSTDOCFLAGS="-D warnings" cargo doc -p paladin-llm --all-features --no-deps &amp;&amp; cargo fmt --all -- --check &amp;&amp; cargo check --workspace --all-targets --all-features &amp;&amp; ./scripts/check-api-surface.sh .project/current-exports.txt</automated>
  </verify>
  <acceptance_criteria>
    - `RUSTDOCFLAGS="-D warnings" cargo doc -p paladin-llm --all-features --no-deps` exits 0.
    - A forced re-documentation of `paladin-llm` under default features produces a capture containing a `Documenting paladin-llm` line and zero lines matching `warning:`.
    - `grep -rn 'allow(rustdoc::' crates/paladin-llm/src` produces no output.
    - `grep -rn 'cfg_attr(docsrs' crates/paladin-llm/src` produces no output.
    - `grep -c '&amp;lt;' crates/paladin-llm/src/http_status.rs` is 0 — no HTML-entity escaping was used.
    - `grep -rn 'pub const JWT_MIN_SEGMENT_LEN\|pub const PESSIMISTIC_TOKENS_PER_1000_BYTES' crates/paladin-llm/src` produces no output.
    - `grep -c 'Quartermaster' crates/paladin-llm/src/services/commissary.rs` is 0.
    - `git diff HEAD~1 -- crates/paladin-llm/` adds and removes only doc-comment lines, and `git diff --stat HEAD~1 -- crates/ src/` lists only paths under `crates/paladin-llm/`.
    - `./scripts/check-api-surface.sh .project/current-exports.txt` reports the surface unchanged.
  </acceptance_criteria>
  <done>All 13 `paladin-llm` rows are closed, the crate documents clean under both bar commands, and the work is in one commit citing the RD range.</done>
</task>

<task type="auto">
  <name>Task 2: paladin-web — 8 groups, 11 rows across the thread controller and the dev-ui controller</name>
  <files>crates/paladin-web/src/thread_controller.rs, crates/paladin-web/src/dev_ui_controller.rs, .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-04-llm-web.txt</files>
  <read_first>
    - crates/paladin-web/src/thread_controller.rs lines 470-500, 675-700 and 745-770 (the `limit`, `resume_thread` and `get_thread_history` doc blocks at lines 483, 686 and 757, and the private history-limit constant and error-mapping helper they link to)
    - crates/paladin-web/src/dev_ui_controller.rs lines 1-40, 60-80 and 120-140 (the module doc block at line 3 naming a run-inspector port, line 20 naming the inspector page handler, line 28 naming an inspector-view member, the state block at line 69 naming a not-wired message constant, and the handler block at line 131 naming a script-escaping helper)
    - crates/paladin-web/Cargo.toml (confirms the dev-ui feature name that gates the dev-ui controller, and whether the run-inspector port is reachable from this crate at all)
    - crates/paladin-llm/src/services/commissary.rs (as fixed in Task 1 — the in-plan reference for the de-link wording)
    - .planning/phases/34-documentation-currency-audit/34-AUDIT.md §3 rows RD-07, RD-08 and RD-09, and the per-crate sweep rows RD-129 through RD-133
  </read_first>
  <action>
`thread_controller.rs:483` and `:757` — the public documentation for `limit` and for
`get_thread_history` each links to the private constant `MAX_HISTORY_LIMIT` (RD-07 and RD-09,
followers RD-134 and RD-136). `:686` — the public documentation for `resume_thread` links to the
private helper `map_parley_error` (RD-08, follower RD-135). Apply D-05 to all three: plain code
font, or reword to state the cap and the error mapping as behaviour of the public route. Use the
same wording for both history-limit mentions so the two routes read consistently.

`dev_ui_controller.rs` — five groups (RD-129 through RD-133), all visible only under all features
because the module is gated by dev-ui. Line 3 names `RunInspectorPort`, line 20 names
`dev_ui_inspector_page`, and line 28 names `InspectorView::supersteps` — all three are the D-06
unresolved kind, so first determine each target's real path and visibility: resolve with an
explicit path where the item is public and reachable from this crate, and fall back to plain code
font per D-05 where it is private or not reachable. Lines 69 and 131 are the D-05 private kind —
the state type linking to `NOT_WIRED_MESSAGE` and the page handler linking to `escape_for_script` —
so both become plain code font or a reword toward the public route contract. The script-escaping
helper is security-relevant; de-linking it must not describe its escaping algorithm in more detail
than its own doc comment already does.

Because every doc'd item here is itself behind the dev-ui gate, D-07's rule is satisfied in place.
Do not introduce docsrs conditional-attribute machinery and do not widen visibility.

Then close the crate (D-10, D-26, D-28):
  - Append to `36-evidence/36-04-llm-web.txt`: a forced re-documentation of `paladin-web` under
    default features with its diagnostic count, and
    `RUSTDOCFLAGS="-D warnings" cargo doc -p paladin-web --all-features --no-deps` with its exit
    code.
  - Run `cargo fmt --all -- --check`, `cargo check --workspace --all-targets --all-features`,
    `cargo test --workspace --doc` and `make api-surface` (expect no change).
  - Commit per D-26:
    `docs(36): resolve rustdoc links in paladin-web (RD-07…RD-09, RD-129…RD-136)`. Stage with
    `git add -- crates/paladin-web/ .planning/phases/36-*/36-evidence/36-04-llm-web.txt`, never
    `git add .`.
  </action>
  <verify>
    <automated>touch crates/paladin-web/src/lib.rs &amp;&amp; cargo doc -p paladin-web --no-deps 2>&amp;1 | tee /tmp/36-04-web-default.txt &amp;&amp; test "$(grep -c 'warning:' /tmp/36-04-web-default.txt)" = 0 &amp;&amp; RUSTDOCFLAGS="-D warnings" cargo doc -p paladin-web --all-features --no-deps &amp;&amp; cargo fmt --all -- --check &amp;&amp; cargo check --workspace --all-targets --all-features &amp;&amp; cargo test --workspace --doc &amp;&amp; ./scripts/check-api-surface.sh .project/current-exports.txt</automated>
  </verify>
  <acceptance_criteria>
    - `RUSTDOCFLAGS="-D warnings" cargo doc -p paladin-web --all-features --no-deps` exits 0.
    - A forced re-documentation of `paladin-web` under default features produces a capture containing a `Documenting paladin-web` line and zero lines matching `warning:`.
    - `grep -rn 'allow(rustdoc::' crates/paladin-web/src` produces no output.
    - `grep -rn 'cfg_attr(docsrs' crates/paladin-web/src` produces no output.
    - `grep -rn 'pub const MAX_HISTORY_LIMIT\|pub const NOT_WIRED_MESSAGE\|pub fn escape_for_script\|pub fn map_parley_error' crates/paladin-web/src` produces no output.
    - `git diff HEAD~1 -- crates/paladin-web/` adds and removes only doc-comment lines, and `git diff --stat HEAD~1 -- crates/ src/` lists only paths under `crates/paladin-web/`.
    - `cargo test --workspace --doc` reports 0 failed.
    - `./scripts/check-api-surface.sh .project/current-exports.txt` reports the surface unchanged.
  </acceptance_criteria>
  <done>All 11 `paladin-web` rows are closed, the crate documents clean under both bar commands, and the work is in one commit citing the RD range.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

This plan creates no new Rust symbols and no new public API — every change is a rewritten
doc-comment line inside an existing `paladin-llm` or `paladin-web` file. SC5 requires
`make api-surface` to report no change.

New file created by this plan:
- `.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-04-llm-web.txt`

New symbols created elsewhere in Phase 36 (listed so drift verification does not flag them):
example targets `token_economy_commissary`, `war_engine_configuration`,
`control_flow_dynamic_routing`, `human_in_the_loop_gate`, `graceful_shutdown`,
`agent_runtime_middleware`, `structured_output_schema`, `sanctum_rag_retrieval`,
`platform_api_client`, `webhook_receiver`, `node_result_cache`, `observability_tracing`,
`observability_otel_export`, `eval_scenarios_demo`; Makefile targets `doc-check` and
`check-examples`; the `.pre-commit-config.yaml` hook id `doc-check`; the CI step name
`Check documentation (all features, -D warnings)`; requirement IDs `CURR-11`…`CURR-15`;
`36-EVIDENCE.md` and `36-CI-EVIDENCE.md`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository → published rustdoc site | the redaction module and the dev-ui escaping helper are security-relevant; their doc comments render publicly |
| doc-comment edit → public API baseline | a link "fixed" by widening visibility silently moves the published surface |
| feature gate → documented surface | a link that resolves under one feature set and not the other re-opens the bar the moment CI runs the other invocation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-10 | Information Disclosure | the paladin-llm redaction module's doc block | medium | mitigate | The module states the redact-before-truncate invariant that keeps a secret's tail from surviving truncation. This plan changes link syntax only; the acceptance criterion restricts the diff to doc-comment lines and the action forbids rewording that statement. |
| T-36-11 | Information Disclosure | the dev-ui controller's script-escaping de-link | medium | mitigate | D-05 requires rewording toward the public route contract; the de-linked mention must not describe the script-escaping algorithm in more detail than the private helper's own doc comment already did. |
| T-36-12 | Tampering | public API surface of paladin-llm and paladin-web | high | mitigate | `make api-surface` runs before each commit and must report no change (D-28). Acceptance criteria grep that the five named private items never gain a public declaration. |
| T-36-13 | Elevation of Privilege | feature-gated documentation | low | mitigate | Twelve of these rows exist only under all features; the per-crate all-features sweep is run for both crates after the fix, so a fix that holds under one feature set and not the other cannot pass (D-07, D-10). |
| T-36-SC | Tampering | cargo package installs | low | accept | No package-manager install runs in this plan; `36-RESEARCH.md`'s Package Legitimacy Audit records "not applicable — this phase installs no new external package". |
</threat_model>

<verification>
- `RUSTDOCFLAGS="-D warnings" cargo doc -p paladin-llm --all-features --no-deps` and the same for `paladin-web` both exit 0.
- Forced default-feature re-documentation of each crate emits zero lines matching `warning:`, down from 4 and 3.
- `cargo check --workspace --all-targets --all-features` exits 0 and `cargo test --workspace --doc` reports 0 failed.
- `cargo fmt --all -- --check` and `make api-surface` are green.
- Two commits, one per crate, each touching only its own crate (D-26).
</verification>

<success_criteria>
- All 17 location groups across `paladin-llm` and `paladin-web` are fixed at their cited file:line, closing all 24 IDs.
- Every fix holds under both the default and the all-features build, with no docsrs conditional-attribute machinery introduced.
- `make api-surface` reports no change.
</success_criteria>

<output>
Create `.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-04-SUMMARY.md` when done, carrying the D-24 closure table: `ID | file:line (cited) | file:line (actual, if different) | kind | fix | commit`, with every follower listed on its lead's row and the twelve all-features-only rows marked as such.
</output>
