---
phase: 36-rustdoc-zero-warning-bar-examples-currency
plan: 08
type: execute
wave: 5
depends_on: ["36-01", "36-06"]
files_modified:
  - examples/agent_runtime_middleware.rs
  - examples/structured_output_schema.rs
  - examples/sanctum_rag_retrieval.rs
  - .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-08-examples.txt
autonomous: true
requirements: [CURR-13, CURR-14, CURR-15]
must_haves:
  truths:
    - "`cargo run --example agent_runtime_middleware` exits 0 offline and its stdout shows a custom middleware's three hooks firing in order, the runtime config's built-in middleware settings resolved, a custom token counter answering beside the defaulted one, history trimming and summarization reducing a long history, a confined vault namespacing two agents' memories apart, and a tool failure failing the run under the fail-run tool error mode."
    - "`cargo run --example structured_output_schema` exits 0 offline and its stdout shows a schema derived from a Rust type and a typed value returned by the structured execution path, with a schema-violating response rejected rather than silently accepted."
    - "`cargo run --example sanctum_rag_retrieval` exits 0 offline and its stdout shows a retrieval result's context, its shed records, the typed error variant produced by a deliberately failing retrieval, the timeout-bounded free function's result, and an exact token counter injected into the retrieval service."
    - "All three programs are picked up by the bulk `cargo build --examples` selector — none declares required-features."
    - "EX-83, EX-84, EX-85, EX-86, EX-87 and EX-89 map to `examples/agent_runtime_middleware.rs`; EX-88 and EX-90 map to `examples/structured_output_schema.rs`; EX-116, EX-117, EX-118, EX-119 and EX-120 map to `examples/sanctum_rag_retrieval.rs`."
    - "`cargo check --workspace --all-targets --all-features` exits 0 and `make api-surface` reports the surface unchanged."
  artifacts:
    - "examples/agent_runtime_middleware.rs — new runnable example binary"
    - "examples/structured_output_schema.rs — new runnable example binary"
    - "examples/sanctum_rag_retrieval.rs — new runnable example binary"
    - ".planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-08-examples.txt"
  key_links:
    - "The execution-middleware trait and the history trimmer are facade re-exports reachable from the prelude; the confined vault is re-exported at src/prelude.rs:58 and the trait it satisfies is the vault port in paladin-ports."
    - "The structured execution entry point lives in src/application/services/paladin/structured.rs over the structured-executor port trait in paladin-ports; schemars is the workspace-pinned schema deriver both the shipped machinery and this example must use."
    - "crates/doc-examples/src/sanctum_vector_memory.rs, added by Phase 35 after the Phase 34 audit SHA, already calls the timeout-bounded retrieval free function, so EX-119's recorded zero-hit grep evidence is stale; the executor re-runs the capability grep and records the drift per D-02 rather than skipping the row."
  prohibitions:
    - statement: "No example prints or hard-codes an API key; a program that reads a key from the environment says so in its header comment (D-29)."
      status: recalled
      verification: flagged-unverified
    - statement: "No example body uses unwrap or expect — errors propagate with the question-mark operator through a boxed-error main (CLAUDE.md, house example pattern)."
      status: recalled
      verification: flagged-unverified
    - statement: "The word Quartermaster never appears, and no bare token total is printed beside a prompt and completion split (D-00i)."
      status: recalled
      verification: flagged-unverified
    - statement: "No library source under src/ or crates/ changes; the existing RAG gallery program is neither deleted nor rewritten (D-18, ROADMAP Phase 36 boundary)."
      status: recalled
      verification: flagged-unverified
---

<objective>
Demonstrate the Phase 26 agent-runtime and structured-output capabilities and the Phase 33 RAG
retrieval surface: EX-83, EX-84, EX-85, EX-86, EX-87, EX-88, EX-89, EX-90 and EX-116, EX-117,
EX-118, EX-119, EX-120 — thirteen of the fifty-nine gap rows.

This plan implements D-00i (Medieval-military vocabulary and no bare token total beside a split),
D-15 (each capability maps to exactly one named program; the agent-runtime cluster is split across
two programs so structured output reads as its own story — the planner's stated discretion under
D-15 — and the RAG cluster gets a sibling rather than a rewrite of the existing RAG program), D-16
(offline-first via the mock LLM adapter and in-memory ports), D-18 (no example is deleted), D-24
(closure table), D-26 (one commit per program) and D-28 (`make api-surface` before every commit).

Purpose: the agent runtime is the largest undemonstrated surface in the audit — a middleware trait,
twelve built-in middleware sub-structs, a token-counter port, a context-window manager, a
namespaced vault and a tool-error policy, none with a gallery program. The RAG rows are the Phase 33
return-type change a caller has to handle and today cannot see used anywhere.

Scheduling note: the dependency on 36-06 is a worktree-serialization edge, not a code dependency —
each isolated executor worktree builds a cold `target/` at roughly 16 GB and the volume fits at most
two at once, so no wave may hold more than two build-heavy plans.

Spec-less probe fallback skipped (visible, recorded): Phase 36 has no SPEC and its requirement IDs
were minted by this planning run, so `must_haves` are derived from the five ROADMAP Phase 36
Success Criteria and the CONTEXT decisions.

Output: three runnable, offline, default-feature example binaries. Their `examples/README.md`
sections are written by plan 36-11, which owns that file.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-CONTEXT.md
@.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-RESEARCH.md
@.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-PATTERNS.md
@.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: examples/agent_runtime_middleware.rs — EX-83, EX-84, EX-85, EX-86, EX-87, EX-89</name>
  <files>examples/agent_runtime_middleware.rs, .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-08-examples.txt</files>
  <read_first>
    - src/prelude.rs lines 40-60 (the execution-middleware trait, the middleware-flow type, the model-call context, the response view, the final-result type and the confined vault — the re-exports this program imports)
    - src/application/services/paladin/middleware/mod.rs (the middleware trait's hook signatures and the history-trimmer and summarization re-exports)
    - src/config/agent_runtime.rs lines 1-120 and 1150-1300 (the runtime config's built-in middleware sub-structs, the chain-resolution precedence, and the tool-error mode including its fail-run variant with redact-then-bound tool-text sanitization)
    - crates/paladin-ports/src/output/token_counter_port.rs (the token-counter trait, its required method and its defaulted exactness method)
    - crates/paladin-ports/src/output/vault_port.rs lines 100-180 (the vault trait the confined vault satisfies)
    - src/application/services/paladin/vault_confined.rs (the confined vault's constructor and the namespacing it enforces)
    - examples/basic_paladin.rs and examples/paladin_with_config.rs (the house header-comment, import ordering, async main shape and builder usage)
    - .planning/phases/34-documentation-currency-audit/34-AUDIT.md §4 capability rows EX-83, EX-84, EX-85, EX-86, EX-87 and EX-89
  </read_first>
  <action>
Create `examples/agent_runtime_middleware.rs` as a single default-feature example binary — no
required-features.

Structure it as one runnable story that prints each step:

  1. **A custom execution middleware (EX-83).** Implement the trait on a small local type whose
     before-model, after-model and around-tool hooks each print that they fired and what they saw,
     attach it to a Paladin built with the mock adapter, execute once, and print the hook order.
  2. **The runtime config's built-in middleware (EX-84).** Construct a runtime config, print the
     resolved settings of its built-in middleware sub-structs, and state in the printed output that
     these are configuration, not hand-wiring.
  3. **A custom token counter (EX-85).** Implement the token-counter trait on a local type, inject
     it, and print both the count it returns and its exactness answer beside the defaulted heuristic
     counter's, so the difference is visible.
  4. **Context-window management (EX-86).** Build a long history, run it through the history trimmer
     and the summarization middleware, and print the message count and the prompt and completion
     token split before and after. Never print a bare total beside a split (D-00i).
  5. **Structural memory namespacing (EX-87).** Construct two confined vaults over the same
     underlying store with different namespaces, write a value through each, and print that each
     agent reads back only its own — the namespacing is the capability, so the cross-read must be
     attempted and its empty or denied result printed.
  6. **Fail-run tool error mode (EX-89).** Configure the runtime's tool error mode to fail-run,
     trigger a tool failure through a mock tool, and print the run-level failure the policy produced
     together with the sanitized tool text, showing that the text was redacted before it was
     bounded.

Header comment follows the house shape: what it demonstrates naming each capability, the exact
`cargo run --example agent_runtime_middleware` command, and an explicit statement that it needs no
provider key and no external service (D-16, D-29). Use question-mark propagation through a
boxed-error main; no unwrap or expect in the body.

Run it once and record the exit code and the first lines of stdout in
`36-evidence/36-08-examples.txt`.

Commit per D-26:
`docs(36): add agent_runtime_middleware example (EX-83, EX-84, EX-85, EX-86, EX-87, EX-89)`. Run
`cargo fmt --all -- --check`, `cargo check --workspace --all-targets --all-features` and
`make api-surface` (expect no change) first. Stage with explicit paths, never `git add .`.
  </action>
  <verify>
    <automated>cargo build --example agent_runtime_middleware &amp;&amp; env -u OPENAI_API_KEY -u ANTHROPIC_API_KEY -u DEEPSEEK_API_KEY cargo run --example agent_runtime_middleware &amp;&amp; cargo fmt --all -- --check &amp;&amp; cargo check --workspace --all-targets --all-features &amp;&amp; ./scripts/check-api-surface.sh .project/current-exports.txt</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --example agent_runtime_middleware` exits 0 with no features flag.
    - `cargo run --example agent_runtime_middleware` exits 0 with all three provider key variables unset.
    - The program's stdout mentions the three middleware hooks in order, the runtime config's resolved middleware settings, both exactness answers, the before and after history sizes, both vault namespaces with the cross-read result, and the fail-run tool failure with its sanitized text.
    - `grep -c 'ExecutionMiddleware' examples/agent_runtime_middleware.rs` is at least 1 and `grep -c 'ConfinedVault' examples/agent_runtime_middleware.rs` is at least 1.
    - `grep -cE '\.unwrap\(\)|\.expect\(' examples/agent_runtime_middleware.rs` is 0.
    - `grep -cE 'OPENAI_API_KEY|ANTHROPIC_API_KEY|DEEPSEEK_API_KEY' examples/agent_runtime_middleware.rs` is 0.
    - `grep -c 'Quartermaster' examples/agent_runtime_middleware.rs` is 0.
    - `git diff --stat HEAD~1 -- src/ crates/` is empty.
    - `./scripts/check-api-surface.sh .project/current-exports.txt` reports the surface unchanged.
  </acceptance_criteria>
  <done>Six agent-runtime capabilities have one runnable offline program that prints each of them, committed with their EX IDs in the subject.</done>
  <reversibility rating="costly">The program name becomes a README anchor and a CHANGELOG bullet (D-15).</reversibility>
</task>

<task type="auto">
  <name>Task 2: examples/structured_output_schema.rs — EX-88, EX-90</name>
  <files>examples/structured_output_schema.rs, .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-08-examples.txt</files>
  <read_first>
    - src/application/services/paladin/structured.rs lines 90-160 (the structured execution entry points — their generic bounds are what the example's local type must satisfy)
    - crates/paladin-ports/src/output/structured_executor_port.rs lines 70-200 (the structured-executor trait and the repair behaviour documented on its run entry point, whose doc comment plan 36-01 already corrected)
    - crates/paladin-battalion/Cargo.toml (confirms the workspace schemars pin and how the shipped code derives schemas — the example must use the same crate and the same derive, not a hand-built schema)
    - src/lib.rs lines 218-230 (the structured-output re-exports available through the facade)
    - examples/agent_runtime_middleware.rs (as written in Task 1 — reuse its mock-adapter setup and printing conventions)
    - .planning/phases/34-documentation-currency-audit/34-AUDIT.md §4 capability rows EX-88 and EX-90
  </read_first>
  <action>
Create `examples/structured_output_schema.rs` as a single default-feature example binary — no
required-features.

Structure it as one runnable story that prints each step:

  1. **Derive a JSON schema (EX-90).** Define a small local result type with the derives the shipped
     structured-output machinery requires, derive its schema through schemars, and print the schema.
     Do not hand-build a schema — the point of the row is that the framework's own deriver is what a
     caller uses.
  2. **Schema-validated structured output (EX-88).** Execute through the structured-output path with
     the mock adapter returning a response that satisfies the schema, and print the typed value that
     came back — its fields, not a raw string. Then execute again with a mock response that violates
     the schema and print the typed failure or repair the machinery produces. The rejection is the
     capability; it must be printed, not swallowed.

Header comment follows the house shape: what it demonstrates naming both capabilities, the exact
`cargo run --example structured_output_schema` command, and an explicit statement that it needs no
provider key and no external service (D-16, D-29). Use question-mark propagation through a
boxed-error main; no unwrap or expect in the body.

Run it once and append the exit code and the first lines of stdout to
`36-evidence/36-08-examples.txt`.

Commit per D-26: `docs(36): add structured_output_schema example (EX-88, EX-90)`. Run
`cargo fmt --all -- --check`, `cargo check --workspace --all-targets --all-features` and
`make api-surface` (expect no change) first.
  </action>
  <verify>
    <automated>cargo build --example structured_output_schema &amp;&amp; env -u OPENAI_API_KEY -u ANTHROPIC_API_KEY -u DEEPSEEK_API_KEY cargo run --example structured_output_schema &amp;&amp; cargo fmt --all -- --check &amp;&amp; cargo check --workspace --all-targets --all-features &amp;&amp; ./scripts/check-api-surface.sh .project/current-exports.txt</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --example structured_output_schema` exits 0 with no features flag.
    - `cargo run --example structured_output_schema` exits 0 with all three provider key variables unset.
    - The program's stdout contains a derived JSON schema and the field values of a typed result, plus the outcome of the schema-violating attempt.
    - `grep -c 'schemars' examples/structured_output_schema.rs` is at least 1.
    - `grep -cE '\.unwrap\(\)|\.expect\(' examples/structured_output_schema.rs` is 0.
    - `grep -cE 'OPENAI_API_KEY|ANTHROPIC_API_KEY|DEEPSEEK_API_KEY' examples/structured_output_schema.rs` is 0.
    - `git diff --stat HEAD~1 -- src/ crates/ Cargo.toml` is empty — no dependency was added, since schemars is already a workspace pin.
    - `./scripts/check-api-surface.sh .project/current-exports.txt` reports the surface unchanged.
  </acceptance_criteria>
  <done>Structured output and schema derivation have one runnable offline program that shows both the accepted and the rejected path, committed with their EX IDs in the subject.</done>
</task>

<task type="auto">
  <name>Task 3: examples/sanctum_rag_retrieval.rs — EX-116, EX-117, EX-118, EX-119, EX-120</name>
  <files>examples/sanctum_rag_retrieval.rs, .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-08-examples.txt</files>
  <read_first>
    - crates/paladin-memory/src/services/rag_retrieval_service.rs lines 1-140, 200-260 and 480-540 (the retrieval result type, the shed-item re-export, the retrieval error enum, the token-counter injection builder method, and the timeout-bounded free function)
    - crates/doc-examples/src/sanctum_vector_memory.rs (Phase 35's module — it already constructs a retrieval service with an in-memory Sanctum and calls the timeout-bounded free function; it is the closest working seed and the reason EX-119's zero-hit grep evidence is now stale)
    - examples/paladin_with_rag.rs (the existing gallery program — read it to confirm what it does and does not do, so this sibling does not duplicate it; it is neither rewritten nor deleted per D-18)
    - examples/paladin_with_sanctum.rs (the existing Sanctum memory program — the in-memory store setup this program reuses)
    - crates/paladin-ports/src/output/token_counter_port.rs (the token-counter trait for the exact-counter injection)
    - .planning/phases/34-documentation-currency-audit/34-AUDIT.md §4 capability rows EX-116, EX-117, EX-118, EX-119 and EX-120
  </read_first>
  <action>
Create `examples/sanctum_rag_retrieval.rs` as a sibling to the existing RAG gallery program — the
D-15 discretion this plan exercises. The existing program is a printed conceptual walkthrough that
calls none of the Phase 33 API; rewriting it would delete a gallery entry the audit marked current,
which D-18 forbids. The new sibling is the runnable one.

Single default-feature example binary — no required-features. Structure it as one runnable story
that prints each step:

  1. **Re-check the drift first (D-02).** Before writing the program, run the audit's own capability
     greps for the retrieval result type, the shed item, the retrieval error enum, the
     timeout-bounded free function and the token-counter injection method across `examples/` and
     `crates/doc-examples/src/`. Phase 35 added a Sanctum vector-memory module after the Phase 34
     audit SHA, so at least the timeout-bounded free function now has a hit that the audit recorded
     as zero. Record each token's current hit count verbatim in `36-evidence/36-08-examples.txt` and
     note it in the SUMMARY's closure table as post-audit drift. Do **not** skip any row on that
     basis — the gap is about the `examples/` gallery, and D-02 requires drift to be recorded with
     the command output that proves it, never silently.
  2. **Retrieve and read the result type (EX-116).** Build a retrieval service over an in-memory
     Sanctum seeded with a few documents, retrieve context for a query, and print the returned
     result's context — as the typed result, not a bare string.
  3. **Read the shed record (EX-117).** Set a budget small enough to force truncation and print each
     shed item the result carries: what was dropped and why.
  4. **Handle the typed error (EX-118).** Trigger a retrieval that fails — an unreachable Sanctum,
     an impossible budget, or whichever variant the enum makes reachable — and match on the error
     enum, printing the variant name and its message.
  5. **Use the timeout-bounded free function (EX-119).** Call it and print its result, naming in the
     output that this is the free-function form with a deadline.
  6. **Inject an exact token counter (EX-120).** Rebuild the service through the token-counter
     injection builder method with a counter whose exactness answer is true, retrieve again, and
     print how the shed decision differed from the heuristic run.

Header comment follows the house shape: what it demonstrates naming each capability, the exact
`cargo run --example sanctum_rag_retrieval` command, an explicit statement that it needs no provider
key and no external service, and a pointer to the existing RAG program as the conceptual overview
(D-16, D-29). Use question-mark propagation through a boxed-error main; no unwrap or expect in the
body. Use Commissary for the budgeting officer and never Quartermaster; never print a bare token
total beside a prompt and completion split (D-00i).

Commit per D-26:
`docs(36): add sanctum_rag_retrieval example (EX-116, EX-117, EX-118, EX-119, EX-120)`. Run
`cargo fmt --all -- --check`, `cargo check --workspace --all-targets --all-features`,
`cargo build --examples` and `make api-surface` (expect no change) first.
  </action>
  <verify>
    <automated>cargo build --example sanctum_rag_retrieval &amp;&amp; env -u OPENAI_API_KEY -u ANTHROPIC_API_KEY -u DEEPSEEK_API_KEY cargo run --example sanctum_rag_retrieval &amp;&amp; cargo build --examples &amp;&amp; cargo fmt --all -- --check &amp;&amp; cargo check --workspace --all-targets --all-features &amp;&amp; ./scripts/check-api-surface.sh .project/current-exports.txt</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --example sanctum_rag_retrieval` exits 0 with no features flag, and `cargo build --examples` exits 0 with all three of this plan's binaries present under the debug examples directory.
    - `cargo run --example sanctum_rag_retrieval` exits 0 with all three provider key variables unset.
    - The program's stdout mentions the retrieved context, at least one shed record with a reason, a named retrieval-error variant, the timeout-bounded call's result, and the exact-counter contrast.
    - `grep -c 'RagRetrievalResult' examples/sanctum_rag_retrieval.rs`, `grep -c 'ShedItem' examples/sanctum_rag_retrieval.rs`, `grep -c 'RagRetrievalError' examples/sanctum_rag_retrieval.rs`, `grep -c 'retrieve_context_with_timeout' examples/sanctum_rag_retrieval.rs` and `grep -c 'with_token_counter' examples/sanctum_rag_retrieval.rs` are each at least 1.
    - `git status --porcelain examples/paladin_with_rag.rs` is empty — the existing program is untouched, per D-18.
    - `grep -cE '\.unwrap\(\)|\.expect\(' examples/sanctum_rag_retrieval.rs` is 0.
    - `grep -cE 'OPENAI_API_KEY|ANTHROPIC_API_KEY|DEEPSEEK_API_KEY' examples/sanctum_rag_retrieval.rs` is 0.
    - `36-evidence/36-08-examples.txt` records the five post-audit capability-grep hit counts verbatim.
    - `./scripts/check-api-surface.sh .project/current-exports.txt` reports the surface unchanged.
  </acceptance_criteria>
  <done>Five RAG retrieval capabilities have one runnable offline sibling program, the post-audit grep drift is recorded rather than skipped, and the existing RAG program is untouched.</done>
  <reversibility rating="costly">The program name becomes a README anchor and a CHANGELOG bullet; the extend-versus-sibling choice is recorded here as the D-15 discretion exercised.</reversibility>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

New files and binary targets created by **this plan**:
- `examples/agent_runtime_middleware.rs` — new example binary target `agent_runtime_middleware`
- `examples/structured_output_schema.rs` — new example binary target `structured_output_schema`
- `examples/sanctum_rag_retrieval.rs` — new example binary target `sanctum_rag_retrieval`
- `.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-08-examples.txt`

None of the three declares required-features, so no manifest example declaration is added here.
Each program defines local types — a custom middleware, a custom token counter, a schema-derived
result type — that exist only inside its own example binary and are not part of any crate's public
API.

New symbols created elsewhere in Phase 36 (listed so drift verification does not flag them):
example targets `token_economy_commissary`, `war_engine_configuration`,
`control_flow_dynamic_routing`, `human_in_the_loop_gate`, `graceful_shutdown`,
`platform_api_client`, `webhook_receiver`, `node_result_cache`, `observability_tracing`,
`observability_otel_export`, `eval_scenarios_demo`; Makefile targets `doc-check` and
`check-examples`; the `.pre-commit-config.yaml` hook id `doc-check`; the CI step name
`Check documentation (all features, -D warnings)`; requirement IDs `CURR-11`…`CURR-15`;
`36-EVIDENCE.md` and `36-CI-EVIDENCE.md`.

No new public Rust API is created — SC5 requires `make api-surface` to report no change.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer shell → example process | the example executes in the developer's environment and may read process environment variables |
| tool output → model context | the fail-run tool error mode demonstration feeds a failing tool's text back through the sanitizer |
| agent A memory ↔ agent B memory | the confined vault demonstration deliberately attempts a cross-namespace read |
| model response → typed value | structured output parses an untrusted model response against a schema |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-24 | Information Disclosure | the confined-vault namespacing demonstration | high | mitigate | EX-87's capability is that one agent cannot read another's memories. The program must attempt the cross-namespace read and print its empty or denied result — an example that only wrote and read within one namespace would not demonstrate the boundary at all. |
| T-36-25 | Information Disclosure | the fail-run tool error mode demonstration | medium | mitigate | The shipped policy redacts tool text before bounding it, which is the same redact-then-truncate invariant the security instructions name. The program prints the sanitized text, never a raw error body, and the sanitization order is stated in the printed output. |
| T-36-26 | Tampering | structured output schema validation | high | mitigate | EX-88's capability includes rejecting a response that violates the schema. The program runs the violating case and prints the typed failure, so a reader learns the machinery validates rather than trusts. |
| T-36-27 | Information Disclosure | all three example programs | medium | mitigate | None reads or prints a provider key; all use the mock LLM adapter and in-memory ports and say so in their header comments (D-16, D-29). Acceptance criteria grep each file for the three provider key variable names. |
| T-36-28 | Repudiation | EX-119 post-audit grep drift | medium | mitigate | Phase 35 added a doc-examples module after the audit SHA, making one recorded zero-hit grep stale. D-02 requires the drift to be recorded with the command output that proves it; Task 3 captures all five hit counts into the evidence file and the SUMMARY's closure table. |
| T-36-SC | Tampering | cargo package installs | low | accept | No package-manager install runs in this plan; schemars and every other crate these programs use is already a pinned workspace dependency, per `36-RESEARCH.md`'s Package Legitimacy Audit. |
</threat_model>

<verification>
- `cargo build --examples` exits 0 and all three new binaries appear under the debug examples directory.
- All three programs run to exit 0 with no provider key set and no external service available.
- `cargo fmt --all -- --check`, `cargo check --workspace --all-targets --all-features` and `make api-surface` are green.
- `git diff --stat` for the plan's commits touches no path under `src/`, `crates/` or the root manifest.
- `git status --porcelain examples/paladin_with_rag.rs` is empty.
- `36-evidence/36-08-examples.txt` records all three programs' exit codes and the five RAG capability-grep hit counts.
</verification>

<success_criteria>
- EX-83 through EX-90 and EX-116 through EX-120 each map to exactly one named program — thirteen more of the fifty-nine gap rows closed.
- All three programs are default-feature targets covered by the bulk `cargo build --examples` invocation.
- The post-audit drift in EX-119's grep evidence is recorded, not skipped.
- `make api-surface` reports no change.
</success_criteria>

<output>
Create `.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-08-SUMMARY.md` when done, carrying the D-24 closure table with one row per EX ID: `ID | capability | program | commit`, plus a short post-audit drift note for the five RAG capability greps.
</output>
