---
phase: 36-rustdoc-zero-warning-bar-examples-currency
plan: 10
type: execute
wave: 6
depends_on: ["36-08", "36-09"]
files_modified:
  - examples/node_result_cache.rs
  - examples/observability_tracing.rs
  - examples/observability_otel_export.rs
  - examples/eval_scenarios_demo.rs
  - Cargo.toml
  - .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-10-examples.txt
autonomous: true
requirements: [CURR-13, CURR-14, CURR-15]
must_haves:
  truths:
    - "`cargo build --example node_result_cache --features \"redis-cache\"` exits 0; the program is build-only in this environment and in CI because it needs a Redis server, and it says so in its header comment and will say so in its README section."
    - "`cargo build --example observability_otel_export --features \"otel\"` exits 0; the program is build-only because it needs a collector endpoint, and it says so in its header comment."
    - "`cargo run --example observability_tracing` exits 0 offline and its stdout shows a trace record envelope with named trace event variants, a trace configuration resolved from its fields and from the export-enable variable, and rows read back from the persisted trace-history table."
    - "`cargo run --example eval_scenarios_demo` exits 0 offline and its stdout shows scenarios declared through the eval macro, a run in the default non-live mode, the live-mode variable's effect named, and the command-line form of running a scenario glob."
    - "Both gated programs have an example declaration in the root manifest naming their required features; the two ungated programs are picked up by the bulk `cargo build --examples` selector."
    - "EX-81 and EX-82 map to `examples/node_result_cache.rs`; EX-100, EX-101, EX-102 and EX-108 map to `examples/observability_tracing.rs`; EX-103 maps to `examples/observability_otel_export.rs`; EX-105, EX-106 and EX-107 map to `examples/eval_scenarios_demo.rs`."
    - "`cargo check --workspace --all-targets --all-features` exits 0 and `make api-surface` reports the surface unchanged."
  artifacts:
    - "examples/node_result_cache.rs — new gated example binary, build-only"
    - "examples/observability_tracing.rs — new runnable example binary"
    - "examples/observability_otel_export.rs — new gated example binary, build-only"
    - "examples/eval_scenarios_demo.rs — new runnable example binary"
    - "Cargo.toml — two new example declarations with required-features"
    - ".planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-10-examples.txt"
  key_links:
    - "The eval crate is an unconditional dev-dependency of the root package, so the eval demo needs no required-features — only a gated example gets a manifest declaration (D-17)."
    - "The redis-cache and otel features pull real external dependencies, so their programs are build-verified and never run in CI (D-16); the CI examples job builds every example and runs none."
    - "The root manifest is also edited by plan 36-09, which runs in the earlier wave — this plan appends its two example declarations to the four existing plus 36-09's two."
  prohibitions:
    - statement: "No example prints or hard-codes an API key; a program that reads a key from the environment says so in its header comment (D-29)."
      status: recalled
      verification: flagged-unverified
    - statement: "No example body uses unwrap or expect — errors propagate with the question-mark operator through a boxed-error main (CLAUDE.md, house example pattern)."
      status: recalled
      verification: flagged-unverified
    - statement: "No example that requires an external service is executed by any automated check — services-requiring programs are build-verified only (D-16)."
      status: recalled
      verification: flagged-unverified
    - statement: "No library source under src/ or crates/ changes; the only manifest edit is two example target declarations (ROADMAP Phase 36 boundary)."
      status: recalled
      verification: flagged-unverified
---

<objective>
Demonstrate the Phase 25 node-result cache and the Phase 28 observability and eval capabilities:
EX-81, EX-82, EX-100, EX-101, EX-102, EX-103, EX-105, EX-106, EX-107 and EX-108 — ten of the
fifty-nine gap rows.

This plan implements D-15 (each capability maps to exactly one named program, and the observability
cluster is split so the export feature gets its own gated target — the planner's stated discretion
under D-15 and the resolution of `36-RESEARCH.md` Assumption A3), D-16 (offline-first where the
capability allows; programs that genuinely need an external service build under the CI split, state
the prerequisite in their header, and are not run), D-17 (feature gating: a program is declared as
an example target with required-features only when the capability *is* the feature, and every gated
target needs its own CI and local-script invocation — those lines are added by plan 36-12, which
owns those files), D-24 (closure table), D-26 (one commit per program) and D-28 (`make api-surface`
before every commit). EX-107's runnable form is resolved here as the planner's D-15 discretion: a
thin program that drives the eval runner in-process and prints the command-line equivalent, rather
than a README-only entry, so every gap row still maps to exactly one named program.

Purpose: the observability surface — trace records, trace configuration, the export toggle, the
persisted trace-history table — and the eval scenario runner are the two Phase 28 capabilities a
reader cannot discover from the gallery at all today.

Spec-less probe fallback skipped (visible, recorded): Phase 36 has no SPEC and its requirement IDs
were minted by this planning run, so `must_haves` are derived from the five ROADMAP Phase 36
Success Criteria and the CONTEXT decisions.

Output: four new example binaries, two of them gated and build-only. Their `examples/README.md`
sections are written by plan 36-11; their CI and script invocations by plan 36-12.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-CONTEXT.md
@.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-RESEARCH.md
@.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-PATTERNS.md
@.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-09-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: examples/node_result_cache.rs — EX-81, EX-82, gated on redis-cache and build-only</name>
  <files>examples/node_result_cache.rs, Cargo.toml, .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-10-examples.txt</files>
  <read_first>
    - src/config/node_cache.rs (the node-cache config including the enable variable read at line 147, and what the cache keys off)
    - crates/paladin-battalion/src/engine/cache_key.rs (the cache key composition — its doc comments were corrected by plan 36-02)
    - crates/paladin-storage/src (locate the redis-cache-gated cache adapter and read its constructor's connection requirements)
    - Cargo.toml lines 444-475 (the example declarations, now including plan 36-09's two) and the features-block line for redis-cache
    - crates/doc-examples/src/superstep_engine.rs (the graph-construction and run seed — the node whose result gets cached)
    - examples/war_engine_memory_baseline.rs (the nearest existing cache and performance flavoured gallery program)
    - .planning/phases/34-documentation-currency-audit/34-AUDIT.md §4 capability rows EX-81 and EX-82
  </read_first>
  <action>
Create `examples/node_result_cache.rs` and add an example declaration to the root manifest with the
name `node_result_cache`, the path `examples/node_result_cache.rs` and required-features
`["redis-cache"]` (D-17 — the capability *is* the feature).

The program wires node-result caching end to end and prints each step: constructing the
Redis-backed cache adapter the feature enables (EX-81), running a small graph twice so the second
run reads the cached node result, printing the cache key and the hit, then toggling the node-cache
enable variable off, re-running, and printing that the cache was bypassed (EX-82). Restore the
variable afterwards.

This program needs a running Redis server, so per D-16 it is **build-verified only**: it is never
executed by any automated check here or in CI. State that prerequisite explicitly in the header
comment — what service it needs, how to start it (`make services-up` brings up the dev stack), and
that it is build-only in CI. Plan 36-11 repeats the prerequisite in the README section. The header
comment otherwise follows the house shape: what it demonstrates naming both capabilities, the exact
`cargo run --example node_result_cache --features "redis-cache"` command, and an explicit statement
that it needs no provider key. Use question-mark propagation through a boxed-error main; no unwrap
or expect in the body.

Record the build result in `36-evidence/36-10-examples.txt`, explicitly noting that the program was
built and not run, with the reason.

Commit per D-26: `docs(36): add node_result_cache example (EX-81, EX-82)`. Run
`cargo fmt --all -- --check`, `cargo check --workspace --all-targets --all-features` and
`make api-surface` (expect no change) first. Stage the program and the manifest explicitly, never
`git add .`.
  </action>
  <verify>
    <automated>cargo build --example node_result_cache --features "redis-cache" &amp;&amp; cargo build --examples &amp;&amp; cargo fmt --all -- --check &amp;&amp; cargo check --workspace --all-targets --all-features &amp;&amp; ./scripts/check-api-surface.sh .project/current-exports.txt</automated>
  </verify>
  <acceptance_criteria>
    - The root manifest contains an example block naming `node_result_cache` with required-features `["redis-cache"]`.
    - `cargo build --example node_result_cache --features "redis-cache"` exits 0.
    - `cargo build --examples` with no features exits 0 and does not attempt this target.
    - `grep -c 'APP_NODE_CACHE_ENABLED' examples/node_result_cache.rs` is at least 1.
    - The header comment states that a Redis server is required and that the program is build-only in CI.
    - `grep -cE '\.unwrap\(\)|\.expect\(' examples/node_result_cache.rs` is 0.
    - `grep -cE 'OPENAI_API_KEY|ANTHROPIC_API_KEY|DEEPSEEK_API_KEY' examples/node_result_cache.rs` is 0.
    - `36-evidence/36-10-examples.txt` records this program as built-not-run with the stated reason.
    - `git diff --stat HEAD~1 -- src/ crates/` is empty.
    - `./scripts/check-api-surface.sh .project/current-exports.txt` reports the surface unchanged.
  </acceptance_criteria>
  <done>Node-result caching and its enable toggle have one named gated program that builds under its feature, with its external-service prerequisite stated rather than assumed.</done>
  <reversibility rating="costly">The program name becomes a README anchor, a CI invocation, a local examples-script line and a CHANGELOG bullet (D-15, D-17).</reversibility>
</task>

<task type="auto">
  <name>Task 2: examples/observability_tracing.rs (EX-100, EX-101, EX-102, EX-108) and examples/observability_otel_export.rs (EX-103, gated, build-only)</name>
  <files>examples/observability_tracing.rs, examples/observability_otel_export.rs, Cargo.toml, .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-10-examples.txt</files>
  <read_first>
    - crates/paladin-core/src/platform/container/trace.rs lines 1-60 and 300-420 (the module doc block plan 36-03 corrected, the trace record envelope, and the trace event enum's twelve variants — the example must name real variants)
    - src/config/trace.rs lines 1-120 and 260-300 (the trace config's log-sink, persist, state-values and export fields, and the export-enable environment read at line 276)
    - src/infrastructure/telemetry/otel_sink.rs (the gated export sink — its module doc was corrected by plan 36-05; read what it needs at construction and what endpoint configuration it expects)
    - src/application/services (locate the persisted trace-history reader for the append-only trace table and read its query entry point — EX-108)
    - Cargo.toml (the example declarations and the otel feature definition)
    - crates/doc-examples/src/superstep_engine.rs (the graph run that emits the trace events)
    - .planning/phases/34-documentation-currency-audit/34-AUDIT.md §4 capability rows EX-100, EX-101, EX-102, EX-103 and EX-108
  </read_first>
  <action>
Create two programs. Splitting the cluster keeps the runnable part runnable: folding the gated
export into the main observability program would make the whole thing build-only and lose the
capability demonstration for a reader with no collector. This is the resolution of
`36-RESEARCH.md` Assumption A3, exercised as D-15 discretion.

**`examples/observability_tracing.rs`** — default features, no required-features, fully runnable
offline. It prints each step:
  1. **Consume the trace envelope (EX-100).** Run a small graph with tracing on, then print each
     emitted trace record's envelope fields and the name of each trace event variant it carried —
     use the real variant names from the enum, not paraphrases.
  2. **Configure tracing (EX-101).** Construct a trace config naming its log-sink, persist,
     state-values and export fields, print the resolved values, and re-run so the effect of a
     changed field is visible.
  3. **Toggle export from the environment (EX-102).** Set `PALADIN_TRACE_OTEL_ENABLED` in-process
     before the config loads, reload, and print the before and after value, stating that the export
     itself additionally requires the otel Cargo feature and pointing at the sibling program.
     Restore the variable afterwards.
  4. **Query the persisted trace history (EX-108).** Persist the run's traces to a local store, read
     the append-only trace-history rows back, and print run id, sequence and event name per row so
     the table's shape is visible.

**`examples/observability_otel_export.rs`** — add an example declaration with the name
`observability_otel_export`, the path `examples/observability_otel_export.rs` and required-features
`["otel"]` (D-17). It wires the export sink the feature enables, shows the endpoint configuration it
reads, and prints what it would export (EX-103). It needs a reachable collector, so per D-16 it is
**build-verified only** and never executed by any automated check; its header comment states that
prerequisite explicitly, names the three telemetry crates the feature pulls in, and says it is
build-only in CI.

Both header comments follow the house shape: what each demonstrates naming its capabilities, the
exact run command including features, and an explicit statement about provider keys and services
(D-29). Use question-mark propagation through a boxed-error main; no unwrap or expect in either
body.

Run the tracing program once and record its exit code and first stdout lines in
`36-evidence/36-10-examples.txt`; record the export program as built-not-run with the reason.

Commit per D-26, one per program:
`docs(36): add observability_tracing example (EX-100, EX-101, EX-102, EX-108)` and
`docs(36): add observability_otel_export example (EX-103)`. Run `cargo fmt --all -- --check`,
`cargo check --workspace --all-targets --all-features` and `make api-surface` (expect no change)
before each.
  </action>
  <verify>
    <automated>cargo build --example observability_tracing &amp;&amp; env -u OPENAI_API_KEY -u ANTHROPIC_API_KEY -u DEEPSEEK_API_KEY cargo run --example observability_tracing &amp;&amp; cargo build --example observability_otel_export --features "otel" &amp;&amp; cargo build --examples &amp;&amp; cargo fmt --all -- --check &amp;&amp; cargo check --workspace --all-targets --all-features &amp;&amp; ./scripts/check-api-surface.sh .project/current-exports.txt</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --example observability_tracing` exits 0 with no features flag and `cargo run --example observability_tracing` exits 0 with all three provider key variables unset.
    - The root manifest contains an example block naming `observability_otel_export` with required-features `["otel"]`, and `cargo build --example observability_otel_export --features "otel"` exits 0.
    - `cargo build --examples` with no features exits 0 and does not attempt the gated export target.
    - The tracing program's stdout names at least three real trace event variants, prints the trace config field values, prints the export-enable before and after values, and prints trace-history rows with a run id and a sequence.
    - `grep -c 'PALADIN_TRACE_OTEL_ENABLED' examples/observability_tracing.rs` is at least 1 and `grep -c 'TraceRecord' examples/observability_tracing.rs` is at least 1.
    - The export program's header states that a collector endpoint is required and that the program is build-only in CI.
    - `grep -cE '\.unwrap\(\)|\.expect\(' examples/observability_tracing.rs examples/observability_otel_export.rs` is 0.
    - `git diff --stat HEAD~1 -- src/ crates/` is empty for each commit.
    - `./scripts/check-api-surface.sh .project/current-exports.txt` reports the surface unchanged.
  </acceptance_criteria>
  <done>Five observability capabilities have named programs — four in one runnable offline program, the export in its own gated build-only target — committed with their EX IDs in the subjects.</done>
  <reversibility rating="costly">Both program names become README anchors and CHANGELOG bullets; the gated target also becomes a CI invocation and a local examples-script line (D-15, D-17).</reversibility>
</task>

<task type="auto">
  <name>Task 3: examples/eval_scenarios_demo.rs — EX-105, EX-106, EX-107</name>
  <files>examples/eval_scenarios_demo.rs, .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-10-examples.txt</files>
  <read_first>
    - crates/paladin-eval/src/runner.rs lines 640-760 (the scenario-declaration macro and its calling convention — the only in-tree reference for how a caller declares scenarios)
    - crates/paladin-eval/src/lib.rs lines 50-90 (the crate's public exports including the live-mode environment constant, the run options, the scenario runner and its error type)
    - src/application/cli/commands/eval.rs (the CLI's eval-run subcommand — read what it does so the printed command-line equivalent is accurate; its doc block was corrected by plan 36-05)
    - Cargo.toml lines 310-320 (confirms the eval crate is an unconditional dev-dependency of the root package, so this example needs no required-features and no manifest declaration)
    - examples/basic_paladin.rs (the house header-comment and main shape)
    - .planning/phases/34-documentation-currency-audit/34-AUDIT.md §4 capability rows EX-105, EX-106 and EX-107
  </read_first>
  <action>
Create `examples/eval_scenarios_demo.rs` as a single default-feature example binary. The eval crate
is an unconditional dev-dependency of the root package, so no manifest declaration and no
required-features are needed — only a genuinely gated target gets one (D-17).

Structure it as one runnable story that prints each step:

  1. **Declare scenarios with the macro (EX-105).** Use the scenario-declaration macro to declare two
     or three small scenarios against a Paladin built with the mock adapter, run them through the
     scenario runner, and print each scenario's name and outcome.
  2. **Name the live-mode toggle (EX-106).** Print the current live-mode environment variable's value
     and one sentence stating what enabling it changes — scenarios call a real provider instead of
     the mock — and state that this program deliberately runs in the default non-live mode so it
     needs no key. Do not enable live mode.
  3. **Show the command-line form (EX-107).** Write a small scenario file into a temporary directory,
     then print the exact CLI eval-run command a user would run against it, and drive the same glob
     through the runner in-process so the printed command and the demonstrated behaviour are the
     same thing. Do not spawn the workspace's own CLI binary as a subprocess — it is gated behind
     the cli feature, which would force this program to declare a required-features list it does not
     otherwise need.

Header comment follows the house shape: what it demonstrates naming each capability, the exact
`cargo run --example eval_scenarios_demo` command, and an explicit statement that it needs no
provider key and no external service because it runs in the default non-live mode (D-16, D-29). Use
question-mark propagation through a boxed-error main; no unwrap or expect.

Run it once and append the exit code and first stdout lines to `36-evidence/36-10-examples.txt`.

Commit per D-26: `docs(36): add eval_scenarios_demo example (EX-105, EX-106, EX-107)`. Run
`cargo fmt --all -- --check`, `cargo check --workspace --all-targets --all-features`,
`cargo build --examples` and `make api-surface` (expect no change) first.
  </action>
  <verify>
    <automated>cargo build --example eval_scenarios_demo &amp;&amp; env -u OPENAI_API_KEY -u ANTHROPIC_API_KEY -u DEEPSEEK_API_KEY cargo run --example eval_scenarios_demo &amp;&amp; cargo build --examples &amp;&amp; cargo fmt --all -- --check &amp;&amp; cargo check --workspace --all-targets --all-features &amp;&amp; ./scripts/check-api-surface.sh .project/current-exports.txt</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --example eval_scenarios_demo` exits 0 with no features flag.
    - `cargo run --example eval_scenarios_demo` exits 0 with all three provider key variables unset.
    - The program's stdout names each declared scenario and its outcome, prints the live-mode variable's current value with a sentence on what it changes, and prints an eval-run command line containing a glob.
    - `grep -c 'eval_scenarios' examples/eval_scenarios_demo.rs` is at least 1.
    - `grep -cE 'Command::new|process::Command' examples/eval_scenarios_demo.rs` is 0 — the CLI binary is not spawned.
    - `grep -cE '\.unwrap\(\)|\.expect\(' examples/eval_scenarios_demo.rs` is 0.
    - `grep -cE 'OPENAI_API_KEY|ANTHROPIC_API_KEY|DEEPSEEK_API_KEY' examples/eval_scenarios_demo.rs` is 0.
    - `git diff --stat HEAD~1 -- src/ crates/ Cargo.toml` is empty — no manifest declaration was needed.
    - `./scripts/check-api-surface.sh .project/current-exports.txt` reports the surface unchanged.
  </acceptance_criteria>
  <done>Three eval capabilities have one runnable offline program that declares scenarios through the shipped macro, names the live-mode toggle without enabling it, and shows the command-line form against a real scenario file.</done>
  <reversibility rating="costly">The program name becomes a README anchor and a CHANGELOG bullet; EX-107's spawn-the-CLI versus drive-the-runner choice is recorded here as the D-15 discretion exercised.</reversibility>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

New files and binary targets created by **this plan**:
- `examples/node_result_cache.rs` — new example binary target `node_result_cache`, declared with required-features `["redis-cache"]`
- `examples/observability_tracing.rs` — new example binary target `observability_tracing`, default features
- `examples/observability_otel_export.rs` — new example binary target `observability_otel_export`, declared with required-features `["otel"]`
- `examples/eval_scenarios_demo.rs` — new example binary target `eval_scenarios_demo`, default features
- `.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-10-examples.txt`

Modified (not new): the root `Cargo.toml` — two new example target declarations only.

New symbols created elsewhere in Phase 36 (listed so drift verification does not flag them):
example targets `token_economy_commissary`, `war_engine_configuration`,
`control_flow_dynamic_routing`, `human_in_the_loop_gate`, `graceful_shutdown`,
`agent_runtime_middleware`, `structured_output_schema`, `sanctum_rag_retrieval`,
`platform_api_client`, `webhook_receiver`; Makefile targets `doc-check` and `check-examples`; the
`.pre-commit-config.yaml` hook id `doc-check`; the CI step name
`Check documentation (all features, -D warnings)`; requirement IDs `CURR-11`…`CURR-15`;
`36-EVIDENCE.md` and `36-CI-EVIDENCE.md`.

No new public Rust API is created — SC5 requires `make api-surface` to report no change.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer shell → example process | the example executes in the developer's environment and may read process environment variables |
| example process → external service | the cache program reaches a Redis server and the export program reaches a collector; neither is present in CI |
| trace sink → external collector | trace state values may carry user content; exporting them is an information-flow decision |
| eval runner → provider | live mode makes scenarios call a real provider with a real key |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-35 | Denial of Service | the CI examples job | high | mitigate | An example that needs Redis or a collector and is *run* in CI would flake on every build. Both are build-verified only, matching the existing pattern where CI builds every example and runs none (D-16). The evidence file records each as built-not-run with the reason. |
| T-36-36 | Information Disclosure | the tracing example's trace state values | medium | mitigate | The trace config's state-values field controls whether node state is captured into trace records; the program prints the field's value and what enabling it captures, so a reader configuring an exporter knows what would leave the process. |
| T-36-37 | Information Disclosure | the eval demo's live mode | medium | mitigate | Live mode makes scenarios call a real provider with a real key. The program names the variable and its effect but never enables it, never reads a provider key, and states in its header that it runs in the default non-live mode (D-29). |
| T-36-38 | Information Disclosure | both gated programs' prerequisites | low | mitigate | Neither program embeds a connection string or a credential; each states the service it needs and how to start it locally, so a reader does not copy a hard-coded endpoint. |
| T-36-SC | Tampering | cargo package installs | low | accept | No package-manager install runs in this plan; the eval crate is already an unconditional dev-dependency and the redis-cache and otel features resolve to already-pinned workspace dependencies, per `36-RESEARCH.md`'s Package Legitimacy Audit. The only manifest edit is two example target declarations. |
</threat_model>

<verification>
- `cargo build --example node_result_cache --features "redis-cache"` and `cargo build --example observability_otel_export --features "otel"` both exit 0; neither is executed.
- `cargo run --example observability_tracing` and `cargo run --example eval_scenarios_demo` both exit 0 offline with no provider key set.
- `cargo build --examples` with no features exits 0 and attempts neither gated target.
- `cargo fmt --all -- --check`, `cargo check --workspace --all-targets --all-features` and `make api-surface` are green.
- `git diff --stat` for the plan's commits touches no path under `src/` or `crates/`; the only manifest change is two example declarations.
- `36-evidence/36-10-examples.txt` records two run results and two built-not-run results with reasons.
</verification>

<success_criteria>
- EX-81, EX-82, EX-100, EX-101, EX-102, EX-103, EX-105, EX-106, EX-107 and EX-108 each map to exactly one named program — ten more of the fifty-nine gap rows closed.
- The two gated targets are declared in the root manifest with their required features, ready for the CI and script invocations plan 36-12 adds.
- `make api-surface` reports no change.
</success_criteria>

<output>
Create `.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-10-SUMMARY.md` when done, carrying the D-24 closure table with one row per EX ID: `ID | capability | program | commit`, plus the two new gated targets and their required-features lists so plan 36-12 can add their CI and script invocations, and an explicit built-not-run note for each services-requiring program.
</output>
