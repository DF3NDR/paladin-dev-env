---
phase: 36-rustdoc-zero-warning-bar-examples-currency
plan: 12
type: execute
wave: 7
depends_on: ["36-02", "36-03", "36-04", "36-05", "36-09", "36-10"]
files_modified:
  - scripts/check-all-examples.sh
  - Makefile
  - .pre-commit-config.yaml
  - .github/workflows/ci.yml
  - .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-12-check-examples.txt
  - .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-12-closing-measurement.txt
autonomous: true
requirements: [CURR-11, CURR-12, CURR-13, CURR-15]
must_haves:
  truths:
    - "`cargo doc --workspace --no-deps` teed to a file followed by the negated grep — the exact `ci.yml` lint-job expression — exits 0 on the closing commit: the default-feature run emits zero lines matching the warning prefix, down from 65 content diagnostics plus 8 summary lines."
    - "`RUSTDOCFLAGS=\"-D warnings\" cargo doc --workspace --all-features --no-deps` exits 0 on the closing commit, down from exit 101."
    - "`cargo test --workspace --doc` is green on the closing commit, with no fewer passing doctests than the 462 recorded at baseline."
    - "`make doc-check` exists, runs the three checks in order with a labelled echo before each, and exits 0; `make clean-code` depends on it."
    - "`.pre-commit-config.yaml` carries a `doc-check` hook at the pre-push stage with the same file filter as the api-surface hook."
    - "The CI lint job carries a step named `Check documentation (all features, -D warnings)` immediately after the existing documentation step."
    - "`scripts/check-all-examples.sh` runs the CI Example Muster invocations verbatim — one per feature split, including a dedicated invocation for every gated target this phase added — plus the binary-count assertion, and `make check-examples` invokes it."
    - "The CI Example Muster comment's file count and target count match what `find examples -name '*.rs' | wc -l` and the `[[example]]` block count actually report."
    - "`make api-surface` reports the surface unchanged."
  artifacts:
    - "scripts/check-all-examples.sh — rewritten to mirror the CI feature-split invocations"
    - "Makefile — new doc-check and check-examples targets; clean-code depends on doc-check"
    - ".pre-commit-config.yaml — new doc-check pre-push hook"
    - ".github/workflows/ci.yml — new lint-job all-features documentation step, new gated-example build steps, corrected count comment"
    - ".planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-12-check-examples.txt"
    - ".planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-12-closing-measurement.txt"
  key_links:
    - "The gate edits and the closing measurement land in ONE commit so `git bisect` never reaches a commit where the gate exists but fails (D-13)."
    - "`cargo build --examples` silently skips targets whose required-features are unmet — exit 0, no warning — so every gated target added by plans 36-09 and 36-10 needs its own explicit invocation in both `ci.yml` and `scripts/check-all-examples.sh`, or it is never built anywhere."
    - "The CI binary-count assertion derives its expected count at runtime from `find examples -name '*.rs' | wc -l`, so it self-corrects; only the human-readable comment above it needs updating."
    - "`cargo test --workspace --doc` already runs in CI and is not duplicated by the new lint-job step (D-12)."
  prohibitions:
    - statement: "make check-examples is NOT added to the pre-push hook — four or more full example builds are too slow for a push hook (D-14, and the deferred-ideas register)."
      status: recalled
      verification: flagged-unverified
    - statement: "The rewritten script must not use a per-file cargo check with all features — that invocation silently satisfies every required-features gate and is exactly the defect being removed (D-14)."
      status: recalled
      verification: flagged-unverified
    - statement: "No library source under src/ or crates/ and no examples/*.rs file is modified by this plan."
      status: recalled
      verification: flagged-unverified
    - statement: "The existing make test-doc target and the make doc target are kept, not replaced (D-11)."
      status: recalled
      verification: flagged-unverified
    - statement: "The zero bar is not weakened, narrowed in scope, or given a carve-out; the ci.yml lint-job expression is reproduced byte-identically in make doc-check (D-00a)."
      status: recalled
      verification: flagged-unverified
---

<objective>
Wire both rustdoc bars and the doctest run into the local gate set and into CI, rewrite the local
examples check so it tests what CI tests, and capture the closing zero measurement — all in the
last wave, after the workspace zero is actually reachable.

This plan implements D-00a (the two bar commands, neither reopened nor weakened), D-00d (the
`doc-examples` crate is outside the missing-docs bar but inside the rustdoc link bar, so it counts
in the workspace run), D-00f (doctests stay green, and the wider all-features doctest invocation is
deliberately **not** run because it trips the unrelated CLI-isolation conflict Phase 36.1 SC3
owns), D-01 and D-02 (the closing measurement is the re-baseline's mirror image: zero, with any
residual diagnostic minted as a new row rather than explained away), D-03 (if the local zero and
the CI figure disagree on the closing commit, the CI figure wins and the difference is recorded,
not explained away), D-11 (`make doc-check` as the single local source of truth, `clean-code`
gaining it as a dependency, and the pre-push hook entry), D-12 (the CI lint-job step, which is a
change to a required check and therefore needs a real recorded CI run — plan 36-13 captures it),
D-13 (ordering: this is the last wave, and the gate edits plus the closing measurement are one
commit), D-14 (`scripts/check-all-examples.sh` rewritten to the CI invocations, exposed as
`make check-examples`, and deliberately kept out of the pre-push hook), D-17 (each gated target
gets its own CI and script invocation, and the count comment is corrected in the same edit),
D-23 (the doctest guarantee is the narrow mechanical one: `cargo test --workspace --doc` stays
green), D-24, D-26 and D-28.

Purpose: a bar nobody runs regrows. Until `make doc-check` exists and `clean-code` depends on it,
the only thing standing between the workspace and a sixty-sixth warning is that someone remembers
to look.

Spec-less probe fallback skipped (visible, recorded): Phase 36 has no SPEC and its requirement IDs
were minted by this planning run, so `must_haves` are derived from the five ROADMAP Phase 36
Success Criteria and the CONTEXT decisions.

Output: a rewritten examples check, a `doc-check` target wired into `clean-code` and pre-push, a
new CI lint step, corrected CI example coverage, and the closing zero measurement — in two commits,
the second of which carries the whole gate.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-CONTEXT.md
@.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-RESEARCH.md
@.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-PATTERNS.md
@.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-09-SUMMARY.md
@.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-10-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite scripts/check-all-examples.sh to mirror the CI Example Muster, and expose it as make check-examples</name>
  <files>scripts/check-all-examples.sh, Makefile, .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-12-check-examples.txt</files>
  <read_first>
    - scripts/check-all-examples.sh (the whole file — the per-file loop it replaces, which runs a cargo check with all features enabled and therefore can never reproduce a required-features gap)
    - .github/workflows/ci.yml lines 530-580 (the Example Muster job: the explanatory comment stating how many `.rs` files and how many declared targets exist, the four build invocations, and the binary-count assertion that derives its expected count at runtime)
    - Cargo.toml lines 444-495 (every `[[example]]` declaration, including the four gated targets plans 36-09 and 36-10 added, with their required-features lists)
    - scripts/check-doc-examples.sh (the house script conventions: shebang, strict mode, coloured section echoes, exit codes)
    - Makefile lines 155-170 (the existing `check-doc-examples` target) and lines 374-384 (the `api-surface` target) — the two style analogs for the new `check-examples` target
    - .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-09-SUMMARY.md and 36-10-SUMMARY.md (each lists the gated targets it added and their required-features lists)
  </read_first>
  <action>
Rewrite `scripts/check-all-examples.sh` so the local check and the CI job test the same thing
(D-14). The current per-file loop enables every feature for every example, which silently satisfies
every gate and is precisely the blind spot the CI job's split exists to cover.

The rewritten script runs, in order:
  1. The default-features bulk build with no feature flags — the invocation that covers every
     auto-discovered target and silently skips every gated one.
  2. One explicit build invocation per feature split, mirroring the CI job's steps: the vision
     pair, the content-processing target, the web-server targets, and one invocation for each
     gated target this phase added, each with exactly the feature list its `[[example]]` block
     declares. Read those lists from `Cargo.toml` and from the two SUMMARYs rather than assuming
     them.
  3. The binary-count assertion: derive the expected count from the number of `.rs` files under
     `examples/`, count the executables actually produced under the debug examples directory, and
     fail with the missing names listed if the two disagree.

Keep the `--offline` flag the CI steps use if the local registry cache supports it; if a build
fails only because of `--offline`, drop that flag and record the reason in the evidence file
rather than silently diverging from CI. Follow the house script conventions: strict-mode shebang,
a labelled echo before each invocation, a summary at the end, non-zero exit on any failure.

Add a `check-examples` target to the `Makefile` in the style of the existing `check-doc-examples`
and `api-surface` targets — a `.PHONY` declaration, a help comment, a coloured echo and the script
invocation. Do **not** add it to `clean-code` and do **not** add it to the pre-push hook: several
full example builds are too slow for a push hook, which is the deferred-ideas register's recorded
position (D-14).

Run `make check-examples` and record its full output — every invocation and the binary-count
result — in `36-evidence/36-12-check-examples.txt`. Run `make lint-shell` as well; it only scans
git-tracked shell files, so stage the rewritten script before trusting a clean report.

Commit this on its own, before the gate commit, because D-14 requires the examples check to be run
and recorded *before* the closing commit:
`docs(36): rewrite check-all-examples.sh to mirror the CI feature split (D-14)`. Stage with
`git add -- scripts/check-all-examples.sh Makefile .planning/phases/36-*/36-evidence/36-12-check-examples.txt`,
never `git add .`.
  </action>
  <verify>
    <automated>make lint-shell &amp;&amp; test "$(grep -c 'cargo check --example' scripts/check-all-examples.sh)" = 0 &amp;&amp; grep -q 'check-examples:' Makefile &amp;&amp; make check-examples</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/check-all-examples.sh` contains no `cargo check --example` invocation and no `--all-features` flag.
    - The script contains one `cargo build --examples` invocation with no feature flags, plus one explicit `cargo build --example` invocation for each feature split, and the number of explicit invocations equals the number of `[[example]]` blocks' distinct required-features lists in `Cargo.toml`.
    - `make check-examples` exits 0 and its output names every invocation it ran and the expected-versus-found binary count.
    - `grep -c 'check-examples' Makefile` is at least 2 (the `.PHONY` line and the target line) and `grep -c 'check-examples' .pre-commit-config.yaml` is 0.
    - `grep -c 'clean-code: .*check-examples' Makefile` is 0 — the examples check is not in the `clean-code` chain.
    - `make lint-shell` exits 0 with the rewritten script staged.
    - `git diff --stat HEAD~1 -- src/ crates/ examples/` is empty.
    - `36-evidence/36-12-check-examples.txt` records the full run output including the binary-count line.
  </acceptance_criteria>
  <done>The local examples check runs exactly what CI runs, every gated target is explicitly built, and `make check-examples` is available and recorded green — committed ahead of the gate commit.</done>
</task>

<task type="auto">
  <name>Task 2: The single gate-wiring commit — make doc-check, clean-code, the pre-push hook, the CI lint step, the CI example coverage, and the closing zero measurement</name>
  <files>Makefile, .pre-commit-config.yaml, .github/workflows/ci.yml, .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-12-closing-measurement.txt</files>
  <read_first>
    - .github/workflows/ci.yml lines 55-70 (the lint job's formatting, clippy and documentation steps — the new step goes immediately after the documentation step, in the same shape)
    - .github/workflows/ci.yml lines 495-505 (the existing workspace doctest step, which must NOT be duplicated)
    - .github/workflows/ci.yml lines 530-580 (the Example Muster comment, the build invocations and the binary-count assertion — the comment's stated file count and target count are corrected here)
    - Makefile lines 118-170 (the existing `test-doc` and `check-doc-examples` targets, both kept) and lines 325-350, 374-384, 410-422 (`lint`, `check`, `api-surface`, `doc` and the `clean-code` dependency chain)
    - .pre-commit-config.yaml lines 95-135 (the pre-push stage block, including the api-surface hook whose id, language, stages, pass_filenames and files filter the new hook copies, and the comment above it explaining why it sits at pre-push)
    - .planning/decisions/0033-cargo-doc-warning-bar.md (the ratified bar and the doc-examples missing-docs disposition — the new target must reproduce the bar, not restate it differently)
    - Cargo.toml lines 444-495 (every `[[example]]` declaration, for the CI invocations and the corrected count comment)
    - .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-12-check-examples.txt (Task 1's recorded run — the invocation list the CI steps must match)
  </read_first>
  <action>
All the edits below plus the closing measurement go into **one commit** (D-13), so no commit in
history has the gate installed while the gate is red.

**`Makefile` — new `doc-check` target (D-11).** Add a `.PHONY: doc-check` target with a help
comment naming the ADR that ratified the bar, running three labelled steps in order, each preceded
by a coloured echo in the file's existing style:
  1. The `ci.yml` lint-job expression reproduced byte-identically: the workspace documentation
     build with no dependencies, teed to a temporary file, followed by the negated grep on that
     file. Do not paraphrase it — copy it from `ci.yml`.
  2. The all-features build with rustdoc warnings promoted to errors.
  3. The workspace doctest run.
Then extend the `clean-code` dependency chain so it ends with `doc-check`. Keep `test-doc` and
`doc` exactly as they are — they are not replaced (D-11). The per-crate all-features sweep is
deliberately **not** added as a fourth step: the workspace all-features command already fails on
the first red crate, which is sufficient as a gate, and the sweep's value is enumeration during
debugging rather than gating (the Claude's-discretion item CONTEXT leaves open, resolved here and
recorded in the SUMMARY).

**`.pre-commit-config.yaml` — new `doc-check` hook (D-11).** Add an entry beside the api-surface
hook with `id: doc-check`, a name naming the bar, `entry: make doc-check`, `language: system`,
`stages: [pre-push]`, `pass_filenames: false` and the same `files:` filter the api-surface hook
uses for Rust sources and the manifest. Add a comment above it in the same voice as the existing
one, explaining why it sits at pre-push: the cost, and the fact that executor commits bypass the
commit stage entirely.

**`.github/workflows/ci.yml` — lint job (D-12).** Insert one step immediately after the existing
documentation step, named `Check documentation (all features, -D warnings)`, running the
all-features build with rustdoc warnings promoted to errors. Do not add a doctest step — the
workspace doctest run already exists later in the file and is not duplicated.

**`.github/workflows/ci.yml` — Example Muster (D-17).** Add one build step per gated target this
phase introduced, in the same shape as the existing web-server step, each with exactly the feature
list its `[[example]]` block declares; match the invocation list Task 1 put in
`scripts/check-all-examples.sh` exactly, so the two never disagree. Then correct the explanatory
comment above the steps: recompute the `.rs` file count from the tree and the declared-target count
from `Cargo.toml`, update both numbers and the default-features step's own name where it states how
many auto-discovered targets it covers, and update the binary-count assertion step's name.
Recompute these numbers rather than trusting any figure quoted in a planning document — the
comment was already off by one before this phase started. The assertion's own logic derives its
expected count at runtime and needs no change.

**Closing measurement (D-01, D-02, D-03).** With the edits in the working tree, run and capture
verbatim into `36-evidence/36-12-closing-measurement.txt`:
  - `make doc-check`, with each of its three steps' output and the overall exit code.
  - The two bar commands run individually, with their exit codes, so the evidence records them
    separately from the target that wraps them.
  - `cargo test --workspace --doc`, with its passed/failed/ignored counts compared against the
    462 passed / 0 failed / 210 ignored baseline.
  - `make check-examples` re-run after the CI-step edits, to confirm the script and the workflow
    still agree.
  - `make api-surface`, expecting no change.
  - `cargo fmt --all -- --check` and `cargo check --workspace --all-targets --all-features`.
  - The toolchain versions and the commit SHA being measured.
If any residual diagnostic remains, do **not** narrow the command or add a carve-out: mint it as a
new row numbered after the audit's last `RD-nn` or `EX-nn`, record it in the evidence file with
the command output that found it, and report it in the SUMMARY as a deviation for the planner
(D-02). If the local result and a CI result later disagree, the CI figure wins and the difference
is recorded in `36-CI-EVIDENCE.md` rather than explained away (D-03) — plan 36-13 captures the CI
run.

Commit everything above together per D-13 and D-26:
`docs(36): wire the rustdoc zero-warning gate into make, pre-push and CI (D-11, D-12, D-13)`.
Stage `Makefile`, `.pre-commit-config.yaml`, `.github/workflows/ci.yml` and the closing-measurement
evidence file with explicit paths, never `git add .`.
  </action>
  <verify>
    <automated>grep -q '^doc-check:' Makefile &amp;&amp; grep -qE '^clean-code: .*doc-check' Makefile &amp;&amp; grep -q 'id: doc-check' .pre-commit-config.yaml &amp;&amp; grep -q 'Check documentation (all features, -D warnings)' .github/workflows/ci.yml &amp;&amp; make doc-check &amp;&amp; make check-examples &amp;&amp; cargo fmt --all -- --check &amp;&amp; cargo check --workspace --all-targets --all-features &amp;&amp; ./scripts/check-api-surface.sh .project/current-exports.txt</automated>
  </verify>
  <acceptance_criteria>
    - `make doc-check` exits 0 and its output shows three labelled steps.
    - `cargo doc --workspace --no-deps` teed to a file, followed by the negated grep, exits 0 — the composite lint-job expression is green.
    - `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --all-features --no-deps` exits 0.
    - `cargo test --workspace --doc` reports 0 failed and at least 462 passed.
    - `grep -cE '^clean-code: .*doc-check' Makefile` is 1, and `grep -c '^test-doc:' Makefile` and `grep -c '^doc:' Makefile` are each still 1.
    - `.pre-commit-config.yaml` contains `id: doc-check` with `stages: [pre-push]`, `entry: make doc-check`, `pass_filenames: false` and the same `files:` filter as the api-surface hook.
    - `.github/workflows/ci.yml` contains exactly one step named `Check documentation (all features, -D warnings)`, and `grep -c 'cargo test --workspace --doc' .github/workflows/ci.yml` is still 1 (the doctest step was not duplicated).
    - The number of `cargo build --example` steps in the CI Example Muster job equals the number of such invocations in `scripts/check-all-examples.sh`.
    - The `.rs` file count stated in the Example Muster comment equals `find examples -name '*.rs' | wc -l`, and the declared-target count stated there equals the number of `[[example]]` blocks in `Cargo.toml`.
    - `make check-examples` exits 0 after the CI edits.
    - `./scripts/check-api-surface.sh .project/current-exports.txt` reports the surface unchanged.
    - `git log -1 --pretty=%s` names D-11, D-12 and D-13, and `git show --stat HEAD` lists `Makefile`, `.pre-commit-config.yaml`, `.github/workflows/ci.yml` and the closing-measurement evidence file — and no `.rs` file.
    - `36-evidence/36-12-closing-measurement.txt` records both bar commands' exit codes, the doctest counts against the baseline, the toolchain versions and the measured SHA.
  </acceptance_criteria>
  <done>Both bars and the doctest run are enforced locally through `make clean-code` and pre-push and in CI through the lint job, every gated example is built in CI, and the commit that installs the gate is the same commit that proves it green.</done>
  <reversibility rating="costly">D-12 changes a required CI check: once merged, every pull request gates on the all-features rustdoc command, and removing it later is a visible weakening of the bar that would need its own ADR-0033 amendment.</reversibility>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

New symbols and entries created by **this plan**:
- Makefile target `doc-check` (and its `.PHONY` declaration)
- Makefile target `check-examples` (and its `.PHONY` declaration)
- `.pre-commit-config.yaml` hook id `doc-check`
- `.github/workflows/ci.yml` step name `Check documentation (all features, -D warnings)`, plus one
  `cargo build --example` step per gated target added by plans 36-09 and 36-10
- `.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-12-check-examples.txt`
- `.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-12-closing-measurement.txt`

Rewritten (not new): `scripts/check-all-examples.sh`.

No Rust symbol, no file under `src/`, `crates/` or `examples/`, and no public API is created or
modified by this plan.

New symbols created elsewhere in Phase 36 (listed so drift verification does not flag them):
example targets `token_economy_commissary`, `war_engine_configuration`,
`control_flow_dynamic_routing`, `human_in_the_loop_gate`, `graceful_shutdown`,
`agent_runtime_middleware`, `structured_output_schema`, `sanctum_rag_retrieval`,
`platform_api_client`, `webhook_receiver`, `node_result_cache`, `observability_tracing`,
`observability_otel_export`, `eval_scenarios_demo`; requirement IDs `CURR-11`…`CURR-15`;
`36-EVIDENCE.md` and `36-CI-EVIDENCE.md`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer push → required CI check | this plan changes what a required check enforces, so every future pull request is affected |
| local gate → CI gate | if `make doc-check` and the CI steps drift apart, a developer's green local run stops predicting CI |
| local examples script → CI examples job | if the two invocation lists drift, a gated example regresses locally unnoticed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-43 | Tampering | the ratified zero bar | critical | mitigate | The bar must not be narrowed, scoped down or given a carve-out to make the gate pass. `make doc-check` reproduces the `ci.yml` lint-job expression byte-identically, copied from the workflow rather than paraphrased, and any residual diagnostic is minted as a new row and reported as a deviation rather than suppressed (D-00a, D-02). |
| T-36-44 | Repudiation | the closing commit | high | mitigate | D-13 requires the gate edits and the closing measurement in one commit, so `git bisect` can never land on a commit where the gate exists but fails, and the evidence file in that same commit is the proof it was green at install time. |
| T-36-45 | Spoofing | gated example coverage in CI | high | mitigate | `cargo build --examples` exits 0 while silently skipping every target whose required-features are unmet. Each gated target gets an explicit CI step, and the acceptance criterion asserts the CI step count equals the script's invocation count so the two lists cannot drift apart. |
| T-36-46 | Denial of Service | the pre-push hook | medium | mitigate | `make check-examples` is deliberately kept out of the pre-push hook — several full example builds per push would make the hook unusable and push developers toward bypassing it entirely (D-14). Only `doc-check` joins pre-push, filtered to Rust sources and the manifest. |
| T-36-47 | Repudiation | local-versus-CI disagreement | medium | mitigate | D-03 fixes the precedence in advance: the CI figure wins and the difference is recorded in `36-CI-EVIDENCE.md`, never explained away. Plan 36-13 captures the real CI run that D-12 requires. |
| T-36-SC | Tampering | cargo package installs | low | accept | No package-manager install runs in this plan; the edits are a shell script, a Makefile, a hook config and a workflow file. |
</threat_model>

<verification>
- `make doc-check` exits 0, and each of its three steps is individually green when run alone.
- `cargo test --workspace --doc` reports 0 failed and no fewer than 462 passed.
- `make check-examples` exits 0 and its invocation list matches the CI Example Muster steps one for one.
- `make lint-shell`, `cargo fmt --all -- --check`, `cargo check --workspace --all-targets --all-features` and `make api-surface` are green.
- `git show --stat HEAD` for the gate commit lists only `Makefile`, `.pre-commit-config.yaml`, `.github/workflows/ci.yml` and the closing-measurement evidence file.
- Both evidence files record verbatim command output including exit codes, toolchain versions and the measured SHA.
</verification>

<success_criteria>
- Both bar commands exit 0 on the closing commit, closing ROADMAP Phase 36 SC1 locally.
- `make clean-code` and the pre-push hook both run the bars, so the warning count cannot silently regrow (SC2).
- Every example target — gated and ungated — is built by both the CI job and the local script under the feature set it actually needs, and the doctest run is green (SC3).
- `make api-surface` reports no change (SC5).
- The gate commit is also the commit that proves the gate green (D-13).
</success_criteria>

<output>
Create `.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-12-SUMMARY.md` when done, recording: the closing measurement figures beside their baselines (65 → 0 default diagnostics, exit 101 → exit 0 all-features, doctests against 462/0/210), the resolved Claude's-discretion item on whether the per-crate sweep joins `make doc-check`, the corrected example file and target counts, and any newly minted `RD-` or `EX-` row with the output that found it.
</output>
