---
phase: 36-rustdoc-zero-warning-bar-examples-currency
plan: 13
type: execute
wave: 8
depends_on: ["36-11", "36-12"]
files_modified:
  - .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-EVIDENCE.md
  - .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-CI-EVIDENCE.md
  - .planning/WINDOWS.md
  - CHANGELOG.md
autonomous: false
requirements: [CURR-11, CURR-12, CURR-14]
must_haves:
  truths:
    - "`36-EVIDENCE.md` carries the full closure map: every one of the 143 `RD-nn` rows and every one of the 64 `EX-nn` work rows from `34-AUDIT.md` §6, each mapped to a commit, with followers listed on their lead's row."
    - "`36-EVIDENCE.md` carries the baseline figures and the closing figures side by side — 65 default-feature diagnostics to zero, all-features exit 101 to exit 0, and the doctest counts against the 462 passed / 0 failed / 210 ignored baseline."
    - "`WINDOWS.md` rows 36 and 37 both read `fixed`, moved only through the `gsd-tools` windows command, never by hand-editing the ledger."
    - "`CHANGELOG.md`'s `[0.10.0]` `### Documentation` subsection carries this phase's reader-facing bullets, appended to the existing Phase 35 entries and naming no `RD-nn` or `EX-nn` identifier."
    - "`36-CI-EVIDENCE.md` records a real CI run on the pushed branch: the job name, the run id, and the result of the new all-features documentation step, plus the Example Muster job's result."
    - "Any disagreement between the local closing measurement and the CI figures is recorded in `36-CI-EVIDENCE.md` with the CI figure taken as authoritative."
  artifacts:
    - ".planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-EVIDENCE.md — assembled closure map and measurement record"
    - ".planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-CI-EVIDENCE.md — the real CI run that proves the new required-check step"
    - ".planning/WINDOWS.md — rows 36 and 37 resolved"
    - "CHANGELOG.md — appended Documentation bullets under [0.10.0]"
  key_links:
    - "`WINDOWS.md` row 37 names the `HeuristicTokenCounter` link at `crates/paladin-memory/src/token_counter/mod.rs:3`, which is RD-01 with followers RD-66 and RD-126 — closed by plan 36-01's tracer task; row 36 is the workspace-wide observation the whole 143-row enumeration closes."
    - "The `gsd-tools windows fixed <id>` subcommand takes the row id only and records no reason string, so the closing-commit citation and the zero-warning capture live in `36-EVIDENCE.md` and in this plan's SUMMARY."
    - "D-12 makes the all-features documentation step part of a required check, so a real pushed-branch CI run is the only acceptable evidence — it cannot be simulated from this environment."
  prohibitions:
    - statement: "WINDOWS.md is never edited by hand — rows move status only through the gsd-tools windows command (D-27, anti-pattern rule 15)."
      status: recalled
      verification: flagged-unverified
    - statement: "No new WINDOWS.md row is added for a finding this phase closed itself (D-27)."
      status: recalled
      verification: flagged-unverified
    - statement: "CHANGELOG bullets are reader-facing and cite no RD-nn or EX-nn identifier (D-00j)."
      status: recalled
      verification: flagged-unverified
    - statement: "No source file under src/, crates/ or examples/ is modified by this plan."
      status: recalled
      verification: flagged-unverified
    - statement: "A row that is neither closed nor explicitly dispositioned is never omitted from the closure map — Phase 36.1 SC4 and Phase 37 verify by ID against this map (D-02, D-24)."
      status: recalled
      verification: flagged-unverified
---

<objective>
Close the phase's bookkeeping: assemble the full 207-row closure map, resolve the two ledger rows
this phase owns, tell the reader what changed, and capture the real CI run the new required-check
step demands.

This plan implements D-00f (the doctest guarantee, recorded against its baseline), D-00j
(`CHANGELOG.md` `[0.10.0]` already carries a `### Documentation` subsection; Phase 36 **appends**
to it), D-01 and D-02 (the closure map records every row as closed or explicitly dispositioned,
and any row that did not reproduce is recorded as closed-by-drift with the command output that
proves it, never silently skipped), D-03 (if the local and CI figures disagree, the CI figure wins
and the difference is recorded rather than explained away), D-12 (a change to a required check
follows the Phase 33 pattern: a real CI run on the pushed branch recorded in `36-CI-EVIDENCE.md`
before the phase is verified), D-18 (no example was deleted, so no deletion note is needed in the
changelog), D-24 (the phase-level record reproduces the full map from the per-plan closure
tables), D-25 (`36-EVIDENCE.md` plus `36-evidence/` for the long captures, and `36-CI-EVIDENCE.md`
for the CI run), D-26 (a commit per artifact group), D-27 (`WINDOWS.md` rows 36 and 37 move to
`fixed` through `gsd-tools` only) and D-28 (`make api-surface` as the last gate).

Purpose: Phase 36.1 SC4 and Phase 37 both verify this phase by ID. A row that is neither closed
nor dispositioned breaks that chain, and a ledger row left open after its defect is gone makes the
ledger disagree with the registers — which is exactly what Phase 36.1 exists to repair.

Spec-less probe fallback skipped (visible, recorded): Phase 36 has no SPEC and its requirement IDs
were minted by this planning run, so `must_haves` are derived from the five ROADMAP Phase 36
Success Criteria and the CONTEXT decisions.

Output: an assembled evidence file, two resolved ledger rows, appended changelog bullets, and a
recorded CI run.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-CONTEXT.md
@.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-01-SUMMARY.md
@.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-11-SUMMARY.md
@.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-12-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assemble 36-EVIDENCE.md — the full 143 + 64 closure map and the baseline-versus-closing measurement record</name>
  <files>.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-EVIDENCE.md (as created by plan 36-01 — the header, the baseline section and the tracer plan's own section, which this task extends rather than replaces)
    - the closure tables in every SUMMARY of this phase: 36-01, 36-02, 36-03, 36-04, 36-05, 36-06, 36-07, 36-08, 36-09, 36-10, 36-11, 36-12
    - .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/ (every capture file the plans wrote: the baseline, the per-crate sweeps, the example runs, the README cross-check, the examples check and the closing measurement)
    - .planning/phases/34-documentation-currency-audit/34-AUDIT.md §6 (the authoritative 143 `RD-nn` and 64 `EX-nn` row list, with each lead row's Blocks cell)
    - .planning/phases/34-documentation-currency-audit/34-EVIDENCE.md (the house `NN-EVIDENCE.md` shape to follow)
    - .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-RESEARCH.md, the Re-baseline delta section (the baseline figures: 65 default-feature diagnostics across 8 crates, 77 per-crate all-features errors, workspace all-features exit 101, doctests 462 passed / 0 failed / 210 ignored, 48 of 48 example binaries, api-surface clean at 3959 items)
  </read_first>
  <action>
Extend `36-EVIDENCE.md` into the phase-level record.

**Closure map.** Reproduce every one of the 143 `RD-nn` rows and every one of the 64 `EX-nn` work
rows from `34-AUDIT.md` §6, assembled from the per-plan closure tables — do not re-derive them from
the source tree. Columns for the rustdoc rows: ID, cited file:line, actual file:line where it
differed, kind, fix, commit; followers appear on their lead's row. Columns for the example rows:
ID, capability, program or file, README section, commit. Every ID must appear exactly once. If any
ID is missing from every SUMMARY, do not omit it and do not invent a closure: record it as
outstanding with the reason, and report it in this plan's SUMMARY as a deviation for the planner
(D-02, D-24).

**Drift record.** Record the two known post-audit drift observations with their evidence: the
re-baseline found zero drift against `34-AUDIT.md` at the phase's starting HEAD, and plan 36-08
re-ran the RAG capability greps and found that a module Phase 35 added after the audit SHA changed
at least one recorded zero-hit figure. Record both with the command output the plans captured. If
plan 36-12 minted any new `RD-` or `EX-` row, list it here with the same columns as the audit's
own table.

**Measurement record.** Put the baseline and the closing figures side by side in one table: the
default-feature diagnostic count (65 across 8 crates, with the per-crate split) against zero; the
workspace all-features exit code (101) against zero; the per-crate all-features error count (77
across 8 crates) against zero; the doctest counts against the 462 passed / 0 failed / 210 ignored
baseline; the example binary count and the invocation list; and the api-surface item count and
verdict. Cite the capture file under `36-evidence/` for each figure rather than pasting long
output inline (D-25).

**Ledger note.** State which rows closed `WINDOWS.md` row 36 (the whole 143-row enumeration) and
row 37 (RD-01 with followers RD-66 and RD-126, at
`crates/paladin-memory/src/token_counter/mod.rs:3`), and name the closing commit. Task 2 performs
the actual status move.

Commit per D-26: `docs(36): assemble the phase closure map and measurement record (D-24, D-25)`.
Stage with `git add -- .planning/phases/36-*/36-EVIDENCE.md`, never `git add .`. No `.rs` file is
staged.
  </action>
  <verify>
    <automated>EV=.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-EVIDENCE.md; for i in $(seq -w 1 143); do grep -q "RD-$i" "$EV" || { echo "MISSING RD-$i"; exit 1; }; done; echo "all 143 RD rows present"</automated>
  </verify>
  <acceptance_criteria>
    - `36-EVIDENCE.md` mentions every identifier `RD-01` through `RD-143` at least once — proven by the verify loop exiting 0.
    - `36-EVIDENCE.md` mentions `EX-01`, `EX-33`, `EX-55`, `EX-121`, `EX-122` and every identifier from `EX-62` through `EX-120`.
    - Every `RD-` and `EX-` row in the closure map carries a commit reference or an explicit outstanding-with-reason note; none is blank.
    - The measurement table states the baseline figure and the closing figure for each of: default-feature diagnostics, workspace all-features exit code, per-crate all-features errors, doctest passed/failed/ignored, example binary count, and api-surface verdict.
    - Each measurement row cites a capture file path under `36-evidence/`.
    - `git diff --stat HEAD~1 -- src/ crates/ examples/ Makefile .github/` is empty.
  </acceptance_criteria>
  <done>Every one of the 207 audit rows is traceable from `36-EVIDENCE.md` to a commit or an explicit disposition, and the phase's before-and-after figures sit side by side with their captures cited.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve WINDOWS.md rows 36 and 37 through gsd-tools, and append the CHANGELOG Documentation bullets</name>
  <files>.planning/WINDOWS.md, CHANGELOG.md</files>
  <read_first>
    - .planning/WINDOWS.md rows 36 and 37 (their `description` text, which names what each row claims, and their current `open` status — read only; never hand-edit this file)
    - .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-EVIDENCE.md (as assembled in Task 1 — the closing commit and the zero-warning capture the ledger note cites)
    - CHANGELOG.md lines 421-460 (the `[0.10.0]` `### Documentation` subsection Phase 35 created — the bullet voice and length to match, and the append point)
    - .planning/phases/35-mdbook-currency/35-CONTEXT.md D-25 (the convention that established this subsection)
    - .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-11-SUMMARY.md (the gallery's new sections — the reader-facing summary of what the examples work delivered)
  </read_first>
  <action>
**`WINDOWS.md` (D-27).** Move rows 36 and 37 to `fixed` using the `gsd-tools` windows command
only — `node .claude/gsd-core/bin/gsd-tools.cjs windows fixed 36` and the same for row 37, resolved
through whichever launcher path this repository's executor already uses. Never hand-edit
`.planning/WINDOWS.md`: that file is a tool-managed ledger and a manual edit desynchronises its
recomputed counts. The `fixed` subcommand takes the row id only and records no reason string, so
put the citation where it is durable: `36-EVIDENCE.md`'s ledger note (Task 1) and this plan's
SUMMARY both name the closing commit, the zero-warning capture, and — for row 37 — the specific
rows RD-01, RD-66 and RD-126 at `crates/paladin-memory/src/token_counter/mod.rs:3`. Add no new
`WINDOWS.md` row for anything this phase closed itself (D-27). Confirm the ledger's own open count
dropped by two.

**`CHANGELOG.md` (D-00j).** Append bullets to the existing `[0.10.0]` `### Documentation`
subsection — do not create a second subsection and do not reorder the existing entries. Write them
reader-facing, in the same voice and length as the Phase 35 bullets already there, naming no
`RD-nn` or `EX-nn` identifier. Cover, in roughly this shape:
  - The rustdoc corpus now builds warning-free under both the default and the all-features
    documentation commands, and both are enforced — locally through the code-quality target and
    the pre-push hook, and in CI through the lint job — so the count cannot silently regrow.
  - The examples gallery gained runnable programs for the engine configuration and checkpoint
    surface, control flow and dynamic routing, human-in-the-loop gating and resume, graceful
    shutdown, the agent runtime and middleware, structured output, the Platform API and webhook
    signature verification, the node-result cache, observability and tracing, evaluation
    scenarios, the token economy and RAG retrieval — each listed in the gallery index with what it
    demonstrates and how to run it.
  - The two in-process HTTP host examples now mount the same routers the shipped server mounts.
  - The gallery index is complete in both directions and its stale minimum-Rust-version and
    result-field claims are corrected.
Name no identifier from the audit's work list; a reader of the changelog has no access to it.

Commit the two artifacts separately per D-26:
`chore(36): resolve WINDOWS.md rows 36 and 37` and
`docs(36): add the Phase 36 documentation entries to the changelog`. Stage each with explicit
paths, never `git add .`.
  </action>
  <verify>
    <automated>test "$(grep -cE 'RD-[0-9]+|EX-[0-9]+' CHANGELOG.md)" = 0 &amp;&amp; test "$(grep -c '^### Documentation' CHANGELOG.md)" = 1 &amp;&amp; test "$(grep -cE '^\| 3[67] \|.*\| open \|' .planning/WINDOWS.md)" = 0 &amp;&amp; test "$(grep -cE '^\| 3[67] \|.*\| fixed \|' .planning/WINDOWS.md)" = 2</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/WINDOWS.md` rows 36 and 37 both carry status `fixed` and a non-null `resolved_at`, and neither carries status `open`.
    - The ledger's recomputed open count is exactly two lower than before this task ran.
    - `git diff .planning/WINDOWS.md` shows only the two rows' status and timestamp fields changing, and the ledger's own counts block — no hand-formatted edit anywhere else.
    - `CHANGELOG.md` contains exactly one `### Documentation` heading, and `grep -cE 'RD-[0-9]+|EX-[0-9]+' CHANGELOG.md` is 0.
    - The new changelog bullets sit inside the existing `[0.10.0]` `### Documentation` subsection, after the Phase 35 entries, and mention the zero-warning documentation build, the new example programs, the HTTP host router parity and the gallery index correction.
    - `git diff --stat HEAD~2 -- src/ crates/ examples/` is empty.
  </acceptance_criteria>
  <done>Both ledger rows this phase owns read `fixed`, moved only through the tool that owns the ledger, and the changelog tells a reader what changed without exposing an internal work-list identifier.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Push the branch and record the real CI run that proves the new required-check step</name>
  <files>.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-CI-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/33-commissary-in-tree-adoption/33-CI-EVIDENCE.md (the house `NN-CI-EVIDENCE.md` shape: the CI-run table with job, run id and step result, and row 26's own record of the carried 73-warning baseline this phase ends)
    - .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/36-12-closing-measurement.txt (the local closing figures the CI figures are compared against)
    - .github/workflows/ci.yml lines 55-70 and 530-590 (the lint job's step names and the Example Muster job's step names, so the recorded rows name the real steps)
  </read_first>
  <what-built>
Plan 36-12 added a step named `Check documentation (all features, -D warnings)` to the CI lint job
and added one build step per gated example to the Example Muster job. The lint job is a required
check, so per D-12 and the Phase 33 pattern the phase cannot be verified on a local measurement
alone — a real run on the pushed branch is the evidence.

Before this checkpoint, do the automatable part: push the branch, wait for the run to finish, and
write `36-CI-EVIDENCE.md` in the house shape with one row per relevant job — the lint job with the
result of both documentation steps, the Example Muster job with its build steps and its
binary-count assertion, and the doctest step's result — each row carrying the job name, the run id
and the step conclusion. Record the CI figures beside the local closing figures from
`36-evidence/36-12-closing-measurement.txt`. If they disagree, the CI figure is authoritative
(D-03): record the difference plainly as a finding, do not adjust the local record to match and do
not explain it away. Commit as
`docs(36): record the CI run proving the all-features documentation gate (D-12)`.
  </what-built>
  <how-to-verify>
    1. Confirm the branch was pushed and a workflow run exists for the head commit:
       `git log -1 --pretty=%H` and `gh run list --branch "$(git rev-parse --abbrev-ref HEAD)" --limit 5`.
    2. Open the lint job for that run and confirm both documentation steps are green — the
       pre-existing `Check documentation` step and the new
       `Check documentation (all features, -D warnings)` step.
    3. Open the Example Muster job and confirm every build step is green and the binary-count
       assertion passed with expected equal to found.
    4. Confirm the doctest step is green.
    5. Read `36-CI-EVIDENCE.md` and confirm it names the run id, the job names and each step's
       conclusion, and that any local-versus-CI difference is recorded with the CI figure taken as
       authoritative.
    If the token available in this environment cannot read Actions runs, or the run is still
    queued, say so — the maintainer reads the run in the browser and confirms.
  </how-to-verify>
  <acceptance_criteria>
    - `36-CI-EVIDENCE.md` exists and contains a run id, the lint job name and a conclusion for both documentation steps.
    - The row for `Check documentation (all features, -D warnings)` records a successful conclusion.
    - The Example Muster job's row records every build step and the binary-count assertion's expected-versus-found figures.
    - The file records the CI figures beside the local closing figures, with any difference stated as a finding rather than reconciled away.
    - `git log -1 --pretty=%s` names D-12.
  </acceptance_criteria>
  <resume-signal>Type "approved" once the CI run is green and recorded, or describe the failing step so the phase can address it before verification.</resume-signal>
  <reversibility rating="costly">This is the point at which the all-features documentation command becomes a gate on every future pull request; reverting it later is a visible weakening of the ratified bar (D-12).</reversibility>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

New files created by **this plan**:
- `.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-CI-EVIDENCE.md`

Extended (not new): `.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-EVIDENCE.md`
(created by plan 36-01), `.planning/WINDOWS.md` (two existing rows' status only), `CHANGELOG.md`
(bullets appended to an existing subsection).

No Rust symbol, no file under `src/`, `crates/` or `examples/`, and no public API is created or
modified by this plan.

New symbols created elsewhere in Phase 36 (listed so drift verification does not flag them):
example targets `token_economy_commissary`, `war_engine_configuration`,
`control_flow_dynamic_routing`, `human_in_the_loop_gate`, `graceful_shutdown`,
`agent_runtime_middleware`, `structured_output_schema`, `sanctum_rag_retrieval`,
`platform_api_client`, `webhook_receiver`, `node_result_cache`, `observability_tracing`,
`observability_otel_export`, `eval_scenarios_demo`; Makefile targets `doc-check` and
`check-examples`; the `.pre-commit-config.yaml` hook id `doc-check`; the CI step name
`Check documentation (all features, -D warnings)`; requirement IDs `CURR-11`…`CURR-15`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local measurement → CI measurement | the devcontainer toolchain and the CI toolchain may differ; the gate lives in CI |
| phase record → downstream phases | Phase 36.1 SC4 and Phase 37 verify this phase by ID against the closure map |
| repository → published changelog | changelog bullets are read by users who have no access to the internal work list |
| ledger file → tool-managed state | `WINDOWS.md` carries recomputed counts that a hand edit desynchronises |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-48 | Repudiation | the 207-row closure map | high | mitigate | A row that is neither closed nor dispositioned silently breaks the verification chain two phases downstream. The verify loop asserts every `RD-` identifier from 01 to 143 appears, and the action forbids omitting or inventing a closure — an unmatched ID is recorded as outstanding and reported as a deviation (D-02, D-24). |
| T-36-49 | Spoofing | the local zero measurement | high | mitigate | A local green does not prove a required check passes. D-12 requires a real pushed-branch run, and D-03 makes the CI figure authoritative on any disagreement; the checkpoint blocks phase verification until that run is recorded. |
| T-36-50 | Tampering | `.planning/WINDOWS.md` | medium | mitigate | The ledger recomputes its own counts, so a hand edit leaves the file internally inconsistent. Rows move status only through the `gsd-tools` windows command, and the acceptance criteria assert the diff touches only the two rows' status and timestamp fields plus the counts block (D-27). |
| T-36-51 | Information Disclosure | changelog bullets | low | mitigate | Internal work-list identifiers mean nothing to a reader and imply access to planning artifacts that are not published. The acceptance criterion greps the whole changelog for any such identifier and requires zero (D-00j). |
| T-36-SC | Tampering | cargo package installs | low | accept | This plan touches planning artifacts and one changelog file; no package-manager install runs and no source file is staged. |
</threat_model>

<verification>
- `36-EVIDENCE.md` names every identifier from `RD-01` to `RD-143` and every `EX-nn` work row, each with a commit or an explicit disposition.
- `WINDOWS.md` rows 36 and 37 read `fixed` with a resolved timestamp, and the diff touches nothing else.
- `CHANGELOG.md` has exactly one `### Documentation` heading under `[0.10.0]` and contains no work-list identifier.
- `36-CI-EVIDENCE.md` records a real run id with the new all-features documentation step green.
- `make api-surface` is green as the phase's last gate (D-28).
- No commit in this plan stages a file under `src/`, `crates/` or `examples/`.
</verification>

<success_criteria>
- All 207 audit rows are traceable by ID from `36-EVIDENCE.md` to a commit or a recorded disposition, so Phase 36.1 SC4 and Phase 37 can verify this phase without re-deriving anything.
- `WINDOWS.md` rows 36 and 37 are `fixed`, moved only through the tool that owns the ledger.
- The changelog tells a reader what the phase delivered without exposing an internal identifier.
- A real CI run proves the new required-check step, closing the last piece of ROADMAP Phase 36 SC1 and SC2.
</success_criteria>

<output>
Create `.planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-13-SUMMARY.md` when done, recording: the assembled closure map's row counts (143 `RD-nn`, 64 `EX-nn`, plus any minted rows), the two `WINDOWS.md` rows' resolution with the closing-commit citation and, for row 37, the RD-01 / RD-66 / RD-126 reference, the changelog bullets added, and the CI run id with each recorded step's conclusion.
</output>
