# Plan 36-06 evidence: war_engine_configuration.rs, control_flow_dynamic_routing.rs

## Task 1: examples/war_engine_configuration.rs

Command: `env -u OPENAI_API_KEY -u ANTHROPIC_API_KEY -u DEEPSEEK_API_KEY cargo run --example war_engine_configuration`
Exit code: 0

First lines of stdout:
```
WarEngine Configuration & Checkpoints

1. Inject a Waypoint backend (EX-62)

   Injected InMemoryWaypointStore as this run's WaypointPort implementor.
   A SqliteWaypointStore or PostgresWaypointStore backend satisfies the exact same
    WaypointPort trait -- this program only swaps the injection point, never the
    WarEngine's own logic.

2. Configure the WarEngine (EX-64)

   max_supersteps      = 20
   max_node_visits     = 10
   run_timeout_secs    = Some(60)
   waypoint_durability = Strict
   max_muster_tasks    = 50

3. Override the superstep cap from the environment (EX-65)

   before APP_ENGINE_MAX_SUPERSTEPS: max_supersteps = 20
   after  APP_ENGINE_MAX_SUPERSTEPS: max_supersteps = 5
```

Full stdout also showed (Part 4-6): a 3-waypoint checkpoint history (superstep 1/2/3,
each naming node "looper"), the retention service pruning 2 waypoints and leaving 1
(the thread's protected latest), and the graph fingerprint version `v6` plus the full
`v6:<hex>` fingerprint string with the GraphMismatch explanation.

Also verified:
- `cargo build --example war_engine_configuration` -- exit 0, no `--features` flag.
- `cargo fmt --all -- --check` -- exit 0.
- `cargo check --workspace --all-targets --all-features` -- exit 0.
- `make api-surface` -- "API surface unchanged".
- `grep -c 'Quartermaster' examples/war_engine_configuration.rs` -- 0.
- `grep -cE '\.unwrap\(\)|\.expect\(' examples/war_engine_configuration.rs` -- 0.
- `grep -cE 'OPENAI_API_KEY|ANTHROPIC_API_KEY|DEEPSEEK_API_KEY' examples/war_engine_configuration.rs` -- 0.
- `grep -q 'cargo run --example war_engine_configuration' examples/war_engine_configuration.rs` -- found.

<!-- gsd:write-continue -->
