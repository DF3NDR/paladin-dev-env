---
phase: 36.1-deferred-items-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/cli_isolation_test.rs
  - .planning/phases/36.1-deferred-items-closure/36.1-EVIDENCE.md
  - .planning/phases/36.1-deferred-items-closure/COVERAGE.md
  - .planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-01-all-features-sweep.txt
autonomous: true
requirements: [CURR-18, CURR-19]
must_haves:
  truths:
    - "`cargo test --test cli_isolation --all-features` reports 9 passed and 0 failed — the three-phase-old carried failure is gone, and the guard still fails if `cli` is ever added to the manifest's default feature list (D-15, CURR-18)."
    - "`cargo test --test cli_isolation` under default features still reports 9 passed and 0 failed — the rewrite did not trade one invocation's green for another's red."
    - "`tests/cli_isolation_test.rs`'s `test_cli_feature_is_not_default` reads the root manifest's declared default feature array rather than the current build's compile-time feature configuration, so `--all-features` can no longer force a false failure (D-15)."
    - "`36.1-EVIDENCE.md` exists with the closure-table skeleton covering all seventeen register/ledger/todo lines, and the `cli_isolation` line is already complete with its disposition, fixing commit and verification command (D-20)."
    - "`COVERAGE.md` records the API-coverage declaration for this phase in table-free prose, so the seal gate cannot parse it as a matrix."
    - "The result of `cargo test --workspace --all-features --no-fail-fast` on this plan's head commit is captured verbatim, and every failing target other than `cli_isolation` is named in the capture as a finding for the closing plan (D-16, RESEARCH assumption A2)."
  artifacts:
    - "tests/cli_isolation_test.rs — rewritten guard plus two added `//!` header sentences"
    - ".planning/phases/36.1-deferred-items-closure/36.1-EVIDENCE.md — seeded closure table"
    - ".planning/phases/36.1-deferred-items-closure/COVERAGE.md — table-free API-coverage declaration"
    - ".planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-01-all-features-sweep.txt — verbatim sweep capture"
  key_links:
    - "The `toml` crate is a regular dependency of the root package at `Cargo.toml:180`, and `tests/cli_isolation_test.rs` is a `[[test]]` target of that same package (`Cargo.toml:370-372`), so the manifest parse compiles without any dependency change."
    - "`36.1-EVIDENCE.md`'s closure table is the artifact `36.1-VERIFICATION.md` and Phase 37 verify this phase by; plan 36.1-13 fills the remaining sixteen lines and this plan fixes its shape."
    - "The ledger row for this register entry is deliberately NOT minted here — D-18 requires every `fixed`-destined row to be appended after its fixing commit exists so the commit SHA can ride in the row description, which makes all ledger mutation plan 36.1-13's work."
  prohibitions:
    - statement: "No `.rs` file outside the D-27 list is modified — this plan touches `tests/cli_isolation_test.rs` only."
      status: recalled
      verification: flagged-unverified
    - statement: "No private item is widened to `pub` anywhere in this phase; the only visibility change is the `pub(crate)` widening in plan 36.1-02."
      status: recalled
      verification: flagged-unverified
    - statement: "`.planning/WINDOWS.md` is never edited by hand and no row is ever deleted — rows move only through the ledger CLI (D-00b)."
      status: recalled
      verification: flagged-unverified
    - statement: "The other eight tests in `tests/cli_isolation_test.rs` are not modified, renamed or removed (D-15)."
      status: recalled
      verification: flagged-unverified
    - statement: "The executor never runs `git push` — the branch push is the maintainer's action at the plan 36.1-14 checkpoint."
      status: recalled
      verification: flagged-unverified
---

<objective>
Prove the whole phase's chain on one register entry before the fan-out: take the Phase 31/32
registers' `cli_isolation` entry from defect, through a fix in the tree, through a narrow
automated verification, into the closure record that `36.1-VERIFICATION.md` and Phase 37 read —
then seed the two phase-level files every later plan depends on.

This is the tracer slice. It touches every layer this phase modifies (a test in the tree, a
verification command, the evidence record, and the ledger disposition path) on exactly one item,
so an architectural dead end in the fix-evidence-ledger chain surfaces after one commit rather
than after twelve. The ledger mutation itself is deliberately the one link left to plan 36.1-13:
`gsd-tools windows fixed` takes no reason argument, so D-18 requires the row to be appended after
its fixing commit exists with the SHA already in the description.

Decisions honoured: D-15 (rewrite the guard to assert the manifest property its doc comment names,
via the `toml` crate, keeping the panic message's intent and leaving the other eight tests
untouched), D-16 (CI is not changed for SC3; the closing evidence records the `--all-features`
sweep, and any other failing target is waived with a v0.11.0 owner rather than fixed here), D-20
(the closure table lives in `36.1-EVIDENCE.md`, one line per register entry and per todo), D-18
(why no ledger row is minted in this plan), D-28 (`fix(36.1):` for the test rewrite, `docs(36.1):`
for the evidence and coverage files).

Purpose: ROADMAP SC3 asks that the carried failure "stops being re-logged". A guard that reads the
build's own `cfg` cannot distinguish "`cli` is in `default`" from "`cli` was forced on by
`--all-features`" — which is exactly why the same failure was logged by three consecutive phases.
Reading the manifest asserts the invariant the test's own name claims.

Output: a green `cli_isolation` target under both invocations, a seeded closure table, a
table-free coverage declaration, and a verbatim record of the full `--all-features` sweep.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/36.1-deferred-items-closure/36.1-CONTEXT.md
@.planning/phases/36.1-deferred-items-closure/36.1-RESEARCH.md
@.planning/phases/36.1-deferred-items-closure/36.1-PATTERNS.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end — the cli_isolation register entry from defect to closure record, one entry only</name>
  <files>tests/cli_isolation_test.rs, .planning/phases/36.1-deferred-items-closure/36.1-EVIDENCE.md</files>
  <read_first>
    - tests/cli_isolation_test.rs — the whole file: the `//!` header at lines 1-12, the eight tests that stay untouched, and `test_cli_feature_is_not_default` at line 144 with its current doc comment and panic message
    - Cargo.toml lines 170-190 (the `toml` dependency declaration), lines 360-380 (the `[[test]]` target list confirming `cli_isolation` is a root-package test target), and the `[features]` block (the `default` array whose declared value is the thing under assertion)
    - .planning/phases/36.1-deferred-items-closure/36.1-RESEARCH.md, the "D-15: manifest-parse sketch" subsection — the exact parse shape, and "State of the Art" for why the assertion changes subject
    - .planning/phases/36.1-deferred-items-closure/36.1-PATTERNS.md, the `tests/cli_isolation_test.rs` section — the one genuinely new pattern in this phase, fully specified there
    - .planning/phases/31-lossless-token-accounting/deferred-items.md (the Plan 31-01 entry) and .planning/phases/32-unified-token-primitives/deferred-items.md — the register text this line of the closure table cites, and the two fix options D-15 chose between
    - .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-EVIDENCE.md — the house `NN-EVIDENCE.md` shape: header, sections, and how a row cites a capture file rather than pasting output
  </read_first>
  <behavior>
    - The guard passes when the root manifest's `[features] default` array does not contain the `cli` entry, under every feature-set invocation including `--all-features`.
    - The guard fails with a message naming the parsed default list when `cli` is present in that array.
    - The guard fails with a readable message when the manifest is unreadable or is not valid TOML, rather than silently passing.
    - The other eight tests in the file behave exactly as before.
  </behavior>
  <action>
Rewrite `test_cli_feature_is_not_default` (currently at line 144) so it asserts the manifest
property its own name claims, and record the entry's closure in the same task.

**The rewrite.** Read the root manifest through `concat!(env!("CARGO_MANIFEST_DIR"), "/Cargo.toml")`
with `std::fs::read_to_string`, parse it into a `toml::Value`, walk `features` then `default`, take
it as an array, collect the string entries, and assert that the collected list does not contain the
entry named `cli`. The `toml` crate is already a regular dependency at `Cargo.toml:180` — do not add
a dev-dependency and do not hand-roll a line scanner over TOML syntax. The current declared value is
`default = ["llm-openai", "llm-anthropic", "llm-deepseek"]`, so the assertion passes today. Keep the
panic message's current first sentence and append the parsed default list to it, so a future failure
prints what it saw. Keep the function name, keep its position in the file, and leave the other eight
tests byte-identical. Remove the compile-time feature-configuration branch entirely — it is the
defect, not a fallback to keep alongside.

**The header.** Add two sentences to the file's `//!` block (lines 1-12) explaining why the guard
reads the manifest rather than the build's own feature configuration: a compile-time feature check
cannot distinguish a feature that is declared in the default set from a feature that was switched on
by a whole-workspace feature-union invocation, and that indistinguishability is what made three
consecutive phases re-log the same failure. Keep the existing header sentences and the existing
runnable command block.

**The closure record.** Create `36.1-EVIDENCE.md` in the house `NN-EVIDENCE.md` shape (header naming
the phase, the date, and what the file is for; then the closure table; then a measurement section
that later plans extend). The closure table has one line per register entry and per todo (D-20),
with columns: register, entry, disposition, ledger row, closing commit, verification command,
output anchor. Seed all seventeen lines with their register and entry text filled in and the
remaining cells marked pending, in this order: (1) Phase 31 register — the `cli_isolation`
`--all-features` failure, which also appears in the Phase 32 register; (2) Phase 31 register — the
sixteen pre-existing rustdoc warnings; (3) Phase 32 register — the `HeuristicTokenCounter`
intra-doc link; (4) Phase 34 register plan 34-04 Task 2 — the Docker-machine coverage walk;
(5) Phase 34 register plan 34-07 Task 2 — the public-API examples script's scope drift, its
nineteen missing items and its unwired gate; (6) Phase 34 register plan 34-08 Task 1 — the CI
examples-count comment; (7) Phase 34 register plan 34-09 Task 2 item 1 — the PROJECT.md
crate-examples claim; (8) Phase 34 register plan 34-09 Task 2 item 2 — the RustFS evaluation;
(9) Phase 35 register plan 35-01 Task 3 item 1 — the contributing-providers import path at lines
272 and 367; (10) Phase 35 register plan 35-01 Task 3 item 2 — the standing rule for Phase 36
pointers; (11) Phase 35 register plan 35-04 — the adr-index observation resolved by commit
`64a44c51`; (12) Phase 35 register plan 35-06 — the testing-guide fixture-path claim at line 96;
(13) Phase 35 register plan 35-07 — the cli-configuration Garrison and Arsenal troubleshooting
entries; (14) Phase 35 register plan 35-09 — the battalion-patterns-guide bodies and the adapter
casing defect; (15) Phase 35 register security audit of 2026-09-17 — the cicd deployment and
best-practice YAML; (16) Phase 36 register plan 36-12 — the eight crate-level rustdoc
suppressions; (17) the ledger's own row 38 — the tool-error reason. Then complete line 1 in this
same task: disposition `fixed`, ledger row `pending — minted in 36.1-13 per D-18`, the closing
commit SHA of this task's own commit, the verification command
`cargo test --test cli_isolation --all-features`, and its observed `9 passed` result. State in the
file's header that every other line is filled by plan 36.1-13 and that no line may be deleted.

Commit the test rewrite and the evidence seed as two commits per D-28:
`fix(36.1): assert the manifest default feature set in the cli isolation guard (D-15)` and
`docs(36.1): seed the phase closure table (D-20)`. Stage with explicit paths, never `git add .`.
Use plain `git commit` with a 600-second timeout — the `cargo-clippy` pre-commit hook runs on every
commit and takes minutes. Do not use the `gsd-tools` commit helper for the `.rs` commit.
  </action>
  <verify>
    <automated>cargo test --test cli_isolation --all-features 2>&amp;1 | grep -E '^test result: ok\. 9 passed' &amp;&amp; cargo test --test cli_isolation 2>&amp;1 | grep -E '^test result: ok\. 9 passed' &amp;&amp; grep -c 'cli_isolation' .planning/phases/36.1-deferred-items-closure/36.1-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test cli_isolation --all-features` prints a line matching `^test result: ok. 9 passed` and no line matching `FAILED`.
    - `cargo test --test cli_isolation` (default features) prints a line matching `^test result: ok. 9 passed`.
    - `grep -c '#\[test\]' tests/cli_isolation_test.rs` is 9 — no test added, none removed.
    - `tests/cli_isolation_test.rs` contains `CARGO_MANIFEST_DIR` and `toml::Value`, and `grep -c 'cfg(feature = "cli")' tests/cli_isolation_test.rs` is 0.
    - The file's `//!` header is at least two lines longer than before and names the manifest as the thing the guard reads.
    - `.planning/phases/36.1-deferred-items-closure/36.1-EVIDENCE.md` exists and contains the strings `34-07`, `34-08`, `34-09`, `35-01`, `35-06`, `35-07`, `35-09`, `36-12` and `row 38`.
    - The closure table's first line carries a non-placeholder 7-or-more-character commit SHA and the text `9 passed`.
    - `git diff --stat HEAD~2 -- src/ crates/ docs/ Makefile .github/` is empty.
  </acceptance_criteria>
  <done>The `cli_isolation` register entry is closed in the tree, proven by a narrow automated command under both feature invocations, and recorded in the closure table with its commit — the same chain every remaining entry in this phase follows.</done>
</task>

<task type="auto">
  <name>Task 2: Capture the all-features sweep baseline and write the API-coverage declaration</name>
  <files>.planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-01-all-features-sweep.txt, .planning/phases/36.1-deferred-items-closure/COVERAGE.md</files>
  <read_first>
    - .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/COVERAGE.md — the exact declaration form to copy: one heading and one prose line, no table anywhere in the file
    - .planning/phases/36.1-deferred-items-closure/36.1-RESEARCH.md, the "Assumptions Log" row A2 — the claim this capture tests, and D-16's fallback if it is wrong
    - .planning/phases/36.1-deferred-items-closure/36.1-CONTEXT.md, decisions D-16 and D-25 — what the sweep is evidence for and where it sits in the closing gate order
    - .planning/phases/36-rustdoc-zero-warning-bar-examples-currency/36-evidence/ — the house shape for a verbatim capture file under an `NN-evidence/` directory
  </read_first>
  <action>
**The sweep.** Run `cargo test --workspace --all-features --no-fail-fast` on this plan's head commit
and tee the complete output to
`.planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-01-all-features-sweep.txt`. This is
a long run — expect well beyond a single tool-call timeout, so run it in the background or under an
explicit long timeout and wait for it; do not shorten it with a filter, and do not drop
`--no-fail-fast` (the whole point is to see every failing target, not the first). Prepend the
capture with the commit SHA it ran on and the exact command line.

Then read the capture and write a short findings block at the top of the file: the number of test
targets run, the number that failed, and — for each failing target other than `cli_isolation` — its
name, the failing test's name, and one line on what it asserts. RESEARCH assumption A2 predicts zero
such targets. If any exist, do NOT fix them and do NOT widen this phase: record each one as a
finding addressed to plan 36.1-13, which per D-16 mints it its own ledger row of kind
`skipped-test` or `unmet-truth` and waives it with the v0.11.0 milestone as owner. If the run cannot
complete in this environment at all, say so plainly in the capture with the failure mode rather than
recording a green.

**The declaration.** `COVERAGE.md` was written in the phase directory at plan time, carrying a single
heading and one prose line beginning `No external API integration:` with its reason. Read it and
confirm three things: that it is still accurate against this phase's actual scope, that it contains
no pipe-delimited row anywhere, and that its heading line is the only heading. It must stay
table-free because the verification seal gate parses any pipe table in this file as matrix rows and
then blocks acceptance; any note you add goes in as prose or a hyphen bullet. Correct the file only
if the phase's scope has genuinely changed — do not rewrite a correct declaration.

Commit both as `docs(36.1): record the all-features sweep baseline and the API coverage declaration`.
Stage with explicit paths.
  </action>
  <verify>
    <automated>test -s .planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-01-all-features-sweep.txt &amp;&amp; grep -q '^# API Coverage — Phase 36.1$' .planning/phases/36.1-deferred-items-closure/COVERAGE.md &amp;&amp; ! grep -q '|' .planning/phases/36.1-deferred-items-closure/COVERAGE.md &amp;&amp; grep -q 'No external API integration:' .planning/phases/36.1-deferred-items-closure/COVERAGE.md</automated>
  </verify>
  <acceptance_criteria>
    - The sweep capture file is non-empty, begins with the commit SHA and the exact command line, and contains at least one line matching `test result:`.
    - The capture's findings block states a failing-target count and, if that count is greater than one, names every failing target other than `cli_isolation` with the text `finding for 36.1-13`.
    - `COVERAGE.md` contains exactly one line starting with `# ` and zero lines containing a pipe character.
    - `COVERAGE.md`'s declaration line names documentation, the private match arm, the test, the gate wiring and the planning corpus as the phase's scope.
    - `git diff --stat HEAD~1 -- src/ crates/ tests/ docs/` is empty.
  </acceptance_criteria>
  <done>The phase knows, in wave 1 rather than wave 6, whether any target other than `cli_isolation` fails under the full feature union, and the coverage declaration the seal gate reads is on disk in a form that gate can parse.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Created by **this plan**:
- `.planning/phases/36.1-deferred-items-closure/36.1-EVIDENCE.md` (new file)
- `.planning/phases/36.1-deferred-items-closure/COVERAGE.md` (new file)
- `.planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-01-all-features-sweep.txt` (new file)
- The rewritten body of `tests/cli_isolation_test.rs::test_cli_feature_is_not_default` (existing
  function, new implementation — no new symbol name)

Created elsewhere in Phase 36.1 (listed so drift verification does not flag them as unexplained new
symbols): the `pub(crate)` visibility of `ToolResultFormatter::sanitize_tool_text` and the pinning
test `fail_run_redacts_a_secret_in_the_reason` (plan 36.1-02); nineteen new `# Examples` rustdoc
sections on `RunTracePort`, `NodeCachePort`, `RunRepositoryPort`, `AssistantRepositoryPort`,
`RunQueuePort`, `WebhookDeliveryRepositoryPort`, `RunScheduleRepositoryPort`, `WaypointPort`,
`StructuredExecutorPort`, `AssistantAdminPort`, `RunSubmissionPort`, `ScheduleAdminPort`,
`ScheduleService`, `WebhookDeliveryService`, `RunInspectorService`, `RunEventStreamService`,
`RunSubmissionService`, `AssistantService` and `WaypointRetentionService` (plans 36.1-05 through
36.1-11); the `make check-api-examples` target, the `.pre-commit-config.yaml` hook id
`check-api-examples` and the CI lint step named "Check public-API `# Examples` headings" (plan
36.1-11); the files `36.1-ENTRY-POINTS.md` (36.1-11) and `36.1-CI-EVIDENCE.md` (36.1-14);
requirement ids `CURR-16` through `CURR-21` (36.1-12); `WINDOWS.md` rows 39 through 50 (36.1-13).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build configuration → test assertion | the guard's subject changes from the compiled feature set to a file read at test time |
| repository file → test process | the test now reads a path derived from a build-time environment variable |
| this phase's record → downstream phases | `36.1-VERIFICATION.md` and Phase 37 verify this phase by the closure table alone |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36.1-01 | Tampering | `tests/cli_isolation_test.rs` guard | medium | mitigate | A guard rewritten to stop failing could be rewritten into one that can no longer fail at all. The behavior block requires the guard to fail when the `cli` entry is present in the declared default array, and the acceptance criteria require the compile-time feature branch to be gone rather than merely bypassed, so the invariant is still enforced under every invocation (D-15). |
| T-36.1-02 | Information Disclosure | manifest read path | low | accept | The path is `CARGO_MANIFEST_DIR` joined to a fixed filename, resolved at compile time from the crate being built; it takes no runtime input and reads a file already public in the repository. |
| T-36.1-03 | Repudiation | the seeded closure table | high | mitigate | A register entry omitted from the table breaks the verification chain two phases downstream. All seventeen lines are seeded here with their entry text before any disposition is known, and a prohibition forbids deleting a line, so a missing disposition is visible as a pending cell rather than as an absence (D-20). |
| T-36.1-04 | Spoofing | the all-features sweep capture | medium | mitigate | A filtered or fail-fast run would report a green that the full sweep does not support. The action forbids both, requires the commit SHA and command line in the capture, and requires an incomplete run to be recorded as incomplete rather than as a pass (D-16, assumption A2). |
| T-36.1-SC | Tampering | cargo/npm package installs | low | accept | This plan installs no package. The `toml` crate it uses is already a resolved regular dependency of the root package at `Cargo.toml:180`; the workspace lockfile does not change. |
</threat_model>

<verification>
- `cargo test --test cli_isolation --all-features` and `cargo test --test cli_isolation` each report 9 passed, 0 failed.
- `tests/cli_isolation_test.rs` still declares exactly nine tests and no longer branches on the compile-time `cli` feature.
- `36.1-EVIDENCE.md` carries all seventeen closure lines, with line 1 complete and the rest pending.
- `COVERAGE.md` exists, has one heading, one declaration line, and no pipe character.
- The all-features sweep capture is on disk with its command line, its SHA and a findings block.
- No file under `src/`, `crates/`, `docs/`, `Makefile` or `.github/` is modified by this plan.
- The plan's SUMMARY carries a `## Self-Check` section.
</verification>

<success_criteria>
- ROADMAP SC3 is closed in the tree: the carried `cli_isolation` failure is gone under the full feature union and cannot silently return.
- The fix-to-record chain every later plan repeats is proven once, end to end, on a single register entry.
- The phase's two shared files — the closure table and the coverage declaration — exist in the shape the closing plan and the seal gate require.
</success_criteria>

<output>
Create `.planning/phases/36.1-deferred-items-closure/36.1-01-SUMMARY.md` when done, recording: the two commit SHAs, the observed `test result` lines for both `cli_isolation` invocations, the seventeen seeded closure lines with line 1's completed cells, the all-features sweep's failing-target count with any non-`cli_isolation` target named as a finding for plan 36.1-13, and a `## Self-Check` section.
</output>
