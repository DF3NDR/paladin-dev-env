---
phase: 36.1-deferred-items-closure
plan: 01
subsystem: testing
tags: [cargo, toml, rustdoc, ledger, closure-table, all-features]

# Dependency graph
requires: []
provides:
  - "cli_isolation register entry closed in the tree: test_cli_feature_is_not_default reads
    Cargo.toml's [features] default array instead of the build's own compile-time feature
    configuration, so it gives the same answer under every invocation, including --all-features"
  - "36.1-EVIDENCE.md seeded with all seventeen closure-table lines (register + entry text),
    line 1 complete with disposition, commit, verification command and output anchor"
  - "COVERAGE.md verified accurate (no edit needed) against the plan's table-free-declaration
    requirement"
  - "Full cargo test --workspace --all-features --no-fail-fast sweep captured verbatim, proving
    zero non-cli_isolation failing targets (RESEARCH.md assumption A2 confirmed)"
affects: [36.1-13-closing-plan]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Manifest-property assertion in an integration test via the toml crate (already a regular
      dependency) instead of reading the build's own cfg — distinguishes a feature genuinely
      listed in [features] default from a feature forced on by --all-features"
    - "Closure-table seeding: every register/ledger line is written before any disposition is
      known, so a missing disposition shows as a pending cell rather than an absent row"

key-files:
  created:
    - .planning/phases/36.1-deferred-items-closure/36.1-EVIDENCE.md
    - .planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-01-all-features-sweep.txt
  modified:
    - tests/cli_isolation_test.rs

key-decisions:
  - "D-15 rewrite: test_cli_feature_is_not_default reads CARGO_MANIFEST_DIR/Cargo.toml via toml::Value and asserts features.default excludes \"cli\", replacing the #[cfg(feature = \"cli\")] branch entirely rather than skipping it under --all-features"
  - "D-20 closure table: 36.1-EVIDENCE.md seeded with all 17 lines' register/entry text in wave 1; only line 1 (cli_isolation) is completed here, the other 16 stay pending for plan 36.1-13 per D-18"
  - "D-16 sweep: cargo test --workspace --all-features --no-fail-fast captured verbatim at commit a90c5f0d — 59 targets, 65 test result lines, 0 failed, confirming RESEARCH.md assumption A2 with no fallback needed"

patterns-established:
  - "House NN-EVIDENCE.md closure-table shape for Phase 36.1, following 35-EVIDENCE.md/36-EVIDENCE.md"

requirements-completed: [CURR-18, CURR-19]

coverage:
  - id: D1
    description: "cli_isolation guard rewritten to assert Cargo.toml's declared [features] default array; passes 9/9 under both cargo test --test cli_isolation --all-features and cargo test --test cli_isolation"
    requirement: "CURR-18"
    verification:
      - kind: unit
        ref: "tests/cli_isolation_test.rs#test_cli_feature_is_not_default (cargo test --test cli_isolation --all-features)"
        status: pass
      - kind: unit
        ref: "tests/cli_isolation_test.rs#test_cli_feature_is_not_default (cargo test --test cli_isolation)"
        status: pass
    human_judgment: false
  - id: D2
    description: "36.1-EVIDENCE.md closure table seeded with all seventeen register/ledger lines; line 1 (cli_isolation) fully completed with disposition, ledger-row placeholder, closing commit and verification command"
    requirement: "CURR-19"
    verification:
      - kind: other
        ref: "grep -c 'cli_isolation' .planning/phases/36.1-deferred-items-closure/36.1-EVIDENCE.md; grep for 34-07/34-08/34-09/35-01/35-06/35-07/35-09/36-12/'row 38'"
        status: pass
    human_judgment: false
  - id: D3
    description: "COVERAGE.md read and confirmed accurate (table-free API-coverage declaration); no edit needed"
    verification:
      - kind: other
        ref: "grep -q '^# API Coverage — Phase 36.1$' COVERAGE.md; ! grep -q '|' COVERAGE.md"
        status: pass
    human_judgment: false
  - id: D4
    description: "cargo test --workspace --all-features --no-fail-fast sweep captured verbatim with commit SHA, command line and findings block; zero non-cli_isolation failing targets"
    verification:
      - kind: integration
        ref: ".planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-01-all-features-sweep.txt"
        status: pass
    human_judgment: false

duration: 20min
completed: 2026-09-18
status: complete
---

# Phase 36.1 Plan 01: Tracer — cli_isolation Closure Chain Summary

**Rewrote the `cli_isolation` guard to assert `Cargo.toml`'s declared `[features] default` array via the `toml` crate instead of the build's own `cfg`, closing the three-phase-old `--all-features` false failure, and seeded the phase's two shared bookkeeping files (`36.1-EVIDENCE.md` closure table, `COVERAGE.md` declaration) plus a verbatim full-workspace `--all-features` sweep proving no other target fails.**

## Performance

- **Duration:** ~20 min
- **Started:** 2026-09-18T03:16:00Z (approx, file reads)
- **Completed:** 2026-09-18T03:36:00Z
- **Tasks:** 2 completed
- **Files modified:** 3 (1 rewritten test, 1 new evidence file, 1 new sweep capture)

## Accomplishments

- `tests/cli_isolation_test.rs::test_cli_feature_is_not_default` now reads the root manifest
  through `concat!(env!("CARGO_MANIFEST_DIR"), "/Cargo.toml")`, parses it with `toml::Value`,
  and asserts `features.default` does not contain `"cli"` — removing the compile-time
  `#[cfg(feature = "cli")]` branch that could not distinguish "genuinely in `default`" from
  "forced on by `--all-features`". Both invocations now report `9 passed, 0 failed`:
  - `cargo test --test cli_isolation --all-features` → `test result: ok. 9 passed; 0 failed`
  - `cargo test --test cli_isolation` → `test result: ok. 9 passed; 0 failed`
- `.planning/phases/36.1-deferred-items-closure/36.1-EVIDENCE.md` created with the full
  seventeen-line closure table (register + entry text for every line); line 1 (the
  `cli_isolation` entry, appearing in both the Phase 31 and Phase 32 registers) is fully
  completed: disposition `fixed`, closing commit `45dd7603`, verification command
  `cargo test --test cli_isolation --all-features`, output anchor `9 passed`. The remaining
  16 lines are `pending`, explicitly reserved for the closing plan 36.1-13 per D-18 (a `fixed`
  ledger row can only be minted after its commit exists, since `windows fixed <id>` takes no
  reason argument).
- `COVERAGE.md` (already on disk from planning) was read and confirmed accurate — one heading,
  zero pipe characters, and a declaration naming documentation, the private match arm, the
  test rewrite, the gate wiring and the planning corpus as the phase's scope. No edit needed.
- `cargo test --workspace --all-features --no-fail-fast` run in full on commit `a90c5f0d` and
  captured verbatim to `36.1-evidence/36.1-01-all-features-sweep.txt`: 59 test targets, 65
  `test result:` lines, **0 failed**, **0 occurrences of `FAILED`** anywhere in the 8,010-line
  capture. This confirms RESEARCH.md's Assumption A2 exactly — no target other than
  `cli_isolation` was failing under the full feature union, so no finding needed to be handed
  to plan 36.1-13 and D-16's fallback (mint a `skipped-test`/`unmet-truth` row) was not needed.

## Task Commits

Each task was committed atomically:

1. **Task 1: End-to-end — the cli_isolation register entry from defect to closure record**
   - `45dd7603` (fix) — `fix(36.1): assert the manifest default feature set in the cli isolation guard (D-15)`
   - `a90c5f0d` (docs) — `docs(36.1): seed the phase closure table (D-20)`
2. **Task 2: Capture the all-features sweep baseline and write the API-coverage declaration**
   - `cf903707` (docs) — `docs(36.1): record the all-features sweep baseline and the API coverage declaration`

**Plan metadata:** (this commit, below)

_Note: Task 1 is `type="tracer" tdd="true"` per the plan; the "tracer feedback gate" is the
`<verify>` command itself, re-run immediately after each commit (both `cli_isolation`
invocations green) before proceeding to Task 2, per the interactive/autonomous tracer
protocol._

## Files Created/Modified

- `tests/cli_isolation_test.rs` — rewrote `test_cli_feature_is_not_default` to assert the
  manifest's `[features] default` array via `toml::Value`; added two `//!` header sentences
  explaining why; kept the other eight tests byte-identical.
- `.planning/phases/36.1-deferred-items-closure/36.1-EVIDENCE.md` — new closure table, house
  `NN-EVIDENCE.md` shape, all seventeen register/ledger lines seeded.
- `.planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-01-all-features-sweep.txt` —
  new verbatim capture of the full-workspace `--all-features --no-fail-fast` sweep with a
  findings block.
- `.planning/phases/36.1-deferred-items-closure/COVERAGE.md` — read, verified accurate, not
  modified.

## Decisions Made

- Reused the exact D-15 manifest-parse sketch from `36.1-RESEARCH.md` verbatim (no deviation) —
  `toml` crate, `CARGO_MANIFEST_DIR`, `features.default` array walk.
- Kept the panic message's original first sentence and appended the parsed `default` list, per
  the plan's "Specific Ideas" guidance, so a future failure prints what it saw.
- Reworded the two new `//!`/`///` explanatory sentences to avoid the literal string
  `cfg(feature = "cli")` (used "the build's own compile-time feature configuration" instead),
  so the acceptance criterion `grep -c 'cfg(feature = "cli")' tests/cli_isolation_test.rs` is 0
  even though prose explaining the rationale is present — the guard behavior is identical
  either way, only the wording of the explanatory prose changed.
- Seeded all seventeen closure-table lines with register/entry text only (per the plan's
  explicit instruction), leaving disposition/ledger-row/commit/verification/anchor cells
  `pending` for every line except line 1 — this is not a deviation, it is the plan's own D-18
  ordering rule.

## Deviations from Plan

None — plan executed exactly as written. One small self-correction during Task 1 (the header
and doc-comment wording initially included the literal string `cfg(feature = "cli")` in
prose, which would have failed the acceptance criterion's `grep -c` check for that exact
string at 0; reworded before committing) — caught during my own verification pass, not left
as a defect, and does not rise to a Rule 1-4 deviation since no code path changed.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 36.1-01's tracer chain (fix → narrow automated verify → closure-table record) is proven
  end to end on one register entry. Plans 36.1-02 through 36.1-12 can proceed independently in
  their own waves; plan 36.1-13 (the closing plan) has its `36.1-EVIDENCE.md` skeleton and its
  `36.1-01-all-features-sweep.txt` finding ("zero non-cli_isolation failing targets") ready to
  consume when it fills the remaining sixteen closure-table lines and mints the twelve
  `WINDOWS.md` rows (D-17).
- No blockers. `git diff --stat HEAD~3 -- src/ crates/ docs/ Makefile .github/` is empty —
  this plan touched only `tests/cli_isolation_test.rs` and the two `.planning/` bookkeeping
  files, as required.

## Self-Check: PASSED

- `tests/cli_isolation_test.rs` — FOUND, modified as described.
- `.planning/phases/36.1-deferred-items-closure/36.1-EVIDENCE.md` — FOUND.
- `.planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-01-all-features-sweep.txt` — FOUND.
- Commit `45dd7603` — FOUND in `git log --oneline --all`.
- Commit `a90c5f0d` — FOUND in `git log --oneline --all`.
- Commit `cf903707` — FOUND in `git log --oneline --all`.
- `cargo test --test cli_isolation --all-features` — re-confirmed `9 passed; 0 failed`.
- `cargo test --test cli_isolation` — re-confirmed `9 passed; 0 failed`.
- `grep -c 'cfg(feature = "cli")' tests/cli_isolation_test.rs` — confirmed 0.
- `36.1-EVIDENCE.md` contains `34-07`, `34-08`, `34-09`, `35-01`, `35-06`, `35-07`, `35-09`,
  `36-12`, `row 38` — all confirmed present.

---
*Phase: 36.1-deferred-items-closure*
*Completed: 2026-09-18*
