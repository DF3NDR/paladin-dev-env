---
phase: 36.1-deferred-items-closure
plan: 03
type: execute
wave: 2
depends_on: ["36.1-01"]
files_modified:
  - docs/src/contributing/contributing-providers.md
  - docs/src/appendix/cli-configuration.md
  - docs/src/deployment/cicd.md
autonomous: true
requirements: [CURR-16]
must_haves:
  truths:
    - "`docs/src/contributing/contributing-providers.md` names the module path a new provider adapter actually lives at today, mirroring the shipped per-provider directory layout, and no longer points readers at the pre-decomposition facade path (D-04, CURR-16)."
    - "The nine-signal page check the Phase 35 register asked for has been re-run against `contributing/contributing-providers.md` and its result is recorded, including an explicit verdict on the page's module-declaration line (D-04, RESEARCH Open Question 1)."
    - "`docs/src/appendix/cli-configuration.md`'s Garrison and Arsenal troubleshooting entries point at the live configuration key and the route family the agent command calls, and no entry on any page under `docs/src` still instructs a reader to inspect a source line for an absent marker (D-06)."
    - "`docs/src/deployment/cicd.md`'s Kubernetes deployment block and all eight best-practice fragments each carry a bold caption naming the block as illustrative and linking the real release path, and the section intro says the same once (D-08)."
    - "The secret-expression references in the captioned cicd blocks are unchanged — they were verified not a disclosure under the Phase 35 security audit, and captioning a block is not a licence to rewrite it."
    - "`mdbook build docs/` with the linkcheck backend still passes on the exact documentation-workflow sequence, and `git status --porcelain -- docs` is clean afterwards (D-00f)."
  artifacts:
    - "docs/src/contributing/contributing-providers.md — corrected adapter module paths"
    - "docs/src/appendix/cli-configuration.md — rewritten Garrison and Arsenal troubleshooting entries"
    - "docs/src/deployment/cicd.md — nine illustrative captions plus one section-intro sentence"
  key_links:
    - "The live per-provider layout is `crates/paladin-llm/src/<provider>/adapter.rs` with a sibling `mod.rs` — `openai`, `anthropic`, `deepseek`, `gemini`, `kimi`, `qwen`, `grok`, `ollama`, `openai_compatible` — so the template path a contributor copies must be shaped the same way."
    - "The same cli-configuration page already contains the model rewrite for this defect class: the Scheduler troubleshooting entry near lines 446-470, rewritten to name live wiring instead of a source-line marker."
    - "No workflow under `.github/workflows/` deploys anywhere — the set is benchmarks, ci, codeql, docs, feature-flags, pre-commit and release — which is why the deployment block is captioned rather than replaced with a real excerpt."
    - "The page's own excerpt captions are the voice to invert: they name the workflow and job a block was taken from; the new captions name what the block is not and link the real release path."
  prohibitions:
    - statement: "No `.rs` file is modified by this plan — it edits three documentation pages only (D-27)."
      status: recalled
      verification: flagged-unverified
    - statement: "No new page is added under `docs/src` and no page outside the three named files is edited."
      status: recalled
      verification: flagged-unverified
    - statement: "No secret-expression reference is removed from or added to any captioned block."
      status: recalled
      verification: flagged-unverified
    - statement: "No deployment workflow is invented to replace an illustrative block — fabricating an excerpt is the failure mode Phase 35 deleted from another page."
      status: recalled
      verification: flagged-unverified
    - statement: "The domain-role vocabulary rules hold in every rewritten line: the Medieval-military term is used for a domain role, the retired quartermaster term never appears, and no bare token total is written beside a split usage figure (D-00h)."
      status: recalled
      verification: flagged-unverified
    - statement: "The executor never runs `git push`."
      status: recalled
      verification: flagged-unverified
---

<objective>
Close three of the six documentation-prose defects the Phase 35 register deferred: the contributor
guide's adapter module path, the CLI appendix's two troubleshooting entries that tell a reader to go
look for a marker that is not in the source, and the CI/CD page's uncaptioned deployment YAML.

Decisions honoured: D-00a (the shipped tree outranks the page — every correction moves the page
toward the tree, never the reverse), D-00e (the relocated-adapter fix shape and the fence rule:
complete flows become compiled anchors, fragments are corrected and fenced as non-compiling, and any
appendix sample is compile-proven with the throwaway scratch-example probe that is never committed),
D-00f (the documentation gate is the exact documentation-workflow sequence, and the docs tree is
clean afterwards), D-00h (the vocabulary rules hold in every rewritten line), D-02 (the live-verified
defect list this plan works from), D-04 (contributing-providers lines 272 and 367, plus the
nine-signal re-run and its verdict on the module-declaration line), D-06 (the two cli-configuration
entries rewritten the way the Scheduler entry on the same page was rewritten), D-08 (the CI/CD
blocks are captioned illustrative, not replaced, and the secret expressions stay), D-28
(`docs(36.1):` subjects).

Purpose: each of these three is a page telling a contributor something the tree contradicts. A
contributor following the adapter path writes a module in a directory that no longer exists; a
maintainer following the troubleshooting entry greps for a marker that was removed; a reader of the
CI/CD page believes this repository deploys to Kubernetes. All three ship in the v0.10.0 book.

Output: three corrected pages, a recorded nine-signal result, and a clean documentation build.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/36.1-deferred-items-closure/36.1-CONTEXT.md
@.planning/phases/36.1-deferred-items-closure/36.1-RESEARCH.md
@.planning/phases/36.1-deferred-items-closure/36.1-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct the provider-adapter module path in the contributor guide and re-run the nine-signal page check</name>
  <files>docs/src/contributing/contributing-providers.md</files>
  <read_first>
    - docs/src/contributing/contributing-providers.md in full — at minimum the directory-layout statement near line 40, the module-declaration instruction near line 233, and the two import statements at lines 272 and 367, so the page's path claims can be made to agree with each other as well as with the tree
    - crates/paladin-llm/src/openai/ and crates/paladin-llm/src/anthropic/ — the shipped per-provider directory shape (a `mod.rs` beside an `adapter.rs`) the template must mirror
    - crates/paladin-llm/src/lib.rs — how the provider modules are actually declared and re-exported, which settles what a contributor's own declaration line should say
    - .planning/phases/35-mdbook-currency/deferred-items.md, the Plan 35-01 Task 3 item 1 entry — the register text, its proposed classification and the nine-signal re-run it asks for
    - .planning/phases/34-documentation-currency-audit/34-signals.sh — the check to run, and how to pass it a single page
    - .planning/phases/36.1-deferred-items-closure/36.1-RESEARCH.md, Open Question 1 — the module-declaration line's status and the explicit instruction to record a verdict on it either way
    - .planning/phases/35-mdbook-currency/35-CONTEXT.md decisions D-11 and D-13 — the fence rule and the relocated-adapter fix shape this correction inherits
  </read_first>
  <action>
**Run the check first.** Execute the Phase 34 nine-signal page script against this one page
(`bash .planning/phases/34-documentation-currency-audit/34-signals.sh contributing/contributing-providers.md`)
and capture its full output to
`.planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-03-signals-contributing-providers.txt`.
Read the result before editing: it tells you which of the nine signal classes actually reproduce on
this page.

**Fix the two import lines.** Lines 272 and 367 import a hypothetical provider adapter from the
pre-decomposition facade module tree. Correct both to the path a new provider adapter would live at
today: the `paladin_llm` crate, a provider-named module, and the adapter type inside it — for the
page's hypothetical provider that is `paladin_llm::myprovider::MyProviderAdapter`, mirroring the
shipped `openai`, `anthropic`, `deepseek`, `gemini`, `kimi`, `qwen`, `grok`, `ollama` and
`openai_compatible` modules, each of which is a directory holding `mod.rs` and `adapter.rs`. Use
the capitalisation the shipped adapter types use. Apply the inherited fence rule to any block you
touch: a fragment stays a fragment and is fenced so it is not compiled; a complete flow becomes a
compiled anchor; an appendix-style sample is compile-proven with the throwaway scratch-example probe
and the probe file is never committed.

**Re-read the surrounding prose.** The page states a directory layout near line 40 and a module
declaration near line 233. Make every path claim on the page agree with the two corrected imports
and with the tree. The module-declaration line names a module after the adapter file rather than
after the provider directory; decide it on the signal script's own class-4 result, not on taste — if
the script reproduces it as the same relocated-path class, correct it in this same commit; if it
does not, leave it and record the decision. Either way the SUMMARY must state the verdict
explicitly and name the line — this is a flagged open question, and leaving it silently unaddressed
is the one unacceptable outcome. Do not mint a ledger row for it: the twelve-row mapping is fixed
and has no slot for it.

**Check the vocabulary.** Every line you rewrite uses the Medieval-military term for a domain role,
never the retired quartermaster term, and never places a bare token total beside a split usage
figure.

Commit as `docs(36.1): point the provider guide at the shipped adapter module layout (D-04)`.
Stage the page and the signals capture by explicit path.
  </action>
  <verify>
    <automated>test "$(grep -c 'paladin::infrastructure::adapters' docs/src/contributing/contributing-providers.md)" = 0 &amp;&amp; grep -q 'paladin_llm::myprovider' docs/src/contributing/contributing-providers.md &amp;&amp; test -s .planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-03-signals-contributing-providers.txt</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'paladin::infrastructure::adapters' docs/src/contributing/contributing-providers.md` returns nothing.
    - The page contains `paladin_llm::myprovider` at both former defect locations, and the adapter type name uses the same capitalisation as the shipped adapter types.
    - The signals capture file exists, is non-empty, and names the page it ran against.
    - The SUMMARY states the verdict on the module-declaration line near 233 — corrected in this commit, or left with the signal-class reason — and names the line number.
    - `git diff --stat HEAD~1 -- src/ crates/ tests/ examples/` is empty.
    - No scratch example file is present in the working tree or in the commit.
  </acceptance_criteria>
  <done>A contributor following this page writes their adapter where the tree actually keeps adapters, and the register's requested nine-signal re-run is on disk with a recorded verdict on every class it reproduced.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the Garrison and Arsenal troubleshooting entries to name live wiring</name>
  <files>docs/src/appendix/cli-configuration.md</files>
  <read_first>
    - docs/src/appendix/cli-configuration.md lines 400-475 — the Garrison entry at 409, the Arsenal entry at 422, and the Scheduler entry near 446-470 that is the model rewrite: read all three together so the new entries match the model's structure, length and voice
    - src/application/cli/commands/agent.rs — what the agent command actually does for memory and tool configuration: the configuration keys it reads and the route family or service it calls, which is what the rewritten entries must name
    - .planning/phases/35-mdbook-currency/deferred-items.md, the Plan 35-07 entry — the register text and its proposed classification
    - .planning/phases/36.1-deferred-items-closure/36.1-PATTERNS.md, the cli-configuration section — the analog and the exit check
  </read_first>
  <action>
Rewrite the Garrison troubleshooting entry (line 409) and the Arsenal troubleshooting entry (line
422) so each points a reader at the live wiring the agent command performs — the configuration key
it reads and the route family or service it calls — instead of instructing them to confirm the
absence of a marker at a numbered source line. Those source lines carry no such marker; the
instruction has been unfollowable since the marker was removed.

Use the Scheduler entry on the same page (near 446-470) as the structural model: it names the key,
names what reads it, and tells the reader what to check when the behaviour is wrong. Read
`src/application/cli/commands/agent.rs` first and name the real key and the real call path — do not
paraphrase from the old entry, which is the thing being replaced. Keep each entry to roughly the
Scheduler entry's length; this is a troubleshooting appendix, not a design document.

Afterwards, confirm that no page anywhere under `docs/src` still carries the source-line marker
instruction — the two entries here are the only known occurrences, and the exit condition is a
tree-wide absence, not a per-page one.

Check the vocabulary rules on every rewritten line as in Task 1.

Commit as `docs(36.1): rewrite the Garrison and Arsenal troubleshooting entries against live wiring (D-06)`.
Stage the page by explicit path.
  </action>
  <verify>
    <automated>test "$(grep -rn 'verify no TODO' docs/src | wc -l)" = 0 &amp;&amp; grep -q 'Garrison' docs/src/appendix/cli-configuration.md &amp;&amp; grep -q 'Arsenal' docs/src/appendix/cli-configuration.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'verify no TODO' docs/src` returns nothing, across the whole documentation source tree.
    - Neither rewritten entry references a numbered source line as a verification step.
    - Each rewritten entry names a configuration key and the route family or service the agent command calls, both taken from `src/application/cli/commands/agent.rs` as it reads today.
    - The Scheduler entry near lines 446-470 is unmodified — `git diff docs/src/appendix/cli-configuration.md` shows changes only within the Garrison and Arsenal entries.
    - `git diff --stat HEAD~1 -- src/ crates/ tests/` is empty.
  </acceptance_criteria>
  <done>Both troubleshooting entries tell a reader something they can act on against the shipped CLI, and the unfollowable instruction is gone from the whole book.</done>
</task>

<task type="auto">
  <name>Task 3: Caption the CI/CD deployment and best-practice YAML as illustrative</name>
  <files>docs/src/deployment/cicd.md</files>
  <read_first>
    - docs/src/deployment/cicd.md lines 260-430 — the Deployment Automation section intro, the "Deploy to Kubernetes" block at 282-330, and the eight "Best Practices" blocks at 338-349, 355-360, 364-375, 379-384, 388-395, 399-405, 409-415 and 419-426
    - docs/src/deployment/cicd.md lines 85-110 and 180-245 — the page's own existing excerpt captions, whose voice the new captions invert
    - .github/workflows/ — the seven workflows that actually ship (benchmarks, ci, codeql, docs, feature-flags, pre-commit, release), confirming no deployment workflow exists to excerpt
    - .planning/phases/35-mdbook-currency/deferred-items.md, the security-audit entry of 2026-09-17 — the register text and the finding that the secret-expression references are not a disclosure
    - .planning/phases/36.1-deferred-items-closure/36.1-PATTERNS.md, the cicd section — the caption form and the link target
  </read_first>
  <action>
Add one bold caption line immediately above each of the nine YAML blocks in the Deployment
Automation and Best Practices sections: the Kubernetes deployment block and the eight best-practice
fragments. Each caption begins with the words `**Illustrative only` and, in the inverted voice of
the page's own excerpt captions, states that no workflow of that shape ships in this repository and
links the real release path — the release workflow's own section anchor on this page. Vary only what
is needed to make each caption read naturally above its own block; the leading phrase is identical
so a reader scanning the section sees the same signal every time.

Add one sentence to the Deployment Automation section intro saying the same thing once, in prose, so
a reader who skips the blocks still learns it.

Do not replace any block with a real excerpt: no workflow under `.github/workflows/` deploys
anywhere, and inventing one would be the fabrication Phase 35 deleted from another page. Do not
modify any line inside a block — in particular, leave every secret-expression reference exactly as
it is; the Phase 35 security audit verified them as not a disclosure, and a caption is not a licence
to edit the body. Add no new claim to the page's Overview.

Then run the documentation gate end to end on the three pages this plan touched: install the
mermaid preprocessor into the book, build the book with the linkcheck backend in error mode, run the
documentation-example check script and the documentation-config check script, and confirm
`git status --porcelain -- docs` is clean afterwards; if the build leaves artifacts, restore with
`git checkout -- docs/` and record that it did.

Commit as `docs(36.1): caption the illustrative deployment and best-practice YAML (D-08)`.
Stage the page by explicit path.
  </action>
  <verify>
    <automated>test "$(grep -c 'Illustrative only' docs/src/deployment/cicd.md)" -ge 9 &amp;&amp; test "$(grep -c 'release.yml' docs/src/deployment/cicd.md)" -ge 9</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Illustrative only' docs/src/deployment/cicd.md` is 9 or greater — one per block, plus the section-intro sentence.
    - Each of the nine YAML blocks in the Deployment Automation and Best Practices sections has a bold caption line immediately above its opening fence.
    - Every caption links the release path, and the link resolves under the linkcheck build.
    - `git diff docs/src/deployment/cicd.md` shows no change to any line inside a fenced block — the diff is caption lines and the one intro sentence only.
    - The count of secret-expression references in the file is unchanged from before this task.
    - `mdbook build docs/` with the linkcheck backend exits 0 and `git status --porcelain -- docs` prints nothing afterwards.
    - `git diff --stat HEAD~1 -- src/ crates/ tests/ .github/` is empty.
  </acceptance_criteria>
  <done>A reader of the CI/CD page can tell at a glance which YAML this repository actually runs and which is teaching material, and the book still builds with zero broken links.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Created by **this plan**:
- `.planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-03-signals-contributing-providers.txt`
  (new file — the nine-signal page-check capture)
- Nine bold illustrative-caption lines and one section-intro sentence in `docs/src/deployment/cicd.md`
- Rewritten Garrison and Arsenal troubleshooting entries in `docs/src/appendix/cli-configuration.md`
- Corrected adapter module paths in `docs/src/contributing/contributing-providers.md`

No Rust symbol, no new page, no new dependency.

New symbols created elsewhere in Phase 36.1 (so drift verification does not flag them): the
`pub(crate)` visibility of `ToolResultFormatter::sanitize_tool_text` and the `fail_run_`-prefixed
pinning test (36.1-02); nineteen `# Examples` rustdoc sections across twelve port traits and seven
services (36.1-05 through 36.1-11); the `make check-api-examples` target, the hook id
`check-api-examples` and the CI lint step named "Check public-API `# Examples` headings" (36.1-11);
the files `36.1-ENTRY-POINTS.md`, `36.1-EVIDENCE.md`, `36.1-CI-EVIDENCE.md` and `COVERAGE.md`;
requirement ids `CURR-16` through `CURR-21`; `WINDOWS.md` rows 39 through 50.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation page → contributor action | a reader copies a path, a command or a workflow from the page into their own tree |
| illustrative example → operational belief | an uncaptioned deployment manifest reads as something this repository runs |
| page body → published book | every edited line ships in the v0.10.0 book |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36.1-09 | Information Disclosure | secret-expression references in the captioned CI/CD blocks | low | accept | The references name workflow secret keys, not values, and the Phase 35 security audit verified them as not a disclosure. The action forbids editing any line inside a block and the acceptance criteria assert the reference count is unchanged, so this plan neither adds nor removes any. |
| T-36.1-10 | Spoofing | the uncaptioned Kubernetes deployment block | medium | mitigate | An operator could believe this repository ships a deployment pipeline and look for credentials or a cluster that do not exist. Every block gains a caption naming it as illustrative and linking the real release path, and the section intro says it once in prose. |
| T-36.1-11 | Tampering | fabricated replacement excerpt | medium | mitigate | Replacing an illustrative block with an invented "real" workflow is the failure mode Phase 35 deleted from another page. The action forbids replacement outright and a prohibition records it; the fix is a caption, not a rewrite. |
| T-36.1-12 | Repudiation | the flagged module-declaration line | medium | mitigate | An open question left silently unaddressed reads as if it never existed. The action requires the signal script's own class result to decide it and requires the SUMMARY to state the verdict and the line number either way. |
| T-36.1-SC | Tampering | package installs | low | accept | This plan installs no package. The documentation toolchain binaries are already pinned and present in this environment. |
</threat_model>

<verification>
- The pre-decomposition facade adapter path is absent from `docs/src/contributing/contributing-providers.md`, and the shipped crate path is present at both former defect sites.
- The source-line marker instruction is absent from the whole of `docs/src`.
- `docs/src/deployment/cicd.md` carries at least nine illustrative captions and its fenced block bodies are byte-identical to before.
- The documentation-workflow sequence — mermaid install, linkcheck build in error mode, the example check and the config check — exits 0, and `git status --porcelain -- docs` is clean.
- No file under `src/`, `crates/`, `tests/`, `examples/` or `.github/` is modified.
- The plan's SUMMARY carries a `## Self-Check` section and the module-declaration verdict.
</verification>

<success_criteria>
- Three of ROADMAP SC1's six named defects are closed on the page, each against the tree rather than against another document.
- The register's requested nine-signal re-run exists as evidence with a recorded verdict.
- The book still builds with zero broken links and the docs tree is clean.
</success_criteria>

<output>
Create `.planning/phases/36.1-deferred-items-closure/36.1-03-SUMMARY.md` when done, recording: the three commit SHAs, the exit checks with their observed output, the nine-signal script's reproduced classes and the verdict on the module-declaration line with its line number, the caption count in the CI/CD page, the documentation-gate result, and a `## Self-Check` section.
</output>
