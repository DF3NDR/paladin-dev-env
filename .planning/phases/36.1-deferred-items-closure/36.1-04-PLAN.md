---
phase: 36.1-deferred-items-closure
plan: 04
type: execute
wave: 2
depends_on: ["36.1-01"]
files_modified:
  - docs/src/contributing/testing-guide.md
  - docs/src/contributing/architecture-decisions.md
  - docs/src/user-guides/memory-management.md
  - docs/src/user-guides/tool-integration.md
  - docs/src/appendix/battalion-vision-support.md
  - docs/src/appendix/battalion-patterns-guide.md
  - docs/src/appendix/conclave-pattern.md
autonomous: true
requirements: [CURR-16]
must_haves:
  truths:
    - "`docs/src/contributing/testing-guide.md`'s `tests/` tree reflects what `ls -p tests` actually shows — real directory and file names only, with the test configuration file placed at the repository root as a sibling of `tests/` rather than inside the fixtures directory (D-05, CURR-16)."
    - "The tree names no file or directory the repository does not have, at any depth it shows."
    - "Every word-boundary occurrence of the mis-cased adapter type is corrected to the shipped capitalisation across all seven pages that carry one, and a whole-tree search for the mis-cased spelling under `docs/src` returns nothing (D-07)."
    - "Each of the four battalion-patterns-guide example bodies the Phase 35 register flagged has been run through the throwaway scratch compile probe, and every body that does not compile is recorded with the exact reason so plan 36.1-13 can mint its waived ledger row (D-07)."
    - "The Phase 35 evidence allowlist row that excused the stale tests tree as a false positive is retired in this phase's own evidence record rather than left standing (D-05)."
    - "`mdbook build docs/` with the linkcheck backend passes on the exact documentation-workflow sequence and `git status --porcelain -- docs` is clean afterwards (D-00f)."
  artifacts:
    - "docs/src/contributing/testing-guide.md — corrected tests tree and corrected adapter casing"
    - "docs/src/appendix/battalion-patterns-guide.md — four corrected casings plus a recorded compile-probe verdict per body"
    - "docs/src/{contributing/architecture-decisions.md,user-guides/memory-management.md,user-guides/tool-integration.md,appendix/battalion-vision-support.md,appendix/conclave-pattern.md} — corrected adapter casing"
    - ".planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-04-scratch-probe.txt — the per-body compile-probe result"
  key_links:
    - "The real top-level entries under `tests/` include directories the current tree omits entirely — `cli/`, `helpers/`, `repository/` and `scripts/` — and the fixtures directory holds a README, a config directory and three sample media files, none of which the current tree names."
    - "The test configuration file lives at the repository root, not under the fixtures directory; showing it as a sibling of `tests/` is what makes the tree's own claim true."
    - "The casing sweep is scope-limited to capitalisation; any body that additionally fails to compile is recorded as its own waived ledger row rather than rewritten, so this phase does not grow into a page rewrite."
    - "The scratch compile probe is a throwaway example file plus a single-example cargo check with the needed features; it is never committed."
  prohibitions:
    - statement: "No `.rs` file under `src/`, `crates/`, `tests/` or `examples/` is modified by this plan — it edits seven documentation pages only (D-27)."
      status: recalled
      verification: flagged-unverified
    - statement: "The casing sweep changes capitalisation only; no argument list, method chain or import in any touched body is rewritten (D-07)."
      status: recalled
      verification: flagged-unverified
    - statement: "No entry is invented for the tests tree — every name shown is one the repository actually has."
      status: recalled
      verification: flagged-unverified
    - statement: "The throwaway scratch example file is never committed."
      status: recalled
      verification: flagged-unverified
    - statement: "The domain-role vocabulary rules hold in every rewritten line: the Medieval-military term is used for a domain role, the retired quartermaster term never appears, and no bare token total is written beside a split usage figure (D-00h)."
      status: recalled
      verification: flagged-unverified
    - statement: "The executor never runs `git push`."
      status: recalled
      verification: flagged-unverified
---

<objective>
Close the remaining three of ROADMAP SC1's six documentation defects: the testing guide's fabricated
`tests/` tree, and the mis-cased LLM adapter type across the seven pages that carry it.

Decisions honoured: D-00a (the shipped tree outranks the page), D-00e (the relocated-adapter fix
shape, the shipped capitalisation, the fence rule, and the throwaway scratch compile probe for
appendix samples — never committed), D-00f (the documentation gate is the exact documentation-workflow
sequence and the docs tree is clean afterwards), D-00h (the vocabulary rules hold in every rewritten
line), D-02 (the live-verified defect list: eleven word-boundary hits across seven pages, and a tests
tree that names files the repository does not have), D-05 (the whole tree block is corrected to what
the directory listing shows, one level deep under each subdirectory, with the test configuration file
at the repository root; and the Phase 35 allowlist row that excused it is retired), D-07 (one sweep
commit, casing only, with the four flagged bodies probed and any non-compiling body recorded for a
waived ledger row rather than rewritten), D-28 (`docs(36.1):` subjects).

Purpose: the tests tree is the first thing a new contributor reads to find where a test goes, and it
currently sends them to directories that do not exist while hiding four that do. The adapter casing
is the last eleven occurrences of a rename the rest of the book already completed — a reader copying
any of them gets a type that will not resolve.

Output: a tree that matches the repository, eleven corrected type names, a recorded compile-probe
verdict for four appendix bodies, and a clean documentation build.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/36.1-deferred-items-closure/36.1-CONTEXT.md
@.planning/phases/36.1-deferred-items-closure/36.1-RESEARCH.md
@.planning/phases/36.1-deferred-items-closure/36.1-PATTERNS.md
</context>

<tasks>

<!-- planner-discipline-allow: OpenAiAdapter -->
<!-- planner-discipline-allow: sample_data.json -->
<!-- planner-discipline-allow: redis_queue_test.rs -->
<!-- planner-discipline-allow: battalion_execution_test.rs -->

<task type="auto">
  <name>Task 1: Rebuild the testing guide's tests tree from the real directory listing</name>
  <files>docs/src/contributing/testing-guide.md</files>
  <read_first>
    - docs/src/contributing/testing-guide.md lines 70-105 — the whole Directory Structure block as it stands, plus the prose above and below it that the corrected tree must stay consistent with
    - The real listing: run `ls -p tests` and then `ls -p tests/unit tests/integration tests/functional tests/helpers tests/fixtures tests/cli tests/repository tests/scripts`, and confirm whether the test configuration file sits at the repository root with `ls -d config.test.yml`
    - .planning/phases/35-mdbook-currency/deferred-items.md, the Plan 35-06 entry — the register text, which names the fixture-path claim at line 96 as the defect
    - .planning/phases/35-mdbook-currency/35-EVIDENCE.md, the D-21 allowlist row that excused this tree as a false positive — the row this phase retires
    - .planning/phases/36.1-deferred-items-closure/36.1-CONTEXT.md decision D-05 — the exact depth rule and the placement of the configuration file
  </read_first>
  <action>
Replace the whole `tests/` ASCII tree block with one built from the real listing. Do not patch
individual lines: the block names three files the repository does not have
(`tests/integration/redis_queue_test.rs`, `tests/functional/battalion_execution_test.rs` and
`tests/fixtures/sample_data.json`) and omits four top-level directories it does have (`cli/`,
`helpers/`, `repository/`, `scripts/`), so a line-level patch leaves a tree that is still wrong.

Depth rule (D-05): show every top-level entry under `tests/`, then one level of children under each
of `unit/`, `integration/`, `functional/`, `helpers/` and `fixtures/`. Do not exhaustively list
every file — `tests/integration/` alone holds dozens; show a representative handful of real names
per directory and mark the elision plainly so a reader knows the listing is partial rather than
complete. Every name you do show must be one `ls` actually returned.

Show the test configuration file at the repository root, as a sibling of `tests/` — that is where it
lives, and placing it under the fixtures directory is the specific claim the register filed. The
fixtures directory's real contents are a README, a `config/` directory and three sample media files;
show those instead of the invented data file.

Re-read the prose around the block and correct any sentence the new tree contradicts, including the
claim at line 96 the register cites. Check the vocabulary rules on every rewritten line.

Record in the SUMMARY that the Phase 35 evidence allowlist row excusing this tree as a false positive
is retired by this commit — plan 36.1-13 carries that statement into `36.1-EVIDENCE.md`.

Commit as `docs(36.1): rebuild the tests tree from the real directory listing (D-05)`. Stage the page
by explicit path.
  </action>
  <verify>
    <automated>test "$(grep -c 'sample_data.json' docs/src/contributing/testing-guide.md)" = 0 &amp;&amp; test "$(grep -c 'redis_queue_test.rs' docs/src/contributing/testing-guide.md)" = 0 &amp;&amp; test "$(grep -c 'battalion_execution_test.rs' docs/src/contributing/testing-guide.md)" = 0 &amp;&amp; grep -q 'helpers/' docs/src/contributing/testing-guide.md &amp;&amp; grep -q 'config.test.yml' docs/src/contributing/testing-guide.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'sample_data.json' docs/src/contributing/testing-guide.md` is 0, and the same is true for `redis_queue_test.rs` and `battalion_execution_test.rs`.
    - The tree block names all eight real top-level directories under `tests/`: `cli/`, `fixtures/`, `functional/`, `helpers/`, `integration/`, `repository/`, `scripts/` and `unit/`.
    - The tree shows `config.test.yml` outside the `tests/` subtree, at the same indentation level as `tests/` itself.
    - Every file name shown inside the tree is returned by an `ls` of its stated directory — spot-checkable name by name.
    - The block marks its per-directory listings as partial where they are partial.
    - `git diff --stat HEAD~1 -- src/ crates/ tests/ examples/` is empty.
  </acceptance_criteria>
  <done>A contributor reading the tree finds the directory their test belongs in, and every name the tree shows exists.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep the mis-cased adapter type across all seven pages and probe the four flagged bodies</name>
  <files>docs/src/contributing/testing-guide.md, docs/src/contributing/architecture-decisions.md, docs/src/user-guides/memory-management.md, docs/src/user-guides/tool-integration.md, docs/src/appendix/battalion-vision-support.md, docs/src/appendix/battalion-patterns-guide.md, docs/src/appendix/conclave-pattern.md</files>
  <read_first>
    - The eleven hit sites themselves, found with `grep -rnw OpenAiAdapter docs/src`: architecture-decisions 236, testing-guide 324, memory-management 113, tool-integration 187 and 295, battalion-vision-support 278, battalion-patterns-guide 43, 134, 232 and 354, conclave-pattern 87 — read each in its surrounding block so the sweep does not change anything but capitalisation
    - crates/paladin-llm/src/openai/adapter.rs — the shipped type's actual declared name, which fixes the target capitalisation
    - .planning/phases/35-mdbook-currency/deferred-items.md, the Plan 35-09 entry — the register text, its classification as a systemic casing defect, and the note that the four battalion-patterns-guide bodies carry further API-shape drift
    - .planning/phases/35-mdbook-currency/35-CONTEXT.md decisions D-11 and D-13 — the fence rule and the relocated-adapter fix shape
    - .planning/phases/36.1-deferred-items-closure/36.1-CONTEXT.md decision D-07 — the exact waive reason text for a body that does not compile, and the bound that stops this becoming a page rewrite
  </read_first>
  <action>
**The sweep.** Correct every word-boundary occurrence of the mis-cased adapter type to the shipped
capitalisation, across all seven pages, in one commit. The shipped type spells the provider prefix in
full capitals; read `crates/paladin-llm/src/openai/adapter.rs` and use exactly what it declares.
Scope is capitalisation and nothing else: do not change an argument list, a method chain, an import,
a fence attribute or any surrounding prose in the same edit.

**The probe.** The Phase 35 register flagged the four battalion-patterns-guide bodies at lines 43,
134, 232 and 354 as carrying further API-shape drift beyond the casing. For each of the four, run the
throwaway scratch compile probe: write the body into a scratch example file, run a single-example
cargo check with whichever features that body needs, record the result, and delete the scratch file.
Capture all four results to
`.planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-04-scratch-probe.txt` with the
command line and the compiler output for each.

A body that compiles needs nothing further. A body that does not compile is **not** rewritten here:
record it in the SUMMARY with its line number and the compiler's own reason, and state that plan
36.1-13 mints it a ledger row of the unmet-truth kind and waives it with the reason naming the
appendix sample as fenced non-compiling, the v0.11.0 documentation-currency pass as owner, and that
milestone's discuss step as the re-check trigger. Note that this is an addition to the twelve-row
mapping, so 36.1-13 must be told the exact count — state it plainly rather than leaving it to be
inferred.

Confirm the scratch file is absent from the working tree and from the commit before committing.

Commit as `docs(36.1): correct the LLM adapter type casing across the book (D-07)`. Stage the seven
pages and the probe capture by explicit path.
  </action>
  <verify>
    <automated>test "$(grep -rnw OpenAiAdapter docs/src | wc -l)" = 0 &amp;&amp; test "$(grep -rlw OpenAIAdapter docs/src | wc -l)" -ge 7 &amp;&amp; test -s .planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-04-scratch-probe.txt</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rnw OpenAiAdapter docs/src` returns nothing.
    - At least seven files under `docs/src` now contain the correctly-cased type name, and the total count of correctly-cased word-boundary occurrences across those pages is at least eleven.
    - `git diff` for the sweep commit shows only capitalisation changes — no changed argument, method chain, import or fence attribute on any touched line.
    - The probe capture file records four entries, each with the page line number, the cargo command line including its feature list, and the compiler's verdict.
    - The SUMMARY names, per body, whether it compiled, and for each that did not, the compiler reason and the ledger-row instruction for plan 36.1-13 including the resulting total row count.
    - No scratch example file exists under `examples/` after the commit, and none appears in the commit's file list.
    - `git diff --stat HEAD~1 -- src/ crates/ tests/` is empty.
  </acceptance_criteria>
  <done>The book no longer names a type the crate does not export, and every appendix body that still would not compile is recorded with an owner rather than left silently broken.</done>
</task>

<task type="auto">
  <name>Task 3: Run the documentation gate on the seven edited pages</name>
  <files>docs/src/contributing/testing-guide.md</files>
  <read_first>
    - .github/workflows/docs.yml — the exact command sequence the gate runs, in order, including the mermaid preprocessor install and the linkcheck warning policy
    - docs/book.toml — the linkcheck backend's configured warning policy, confirming a broken link is an error rather than a warning
    - scripts/check-doc-examples.sh and scripts/check-doc-config.sh — what each asserts, so a failure can be read rather than guessed at
    - .planning/phases/36.1-deferred-items-closure/36.1-CONTEXT.md decision D-00f — the gate definition and the clean-tree requirement
  </read_first>
  <action>
Run the documentation gate exactly as the documentation workflow runs it, in order: install the
mermaid preprocessor into the book directory, build the book with the linkcheck backend in its
error-on-warning policy, then run the documentation-example check script and the documentation-config
check script. Capture the full output to
`.planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-04-docs-gate.txt`.

Then confirm `git status --porcelain -- docs` prints nothing. The build writes artifacts into the
book directory; if anything is left behind, restore with `git checkout -- docs/` and record in the
capture that it was necessary.

If the linkcheck build reports a broken link introduced by this plan's edits, fix the link in the
page that introduced it and re-run. If it reports a pre-existing broken link on a page this plan did
not touch, do not fix it here: record it in the SUMMARY as a finding for plan 36.1-13 to disposition.

Commit the capture as `docs(36.1): record the documentation gate result for the SC1 page fixes`.
Stage the capture by explicit path.
  </action>
  <verify>
    <automated>test -s .planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-04-docs-gate.txt &amp;&amp; test -z "$(git status --porcelain -- docs)"</automated>
  </verify>
  <acceptance_criteria>
    - The capture file records all four gate steps in order with their exit status.
    - The linkcheck build's step records a zero broken-link count, or names each broken link with the page it is on.
    - `git status --porcelain -- docs` prints nothing after the run.
    - Any broken link on a page this plan did not touch is named in the SUMMARY as a finding for plan 36.1-13 rather than fixed here.
    - `git diff --stat HEAD~1 -- src/ crates/ tests/ docs/` is empty — this commit carries the capture only.
  </acceptance_criteria>
  <done>The book builds with zero broken links after seven pages changed, and the evidence says so with its own output.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Created by **this plan**:
- `.planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-04-scratch-probe.txt` (new file)
- `.planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-04-docs-gate.txt` (new file)
- A rebuilt `tests/` tree block in `docs/src/contributing/testing-guide.md`
- Eleven corrected type-name occurrences across seven pages under `docs/src`

No Rust symbol, no new page, no new dependency. The throwaway scratch example file is deliberately
not an artifact — it is deleted before the commit.

New symbols created elsewhere in Phase 36.1 (so drift verification does not flag them): the
`pub(crate)` visibility of `ToolResultFormatter::sanitize_tool_text` and the `fail_run_`-prefixed
pinning test (36.1-02); nineteen `# Examples` rustdoc sections across twelve port traits and seven
services (36.1-05 through 36.1-11); the `make check-api-examples` target, the hook id
`check-api-examples` and the CI lint step named "Check public-API `# Examples` headings" (36.1-11);
the files `36.1-ENTRY-POINTS.md`, `36.1-EVIDENCE.md`, `36.1-CI-EVIDENCE.md` and `COVERAGE.md`;
requirement ids `CURR-16` through `CURR-21`; `WINDOWS.md` rows 39 through 50.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation page → contributor action | a reader copies a path or a type name from the page into their own tree |
| scratch probe file → repository | a throwaway compile probe is written into the tracked examples directory |
| page body → published book | every edited line ships in the v0.10.0 book |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36.1-13 | Spoofing | the `tests/` tree block | medium | mitigate | A tree naming directories that do not exist sends a contributor's test to the wrong place and hides four directories that do exist. The tree is rebuilt from the real listing, the three fabricated names are asserted absent, and the eight real top-level directories are asserted present. |
| T-36.1-14 | Tampering | the throwaway scratch compile probe | medium | mitigate | A probe file left behind would be committed into the tracked examples directory and picked up by the bulk example build. The action requires deletion before commit and the acceptance criteria assert its absence from both the working tree and the commit's file list. |
| T-36.1-15 | Repudiation | the four flagged example bodies | medium | mitigate | A body that does not compile, corrected only for casing and then left unmentioned, reads as fixed. Each body is probed, the result is captured verbatim, and a non-compiling body is recorded with the exact waive reason, owner and re-check trigger for plan 36.1-13 including the resulting row count. |
| T-36.1-16 | Information Disclosure | example bodies under probe | low | accept | The probe compiles documentation snippets that construct adapters with placeholder values; no real credential is introduced, and the scratch file never reaches a commit. |
| T-36.1-SC | Tampering | package installs | low | accept | This plan installs no package. The documentation toolchain binaries are already pinned and present. |
</threat_model>

<verification>
- The mis-cased adapter type is absent from the whole of `docs/src`, and the correctly-cased name is present on all seven pages.
- The tests tree names no file the repository lacks and shows all eight real top-level directories, with the test configuration file outside the `tests/` subtree.
- The scratch-probe capture records a verdict for all four flagged bodies, with any non-compiling body routed to a waived ledger row in plan 36.1-13.
- The documentation gate's four steps exit 0 and `git status --porcelain -- docs` is clean.
- No file under `src/`, `crates/`, `tests/` or `examples/` is modified, and no scratch example file is committed.
- The plan's SUMMARY carries a `## Self-Check` section.
</verification>

<success_criteria>
- The remaining three of ROADMAP SC1's six named defects are closed on the page, against the tree.
- Every appendix body that still would not compile has a named owner and a re-check trigger instead of a silent correction.
- The book builds with zero broken links after seven pages changed.
</success_criteria>

<output>
Create `.planning/phases/36.1-deferred-items-closure/36.1-04-SUMMARY.md` when done, recording: the three commit SHAs, the observed exit-check outputs, the eight top-level directories the rebuilt tree shows, the per-body compile-probe verdicts with the ledger-row instruction and resulting row count for plan 36.1-13, the retirement of the Phase 35 allowlist row, the documentation-gate result with any pre-existing broken link named as a finding, and a `## Self-Check` section.
</output>
