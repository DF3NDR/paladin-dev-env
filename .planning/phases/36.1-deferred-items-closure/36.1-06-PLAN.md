---
phase: 36.1-deferred-items-closure
plan: 06
type: execute
wave: 2
depends_on: ["36.1-01"]
files_modified:
  - crates/paladin-ports/src/output/run_trace_port.rs
  - crates/paladin-ports/src/input/assistant_admin_port.rs
  - crates/paladin-ports/src/output/assistant_repository_port.rs
autonomous: true
requirements: [CURR-17]
must_haves:
  truths:
    - "`RunTracePort`, `AssistantAdminPort` and `AssistantRepositoryPort` each carry a plural `# Examples` heading in their own doc block, and the public-API examples script reports each as satisfied rather than missing (D-09, CURR-17)."
    - "Each of the three sections carries at least one doctest that compiles and is reported as passed — not ignored — by `cargo test -p paladin-ports --doc` (D-10)."
    - "`RunTracePort`'s example demonstrates the behaviour its own doc comment promises: reading a thread that was never written yields an empty result rather than an error."
    - "Every doctest builds its example from a minimal local struct implementing the trait inline; none imports a storage adapter, because the ports crate has no dependency edge to the storage crate."
    - "`cargo doc -p paladin-ports --no-deps` still emits zero warning lines with the new sections in place."
  artifacts:
    - "crates/paladin-ports/src/output/run_trace_port.rs — new `# Examples` section"
    - "crates/paladin-ports/src/input/assistant_admin_port.rs — new `# Examples` section"
    - "crates/paladin-ports/src/output/assistant_repository_port.rs — new `# Examples` section"
  key_links:
    - "The ports crate's dependency list bounds every doctest's reachable namespace: the core crate plus async-trait, serde, thiserror, uuid, chrono, tokio, serde_json, futures, log, md5 and mime_guess."
    - "`TokenCounterPort`'s existing section in the same crate is the working model: a local type, an inline trait implementation, then assertions."
    - "`AssistantAdminPort` has a request-struct constructor near line 46 that is not the port — the example must implement the trait itself."
    - "These three traits declare several methods each; a trait implementation that omits one will not compile, so the example must implement every declared method even where the body is trivial."
  prohibitions:
    - statement: "No `.rs` file outside the D-27 list is modified — this plan touches three port trait files' doc comments only."
      status: recalled
      verification: flagged-unverified
    - statement: "No doctest in this plan imports the storage crate or any other crate absent from the ports crate's own dependency list."
      status: recalled
      verification: flagged-unverified
    - statement: "No new `# Examples` section is fenced so that its block is skipped rather than compiled — every section's doctest is reported as passed, never as ignored (D-10)."
      status: recalled
      verification: flagged-unverified
    - statement: "No production item is added, removed or re-signed in any touched file; the diff is doc comments only."
      status: recalled
      verification: flagged-unverified
    - statement: "No rustdoc lint suppression attribute is added to or removed from any file in this phase (D-29)."
      status: recalled
      verification: flagged-unverified
    - statement: "The executor never runs `git push`."
      status: recalled
      verification: flagged-unverified
---

<objective>
Add three more of the nineteen missing `# Examples` sections — the trace port and the two assistant
ports, each of which declares enough methods that the inline implementation is the bulk of the work.

Decisions honoured: D-00d (the plural-heading rule and its handed-over drift), D-09 (wire, refreeze
and fix — the script keeps deriving the entry-point set live, and fixing the missing items is what
closes the gap), D-10 (a compliant section is a plural heading followed by at least one doctest that
compiles under the workspace doctest run; not-run is allowed where a live service is needed, skipped
is not), D-27 (the bounded list of source files this phase may touch), D-28 (`docs(36.1):` subjects).

Purpose: these three ports carry the run-trace and assistant-management contracts that Phases 22
through 27 added. Each has a doc comment describing behaviour no example demonstrates — most
pointedly the trace port, whose contract that a missing thread reads as empty rather than as an error
is exactly the kind of thing an example settles and prose does not.

Output: three compliant sections, each proven by a doctest that actually runs.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/36.1-deferred-items-closure/36.1-CONTEXT.md
@.planning/phases/36.1-deferred-items-closure/36.1-RESEARCH.md
@.planning/phases/36.1-deferred-items-closure/36.1-PATTERNS.md
@crates/paladin-ports/src/output/token_counter_port.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RunTracePort</name>
  <files>crates/paladin-ports/src/output/run_trace_port.rs</files>
  <read_first>
    - crates/paladin-ports/src/output/run_trace_port.rs in full — the trait declaration at line 130, every async method it declares with its argument and return types, the `RunTraceError` type, and the doc statement that a missing thread reads as an empty result rather than an error
    - crates/paladin-ports/src/output/token_counter_port.rs lines 45-80 — the section model
    - crates/paladin-ports/Cargo.toml — the dependency bound on what a doctest may import
    - .planning/phases/36.1-deferred-items-closure/36.1-RESEARCH.md, the twelve-port inventory row for this trait
  </read_first>
  <behavior>
    - The workspace doctest suite filtered to the run-trace module reports at least one passed doctest and zero failed.
    - The example's assertions demonstrate that reading a thread never written to yields an empty result, not an error.
    - The public-API examples script no longer reports this trait as missing.
  </behavior>
  <action>
Add a `# Examples` section to the trait's own doc comment at line 130. The example declares a local
struct holding the appended records in a map behind a mutex, implements every method the trait
declares on it inline, then does two things with assertions: reads a thread that was never written
and asserts the result is empty and successful; appends a batch and reads it back, asserting the
count. The first assertion is the point — it is the contract the trait's own doc comment states and
that no example currently demonstrates.

Implement every declared method; a missing method will not compile. Bodies for methods the example
does not exercise may be trivial, but they must type-check.

Wrap the async body with the crate's own tokio dependency. Use a plain compiling fence; do not mark
the block skipped. Import nothing outside the ports crate's own dependency list — in particular, not
the storage crate, which is not a dependency of this crate and whose use here would invert the
architecture's dependency direction.

Commit as `docs(36.1): add # Examples to RunTracePort (D-10)`. Stage the file by explicit path; plain
`git commit` with a 600-second timeout, not the `gsd-tools` commit helper.
  </action>
  <verify>
    <automated>cargo test -p paladin-ports --doc run_trace_port 2>&amp;1 | grep -E '^test result: ok\. [1-9][0-9]* passed'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p paladin-ports --doc run_trace_port` prints `^test result: ok. [1-9][0-9]* passed` with zero failed and zero ignored.
    - `bash scripts/check-public-api-examples.sh --list | grep RunTracePort` shows the satisfied verdict.
    - The example asserts both the empty-read-of-an-unknown-thread behaviour and a successful append-then-read round trip.
    - `grep -c 'paladin_storage' crates/paladin-ports/src/output/run_trace_port.rs` is 0.
    - `git diff HEAD~1 -- crates/paladin-ports/` shows changes on comment lines only.
  </acceptance_criteria>
  <done>The trace port's stated empty-read contract is demonstrated by an example the test suite runs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AssistantAdminPort</name>
  <files>crates/paladin-ports/src/input/assistant_admin_port.rs</files>
  <read_first>
    - crates/paladin-ports/src/input/assistant_admin_port.rs in full — the trait declaration at line 124 with its six async methods, the request-struct constructor near line 46 that is not the port, and the error type the methods return
    - crates/paladin-ports/src/output/token_counter_port.rs lines 45-80 — the section model
    - crates/paladin-ports/Cargo.toml — the dependency bound
    - .planning/phases/36.1-deferred-items-closure/36.1-RESEARCH.md, the twelve-port inventory row for this trait, including the explicit warning not to mistake the request-struct constructor for the port
  </read_first>
  <behavior>
    - The workspace doctest suite filtered to the assistant-admin module reports at least one passed doctest and zero failed.
    - The example implements the port trait itself, not merely the request struct it takes.
    - The public-API examples script no longer reports this trait as missing.
  </behavior>
  <action>
Add a `# Examples` section to the trait's own doc comment at line 124. The example declares a local
struct, implements all six declared methods on it inline, then exercises create followed by get with
an assertion that the retrieved record matches what was created.

Do not confuse the request-struct constructor near line 46 with the port: the example must implement
the trait, and may use that constructor to build the argument it passes in. Implement every declared
method; bodies for unexercised methods may be trivial but must type-check.

Wrap the async body with the crate's own tokio dependency. Plain compiling fence, no skipped block,
no import outside the ports crate's dependency list.

Commit as `docs(36.1): add # Examples to AssistantAdminPort (D-10)`. Stage the file by explicit path.
  </action>
  <verify>
    <automated>cargo test -p paladin-ports --doc assistant_admin_port 2>&amp;1 | grep -E '^test result: ok\. [1-9][0-9]* passed'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p paladin-ports --doc assistant_admin_port` prints `^test result: ok. [1-9][0-9]* passed` with zero failed and zero ignored.
    - `bash scripts/check-public-api-examples.sh --list | grep AssistantAdminPort` shows the satisfied verdict.
    - The example contains an inline implementation of the port trait with all six methods present.
    - `grep -c 'paladin_storage' crates/paladin-ports/src/input/assistant_admin_port.rs` is 0.
    - `git diff HEAD~1 -- crates/paladin-ports/` shows changes on comment lines only.
  </acceptance_criteria>
  <done>The admin port carries an example that implements the port itself and exercises a real round trip.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: AssistantRepositoryPort, then re-measure the crate</name>
  <files>crates/paladin-ports/src/output/assistant_repository_port.rs</files>
  <read_first>
    - crates/paladin-ports/src/output/assistant_repository_port.rs in full — the trait declaration at line 119 with its seven async methods covering create, version append, get, version get, list, version list and soft delete, plus the error type
    - crates/paladin-ports/src/output/token_counter_port.rs lines 45-80 — the section model
    - crates/paladin-ports/Cargo.toml — the dependency bound
    - .planning/decisions/0033-cargo-doc-warning-bar.md — the rustdoc bar this crate sits under
  </read_first>
  <behavior>
    - The workspace doctest suite filtered to the assistant-repository module reports at least one passed doctest and zero failed.
    - `cargo doc -p paladin-ports --no-deps` emits zero warning lines with this plan's three sections in place.
  </behavior>
  <action>
Add a `# Examples` section to the trait's own doc comment at line 119. The example declares a local
struct backed by a map behind a mutex, implements all seven declared methods on it inline, then
exercises create followed by version append followed by version list, asserting the version count.
Bodies for unexercised methods may be trivial but must type-check.

Plain compiling fence, no skipped block, async body wrapped with the crate's own tokio dependency, no
import outside the ports crate's dependency list.

Then re-measure the crate with this plan's three sections in place: run the crate's full doctest
suite and `cargo doc -p paladin-ports --no-deps`, and confirm the documentation build emits no
warning line. Capture both to
`.planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-06-ports-measure.txt`.

Commit the section as `docs(36.1): add # Examples to AssistantRepositoryPort (D-10)` and the capture
as `docs(36.1): record the ports-crate measurement after the assistant sections`. Stage by explicit
path.
  </action>
  <verify>
    <automated>cargo test -p paladin-ports --doc assistant_repository_port 2>&amp;1 | grep -E '^test result: ok\. [1-9][0-9]* passed' &amp;&amp; cargo doc -p paladin-ports --no-deps 2>&amp;1 | tee /tmp/36.1-06-doc.txt &amp;&amp; ! grep -q 'warning:' /tmp/36.1-06-doc.txt</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p paladin-ports --doc assistant_repository_port` prints `^test result: ok. [1-9][0-9]* passed` with zero failed and zero ignored.
    - `cargo doc -p paladin-ports --no-deps` produces no line containing a warning marker.
    - `cargo test -p paladin-ports --doc` across the whole crate reports zero failed.
    - The example contains an inline implementation with all seven methods present and asserts a version count after an append.
    - The measurement capture exists, is non-empty, and records both command lines with their output.
    - `git diff HEAD~2 -- crates/paladin-ports/` shows changes on comment lines only.
  </acceptance_criteria>
  <done>Three more of the nineteen missing sections are closed, each proven by a running doctest, and the ports crate's rustdoc bar is still at zero.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Created by **this plan**:
- `# Examples` doc sections on `RunTracePort`, `AssistantAdminPort` and `AssistantRepositoryPort`
  (doc comments on existing public traits — no new public item)
- Local example types declared inside those doctests (doctest-scope only, not crate items)
- `.planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-06-ports-measure.txt` (new file)

New symbols created elsewhere in Phase 36.1 (so drift verification does not flag them): the
`pub(crate)` visibility of `ToolResultFormatter::sanitize_tool_text` and the `fail_run_`-prefixed
pinning test (36.1-02); the other sixteen `# Examples` sections listed in plan 36.1-05's artifacts
section (36.1-05, 36.1-07 through 36.1-11); the `make check-api-examples` target, the hook id
`check-api-examples` and the CI lint step named "Check public-API `# Examples` headings" (36.1-11);
the files `36.1-ENTRY-POINTS.md`, `36.1-EVIDENCE.md`, `36.1-CI-EVIDENCE.md` and `COVERAGE.md`;
requirement ids `CURR-16` through `CURR-21`; `WINDOWS.md` rows 39 through 50.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doctest namespace → crate dependency graph | a doctest may only reach its own crate's dependencies, not the workspace |
| example code → reader's own implementation | a reader copies the example into their own adapter |
| doc comment → public API baseline | doc changes must not move the committed surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36.1-21 | Spoofing | a heading over a skipped block | high | mitigate | The gate script checks heading text only and never inspects fence attributes, so a skipped block would satisfy it while teaching nothing. Every acceptance criterion asserts a non-zero passed count from the doctest runner, which a skipped block cannot produce (D-10). |
| T-36.1-22 | Tampering | crate dependency graph | medium | mitigate | These three traits are the ones whose examples most tempt an executor to reach for a shipped in-memory adapter. The action forbids it, the acceptance criteria assert the storage crate name is absent from each touched file, and the dependency list is required reading. |
| T-36.1-23 | Information Disclosure | example placeholder values | medium | mitigate | Assistant records in an example could carry plausible-looking secrets. None of these three ports needs a credential; placeholder strings use obviously fake values in the house style. |
| T-36.1-24 | Tampering | the rustdoc zero-warning bar | medium | mitigate | New doc blocks are the change that reopens a closed bar. Task 3 re-measures the crate's documentation build and requires a zero warning count before the plan closes. |
| T-36.1-SC | Tampering | cargo package installs | low | accept | No package-manager install runs; no manifest or lockfile changes. |
</threat_model>

<verification>
- Three filtered doctest runs each report a non-zero passed count with zero failed and zero ignored.
- `cargo doc -p paladin-ports --no-deps` emits no warning line.
- The public-API examples script's missing count has dropped by three from this plan's starting point.
- Every touched file's diff is confined to comment lines, and none names the storage crate.
- No rustdoc suppression attribute is added or removed anywhere.
- The plan's SUMMARY carries a `## Self-Check` section.
</verification>

<success_criteria>
- Three more of the nineteen entry points ROADMAP SC2 names carry a plural heading backed by a doctest the suite runs.
- The trace port's stated empty-read contract is demonstrated rather than merely asserted in prose.
- The ports crate's rustdoc bar is still at zero.
</success_criteria>

<output>
Create `.planning/phases/36.1-deferred-items-closure/36.1-06-SUMMARY.md` when done, recording: the commit SHAs, the observed `test result` line for each of the three filtered doctest runs, the crate's documentation-build warning count, the examples script's new missing count, and a `## Self-Check` section.
</output>
