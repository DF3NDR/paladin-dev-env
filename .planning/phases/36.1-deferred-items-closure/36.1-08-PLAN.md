---
phase: 36.1-deferred-items-closure
plan: 08
type: execute
wave: 2
depends_on: ["36.1-01"]
files_modified:
  - src/application/services/run/schedule/service.rs
  - src/application/services/run/webhook/service.rs
autonomous: true
requirements: [CURR-17]
must_haves:
  truths:
    - "`ScheduleService` and `WebhookDeliveryService` each carry a plural `# Examples` heading in their own doc block, and the public-API examples script reports each as satisfied rather than missing (D-09, CURR-17)."
    - "Each of the two sections carries at least one doctest that compiles and is reported as passed — not ignored — by `cargo test -p paladin-ai --doc` under default features (D-10)."
    - "Each example constructs the service from its real port dependencies using the in-memory adapters the storage crate already ships, rather than from a mock written for the occasion."
    - "The webhook example handles its constructor's fallible return rather than hiding it, so a reader learns that constructing that service can fail."
    - "`cargo doc --workspace --no-deps` still emits zero warning lines with the new sections in place."
  artifacts:
    - "src/application/services/run/schedule/service.rs — new `# Examples` section"
    - "src/application/services/run/webhook/service.rs — new `# Examples` section"
  key_links:
    - "The facade depends on the storage crate unconditionally with the sqlite feature, so its in-memory adapters are reachable from a facade doctest under default features with no extra feature flag."
    - "The shipped in-memory adapters these two examples need are the schedule repository, the webhook-delivery repository and the run repository, each with a plain no-argument constructor."
    - "No shipped in-memory implementation of the run-submission port exists, so the schedule example supplies one inline the way the port-trait examples do — the shape is already proven by the internal test-module implementation in that port's own file."
    - "The fence-style models to copy are the vault port's not-run example and the field-version service's plain running example; the formation service's existing section is NOT a model — it is skipped rather than compiled and would not type-check if it were run."
  prohibitions:
    - statement: "No `.rs` file outside the D-27 list is modified — this plan touches two service files' doc comments only."
      status: recalled
      verification: flagged-unverified
    - statement: "No new `# Examples` section is fenced so that its block is skipped rather than compiled — every section's doctest is reported as passed, never as ignored (D-10)."
      status: recalled
      verification: flagged-unverified
    - statement: "No production item is added, removed or re-signed in any touched file; the diff is doc comments only."
      status: recalled
      verification: flagged-unverified
    - statement: "No example hardcodes a plausible-looking credential; placeholder values are obviously fake."
      status: recalled
      verification: flagged-unverified
    - statement: "No rustdoc lint suppression attribute is added to or removed from any file in this phase (D-29)."
      status: recalled
      verification: flagged-unverified
    - statement: "The executor never runs `git push`."
      status: recalled
      verification: flagged-unverified
---

<objective>
Add the first two of the seven missing facade-service `# Examples` sections: the schedule service and
the webhook delivery service.

Decisions honoured: D-00d (the plural-heading rule and the drift handed to this phase), D-09 (wire,
refreeze and fix — the nineteen missing items are a release-readiness gap this phase exists to
close), D-10 (for a service the example shows construction from its port dependencies using the mock
or in-memory adapters the crate already ships; a skipped block is not a compliant section), D-27 (the
bounded source-file list), D-28 (`docs(36.1):` subjects).

Purpose: unlike the port traits, these services can be built from adapters that actually ship, so the
example a reader copies is the real wiring rather than a teaching fiction. The webhook service's
constructor is fallible, which no current documentation anywhere says — the example is where that
becomes visible.

Output: two compliant sections, each proven by a doctest that actually runs.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/36.1-deferred-items-closure/36.1-CONTEXT.md
@.planning/phases/36.1-deferred-items-closure/36.1-RESEARCH.md
@.planning/phases/36.1-deferred-items-closure/36.1-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ScheduleService</name>
  <files>src/application/services/run/schedule/service.rs</files>
  <read_first>
    - src/application/services/run/schedule/service.rs lines 120-200 — the struct declaration at line 143 and its constructor near line 158, with the full argument list in order and the exact port types each argument takes
    - crates/paladin-storage/src/run_schedule/in_memory.rs lines 30-60 — the shipped in-memory schedule repository and its constructor
    - crates/paladin-ports/src/input/run_submission_port.rs lines 150-260 — the submission port's declared methods and the internal test-module implementation whose shape the example copies for the argument that has no shipped in-memory adapter
    - crates/paladin-ports/src/output/vault_port.rs lines 40-50 and src/.../field_version_service.rs lines 90-100 — the two fence-style models: a not-run example and a plain running example
    - crates/paladin-battalion/src/formation_service.rs lines 20-40 — the counter-example: an existing satisfied row whose block is skipped rather than compiled, and which would not type-check if it were run. Read it so as not to copy it
    - Cargo.toml line 111 — the unconditional storage dependency that makes the in-memory adapters reachable under default features
  </read_first>
  <behavior>
    - The workspace doctest suite filtered to the schedule service reports at least one passed doctest and zero failed.
    - The example constructs the service from the shipped in-memory schedule repository plus an inline submission-port implementation, in the constructor's declared argument order.
    - The public-API examples script no longer reports this service as missing.
  </behavior>
  <action>
Add a `# Examples` section to the struct's own doc comment at line 143. The example constructs the
service through its constructor with every argument the constructor declares, in order: the shipped
in-memory schedule repository for the repository argument, and — because no in-memory implementation
of the submission port ships — a minimal local struct implementing that port inline, the same way the
port-trait examples in the ports crate do. Read the submission port's own internal test-module
implementation first and copy its shape.

Keep the example to construction plus, at most, one cheap accessor or a single call that needs no
live backend. D-10 asks for construction from port dependencies, not a full scheduling flow.

Use a plain compiling fence if the example runs, or the not-run form if some argument would require a
live backend to build. Do not mark the block skipped: a skipped block satisfies the gate script,
which inspects heading text only, while teaching nothing — that is exactly the failure this phase is
closing.

Check the vocabulary rules on the prose you write: the Medieval-military term for a domain role, never
the retired quartermaster term, and no bare token total beside a split usage figure.

Commit as `docs(36.1): add # Examples to ScheduleService (D-10)`. Stage the file by explicit path;
plain `git commit` with a 600-second timeout, not the `gsd-tools` commit helper.
  </action>
  <verify>
    <automated>cargo test -p paladin-ai --doc ScheduleService 2>&amp;1 | grep -E '^test result: ok\. [1-9][0-9]* passed'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p paladin-ai --doc ScheduleService` prints `^test result: ok. [1-9][0-9]* passed` with zero failed and zero ignored.
    - `bash scripts/check-public-api-examples.sh --list | grep ScheduleService` shows the satisfied verdict.
    - The example calls the service's constructor with every declared argument, in the declared order.
    - The example uses the shipped in-memory schedule repository by its real type name, not a locally defined stand-in.
    - `git diff HEAD~1 -- src/` shows changes on comment lines only — no production item added, removed or re-signed.
  </acceptance_criteria>
  <done>The schedule service carries a worked construction example a reader can copy, proven by a doctest the suite runs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WebhookDeliveryService, then re-measure the workspace documentation build</name>
  <files>src/application/services/run/webhook/service.rs</files>
  <read_first>
    - src/application/services/run/webhook/service.rs lines 80-160 — the struct declaration at line 103 and its constructor, whose return type is fallible; note the full argument list and each argument's port type
    - crates/paladin-storage/src/webhook/in_memory.rs lines 30-60 and crates/paladin-storage/src/run/in_memory.rs lines 35-60 — the two shipped in-memory adapters this example needs, with their constructors
    - src/application/services/run/webhook/ssrf.rs — the module docs, so the example's prose does not contradict the guard's documented limits
    - crates/paladin-ports/src/output/vault_port.rs lines 40-50 — the not-run fence model
    - .planning/decisions/0033-cargo-doc-warning-bar.md — the rustdoc bar the workspace sits under
  </read_first>
  <behavior>
    - The workspace doctest suite filtered to the webhook delivery service reports at least one passed doctest and zero failed.
    - The example handles the constructor's fallible return explicitly rather than discarding it.
    - `cargo doc --workspace --no-deps` emits zero warning lines with this plan's two sections in place.
    - The public-API examples script no longer reports this service as missing.
  </behavior>
  <action>
Add a `# Examples` section to the struct's own doc comment at line 103. The example constructs the
service with the shipped in-memory webhook-delivery repository and the shipped in-memory run
repository, plus every remaining argument the constructor declares, in order.

The constructor returns a fallible result. Handle it visibly: wrap the example body in an entry
function returning a boxed-error result and propagate with the question-mark operator, so a reader
sees that construction can fail rather than seeing it unwrapped away. Do not swallow the error.

If the example's prose mentions the delivery URL, do not restate or contradict what the guard module's
own docs say about its limits — read that module first. Use an obviously fake placeholder for any URL
or header value; never a plausible-looking credential.

Use a plain compiling fence if the body runs without a live backend, or the not-run form if the
constructor would attempt real I/O. Do not mark the block skipped.

Then re-measure: run the workspace documentation build and the workspace doctest suite, and confirm
the documentation build emits no warning line. New doc blocks in the facade are exactly the change
that reopens the bar Phase 36 closed, and finding it here is cheaper than finding it in the closing
plan. Capture both outputs to
`.planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-08-services-measure.txt`.

Commit the section as `docs(36.1): add # Examples to WebhookDeliveryService (D-10)` and the capture
as `docs(36.1): record the workspace documentation measurement after the first service sections`.
Stage by explicit path.
  </action>
  <verify>
    <automated>cargo test -p paladin-ai --doc WebhookDeliveryService 2>&amp;1 | grep -E '^test result: ok\. [1-9][0-9]* passed' &amp;&amp; cargo doc --workspace --no-deps 2>&amp;1 | tee /tmp/36.1-08-doc.txt &amp;&amp; ! grep -q 'warning:' /tmp/36.1-08-doc.txt</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p paladin-ai --doc WebhookDeliveryService` prints `^test result: ok. [1-9][0-9]* passed` with zero failed and zero ignored.
    - `cargo doc --workspace --no-deps` produces no line containing a warning marker.
    - `bash scripts/check-public-api-examples.sh --list | grep -c MISSING` is 5.
    - The example's body sits inside an entry function returning a fallible result and propagates the constructor's error with the question-mark operator.
    - The example uses both shipped in-memory adapters by their real type names.
    - No string in the example resembles a real credential or a routable private address.
    - The measurement capture records both command lines with their output.
    - `git diff HEAD~2 -- src/ crates/` shows changes on comment lines only.
  </acceptance_criteria>
  <done>Both services carry running examples, the fallible constructor is visible to a reader, and the workspace documentation bar is still at zero.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Created by **this plan**:
- `# Examples` doc sections on `ScheduleService` and `WebhookDeliveryService` (doc comments on
  existing public structs — no new public item)
- A local submission-port example type declared inside the schedule doctest (doctest-scope only)
- `.planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-08-services-measure.txt` (new file)

New symbols created elsewhere in Phase 36.1 (so drift verification does not flag them): the
`pub(crate)` visibility of `ToolResultFormatter::sanitize_tool_text` and the `fail_run_`-prefixed
pinning test (36.1-02); the twelve port-trait sections (36.1-05 through 36.1-07); the five remaining
service sections on `RunSubmissionService`, `RunInspectorService`, `RunEventStreamService`,
`AssistantService` and `WaypointRetentionService` (36.1-09 through 36.1-11); the
`make check-api-examples` target, the hook id `check-api-examples` and the CI lint step named
"Check public-API `# Examples` headings" (36.1-11); the files `36.1-ENTRY-POINTS.md`,
`36.1-EVIDENCE.md`, `36.1-CI-EVIDENCE.md` and `COVERAGE.md`; requirement ids `CURR-16` through
`CURR-21`; `WINDOWS.md` rows 39 through 50.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| example code → reader's own wiring | a reader copies the construction sequence into their own application |
| documentation example → outbound HTTP | the webhook service's example names a delivery endpoint |
| doc comment → public API baseline | doc changes must not move the committed surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36.1-29 | Information Disclosure | placeholder values in the webhook example | medium | mitigate | A documentation example that hardcodes a plausible-looking signing secret or a routable internal address teaches readers to do the same and can be mistaken for a real value. The action requires obviously fake placeholders and the acceptance criteria assert no string resembles a real credential or a private address. |
| T-36.1-30 | Spoofing | a heading over a skipped block | high | mitigate | The gate script checks heading text only, so a skipped block would satisfy it while teaching nothing — and the workspace already contains one such satisfied row. Every acceptance criterion asserts a non-zero passed count from the doctest runner (D-10). |
| T-36.1-31 | Repudiation | the fallible constructor | medium | mitigate | An example that unwraps a fallible constructor teaches that it cannot fail. The action requires explicit propagation inside a fallible entry function and the acceptance criteria assert it. |
| T-36.1-32 | Tampering | the rustdoc zero-warning bar | medium | mitigate | New facade doc blocks are the change that reopens the bar Phase 36 closed. Task 2 re-measures the workspace documentation build and requires a zero warning count. |
| T-36.1-33 | Information Disclosure | the delivery-guard documentation | low | mitigate | Example prose that overstates what the request guard covers would contradict the module's own documented limits, including its recorded rebinding limitation. The guard module's docs are required reading and the action forbids restating or contradicting them. |
| T-36.1-SC | Tampering | cargo package installs | low | accept | No package-manager install runs; no manifest or lockfile changes. |
</threat_model>

<verification>
- Two filtered doctest runs each report a non-zero passed count with zero failed and zero ignored.
- `cargo doc --workspace --no-deps` emits no warning line.
- The public-API examples script's missing count is 5.
- Both touched files' diffs are confined to comment lines.
- No rustdoc suppression attribute is added or removed anywhere.
- The plan's SUMMARY carries a `## Self-Check` section.
</verification>

<success_criteria>
- Two of the seven facade services ROADMAP SC2 names carry a plural heading backed by a doctest the suite runs.
- Each example is the real wiring a reader can copy, built from adapters that actually ship.
- The workspace documentation bar is still at zero.
</success_criteria>

<output>
Create `.planning/phases/36.1-deferred-items-closure/36.1-08-SUMMARY.md` when done, recording: the commit SHAs, the observed `test result` line for each filtered doctest run, the fence style chosen for each example with the reason, the workspace documentation-build warning count, the examples script's new missing count, and a `## Self-Check` section.
</output>
