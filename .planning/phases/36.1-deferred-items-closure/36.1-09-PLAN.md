---
phase: 36.1-deferred-items-closure
plan: 09
type: execute
wave: 2
depends_on: ["36.1-01"]
files_modified:
  - src/application/services/run/submission.rs
  - src/application/services/run/inspector.rs
autonomous: true
requirements: [CURR-17]
must_haves:
  truths:
    - "`RunSubmissionService` and `RunInspectorService` each carry a plural `# Examples` heading in their own doc block, and the public-API examples script reports each as satisfied rather than missing (D-09, CURR-17)."
    - "Each of the two sections carries at least one doctest that compiles and is reported as passed — not ignored — by `cargo test -p paladin-ai --doc` under default features (D-10)."
    - "Each example constructs the service from its real port dependencies using the in-memory adapters the storage crate already ships, and satisfies the assistant-resolver argument with a shipped implementation where one exists or a minimal inline one where it does not — with the choice stated in the SUMMARY."
    - "`cargo doc --workspace --no-deps` still emits zero warning lines with the new sections in place."
  artifacts:
    - "src/application/services/run/submission.rs — new `# Examples` section"
    - "src/application/services/run/inspector.rs — new `# Examples` section"
  key_links:
    - "Both services take an assistant-resolver argument whose construction path was not verified during research — the executor reads the resolver type's own file first and reports what it found, rather than assuming a no-argument constructor exists."
    - "The shipped in-memory adapters these two examples need are the run repository, the run queue and the waypoint store, each with a plain no-argument constructor."
    - "The facade depends on the storage crate unconditionally with the sqlite feature, so the in-memory adapters are reachable from a facade doctest under default features."
    - "The fence-style models are the vault port's not-run example and the field-version service's plain running example; the formation service's skipped section is a counter-example, not a model."
  prohibitions:
    - statement: "No `.rs` file outside the D-27 list is modified — this plan touches two service files' doc comments only."
      status: recalled
      verification: flagged-unverified
    - statement: "No new `# Examples` section is fenced so that its block is skipped rather than compiled — every section's doctest is reported as passed, never as ignored (D-10)."
      status: recalled
      verification: flagged-unverified
    - statement: "No production item is added, removed or re-signed in any touched file; the diff is doc comments only."
      status: recalled
      verification: flagged-unverified
    - statement: "No new production helper, constructor or trait implementation is added anywhere to make a doctest compile — if a collaborator cannot be built, the example uses a minimal inline implementation instead."
      status: recalled
      verification: flagged-unverified
    - statement: "No example hardcodes a plausible-looking credential; placeholder values are obviously fake."
      status: recalled
      verification: flagged-unverified
    - statement: "No rustdoc lint suppression attribute is added to or removed from any file in this phase (D-29)."
      status: recalled
      verification: flagged-unverified
    - statement: "The executor never runs `git push`."
      status: recalled
      verification: flagged-unverified
---

<objective>
Add two more of the seven missing facade-service `# Examples` sections: the run submission service and
the run inspector service — the two whose constructors both take an assistant resolver, the one
collaborator research could not confirm a construction path for.

Decisions honoured: D-00d (the plural-heading rule and the drift handed to this phase), D-09 (wire,
refreeze and fix; nineteen entry points added in Phases 22 through 27 without an example are a
release-readiness gap), D-10 (a service example shows construction from its port dependencies using
adapters the crate already ships; a skipped block is not compliant), D-27 (the bounded source-file
list — in particular, no production helper may be added to make an example compile), D-28
(`docs(36.1):` subjects).

Purpose: these two are the read and write ends of the run surface, and both are named in the missing
set. They are grouped here because they share the unverified collaborator: whatever the resolver
argument turns out to need, it is solved once and applied twice.

Output: two compliant sections, each proven by a doctest that actually runs, and a recorded finding
about the resolver's construction path.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/36.1-deferred-items-closure/36.1-CONTEXT.md
@.planning/phases/36.1-deferred-items-closure/36.1-RESEARCH.md
@.planning/phases/36.1-deferred-items-closure/36.1-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Settle the assistant-resolver construction path, then add the RunSubmissionService section</name>
  <files>src/application/services/run/submission.rs</files>
  <read_first>
    - src/application/services/run/submission.rs lines 90-170 — the struct declaration at line 105 and its constructor near line 120, with the full argument list in order and each argument's port type
    - The assistant-resolver trait's own declaration and every implementation of it in the tree — find them with `grep -rn 'AssistantResolver' src/ crates/ --include=*.rs` and read the declaration plus any shipped implementation, so the example can use a real one if one exists
    - crates/paladin-storage/src/run/in_memory.rs lines 35-60 and crates/paladin-storage/src/run_queue/in_memory.rs lines 110-135 — the two shipped in-memory adapters and their constructors
    - crates/paladin-ports/src/output/vault_port.rs lines 40-50 and src/.../field_version_service.rs lines 90-100 — the two fence-style models
    - .planning/phases/36.1-deferred-items-closure/36.1-RESEARCH.md, assumption A1 — the explicit statement that this collaborator's construction path was not verified and must be checked before the doctest is written
  </read_first>
  <behavior>
    - The workspace doctest suite filtered to the run submission service reports at least one passed doctest and zero failed.
    - The example constructs the service through its constructor with every declared argument, in order.
    - The public-API examples script no longer reports this service as missing.
  </behavior>
  <action>
**Settle the resolver first.** Search the tree for the assistant-resolver trait and every
implementation of it. If a shipped implementation exists with a construction path a doctest can use,
use it. If none does, declare a minimal local struct in the doctest and implement the resolver trait
on it inline — the same technique the port-trait examples use. Do **not** add a production
constructor, a `Default`, or a new implementation anywhere in `src/` or `crates/` to make the example
compile; that would breach the phase's source-file bound. Record in the SUMMARY which route you took
and why, because plan 36.1-10 faces the same question for a different service.

**Then the section.** Add a `# Examples` section to the struct's own doc comment at line 105. The
example constructs the service through its constructor with every declared argument in order: the
shipped in-memory run repository, the shipped in-memory run queue, the resolver as settled above, and
whatever else the constructor declares. Keep it to construction plus at most one cheap call that
needs no live backend.

Use a plain compiling fence if the body runs, or the not-run form if some argument would require real
I/O. Do not mark the block skipped: the gate script inspects heading text only, so a skipped block
would satisfy it while teaching nothing — the exact failure this phase closes.

Check the vocabulary rules on any prose you write.

Commit as `docs(36.1): add # Examples to RunSubmissionService (D-10)`. Stage the file by explicit
path; plain `git commit` with a 600-second timeout, not the `gsd-tools` commit helper.
  </action>
  <verify>
    <automated>cargo test -p paladin-ai --doc RunSubmissionService 2>&amp;1 | grep -E '^test result: ok\. [1-9][0-9]* passed'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p paladin-ai --doc RunSubmissionService` prints `^test result: ok. [1-9][0-9]* passed` with zero failed and zero ignored.
    - `bash scripts/check-public-api-examples.sh --list | grep RunSubmissionService` shows the satisfied verdict.
    - The example calls the service's constructor with every declared argument, in the declared order.
    - The example uses the shipped in-memory run repository and run queue by their real type names.
    - `git diff HEAD~1 -- src/ crates/` shows changes on comment lines only — no production item added, removed or re-signed anywhere, including no new resolver implementation.
    - The SUMMARY states which resolver route was taken and the evidence for it.
  </acceptance_criteria>
  <done>The submission service carries a running construction example, and the resolver question is settled once with the answer written down.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RunInspectorService, then re-measure the workspace documentation build</name>
  <files>src/application/services/run/inspector.rs</files>
  <read_first>
    - src/application/services/run/inspector.rs lines 60-130 — the struct declaration at line 75 and its constructor, with its three arguments in order: the waypoint port, the run repository port and the assistant resolver
    - crates/paladin-storage/src/waypoint/in_memory.rs lines 30-55 and crates/paladin-storage/src/run/in_memory.rs lines 35-60 — the two shipped in-memory adapters and their constructors
    - The resolver route settled in Task 1, as recorded in this plan's own working notes — reuse it rather than re-deriving it
    - .planning/decisions/0033-cargo-doc-warning-bar.md — the rustdoc bar the workspace sits under
  </read_first>
  <behavior>
    - The workspace doctest suite filtered to the run inspector service reports at least one passed doctest and zero failed.
    - `cargo doc --workspace --no-deps` emits zero warning lines with this plan's two sections in place.
    - The public-API examples script no longer reports this service as missing.
  </behavior>
  <action>
Add a `# Examples` section to the struct's own doc comment at line 75. The example constructs the
service with the shipped in-memory waypoint store, the shipped in-memory run repository and the
resolver by the route Task 1 settled — reuse that route verbatim rather than inventing a second one,
so the two examples teach the same thing.

Keep it to construction plus at most one cheap read that needs no live backend. Plain compiling fence
or the not-run form; never a skipped block.

Then re-measure: run the workspace documentation build and confirm it emits no warning line, and run
the workspace doctest suite and confirm zero failures. Capture both to
`.planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-09-services-measure.txt`.

Commit the section as `docs(36.1): add # Examples to RunInspectorService (D-10)` and the capture as
`docs(36.1): record the workspace documentation measurement after the run-service sections`. Stage by
explicit path.
  </action>
  <verify>
    <automated>cargo test -p paladin-ai --doc RunInspectorService 2>&amp;1 | grep -E '^test result: ok\. [1-9][0-9]* passed' &amp;&amp; cargo doc --workspace --no-deps 2>&amp;1 | tee /tmp/36.1-09-doc.txt &amp;&amp; ! grep -q 'warning:' /tmp/36.1-09-doc.txt</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p paladin-ai --doc RunInspectorService` prints `^test result: ok. [1-9][0-9]* passed` with zero failed and zero ignored.
    - `cargo doc --workspace --no-deps` produces no line containing a warning marker.
    - `bash scripts/check-public-api-examples.sh --list | grep -c MISSING` is 3.
    - The example uses the same resolver route as Task 1 and both shipped in-memory adapters by their real type names.
    - The measurement capture records both command lines with their output.
    - `git diff HEAD~2 -- src/ crates/` shows changes on comment lines only.
  </acceptance_criteria>
  <done>Four of the seven facade services are satisfied, the two run-surface examples agree with each other, and the workspace documentation bar is still at zero.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Created by **this plan**:
- `# Examples` doc sections on `RunSubmissionService` and `RunInspectorService` (doc comments on
  existing public structs — no new public item)
- A local assistant-resolver example type declared inside those doctests, if and only if no shipped
  implementation is usable (doctest-scope only, never a crate item)
- `.planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-09-services-measure.txt` (new file)

New symbols created elsewhere in Phase 36.1 (so drift verification does not flag them): the
`pub(crate)` visibility of `ToolResultFormatter::sanitize_tool_text` and the `fail_run_`-prefixed
pinning test (36.1-02); the twelve port-trait sections (36.1-05 through 36.1-07); the sections on
`ScheduleService` and `WebhookDeliveryService` (36.1-08) and on `RunEventStreamService`,
`AssistantService` and `WaypointRetentionService` (36.1-10, 36.1-11); the `make check-api-examples`
target, the hook id `check-api-examples` and the CI lint step named "Check public-API `# Examples`
headings" (36.1-11); the files `36.1-ENTRY-POINTS.md`, `36.1-EVIDENCE.md`, `36.1-CI-EVIDENCE.md` and
`COVERAGE.md`; requirement ids `CURR-16` through `CURR-21`; `WINDOWS.md` rows 39 through 50.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| example code → reader's own wiring | a reader copies the construction sequence into their own application |
| doctest requirement → production source | an example that will not compile tempts an edit to production code to make it compile |
| doc comment → public API baseline | doc changes must not move the committed surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36.1-34 | Tampering | production source under doctest pressure | high | mitigate | The unverified resolver collaborator is the most likely point in this phase where an executor adds a production constructor or trait implementation to make an example compile, silently breaching the phase's source-file bound and possibly the API baseline. The action forbids it explicitly, offers the inline-implementation route instead, and the acceptance criteria assert the diff is comment lines only. |
| T-36.1-35 | Spoofing | a heading over a skipped block | high | mitigate | The gate script checks heading text only. Every acceptance criterion asserts a non-zero passed count from the doctest runner, which a skipped block cannot produce (D-10). |
| T-36.1-36 | Repudiation | the resolver route | medium | mitigate | Two examples solving the same collaborator two different ways would teach contradictory things. Task 1 records the route and its evidence in the SUMMARY, and Task 2 is required to reuse it verbatim. |
| T-36.1-37 | Information Disclosure | example placeholder values | medium | mitigate | Run and assistant identifiers in an example could carry plausible-looking secrets. Neither service's construction needs a credential; placeholder strings use obviously fake values. |
| T-36.1-38 | Tampering | the rustdoc zero-warning bar | medium | mitigate | New facade doc blocks are the change that reopens the bar. Task 2 re-measures the workspace documentation build and requires a zero warning count. |
| T-36.1-SC | Tampering | cargo package installs | low | accept | No package-manager install runs; no manifest or lockfile changes. |
</threat_model>

<verification>
- Two filtered doctest runs each report a non-zero passed count with zero failed and zero ignored.
- `cargo doc --workspace --no-deps` emits no warning line.
- The public-API examples script's missing count is 3.
- Both touched files' diffs are confined to comment lines, and no production item was added anywhere to make an example compile.
- The SUMMARY names the resolver route and its evidence.
- The plan's SUMMARY carries a `## Self-Check` section.
</verification>

<success_criteria>
- Two more of the seven facade services ROADMAP SC2 names carry a plural heading backed by a doctest the suite runs.
- The unverified collaborator is settled once, recorded, and reused rather than re-derived.
- No production code was changed to make documentation compile.
</success_criteria>

<output>
Create `.planning/phases/36.1-deferred-items-closure/36.1-09-SUMMARY.md` when done, recording: the commit SHAs, the observed `test result` line for each filtered doctest run, the resolver route taken with its evidence, the fence style chosen per example, the workspace documentation-build warning count, the examples script's new missing count, and a `## Self-Check` section.
</output>
