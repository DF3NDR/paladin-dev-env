---
phase: 36.1-deferred-items-closure
plan: 10
type: execute
wave: 2
depends_on: ["36.1-01"]
files_modified:
  - src/application/services/run/events.rs
  - src/application/services/assistant/service.rs
autonomous: true
requirements: [CURR-17]
must_haves:
  truths:
    - "`RunEventStreamService` and `AssistantService` each carry a plural `# Examples` heading in their own doc block, and the public-API examples script reports each as satisfied rather than missing (D-09, CURR-17)."
    - "Each of the two sections carries at least one doctest that compiles and is reported as passed — not ignored — by `cargo test -p paladin-ai --doc` under default features (D-10)."
    - "Each example constructs the service from its real port dependencies using the in-memory adapters the storage crate already ships, plus the facade's own event bus, without adding any production item to make the example compile."
    - "After this plan the public-API examples script's missing count is 1 — the retention service and nothing else, which plan 36.1-11 closes in the same commit as the gate wiring."
    - "`cargo doc --workspace --no-deps` still emits zero warning lines with the new sections in place."
  artifacts:
    - "src/application/services/run/events.rs — new `# Examples` section"
    - "src/application/services/assistant/service.rs — new `# Examples` section"
  key_links:
    - "The event bus the stream service takes is declared in the same file as the service, so it is always reachable from that file's own doctest."
    - "The assistant service's validator argument takes a registries value whose construction path was not verified during research — the executor reads the validator's own file and the registries type first and reports what it found."
    - "The shipped in-memory adapters these two examples need are the run repository, the waypoint store and the assistant repository, each with a plain no-argument constructor."
    - "The stream service's constructor takes a poll interval, so the example must name a concrete duration rather than leaving it implicit."
  prohibitions:
    - statement: "No `.rs` file outside the D-27 list is modified — this plan touches two service files' doc comments only."
      status: recalled
      verification: flagged-unverified
    - statement: "No new `# Examples` section is fenced so that its block is skipped rather than compiled — every section's doctest is reported as passed, never as ignored (D-10)."
      status: recalled
      verification: flagged-unverified
    - statement: "No production item is added, removed or re-signed in any touched file; the diff is doc comments only."
      status: recalled
      verification: flagged-unverified
    - statement: "No new production helper, constructor or trait implementation is added anywhere to make a doctest compile — if a collaborator cannot be built, the example uses a minimal inline implementation instead."
      status: recalled
      verification: flagged-unverified
    - statement: "No example spawns a background task or awaits a stream that would block the doctest process."
      status: recalled
      verification: flagged-unverified
    - statement: "No rustdoc lint suppression attribute is added to or removed from any file in this phase (D-29)."
      status: recalled
      verification: flagged-unverified
    - statement: "The executor never runs `git push`."
      status: recalled
      verification: flagged-unverified
---

<objective>
Add two more of the seven missing facade-service `# Examples` sections — the run event stream service
and the assistant service — leaving exactly one for the wiring plan to close in the same commit as
the gate.

Decisions honoured: D-00d (the plural-heading rule and the drift handed to this phase), D-09 (wire,
refreeze and fix), D-10 (a service example shows construction from its port dependencies using the
mock or in-memory adapters the crate already ships; a skipped block is not compliant), D-27 (the
bounded source-file list — no production item may be added to make an example compile), D-28
(`docs(36.1):` subjects).

Purpose: these two are the last straightforward services in the missing set. Closing them here leaves
the count at one, which is what makes the ordering rule workable: the nineteenth fix and the gate
wiring can then land in a single commit, so no commit in this phase's history has the gate wired and
red.

Output: two compliant sections, a missing count of one, and a workspace documentation build still at
zero warnings.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/36.1-deferred-items-closure/36.1-CONTEXT.md
@.planning/phases/36.1-deferred-items-closure/36.1-RESEARCH.md
@.planning/phases/36.1-deferred-items-closure/36.1-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RunEventStreamService</name>
  <files>src/application/services/run/events.rs</files>
  <read_first>
    - src/application/services/run/events.rs lines 250-290 — the event bus declared in this same file and its constructor, which the example uses directly
    - src/application/services/run/events.rs lines 750-820 — the struct declaration at line 764 and its constructor, whose four arguments are the bus, the run repository port, the waypoint port and a poll interval
    - crates/paladin-storage/src/run/in_memory.rs lines 35-60 and crates/paladin-storage/src/waypoint/in_memory.rs lines 30-55 — the two shipped in-memory adapters and their constructors
    - crates/paladin-ports/src/output/vault_port.rs lines 40-50 and src/.../field_version_service.rs lines 90-100 — the two fence-style models
    - crates/paladin-battalion/src/formation_service.rs lines 20-40 — the counter-example: a satisfied row whose block is skipped rather than compiled. Read it so as not to copy it
  </read_first>
  <behavior>
    - The workspace doctest suite filtered to the run event stream service reports at least one passed doctest and zero failed.
    - The doctest completes promptly — it does not await a stream or spawn a listener that would keep the process alive.
    - The public-API examples script no longer reports this service as missing.
  </behavior>
  <action>
Add a `# Examples` section to the struct's own doc comment at line 764. The example constructs the
event bus declared in this same file, then constructs the service with that bus, the shipped
in-memory run repository, the shipped in-memory waypoint store and a concrete poll interval — name a
short, explicit duration rather than leaving it implicit, because the argument's meaning is the thing
worth showing.

Stop at construction. Do not await a stream, do not spawn a background listener, and do not sleep:
this is a doctest process, and a body that waits for an event that never arrives hangs the suite
rather than failing it. If the example publishes anything at all, publish once to the bus and assert a
cheap synchronous property.

Use a plain compiling fence if the body runs, or the not-run form otherwise. Do not mark the block
skipped: the gate script checks heading text only, so a skipped block satisfies it while teaching
nothing.

Check the vocabulary rules on any prose you write.

Commit as `docs(36.1): add # Examples to RunEventStreamService (D-10)`. Stage the file by explicit
path; plain `git commit` with a 600-second timeout, not the `gsd-tools` commit helper.
  </action>
  <verify>
    <automated>timeout 300 cargo test -p paladin-ai --doc RunEventStreamService 2>&amp;1 | grep -E '^test result: ok\. [1-9][0-9]* passed'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p paladin-ai --doc RunEventStreamService` prints `^test result: ok. [1-9][0-9]* passed` with zero failed and zero ignored, and returns within the command's timeout.
    - `bash scripts/check-public-api-examples.sh --list | grep RunEventStreamService` shows the satisfied verdict.
    - The example constructs the bus from this file's own type and passes a named concrete poll duration.
    - The example body contains no stream await, no spawned listener and no sleep.
    - `git diff HEAD~1 -- src/ crates/` shows changes on comment lines only.
  </acceptance_criteria>
  <done>The event stream service carries a construction example that runs and returns, rather than one that waits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AssistantService, then re-measure the workspace documentation build</name>
  <files>src/application/services/assistant/service.rs</files>
  <read_first>
    - src/application/services/assistant/service.rs lines 20-70 — the struct declaration at line 29 and its constructor, whose two arguments are the assistant repository port and a validator
    - src/application/services/assistant/validator.rs lines 90-130 — the validator's constructor at line 101 and the registries value it takes; then read that registries type's own file for its construction path, which research did not verify
    - crates/paladin-storage/src/assistant/in_memory.rs lines 35-60 — the shipped in-memory assistant repository and its constructor
    - The resolver route plan 36.1-09 recorded in its SUMMARY — the precedent for what to do when a collaborator has no usable construction path
    - .planning/decisions/0033-cargo-doc-warning-bar.md — the rustdoc bar the workspace sits under
  </read_first>
  <behavior>
    - The workspace doctest suite filtered to the assistant service reports at least one passed doctest and zero failed.
    - `cargo doc --workspace --no-deps` emits zero warning lines with this plan's two sections in place.
    - The public-API examples script reports exactly one remaining missing item.
  </behavior>
  <action>
Add a `# Examples` section to the struct's own doc comment at line 29. The example constructs the
shipped in-memory assistant repository, constructs the validator with a registries value, and then
constructs the service from the two.

Read the registries type's own file before writing anything: research did not verify that it has a
zero-argument construction path. If it has a derived default or a simple constructor, use it. If it
does not, build the smallest registries value the type's own API allows. Do **not** add a `Default`,
a constructor, or any other production item to make the example compile — if no construction path
exists at all, say so in the SUMMARY and use the not-run fence with a body that still type-checks,
rather than silently switching to a skipped block.

Keep the example to construction plus at most one cheap call. Plain compiling fence where the body
runs; never a skipped block.

Then re-measure: run the workspace documentation build, confirm it emits no warning line; run the
workspace doctest suite, confirm zero failures; and run the public-API examples script in report
mode, confirming exactly one remaining missing item, which must be the retention service. If the
count is anything other than one, stop and record the discrepancy rather than adjusting it — plan
36.1-11's single-commit ordering rule depends on that count being exactly one. Capture all three to
`.planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-10-services-measure.txt`.

Commit the section as `docs(36.1): add # Examples to AssistantService (D-10)` and the capture as
`docs(36.1): record the measurement leaving one entry point outstanding (D-12)`. Stage by explicit
path.
  </action>
  <verify>
    <automated>cargo test -p paladin-ai --doc AssistantService 2>&amp;1 | grep -E '^test result: ok\. [1-9][0-9]* passed' &amp;&amp; test "$(bash scripts/check-public-api-examples.sh --list | grep -c MISSING)" = 1 &amp;&amp; cargo doc --workspace --no-deps 2>&amp;1 | tee /tmp/36.1-10-doc.txt &amp;&amp; ! grep -q 'warning:' /tmp/36.1-10-doc.txt</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p paladin-ai --doc AssistantService` prints `^test result: ok. [1-9][0-9]* passed` with zero failed and zero ignored.
    - `bash scripts/check-public-api-examples.sh --list | grep -c MISSING` is exactly 1, and that one row names the retention service.
    - `cargo doc --workspace --no-deps` produces no line containing a warning marker.
    - `cargo test --workspace --doc` reports zero failed.
    - The example uses the shipped in-memory assistant repository by its real type name and constructs the validator rather than bypassing it.
    - The SUMMARY states how the registries value was built and whether any construction path was missing.
    - The measurement capture records all three command lines with their output.
    - `git diff HEAD~2 -- src/ crates/` shows changes on comment lines only.
  </acceptance_criteria>
  <done>Six of the seven facade services are satisfied, exactly one entry point remains, and the workspace documentation bar is still at zero.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Created by **this plan**:
- `# Examples` doc sections on `RunEventStreamService` and `AssistantService` (doc comments on
  existing public structs — no new public item)
- `.planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-10-services-measure.txt` (new file)

New symbols created elsewhere in Phase 36.1 (so drift verification does not flag them): the
`pub(crate)` visibility of `ToolResultFormatter::sanitize_tool_text` and the `fail_run_`-prefixed
pinning test (36.1-02); the twelve port-trait sections (36.1-05 through 36.1-07); the sections on
`ScheduleService` and `WebhookDeliveryService` (36.1-08), `RunSubmissionService` and
`RunInspectorService` (36.1-09) and `WaypointRetentionService` (36.1-11); the
`make check-api-examples` target, the hook id `check-api-examples` and the CI lint step named "Check
public-API `# Examples` headings" (36.1-11); the files `36.1-ENTRY-POINTS.md`, `36.1-EVIDENCE.md`,
`36.1-CI-EVIDENCE.md` and `COVERAGE.md`; requirement ids `CURR-16` through `CURR-21`; `WINDOWS.md`
rows 39 through 50.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| example code → reader's own wiring | a reader copies the construction sequence into their own application |
| doctest process → event stream | an example that awaits an event can hang the suite rather than fail it |
| doctest requirement → production source | an example that will not compile tempts an edit to production code |
| measured missing count → the wiring plan's single-commit rule | plan 36.1-11 depends on exactly one item remaining |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36.1-39 | Denial of Service | the event stream doctest | high | mitigate | A doctest that awaits a stream event that never arrives hangs the whole doctest suite instead of failing, which in turn hangs the documentation gate and CI. The action forbids stream awaits, spawned listeners and sleeps; the acceptance criteria assert their absence and the verify runs under an explicit timeout. |
| T-36.1-40 | Tampering | production source under doctest pressure | high | mitigate | The unverified registries collaborator is a likely point for an executor to add a derived default or a constructor to production code. The action forbids it, offers the not-run fence as the honest fallback, and the acceptance criteria assert the diff is comment lines only. |
| T-36.1-41 | Spoofing | a heading over a skipped block | high | mitigate | The gate script checks heading text only, and the workspace already contains one satisfied row whose block is skipped. Every acceptance criterion asserts a non-zero passed count from the doctest runner (D-10). |
| T-36.1-42 | Repudiation | the remaining missing count | high | mitigate | Plan 36.1-11 must land the last fix and the gate wiring in one commit; a count other than one breaks that ordering. The action requires the executor to stop and record any other figure rather than reconcile it, and the verify asserts the exact value. |
| T-36.1-43 | Information Disclosure | example placeholder values | medium | mitigate | Assistant records and event payloads in an example could carry plausible-looking secrets. Neither construction needs a credential; placeholder strings use obviously fake values. |
| T-36.1-SC | Tampering | cargo package installs | low | accept | No package-manager install runs; no manifest or lockfile changes. |
</threat_model>

<verification>
- Two filtered doctest runs each report a non-zero passed count with zero failed and zero ignored, and each returns within its timeout.
- The public-API examples script's missing count is exactly 1 and names the retention service.
- `cargo doc --workspace --no-deps` emits no warning line and `cargo test --workspace --doc` reports zero failed.
- Both touched files' diffs are confined to comment lines, and no production item was added anywhere.
- The plan's SUMMARY carries a `## Self-Check` section and states how the registries value was built.
</verification>

<success_criteria>
- Two more of the seven facade services ROADMAP SC2 names carry a plural heading backed by a doctest the suite runs.
- Exactly one entry point remains missing, measured and recorded, so the wiring plan's single-commit rule is workable.
- No production code was changed to make documentation compile, and no doctest can hang the suite.
</success_criteria>

<output>
Create `.planning/phases/36.1-deferred-items-closure/36.1-10-SUMMARY.md` when done, recording: the commit SHAs, the observed `test result` line for each filtered doctest run, how the registries value was built, the exact remaining missing count with the item it names, the workspace documentation-build warning count, and a `## Self-Check` section.
</output>
