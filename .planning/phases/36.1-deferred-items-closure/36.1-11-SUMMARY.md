---
phase: 36.1-deferred-items-closure
plan: 11
subsystem: docs
tags: [rustdoc, doctest, api-examples, makefile, pre-commit, ci, gate]

# Dependency graph
requires:
  - phase: 36.1-deferred-items-closure
    provides: "plan 36.1-01's `scripts/check-public-api-examples.sh` gate script; plans 36.1-05 through 36.1-10's eighteen prior `# Examples` sections, leaving `WaypointRetentionService` as the sole MISSING entry point"
provides:
  - "`# Examples` doctest on `WaypointRetentionService` (src/application/services/waypoint_retention.rs) -- the 19th and last MISSING entry point, closing SC2's fix half"
  - "`make check-api-examples` (gate mode) wired into the `clean-code` dependency list"
  - "`.pre-commit-config.yaml` `check-api-examples` pre-push hook, directly after `doc-check`"
  - "One new CI `lint`-job step, directly after \"Check documentation (all features, -D warnings)\", invoking the script"
  - "`.planning/phases/36.1-deferred-items-closure/36.1-ENTRY-POINTS.md` -- the refrozen 101-row snapshot at closure (D-11)"
  - "Re-pointed citations in `.planning/codebase/CONVENTIONS.md` and the script's own header comment, from the archived 76-item Phase 16 freeze to the new 101-item snapshot"
affects: ["36.1-12", "36.1-13", "36.1-14"]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Three-place gate wiring (make target + pre-push hook + CI step) copied verbatim in placement and voice from the existing `doc-check`/rustdoc bar (D-12)"
    - "The nineteenth fix and the gate wiring land in one commit so no commit in history has the gate wired and failing (D-12, Phase 36 ordering precedent)"

key-files:
  created:
    - .planning/phases/36.1-deferred-items-closure/36.1-ENTRY-POINTS.md
  modified:
    - src/application/services/waypoint_retention.rs
    - Makefile
    - .pre-commit-config.yaml
    - .github/workflows/ci.yml
    - .planning/codebase/CONVENTIONS.md
    - scripts/check-public-api-examples.sh

key-decisions:
  - "The nineteenth `# Examples` section and all three wiring edits landed in one commit (b86a706b), per D-12's ordering rule -- verified all four checks green (doctest, gate script, make target, pre-push hooks) before staging, so no commit exists where the gate is wired and red."
  - "CURR-17 stays pending, per the plan's own scope and the orchestrator's explicit instruction: this plan closes SC2's tooling half (fix + wire + refreeze), but CURR-17's full satisfaction also needs plan 36.1-12's PROJECT.md correction (D-13), which has not landed yet. No REQUIREMENTS.md edit was made in this plan."

patterns-established:
  - "A `pub *Service`/`*Port`/`*Builder` doctest that only needs construction (no live call) mirrors the sibling `ChronicleService::new` example in the same directory -- Arc-wrap the shipped in-memory adapter, build the config by struct literal, construct the service, no async runtime needed."

requirements-completed: []

coverage:
  - id: D1
    description: "WaypointRetentionService carries a compiling `# Examples` doctest, closing the last of 19 MISSING entry points"
    verification:
      - kind: unit
        ref: "cargo test -p paladin-ai --doc WaypointRetentionService"
        status: pass
    human_judgment: false
  - id: D2
    description: "The public-API `# Examples` gate is wired in three places (make target, pre-push hook, CI lint step) and exits 0 in gate mode at 101/101 OK"
    verification:
      - kind: unit
        ref: "bash scripts/check-public-api-examples.sh"
        status: pass
      - kind: unit
        ref: "make check-api-examples"
        status: pass
      - kind: integration
        ref: "pre-commit run doc-check --hook-stage pre-push; pre-commit run check-api-examples --hook-stage pre-push; pre-commit run check-api-surface --hook-stage pre-push"
        status: pass
    human_judgment: false
  - id: D3
    description: "The nineteenth fix and the three-place wiring land in exactly one commit carrying exactly four files"
    verification:
      - kind: other
        ref: "git log -1 --name-only for commit b86a706b lists exactly Makefile, .pre-commit-config.yaml, .github/workflows/ci.yml, src/application/services/waypoint_retention.rs"
        status: pass
    human_judgment: false
  - id: D4
    description: "The refrozen 101-row entry-point snapshot is recorded at 36.1-ENTRY-POINTS.md, generated from the script's report mode, and both stale references (CONVENTIONS.md, script header) are re-pointed to it; the archived Phase 16 file is untouched"
    verification:
      - kind: unit
        ref: "grep checks against 36.1-ENTRY-POINTS.md, CONVENTIONS.md, scripts/check-public-api-examples.sh; git status --porcelain -- .planning/milestones/"
        status: pass
      - kind: unit
        ref: "make lint-shell; shellcheck scripts/check-public-api-examples.sh"
        status: pass
    human_judgment: false
  - id: D5
    description: "A real CI run on the pushed branch records the new lint-job step's result (T-36.1-44's DoS mitigation completes)"
    verification: []
    human_judgment: true
    rationale: "Per D-12 and orchestrator instruction #11: the maintainer pushes; the real CI run is recorded by plan 36.1-14 after the push. This plan verified the gate locally (script, make target, pre-push hooks) but did not and must not push."

# Metrics
duration: ~11min
completed: 2026-09-18
status: complete
---

# Phase 36.1 Plan 11: The Nineteenth `# Examples` Section and the Three-Place Gate Wiring, Refrozen Summary

**Closed the last MISSING `# Examples` entry point (`WaypointRetentionService`) and wired `scripts/check-public-api-examples.sh` into `make clean-code`, the pre-push hook set, and the CI lint job -- all in one commit -- then recorded the refrozen 101-item set and re-pointed the two stale references to it.**

## Performance

- **Duration:** ~11 min
- **Started:** 2026-09-18T05:02:00Z (session start, per STATE.md)
- **Completed:** 2026-09-18T05:13:00Z
- **Tasks:** 2
- **Files modified:** 6 modified, 1 created

## Accomplishments
- `WaypointRetentionService` gained a compiling `# Examples` doctest (construction-only, mirroring the sibling `ChronicleService::new` example), closing the last of the 19 entry points Phases 22-27 added without one.
- `scripts/check-public-api-examples.sh`'s gate mode is now wired in the exact three places the rustdoc bar (`doc-check`) is wired: `make check-api-examples` in the `clean-code` dependency list, a `.pre-commit-config.yaml` pre-push hook directly after `doc-check` with the same file filter, and one new CI `lint`-job step directly after the all-features documentation step.
- The refrozen set at closure -- 101 entry points, all `OK` -- is recorded verbatim in `.planning/phases/36.1-deferred-items-closure/36.1-ENTRY-POINTS.md`, generated from the script's own `--list` report mode at the wiring commit.
- Both stale references to the archived 76-item Phase 16 freeze (`.planning/codebase/CONVENTIONS.md`'s heading-rule paragraph and the script's own header comment) now cite the new snapshot; the archived Phase 16 file itself is untouched.

## Task Commits

Each task was committed atomically:

1. **Task 1: The nineteenth section and the three-place gate wiring, in one commit** - `b86a706b` (build)
2. **Task 2: Record the refrozen entry-point set and re-point the two stale references** - `3e1441cc` (docs)

**Plan metadata:** pending (this commit)

## Files Created/Modified
- `src/application/services/waypoint_retention.rs` - Added the 19th `# Examples` doctest, construction-only, using the shipped `InMemoryWaypointStore` and a struct-literal `WaypointRetentionConfig`
- `Makefile` - New `.PHONY: check-api-examples` target (gate mode, labelled echo matching `doc-check`'s style); `clean-code` now depends on it after `doc-check`
- `.pre-commit-config.yaml` - New `check-api-examples` hook at `stages: [pre-push]`, directly after `doc-check`, same `files:` filter, comment in the same voice
- `.github/workflows/ci.yml` - New `lint`-job step "Check public-API `` `# Examples` `` headings", directly after "Check documentation (all features, -D warnings)"
- `.planning/phases/36.1-deferred-items-closure/36.1-ENTRY-POINTS.md` - New file: the 101-row snapshot generated from `--list` at commit `b86a706b`, with a header stating live derivation is the enforcement and this file is the snapshot
- `.planning/codebase/CONVENTIONS.md` - Re-pointed the heading-rule paragraph's citation and count (76 -> 101), added a clause naming the three wiring locations
- `scripts/check-public-api-examples.sh` - Re-pointed the header comment's citation from the archived Phase 16 file to the new snapshot

## Decisions Made
- **Single-commit ordering (D-12):** verified `cargo test -p paladin-ai --doc WaypointRetentionService`, `bash scripts/check-public-api-examples.sh`, `make check-api-examples`, and the `doc-check`/`check-api-examples`/`check-api-surface` pre-push hooks all green *before* staging, then made exactly one commit carrying all four wiring files. No intermediate commit exists where the gate is wired and failing.
- **CURR-17 left pending:** this plan satisfies SC2's fix/wire/refreeze work, but CURR-17 (per orchestrator instruction #9 and D-13) also needs plan 36.1-12's PROJECT.md correction before it is fully satisfied. No `REQUIREMENTS.md` edit was made here.
- **Doctest scope kept minimal:** the `WaypointRetentionService` example constructs the service from the shipped in-memory port and a struct-literal config and stops there (no `prune()` call) -- "construction plus at most one cheap call" per the plan's action text, and consistent with the sibling `ChronicleService::new` example in the same crate.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None. The full `pre-commit run --hook-stage pre-push` sweep (which also runs `cargo-clippy`, `cargo-build-push`, `cargo-test-lib-push`) was not run in its entirety, as those hooks are unrelated to this plan's four changed files and the literal `<verify><automated>` block does not require it; instead the three hooks directly relevant to this plan's changes (`doc-check`, `check-api-examples`, `check-api-surface`) were run explicitly against the changed files and all passed. `cargo check --workspace --all-targets --all-features` was also run and is clean.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- SC2's tooling half (ROADMAP disposition: fix the nineteen, wire the gate, refreeze the record) is complete. `bash scripts/check-public-api-examples.sh --list` now reports 0 MISSING, 0 SINGULAR, 101/101 OK.
- Plan 36.1-12 still owns the PROJECT.md dated correction (D-13) that CURR-17's full closure depends on.
- Plan 36.1-14 still owns recording a real pushed-branch CI run of the new lint-job step in `36.1-CI-EVIDENCE.md` before the phase is verified (T-36.1-44's mitigation is local-only until that push happens); this executor did not and must not push.

---
*Phase: 36.1-deferred-items-closure*
*Completed: 2026-09-18*

## Self-Check: PASSED

- FOUND: src/application/services/waypoint_retention.rs (doctest present, `cargo test -p paladin-ai --doc WaypointRetentionService` passes)
- FOUND: Makefile (`check-api-examples` target present, `grep -c check-api-examples Makefile` = 3)
- FOUND: .pre-commit-config.yaml (`check-api-examples` hook present at pre-push, `grep -c check-api-examples .pre-commit-config.yaml` = 2)
- FOUND: .github/workflows/ci.yml (`grep -c check-public-api-examples .github/workflows/ci.yml` = 1)
- FOUND: .planning/phases/36.1-deferred-items-closure/36.1-ENTRY-POINTS.md (101 OK rows)
- FOUND: .planning/codebase/CONVENTIONS.md (re-pointed, no `16-DOCS-03-ENTRY-POINTS` citation remains)
- FOUND: scripts/check-public-api-examples.sh (re-pointed, no `16-DOCS-03-ENTRY-POINTS` citation remains, shellcheck clean)
- FOUND: commit b86a706b331a080ff5ce88a97e6f0d337275135e (`git log --oneline --all | grep b86a706b` matches)
- FOUND: commit 3e1441cc718c0a9be8bfb92b84169c70e85b2ade (`git log --oneline --all | grep 3e1441cc` matches)
- FOUND: `bash scripts/check-public-api-examples.sh` exits 0, reports 101 OK, 0 MISSING, 0 SINGULAR
- FOUND: `make check-api-examples` exits 0
- FOUND: `make lint-shell` exits 0 with the script staged
