---
phase: 36.1-deferred-items-closure
plan: 12
subsystem: docs
tags: [project-corpus, adr, todos, requirements, changelog, rustdoc-suppressions]

# Dependency graph
requires:
  - phase: 36.1-deferred-items-closure/02
    provides: "the ToolErrorMode::FailRun sanitizer fix (D-03) this plan's changelog Fixed bullet describes"
  - phase: 36.1-deferred-items-closure/03
    provides: "contributing-providers.md and cli-configuration.md corrections this plan's changelog Documentation bullet summarizes"
  - phase: 36.1-deferred-items-closure/04
    provides: "testing-guide.md tree correction and the OpenAIAdapter casing sweep, summarized in the same bullet"
  - phase: 36.1-deferred-items-closure/11
    provides: "the nineteenth # Examples section and the make check-api-examples gate, summarized in the same bullet"
provides:
  - "PROJECT.md's stale 47-file / 4-target / 'no crate ships examples/' claim corrected in place at both occurrences (D-13), verified against the tree: 62 .rs files, 8 declared [[example]] targets, paladin-llm named as the one crate with its own examples/ directory"
  - "ADR-0033 dated amendment recording the eight kept crate-level rustdoc suppressions with file:line, lint, per-crate hidden-diagnostic count (309 total), structural reason, owner and re-check trigger (D-29) -- no .rs file changed, cargo doc still zero warnings"
  - "Both todos/pending/ files dispositioned in place with owner/deferred_past/recheck_by/dispositioned_by frontmatter and a dated Disposition section (D-22, D-23, D-24), still surfacing via gsd-tools list-todos"
  - "REQUIREMENTS.md v2 Platform & Tooling gained FUT-10, naming the RustFS adapter evaluation as a v0.11.0 candidate pointing at its todo file (D-24)"
  - "CHANGELOG.md [0.10.0] gained one Fixed bullet (tool-failure reason redaction) and one Documentation bullet (six page fixes, 19 examples, the new gate, the ADR-0033 amendment), both reader-facing with no internal identifier (D-26)"
affects: ["36.1-13 (WINDOWS.md ledger reconciliation, which mints the waived rows this plan's disposition text matches)", "37 (release readiness -- these six files are part of the pre-tag closing state)"]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Dated italic-parenthetical correction appended in place at both PROJECT.md occurrences, matching the file's own existing correction house style rather than rewriting the original stale sentence"
    - "ADR amendment appended as a new bold-lead paragraph inside the existing Context section, mirroring the 2026-08-24 Phase 16 amendment's own heading/dating convention, rather than a new top-level ## section"

key-files:
  created: []
  modified:
    - .planning/PROJECT.md
    - .planning/decisions/0033-cargo-doc-warning-bar.md
    - .planning/todos/pending/2026-08-13-verify-local-coverage-reproduction.md
    - .planning/todos/pending/2026-09-13-evaluate-rustfs-replacement-for-minio.md
    - .planning/REQUIREMENTS.md
    - CHANGELOG.md

key-decisions:
  - "All three corpus figures (62 .rs files, 8 declared [[example]] targets, paladin-llm as the sole crate with its own examples/ directory) were re-measured against the tree this session (find, grep, find -maxdepth 2 -type d) before writing, per D-13's requirement, rather than trusted from the plan's own stated figures"
  - "The ADR-0033 amendment's per-crate table lists the facade's three attributes (broken_intra_doc_links, redundant_explicit_links, invalid_html_tags) as sharing one 108-diagnostic count and paladin-ports' two attributes as sharing one 119-diagnostic count, since the Phase 36 measurement capture reports per-crate summary lines, not per-attribute breakdowns -- the total (309) is unaffected"
  - "All eight attribute sites were re-confirmed present and unchanged at their recorded file:line via a direct grep before the amendment was written, and cargo doc --workspace --no-deps was re-run after both Task 1 commits, confirming zero warning: lines both times"
  - "REQUIREMENTS.md's new v2 line takes the next available FUT number (FUT-10) rather than reusing or renumbering FUT-06..09, which are already owned/reserved elsewhere in the file"

patterns-established: []

requirements-completed: [CURR-17, CURR-20, CURR-21]

coverage:
  - id: D1
    description: "Both PROJECT.md occurrences of the stale examples claim carry a dated correction naming 62 .rs files, 8 declared [[example]] targets, and paladin-llm as the one crate shipping its own examples/ directory"
    requirement: "CURR-17"
    verification:
      - kind: other
        ref: "grep -c 'Corrected 2026-09-18' .planning/PROJECT.md == 2; grep -c 'paladin-llm' .planning/PROJECT.md == 18; find examples -name '*.rs' | wc -l == 62; grep -c '^[[example]]' Cargo.toml == 8"
        status: pass
    human_judgment: false
  - id: D2
    description: "ADR-0033 carries a dated amendment naming all eight crate-level rustdoc suppressions across five library files with per-crate hidden-diagnostic counts, the structural reason, and the bar's with-suppressions-in-force scope statement; no .rs file changed and cargo doc stays warning-free"
    requirement: "CURR-17"
    verification:
      - kind: other
        ref: "grep -q '2026-09-18' .planning/decisions/0033-cargo-doc-warning-bar.md; grep -cE 'lib\\.rs' .planning/decisions/0033-cargo-doc-warning-bar.md >= 5; cargo doc --workspace --no-deps 2>&1 | grep -c 'warning:' == 0; git diff --stat HEAD~2 -- src/ crates/ tests/ docs/ (empty)"
        status: pass
    human_judgment: false
  - id: D3
    description: "Both pending todos gain owner/deferred_past/recheck_by/dispositioned_by frontmatter and a dated Disposition section, no resolves_phase tag, and still surface via gsd-tools list-todos with unchanged title/area/severity"
    requirement: "CURR-20"
    verification:
      - kind: other
        ref: "node .claude/gsd-core/bin/gsd-tools.cjs query list-todos -> count 2, both titles/areas/severities unchanged; grep -c '^owner:' == 1 in each file; grep -c 'recheck_by: 2026-10-16' == 1 in each file"
        status: pass
    human_judgment: false
  - id: D4
    description: "REQUIREMENTS.md v2 Platform & Tooling gains exactly one new line naming the RustFS adapter evaluation and its todo path"
    requirement: "CURR-20"
    verification:
      - kind: other
        ref: "grep -q RustFS .planning/REQUIREMENTS.md; git diff .planning/REQUIREMENTS.md touches only the Platform & Tooling subsection (2 added lines)"
        status: pass
    human_judgment: false
  - id: D5
    description: "CHANGELOG.md [0.10.0] gains one Fixed bullet and one Documentation bullet, both reader-facing, no internal identifier, subsection counts unchanged (2 Documentation headings, 9 Fixed headings across the whole file)"
    requirement: "CURR-21"
    verification:
      - kind: other
        ref: "grep -cE 'CURR-[0-9]+|RD-[0-9]+|EX-[0-9]+|WINDOWS' CHANGELOG.md == 0; grep -c '^### Documentation' CHANGELOG.md == 2; grep -c '^### Fixed' CHANGELOG.md == 9; git diff CHANGELOG.md is additions only"
        status: pass
    human_judgment: false

duration: ~20min
completed: 2026-09-18
status: complete
---

# Phase 36.1 Plan 12: Corpus corrections, ADR-0033 amendment, todo disposition and changelog entries Summary

**Corrected PROJECT.md's stale examples figures at both occurrences, amended ADR-0033 with the eight kept crate-level rustdoc suppressions, dispositioned both pending todos with an owner and a re-check date, added one v2 requirements line, and appended two reader-facing changelog bullets — no `.rs` file touched, `cargo doc --workspace --no-deps` still zero warnings.**

## Performance

- **Duration:** ~20 min
- **Tasks:** 3
- **Files modified:** 6

## Accomplishments
- `.planning/PROJECT.md` corrected in place at both the Phase 4 amendment block and the validated-requirements bullet: 47 → 62 `.rs` files under `examples/`, 4 → 8 declared `[[example]]` targets, and "no crate ships its own `examples/` directory" → `paladin-llm` named as the one that does (`crates/paladin-llm/examples/live_vendor_smoke.rs`). All three figures verified against the tree before writing.
- `.planning/decisions/0033-cargo-doc-warning-bar.md` gained a dated amendment ("Amendment, 2026-09-18, Phase 36.1 / plan 36.1-12 — Known crate-level suppressions") listing all eight `#![allow(rustdoc::...)]` attributes across five crates, a per-crate hidden-diagnostic table (309 total: ports 119, facade 108, llm 69, storage 10, notifications 3), the cross-crate-doc-link structural reason, and the plain statement that the zero-warning bar measures the tree with these allows in force. No `.rs` file changed; `cargo doc --workspace --no-deps` confirmed zero `warning:` lines both before and after.
- Both `.planning/todos/pending/` files gained `owner:`, `deferred_past: v0.10.0`, `recheck_by: 2026-10-16`, `dispositioned_by: Phase 36.1 (2026-09-18)` frontmatter and a dated `## Disposition (Phase 36.1, 2026-09-18)` section — the coverage-walk todo records the confirmed absence of a container runtime here (`which docker` empty, `cargo-llvm-cov` present) and re-points its comparison target at the Phase 37 pre-merge CI coverage run; the RustFS todo records its documentation half as settled and re-homes the adapter evaluation itself to the maintainer. Neither file gained a `resolves_phase` tag, and both still appear in `gsd-tools list-todos` with unchanged title/area/severity.
- `.planning/REQUIREMENTS.md` gained `FUT-10` under `## v2 Requirements` → `### Platform & Tooling`, naming the RustFS `FileStoragePort` adapter evaluation as a v0.11.0 candidate and pointing at its todo file.
- `CHANGELOG.md`'s `[0.10.0]` section gained one `### Fixed` bullet (the `ToolErrorMode::FailRun` reason is now redacted and bounded like the model-facing path) and one `### Documentation` bullet (six corrected mdBook pages, nineteen new worked `# Examples`, the new `make check-api-examples` gate, and the ADR-0033 known-suppressions amendment) — both reader-facing, neither naming a register entry, ledger row or requirement identifier.

## Task Commits

Each task was committed atomically:

1. **Task 1a: Correct both stale corpus statements** - `45454138` (docs)
2. **Task 1b: Amend ADR-0033 with the kept rustdoc suppressions** - `b9bacc58` (docs)
3. **Task 2a: Disposition both pending todos** - `01640b59` (chore)
4. **Task 2b: Add the v2 RustFS requirement line** - `dcdc6138` (docs)
5. **Task 3: Append the changelog bullets** - `15bdb575` (docs)

**Plan metadata:** (this commit)

## Files Created/Modified
- `.planning/PROJECT.md` - two dated in-place corrections of the stale examples figures
- `.planning/decisions/0033-cargo-doc-warning-bar.md` - dated amendment listing the eight kept crate-level rustdoc suppressions
- `.planning/todos/pending/2026-08-13-verify-local-coverage-reproduction.md` - owner/deferred_past/recheck_by/dispositioned_by frontmatter and a Disposition section
- `.planning/todos/pending/2026-09-13-evaluate-rustfs-replacement-for-minio.md` - owner/deferred_past/recheck_by/dispositioned_by frontmatter and a Disposition section
- `.planning/REQUIREMENTS.md` - one new v2 Platform & Tooling line (FUT-10)
- `CHANGELOG.md` - one new `### Fixed` bullet and one new `### Documentation` bullet under `[0.10.0]`

## Decisions Made
- Verified figures were re-measured directly rather than transcribed from the plan text: `find examples -name '*.rs' | wc -l` = 62, `grep -c '^[[example]]' Cargo.toml` = 8, `find crates -maxdepth 2 -type d -name examples` = `crates/paladin-llm/examples` only.
- The ADR-0033 amendment table groups the facade's three attributes under one shared 108-count and `paladin-ports`' two attributes under one shared 119-count, matching how the Phase 36 closing measurement reported per-crate (not per-attribute) summary lines; the 309 total is unaffected by this grouping choice.
- `FUT-10` was chosen as the next available number in the v2 requirements file rather than reusing `FUT-06`..`FUT-09`, which are already owned or reserved for other subjects.
- The Fixed and Documentation changelog bullets were each written as a single bullet (not split across several), matching D-26's "one bullet per subsection" instruction even though each covers several sub-topics.

## Deviations from Plan

None - plan executed exactly as written. All `must_haves.prohibitions` held: no `.rs` file was modified (`git diff --stat 417eb7da..HEAD` touches only the six planned files), no rustdoc suppression attribute was added or removed (all eight re-confirmed present, byte-identical, before the amendment was written), neither todo file moved, was renamed, or gained a `resolves_phase` tag, no changelog bullet names a register/ledger/requirement identifier, `.planning/WINDOWS.md` was not touched, and `git push` was never run.

## Issues Encountered
None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- All six `files_modified` from the plan frontmatter landed exactly as scoped (`git diff --stat 417eb7da..HEAD` shows only these six files).
- `cargo doc --workspace --no-deps` remains warning-free; `make api-surface` is unaffected (no `.rs` file touched).
- Plan 36.1-13 (WINDOWS.md ledger reconciliation) can now mint its `lint-warning` row for the ADR-0033 finding and its `todo` rows for both dispositioned todos, using the exact owner/re-check wording this plan recorded so the two records agree.
- `CURR-17`, `CURR-20`, `CURR-21` are ready to be marked complete in `.planning/REQUIREMENTS.md` (this plan's `requirements` frontmatter); done via the standard state-update step below.

## Self-Check: PASSED

- `.planning/PROJECT.md` exists and carries the correction at both occurrences (`grep -c 'Corrected 2026-09-18'` = 2). FOUND.
- `.planning/decisions/0033-cargo-doc-warning-bar.md` exists and carries the 2026-09-18 amendment. FOUND.
- `.planning/todos/pending/2026-08-13-verify-local-coverage-reproduction.md` exists with the four new frontmatter keys and a Disposition section. FOUND.
- `.planning/todos/pending/2026-09-13-evaluate-rustfs-replacement-for-minio.md` exists with the four new frontmatter keys and a Disposition section. FOUND.
- `.planning/REQUIREMENTS.md` carries `FUT-10`. FOUND.
- `CHANGELOG.md` carries both new bullets under the existing `[0.10.0]` subsections. FOUND.
- Commits `45454138`, `b9bacc58`, `01640b59`, `dcdc6138`, `15bdb575` all present in `git log --oneline --all`. FOUND (verified via `git log --oneline -6`).
- `cargo doc --workspace --no-deps` re-run after all commits: zero `warning:` lines. CONFIRMED.
- `git diff --stat 417eb7da..HEAD` confined to the six `files_modified` paths. CONFIRMED.

No missing items.

---
*Phase: 36.1-deferred-items-closure*
*Completed: 2026-09-18*
