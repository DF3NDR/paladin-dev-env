---
phase: 36.1-deferred-items-closure
plan: 13
subsystem: infra
tags: [windows-ledger, closure-table, release-readiness, rustdoc, api-surface, gates]

# Dependency graph
requires:
  - phase: 36.1-deferred-items-closure/01
    provides: "the seeded 17-line 36.1-EVIDENCE.md closure table skeleton and the cli_isolation register entry closed end to end"
  - phase: 36.1-deferred-items-closure/02
    provides: "the ToolErrorMode::FailRun sanitizer fix and its pinning test (D-03), closing WINDOWS.md row 38"
  - phase: 36.1-deferred-items-closure/03
    provides: "contributing-providers.md, cli-configuration.md and cicd.md corrections (D-04, D-06, D-08)"
  - phase: 36.1-deferred-items-closure/04
    provides: "the testing-guide.md tree rebuild, the OpenAIAdapter casing sweep, and the four-body D-07 scratch-compile probe verdict (all fail)"
  - phase: 36.1-deferred-items-closure/11
    provides: "the nineteenth # Examples section and the three-place check-api-examples gate wiring, plus the refrozen 36.1-ENTRY-POINTS.md snapshot"
  - phase: 36.1-deferred-items-closure/12
    provides: "the PROJECT.md corpus correction, the ADR-0033 kept-suppressions amendment, and both todos' owner/recheck-date frontmatter"
provides:
  - "WINDOWS.md reconciled with every open Phase 31/32/34/35/36 deferred-item register entry: sixteen new rows (ids 40-55: 11 fixed, 5 waived) plus row 38 marked fixed; open_count: 0, total_count: 55"
  - "36.1-EVIDENCE.md closure table complete: all seventeen seeded lines plus four D-07 fallback lines (21 total), every disposition filled, the Phase 35 testing-guide.md:96 allowlist row retired, a findings section, and a spec-less probe note"
  - "36.1-evidence/36.1-13-closing-gates.txt: all seven D-25 closing gates run in order on the closing commit stack, captured verbatim, including a transient Gate 4 failure recorded honestly alongside its clean re-run"
  - "make api-surface confirmed unchanged (3959 items) on the phase's final commit"
affects: ["37 (release readiness — /gsd-ship reads open_count and this closure table)"]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Ledger mutation entirely through gsd-tools windows append/waive/fixed — every fixed row's description carries its closing commit SHA and plan number (windows fixed takes no reason argument), every waived row's reason ends with an owner and a re-check date/trigger"
    - "A gate's first, genuinely-failing attempt is recorded verbatim alongside a full unfiltered re-run when the failure looks environmental — never a selective re-run of only the failing subset to manufacture a green record"

key-files:
  created:
    - .planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-13-closing-gates.txt
  modified:
    - .planning/WINDOWS.md
    - .planning/phases/36.1-deferred-items-closure/36.1-EVIDENCE.md

key-decisions:
  - "An accidental real ledger row (id 39) was minted while probing the gsd-tools windows append CLI's required-flag syntax before any real mutation was intended — the tool has no dry-run mode. Waived immediately with a reason naming it an executor error, not a register defect. The ledger's total_count therefore reads 55 (38 pre-existing + 1 accidental + 16 real new rows), not the phase's originally expected 54 (38 + 12 + 4). Documented in WINDOWS.md itself, in 36.1-EVIDENCE.md's Findings section, and here."
  - "Gate 4 (cargo test --workspace) failed on its first attempt: three run/cancel and run/stream tests hit their 30-second timeout, immediately after two CPU-heavy gates (make clean-code, make security) ran back to back in this devcontainer. None of the failing files are in this phase's files_modified. Per D-25's 'record it as failing, do not re-run selectively' instruction, the first failing run is recorded verbatim; a full, unfiltered second run of the identical command (not a selective re-run of the three failing tests) then confirmed 0 failures, which is recorded alongside it as diagnostic evidence of an environmental flake rather than a regression."
  - "The two todos' ledger rows use ledger status waived (the schema has no 're-homed' status) but the closure table's own disposition column uses re-homed for those two lines specifically, per the plan's own vocabulary distinction between ledger status and closure-table disposition."
  - "Row 39's accidental mint means the ledger's actual row range for this phase's real work is 40-55, not 39-50 as RESEARCH.md predicted before the D-07 fallback's four extra rows were known; the plan's own text anticipated this ('if plan 36.1-01/36.1-04 reported an extra finding, mint one additional row per finding... state the final row count explicitly rather than assuming twelve') and the same allowance covers the accidental row's effect on the numbering."

patterns-established: []

requirements-completed: [CURR-19, CURR-21]

coverage:
  - id: D1
    description: "Every open register entry across the Phase 31, 32, 34, 35 and 36 deferred-item registers has exactly one WINDOWS.md row (fixed or waived, with a commit SHA or an owner+re-check reason); row 38 is fixed; open_count reaches 0"
    requirement: "CURR-19"
    verification:
      - kind: other
        ref: "node .claude/gsd-core/bin/gsd-tools.cjs query windows status → open_count: 0, total_count: 55"
        status: pass
    human_judgment: false
  - id: D2
    description: "36.1-EVIDENCE.md's closure table has no pending cell, covers every register entry and todo with a disposition/ledger-row/commit/verification/anchor, retires the Phase 35 allowlist row, and carries a findings section and a spec-less probe note"
    requirement: "CURR-19"
    verification:
      - kind: other
        ref: "grep -c 'pending' 36.1-EVIDENCE.md = 0; grep -q 'covered-by-row'/'closed-by-drift'/'re-homed'/'row 38' all present"
        status: pass
    human_judgment: false
  - id: D3
    description: "All seven D-25 closing gates ran in the decided order on the closing commit, captured verbatim; make api-surface unchanged; no gate output edited or selectively re-run to fake a green record"
    requirement: "CURR-21"
    verification:
      - kind: other
        ref: ".planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-13-closing-gates.txt (all 7 gates, exit statuses and headline figures); make api-surface exit 0, unchanged, 3959 items"
        status: pass
    human_judgment: false
  - id: D4
    description: "The transient Gate 4 (cargo test --workspace) failure is a genuine, honestly-recorded observation, not a hidden or fabricated result — a human should confirm this reading is reasonable before the phase is sealed for release"
    verification: []
    human_judgment: true
    rationale: "The classification of the three timeouts as an environmental flake (vs. a real intermittent regression) rests on this executor's judgment — resource contention immediately after two CPU-heavy gates, files outside this phase's scope, and a clean full re-run. A human reviewing the release-readiness record should confirm this reasoning before /gsd-ship treats the closing gates as green."

duration: ~28min
completed: 2026-09-18
status: complete
---

# Phase 36.1 Plan 13: Ledger Reconciliation and Closing Gates Summary

**Reconciled `WINDOWS.md` with every open Phase 31/32/34/35/36 deferred-item register entry (sixteen new rows plus row 38 resolved, `open_count: 0`), completed the `36.1-EVIDENCE.md` closure table, and ran all seven D-25 closing gates on the closing commit — recording one transient, environmental `cargo test --workspace` failure honestly alongside its clean re-run rather than hiding it.**

## Performance

- **Duration:** ~28 min
- **Started:** 2026-09-18T05:23:00Z (approx, file reads)
- **Completed:** 2026-09-18T05:52:00Z
- **Tasks:** 3
- **Files modified:** 3 (`WINDOWS.md`, `36.1-EVIDENCE.md`, plus one new evidence capture file)

## Accomplishments

- Minted and resolved sixteen `WINDOWS.md` rows (ids 40-55): 11 `fixed` (each description carrying its closing commit SHA and plan number, since `windows fixed <id>` takes no reason argument) and 5 `waived` (each reason ending in an owner and a re-check date or trigger). Marked row 38 (`ToolErrorMode::FailRun` raw reason) `fixed`, closed by `979f2a1d`/`e7284987` (36.1-02). Final ledger: `open_count: 0`, `waived_count: 34`, `fixed_count: 21`, `total_count: 55`.
- Completed all sixteen remaining `36.1-EVIDENCE.md` closure-table lines plus four new lines (18-21) for the D-07 fallback (`battalion-patterns-guide.md` bodies that fail the scratch-compile probe), retired the Phase 35 `35-EVIDENCE.md` allowlist row that had excused the stale `testing-guide.md:96` tree as a false positive, and added a findings section and a spec-less probe note.
- Re-ran rows 36 and 37's two verification commands live on this phase's closing commit: `cargo doc --workspace --no-deps 2>&1 | grep -c 'warning:'` → `0`; `RUSTDOCFLAGS="-D warnings" cargo doc -p paladin-memory --features content-processing --no-deps` → exit `0`.
- Ran all seven D-25 closing gates in the decided order and captured every output verbatim in `36.1-evidence/36.1-13-closing-gates.txt`: `make clean-code`, `make lint-shell`, `make security`, `cargo test --workspace`, `cargo test --workspace --all-features --no-fail-fast`, the four-step mdBook sequence, `make api-surface`. Six of seven were green on the first attempt; `cargo test --workspace` failed transiently on its first run (three tests hit a 30-second timeout, all outside this phase's `files_modified`, immediately after two CPU-heavy gates) — recorded verbatim, then a full unfiltered re-run confirmed `0 failed`.
- `make api-surface`: unchanged, `3959` items — confirms this phase's changes (docs, test bodies, make/hook/CI lines, one `pub(crate)`-only match arm, planning corpus) never moved the public surface.

## Task Commits

Each task was committed atomically:

1. **Task 1: Mint and resolve the twelve register rows, and close row 38** - `6dd9164c` (chore)
2. **Task 2: Complete the closure table** - `56434114` (docs)
3. **Task 3: Run the closing gate sequence and record it** - `e044b99b` (docs; first attempt failed the pre-commit `end-of-file-fixer` hook, which auto-corrected a trailing-newline issue and did not create a commit; re-staged and committed as a fresh commit per protocol — no `--amend` was used)

**Plan metadata:** pending (this commit)

## Files Created/Modified

- `.planning/WINDOWS.md` — sixteen new rows (ids 40-55) plus one accidental row (id 39, waived), row 38 marked fixed
- `.planning/phases/36.1-deferred-items-closure/36.1-EVIDENCE.md` — closure table completed (21 lines total), retirement note, findings section, spec-less probe note, closing-measurement section
- `.planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-13-closing-gates.txt` — new file: verbatim capture of all seven closing gates

## Decisions Made

See `key-decisions` in frontmatter for the full account of the accidental row-39 mint and the Gate 4 transient-failure classification. In brief:

- **Row 39 (accidental).** While verifying `gsd-tools windows append`'s required-flag syntax (the tool has no dry-run mode), a real ledger row was minted with placeholder text. Rather than pretend this did not happen, it was waived immediately with a reason explaining the executor error, and the whole account is documented in three places (the row's own reason, `36.1-EVIDENCE.md`'s Findings section, and here) so no reader has to reconstruct it. The ledger's `total_count` is `55`, not the phase's originally expected `54`.
- **Gate 4 transient failure.** `cargo test --workspace`'s first run failed with three 30-second-timeout panics in `application::services::run::cancel_tests` and `application::services::run::stream_tests` — files this phase never touched (D-27's exact list: two D-03 match arms and their test, nineteen doc comments, `tests/cli_isolation_test.rs`). This followed immediately after `make clean-code` (full workspace clippy build) and `make security` (advisory-db fetch, cargo-deny) ran back to back — consistent with this devcontainer's documented history of resource-exhaustion flakes (`WINDOWS.md` rows 5, 6, 13, 27). Per D-25's explicit instruction to record a failing gate rather than re-run selectively, the first run's `979 passed; 3 failed` is recorded verbatim; a full, unfiltered second run of the identical command then reported `982 passed; 0 failed`, recorded alongside it as diagnostic evidence, not as a substitute for the first observation. This is flagged `human_judgment: true` in the coverage block above — a human should confirm this reading before treating the closing gates as fully green for release.
- **Closure-table disposition vs. ledger status for the two todos.** The ledger schema has no `re-homed` status (only `open`/`waived`/`fixed`), so both todo rows carry ledger status `waived`; the closure table's own disposition column correctly reads `re-homed` for those two lines per the plan's explicit vocabulary, since the todo files themselves (not just the ledger) now carry the owner/re-check disposition.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Recovered from a Bash-tool commit failure caused by the pre-commit `end-of-file-fixer` hook**
- **Found during:** Task 3 (closing gate commit)
- **Issue:** The first `git commit` attempt for Task 3 exited 1 because the `end-of-file-fixer` pre-commit hook modified `36.1-EVIDENCE.md` (trailing-newline normalization) and, per pre-commit's contract, a hook that modifies files fails the run without creating a commit.
- **Fix:** Re-staged the already-corrected file and re-ran `git commit` as a fresh commit (no `--amend`, per the Rust completion protocol and git-safety rules).
- **Files modified:** none beyond the already-planned `36.1-EVIDENCE.md` and the new evidence capture file.
- **Verification:** Second commit attempt passed all pre-commit hooks and succeeded (`e044b99b`).
- **Committed in:** `e044b99b`

**2. [Rule 1 - Bug, self-caused] Waived an accidentally-minted ledger row rather than leave it undocumented**
- **Found during:** Task 1, immediately after minting row 39 while probing the ledger CLI's syntax
- **Issue:** `gsd-tools windows append` has no dry-run mode; a syntax-verification call became a real, permanent ledger mutation (`.planning/WINDOWS.md` rows are never deleted, per D-00b).
- **Fix:** Waived row 39 immediately with a reason naming it an executor error, not a register defect, and documented the incident in `36.1-EVIDENCE.md`'s Findings section and in this SUMMARY.
- **Files modified:** `.planning/WINDOWS.md`
- **Verification:** `node .claude/gsd-core/bin/gsd-tools.cjs query windows status` confirms `open_count: 0` and row 39 reads `waived` with the explanatory reason.
- **Committed in:** `6dd9164c`

---

**Total deviations:** 2 auto-fixed (1 blocking/tooling, 1 self-caused bug corrected transparently)
**Impact on plan:** Neither affects the phase's substantive deliverables. The row-39 incident shifts the ledger's final `total_count` from the phase's originally expected `54` to `55`, documented everywhere a reader would look. No scope creep — no `.rs`/docs/Makefile/hook/workflow file outside this plan's declared `files_modified` was touched (`git diff --stat` for each task's commit confirms this).

## Issues Encountered

- **Devcontainer resource contention affecting Gate 4.** See "Decisions Made" above. Not a defect in this phase's own changes; the second full run confirmed the workspace test suite is genuinely green.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- ROADMAP SC4 is closed: the ledger and the registers agree, every open entry has exactly one row with a disposition and a reason, and `open_count` is `0`.
- ROADMAP SC6 is measured: every closing gate ran on the closing commit with its output on disk, and the public surface did not move (`3959` items, unchanged).
- `36.1-EVIDENCE.md` is the single table the next phase (37, release readiness) can verify this one by identifier from.
- A human should review the Gate 4 transient-failure classification (`coverage` block D4 above) before `/gsd-ship` treats the closing gates as fully green — the underlying evidence (both full runs, verbatim) is on disk for that review.
- Plan 36.1-14 still owns pushing the branch and recording a real CI run of the new `check-api-examples` lint step in `36.1-CI-EVIDENCE.md`; this plan did not and must not push.

## Self-Check: PASSED

- `.planning/WINDOWS.md` — FOUND, modified; `open_count: 0`, `total_count: 55` confirmed via `gsd-tools query windows status`.
- `.planning/phases/36.1-deferred-items-closure/36.1-EVIDENCE.md` — FOUND, modified; 21 closure-table lines, 0 `pending` occurrences, `covered-by-row`/`closed-by-drift`/`re-homed`/`row 38` all present.
- `.planning/phases/36.1-deferred-items-closure/36.1-evidence/36.1-13-closing-gates.txt` — FOUND, non-empty, all seven gates present with exit statuses.
- Commit `6dd9164c` — FOUND in `git log --oneline`.
- Commit `56434114` — FOUND in `git log --oneline`.
- Commit `e044b99b` — FOUND in `git log --oneline`.
- `make api-surface` — re-confirmed exit 0, "API surface unchanged", `3959` items.
- `bash scripts/check-public-api-examples.sh` — re-confirmed exit 0, 101/101 OK.
- `git status --porcelain -- docs` — confirmed empty.
- `git diff --stat -- src/ crates/ docs/ tests/ Makefile .github/` across all three task commits — confirmed empty (no source/doc/test/CI drift from this plan).

---
*Phase: 36.1-deferred-items-closure*
*Completed: 2026-09-18*
