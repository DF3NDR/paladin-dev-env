---
phase: 36.1-deferred-items-closure
plan: 14
subsystem: infra
tags: [ci-evidence, closure, adr-0033, required-check, d-12]

# Dependency graph
requires:
  - phase: 36.1-deferred-items-closure
    provides: 36.1-13-SUMMARY.md (the closing-gate capture this plan's local figures are cited
      from), the seeded 36.1-CI-EVIDENCE.md from this plan's own Task 1
provides:
  - .planning/phases/36.1-deferred-items-closure/36.1-CI-EVIDENCE.md completed with the real
    pushed-branch CI run (35329107346) proving the new lint-job public-API `# Examples` heading
    step is green, plus the two pre-existing documentation steps, the formatting/clippy steps,
    and both unit-test-job steps (unit tests, doctests), with a filled local-vs-CI comparison
    section
affects: [37]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "A required-check change is never verified on a local measurement alone (D-12's
      Phase-33/36-derived pattern, applied a third time here): push, wait for the real run,
      record job id + step conclusions in an NN-CI-EVIDENCE.md, treat the CI figure as
      authoritative over the local one on any disagreement. This run was read live via
      `gh run view --json jobs` and cross-checked against raw per-job logs (`gh api
      .../jobs/<id>/logs`), not trusted from the green checkmark alone."

key-files:
  created: []
  modified:
    - .planning/phases/36.1-deferred-items-closure/36.1-CI-EVIDENCE.md

key-decisions:
  - "The branch push was performed by the maintainer (not this executor), per the checkpoint's
    own instructions; `origin/feature/phase-33` was confirmed to already equal local HEAD
    `6fe5b70a` before this task began, so no push command was run by this task."
  - "This continuation independently re-read the run via `gh run view 35329107346 --json jobs`
    and `gh api .../jobs/<id>/logs` rather than trusting the orchestrator's reported figures
    verbatim — every job/step conclusion and every quoted log line in the completed evidence
    file was confirmed against the live API response, not just transcribed from the checkpoint
    message."
  - "Row 7 (doctests) has no local baseline to compare against (D-25's seven closing gates did
    not include a standalone `cargo test --workspace --doc` run), so its CI figure (480 passed /
    0 failed / 210 ignored) is recorded on its own merits, not as an agree/disagree verdict. A
    delta against Phase 36's own CI-observed doctest total (462 passed) is noted for a future
    reader as context only — different trees at different points in time, not a disagreement."

requirements-completed: [CURR-17, CURR-21]

coverage:
  - id: D1
    description: "36.1-CI-EVIDENCE.md's run table has no pending cell: every lint-job step
      (formatting, clippy, both documentation steps, the new public-API `# Examples` heading
      step) and both test-job steps (unit tests, doctests) carry a real run ID and an observed
      success conclusion"
    requirement: CURR-17
    verification:
      - kind: other
        ref: "gh run view 35329107346 --repo DF3NDR/paladin-dev-env --json jobs -> Code Quality,
          Unit Tests (stable), Unit Tests (beta) all conclusion=success; gh api
          repos/DF3NDR/paladin-dev-env/actions/jobs/105549054274 --jq '.steps[]' -> all 5 lint
          steps success; jobs 105549054279 / 105549054378 --jq '.steps[]' -> Run unit tests /
          Run doc tests both success on both toolchains"
        status: pass
    human_judgment: false
  - id: D2
    description: "The new public-API `# Examples` heading step's conclusion is recorded as
      successful, matching the local figure verbatim"
    requirement: CURR-21
    verification:
      - kind: other
        ref: "raw log line 1784 of job 105549054274: 'All 101 D-05 public API entry points carry
          a plural '#Examples' heading.' -- identical to the local closing-gate figure (101/101
          OK, 0 MISSING, 0 SINGULAR)"
        status: pass
    human_judgment: false
  - id: D3
    description: "Any figure differing from the local closing capture is recorded in the
      comparison section as a finding, with the CI figure stated authoritative and the local
      record left as observed"
    requirement: CURR-17
    verification:
      - kind: other
        ref: "36.1-CI-EVIDENCE.md comparison table row 3 (local first run's 3-failure flake vs
          CI's clean 982/0) and row 7 (doctests, no local baseline, CI figure recorded on its
          own merits) -- both present as findings, neither reconciled by adjusting the local
          record"
        status: pass
    human_judgment: false

duration: ~20min
completed: 2026-09-18
status: complete
---

# Phase 36.1 Plan 14: CI Evidence Closure — Record the Real Run Summary

**Recorded a live-read, cross-checked CI run (35329107346) proving the new public-API `# Examples` heading step is a green required check, closing the one gate this devcontainer cannot measure locally.**

## Performance

- **Duration:** ~20 min (this continuation; Task 1 was completed by a prior executor)
- **Started:** 2026-09-18 (Task 1 by prior executor)
- **Completed:** 2026-09-18
- **Tasks:** 2/2 (Task 1 by prior executor, Task 2 by this continuation)
- **Files modified:** 1 (`36.1-CI-EVIDENCE.md`)

## Accomplishments

- Confirmed `origin/feature/phase-33` already equals local HEAD (`6fe5b70a1ff973e088a58184f57d5401c9f97895`)
  — the maintainer's push had already landed before this continuation began, per the checkpoint's
  resolution message.
- Independently re-read CI run `35329107346` via `gh run view --json jobs` (not just trusting the
  orchestrator's transcribed figures): confirmed all 33 completed jobs, of which every
  gate-relevant one concluded `success` and none failed (`Docker Build` was still `in_progress`
  at read time, unrelated to any row this plan's table tracks).
- Fetched the raw per-job logs for the `lint` job (`105549054274`) and both `Unit Tests` jobs
  (`105549054279` stable, `105549054378` beta) via `gh api .../jobs/<id>/logs`, stripped ANSI
  escapes, and confirmed the exact log lines: zero `warning:` lines on both documentation steps,
  `All 101 D-05 public API entry points carry a plural '# Examples' heading.` on the new gate
  step (byte-identical to the local figure), `982 passed; 0 failed` on the workspace unit-test
  step, and a 13-crate doctest sum of `480 passed / 0 failed / 210 ignored`.
- Filled every pending cell in `36.1-CI-EVIDENCE.md`'s run table with the run ID, job IDs and
  observed conclusions, added the head-SHA delta note (seed-time `6735bc64` → run head `6fe5b70a`,
  two doc-only commits between them, no source change), and filled the comparison section with a
  five-row local-vs-CI table plus a written verdict: no disagreement requiring reconciliation.
- Committed the completed evidence file with the D-12 subject line, verified the clippy/fmt
  pre-commit hooks passed, and confirmed `git diff --stat HEAD~1 -- src/ crates/ docs/ tests/
  Makefile .github/ .planning/WINDOWS.md` is empty.

## Task Commits

1. **Task 1: Seed the CI evidence file with the local figures and a pending run table** — `4046b98a` (docs) — by the prior executor
2. **Task 2: Push the branch and record the real run** — `46e9a46a` (docs) — this continuation

**Plan metadata:** (this commit — see final_commit below)

## Files Created/Modified

- `.planning/phases/36.1-deferred-items-closure/36.1-CI-EVIDENCE.md` — completed with the real
  CI-run table (run 35329107346, 7 rows all `success`, job IDs and raw-log-quoted notes) and a
  filled local-vs-CI comparison section (prior executor seeded it in `4046b98a`; this
  continuation completed it)

## Decisions Made

- The branch push was the maintainer's own action, confirmed already complete
  (`origin/feature/phase-33` == local `HEAD`) before this task's work began — this continuation
  never ran `git push`.
- Every gate-relevant figure was independently re-read from the live GitHub API rather than
  transcribed from the checkpoint message alone, per this continuation's own verification
  discipline (the checkpoint message's figures are corroborated, not merely trusted).
- Row 7 (doctests) is recorded on its own merits with no agree/disagree verdict, since D-25's
  seven closing gates never included a standalone doctest run to compare against — stated
  explicitly rather than silently treated as an "agree."

## Deviations from Plan

None — plan executed exactly as written. The checkpoint named in Task 2 was already resolved by
the time this continuation started (the maintainer had pushed and the orchestrator had already
read the run once); this continuation's job was to independently confirm those figures against
the live API and write the completed record, which is exactly what Task 2's action instructs.

## Issues Encountered

None. `gh` was authenticated and reachable in this environment, so no fallback to
orchestrator-reported figures was needed — every recorded conclusion in the evidence file was
independently confirmed against the live GitHub Actions API by this continuation.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- Phase 36.1's evidence is complete: the closing-gate local figures (D-25, plan 36.1-13) and the
  real CI run (D-12, this plan) both exist in `36.1-CI-EVIDENCE.md`, and no disagreement between
  them requires reconciliation.
- `.planning/REQUIREMENTS.md` and `.planning/WINDOWS.md` were not touched by this plan, per its
  own prohibitions — all six CURR-16..CURR-21 rows were already complete before this plan ran.
- Phase 37 can cite the `Coverage` job figure recorded in this file's "Other jobs on the same
  run" note (90.44% lines / 83.61% functions, above the ADR-0006 82% floor) without re-measuring
  it, since this devcontainer has no Docker and cannot run the coverage gate locally.
- No blockers.

---
*Phase: 36.1-deferred-items-closure*
*Completed: 2026-09-18*

## Self-Check: PASSED

Verified `.planning/phases/36.1-deferred-items-closure/36.1-CI-EVIDENCE.md` exists and contains no
literal-pending run-table cell (both remaining occurrences of the word "pending" are historical
prose describing the file's own seeded state, not live cells). Verified commit hashes `4046b98a`
and `46e9a46a` both found in `git log --oneline --all`. Verified `git diff --stat HEAD~1 -- src/
crates/ docs/ tests/ Makefile .github/ .planning/WINDOWS.md` is empty. Verified
`origin/feature/phase-33` equals local `HEAD` at the time this task began
(`6fe5b70a1ff973e088a58184f57d5401c9f97895`).
