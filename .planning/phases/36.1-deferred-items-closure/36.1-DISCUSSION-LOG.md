# Phase 36.1: Deferred Items Closure - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-18
**Phase:** 36.1-deferred-items-closure
**Mode:** `--auto` (every question resolved to the recommended option; no AskUserQuestion)
**Areas discussed:** Row 38 disposition, `# Examples` apparatus (SC2), `cli_isolation` rescoping (SC3), Docs-fix shapes (SC1), Ledger row minting (SC4), Todo deferral form (SC5), Closing gates and bookkeeping (SC6), Phase 36 register — eight crate-level rustdoc allows (added at plan-phase init, 2026-09-18)

---

## Row 38 disposition (the open Phase 36 ledger row)

| Option | Description | Selected |
|--------|-------------|----------|
| Fix in tree, bounded | Route the `FailRun` reason through the same sanitized path `FeedToModel` uses; one pinning test; `windows fixed 38`; waive-and-re-home as fallback if a signature change would be needed | ✓ |
| Waive for v0.10.0 | Waive with a reason naming v0.11.0 as owner; ship with the documented contract unmet | |
| Leave open for Phase 37 | Defer the decision to the release phase; `/gsd-ship` stays blocked until then | |

**Auto selection:** Fix in tree, bounded (recommended default).
**Notes:** Row 38 was filed after the roadmap entry was written, so the roadmap does not name it; it is unowned and blocks shipping, which is this phase's purpose.

---

## `# Examples` apparatus disposition (SC2)

| Option | Description | Selected |
|--------|-------------|----------|
| Wire + refreeze at 101 + fix the 19 | Script keeps deriving live; snapshot record at 101; 19 real doctests; `make check-api-examples` in clean-code, pre-push and the CI lint job | ✓ |
| Script reads the frozen 76 list | Honour Phase 16's freeze literally; 0 MISSING by construction; gate blind to new entry points | |
| Waive in WINDOWS.md | Record the drift with a reason; no wiring, no fixes | |

| Option (where the gate runs) | Description | Selected |
|--------|-------------|----------|
| make + pre-push + CI lint step | Copies `doc-check`'s three-place placement; CI step needs a recorded real run | ✓ |
| make clean-code only | Local only; no CI enforcement | |
| CI only | No local signal before push | |

**Auto selection:** Wire + refreeze + fix; three-place wiring (recommended defaults).

---

## `cli_isolation` rescoping (SC3)

| Option | Description | Selected |
|--------|-------------|----------|
| Parse Cargo.toml | Assert `features.default` lacks `"cli"` via the `toml` crate; green under any feature set; tests the documented invariant | ✓ |
| `#[cfg_attr(feature = "cli", ignore)]` | Skip under `--all-features`; the guard is blind in the one invocation where the invariant matters | |
| Delete the test | Removes the false failure and the guard | |

**Auto selection:** Parse Cargo.toml (recommended default).

---

## Docs-fix shapes (SC1)

| Option (cicd.md) | Description | Selected |
|--------|-------------|----------|
| Caption illustrative | No deploy workflow exists under `.github/workflows/`; caption each block and the section intro | ✓ |
| Replace with real excerpt | Nothing real to excerpt | |
| Delete the blocks | Loses the page's deployment guidance | |

| Option (OpenAiAdapter) | Description | Selected |
|--------|-------------|----------|
| Casing only + compile probe | One sweep commit; scratch-compile the four `battalion-patterns-guide` bodies; waived row if one fails | ✓ |
| Casing + full API-shape rewrite | Grows into a page rewrite outside SC1 | |
| Casing only, no probe | Leaves the flagged drift unowned | |

| Option (testing-guide tree) | Description | Selected |
|--------|-------------|----------|
| Whole block to tree | The block is stale in several names, not one line; correct it to `ls -p tests` | ✓ |
| Move the one line | Satisfies SC1 literally, leaves known-wrong names | |
| Add a note | Leaves the wrong tree in place | |

**Auto selection:** as marked (recommended defaults).

---

## Ledger row minting (SC4)

| Option | Description | Selected |
|--------|-------------|----------|
| Per register entry | 11 rows (ids 39-49), minted in the closing plan after the fixing commits exist; `fixed` rows carry the commit in the description because `windows fixed` takes no reason | ✓ |
| Per success criterion | 6 rows; loses the per-defect trace `/gsd-complete-milestone` reads | |
| Per file | Mixes register entries that share a page | |

**Auto selection:** Per register entry (recommended default).

---

## Todo deferral form (SC5)

| Option | Description | Selected |
|--------|-------------|----------|
| In place: frontmatter + dated Disposition section | `owner`, `deferred_past`, `recheck_by`, `dispositioned_by`; file stays in `todos/pending/` so `list-todos` still surfaces it | ✓ |
| New `todos/deferred/` directory | New convention; `list-todos` would stop surfacing them | |
| Close and re-file | Loses the file's own "must not be silently closed" instruction | |

**Auto selection:** In place (recommended default). Re-check date 2026-10-16 for both; RustFS also gets a v2 requirement line.

---

## Closing gates and bookkeeping (SC6)

| Option | Description | Selected |
|--------|-------------|----------|
| Documentation + Fixed bullets | `### Documentation` for the prose fixes and gate; `### Fixed` for the `FailRun` redaction fix | ✓ |
| Documentation bullet only | Hides a behaviour fix from the changelog | |

**Auto selection:** Both (recommended default).

---

## Phase 36 register — eight crate-level rustdoc allows (D-29)

Found at plan-phase init: `36-rustdoc-zero-warning-bar-examples-currency/deferred-items.md` (opened by plan 36-12 after the ROADMAP 36.1 entry was written) names Phase 36.1 as the recommended owner of eight pre-existing `#![allow(rustdoc::…)]` attributes hiding 309 diagnostics. Resolved under the same `--auto` rule.

| Option | Description | Selected |
|--------|-------------|----------|
| Keep, justify in ADR-0033, re-home to v0.11.0 | Amendment section listing the eight attributes and counts; one `lint-warning` ledger row waived with owner and re-check date; no `.rs` change | ✓ |
| Remove all eight and fix the 309 diagnostics now | A Phase-36-sized job (143 rows took 13 plans) before the release tag | |
| Per-attribute triage now (remove some, keep some) | Partial source changes with no bar movement; splits the record across two milestones | |

**Auto selection:** Keep, justify, re-home (recommended default).

---

## Claude's Discretion

- Plan and wave layout (parallel worktrees for independent fixes; one closing plan for ledger/todo/corpus/wiring/evidence).
- Exact doctest bodies for the 19 sections; exact caption and `//!` wording.
- Whether the 11 ledger rows are minted by one script or by hand.

## Deferred Ideas

- Add `cargo test --workspace --all-features` to CI.
- API-shape rewrite of `battalion-patterns-guide.md` example bodies (only if the compile probe fails).
- RustFS `FileStoragePort` adapter build (v0.11.0 candidate).
- The Docker-machine coverage walk itself.
- Regenerating `doc-coverage-report.md`; consolidating the CI lint job's rustdoc steps; a full `CONVENTIONS.md` refresh.
- Removing the eight crate-level rustdoc allows and fixing the 309 hidden diagnostics (v0.11.0, D-29).
