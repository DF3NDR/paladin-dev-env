---
phase: 37-v0-10-0-crate-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md
autonomous: false
requirements: [SHIP-05]
must_haves:
  truths:
    - "The release-evidence path works end to end before any heavy gate runs: one local gate command produces one row in `37-CI-EVIDENCE.md`, one read-only sparse-index query records the `paladin-eval` registry state, and one read-only `gh` query records the branch's CI/PR state."
    - "Free space on `/workspace` and `git status --porcelain` are measured and recorded in `37-CI-EVIDENCE.md` before any gate in plan 37-02 or 37-03 is attempted, so an out-of-space abort can never be mistaken for a red gate under D-14."
    - "`paladin-eval`'s first-publish bootstrap (D-17) is handed to the maintainer as a blocking-human checkpoint whose text never asks for, echoes or stores a crates.io token, and which states that the crates.io first-publish policy claim is MEDIUM confidence."
    - "The D-13 dry-run dispatch is recorded as deliberately not attempted, with the traced reason (`release.yml`'s `tag` input cannot resolve before a ref literally named `v0.10.0` exists), and no rc tag or non-`v` shadow tag is created."
  artifacts:
    - path: ".planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md"
      provides: "House-form evidence record (provenance lines, Local sweep table, CI-run table, registry table, carried findings, summary)"
      contains: "## Local sweep"
  key_links:
    - from: ".planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md"
      to: "https://index.crates.io/pa/la/paladin-eval"
      via: "read-only sparse-index query recorded as the pre-bootstrap registry state for D-17"
      pattern: "paladin-eval"
    - from: ".planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md"
      to: ".project/v0.10.0/09-program-acceptance-audit.md"
      via: "every §12 gate row (plan 37-05) points back at a numbered Local sweep row in this file"
      pattern: "37-CI-EVIDENCE"
  prohibitions:
    - statement: "MUST NOT record any gate as green that was not actually run on the stated SHA; a gate this devcontainer cannot measure is labelled CI-attributed, never a local pass, and no success criterion is ever marked satisfied by promise (D-00e, D-09)."
      status: unverified
      verification: judgment
    - statement: "MUST NOT perform, simulate or rehearse a maintainer-reserved irreversible act: no agent may tick the §11 sign-off box, merge to `main`, push a `v*` tag (or any rc/shadow tag), or trigger a crates.io publish (D-00a, D-02, D-13, D-16)."
      status: unverified
      verification: judgment
    - statement: "MUST NOT request, echo, store or embed a crates.io token or any registry credential anywhere — not in a command, a file, a log, a PR body, a CI secret, or checkpoint text (D-00g, D-17)."
      status: unverified
      verification: judgment
    - statement: "MUST NOT silently fix, work around, or re-run past a red gate; a red gate is recorded with its command, SHA and failing output and stops the phase at a blocking checkpoint (D-14, D-15)."
      status: unverified
      verification: judgment
    - statement: "MUST NOT delete, rewrite or renumber existing evidence: additions are dated, and existing evidence rows, audit sections, sign-off boxes and `WINDOWS.md` rows stay byte-intact (D-00d, D-06)."
      status: unverified
      verification: judgment
---

<objective>
Open the phase by proving the release-evidence path works end to end, recording the execution
environment honestly, and putting the one long-lead human action (D-17, `paladin-eval`'s
first publish) in the maintainer's hands before anything expensive runs.

Purpose: this phase is the pre-merge half of D-09's two-wave split, and every later plan writes
into the artifact this plan creates. The disk/clean-tree measurement exists so that an ENOSPC or
a dirty-tree refusal in plan 37-02/37-03 is read as an environment stop, never as a red gate
under D-14. D-17's bootstrap is placed first because it has no dependency on the tree and the
PR's CI cycle later costs 60-90 minutes per push.
Output: `37-CI-EVIDENCE.md` in the `29`/`33-CI-EVIDENCE.md` house form, carrying its provenance
lines, Local sweep row 1, the pre-bootstrap registry state for `paladin-eval`, the D-13
non-dispatch record, and the first carried findings (D-00d).

Operating rules for every task in this plan and every later plan in this phase:
- Execution is **sequential on the main checkout with hooks ON**. Worktree executors are not
  viable here (the volume is at 98 %; a cold worktree `target/` is ~16 GB).
- Every commit uses plain git with an explicit path list and a >= 600 s timeout —
  `git add -- <paths> && git commit -q -m "..." -- <paths>` — never `gsd-tools query commit`
  (it times out under the `always_run` clippy pre-commit hook) and never `--no-verify`.
  Wait for any orphaned `pre-commit` process before the next commit.
- The phase prohibitions P1-P5 recorded in this plan's `must_haves.prohibitions` govern every
  later plan; later plans cite them rather than restating them.
- Temperament (CONTEXT `<specifics>`): conservative past the point of convenience. When unsure,
  stop and record. Do not improvise.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-v0-10-0-crate-release/37-CONTEXT.md
@.planning/phases/37-v0-10-0-crate-release/37-RESEARCH.md
@.planning/phases/37-v0-10-0-crate-release/37-PATTERNS.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end release-evidence path — pre-flight, one gate row, one registry read, one GitHub read</name>
  <files>.planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/33-commissary-in-tree-adoption/33-CI-EVIDENCE.md` — the house form to copy: header/provenance block, `## Local sweep` table (`# | Command | Result (verbatim/summarized) | Verdict`), the CI-run table (`Workflow | Run ID (URL) | Conclusion | SHA | Notes`), and the closing "Summary and what remains" prose section
    - `.planning/phases/29-program-gates-release/29-CI-EVIDENCE.md` — the same shape one phase earlier; row numbering convention
    - `.planning/phases/37-v0-10-0-crate-release/37-PATTERNS.md` — the excerpted header and Local-sweep row shapes for this exact file
    - `.planning/phases/37-v0-10-0-crate-release/37-RESEARCH.md` Q4 (sparse-index path-prefix scheme and the mandatory `User-Agent` header), Q5 gate 1, Q6 (branch/PR state), Pitfall 4 (the zero-count exit-code trap)
  </read_first>
  <action>
Establish the phase's whole evidence path on one thin end-to-end slice, in this order, and record
every measurement in the new file — nothing here is a rehearsal, the rows written are the real
row 1 and the real provenance block later plans append to.

1. Pre-flight measurements, recorded verbatim:
   - free space: `df -BG --output=avail /workspace` (record the integer GiB).
   - working tree: `git status --porcelain` (record empty or the exact offending paths). A dirty
     tree is a stop-and-ask, not something this plan cleans: the usual offender is
     `.planning/config.json`, which GSD helpers rewrite, and the agent does not `git checkout --`
     anything unless a later plan names the file and the reason.
   - branch state: `git fetch origin` (read-only), then `git rev-list --left-right --count
     origin/main...HEAD` and `git status -sb`. Record the two counts and the unpushed count.
     If the left count is non-zero, `main` has moved — record it and note that CONTEXT
     Discretion item 6 applies (re-establish "the final commit" and re-run the re-seal from the
     top; §11 is re-confirmed against the new SHA).
   - `git rev-parse HEAD` — this is the local re-seal head SHA for the provenance block.
2. Create `37-CI-EVIDENCE.md` in the house form with: the title line naming the phase and the
   plan range, `**Phase:**`, `**Branch:**` (`feature/phase-33` pre-merge, `chore/37-close`
   post-tag per D-10), `**Written:** 2026-09-18`, and a **Provenance** list with one line per
   wave-relevant SHA (local re-seal head, PR head at open, post-§11-tick final SHA, `main` merge
   commit, tagged commit) — the later four filled in as `pending` placeholders that later plans
   replace. Do not collapse these into a single "Head SHA" line: D-00b makes "the release commit"
   mean both the PR head and the merge commit, and D-05 needs evidence for both.
   Then add the empty section skeletons in this order: `## Local sweep`, `## CI-run table`,
   `## Registry verification (D-08)`, `## Findings carried forward (D-00d)`,
   `## Summary and what remains`, plus a pre-flight subsection holding step 1's measurements.
3. Run the first half of D-06 gate 1 against `MIGRATION.md` — the no-placeholder-marker check —
   capturing the count into a variable and comparing the value to `0`. Never rely on the exit
   code: `grep -c` exits 1 when the count is zero, which is the passing case. Record it as Local
   sweep row 1 with the command, the captured count and the verdict.
4. One read-only sparse-index query for `paladin-eval`, using the 4+-character path prefix
   (`pa/la/paladin-eval`) and the mandatory `User-Agent` header
   (`paladin-release-check (github.com/DF3NDR/paladin-dev-env)`); record the HTTP status code and,
   if 200, the highest `vers` present. A 404 is the expected pre-bootstrap state and is recorded
   as the D-17 baseline, not as a failure.
5. One read-only GitHub query: `gh pr list --head feature/phase-33 --state all --json number,state`
   and `gh run list --branch feature/phase-33 --limit 5 --json workflowName,headSha,conclusion`.
   Record both outputs (an empty PR list and an empty run list are the expected state) as the
   CI-run table's opening note. If `gh` fails with an auth or permission error, record the exact
   error and the exact command, and stop — hand the command to the maintainer rather than
   retrying with a different token path.
6. Commit with the plain-git form and a >= 600 s timeout.
  </action>
  <acceptance_criteria>
    - `.planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md` exists and contains the literal headings `## Local sweep`, `## CI-run table`, `## Registry verification (D-08)`, `## Findings carried forward (D-00d)` and `## Summary and what remains`
    - The file records a free-space figure in GiB, the `git status --porcelain` result, both `git rev-list --left-right --count origin/main...HEAD` numbers, and the local re-seal head SHA (40 hex chars)
    - Local sweep row 1 records the `MIGRATION.md` placeholder-marker check with a captured count of `0` and verdict `PASS`
    - The file records an HTTP status code for the `paladin-eval` sparse-index query and labels it the pre-bootstrap D-17 baseline
    - The file records the `gh pr list` and `gh run list` outputs for `feature/phase-33`
    - `git log -1 --name-only` shows the commit touches only `.planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md`
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md && grep -q '^## Local sweep' .planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md && grep -q '^## CI-run table' .planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md && grep -q '^## Registry verification' .planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md && C=$(grep -c TBD MIGRATION.md; true); test "$C" = "0"</automated>
  </verify>
  <done>`37-CI-EVIDENCE.md` exists in house form with a real provenance block, a real Local sweep row 1, a recorded registry read and a recorded GitHub read — the whole evidence path is proven before any heavy gate runs, and the commit is on `feature/phase-33`.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Maintainer bootstraps `paladin-eval`'s first crates.io publish (D-17)</name>
  <files>.planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/37-v0-10-0-crate-release/37-CONTEXT.md` — D-17 in full (the planning-time addendum), plus D-00g
    - `.planning/phases/37-v0-10-0-crate-release/37-RESEARCH.md` Q3 — the traced consequence of not doing this (ten of twelve crates published, hard fail at position 11, facade never published) and the crates.io policy citation with its confidence level
    - `docs/src/appendix/release-automation.md` — the per-crate Trusted Publishing table (eleven rows, no `paladin-eval` row) and the Credential History ledger
  </read_first>
  <action>
Hand the maintainer the `paladin-eval` first-publish bootstrap (D-17) and stop. The agent performs
no part of it: it never mints, receives, echoes, stores or uses a crates.io credential, and it adds
no repository or environment secret (D-00g). Present the `<what-built>` briefing and the
`<instructions>` steps verbatim, including the MEDIUM-confidence disclosure, then wait.
  </action>
  <what-built>
Task 1 recorded `paladin-eval`'s registry state read-only. Everything else about this crate's
first publish is yours: the agent cannot do it, and by D-00g/D-17 must not try.

**Why this is needed (traced, not assumed):** `paladin-eval` has never been published — the
sparse index returns 404 (re-verified 2026-09-18). crates.io Trusted Publishing cannot perform a
crate's *first* publish; a Trusted Publisher link can only be created for a crate that already
exists on the registry. `scripts/publish-crates.sh` places `paladin-eval` at position 11 of 12,
immediately before the `paladin-ai` facade. So an unassisted `release.yml` run would publish ten
crates, hard-fail at `paladin-eval`, and mark the facade `skipped`.

**Why the obvious alternative is impossible:** publishing the real `paladin-eval 0.10.0` before
the tag cannot work — it depends on `paladin-battalion`, `paladin-ai-core`, `paladin-llm`,
`paladin-ports` and `paladin-storage` at `^0.10.0`, and those versions only reach the registry
during the release run itself. D-17 is the maintainer's chosen answer: a dependency-free
placeholder.

**Confidence disclosure (D-17 requires this be said):** the crates.io first-publish policy claim
is **MEDIUM confidence** — it rests on the rust-lang blog's 2025-07 crates.io development update
plus a search synthesis of the Trusted Publishing docs and RFC 3691; the primary docs page is
JS-rendered and could not be read directly. Please confirm it in the crates.io UI before relying
on it (if crates.io has since shipped a pending-publisher feature, this whole step may be
unnecessary — tell the agent and it will record that instead).
  </what-built>
  <instructions>
Steps (all yours — the agent never sees, requests, stores or handles the credential):

1. Create a **short-lived** crates.io API token in the crates.io UI. Do not paste it into this
   session, any file in this repository, any log, the PR body, or a GitHub secret.
2. In a scratch directory **outside this repository** (so no tree change is made and the
   workspace stays clean for the gate sweep), create a minimal, dependency-free crate:
   - package name `paladin-eval`
   - version `0.0.1` (a placeholder — any version below `0.10.0` works; `0.0.1` is D-17's example)
   - `edition`, a one-line `description`, `license` matching the workspace, and `repository`
     pointing at `https://github.com/DF3NDR/paladin-dev-env`
   - a `src/lib.rs` containing nothing but a doc comment stating it is a placeholder for the
     Paladin evaluation-harness crate
   - **no `[dependencies]` section at all**
3. `cargo publish` it with that token from the scratch directory.
4. In the crates.io UI, add `paladin-eval`'s Trusted Publisher with the **same triple as the
   other eleven crates**: repository `DF3NDR/paladin-dev-env`, workflow `release.yml`,
   environment `crates-io`.
5. **Revoke the token.**
6. Reply here with: "bootstrapped" (plus the placeholder version you used), or "deferred" if you
   want to do it later, or "not needed" if crates.io now supports a pending publisher.

Notes:
- The Trusted Publisher link is **not publicly queryable**, so it cannot be machine-verified now;
  your report is the attestation, exactly as the existing eleven rows read "linked (reported)".
  The observable proof arrives after the release: a non-null `trustpub_data` on `paladin-eval`
  `0.10.0` (plan 37-10 checks it).
- The placeholder version is permanent: a published version can be yanked but never deleted or
  re-used. Yanking the placeholder after the real release is your call, not a task in this phase.
- `docs/src/appendix/release-automation.md`'s per-crate trust table and Credential History ledger
  will now be one row short. Under D-14 and CONTEXT `<deferred>` this phase does not edit docs
  pages — the agent records it as a carried finding in `37-CI-EVIDENCE.md` instead of fixing it.
- If you choose "deferred": the tag hand-off in plan 37-08 is gated on `paladin-eval` resolving
  on the sparse index, and the agent will stop there rather than hand over the tag command.
  </instructions>
  <verification>
Read-only, by the agent, in Task 3 — and again as a hard gate in plan 37-08 before the tag command
is handed over:
- `curl -s -o /tmp/pe.json -w '%{http_code}' -H 'User-Agent: paladin-release-check (github.com/DF3NDR/paladin-dev-env)' https://index.crates.io/pa/la/paladin-eval` returns `200` and a non-empty body with at least one version record (a `404` is the un-bootstrapped state)
- The maintainer's reply is recorded verbatim in `37-CI-EVIDENCE.md` alongside that status
- No credential appears anywhere in the session, the repository, the commit, or the evidence file, and no repository or environment secret was added (`gh secret list` is not consulted and nothing is set)
  </verification>
  <resume-signal>Reply "bootstrapped &lt;version&gt;", "deferred", or "not needed — crates.io supports pending publishers"</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the post-checkpoint registry state, the D-13 non-dispatch decision, and the first carried findings</name>
  <files>.planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md` — the file as Task 1 wrote it (append only; never rewrite earlier rows, D-00d)
    - `.planning/phases/37-v0-10-0-crate-release/37-RESEARCH.md` Q1 (the full four-row trace proving no `tag` input value works pre-tag) and the "State of the Art" table (the three stale documentation figures)
    - `.planning/phases/37-v0-10-0-crate-release/37-CONTEXT.md` — D-13 (including its own documented fallback) and D-00d
  </read_first>
  <action>
Append three things to `37-CI-EVIDENCE.md`. Record only; fix nothing.

1. The post-checkpoint registry state for `paladin-eval`: re-run the same read-only sparse-index
   query as Task 1 and record the HTTP status, the highest `vers`, and the maintainer's reported
   outcome from Task 2 verbatim ("bootstrapped &lt;version&gt;" / "deferred" / "not needed"). Do not
   treat a 404 here as a failure of this plan — the hard gate on this fact sits in plan 37-08,
   immediately before the tag hand-off. State that pointer in the row's note.
2. The **D-13 non-dispatch record**, as a `## Findings carried forward (D-00d)` entry with a
   heading naming D-13. State: no `workflow_dispatch` of `release.yml` was attempted, and no rc
   tag or non-`v` shadow tag was created. Give the traced reason — the dispatch's `tag` input is
   used both as the ref to resolve *and* as the version string matched against every crate
   manifest and the changelog heading, so no value satisfies all three constraints before a ref
   literally named `v0.10.0` exists; `verify-tag-source` fails on an unresolvable string, a raw
   SHA fails the changelog-heading match, and an existing older tag fails the manifest match.
   Cite `37-RESEARCH.md` Q1 for the row-by-row trace and D-13's own fallback sentence as the
   authority for going straight to the tag. Record explicitly that the theoretical
   non-`v`-prefixed shadow-tag escape hatch was considered and rejected.
3. The carried findings observed so far, each as its own bullet with a source pointer and no fix:
   - `docs/src/appendix/release-automation.md`'s per-crate Trusted Publishing table and
     Credential History ledger have no `paladin-eval` row (D-17's bootstrap is not reflected
     there; CONTEXT `<deferred>` puts docs-page currency in v0.11.0).
   - `docs/src/appendix/release-automation.md`'s "Canonical Publish Order" section still
     describes the pre-`paladin-eval` eleven-crate order; `scripts/publish-crates.sh`'s `CRATES`
     array is the authority (twelve, `paladin-eval` at position 11).
   - `docs/src/contributing/development-setup.md` still states eleven publishable crates; the
     tree says twelve (D-00f — the shipped tree outranks any document).
   - `docs/src/appendix/release-recovery.md` §1's example loop enumerates eleven crate names.
   - `CHANGELOG.md`'s `[0.10.0]` heading carries the date `2026-09-10`, earlier than the release
     date. Record it; do not edit it. Nothing in the gate set or
     `scripts/check-release-consistency.sh` reads the date (clause 2 matches the version heading
     only), so this is a currency finding, not a gate failure.
   Commit with the plain-git form.
  </action>
  <acceptance_criteria>
    - `37-CI-EVIDENCE.md` contains a `## Findings carried forward (D-00d)` section with at least five distinct bullets, each naming a file path
    - The findings section contains a D-13 entry that states no dispatch was attempted and gives the reason, and that no rc or shadow tag was created
    - The file records a second, post-checkpoint `paladin-eval` sparse-index status plus the maintainer's verbatim reply from Task 2
    - No line in `37-CI-EVIDENCE.md` written by Task 1 is modified or removed (`git diff HEAD~1 -- <file>` shows additions only, zero deletions)
    - `git log --oneline -1` shows a docs-only commit touching only `37-CI-EVIDENCE.md`
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'Findings carried forward' .planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md && grep -q 'D-13' .planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md && D=$(git diff HEAD~1 --numstat -- .planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md; true); echo "$D" && test -n "$D"</automated>
  </verify>
  <done>The registry state, the D-13 non-dispatch decision with its traced reason, and five carried documentation findings are recorded in `37-CI-EVIDENCE.md` as append-only additions, and committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer credential → crates.io | A short-lived, maintainer-held API token crosses here during the D-17 bootstrap; it must never cross into the agent's session, the repository, CI secrets, or any log |
| agent → crates.io sparse index | Unauthenticated, read-only HTTPS reads; response bodies are untrusted data recorded verbatim, never executed |
| agent → GitHub API (`gh`) | Read-only queries in this plan; a write failure is handed to the maintainer, never retried down a different credential path |
| planning artifacts → release authority | `37-CI-EVIDENCE.md` becomes the evidence basis a human signs; a fabricated or unlabelled row would grant authority the run did not earn |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-01 | Information Disclosure | D-17 bootstrap checkpoint text and any command it suggests | high | mitigate | The checkpoint hands over steps only; it never asks for, echoes or stores a token, the publish happens from a scratch directory outside the repository, and the token is revoked by the maintainer. No task in this phase adds a repository or environment secret. |
| T-37-04 | Repudiation | `37-CI-EVIDENCE.md` rows | medium | mitigate | Every row carries the command, the SHA it was measured on, and the verbatim result; the provenance block names one SHA per wave; additions are dated and append-only (D-00d). |
| T-37-06 | Spoofing | `index.crates.io` read-only responses | medium | accept | TLS-authenticated public endpoint, read-only, with the mandatory `User-Agent`; the registry reading is cross-checked in plan 37-10 against the release run's own per-crate outcome table rather than trusted alone. |
| T-37-SC | Tampering | crates.io publish integrity (twelve crates) | high | mitigate | The real `0.10.0` publish stays with `release.yml` via OIDC Trusted Publishing — no standing credential (D-00g); this phase installs no packages, so the package-legitimacy path is not applicable (`37-RESEARCH.md` Package Legitimacy Audit: not applicable, no new dependencies). |
</threat_model>

<artifacts_this_phase_produces>
New file paths, identifiers and named artifacts created by Phase 37 (no code symbols — this phase
modifies no file under `src/` or `crates/*/src/`, and adds no functions, types, CLI flags or
struct fields):

- `.planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md` (new, this plan)
- `.planning/phases/37-v0-10-0-crate-release/COVERAGE.md` (new, written at plan time)
- `.planning/phases/37-v0-10-0-crate-release/.continue-here.md` (new, plan 37-06)
- `.planning/HANDOFF.json` (new, plan 37-06)
- `.planning/phases/37-v0-10-0-crate-release/37-READY-TO-CLOSE.md` (new, plan 37-11)
- Requirement id `SHIP-05` (new, plan 37-04 — in `.planning/REQUIREMENTS.md` and its traceability table)
- Corpus section heading `## 12. Re-seal for v0.10.0 release (Phase 37, SHIP-05)` in `.project/v0.10.0/09-program-acceptance-audit.md` (new, plan 37-05)
- `.planning/MILESTONES.md` heading `## v0.10.0 Durable Agent Execution Runtime (Shipped: <date>)` (new, plan 37-11)
- Git branch `chore/37-close` (new, plan 37-09)
- Git tag `v0.10.0` (new — created and pushed by the maintainer only, D-02)
- crates.io versions: twelve crates at `0.10.0` (published by `release.yml`), plus a `paladin-eval` placeholder version published by the maintainer (D-17)
</artifacts_this_phase_produces>

<verification>
- `37-CI-EVIDENCE.md` exists with all five house-form sections, a multi-SHA provenance block, and Local sweep row 1 green
- The pre-flight numbers (free GiB, porcelain state, ahead/behind counts) are on record before any gate in 37-02/37-03 runs
- The D-17 checkpoint was answered by a human (its resolution is recorded in this plan's SUMMARY with provenance, Phase 12 precedent)
- The D-13 non-dispatch decision and five carried findings are recorded; nothing was fixed
- Two docs-only commits on `feature/phase-33`, neither touching any file under `src/` or `crates/*/src/`
</verification>

<success_criteria>
- `test -f .planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md` succeeds and the file has the five required `##` sections
- Local sweep row 1 records a captured count of `0` with verdict `PASS`, obtained without relying on `grep`'s exit code
- The `paladin-eval` sparse-index status is recorded twice (pre- and post-checkpoint) with the maintainer's verbatim reply
- `git log --oneline -2 --name-only` shows only `.planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md`
</success_criteria>

<output>
Create `.planning/phases/37-v0-10-0-crate-release/37-01-SUMMARY.md` when done. Record the D-17
checkpoint's resolution verbatim (including "deferred" or "not needed") and the pre-flight numbers,
so plans 37-02 and 37-03 can assert their preconditions against a recorded baseline.
</output>
