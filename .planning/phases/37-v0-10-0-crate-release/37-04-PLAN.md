---
phase: 37-v0-10-0-crate-release
plan: 04
type: execute
wave: 4
depends_on: [37-03]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/29-program-gates-release/29-CI-EVIDENCE.md
  - .planning/phases/33-commissary-in-tree-adoption/33-CI-EVIDENCE.md
autonomous: true
requirements: [SHIP-05]
must_haves:
  truths:
    - "`SHIP-05` exists in `.planning/REQUIREMENTS.md` as a new requirement under the existing `SHIP` prefix, stating that v0.10.0 is released — gates re-sealed on the final commit, tag on the `main` merge commit, every publishable crate on crates.io at `0.10.0`, release evidence in MILESTONES.md — and appears again as a `SHIP-05 | Phase 37` traceability row (D-07)."
    - "`SHIP-04`'s definition row, its checkbox, and its traceability row are byte-identical after this plan: it is not extended, re-opened, re-worded or re-checked (D-07)."
    - "ROADMAP Phase 37's `**Requirements**:` line reads exactly `**Requirements**: SHIP-05`, and no other line in that phase block changes."
    - "The STATE.md sentence that named the previous phase's CI-evidence file as the destination for this release's pre-merge CI run now names `37-CI-EVIDENCE.md`, with the rest of the sentence unchanged (D-05)."
    - "`29-CI-EVIDENCE.md` and `33-CI-EVIDENCE.md` each gain exactly one dated forward-pointer line at the end of the file and are otherwise byte-identical, so neither verified phase's evidence table is staled (D-05, D-00d)."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "The SHIP-05 definition row and its traceability row"
      contains: "SHIP-05"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 37's resolved Requirements line"
      contains: "**Requirements**: SHIP-05"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ROADMAP.md"
      via: "the minted SHIP-05 id is the same string the ROADMAP Phase 37 Requirements line carries and the same string every Phase 37 plan's frontmatter `requirements` field carries"
      pattern: "SHIP-05"
    - from: ".planning/STATE.md"
      to: ".planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md"
      via: "the corrected sentence names this phase's evidence file as the destination for the pre-merge CI run"
      pattern: "37-CI-EVIDENCE"
  prohibitions:
    - statement: "MUST NOT delete, rewrite or renumber existing evidence: additions are dated, and existing evidence rows, requirement rows, audit sections and sign-off boxes stay byte-intact (D-00d, D-06, D-07)."
      status: unverified
      verification: judgment
---

<objective>
Mint the phase's requirement id and repair the two bookkeeping pointers that would otherwise send
a reader to the wrong file.

Purpose: D-07 settles the requirement question — a newly minted `SHIP-05` ("v0.10.0 is released"),
with `SHIP-04` ("releasable", already Complete against Phase 29) left untouched. `SHIP` is an
existing prefix, so no new prefix is spent. D-05 additionally corrects the STATE.md sentence that
still points the pre-merge CI run at Phase 33's evidence file, and adds one dated forward-pointer
line to each of the two prior evidence files rather than editing their tables — editing a verified
phase's evidence would stale its VERIFICATION.
Output: a `SHIP-05` definition row, a `SHIP-05 | Phase 37` traceability row, a resolved ROADMAP
Requirements line, one corrected STATE.md sentence, and two one-line dated addenda.

Governing constraints: sequential execution on the main checkout with hooks ON; every edit is an
addition or a scoped substring replacement — never a whole-file rewrite (`Edit`, not `Write`); the
phase prohibitions P1-P5 in `37-01-PLAN.md` apply. If any task in this plan calls a
`gsd-tools state.*` or `roadmap.*` helper, STATE.md's frontmatter `milestone_name:` will be
clobbered — restore it to `Durable Agent Execution Runtime` in the same commit.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-v0-10-0-crate-release/37-CONTEXT.md
@.planning/phases/37-v0-10-0-crate-release/37-PATTERNS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mint SHIP-05 in REQUIREMENTS.md and resolve the ROADMAP Requirements line</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 298-322 — the `Program Gates & Release (overview §5/§9, epic SHIP)` section and the exact `SHIP-04` row shape (checkbox, bold id, one-line statement, parenthetical source pointer, hanging indent)
    - `.planning/REQUIREMENTS.md` lines 668-675 — the traceability table rows `SHIP-01 | Phase 29 | Complete` through `SHIP-04 | Phase 29 | Complete`, and the row that follows them
    - `.planning/ROADMAP.md` lines 1137-1160 — Phase 37's block: the Goal, Depends on, the `**Requirements**:` placeholder line with its em-dash note, Source, UI hint, the five success criteria, and the plan list
    - `.planning/phases/37-v0-10-0-crate-release/37-PATTERNS.md` — the `SHIP-04` row excerpt and the traceability-row excerpt, with the instruction not to touch the `SHIP-04` row
    - `.planning/phases/37-v0-10-0-crate-release/37-CONTEXT.md` — D-07 in full, including the ROADMAP extension-protocol prefix rule
  </read_first>
  <action>
Two scoped `Edit` operations. Never `Write` either of these files.

1. `.planning/REQUIREMENTS.md`:
   - Insert a new row immediately after the `SHIP-04` row (and before the next `###` section
     heading), in the identical shape: an unchecked box, the bold id `SHIP-05`, the one-line
     statement, and a parenthetical source pointer. The statement text is D-07's, verbatim in
     substance: v0.10.0 is released — the Phase 29 gate set re-sealed on the final commit, the
     annotated tag on the `main` merge commit, every publishable crate resolving on crates.io at
     `0.10.0`, and the release evidence recorded in MILESTONES.md. The parenthetical cites
     `roadmap Phase 37 SC1-SC4; 37-CONTEXT.md D-07; Phase 29 D-18 / D-21`. Wrap and indent
     continuation lines exactly like the `SHIP-04` row does.
   - Insert `| SHIP-05 | Phase 37 | Complete |` in the traceability table directly after the
     `SHIP-04 | Phase 29 | Complete` row. Leave the `SHIP-04` row and every other row untouched.
   - Do not alter `SHIP-04`'s checkbox, wording or status in any way — D-07 is explicit that it
     is not extended, re-opened or edited.
2. `.planning/ROADMAP.md`: replace Phase 37's entire `**Requirements**:` line — the one whose
   value is a placeholder followed by an em-dash note about possibly extending `SHIP-04` in place
   — with exactly `**Requirements**: SHIP-05`. Change nothing else inside the Phase 37 block: not
   the Goal, not Depends on, not Source, not the UI-hint line, not the success criteria, not the
   plan list.

Commit both files together with the plain-git form and a >= 600 s timeout.
  </action>
  <acceptance_criteria>
    - `grep -c 'SHIP-05' .planning/REQUIREMENTS.md` returns at least 2 (definition row plus traceability row)
    - `.planning/REQUIREMENTS.md` contains the literal `| SHIP-05 | Phase 37 | Complete |`
    - The `SHIP-04` definition row and the `| SHIP-04 | Phase 29 | Complete |` row are unchanged: `git diff HEAD~1 -- .planning/REQUIREMENTS.md` contains no removed line mentioning `SHIP-04`
    - The ROADMAP Phase 37 block contains the literal `**Requirements**: SHIP-05`
    - Extracting the Phase 37 block from `.planning/ROADMAP.md` yields zero placeholder markers on lines beginning `**Requirements**` — the negative grep is scoped to that line, because success criterion 1 in the same block legitimately quotes the same marker when describing the `MIGRATION.md` gate, and a block-wide count of zero is therefore unsatisfiable by design
    - `git diff HEAD~1 --numstat -- .planning/ROADMAP.md` shows exactly one line changed (one added, one removed)
  </acceptance_criteria>
  <verify>
    <automated>S=$(grep -c 'SHIP-05' .planning/REQUIREMENTS.md; true); test "$S" -ge 2 && grep -q '| SHIP-05 | Phase 37 | Complete |' .planning/REQUIREMENTS.md && awk '/^### Phase 37:/,/^## Progress/' .planning/ROADMAP.md > /tmp/p37.txt && grep -q 'Requirements\*\*: SHIP-05' /tmp/p37.txt && R=$(grep -c '^\*\*Requirements\*\*.*TBD' /tmp/p37.txt; true); test "$R" = "0" && D=$(git diff HEAD~1 -- .planning/REQUIREMENTS.md | grep -c '^-.*SHIP-04'; true); test "$D" = "0"</automated>
  </verify>
  <done>`SHIP-05` is minted in both places in REQUIREMENTS.md, ROADMAP Phase 37 names it as the phase's requirement, and `SHIP-04` is byte-identical.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the STATE.md CI-evidence sentence and add one dated forward-pointer line to each prior evidence file</name>
  <files>.planning/STATE.md, .planning/phases/29-program-gates-release/29-CI-EVIDENCE.md, .planning/phases/33-commissary-in-tree-adoption/33-CI-EVIDENCE.md</files>
  <read_first>
    - `.planning/STATE.md` lines 28-40 — the `## Project Reference` prose block containing the sentence to correct; note it is prose, not a table row, so a scoped substring replacement is sufficient
    - `.planning/phases/37-v0-10-0-crate-release/37-PATTERNS.md` — the exact current sentence quoted in full, the exact substring to replace, and the forward-pointer line shape (one dated, italicised addendum sentence appended at the very end, nothing above it touched)
    - `.planning/phases/33-commissary-in-tree-adoption/33-CI-EVIDENCE.md` — the file's tail, so the addendum lands after the existing footer lines rather than inside the tables
    - `.planning/phases/29-program-gates-release/29-CI-EVIDENCE.md` — the same, for the second addendum
  </read_first>
  <action>
Three scoped `Edit` operations. Never `Write` any of these files.

1. `.planning/STATE.md`: in the `## Project Reference` sentence that begins "Before that, push
   `feature/phase-33` (or open the PR) so the CI `coverage` job supplies the one gate this
   devcontainer cannot measure…", replace only the substring naming Phase 33's evidence file and
   its CI-run table with the same phrase naming `37-CI-EVIDENCE.md`'s CI-run table. Every other
   word of that sentence stays — including the clause about the tag being cut on the `main` merge
   commit per the two-SHA rule, which is still accurate. D-05 names this one sentence and nothing
   else in STATE.md; leave the preceding "Next" line alone even though D-11 changes when that
   command runs. If a `gsd-tools state.*` helper has clobbered the frontmatter, restore
   `milestone_name: Durable Agent Execution Runtime` in the same commit.
2. `.planning/phases/29-program-gates-release/29-CI-EVIDENCE.md`: append exactly one dated
   forward-pointer line at the end of the file, in the shape `37-PATTERNS.md` gives — a dated
   addendum naming this phase's evidence file as the home of the v0.10.0 release's pre-merge and
   post-tag CI evidence, and stating that this file's own rows are unchanged. Touch nothing else.
3. `.planning/phases/33-commissary-in-tree-adoption/33-CI-EVIDENCE.md`: the same single dated
   line, appended at the end.

Both evidence files belong to verified phases: an edit inside their tables would stale those
phases' VERIFICATION records, which is exactly why D-05 allows only this one appended line each.
Commit all three files together with the plain-git form.
  </action>
  <acceptance_criteria>
    - `.planning/STATE.md` contains the phrase naming `37-CI-EVIDENCE.md`'s CI-run table, and contains no remaining reference to `33-CI-EVIDENCE.md`'s CI-run table
    - `git diff HEAD~1 --numstat -- .planning/STATE.md` shows exactly one line added and one removed (plus at most one frontmatter restoration line)
    - `git diff HEAD~1 --numstat -- .planning/phases/29-program-gates-release/29-CI-EVIDENCE.md` shows additions only and zero deletions; the same for the Phase 33 file
    - Each of the two evidence files contains exactly one line mentioning `37-CI-EVIDENCE`
    - `.planning/STATE.md` frontmatter still reads `milestone_name: Durable Agent Execution Runtime`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "37-CI-EVIDENCE.md\`'s CI-run table" .planning/STATE.md && O=$(grep -c "33-CI-EVIDENCE.md\`'s CI-run table" .planning/STATE.md; true); test "$O" = "0" && A=$(grep -c '37-CI-EVIDENCE' .planning/phases/29-program-gates-release/29-CI-EVIDENCE.md; true); test "$A" = "1" && B=$(grep -c '37-CI-EVIDENCE' .planning/phases/33-commissary-in-tree-adoption/33-CI-EVIDENCE.md; true); test "$B" = "1" && grep -q 'milestone_name: Durable Agent Execution Runtime' .planning/STATE.md && git diff HEAD~1 --numstat -- .planning/phases/33-commissary-in-tree-adoption/33-CI-EVIDENCE.md</automated>
  </verify>
  <done>STATE.md points the pre-merge CI run at this phase's evidence file, and both prior evidence files carry exactly one dated forward-pointer line with zero deletions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this phase's edits → a verified phase's sealed evidence | `29-CI-EVIDENCE.md` and `33-CI-EVIDENCE.md` belong to phases whose VERIFICATION is already recorded; anything beyond a single appended line would silently invalidate those verifications |
| requirement id space → traceability | A minted id that exists in only one of the two places (definition row, traceability table) is an untracked requirement; the milestone audit cross-references three sources |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-13 | Tampering | `SHIP-04`'s already-Complete requirement row | high | mitigate | Only an insertion after it is permitted; the acceptance criteria assert the diff contains no removed line mentioning `SHIP-04`, so re-opening or re-wording it cannot pass unnoticed (D-07). |
| T-37-14 | Tampering | Prior phases' sealed CI-evidence tables | high | mitigate | Each file gets exactly one appended dated line; the diff is asserted additions-only with zero deletions (D-05, D-00d). |
| T-37-05 | Tampering | Whole-file rewrites of `.planning/ROADMAP.md` or `.planning/STATE.md` | high | mitigate | Every change is a scoped `Edit`; `Write` is forbidden on these files, and the ROADMAP diff is asserted to be a single changed line — a whole-file write would destroy phase entries outside the edit window. |
| T-37-15 | Repudiation | STATE.md frontmatter clobbered by a GSD helper | medium | mitigate | `milestone_name:` is asserted present after the commit and restored in the same commit if a helper rewrote it. |
</threat_model>

<artifacts_this_phase_produces>
See `37-01-PLAN.md` → "Artifacts this phase produces" for the phase-wide list. This plan creates
the `SHIP-05` requirement id (new identifier, in `.planning/REQUIREMENTS.md`'s definition rows and
its traceability table, and referenced from `.planning/ROADMAP.md`). No code symbols, CLI flags or
struct fields are created, and no file under `src/` or `crates/*/src/` is modified.
</artifacts_this_phase_produces>

<verification>
- `SHIP-05` appears as a definition row and a traceability row; `SHIP-04` is byte-identical
- ROADMAP Phase 37's Requirements line resolves to `SHIP-05` with no other line in the block changed
- STATE.md's pre-merge-CI sentence names this phase's evidence file; the rest of the sentence is unchanged
- Both prior evidence files gained exactly one dated line each, additions-only
</verification>

<success_criteria>
- `grep -c 'SHIP-05' .planning/REQUIREMENTS.md` >= 2 and the traceability row is present verbatim
- The extracted ROADMAP Phase 37 block contains `**Requirements**: SHIP-05` and zero placeholder markers on that line
- `grep -c '37-CI-EVIDENCE'` returns exactly 1 in each of the two prior evidence files
- `git diff` for both prior evidence files shows zero deleted lines
</success_criteria>

<output>
Create `.planning/phases/37-v0-10-0-crate-release/37-04-SUMMARY.md` when done, listing `SHIP-05`
under `requirements-completed` only once the phase's criteria are observably true — at this point
the id is minted, not satisfied.
</output>
