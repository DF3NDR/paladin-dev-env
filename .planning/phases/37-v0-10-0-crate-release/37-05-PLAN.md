---
phase: 37-v0-10-0-crate-release
plan: 05
type: execute
wave: 5
depends_on: [37-04]
files_modified:
  - .project/v0.10.0/09-program-acceptance-audit.md
  - .planning/phases/29-program-gates-release/29-ACCEPTANCE-AUDIT.md
autonomous: true
requirements: [SHIP-05]
must_haves:
  truths:
    - "The program acceptance audit carries a new dated section `## 12. Re-seal for v0.10.0 release (Phase 37, SHIP-05)` whose seven-row gate table gives, per D-06 row, the command, the SHA it ran on, the result, and a pointer to the numbered `37-CI-EVIDENCE.md` row that holds the verbatim output."
    - "Section 12 states in plain words that it extends the evidence basis of the existing §11 sign-off box to this phase's final commit, so the maintainer signs what is visible: §11 plus §12 plus `37-CI-EVIDENCE.md`."
    - "No existing section of the acceptance audit is edited: §11's text and its sign-off box wording are byte-identical, this plan's own commit diff neither adds nor removes a single checkbox-marker line anywhere in the file, the §11 tag line is still unticked after the append, and no second tag sign-off box is minted (D-06, D-00a, D-00d)."
    - "The `29-ACCEPTANCE-AUDIT.md` pointer file gains exactly one dated re-seal paragraph naming section 12 and this phase's evidence file, in the same shape as its existing 2026-09-16 re-seal paragraph."
  artifacts:
    - path: ".project/v0.10.0/09-program-acceptance-audit.md"
      provides: "Section 12 — the v0.10.0 release re-seal gate table and its statement of scope"
      contains: "## 12. Re-seal for v0.10.0 release (Phase 37, SHIP-05)"
    - path: ".planning/phases/29-program-gates-release/29-ACCEPTANCE-AUDIT.md"
      provides: "One dated paragraph pointing at section 12"
      contains: "## 12."
  key_links:
    - from: ".project/v0.10.0/09-program-acceptance-audit.md"
      to: ".planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md"
      via: "every section-12 gate row cites the numbered Local sweep row holding its verbatim output"
      pattern: "37-CI-EVIDENCE"
    - from: ".planning/phases/29-program-gates-release/29-ACCEPTANCE-AUDIT.md"
      to: ".project/v0.10.0/09-program-acceptance-audit.md"
      via: "the pointer file's new dated paragraph names the corpus document's section 12 as the home of this re-seal"
      pattern: "09-program-acceptance-audit"
  prohibitions:
    - statement: "MUST NOT tick, re-word, re-number or duplicate a sign-off box in the acceptance audit — the §11 box is closed by the maintainer only, at plan 37-08's checkpoint, and no agent edit may change a box's state (D-00a, D-06)."
      status: unverified
      verification: judgment
    - statement: "MUST NOT delete, rewrite or renumber existing audit content: section 12 is an append, and sections 1-11 plus the footer's existing lines stay byte-intact (D-00d)."
      status: unverified
      verification: judgment
---

<objective>
Write SC1's "evidence appended to the Phase 29 acceptance audit" — the dated re-seal section that
makes the maintainer's existing §11 sign-off box signable against this phase's final commit.

Purpose: D-06 asks for one new dated re-seal section with a seven-row gate table. Two verified
tree facts shape where it goes. First, `29-ACCEPTANCE-AUDIT.md` in the phase directory is a
29-line **pointer**; the audit body — and the literal sign-off box SC1 names — live in the corpus
document `.project/v0.10.0/09-program-acceptance-audit.md`. Second, that document already carries
a §11 re-seal (Phase 33's) whose closing box reads "The `v0.10.0` tag may be cut", unticked by
design. So this phase follows the Phase 33 precedent exactly: the new section is appended to the
**corpus document** as `## 12.`, the pointer file gains one dated paragraph naming it, and **no
second tag sign-off box is minted** — the maintainer ticks the box that already exists, and §12's
job is to extend what that box's evidence basis covers. That satisfies both D-06's literal file
name and its stated rationale (the box sits in the same file that shows what was re-verified).
Output: corpus section 12, one footer line for it, and one dated pointer-file paragraph.

Governing constraints: sequential execution on the main checkout with hooks ON; every edit is an
append or a scoped `Edit`, never a `Write` over an existing file; the phase prohibitions P1-P5 in
`37-01-PLAN.md` apply.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-v0-10-0-crate-release/37-CONTEXT.md
@.planning/phases/37-v0-10-0-crate-release/37-PATTERNS.md
@.planning/phases/37-v0-10-0-crate-release/37-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append section 12 — the v0.10.0 release re-seal gate table — to the corpus acceptance audit</name>
  <files>.project/v0.10.0/09-program-acceptance-audit.md</files>
  <read_first>
    - `.project/v0.10.0/09-program-acceptance-audit.md` lines 1391-1559 — section 11 in full: its opening paragraph naming which phases changed API and why the re-seal exists, its head-SHA paragraph, its per-gate `###` subsections with fenced commands and `**Verdict: PASS**` lines, its `**Findings:**` list, its closing sign-off box, and the four italic footer lines
    - `.planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md` — every numbered Local sweep row, which is what each section-12 row cites; the provenance block holds the head SHA
    - `.planning/phases/37-v0-10-0-crate-release/37-CONTEXT.md` — D-06's seven named gate rows, in order, and the rule that existing rows are not edited
    - `.planning/phases/37-v0-10-0-crate-release/37-PATTERNS.md` — the section-12 heading text and the "leave §11 untouched" instruction
  </read_first>
  <action>
Append one new section at the end of the corpus document, immediately before the trailing italic
footer lines, and add one footer line for it.

Heading, exactly: `## 12. Re-seal for v0.10.0 release (Phase 37, SHIP-05)`.

Content, in section 11's own voice and structure:
1. An opening paragraph stating why this re-seal exists: Phases 34, 35, 36 and 36.1 landed after
   §11 was written, and this phase's goal is the release itself rather than releasability, so the
   full release-gate list was re-run on the phase's final pre-merge commit.
2. A head-SHA paragraph naming the local re-seal head SHA from `37-CI-EVIDENCE.md`'s provenance
   block, and stating that this phase modifies no file under `src/` or `crates/*/src/` — so the
   measured tree is identical to the shipped tree.
3. The **seven-row gate table** — D-06's rows in D-06's order, one row each, with columns for the
   gate, the command, the SHA, the result, and the pointer to the `37-CI-EVIDENCE.md` Local sweep
   row number holding the verbatim output:
   - row 1: the `MIGRATION.md` no-placeholder-marker check plus the §9.2 register ↔
     `cargo semver-checks` allowlist set-equality check
   - row 2: the frozen v0.9 config-boot target (with its asserted selected-test count)
   - row 3: the path-restricted OpenAPI golden diff (with its asserted count)
   - row 4: `cargo semver-checks` for the eleven baselined crates, with the `paladin-eval`
     exclusion reason stated in the row
   - row 5: the MSRV floor under toolchain 1.88
   - row 6: `make publish-dry-run` — twelve crates, dependency order
   - row 7: the `CHANGELOG.md` `[0.10.0]` completeness reading, naming which checks were hard
     assertions and which were recorded readings
4. A short paragraph on the two gates §11 also recorded as outside the seven: the 82 % workspace
   line-coverage floor, **CI-attributed** because Docker is absent from this devcontainer (D-00e)
   — pointing at the CI-run table for its conclusion and printed figure — and the API-surface
   baseline, recorded unmoved.
5. A `**Findings:**` list carrying this phase's recorded, unfixed findings by reference to
   `37-CI-EVIDENCE.md`'s own findings section (the stale eleven-crate documentation figures, the
   missing `paladin-eval` Trusted Publishing row, the D-13 non-dispatch, the changelog heading
   date).
6. A closing **scope paragraph** — the load-bearing one. State in plain words that section 12
   **extends the evidence basis of the existing §11 sign-off box to this phase's final commit**:
   the box the maintainer ticks is §11's own "The `v0.10.0` tag may be cut", and what it now
   covers is §11 plus §12 plus `37-CI-EVIDENCE.md`'s complete Local sweep and CI-run tables.
   State explicitly that section 12 mints no new sign-off box and edits no word of §11.

Then add one italic footer line after the existing three, in their voice, attributing section 12
to Phase 37 plan 37-05 (SHIP-05, the v0.10.0 release re-seal).

Forbidden in this task, and asserted by the acceptance criteria: changing any box's state
anywhere in the file, adding a new box, editing §11's text, or renumbering any section. The
assertion is made against **this commit's own diff** — that it neither adds nor removes a
checkbox-marker line — plus a line-scoped reading that the §11 tag line is still unticked. It is
deliberately **not** made against file-wide box totals: the file also holds seven independent
maintainer sign-off boxes from Phase 29 §10 which the maintainer may close at any time, and a
total-count assertion would break on their action rather than on anything this plan did. Commit
with the plain-git form and a >= 600 s timeout.
  </action>
  <acceptance_criteria>
    - `grep -c '^## 12\. Re-seal for v0.10.0 release (Phase 37, SHIP-05)'` returns exactly 1
    - Section 12 contains a table with exactly seven gate rows, each naming a command and citing a `37-CI-EVIDENCE.md` row number
    - Section 12 contains a sentence stating that it extends the evidence basis of §11's sign-off box to this phase's final commit, and a sentence stating that it mints no new sign-off box
    - This commit's own diff of the audit file contains zero added and zero removed checkbox-marker lines, so no box state changed and no box was minted; no assertion is made about file-wide box totals
    - `git diff HEAD~1 --numstat -- .project/v0.10.0/09-program-acceptance-audit.md` shows deletions of at most 1 line (the footer insertion point), and `git diff HEAD~1` contains no removed line from within section 11
    - The line whose text begins "**The `v0.10.0` tag may be cut**" appears exactly once and is still unticked
  </acceptance_criteria>
  <verify>
    <automated>H=$(grep -c '^## 12\. Re-seal for v0.10.0 release (Phase 37, SHIP-05)' .project/v0.10.0/09-program-acceptance-audit.md; true) && test "$H" = "1" && git diff HEAD~1 -- .project/v0.10.0/09-program-acceptance-audit.md > /tmp/d5.txt && B=$(grep -c '^[+-]- \[' /tmp/d5.txt; true) && test "$B" = "0" && U=$(grep -c '^- \[ \] \*\*The .v0\.10\.0. tag may be cut\*\*' .project/v0.10.0/09-program-acceptance-audit.md; true) && test "$U" = "1" && P=$(grep -c '37-CI-EVIDENCE' .project/v0.10.0/09-program-acceptance-audit.md; true) && test "$P" -ge 7</automated>
  </verify>
  <done>Section 12 exists with its seven-row gate table, its CI-attributed coverage paragraph, its findings list and its scope paragraph; this commit's diff adds and removes no checkbox marker and the §11 tag line is still unticked; one footer line attributes the new section.</done>
</task>

<task type="auto">
  <name>Task 2: Add the pointer file's dated re-seal paragraph</name>
  <files>.planning/phases/29-program-gates-release/29-ACCEPTANCE-AUDIT.md</files>
  <read_first>
    - `.planning/phases/29-program-gates-release/29-ACCEPTANCE-AUDIT.md` — all 29 lines; in particular the existing `**Re-sealed on \`69500c9b…\`, 2026-09-16.**` paragraph, which is the exact shape to copy
    - `.planning/phases/37-v0-10-0-crate-release/37-PATTERNS.md` — the filled-in shape for this phase's paragraph
    - `.project/v0.10.0/09-program-acceptance-audit.md` section 12 as Task 1 wrote it — so the paragraph's claims match what the section actually says
  </read_first>
  <action>
Append exactly one dated paragraph to the pointer file, after the existing 2026-09-16 re-seal
paragraph, copying that paragraph's shape and voice: a bold `**Re-sealed on <final SHA>,
2026-09-18.**` opener, then the statement that Phases 34-36.1 landed after §11 and that Phase 37
(SHIP-05) re-ran the full release-gate list for the release itself and appended the result as
`## 12. Re-seal for v0.10.0 release (Phase 37, SHIP-05)` in the corpus document, that this
pointer's ten-section scope is otherwise unchanged, and that the full verbatim evidence lives in
`.planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md`. Close with the one clause that
matters for D-00a: section 12 adds **no** new sign-off box — the human-only box for cutting the
tag is §11's, still unticked, and it is closed by the maintainer at UAT / the phase's own
checkpoint, never by an agent.

Change nothing above the new paragraph. Commit with the plain-git form.
  </action>
  <acceptance_criteria>
    - `.planning/phases/29-program-gates-release/29-ACCEPTANCE-AUDIT.md` contains a paragraph dated `2026-09-18` naming `## 12. Re-seal for v0.10.0 release (Phase 37, SHIP-05)` and `37-CI-EVIDENCE.md`
    - The paragraph states that no new sign-off box was added and that §11's box is closed by the maintainer only
    - `git diff HEAD~1 --numstat -- .planning/phases/29-program-gates-release/29-ACCEPTANCE-AUDIT.md` shows additions only and zero deletions
    - The file still contains its original 2026-09-16 re-seal paragraph verbatim
  </acceptance_criteria>
  <verify>
    <automated>grep -q '2026-09-18' .planning/phases/29-program-gates-release/29-ACCEPTANCE-AUDIT.md && grep -q '## 12. Re-seal for v0.10.0 release' .planning/phases/29-program-gates-release/29-ACCEPTANCE-AUDIT.md && grep -q '69500c9b' .planning/phases/29-program-gates-release/29-ACCEPTANCE-AUDIT.md && D=$(git diff HEAD~1 --numstat -- .planning/phases/29-program-gates-release/29-ACCEPTANCE-AUDIT.md | cut -f2; true); test "$D" = "0"</automated>
  </verify>
  <done>The pointer file carries one dated 2026-09-18 re-seal paragraph naming section 12 and this phase's evidence file, states that no new sign-off box was added, and its earlier content is byte-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent-written evidence → human sign-off authority | Section 12 is what the maintainer reads before ticking the one box that authorises a tag; a row claiming more than the Local sweep proves would launder an unearned authority |
| this phase's append → a sealed audit's ten verified sections | Sections 1-11 are verified content; a renumbering or in-place edit would invalidate every plan and SUMMARY that cites a section number |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-05 | Tampering | Sign-off box state in the acceptance audit | high | mitigate | No agent edit may change a box's state: the acceptance criteria assert this commit's own diff adds and removes zero checkbox-marker lines, and that the §11 tag line is still unticked — a diff-scoped and line-scoped pair of checks that cannot be invalidated by the maintainer independently closing any of the file's seven Phase 29 §10 sign-off boxes. The plan mints no new box (D-00a, D-06). |
| T-37-14 | Tampering | Sections 1-11 and the existing footer lines | high | mitigate | Section 12 is appended before the footer and one footer line is added; the diff is asserted to remove no line from within section 11 (D-00d). |
| T-37-08 | Repudiation | Section-12 gate rows | high | mitigate | Every row carries command + SHA + result + a pointer to the numbered `37-CI-EVIDENCE.md` row holding the verbatim output, so no claim in the audit is unsourced; the CI-attributed coverage gate is named as such rather than rolled into the seven. |
</threat_model>

<artifacts_this_phase_produces>
See `37-01-PLAN.md` → "Artifacts this phase produces" for the phase-wide list. This plan creates
the corpus section heading `## 12. Re-seal for v0.10.0 release (Phase 37, SHIP-05)` in
`.project/v0.10.0/09-program-acceptance-audit.md` (new identifier — later phases and audits will
cite it by number). No code symbols are created and no file under `src/` or `crates/*/src/` is
modified.
</artifacts_this_phase_produces>

<verification>
- Corpus section 12 exists exactly once, with a seven-row gate table sourced row-by-row from `37-CI-EVIDENCE.md`
- Section 12 states that it extends §11's sign-off box evidence basis and that it mints no new box
- This plan's own diff of the audit adds and removes no checkbox-marker line, and the §11 tag line is still unticked; §11's text is byte-identical
- The pointer file gained exactly one dated paragraph, additions-only
</verification>

<success_criteria>
- `grep -c '^## 12\. Re-seal for v0.10.0 release (Phase 37, SHIP-05)'` on the corpus file returns 1
- `git diff HEAD~1 -- .project/v0.10.0/09-program-acceptance-audit.md | grep -c '^[+-]- \['` is 0, and the §11 tag line still reads unticked exactly once
- The corpus file cites `37-CI-EVIDENCE` at least seven times (one per gate row)
- The pointer file mentions section 12, `37-CI-EVIDENCE.md` and the date 2026-09-18, with zero deleted lines
</success_criteria>

<output>
Create `.planning/phases/37-v0-10-0-crate-release/37-05-SUMMARY.md` when done. Record the final
pre-merge head SHA that section 12 names, because plan 37-08's §11-tick checkpoint hand-off quotes
it back to the maintainer.
</output>
