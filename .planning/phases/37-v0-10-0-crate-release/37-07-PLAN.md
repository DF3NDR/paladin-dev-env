---
phase: 37-v0-10-0-crate-release
plan: 07
type: execute
wave: 7
depends_on: [37-06]
files_modified:
  - .planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md
autonomous: true
requirements: [SHIP-05]
must_haves:
  truths:
    - "Every workflow run on the PR head SHA is recorded in the CI-run table by workflow, run ID with URL, conclusion, SHA and note — including both `ci.yml` trigger events on that SHA."
    - "SC2 is satisfied only by the CI `coverage` job's own `success` conclusion together with its printed line-coverage figure recorded verbatim; a conclusion other than `success` is a D-14 stop even when the printed figure looks above the floor, and a figure sitting exactly on the 82 % floor is recorded as CI reported it rather than re-derived, rounded or interpolated."
    - "A red required check stops the phase with the run ID, the failing job and the failing log excerpt recorded; the single exception is a pure infrastructure flake, where exactly one `gh run rerun --failed` is permitted and both the red run ID and the re-run ID are recorded with the log line that justified the classification (D-14, D-15)."
    - "This plan's commit is the last agent commit on `feature/phase-33`: everything observed after the maintainer's §11 tick is recorded on `chore/37-close` instead (D-03, D-10)."
  artifacts:
    - path: ".planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md"
      provides: "The pre-merge CI-run table with conclusions, and the recorded coverage figure that satisfies SC2"
      contains: "## CI-run table"
  key_links:
    - from: ".planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md"
      to: "the CI `coverage` job on the PR head run"
      via: "the recorded job conclusion plus the printed line-coverage figure is the sole evidence for SC2, since Docker is absent from this devcontainer"
      pattern: "coverage"
  prohibitions:
    - statement: "MUST NOT silently fix, work around, or re-run past a red gate; a red required check is recorded with its run ID, job name and failing log excerpt and stops the phase at a blocking checkpoint (D-14)."
      status: unverified
      verification: judgment
    - statement: "MUST NOT re-run a red run more than once, and MUST NOT classify a failure as an infrastructure flake unless the log shows no test or gate assertion failed; a MinIO-image failure on this branch is a real failure, not a flake, because the branch already carries the registry-pin fix (D-15)."
      status: unverified
      verification: judgment
    - statement: "MUST NOT record any gate as green that was not actually run on the stated SHA, and MUST NOT mark a success criterion satisfied by promise (D-00e, D-09)."
      status: unverified
      verification: judgment
---

<objective>
Record the pre-merge CI evidence — including the one gate this devcontainer structurally cannot
measure — and close the branch's agent-side work.

Purpose: D-03's order puts "PR CI green including `coverage` at or above 82 %" and "evidence
appended" immediately before the maintainer's §11 tick. This plan supplies both. It is also the
last plan that commits to `feature/phase-33`: the tick must be the last content commit on the
branch so the maintainer signs what is visible, and D-10 routes everything observed afterwards
onto `chore/37-close`.
Output: a filled CI-run table for the PR head SHA, the recorded coverage conclusion and figure,
and a final docs-only commit pushed to the branch.

Governing constraints: sequential execution on the main checkout with hooks ON; the phase
prohibitions P1-P5 in `37-01-PLAN.md` apply. **D-14 governs every red result here**, with D-15's
narrow infrastructure-flake exception as the only relief.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-v0-10-0-crate-release/37-CONTEXT.md
@.planning/phases/37-v0-10-0-crate-release/37-RESEARCH.md
@.planning/phases/37-v0-10-0-crate-release/.continue-here.md
@.planning/phases/37-v0-10-0-crate-release/37-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record every workflow run on the PR head SHA, and classify any red result under D-14/D-15</name>
  <precondition>`gh run list --branch feature/phase-33 --workflow ci.yml --json headSha,event,status,conclusion` shows a `completed` `ci.yml` run on the PR head SHA recorded in `37-CI-EVIDENCE.md` for both the `push` and the `pull_request` event</precondition>
  <files>.planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/33-commissary-in-tree-adoption/33-CI-EVIDENCE.md` — the CI-run table's exact column set (`Workflow | Run ID (URL) | Conclusion | SHA | Notes`) and the note style, including how a skipped-by-design job is described so it is not misread as a failure
    - `.planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md` — the PR head SHA, PR number and the two pending run IDs plan 37-06 recorded
    - `.planning/phases/37-v0-10-0-crate-release/37-RESEARCH.md` Q6 (the 44 required contexts, the two deliberately-excluded jobs, and the confirmation that this branch already carries the container-registry pin so a MinIO failure would be real) and D-15's text
    - `.planning/phases/37-v0-10-0-crate-release/37-CONTEXT.md` — D-14 and D-15 in full
  </read_first>
  <action>
Read the runs, record them, and classify any red result by the pre-declared rule rather than by
judgment at the keyboard.

1. List every workflow run on the PR head SHA — `ci.yml` (both trigger events), `codeql.yml`,
   `feature-flags.yml`, `pre-commit`, and `docs.yml` (which is PR-gated and now runs for the first
   time on this branch). Record one CI-run table row per run: workflow, run ID with its URL,
   conclusion, SHA, and a note. For `ci.yml`, also record the job tally (how many jobs, how many
   failed, how many skipped by design) in the note, in the house style — a skipped tag-gated job
   is not a failure and the note must say so.
2. Confirm the PR's own mergeability state from GitHub's perspective and record it: the count of
   required status contexts reporting success. `codeql.yml` is advisory-only on this project and is
   not a required context — record its conclusion and label it advisory.
3. If any required check is red:
   - Read the failing job's log and decide by D-15's test only: is there **no** test or gate
     assertion failure in the log — a runner timeout, a registry 5xx, an image-pull failure? If
     and only if so, this is an infrastructure flake, and exactly **one**
     `gh run rerun --failed <run-id>` is permitted. Record the red run ID, the re-run ID, and the
     verbatim log line that justified the classification. Before any re-run, check `gh run list`
     to be sure no other run against the same SHA is in flight.
   - Otherwise — or on a second red, or on any assertion failure — **stop**. Record the run ID,
     the job name, the failing log excerpt, the SHA, and the fact that nothing was fixed, then
     halt for the maintainer (D-14). Fix nothing: not a documentation line, not a citation, not an
     allowlist entry. A MinIO-related failure on this branch is a real failure, not a flake.
4. Commit with the plain-git form.
  </action>
  <acceptance_criteria>
    - The CI-run table contains one row per workflow run on the PR head SHA, each with a run ID, a URL, a conclusion and the SHA
    - Both `ci.yml` rows (the `push`-event and `pull_request`-event runs on the same SHA) are present with their conclusions filled in
    - The `ci.yml` notes record the job tally and describe skipped-by-design jobs as such
    - `codeql.yml`'s row is labelled advisory-only
    - The table records the count of required status contexts reporting success
    - If a re-run was used, both the red run ID and the re-run ID appear with the justifying log line; no second re-run exists
  </acceptance_criteria>
  <verify>
    <automated>SHA=$(gh pr view --json headRefOid --jq '.headRefOid'); gh run list --branch feature/phase-33 --limit 20 --json workflowName,headSha,event,conclusion > /tmp/runs.json && node -e 'const r=require("/tmp/runs.json");const s=process.argv[1];const m=r.filter(x=>x.headSha===s);if(m.length<2)process.exit(1);if(m.some(x=>x.conclusion===null))process.exit(1);process.stdout.write(String(m.length))' "$SHA" && C=$(grep -c "$SHA" .planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md; true); test "$C" -ge 2</automated>
  </verify>
  <done>Every run on the PR head SHA is recorded with its conclusion and note, the required-context tally is on record, and any red result was either classified as an infrastructure flake with both run IDs recorded or halted the phase with nothing fixed.</done>
</task>

<task type="auto">
  <name>Task 2: Record the CI `coverage` job's conclusion and printed figure — the sole evidence for SC2</name>
  <files>.planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md</files>
  <read_first>
    - `.planning/decisions/0006-coverage-gate.md` — ADR-0006: the 82 % workspace line-coverage floor as a single number, and `cargo llvm-cov --fail-under-lines` as its enforcement point
    - `.planning/phases/37-v0-10-0-crate-release/37-RESEARCH.md` "Success Criteria → Evidence Map" row SC2 — the exact `gh run view --json jobs` query shape and the note that the figure comes from the job's own step summary or log
    - `.project/v0.10.0/09-program-acceptance-audit.md` lines 1485-1497 — §11's CI-attributed coverage paragraph, the wording model for this record
    - `.planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md` — the `ci.yml` rows Task 1 recorded, which this reading hangs off
  </read_first>
  <action>
Record SC2 from CI and nowhere else.

1. Query the `ci.yml` run's job list for the coverage job and record its conclusion verbatim.
2. Recover the printed line-coverage figure from that job's step summary or log and record it
   **to the digits CI printed** — no rounding, no truncation, no re-derivation, and no
   recomputation from any other source. State the tool invocation CI used
   (`cargo llvm-cov ... --fail-under-lines 82`) alongside it so a reader can see which comparison
   produced the verdict.
3. Write the SC2 verdict by this rule, stated before the number is known so it cannot be bent to
   fit: **SC2 is satisfied if and only if the coverage job's own conclusion is `success`.** The
   printed figure is recorded as corroborating evidence, not as the verdict — if the job concluded
   anything other than `success`, that is a D-14 stop even when the printed figure looks above the
   floor, and conversely the agent does not overturn a `success` conclusion by re-reading the
   number. How `--fail-under-lines` treats a figure sitting exactly on the floor is CI's own
   contract; this phase records CI's verdict rather than asserting the comparison itself.
4. Add one sentence to the record naming Docker's absence from this devcontainer as the structural
   reason this gate is CI-attributed (D-00e), and pointing at the todo that Phase 36.1 D-23
   re-homed to the maintainer for local reproduction — recorded, not re-opened here.
5. Commit with the plain-git form.
  </action>
  <acceptance_criteria>
    - `37-CI-EVIDENCE.md` records the coverage job's conclusion verbatim and the printed line-coverage figure with its original digits
    - The record names the `cargo llvm-cov` invocation with its `--fail-under-lines` threshold
    - The record states that SC2's verdict is the job's own conclusion, and that a non-`success` conclusion is a D-14 stop regardless of the printed figure
    - The record names Docker's absence as the reason the gate is CI-attributed
    - No local coverage measurement is claimed anywhere in the file
  </acceptance_criteria>
  <verify>
    <automated>RID=$(gh run list --branch feature/phase-33 --workflow ci.yml --limit 5 --json databaseId --jq '.[0].databaseId'); gh run view "$RID" --json jobs --jq '.jobs[] | select(.name | test("[Cc]overage")) | .conclusion' > /tmp/cov.txt && test -s /tmp/cov.txt && grep -qi 'fail-under-lines' .planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md && grep -qi 'CI-attributed' .planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md</automated>
  </verify>
  <done>The coverage job's conclusion and its printed figure are on record with the verdict rule stated, Docker's absence is named as the reason the gate is CI-attributed, and no local pass is claimed.</done>
</task>

<task type="auto">
  <name>Task 3: Push the appended evidence — the last agent commit on `feature/phase-33`</name>
  <files>.planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md` — the whole file as Tasks 1-2 left it, plus its provenance block, whose PR-head line is now fillable
    - `.planning/phases/37-v0-10-0-crate-release/37-CONTEXT.md` — D-03 (the tick is the last content commit on the branch) and D-10 (post-tick commits go to `chore/37-close`)
    - `.pre-commit-config.yaml` — the pre-push hooks this push runs again
  </read_first>
  <action>
Finish the branch's agent-side work and hand the last word to the maintainer.

1. Fill the provenance block's PR-head line with the SHA the recorded runs ran on.
2. Add a short paragraph to `## Summary and what remains` stating, in plain words, the commit
   boundary this phase operates under: this is the **last agent commit on `feature/phase-33`**;
   the maintainer's §11 tick is the next and last content commit on the branch (D-03), and
   everything observed after it — the CI runs on the post-tick final SHA, the merge commit, the
   post-merge runs, the release run, and the registry table — is recorded on the `chore/37-close`
   branch cut from the tagged `main` (D-10). Name plans 37-09 through 37-11 as the ones that do
   that, so a reader is not left wondering why the table stops here.
3. Record that the two `ci.yml` runs the coming push will trigger have their conclusions recorded
   on `chore/37-close`, not here — committing them here would add an agent commit after the tick.
   This is D-03's accepted cost: one extra CI cycle on a docs-only commit.
4. **Commit the evidence append, but do not push yet.** The push happens once this plan's SUMMARY
   is also committed, so both commits ride one `git push` and trigger one CI cycle rather than two
   (see `<output>`). Use the plain-git form with a >= 600 s timeout.
  </action>
  <acceptance_criteria>
    - `37-CI-EVIDENCE.md`'s provenance block names the PR-head SHA the recorded runs ran on
    - The summary contains a paragraph naming this as the last agent commit on `feature/phase-33` and naming `chore/37-close` as the home of all post-tick evidence, listing plans 37-09 through 37-11
    - The evidence commit exists locally and touches only `.planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md`
    - The branch is at least one commit ahead of `origin/feature/phase-33` at this point (the push is deferred to the SUMMARY step)
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'chore/37-close' .planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md && grep -q 'last agent commit' .planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md && git show --name-only --format= HEAD > /tmp/files.txt && test "$(wc -l < /tmp/files.txt)" -le 2 && A=$(git rev-list --count origin/feature/phase-33..HEAD); test "$A" -ge 1</automated>
  </verify>
  <done>The provenance block names the PR-head SHA, the commit boundary is written into the summary, and the evidence append is committed locally awaiting the single push that also carries this plan's SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI job conclusions → success-criteria verdicts | GitHub's recorded conclusions are the authority for SC2 and for the required-context tally; an agent re-interpretation would substitute an unverifiable local judgment for a measured one |
| CI logs → flake classification | A red run's log is the only admissible basis for calling a failure infrastructure rather than substantive; the classification decides whether the phase continues or stops |
| branch commit history → the maintainer's sign-off | The tick must be the last content commit; an agent commit after it would mean the human signed something other than what shipped |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-09 | Tampering | A red required check repaired, re-run repeatedly, or mis-labelled a flake | high | mitigate | D-14 halts the phase with run ID, job name and log excerpt recorded; D-15 permits exactly one re-run and only when the log shows no assertion failure, with both run IDs and the justifying log line recorded; a MinIO failure on this branch is pre-declared a real failure. |
| T-37-11 | Repudiation | The coverage figure | high | mitigate | SC2's verdict is the job's own `success` conclusion; the printed figure is recorded to CI's own digits as corroboration, never rounded, re-derived or overturned locally (D-00e). |
| T-37-18 | Tampering | An agent commit landing after the maintainer's §11 tick | high | mitigate | This plan's push is declared the last agent commit on the branch, the boundary is written into the evidence file, and plans 37-09 to 37-11 commit only on `chore/37-close` (D-03, D-10). |
| T-37-19 | Denial of Service | A second run dispatched against a SHA while another is in flight | low | accept | Before any re-run the plan checks `gh run list` for an in-flight run against the same SHA; the agent never dispatches a new workflow run. |
</threat_model>

<artifacts_this_phase_produces>
See `37-01-PLAN.md` → "Artifacts this phase produces" for the phase-wide list. This plan creates no
new file or identifier; it fills the CI-run table and provenance block of
`.planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md`. No code symbols are created and no
file under `src/` or `crates/*/src/` is modified.
</artifacts_this_phase_produces>

<verification>
- Every workflow run on the PR head SHA is recorded with run ID, URL, conclusion, SHA and note
- The coverage job's conclusion and printed figure are recorded, with the verdict rule and the CI-attribution reason stated
- Any red result was either classified a flake under D-15 with both run IDs recorded, or halted the phase with nothing fixed
- The final docs-only commit is pushed and declared the last agent commit on `feature/phase-33`
</verification>

<success_criteria>
- `gh run list` shows completed `ci.yml` runs on the recorded PR head SHA for both trigger events, and both are in the CI-run table
- The coverage job's conclusion is recorded verbatim and the printed figure appears with its original digits
- After the SUMMARY push, `git rev-parse HEAD` equals `git rev-parse origin/feature/phase-33` and the branch has no uncommitted changes
- The evidence file names `chore/37-close` as the home of all post-tick evidence
</success_criteria>

<output>
Create `.planning/phases/37-v0-10-0-crate-release/37-07-SUMMARY.md` when done, commit it, and then
run a single `git push origin feature/phase-33` (timeout >= 1800 s, never `--no-verify`) carrying
both that commit and Task 3's evidence commit — one push, one CI cycle, and no SUMMARY left to
commit after the maintainer's tick. Record the coverage figure and the required-context tally in
the SUMMARY as well, because plan 37-08's checkpoint quotes both back to the maintainer. After the
push, record nothing further on this branch: plan 37-08 makes no commits at all.
</output>
