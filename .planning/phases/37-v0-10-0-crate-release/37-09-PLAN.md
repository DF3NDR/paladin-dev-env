---
phase: 37-v0-10-0-crate-release
plan: 09
type: execute
wave: 9
depends_on: [37-08]
files_modified:
  - .planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md
  - .planning/phases/37-v0-10-0-crate-release/.continue-here.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [SHIP-05]
must_haves:
  truths:
    - "The post-tag wave opens by re-verifying the written resume condition before anything else: tag `v0.10.0` exists on `origin` and points at a commit contained in `main` (D-12)."
    - "The tagged commit is proven EQUAL to the pull request's merge commit, not merely an ancestor of `main`: `git rev-parse v0.10.0^{commit}` equals the recorded merge-commit SHA, and the containment check passes as well — ancestry alone is not accepted as proof (SC3)."
    - "All post-tick and post-merge evidence lands on a `chore/37-close` branch cut from the tagged `main`, never on `feature/phase-33`, and the tagged commit stays exactly what was released (D-10)."
    - "Before any post-tag record is written, `git log <tick-sha>..feature/phase-33` is asserted empty: no local-only commit — including a stray orchestrator progress-tracking commit for wave 8 — sits on the abandoned feature branch after the maintainer's tick; if one does, it is recorded as a D-00d finding and its content is carried forward by re-applying it on `chore/37-close`, and `feature/phase-33` is never pushed and never rewritten."
    - "Release health is read from the `publish-crates` job's own conclusion and its per-crate outcome table, never from the workflow run's overall conclusion, and the per-crate outcomes are recorded in the dependency order the job emitted them — when two crates' outcomes are identical the recorded order is the job's emitted order, never re-sorted."
    - "If the release run is red, the agent's scope is read-only diagnosis per the recovery runbook §1-§2 plus naming the failure code from its §6 table; it writes that down and hard-stops. Completing forward, re-dispatching and yanking belong to the maintainer (D-16)."
  artifacts:
    - path: ".planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md"
      provides: "The post-tick, post-merge and release-run CI-run rows, the tag-equality proof, and the D-13 non-dispatch record in its final form"
      contains: "## CI-run table"
  key_links:
    - from: "the `v0.10.0` git tag"
      to: "the pull request's merge commit on `main`"
      via: "an equality assertion between the tag's commit and the recorded merge SHA, plus the ancestor containment check"
      pattern: "v0.10.0"
    - from: ".planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md"
      to: "the `release.yml` run triggered by the tag push"
      via: "the `publish-crates` job's own conclusion and per-crate outcome table are the recorded proof, not the workflow's overall conclusion"
      pattern: "publish-crates"
  prohibitions:
    - statement: "MUST NOT trigger, dispatch, re-dispatch or re-run the release workflow, and MUST NOT yank any published version — the agent's post-tag scope is read-only diagnosis and recording (D-16)."
      status: unverified
      verification: judgment
    - statement: "MUST NOT commit anything to `feature/phase-33` after the maintainer's tick; every post-tick artifact lands on `chore/37-close` (D-03, D-10)."
      status: unverified
      verification: judgment
    - statement: "MUST NOT infer publish health from the release workflow's overall run conclusion — the Build Binaries matrix has a documented history of failing without affecting publish health, so only the `publish-crates` job's own table is admissible."
      status: unverified
      verification: judgment
---

<objective>
Open the post-tag wave: prove the resume condition, move onto the close-out branch, and record
what the release actually did.

Purpose: D-09 keeps the phase open across the merge precisely so SC3 and SC4 are verified by
observation rather than by promise. D-12's resume condition is re-verified here before anything
else happens; D-10 routes every post-tag commit through a `chore/37-close` branch cut from the
tagged `main` and a second small docs-only PR, so the tagged commit stays exactly what was
released and the protected branch is never bypassed.
Output: the tag-equality proof, the `chore/37-close` branch carrying the deferred pre-merge
artifacts, and the CI-run rows for the post-tick head, the merge commit and the real release run.

Governing constraints: the phase prohibitions P1-P5 in `37-01-PLAN.md` apply. D-16 bounds the
agent's post-tag authority to read-only diagnosis; D-15's one-re-run flake exception still applies
to a pure infrastructure failure, and nothing else.

**ORCHESTRATOR NOTE — deferred wave-8 tracking resumes here, on `chore/37-close`.**
This is addressed to the `/gsd-execute-phase` orchestrator, not to this plan's executor. Per the
ORCHESTRATOR NOTE in `37-08-PLAN.md`, no progress-tracking commit was made for wave 8 and no
`state.*`/`roadmap.*` helper that writes a tracked file was called while `feature/phase-33` was
checked out. That deferral ends once this plan's Task 2 has created `chore/37-close`: from that
point on, the deferred wave-8 progress tracking **and** all wave 9-11 progress tracking land on
`chore/37-close` — never on `feature/phase-33`, which is dead after the merge (D-10). Do not
create a tracking commit between this plan's Task 1 and Task 2; Task 1 runs while
`feature/phase-33` is still the checked-out branch, and Task 2's own reconciliation check asserts
that nothing landed there after the maintainer's tick.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-v0-10-0-crate-release/37-CONTEXT.md
@.planning/phases/37-v0-10-0-crate-release/37-RESEARCH.md
@.planning/phases/37-v0-10-0-crate-release/.continue-here.md
@.planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify the resume condition and prove the tag sits on the merge commit itself</name>
  <precondition>`git fetch --tags origin` then `git rev-parse v0.10.0^{commit}` resolves — the tag exists on `origin` (this is the written resume condition; if it does not resolve, the maintainer has not tagged yet and the phase stays stopped)</precondition>
  <files>.planning/phases/37-v0-10-0-crate-release/.continue-here.md</files>
  <read_first>
    - `.planning/phases/37-v0-10-0-crate-release/.continue-here.md` — the resume condition as plan 37-08 wrote it, and the post-tick final SHA
    - `.planning/phases/37-v0-10-0-crate-release/37-08-SUMMARY.md` (uncommitted in the working tree) — the merge SHA the maintainer reported
    - `.planning/phases/37-v0-10-0-crate-release/37-RESEARCH.md` "Success Criteria → Evidence Map" row SC3 — why both the equality check and the containment check are needed, and why ancestry alone is insufficient
    - `.planning/phases/37-v0-10-0-crate-release/37-CONTEXT.md` — D-12 (the resume condition) and D-00b (the two-SHA rule)
  </read_first>
  <action>
Verify before doing anything else, and prove the stronger of the two possible claims.

1. `git fetch --tags origin`, then resolve the tag to its commit.
2. **Containment:** confirm the tagged commit is contained in `origin/main`.
3. **Equality — the check that actually matters.** Confirm the tagged commit is *the* merge commit:
   resolve the PR's merge commit from GitHub and assert it equals the tag's commit exactly.
   Containment alone would also be satisfied by a tag on any other ancestor of `main`, which is a
   different release than the one the evidence describes. Also assert the merge commit has two
   parents — a merge-commit merge, not a squash or a rebase (D-04) — and record both parent SHAs:
   the pre-merge `feature/phase-33` tick commit and the previous `main` tip.
4. Confirm the tag is **annotated**, not lightweight, and record its tagger date and message.
5. Record all of it in the working-tree `.continue-here.md` for now (the branch to commit on does
   not exist yet — Task 2 creates it), and note the merge SHA, the tag's commit SHA, and both
   parent SHAs, since the provenance block and MILESTONES.md entry both need them.
6. If the equality check fails, or the merge commit has one parent, **stop** and record it: the
   release that shipped is not the one this evidence describes, and reconciling that is the
   maintainer's decision, not a fix this phase makes.
  </action>
  <acceptance_criteria>
    - `git rev-parse v0.10.0^{commit}` resolves and equals the merge commit SHA reported by `gh pr view --json mergeCommit`
    - `git merge-base --is-ancestor v0.10.0^{commit} origin/main` succeeds
    - The merge commit has exactly two parents, both recorded by SHA
    - `git cat-file -t v0.10.0` reports `tag` (annotated), and its message and tagger date are recorded
    - `.continue-here.md` records the tag commit SHA, the merge SHA and both parent SHAs
  </acceptance_criteria>
  <verify>
    <automated>git fetch --tags origin && TC=$(git rev-parse v0.10.0^{commit}) && MC=$(gh pr list --head feature/phase-33 --state merged --json mergeCommit --jq '.[0].mergeCommit.oid') && test "$TC" = "$MC" && git merge-base --is-ancestor "$TC" origin/main && P=$(git rev-list --parents -n 1 "$TC" | wc -w) && test "$P" = "3" && test "$(git cat-file -t v0.10.0)" = "tag"</automated>
  </verify>
  <done>The resume condition is re-verified, and the tag is proven to sit on the merge commit itself — equality plus containment, with a two-parent merge commit and an annotated tag, all recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Cut `chore/37-close` from the tagged `main` and commit the deferred pre-merge artifacts</name>
  <files>.planning/phases/37-v0-10-0-crate-release/.continue-here.md</files>
  <read_first>
    - `.planning/phases/37-v0-10-0-crate-release/37-CONTEXT.md` — D-10 (the `chore/37-close` branch, the second small docs-only PR, no commits on the feature branch after the merge, the ruleset never bypassed)
    - `.planning/phases/37-v0-10-0-crate-release/37-RESEARCH.md` Q9 — the four `chore/NN-close*` precedents and the note that their naming varies while their structure does not; use `chore/37-close` exactly
    - `git status --porcelain` — the two intentionally uncommitted files plan 37-08 left in the working tree, plus any uncommitted modification to `.planning/ROADMAP.md` or `.planning/STATE.md` that must be carried onto this branch rather than left across the merge window
    - `.planning/phases/37-v0-10-0-crate-release/37-08-SUMMARY.md` (uncommitted in the working tree) — the recorded tick SHA, which step 3's reconciliation range starts from
    - `37-08-PLAN.md`'s ORCHESTRATOR NOTE — why a wave-8 progress-tracking commit should not exist on `feature/phase-33`, and that the deferred wave-8 tracking lands on this branch instead
  </read_first>
  <action>
Move the phase's remaining work onto the close-out branch, carrying the deferred artifacts with it.

1. Confirm the working tree still holds plan 37-08's intentionally uncommitted files
   (`.continue-here.md`'s post-tick update and `37-08-SUMMARY.md`). If either is missing,
   reconstruct it from this plan's Task 1 findings and the checkpoint resolutions recorded in the
   session — and say so in the commit body, rather than silently producing a thinner record.
2. Create the branch from the **tagged** `main`: fetch, then create `chore/37-close` at the tag's
   commit (equivalently the current `origin/main` tip if nothing else has landed — assert the two
   are the same and record it if they are not). Uncommitted `.planning/` files carry across the
   checkout cleanly, since `main` already contains everything the feature branch had.
   Use the name `chore/37-close` exactly, not any of the three historical spelling variants.
3. **Reconcile any stray commit on the abandoned feature branch — before writing any post-tag
   record.** Take the tick SHA from plan 37-08's record and assert that the commit range from it to
   the local `feature/phase-33` ref is empty. The realistic cause of a non-empty range is a
   progress-tracking commit of `.planning/ROADMAP.md` and `.planning/STATE.md` created by the
   execute-phase orchestrator for wave 8 while that branch was still checked out — the very thing
   the ORCHESTRATOR NOTE in `37-08-PLAN.md` defers. If the range is non-empty: record it as a D-00d
   finding naming each stray SHA and the files it touched, then carry its **content** forward by
   re-applying those changes on `chore/37-close` as part of this task's commit, so no progress
   record is stranded on a dead branch. Do **not** push `feature/phase-33`, do **not** rewrite or
   reset it, and do **not** cherry-pick onto `main` — the tagged commit stays exactly what was
   released (D-10).
4. Commit the deferred artifacts on `chore/37-close`: `37-08-SUMMARY.md`, the `.continue-here.md`
   update, and any content carried forward by step 3, with a commit message stating why they land
   here rather than on `feature/phase-33` (the maintainer's tick had to remain that branch's last
   content commit, D-03/D-10).
5. Do not delete, re-create or push to `feature/phase-33` — the repository deletes the branch on
   merge and nothing after the tag depends on it. Do not push `chore/37-close` yet: plan 37-11
   pushes it and opens its PR once the record is complete.
  </action>
  <acceptance_criteria>
    - `git branch --show-current` reports `chore/37-close`
    - `git merge-base --is-ancestor v0.10.0^{commit} HEAD` succeeds — the branch descends from the tagged commit
    - The commit range from the recorded tick SHA to the local `feature/phase-33` ref is empty, or every stray commit in it is recorded as a D-00d finding by SHA and its content is re-applied in this task's commit on `chore/37-close`
    - `.planning/phases/37-v0-10-0-crate-release/37-08-SUMMARY.md` is committed on this branch and records both of plan 37-08's checkpoint resolutions
    - `git log --oneline origin/feature/phase-33 -1` (if the ref still exists) is still the maintainer's tick commit — this plan added nothing to it and pushed nothing to it
    - The commit body states why these artifacts land on `chore/37-close`
  </acceptance_criteria>
  <verify>
    <automated>test "$(git branch --show-current)" = "chore/37-close" && git merge-base --is-ancestor v0.10.0^{commit} HEAD && test -f .planning/phases/37-v0-10-0-crate-release/37-08-SUMMARY.md && git log --oneline -1 -- .planning/phases/37-v0-10-0-crate-release/37-08-SUMMARY.md > /tmp/def.txt && test -s /tmp/def.txt && test -z "$(git status --porcelain -- .planning/phases/37-v0-10-0-crate-release/37-08-SUMMARY.md)" && { S=0; if git rev-parse --verify --quiet refs/heads/feature/phase-33 > /dev/null; then S=$(git rev-list --count HEAD..refs/heads/feature/phase-33); fi; OK=0; if test "$S" = "0"; then OK=1; fi; if grep -q 'stray' .planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md; then OK=1; fi; test "$OK" = "1"; }</automated>
  </verify>
  <done>`chore/37-close` exists at the tagged commit, any stray commit on the abandoned feature branch is reconciled and recorded rather than stranded, the deferred pre-merge artifacts are committed on the close-out branch with the reason recorded, and `feature/phase-33` gained nothing after the tick.</done>
</task>

<task type="auto">
  <name>Task 3: Record the post-tick, post-merge and release-run evidence — publish health from the job's own table</name>
  <files>.planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md` — the CI-run table and the provenance block, whose post-tick, merge-commit and tagged-commit lines are now fillable
    - `docs/src/appendix/release-recovery.md` §1-§2 and §6 — establishing what reached crates.io, reading the run (the four terminal per-crate states and the explicit warning not to infer publish health from the workflow's overall conclusion), and the failure-code table to name a code from
    - `scripts/publish-crates.sh` — the `CRATES` array, which is the canonical dependency order the recorded per-crate table is compared against
    - `.planning/phases/37-v0-10-0-crate-release/37-RESEARCH.md` Q10 and the SC3 evidence-map row — the exact `gh run view --json jobs` query for the publish job's conclusion
    - `.planning/phases/37-v0-10-0-crate-release/37-CONTEXT.md` — D-13 (record the skip), D-15 (the one-re-run flake exception), D-16 (read-only diagnosis, hard stop)
  </read_first>
  <action>
Fill the CI-run table for everything that happened after the evidence was appended, and read
release health from the only admissible source.

1. Complete the provenance block: the post-§11-tick final SHA, the `main` merge commit, and the
   tagged commit (with the equality note from Task 1).
2. Add CI-run rows for the runs on the **post-tick final SHA** — the two `ci.yml` runs plan 37-07
   recorded as pending, plus the other workflows on that SHA — with their conclusions now filled
   in, and for the runs on the **merge commit** on `main`. For `ci.yml` on the merge commit,
   record that this is the run the pre-publish consistency gate requires to have concluded
   `success` before the tag, and whether it did so before the tag's push time (compare the run's
   completion timestamp to the tag's tagger date, both already on record).
3. Add the row for the **real `release.yml` run** triggered by the tag push. Record the workflow
   run's overall conclusion **and** — separately and more prominently — the `publish-crates` job's
   own conclusion, because the overall conclusion is not evidence of publish health: the binaries
   matrix has a documented history of failing without affecting publishing. Then transcribe the
   `publish-crates` job's **per-crate outcome table** exactly as the job emitted it: one row per
   crate, in the order the job printed them, with each crate's terminal state (published now,
   already at this version, skipped, or failed). Record explicitly that the emitted order matches
   `scripts/publish-crates.sh`'s `CRATES` array order, and note that where two crates' states are
   identical the recorded order is the job's emitted order and is not re-sorted.
4. Finalise the **D-13 record** in the findings section: the dry-run dispatch was not attempted
   (the traced reason is already recorded in plan 37-01), no rc tag and no shadow tag were created,
   and the real run is the first execution of this pipeline for this version — cross-reference the
   release-run row.
5. **If the release run is red:** stay inside D-16's read-only scope. Establish what actually
   reached crates.io by querying the versioned crates.io API per crate with the mandatory
   `User-Agent` (200 means published, 404 means not yet, anything else means investigate); read
   the run and the `publish-crates` job log; name the matching failure code from the recovery
   runbook's §6 table and the runbook section that applies; write all of it into
   `37-CI-EVIDENCE.md`; then **hard-stop**. Do not re-dispatch, do not re-run the release workflow,
   do not yank, and do not publish anything — those are the maintainer's, by the runbook's own
   text. The single exception remains D-15's pure infrastructure flake, where exactly one
   `gh run rerun --failed` is permitted with both run IDs and the justifying log line recorded, and
   only after `gh run list` confirms no other run against the same tag is in flight.
6. Commit on `chore/37-close` with the plain-git form and a >= 600 s timeout.
  </action>
  <acceptance_criteria>
    - The provenance block names the post-tick final SHA, the merge commit and the tagged commit, with the equality note
    - The CI-run table contains rows for the post-tick head, the merge commit, and the `release.yml` run, each with a run ID and URL
    - The release-run row records the `publish-crates` job's own conclusion separately from the workflow's overall conclusion, with a sentence stating why the overall conclusion is not admissible as publish evidence
    - The per-crate outcome table is transcribed with one row per crate in the job's emitted order, and the record states that this order matches the publish script's `CRATES` array and was not re-sorted
    - The findings section's D-13 entry is finalised, stating no dispatch, no rc tag and no shadow tag
    - If the run was red, the record names a failure code from the recovery runbook and the phase halted with no re-dispatch, re-run beyond D-15's single permitted case, or yank
    - The commit is on `chore/37-close`, not on `feature/phase-33`
  </acceptance_criteria>
  <verify>
    <automated>test "$(git branch --show-current)" = "chore/37-close" && grep -q 'publish-crates' .planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md && grep -q 'release.yml' .planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md && TC=$(git rev-parse v0.10.0^{commit}) && grep -q "$TC" .planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md && C=$(grep -c 'paladin-' .planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md; true); test "$C" -ge 12</automated>
  </verify>
  <done>The post-tick, post-merge and release-run rows are recorded with publish health read from the `publish-crates` job's own conclusion and per-crate table in emitted dependency order, the D-13 record is final, and any red run was diagnosed read-only and hard-stopped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tag → released artifact identity | The tag's commit is what was published; a tag on any other ancestor of `main` would mean the evidence describes a different tree than the one on the registry |
| release workflow conclusion → publish health | The workflow's overall conclusion mixes unrelated matrix jobs with publishing; only the `publish-crates` job's own table is admissible |
| `chore/37-close` → protected `main` | Post-tag records reach `main` through a small docs-only PR the maintainer merges; the ruleset is never bypassed and the tagged commit is never altered |
| agent → post-tag remediation surface | Re-dispatch, complete-forward and yank are maintainer-only; the agent's authority ends at read-only diagnosis |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-21 | Tampering | The claim that the tag sits on the merge commit | high | mitigate | Task 1 asserts equality between the tag's commit and the PR's merge commit **and** containment in `main`, plus a two-parent merge commit and an annotated tag; ancestry alone is explicitly rejected as proof (SC3, D-04). |
| T-37-22 | Repudiation | Publish health inferred from the wrong signal | high | mitigate | The release-run row records the `publish-crates` job's own conclusion and its per-crate table in emitted order, with an explicit statement that the workflow's overall conclusion is inadmissible (recovery runbook §2). |
| T-37-23 | Elevation of Privilege | Post-tag remediation (re-dispatch, complete forward, yank) | high | mitigate | D-16 bounds the agent to runbook §1-§2 diagnosis plus naming a §6 failure code; every remediation verb is prohibited in this plan's `must_haves.prohibitions` and absent from every task action. |
| T-37-18 | Tampering | A commit landing on `feature/phase-33` after the tick, including a stray orchestrator progress-tracking commit | high | mitigate | Tasks 2-3 assert the current branch is `chore/37-close`; Task 2 step 3 asserts the tick-to-`feature/phase-33` range is empty and, if it is not, records each stray SHA as a D-00d finding and re-applies its content on `chore/37-close` rather than stranding it — never pushing, resetting or rewriting the feature branch; the deferred artifacts and the deferred wave-8 progress tracking are committed on `chore/37-close` with the reason recorded (D-03, D-10). |
| T-37-19 | Denial of Service | A second run racing the same tag | medium | mitigate | Before D-15's single permitted re-run, `gh run list` is checked for an in-flight run against the same tag; the agent never dispatches a new run. |
</threat_model>

<artifacts_this_phase_produces>
See `37-01-PLAN.md` → "Artifacts this phase produces" for the phase-wide list. This plan creates
the git branch `chore/37-close` (new identifier) and completes the CI-run table and provenance
block of `.planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md`. No code symbols are
created and no file under `src/` or `crates/*/src/` is modified.
</artifacts_this_phase_produces>

<verification>
- The resume condition was re-verified first, and the tag is proven equal to the merge commit with containment also asserted
- `chore/37-close` descends from the tagged commit and carries plan 37-08's deferred artifacts
- The tick-to-`feature/phase-33` commit range is empty, or every stray commit in it is recorded as a D-00d finding and its content re-applied on `chore/37-close`; the feature branch was never pushed or rewritten
- The CI-run table covers the post-tick head, the merge commit and the release run, with publish health read from the `publish-crates` job's own table in emitted dependency order
- The D-13 non-dispatch record is final; any red run was diagnosed read-only and hard-stopped
</verification>

<success_criteria>
- `git rev-parse v0.10.0^{commit}` equals the PR's merge commit SHA; the containment check passes; the merge commit has two parents; the tag object type is `tag`
- `git branch --show-current` is `chore/37-close` and it descends from the tagged commit
- `git rev-list --count HEAD..refs/heads/feature/phase-33` is 0, or every commit in that range is recorded as a stray-commit finding with its content re-applied on `chore/37-close`
- `37-CI-EVIDENCE.md` names the `publish-crates` job's conclusion and lists at least twelve `paladin-` crate rows in emitted order
- No commit exists on `feature/phase-33` after the maintainer's tick commit
</success_criteria>

<output>
Create `.planning/phases/37-v0-10-0-crate-release/37-09-SUMMARY.md` on `chore/37-close` when done,
recording the tag commit SHA, the merge SHA, both merge parents, the release run ID and the
`publish-crates` job's conclusion — plan 37-11's MILESTONES.md entry quotes all of them. Record
the stray-commit reconciliation result too (empty range, or the SHAs carried forward), because
the phase's VERIFICATION asserts it.

**Orchestrator hand-off (repeat of the ORCHESTRATOR NOTE above, restated where the orchestrator
reads results):** `chore/37-close` now exists and is the checked-out branch. Resume progress
tracking here — make the deferred wave-8 tracking commit and every wave 9-11 tracking commit on
`chore/37-close`, never on `feature/phase-33`. Plan 37-11 pushes this branch and opens its PR.
</output>
