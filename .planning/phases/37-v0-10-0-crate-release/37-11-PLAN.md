---
phase: 37-v0-10-0-crate-release
plan: 11
type: execute
wave: 11
depends_on: [37-10]
files_modified:
  - .planning/MILESTONES.md
  - .planning/phases/37-v0-10-0-crate-release/37-READY-TO-CLOSE.md
autonomous: false
requirements: [SHIP-05]
must_haves:
  truths:
    - "MILESTONES.md carries a v0.10.0 release record in the v0.9.0 entry's shape, stating the tag, the merge commit SHA, the release run ID, the crate count taken from this phase's live derived figure, the phrase registry-verified, and Trusted Publishing — and pointing at the registry table that proves it (D-08)."
    - "The v0.10.0 crate count in MILESTONES.md is the count this phase derived from `cargo metadata`, not the eleven the v0.9.0 entry records and the documentation repeats (D-00f)."
    - "`37-READY-TO-CLOSE.md` enumerates the literal preconditions `/gsd-audit-milestone` and `/gsd-complete-milestone` each enforce, so the maintainer can see what is already true and what is outstanding before either is run (D-11)."
    - "No plan in this phase runs `/gsd-complete-milestone` or `/gsd-audit-milestone`: the phase ends at ready-to-close, with the archive moves left to run on `chore/37-close` so they ride the same pull request (D-11)."
  artifacts:
    - path: ".planning/MILESTONES.md"
      provides: "The v0.10.0 release record"
      contains: "registry-verified"
    - path: ".planning/phases/37-v0-10-0-crate-release/37-READY-TO-CLOSE.md"
      provides: "The ready-to-close hand-off: what this phase delivered, the enumerated preconditions of both milestone commands, and the ordered close sequence"
      contains: "Preconditions"
  key_links:
    - from: ".planning/MILESTONES.md"
      to: ".planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md"
      via: "the release record's registry-verified claim points at the D-08 registry table rather than restating it"
      pattern: "37-CI-EVIDENCE"
    - from: ".planning/phases/37-v0-10-0-crate-release/37-READY-TO-CLOSE.md"
      to: "/workspace/.claude/gsd-core/workflows/complete-milestone.md"
      via: "each listed precondition names the workflow step that enforces it"
      pattern: "complete-milestone"
  prohibitions:
    - statement: "MUST NOT run `/gsd-complete-milestone` or `/gsd-audit-milestone`, archive the phase directory, delete REQUIREMENTS.md, or perform any milestone-close mutation — the phase ends at ready-to-close (D-11)."
      status: unverified
      verification: judgment
    - statement: "MUST NOT merge the `chore/37-close` pull request or bypass the branch ruleset; the agent opens it and the maintainer merges it (D-10, D-02)."
      status: unverified
      verification: judgment
    - statement: "MUST NOT add a second `## v0.10.0` heading to MILESTONES.md, and MUST NOT edit the v0.9.0 entry — including its superseded-strikethrough paragraph (D-00d)."
      status: unverified
      verification: judgment
---

<objective>
Write the release record, hand the milestone close to the maintainer with its preconditions
enumerated, and open the docs-only pull request that carries this phase's post-tag evidence into
`main`.

Purpose: SC4 asks for the release to be recorded in MILESTONES.md; D-11 draws the phase's boundary
at "ready to close" — the record is written and the prerequisites are listed, but no plan runs the
close itself, because a plan must not archive the phase directory it is executing from and
verify-work must find this phase's artifacts where they were written.
Output: the MILESTONES.md v0.10.0 entry, `37-READY-TO-CLOSE.md`, and an open `chore/37-close` PR.

Governing constraints: work on `chore/37-close` (D-10); the phase prohibitions P1-P5 in
`37-01-PLAN.md` apply.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-v0-10-0-crate-release/37-CONTEXT.md
@.planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md
@.planning/phases/37-v0-10-0-crate-release/37-09-SUMMARY.md
@.planning/phases/37-v0-10-0-crate-release/37-10-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the MILESTONES.md v0.10.0 release record</name>
  <files>.planning/MILESTONES.md</files>
  <read_first>
    - `.planning/MILESTONES.md` lines 1-34 — the `## v0.9.0 Security Tooling (Shipped: …)` entry in full: its metadata lines (`**Phases completed:**`, `**Requirements:**`, `**Timeline:**`, `**Git range:**`, `**Closeout type:**`, `**Audit:**`), its superseded-strikethrough amendment paragraph, and its `**Delivered:**` / `**Key accomplishments:**` narrative sections
    - `.planning/phases/37-v0-10-0-crate-release/37-PATTERNS.md` — the note to model this entry on the v0.9.0 *post-amendment* prose block only, with no strikethrough wrapper, since this release is clean rather than retroactive
    - `.planning/phases/37-v0-10-0-crate-release/37-09-SUMMARY.md` and `37-10-SUMMARY.md` — the tag commit SHA, the merge SHA, the release run ID, the `publish-crates` conclusion, the derived crate count and the `trustpub_data` verdict
    - `.planning/STATE.md` and `.planning/ROADMAP.md` — the phase and plan counts, the milestone name, and the phase range for the metadata lines
  </read_first>
  <action>
Add one new `##` entry for v0.10.0 above the v0.9.0 entry, in that entry's shape.

- Heading: `## v0.10.0 Durable Agent Execution Runtime (Shipped: <the tag's tagger date>)`.
- Metadata lines in the v0.9.0 order: phases completed (the milestone's phase range and the plan
  count, taken from ROADMAP/STATE rather than estimated), requirements satisfied out of total
  (naming the epic prefixes in the v0.9.0 style), the timeline with its commit count, the git
  range, the closeout type (state it as pending `/gsd-complete-milestone`, since D-11 leaves the
  close to the maintainer — do not invent a closeout classification), and the audit pointer
  (`/gsd-audit-milestone` not yet run, per D-11).
- Then the **release paragraph**, modelled on the v0.9.0 entry's post-amendment prose and in the
  same clause order, with **no strikethrough wrapper** because nothing is being reversed here:
  the release PR number, the tag `v0.10.0`, the merge commit SHA it sits on, the release run ID,
  the `publish-crates` job's conclusion, the **crate count taken from this phase's live
  `cargo metadata`-derived figure** (not the eleven the v0.9.0 entry records — that entry predates
  `paladin-eval` and was correct when written), the phrase **registry-verified** with a pointer to
  `37-CI-EVIDENCE.md`'s registry table, and Trusted Publishing with a pointer to the
  `trustpub_data` reading.
- Then `**Delivered:**` and `**Key accomplishments:**` narrative sections in the same two-heading
  shape. Keep the prose to what the evidence supports.
- Do not touch the v0.9.0 entry, including its strikethrough amendment. Do not add a second
  v0.10.0 heading — one entry only, which `/gsd-complete-milestone` will later extend rather than
  duplicate (the ready-to-close file says so explicitly).
- Commit on `chore/37-close` with the plain-git form.
  </action>
  <acceptance_criteria>
    - `.planning/MILESTONES.md` contains exactly one heading beginning `## v0.10.0`
    - The entry contains the tag name, the merge commit SHA, the release run ID, the phrase `registry-verified`, the phrase `Trusted Publishing`, and a pointer to `37-CI-EVIDENCE.md`
    - The stated crate count equals the derived count recorded in `37-10-SUMMARY.md`
    - The entry has `**Delivered:**` and `**Key accomplishments:**` sections
    - `git diff HEAD~1 -- .planning/MILESTONES.md` shows no removed line from the v0.9.0 entry
    - The entry does not claim a closeout type or an audit result that has not been produced
  </acceptance_criteria>
  <verify>
    <automated>H=$(grep -c '^## v0.10.0' .planning/MILESTONES.md; true); test "$H" = "1" && grep -q 'registry-verified' .planning/MILESTONES.md && grep -q 'Trusted Publishing' .planning/MILESTONES.md && grep -q '37-CI-EVIDENCE' .planning/MILESTONES.md && grep -q 'Delivered:' .planning/MILESTONES.md && D=$(git diff HEAD~1 -- .planning/MILESTONES.md | grep -c '^-[^-]'; true); test "$D" = "0"</automated>
  </verify>
  <done>MILESTONES.md carries one v0.10.0 release record in the house shape, with the tag, merge SHA, run ID, the live derived crate count, registry-verified and Trusted Publishing, and the v0.9.0 entry untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Write `37-READY-TO-CLOSE.md` and open the `chore/37-close` pull request</name>
  <files>.planning/phases/37-v0-10-0-crate-release/37-READY-TO-CLOSE.md</files>
  <read_first>
    - `/workspace/.claude/gsd-core/workflows/audit-milestone.md` — read in full; the literal preconditions are in steps 1-2 (scope from ROADMAP/REQUIREMENTS; a phase missing VERIFICATION.md is a blocker), step 5 (the three-source cross-reference: traceability table, each phase's VERIFICATION.md requirements table, and each SUMMARY's `requirements-completed` frontmatter), step 5e (any unsatisfied requirement forces `gaps_found`; a requirement in the traceability table but absent from every VERIFICATION.md is orphaned and counts unsatisfied), and step 5.5 (each phase's `*-VALIDATION.md`: `status: validated` plus `nyquist_compliant: true` is COMPLIANT, `status: draft` reads NOT-VALIDATED and is a coverage TODO)
    - `/workspace/.claude/gsd-core/workflows/complete-milestone.md` — read in full; the literal preconditions are its `pre_close_artifact_audit` step (the open-artifact audit must be clear or every item acknowledged, which forces an override closeout and a STATE.md Deferred Items table), its `verify_readiness` step (every milestone phase must report complete with verification passed, or the closeout becomes an override needing an explicit choice; plus the checked-off requirement count from the traceability table), its required reading list, its branch-handling step (which offers merge/delete/keep for detected branches), its `git_tag` step (which pre-checks and skips when the tag already exists), and its archival behaviour (phase detail to `milestones/v0.10.0-ROADMAP.md`, requirements to `milestones/v0.10.0-REQUIREMENTS.md`, ROADMAP.md overwritten in place with milestone grouping, a safety commit, then REQUIREMENTS.md removed, then the PROJECT.md evolution review)
    - `.planning/phases/37-v0-10-0-crate-release/37-VALIDATION.md` — its current frontmatter, which is what the Nyquist discovery step will read
    - `.planning/phases/37-v0-10-0-crate-release/37-CONTEXT.md` — D-11 and D-10
  </read_first>
  <action>
Write the hand-off, then open the PR.

Create `.planning/phases/37-v0-10-0-crate-release/37-READY-TO-CLOSE.md` with these sections:

1. **What this phase delivered** — the gate re-seal and corpus section 12, `SHIP-05`, the PR and
   the pre-merge CI evidence including the coverage reading, the maintainer's §11 tick, the merge
   and the tag, the release run, the registry verification, and the MILESTONES.md record. One line
   each, each pointing at its artifact.
2. **What this phase deliberately did not do** — run either milestone command, archive anything,
   edit any documentation page, fix any finding, or dispatch a release rehearsal — each with its
   decision id.
3. **Preconditions `/gsd-audit-milestone` enforces** — one row or bullet per literal precondition
   read from the workflow (see `<read_first>`), each marked as already satisfied, outstanding, or
   maintainer-owned, with the reason. Include the specific outstanding ones by name: this phase's
   own VERIFICATION.md does not exist until `/gsd-verify-work 37` runs, and `37-VALIDATION.md`
   will read NOT-VALIDATED until `/gsd-validate-phase 37` promotes its status — both are coverage
   TODOs the audit surfaces rather than blockers it invents.
4. **Preconditions `/gsd-complete-milestone` enforces** — the same treatment, and call out four
   specifics: (a) the open-artifact audit must be clear or every item acknowledged, which forces
   an override closeout; (b) every milestone phase must report complete with verification passed;
   (c) the requirement checkbox count is read from the traceability table, so `SHIP-05` must be
   checked off before the count is clean; (d) its `git_tag` step pre-checks for an existing tag and
   will print that `v0.10.0` already exists and skip — expected, not an error, because this phase
   already cut the tag.
5. **The ordered close sequence** — run `/gsd-verify-work 37` and `/gsd-validate-phase 37` first,
   then `/gsd-audit-milestone`, then `/gsd-complete-milestone v0.10.0`, all **on
   `chore/37-close`** so the archive moves ride this branch's pull request (D-11). Add two
   warnings: the branch-handling step offers to merge or delete detected branches — choose to keep
   them, since the release branch is already merged and this one is an open PR; and the close will
   write its own MILESTONES.md entry, so extend the v0.10.0 record this phase wrote rather than
   adding a second heading for the same version.
6. **Outstanding findings** — a pointer to `37-CI-EVIDENCE.md`'s findings section, not a copy.

Then push `chore/37-close` and open the docs-only pull request into `main` with `gh pr create`,
titled for the phase close-out, and a body that: states it carries only `.planning/` and
`.project/` records for the v0.10.0 release (no source change), lists the files, points at
`37-CI-EVIDENCE.md` and `37-READY-TO-CLOSE.md`, and notes that the milestone close commands are
intended to run on this branch before it merges. Budget 10-20 minutes for the push (the pre-push
hook stage runs in full; never `--no-verify`). If `gh` fails on permissions, record the exact
error and command and hand it to the maintainer. Commit the new file before pushing.
  </action>
  <acceptance_criteria>
    - `.planning/phases/37-v0-10-0-crate-release/37-READY-TO-CLOSE.md` exists with all six sections
    - It lists at least four `/gsd-audit-milestone` preconditions and at least four `/gsd-complete-milestone` preconditions, each marked satisfied, outstanding or maintainer-owned
    - It names `/gsd-verify-work 37` and `/gsd-validate-phase 37` as outstanding, with the reason each is outstanding
    - It states that the close runs on `chore/37-close`, that branch handling should keep branches, and that the existing tag makes the tag step skip
    - It states that `/gsd-complete-milestone` writes its own MILESTONES.md entry and that the existing v0.10.0 record should be extended rather than duplicated
    - `git rev-parse origin/chore/37-close` resolves and equals local `HEAD`
    - `gh pr list --head chore/37-close --state open --json number` returns exactly one PR whose body states it carries no source change
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/37-v0-10-0-crate-release/37-READY-TO-CLOSE.md && grep -q 'Preconditions' .planning/phases/37-v0-10-0-crate-release/37-READY-TO-CLOSE.md && grep -q 'gsd-verify-work 37' .planning/phases/37-v0-10-0-crate-release/37-READY-TO-CLOSE.md && grep -q 'gsd-validate-phase 37' .planning/phases/37-v0-10-0-crate-release/37-READY-TO-CLOSE.md && grep -q 'chore/37-close' .planning/phases/37-v0-10-0-crate-release/37-READY-TO-CLOSE.md && test "$(git rev-parse HEAD)" = "$(git rev-parse origin/chore/37-close)" && N=$(gh pr list --head chore/37-close --state open --json number --jq 'length'); test "$N" = "1"</automated>
  </verify>
  <done>The ready-to-close hand-off enumerates both commands' literal preconditions with their status, names the ordered close sequence and its two traps, and the docs-only `chore/37-close` PR is open.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Maintainer merges the close-out PR and runs the milestone close (D-10, D-11)</name>
  <files>.planning/phases/37-v0-10-0-crate-release/37-READY-TO-CLOSE.md</files>
  <read_first>
    - `.planning/phases/37-v0-10-0-crate-release/37-READY-TO-CLOSE.md` — the hand-off Task 2 wrote, with both commands' preconditions and the ordered sequence
    - `.planning/MILESTONES.md` — the v0.10.0 release record Task 1 wrote
    - `.planning/phases/37-v0-10-0-crate-release/37-CI-EVIDENCE.md` — the complete record: Local sweep, CI-run table, registry table, findings
  </read_first>
  <action>
Hand the maintainer the milestone close and stop. No task in this phase runs
`/gsd-complete-milestone` or `/gsd-audit-milestone`, archives the phase directory, removes
REQUIREMENTS.md, or merges the close-out pull request (D-11, D-10). Present the `<what-built>`
state and the ordered `<instructions>` sequence — including the two traps the close will hit — then
stop.
  </action>
  <what-built>
v0.10.0 is released, not merely releasable, and the record says so from observation rather than
promise:

- corpus audit section 12 records all seven gates re-run on the final pre-merge commit, and your
  §11 tick closed against that basis;
- the PR's CI evidence, including the `coverage` job's own conclusion and printed figure, is in
  `37-CI-EVIDENCE.md`;
- the tag is proven to sit on the merge commit itself — equality, not just ancestry — on a
  two-parent merge commit;
- the release run's `publish-crates` job and its per-crate table are recorded in dependency order;
- every publishable crate, derived live from `cargo metadata`, resolves on the crates.io sparse
  index at `0.10.0` with a checksum and `yanked` false, and `paladin-eval`'s `trustpub_data`
  reading is on record;
- MILESTONES.md carries the release entry, and `37-READY-TO-CLOSE.md` lists what each milestone
  command will require.

The phase stops here by decision: no plan runs `/gsd-complete-milestone` or
`/gsd-audit-milestone` (D-11).
  </what-built>
  <instructions>
1. **Read `37-READY-TO-CLOSE.md`**, then skim `37-CI-EVIDENCE.md`'s four tables and the
   MILESTONES.md entry. Anything that reads as a claim rather than an observation is worth
   pushing back on now.
2. **Run the close sequence on `chore/37-close`, before merging its PR**, so the archive moves
   ride the same pull request:
   - `/gsd-verify-work 37` — produces this phase's VERIFICATION.md, which the milestone audit
     requires;
   - `/gsd-validate-phase 37` — promotes `37-VALIDATION.md` out of draft so the audit reports a
     real compliance verdict instead of a coverage TODO;
   - `/gsd-audit-milestone` — aggregates the phase verifications and the three-source requirement
     cross-reference;
   - `/gsd-complete-milestone v0.10.0` — archives the milestone. Two things to expect: its tag
     step will report that `v0.10.0` already exists and skip it (correct — this phase cut the
     tag), and its branch-handling step will offer to merge or delete detected branches; choose
     **keep**, because the release branch is already merged and this one is an open PR. It will
     also write its own MILESTONES.md entry — extend the v0.10.0 record already there rather than
     adding a second heading for the same version.
3. **Then merge the `chore/37-close` PR** with the merge-commit method, as the house precedent for
   close-out branches does. The agent does not merge it (D-10).

If you would rather merge first and close afterwards, that also works — but then the archive
commits need their own branch and PR, which is the extra round-trip D-11 was written to avoid.
  </instructions>
  <verification>
Observable afterwards, read-only:
- `.planning/phases/37-v0-10-0-crate-release/37-VERIFICATION.md` exists (produced by `/gsd-verify-work 37`)
- `37-VALIDATION.md` frontmatter reads `status: validated` (produced by `/gsd-validate-phase 37`)
- `.planning/v0.10.0-MILESTONE-AUDIT.md` exists (produced by `/gsd-audit-milestone`)
- `.planning/milestones/v0.10.0-ROADMAP.md` and `v0.10.0-REQUIREMENTS.md` exist and `grep -c '^## v0.10.0' .planning/MILESTONES.md` still returns 1 — the close extended the existing record rather than adding a second heading
- `gh pr list --head chore/37-close --state merged --json number --jq 'length'` returns 1
  </verification>
  <resume-signal>Reply "closed" once the close sequence has run and the PR is merged — or describe what you want changed first</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| release record → project history | MILESTONES.md is the durable claim about what shipped; a count or SHA taken from a document rather than from this phase's measurements would propagate the same staleness the release just exposed |
| `chore/37-close` → protected `main` | The post-tag record reaches `main` only through a maintainer-merged docs-only PR; the tagged commit is never altered |
| milestone-close automation → this phase's artifacts | The close archives the phase directory and removes REQUIREMENTS.md; running it from inside a plan would delete the artifacts verify-work needs to read |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-27 | Repudiation | The MILESTONES.md crate count and SHAs | high | mitigate | Every figure is taken from plans 37-09 and 37-10's recorded measurements (the derived count, the tag/merge SHAs, the run ID) and the entry points at the registry table rather than restating it; the acceptance criteria assert the count matches the derived figure (D-08, D-00f). |
| T-37-28 | Tampering | The milestone-close mutations (archival, REQUIREMENTS.md removal, ROADMAP rewrite) | high | mitigate | No task runs either milestone command; D-11 keeps them with the maintainer, and the hand-off names the two traps (the existing tag, branch handling) so the close does not improvise. |
| T-37-02 | Elevation of Privilege | Merging the close-out PR | high | mitigate | The agent opens the PR and stops at a `gate="blocking-human"` checkpoint; merging is the maintainer's (D-10, D-02). |
| T-37-14 | Tampering | The v0.9.0 milestone entry | medium | mitigate | The new entry is inserted above it; the acceptance criteria assert the diff removes no line from the v0.9.0 entry, including its superseded-strikethrough paragraph (D-00d). |
</threat_model>

<artifacts_this_phase_produces>
See `37-01-PLAN.md` → "Artifacts this phase produces" for the phase-wide list. This plan creates
`.planning/phases/37-v0-10-0-crate-release/37-READY-TO-CLOSE.md` (new file path) and the
MILESTONES.md heading `## v0.10.0 Durable Agent Execution Runtime (Shipped: <date>)` (new
identifier), plus the `chore/37-close` pull request (a GitHub object). No code symbols are created
and no file under `src/`, `crates/*/src/` or `docs/` is modified.
</artifacts_this_phase_produces>

<verification>
- MILESTONES.md has exactly one v0.10.0 entry, carrying the tag, merge SHA, run ID, the live derived crate count, registry-verified and Trusted Publishing, with the v0.9.0 entry untouched
- `37-READY-TO-CLOSE.md` enumerates both milestone commands' literal preconditions with a status each, names the outstanding verify-work and validate-phase runs, and gives the ordered close sequence with its two traps
- `chore/37-close` is pushed and its docs-only PR is open, stating it carries no source change
- No plan in this phase ran a milestone command or archived anything
</verification>

<success_criteria>
- `grep -c '^## v0.10.0' .planning/MILESTONES.md` returns 1 and the entry contains `registry-verified` and `Trusted Publishing`
- `37-READY-TO-CLOSE.md` exists with all six sections and names `/gsd-verify-work 37`, `/gsd-validate-phase 37`, `/gsd-audit-milestone` and `/gsd-complete-milestone v0.10.0` in order
- `git rev-parse origin/chore/37-close` equals local `HEAD`, and exactly one open PR exists for that head
- `.planning/milestones/` gained no file and `.planning/REQUIREMENTS.md` still exists — nothing was archived by this phase
</success_criteria>

<output>
Create `.planning/phases/37-v0-10-0-crate-release/37-11-SUMMARY.md` on `chore/37-close` when done,
recording the close-out PR number, the checkpoint's resolution, and the phase's final state:
SC1-SC4 observed, SC5's preconditions listed and outstanding. List `SHIP-05` under
`requirements-completed` — at this point its four clauses are observably true.
</output>
