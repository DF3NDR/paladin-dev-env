---
context: phase
phase: 37.1-v0-10-1-patch-release
plan: 13
status: ready
last_updated: "2026-09-21T23:30:00Z"
---

# Phase 37.1 — v0.10.1 IS RELEASED; post-tag wave open on `chore/37.1-close`; plans 37.1-11 and 37.1-12 recorded; next is plan 37.1-13

This file replaces every previous resume file in this phase (prior versions are preserved in git history — the
tick commit `f3fc061d298c126e1ac1dffe838969fa4bf53e62` carried the pre-merge version; every dated `## UPDATE`
section this file carried between the merge and the release run's conclusion is preserved verbatim in
`37.1-CI-EVIDENCE.md`, not restated here). This file is now committed on `chore/37.1-close` (D-12).
`release/v0.10.1` is dead: GitHub deleted its remote ref on merge (`git ls-remote origin
refs/heads/release/v0.10.1` returns empty); it is never committed to, pushed to, or resurrected again.

## Where things stand — RELEASED

- **v0.10.1 IS RELEASED.** Annotated tag `v0.10.1` (object `7593ab4dc1217ef23d6f8c14f8c8b817d42ec0ae`, tagger
  `Am0rfu5`, `2026-09-21T21:50:30Z`) sits on merge commit `f7dae2676580786e9541fefee8f787623b46c70f` =
  `origin/main`. Two parents: `1d4a9724cc219b85856a23012543458d62559e47` (the previous `main` tip) and
  `f3fc061d298c126e1ac1dffe838969fa4bf53e62` (the maintainer's re-confirmation tick — the release branch's true
  final commit). `tree(merge) == tree(tick)`. **Equality, not just containment:** `gh pr view 56 --json
  mergeCommit` reports `mergeCommit.oid = f7dae267...`, exactly the tag's peeled commit.
- **The tag-after-green precondition held.** The merge commit's own `ci.yml` push run (`35648698841`) concluded
  `success` at `2026-09-21T21:40:06Z`; the tag was pushed at `21:50:30Z`, roughly ten minutes later.
- **Release run `35659477719`, attempt 3: `success`** (`23:11:06Z` -> `23:14:08Z`). Attempt 1 failed
  (`403 Forbidden` publishing `paladin-eval`, a Trusted Publisher environment-name typo — `crates.io` instead of
  `crates-io`); attempt 2 predated the maintainer's fix (same 403, no side effect, ten crates read
  `already-at-this-version`); attempt 3, after the maintainer corrected the environment name, published the
  remaining two crates (`paladin-eval`, `paladin-ai`) and completed 12/12.
- **All 12 publishable crates** (`cargo metadata`-derived: `paladin-ai`, `paladin-ai-core`, `paladin-battalion`,
  `paladin-content`, `paladin-eval`, `paladin-herald`, `paladin-llm`, `paladin-memory`, `paladin-notifications`,
  `paladin-ports`, `paladin-storage`, `paladin-web`) are confirmed **live at `0.10.1`, `yanked: false`**, each
  probed read-only against `index.crates.io` in this session. The three orphaned `0.10.0` versions
  (`paladin-ai-core`, `paladin-ports`, `paladin-herald`) are confirmed **`yanked: true`** — the maintainer's own
  act, reported verbatim as "all three yanked".
- Full per-attempt, per-job and per-crate record: `37.1-CI-EVIDENCE.md` (dated sections through "Plan 37.1-12
  Task 3").
- The repo-admin bypass on ruleset `20868128` is **no longer needed** and may be removed by the maintainer at
  their convenience — it is not a blocker for any remaining plan; nothing in Phase 37.1's remaining plans pushes
  to a `release/*` branch.
- `chore/37.1-close` exists, cut from the tagged `origin/main` (`git merge-base --is-ancestor v0.10.1^{commit}
  HEAD` succeeds), carrying the deferred plan 37.1-10/37.1-11 artifacts plus this wave's own recording (plan
  37.1-11 Tasks 1-4, plan 37.1-12 Tasks 1-3). The tick-to-release-branch stray-commit range
  (`f3fc061d..release/v0.10.1`) was checked and is **empty** — nothing was stranded. Not yet pushed to the
  remote — plan 37.1-16 pushes it and opens its documentation-only close-out pull request.

## What plans 37.1-11 and 37.1-12 did (this session, recorded after the fact per D-00d)

By the time this recording pass ran, the maintainer had already merged the PR, pushed the tag, and (after one
red attempt) completed the release forward. Both plans below therefore record what already happened rather than
gating an act still to come — the merge and the tag are irreversible acts this phase never performs itself
(D-00b), and nothing here re-does, re-verifies-by-re-running, or second-guesses them; every claim is backed by a
live, read-only remote query made in this session.

- **37.1-11 Task 1:** recorded every workflow run and per-job conclusion on the tick commit `f3fc061d` (9 runs,
  all `success`; 124 checks via `gh pr checks 56`, 118 pass / 6 skip / 0 fail; all 44 required contexts from
  `main`'s live ruleset present, 0 failed, only the conditional `End-to-End Tests` skipped as expected).
- **37.1-11 Tasks 2-3:** **N/A by deviation.** The one-way-door reversibility checkpoint and the merge+tag
  hand-off were never formally presented as discrete checkpoints — the maintainer performed both acts directly,
  in session, immediately after being told the required checks were green. Verbatim quotes ("PR Merged so now we
  are waiting for that CI.yml workflow to complete"; the tag-push output; the root-cause and re-run reports) are
  recorded in full in `37.1-CI-EVIDENCE.md`.
- **37.1-11 Task 4 (superseded by this rewrite):** the resume file no longer needs to protect against a
  premature merge/tag hand-off — both acts are done. This file replaces that intended pre-tag resume condition
  with the actual post-release state above.
- **37.1-12 Task 1:** tag-equality proof — tag commit equals the PR's merge commit exactly (not merely contained
  in `main`); the merge commit has exactly two parents; the tag object is annotated; the tag-after-green
  precondition is confirmed to have held (recorded above).
- **37.1-12 Task 2:** cut `chore/37.1-close` from the tagged `origin/main`; confirmed the stray-commit range from
  the tick to the (now-deleted) release branch ref is empty; committed the deferred artifacts with the reason
  they land here (the maintainer's tick had to remain the release branch's last content commit).
- **37.1-12 Task 3:** recorded the release-run evidence — the publish job's own conclusion and its per-crate
  outcome table (attempt 3, job `106552936579`), read separately from the workflow's overall conclusion; the
  source-verification (`Verify Tag From Main`) and release-consistency (`Pre-Publish Consistency Gate`) jobs both
  `success`; `Create Release` succeeded (the SC2 script fix proven in production, unlike `v0.10.0`'s own
  pipe-race failure at this exact step); all 12 crates cross-checked against `cargo metadata`'s live publishable
  list, in the publish script's own `CRATES` array order, and each probed read-only against the sparse index.

## Resume condition for plan 37.1-13

**Nothing is being waited on.** The release already concluded and was read once, live, per D-00i (no watcher, no
poll, no retry). Plan 37.1-13 starts immediately on `chore/37.1-close`. Its own scope is the *formal*
registry-verification artifact (SC5, D-00f/D-00h) — a fuller, count-asserted per-crate row set plus the
`trustpub_data` proof per crate — which this recording pass deliberately did **not** write; that table belongs to
plan 37.1-13, not to this session's plan 37.1-11/37.1-12 recording (the sparse-index probes above are this
session's own verification evidence for Task 3's acceptance criteria, not a substitute for plan 37.1-13's table).
Plan 37.1-14 (the yank checkpoint and the `v0.10.0` release-object banner/pre-release flip) follows plan 37.1-13.

## Must not happen from here

No re-dispatch and no re-run of the release workflow (the single documented infrastructure-flake exception does
not apply — the run already succeeded); no yank by any agent (the yank *register* rows are plan 37.1-14's, and
even there the yank act itself, if any further yank is ever needed, is the maintainer's); no publish; no tag
movement, deletion or re-creation; no commit to or push of `release/v0.10.1` (its remote ref is gone — never
resurrect it); no watcher, no polling loop, no parked run-watching agent.
