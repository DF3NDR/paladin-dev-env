---
context: phase
phase: 37.1-v0-10-1-patch-release
plan: 08
task: 2
total_tasks: 2
status: paused
last_updated: "2026-09-21T01:55:00Z"
---

> **ORCHESTRATOR NOTE, dated 2026-09-21 — added after plan 37.1-08 returned. Nothing below is deleted
> (D-00d, amend-at-source). Read the section "LIVE STATE UPDATE 2026-09-21" at the END of this file FIRST.**
>
> Push #2 was rejected by the `Protect release branches` ruleset (id `20868128`, `GH013`, "Changes must be
> made through a pull request"; `bypass_actors: []`, `current_user_can_bypass: never` — verified live by the
> orchestrator as well as the executor). So **the remote head IS H1** (`7749f161919f3d5f85f20fa36eba7ad800434f97`)
> and PR #56 (`headRefOid`) points at H1. Three statements further down are therefore OBSOLETE:
>
> 1. Every "final head ... not H1" wording. The head whose required checks must conclude is **H1**.
> 2. The anti-pattern row "Treating a cancelled H1 run as red". No push ever superseded the H1 runs, so a
>    red OR cancelled H1 run is **not expected** and is handled under D-08 stage 2 like any other red:
>    record it verbatim, halt, fix nothing.
> 3. "Four commits ... local only". It is six executor commits (`1a07a958`, `3dda866f`, `54858c7c`,
>    `269f5762`, `1e816875`, `af21e5d2`) plus the orchestrator commit that adds this note — all
>    `.planning/`-only, none on `origin`.
>
> **Open maintainer decision.** Under that ruleset NO direct push can update `release/v0.10.1` — not an
> agent's evidence commits, and not the maintainer's own section-13 tick commit, which D-10 requires to be
> pushed to this branch before the merge. Plans 37.1-09, 37.1-10 and 37.1-11 all assume such pushes work.
> Plan 37.1-09's first job (reading check conclusions on H1) is NOT blocked; every later write to the
> branch is, until the maintainer decides how updates reach it.

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure (Phase 37's own
> hard-stop discipline, and this plan's own self-referential-SHA verify defect).

- [ ] CONSTRAINT: D-08 stage 2 red-gate discipline — a required check that concludes red on the
  final head below is recorded (command/URL, SHA, failing output) and halts the phase at a
  blocking checkpoint. Fix nothing, not even a one-line doc or citation fix. Returning to the
  build wave happens only on the maintainer's say-so, and the re-seal restarts from the top.
- [ ] CONSTRAINT: D-00i — waiting is a hard stop with a resume file, never a parked watcher, never
  `gh run watch`, never a polling loop, never an agent spawned to babysit the run.
- [ ] CONSTRAINT: D-00b — the merge, any approval, and any auto-merge/merge-on-green setting are
  exclusively the maintainer's acts. Nothing in this phase, under any mode, may perform or enable
  any of them.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Parked CI watcher | Waiting live for `ci.yml`/`codeql.yml`/etc. instead of stopping | blocking | This resume file; never `gh run watch` or a polling loop |
| Treating a cancelled H1 run as red **(OBSOLETE 2026-09-21 — push #2 never landed; see the orchestrator note at the top: a red or cancelled H1 run IS a red)** | Push #2 (this plan's own final push) supersedes the H1 runs recorded below via the workflow's `cancel-in-progress` concurrency group | blocking | Read conclusions on the **final head** named below, not on H1 **(obsolete: the head is H1)** |
| Auto-approving the eventual merge/tag hand-off | Any future checkpoint asking to merge or tag is `gate="blocking-human"` and is never auto-approved, even in auto mode | blocking | D-00b, D-00a |

**Severity values:** `blocking` — resuming agent must pass understanding check before proceeding.

<current_state>

Phase 37.1 (v0.10.1 Patch Release), plan 08 of 16. The build wave (plans 37.1-02 through 37.1-06)
and the local gate re-seal (plan 37.1-07, all eight numbered gates + house sweep green on
`fbdefec9`) are complete. This plan (37.1-08) pushed `release/v0.10.1` and opened the single
release pull request. **The phase now stops here — plan 37.1-09 does not start until the resume
condition below is externally confirmed.**

**Current position:** branch `release/v0.10.1`, PR **#56**
(https://github.com/DF3NDR/paladin-dev-env/pull/56), base `main`, open, `autoMergeRequest: null`.

</current_state>

<completed_work>

Completed tasks (plan 37.1-08):
- Task 1: pushed `release/v0.10.1` (push #1, all pre-push hooks passed) and opened PR #56 with a
  curated four-part body (what broke, what changed, the re-seal summary, the pointers) plus the
  merge-commit/sign-off statement and the attribution line. Commit `1a07a958`.
- Task 2 (this file): wrote the resume file and the final metadata, then made the second and
  final push. See "Final-push substitute checks" below for the exact head this phase is now
  stopped on.

**Whole phase, plans 37.1-01 through 37.1-08:**
- SC1: `crates/paladin-battalion/Cargo.toml`'s two versioned workspace dev-dependencies on
  `paladin-llm`/`paladin-storage` made path-only.
- SC2: `scripts/create-or-reuse-release.sh`'s `_cor_gh_call` EPIPE race under `pipefail` fixed
  (parameter expansion, no subprocess); regression test added.
- SC3: `scripts/check-publish-order.sh` — the new publish-order gate, proven red on a real
  `git worktree` capture of the tagged `v0.10.0` tree and green on the fix; wired into
  `make check-gates` and into CI's **License & Dependency Policy** job (`cargo-deny`).
- SC4: all fourteen workspace manifests bumped `0.10.0` → `0.10.1`, twelve changelogs,
  `MIGRATION.md` addendum, OpenAPI baseline regenerated.
- Plan 37.1-07: all eight numbered release gates plus the house sweep re-run and recorded green
  on candidate head `fbdefec9c13f5d28d2127aa2da6da990a5285482`; `.project/v0.10.0/
  09-program-acceptance-audit.md` §13 appended with its own fresh, unticked sign-off box
  `- [ ] **The v0.10.1 tag may be cut**`.
- Plan 37.1-08 (this plan): push #1 (H1 = `7749f161919f3d5f85f20fa36eba7ad800434f97`), PR #56
  opened and live-confirmed OPEN with `autoMergeRequest: null`, this resume file, and the final
  metadata commit — see "Final-push substitute checks" for the actual final head.

</completed_work>

<remaining_work>

- **Plan 37.1-09** (not yet dispatched): read the required checks' conclusions on the **final
  head** named below — success or failure, every one of them, including `Coverage` and `License &
  Dependency Policy` — and record them verbatim in `37.1-CI-EVIDENCE.md`'s CI-run table. If any
  required check concluded red: record the command/URL, the SHA, and the failing output, halt at
  a blocking checkpoint, and fix **nothing** (D-08 stage 2). If every required check concluded
  green including `Coverage` ≥ 82%: append that as the CI-attributed proof this phase's local
  re-seal (§13) was waiting on, and proceed toward presenting the §13 sign-off box to the
  maintainer (D-10's hard order — sign-off is a maintainer act, never an agent tick, D-00a).
- Plan 37.1-10 and onward: the maintainer's tick, the CI re-run on the tick commit, the
  merge+tag hand-off (maintainer-only, D-00b), and the post-tag wave (D-04/D-12) — none of this
  starts before plan 37.1-09's read succeeds.

</remaining_work>

<decisions_made>

Point at `.planning/phases/37.1-v0-10-1-patch-release/37.1-CONTEXT.md` for the full decision
record. Called out here so a resuming agent does not have to re-derive them:

- **D-10's hard order:** local re-seal → push → PR CI green including `coverage` ≥ 82% →
  evidence appended → maintainer ticks §13 → tick commit pushed → CI green on that true final SHA
  → only then does the agent produce the merge+tag hand-off. The §13 box must not be presented for
  ticking until PR CI is green on the final head named below.
- **D-08 stage 2:** from this plan's re-seal onward, any red gate — locally or in PR CI — is
  recorded and halts the phase. Fix nothing. Returning to the build wave is the maintainer's call,
  and the re-seal restarts from the top on the new head if that happens.
- **This plan's own self-referential-SHA verify defect (T-37.1-34's mitigation, substituted):**
  see `37.1-CI-EVIDENCE.md`'s dated note "the plan's self-referential-SHA verify defect" for the
  full account. In short: neither this file nor the evidence file can contain the hash of the
  commit that contains them, so the final head is identified by three read-only commands instead
  of a literal hash embedded in either file (see "Final-push substitute checks" below).

</decisions_made>

<blockers>

**This is the load-bearing section.**

## Resume condition

The phase is stopped, not waiting. Resume only when the following single checkable fact is true:

> **Every required status check on the final pushed head (identified below, not H1) has
> concluded — success or failure — including, by name:**
> - **`Coverage`** (job key `coverage` in `.github/workflows/ci.yml`) — the CI-attributed 82%
>   workspace line-coverage floor (ADR-0006, `.planning/decisions/0006-coverage-gate.md`) that
>   this devcontainer cannot measure locally (no Docker/Redis/MinIO). This is the one gate this
>   whole plan exists to obtain.
> - **`License & Dependency Policy`** (job key `cargo-deny` in `.github/workflows/ci.yml`) — the
>   job that now also carries the new `Check publish order` step (SC3), exercising
>   `scripts/check-publish-order.sh` for real on this PR, pre-merge, for the first time.
>
> `main`'s branch-protection ruleset requires 44 named status-check contexts in total
> (`docs/src/appendix/branch-protection.md` § "The required-check set") — every one of them must
> have concluded on the final head, not merely these two; the two above are named explicitly
> because they are this phase's own headline reason for opening the PR at all (D-00e, D-07).
> `Docker Build` and `Kubernetes Smoke Test` run but are **not** required — their conclusion does
> not gate resumption.

**Identifying the final head (the SHA whose checks must conclude) — three read-only commands,
run together, in order, by whoever resumes:**

```bash
git fetch origin && git rev-parse origin/release/v0.10.1
gh pr view 56 --json headRefOid --jq .headRefOid
```

These two must print the **same** SHA — that SHA is the final head. Then:

```bash
git diff --name-only <H_named> origin/release/v0.10.1
```

must list only `.planning/` paths, where `<H_named>` = `1a07a958` (this plan's Task 1 evidence
commit — the parent of the `.continue-here.md` commit, and the last commit that existed when this
file was written). A `.planning/`-only diff between `H_named` and the final head confirms the
final head differs from the state this file describes by documentation only, not by any code
change — the property T-37.1-34's mitigation exists to guarantee.

**Exact command to read the required-check conclusions once the final head is known:**

```bash
gh pr checks 56 --json name,state,bucket,workflow
```

Every `bucket` must read `pass` or `skipping` (a conditional job genuinely not triggered, e.g.
`End-to-End Tests` — the same shape Phase 37's own PR recorded); **zero** may read `fail`.

**What the next plan (37.1-09) does first:** fetch, resolve the final head via the two commands
above, then read `gh pr checks 56` and the individual `Coverage`/`License & Dependency Policy`
job logs on that exact head. Record every conclusion — success or failure — in
`37.1-CI-EVIDENCE.md`'s CI-run table before doing anything else. If any required check is red:
record the command, the SHA, and the failing output verbatim, halt at a blocking checkpoint, and
fix nothing (D-08 stage 2). Do not re-run a red required check speculatively; D-00j's single-flake
re-run exception applies only to `release.yml`'s real release run, not to pre-merge PR CI.

**What must NOT happen while stopped:**
- No watcher process, no polling loop, no `gh run watch`, no agent spawned to babysit the PR's
  checks.
- No merge, no approval, no auto-merge/merge-on-green setting of any kind (D-00b).
- No tag push of any kind (D-00b).
- No tick of the §13 sign-off box — that is the maintainer's act alone, and only after plan
  37.1-09 confirms every required check is green (D-00a, D-10).
- No further commit on `release/v0.10.1` beyond what this plan already pushed — the plan's own
  house rule is that nothing is committed after the final push.

</blockers>

## Background processes

**None.** No `gh run watch`, no polling loop, and no other background/parked process was left
running by this plan. Confirmed at the end of this plan's own dispatch via a `ps` check for any
process this session started.

## Files modified

Plan 37.1-08, by path (all committed; pushed once, in the final push named above):

- `.planning/phases/37.1-v0-10-1-patch-release/37.1-CI-EVIDENCE.md` — push #1 record, PR #56
  record, and the self-referential-SHA defect note.
- `.planning/phases/37.1-v0-10-1-patch-release/.continue-here.md` (this file).
- `.planning/phases/37.1-v0-10-1-patch-release/37.1-08-SUMMARY.md` (this plan's summary).
- `.planning/STATE.md`, `.planning/ROADMAP.md` — plan-completion metadata.

Plus the GitHub-side artifact this plan created: release PR #56
(https://github.com/DF3NDR/paladin-dev-env/pull/56) — not a repository file.

## Outstanding async jobs

The workflow runs triggered by push #1 on H1 (`7749f161919f3d5f85f20fa36eba7ad800434f97`) —
recorded in `37.1-CI-EVIDENCE.md`'s CI-run table — are expected to be **cancelled**, not failed,
once this plan's final push lands, because `.github/workflows/ci.yml`'s own `concurrency` group
(`ci-${{ github.head_ref || github.ref }}`, `cancel-in-progress: true` off `main`) cancels an
in-flight run on the same ref superseded by a newer commit. A cancelled H1 run is **not** a red
gate and must not be read as one — read conclusions on the final head only, per "Resume condition"
above. Do not cancel or re-dispatch anything by hand; resume by reading conclusions once the final
head's checks have concluded.

## Blocking constraints

Severity `blocking` for all three:

- **The §13 tick is maintainer-only (D-00a).** No agent, under any mode including an
  automatic-advance one, ticks the sign-off box in
  `.project/v0.10.0/09-program-acceptance-audit.md`.
- **The merge and the tag are maintainer-only (D-00b).** A future plan hands over the exact merge
  method (merge commit, per this PR's own body) and the exact tag command at a
  `gate="blocking-human"` checkpoint, then stops. No agent merges to `main` and no agent pushes a
  `v*` tag.
- **A red required check stops the phase and is fixed by nobody (D-08 stage 2).** If PR CI turns
  up a red required check — including `Coverage` below 82% — plan 37.1-09 records the command,
  URL, SHA and failing output, then halts at a blocking checkpoint. It fixes nothing, not even a
  one-line doc or citation fix.

<context>
This plan (37.1-08) is intentionally not fully autonomous: it contains a conditional blocking
checkpoint (a credential that cannot open the PR — not triggered this run, the credential worked
on the first attempt) and ends in a hard stop by design (D-00i). Nothing has been merged, tagged,
ticked, or published. The branch and the PR are the only outward-facing acts this plan performs,
both explicitly authorized by D-07. The continuation agent's job (plan 37.1-09) is to read this
file, resolve the final head via the three commands above, and read that head's required-check
conclusions — nothing else, and not before those conclusions exist.
</context>

<next_action>
Resolve the final head (`git fetch origin && git rev-parse origin/release/v0.10.1`, cross-checked
against `gh pr view 56 --json headRefOid --jq .headRefOid`), confirm the `.planning/`-only diff
against `H_named` (`1a07a958`), then run `gh pr checks 56 --json name,state,bucket,workflow` as a
single read. If every bucket is `pass` or `skipping` (zero `fail`), proceed to plan 37.1-09's own
scope (recording the conclusions, including `Coverage`'s printed percentage, in
`37.1-CI-EVIDENCE.md`). If anything reads `fail`, or if the checks have not all concluded yet, do
not proceed — re-check later or update this file's resume condition, never poll live in the same
session.
</next_action>

## LIVE STATE UPDATE 2026-09-21 — this plan's own final push was rejected; read this before acting on anything above

**Everything above this section was written assuming this plan's own second/final push would
succeed. It did not.** Nothing above is deleted or rewritten (D-00d, amend-at-source) — this
section states what is actually true on the remote right now and supersedes only the parts of the
resume condition that depended on a successful final push.

**What actually happened:** push #1 (H1 = `7749f161919f3d5f85f20fa36eba7ad800434f97`) and the PR
open (#56) both succeeded exactly as recorded above. This plan then made three more local
commits (`1a07a958`, `3dda866f`, and a final metadata commit `54858c7c`) plus one more recording
this very failure (`269f5762`), and attempted a second push. **That push was rejected by GitHub**
(`GH013`, "Changes must be made through a pull request") — `release/v0.10.1` is covered by the
live `Protect release branches` ruleset, which permits the branch's initial **creation** (push #1)
but requires every subsequent **update** to go through a merged pull request, with
`bypass_actors: []` and `current_user_can_bypass: "never"` — no actor, including a repository
administrator, may push directly to update it. Full verbatim output and the ruleset read:
`37.1-CI-EVIDENCE.md`'s dated finding "push #2 rejected by the `Protect release branches`
ruleset".

**Consequence:** `origin/release/v0.10.1` is still at **H1**, unchanged. PR #56 is still `OPEN` at
H1, `autoMergeRequest: null` — completely unaffected. Four commits
(`1a07a958`, `3dda866f`, `54858c7c`, `269f5762`) exist **only in the local checkout that ran this
plan** — they are not lost (they are real, valid git commits, just unpushed), but they are not on
`origin` and this file (`.continue-here.md`) and this plan's own `37.1-08-SUMMARY.md` are
therefore **not yet readable from the remote**, only from that local checkout.

**This does NOT block plan 37.1-09's actual work.** The CI runs that matter — the ones gating the
merge — already run against **H1**, which is exactly what `origin/release/v0.10.1` and PR #56
both still point to. Plan 37.1-09 can resolve the final head and read required-check conclusions
using this file's own three-command recipe above with one simplification: since no push #2
landed, `git rev-parse origin/release/v0.10.1` and `gh pr view 56 --json headRefOid --jq
.headRefOid` both simply read **H1** — there is no separate "final head" to distinguish from H1 in
this branch's current remote state.

**What DOES need the maintainer's decision, separate from the CI-conclusion resume condition
above:** how the four local-only commits reach `origin/release/v0.10.1`, since this plan's own
authority does not extend to a workaround (no push to any other branch to stage a PR; no merge of
any PR, on any branch, under any circumstance). Options, for the maintainer to choose from — not
selected or acted on by any agent:

1. Temporarily add themselves (or another trusted actor) as a `bypass_actors` entry on the
   `Protect release branches` ruleset (id `20868128`), push the four commits directly, then
   remove the bypass again.
2. Have a future agent dispatch open a small, additional pull request — from a new branch, into
   `release/v0.10.1` — carrying exactly these four commits' content, and merge it themselves (an
   agent may open such a PR only with explicit new authorization; it may never merge it).
3. Decide the four commits' content can simply be re-authored directly against whatever the
   post-CI-read state turns out to be (i.e., treat this plan's own bookkeeping as informational
   and let plan 37.1-09 fold an equivalent record into its own commit, which — being a normal
   `git push` of a **fast-forward from H1**, exactly like this plan's own push #1 — would face the
   identical `GH013` rejection unless one of options 1-2 has already been applied. **This option
   is therefore not actually independent of 1-2** — it is flagged only so the maintainer sees
   that every future plan on this branch faces the same ruleset until it is resolved one way or
   the other.

**No agent, including this one, chose or acted on any of the above.** This is recorded as a
blocking checkpoint in this plan's own final report, not resolved here.
