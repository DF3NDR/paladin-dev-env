---
phase: 37.1-v0-10-1-patch-release
plan: 06
type: execute
wave: 6
depends_on: [37.1-05]
files_modified:
  - Cargo.toml
  - Cargo.lock
  - crates/paladin-battalion/Cargo.toml
  - crates/paladin-content/Cargo.toml
  - crates/paladin-core/Cargo.toml
  - crates/paladin-eval/Cargo.toml
  - crates/paladin-herald/Cargo.toml
  - crates/paladin-llm/Cargo.toml
  - crates/paladin-memory/Cargo.toml
  - crates/paladin-notifications/Cargo.toml
  - crates/paladin-ports/Cargo.toml
  - crates/paladin-storage/Cargo.toml
  - crates/paladin-web/Cargo.toml
  - crates/doc-examples/Cargo.toml
  - CHANGELOG.md
  - crates/paladin-battalion/CHANGELOG.md
  - crates/paladin-content/CHANGELOG.md
  - crates/paladin-core/CHANGELOG.md
  - crates/paladin-eval/CHANGELOG.md
  - crates/paladin-herald/CHANGELOG.md
  - crates/paladin-llm/CHANGELOG.md
  - crates/paladin-memory/CHANGELOG.md
  - crates/paladin-notifications/CHANGELOG.md
  - crates/paladin-ports/CHANGELOG.md
  - crates/paladin-storage/CHANGELOG.md
  - crates/paladin-web/CHANGELOG.md
  - MIGRATION.md
  - .planning/phases/37.1-v0-10-1-patch-release/37.1-CI-EVIDENCE.md
autonomous: true
requirements: [SHIP-06]
must_haves:
  truths:
    - "Every manifest in the workspace reads the patch version: the workspace package version, every sibling pin in the workspace dependency table, the root package's own direct sibling entries including the exact-equality pin, all eleven crate manifests, and the non-publishable examples manifest (ROADMAP SC4)."
    - "`Cargo.lock` is regenerated by a real cargo invocation and committed in the same change as the manifests, so no step that resolves with a locked lockfile can see a stale package version."
    - "Every publishable crate's changelog carries a new patch-version section above its previous one, in the exact bracketed-version heading grammar the release-consistency gate matches — twelve changelogs in total, the root file plus one per crate directory."
    - "`MIGRATION.md`'s title is NOT rewritten: it names the milestone's feature scope, which a patch does not change. It gains a dated appended note naming the version that actually ships through the pipeline (D-00d, amend at source)."
    - "The semver allowlist is NOT edited: it carries no version field of any kind, and its prose describing the original minor bump stays historically accurate for a patch on top of it. The mismatch between the phase's draft wording and the file's actual shape is recorded as a finding rather than resolved by inventing an edit (D-00d)."
    - "The release-consistency gate passes for the new tag name, and the publish-order gate and the composite gate stay green across the bump."
  artifacts:
    - path: "Cargo.toml"
      provides: "The workspace package version and every sibling pin at the patch version"
      contains: "[workspace.package]"
    - path: "CHANGELOG.md"
      provides: "The new patch-version section above the previous release's section"
      contains: "## [0.10.1]"
    - path: "MIGRATION.md"
      provides: "A dated appended note naming the version that actually ships, with the original title retained"
      contains: "0.10.1"
  key_links:
    - from: "Cargo.toml"
      to: "Cargo.lock"
      via: "a real cargo invocation regenerates the lockfile's package entries from the bumped manifests; the lockfile diff rides the same commit"
      pattern: "paladin"
    - from: "CHANGELOG.md"
      to: "scripts/check-release-consistency.sh"
      via: "the gate's changelog clause matches an exact bracketed-version heading for the release tag, so the heading grammar is what makes the gate pass"
      pattern: "check-release-consistency"
  prohibitions:
    - statement: "MUST NOT rewrite `MIGRATION.md`'s title or find-and-replace the previous version string inside its historical prose — the title names the milestone's feature scope and the prose describes a past decision; both stay, and a dated note is appended instead (D-00d)."
      status: unverified
      verification: judgment
    - statement: "MUST NOT edit `.cargo/semver-checks-allowlist.toml` — it has no version field, and its justification prose about the original minor bump remains accurate for a patch on top of it."
      status: unverified
      verification: judgment
    - statement: "MUST NOT delete or edit any existing changelog section — the new section is inserted above the previous one and every historical bracketed-version heading stays exactly as it is."
      status: unverified
      verification: judgment
    - statement: "MUST NOT hand-edit `Cargo.lock` — it is regenerated by cargo and committed as produced."
      status: unverified
      verification: judgment
---

<!-- planner-discipline-allow: 0.10.0 -->
<!-- planner-discipline-allow: 0\.10\.0 -->

<objective>
Move the whole workspace to the patch version, in every place that carries it and in no place that
does not.

Purpose: the release pipeline reads the version from the tag and requires every publishable
manifest and every changelog to agree with it before a single crate is published. That agreement is
what the release-consistency gate asserts. Two files in scope look like they should change and must
not: the migration guide's title names a milestone's feature scope rather than a patch tag, and the
semver allowlist has no version field at all — the draft success criterion's phrase about it
corresponds to nothing mechanical in the file.
Output: thirteen bumped manifests, a regenerated lockfile, twelve changelog sections, one dated
migration-guide note, and three recorded findings.

Governing constraints: D-00d — amend at source; findings recorded, never fixed silently. D-08
stage 1 — still the build wave. The local re-seal begins in the next plan, and from that point a
red gate is a hard stop rather than something to fix.

**Ordering note carried from plan 37.1-04.** After this bump, a per-crate package or publish check
against the live index cannot succeed for any crate whose siblings are not yet published at the new
version. That is expected, and it is the structural reason the publish-order gate is a static
assertion rather than a live per-crate rehearsal. Do not treat that failure as a regression, and do
not add such a check to any gate.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/37.1-v0-10-1-patch-release/37.1-CONTEXT.md
@.planning/phases/37.1-v0-10-1-patch-release/37.1-RESEARCH.md
@.planning/phases/37.1-v0-10-1-patch-release/37.1-PATTERNS.md
@scripts/check-release-consistency.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump every manifest in the workspace and regenerate the lockfile</name>
  <files>
    Cargo.toml, Cargo.lock,
    crates/paladin-battalion/Cargo.toml, crates/paladin-content/Cargo.toml,
    crates/paladin-core/Cargo.toml, crates/paladin-eval/Cargo.toml,
    crates/paladin-herald/Cargo.toml, crates/paladin-llm/Cargo.toml,
    crates/paladin-memory/Cargo.toml, crates/paladin-notifications/Cargo.toml,
    crates/paladin-ports/Cargo.toml, crates/paladin-storage/Cargo.toml,
    crates/paladin-web/Cargo.toml, crates/doc-examples/Cargo.toml
  </files>
  <read_first>
    - `Cargo.toml` — the workspace package version at line 58, the sibling pins in the workspace dependency table at lines 34-43 (note that the core crate is declared under a local alias with an explicit package key), and the root package's own direct sibling entries at lines 109, 202 and 318
    - `crates/paladin-ports/Cargo.toml` line 16 — the one sibling pin in the workspace that uses an exact-equality requirement rather than a caret requirement; the equality form must be preserved when the number changes
    - `crates/doc-examples/Cargo.toml` — a non-publishable manifest whose own version and sibling pins also carry the old number; RESEARCH.md Open Question 1 leaves whether to bump it to the planner, and this plan resolves it: bump it, for tree consistency, at zero release risk since it is excluded from every publishable-set derivation
    - `.planning/phases/37.1-v0-10-1-patch-release/37.1-RESEARCH.md` § "Version Bump Surface (SC4)" — the verifiable command that enumerates every file carrying the literal, and the note that the lockfile is auto-rewritten by cargo rather than hand-edited
    - `scripts/check-release-consistency.sh` — clause 1, the manifest-agreement check the bump has to satisfy
  </read_first>
  <action>
Change the number everywhere a manifest states it, and let cargo do the lockfile.

1. Bump the workspace package version from the previous patch level to `0.10.1`.
2. Bump every sibling pin in the workspace dependency table to the same number. Keep each entry's
   other keys exactly as they are, including the explicit package key on the aliased core entry.
3. Bump the root package's three direct sibling entries — the mock-featured language-model entry,
   the optional evaluation entry and the plain evaluation entry — to the same number.
4. Bump each of the eleven crate manifests' own package version and every sibling pin inside them.
   In the ports manifest, the core pin uses an exact-equality requirement: change only the number
   and keep the equality operator, since that pin's strictness is deliberate.
5. Bump the non-publishable examples manifest's own version and its sibling pins too. This resolves
   RESEARCH.md's first open question in favour of consistency: it is excluded from every
   publishable-set derivation and from the release pipeline, so the change carries no release risk,
   and leaving it as the one manifest in the tree reading a number nothing else reads would be a
   new inconsistency introduced by this phase.
6. Regenerate the lockfile by running a plain cargo check over the workspace — never by editing the
   file. Confirm the lockfile's package entries for every workspace member now read the new number.
7. Do not touch any changelog in this task, and do not touch the migration guide or the semver
   allowlist at all. Task 2 owns the changelogs and the migration note; the allowlist is not edited
   by any task in this phase.
8. Commit the manifests and the lockfile together in one commit — a lockfile landing in a different
   commit than its manifests is the exact drift a locked resolution step would catch later.
  </action>
  <acceptance_criteria>
    - `grep -c '0\.10\.0' Cargo.toml crates/*/Cargo.toml` reports 0 for every one of the fourteen manifest paths
    - `grep -c '"0\.10\.1"' Cargo.toml` is at least 14 — the workspace version, the ten sibling pins in the workspace dependency table, and the root package's three direct sibling entries
    - `grep -c 'version = "0\.10\.1"' crates/paladin-core/Cargo.toml crates/paladin-ports/Cargo.toml crates/paladin-battalion/Cargo.toml crates/paladin-herald/Cargo.toml crates/paladin-llm/Cargo.toml crates/paladin-memory/Cargo.toml crates/paladin-web/Cargo.toml crates/paladin-notifications/Cargo.toml crates/paladin-content/Cargo.toml crates/paladin-storage/Cargo.toml crates/paladin-eval/Cargo.toml crates/doc-examples/Cargo.toml` is at least 1 for each path
    - `grep -c '"=0\.10\.1"' crates/paladin-ports/Cargo.toml` is 1 — the exact-equality requirement kept its operator
    - `cargo metadata --format-version 1 --no-deps | jq -r '[.packages[] | select(.version != "0.10.1")] | length'` is 0
    - `git diff --stat Cargo.lock` is non-empty and the lockfile was produced by a cargo invocation, not by hand
    - `git diff --stat MIGRATION.md .cargo/semver-checks-allowlist.toml CHANGELOG.md` is empty — none of the three was touched by this task
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c '0\.10\.0' Cargo.toml crates/*/Cargo.toml | awk -F: '{s+=$2} END {print s+0}')" = "0" && test "$(grep -c '"0\.10\.1"' Cargo.toml)" -ge 14 && test "$(grep -c '"=0\.10\.1"' crates/paladin-ports/Cargo.toml)" = "1" && test "$(cargo metadata --format-version 1 --no-deps | jq -r '[.packages[] | select(.version != "0.10.1")] | length')" = "0" && test -n "$(git diff --stat HEAD~1 -- Cargo.lock)" && test -z "$(git diff --stat HEAD~1 -- MIGRATION.md .cargo/semver-checks-allowlist.toml CHANGELOG.md)"</automated>
  </verify>
  <done>All fourteen manifests read the patch version, the exact-equality pin kept its operator, the lockfile was regenerated by cargo and committed alongside them, and the three files that must not change in this task are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add the patch section to all twelve changelogs and append the dated migration-guide note</name>
  <files>
    CHANGELOG.md,
    crates/paladin-battalion/CHANGELOG.md, crates/paladin-content/CHANGELOG.md,
    crates/paladin-core/CHANGELOG.md, crates/paladin-eval/CHANGELOG.md,
    crates/paladin-herald/CHANGELOG.md, crates/paladin-llm/CHANGELOG.md,
    crates/paladin-memory/CHANGELOG.md, crates/paladin-notifications/CHANGELOG.md,
    crates/paladin-ports/CHANGELOG.md, crates/paladin-storage/CHANGELOG.md,
    crates/paladin-web/CHANGELOG.md,
    MIGRATION.md
  </files>
  <read_first>
    - `CHANGELOG.md` lines 1-20 — the file's own conventions and the previous release's heading at line 8, which the new section is inserted above
    - `crates/paladin-battalion/CHANGELOG.md` and `crates/paladin-eval/CHANGELOG.md` — two per-crate files, to see the heading grammar and section conventions the per-crate files use and whether they carry an unreleased section
    - `scripts/check-release-consistency.sh` — clause 2, which matches an exact bracketed-version heading for the tag being released; a prefix match does not count
    - `scripts/check-changelogs.sh` — what the composite gate asserts about per-crate changelogs
    - `MIGRATION.md` lines 1-10 — the title and the scope note directly under it, so the appended note lands where a reader of the title will see it
    - `.planning/phases/37.1-v0-10-1-patch-release/37.1-RESEARCH.md` § Pitfall 4 — why the title is appended to rather than rewritten
  </read_first>
  <action>
Twelve new sections in one grammar, and one dated note that does not rewrite history.

1. In the root changelog, insert a new section immediately above the previous release's section,
   using the same bracketed-version heading grammar with a dated suffix. Use the date the bump is
   committed. Its body states what this release is: a patch that fixes the two defects which
   stopped the previous tag's pipeline partway through, plus the new publish-order gate — no
   library source change, no API change. Name the two defects concretely, by crate and by script.
2. Decide once and apply uniformly how the previous release's section is treated. This plan's
   decision: leave its text intact and append one short dated line to it stating that the tag it
   names was cut but published only three of the twelve crates and is superseded by this patch.
   That is the amend-at-source form, and it means a reader who lands on the previous section is not
   misled about what shipped.
3. In each of the eleven per-crate changelogs, insert a section with the same bracketed-version
   heading and date. For a crate with no source change, say so plainly — a patch release carried
   by the workspace, with the reason named — rather than inventing entries. For the battalion
   crate, name the dev-dependency change, since that crate's published manifest genuinely differs.
4. In the migration guide, leave the title exactly as it is. Append a dated note in the scope area
   directly beneath it: the guide describes the milestone's feature scope, which is unchanged; the
   version that actually shipped through the pipeline is the patch version; point at the root
   changelog's new section. Do not find-and-replace the previous version string anywhere in the
   file — every remaining occurrence describes the milestone or a past decision and stays accurate.
5. Do not touch the semver allowlist. Task 3 records why.
6. Commit the twelve changelogs and the migration note together.
  </action>
  <acceptance_criteria>
    - `grep -c '^## \[0\.10\.1\]' CHANGELOG.md` is 1, and the same is 1 for each of the eleven per-crate changelogs
    - `grep -n '^## \[0\.10\.1\]' CHANGELOG.md` reports a line number smaller than `grep -n '^## \[0\.10\.0\]' CHANGELOG.md` — the new section sits above the previous one
    - `grep -c '^## \[0\.10\.0\]' CHANGELOG.md` is still 1 and every pre-existing bracketed-version heading in every changelog is still present
    - The previous release's section in the root changelog carries a dated line naming the partial publish
    - `head -1 MIGRATION.md` is unchanged from its value at the base commit
    - `MIGRATION.md` contains a dated note naming the patch version and pointing at the changelog
    - `git diff --stat .cargo/semver-checks-allowlist.toml` is empty
    - `make check-changelogs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c '^## \[0\.10\.1\]' CHANGELOG.md)" = "1" && for f in crates/*/CHANGELOG.md; do test "$(grep -c '^## \[0\.10\.1\]' "$f")" = "1" || exit 1; done && N1=$(grep -n '^## \[0\.10\.1\]' CHANGELOG.md | cut -d: -f1) && N0=$(grep -n '^## \[0\.10\.0\]' CHANGELOG.md | cut -d: -f1) && test "$N1" -lt "$N0" && test "$(head -1 MIGRATION.md)" = "$(git show 1d4a9724:MIGRATION.md | head -1)" && grep -q '0\.10\.1' MIGRATION.md && test -z "$(git diff --stat 1d4a9724 -- .cargo/semver-checks-allowlist.toml)" && make check-changelogs >/dev/null</automated>
  </verify>
  <done>All twelve changelogs carry a new patch section in the gate's heading grammar above their previous section, the previous release's section carries a dated superseded line, the migration guide keeps its title and gains a dated note, and the allowlist is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Verify the bump against the gates and record the three findings it produces</name>
  <files>.planning/phases/37.1-v0-10-1-patch-release/37.1-CI-EVIDENCE.md</files>
  <read_first>
    - `.cargo/semver-checks-allowlist.toml` in full — confirm for yourself that no entry carries a version field and that the only occurrences of the previous version are prose inside justifications and header comments
    - `scripts/check-release-consistency.sh` — how the tag argument is supplied and what the three clauses report, so the run below is invoked correctly
    - `docs/src/contributing/development-setup.md` — the stale crate-count claim RESEARCH.md and Phase 37 both noted; confirm it is still stale before recording it again
    - `.planning/phases/37.1-v0-10-1-patch-release/37.1-CI-EVIDENCE.md` — the Local sweep and Findings sections this task extends
  </read_first>
  <action>
Run what the bump has to satisfy, then write down the three things that look like work and are not.

1. Run the release-consistency gate for the new tag name. All three of its clauses must report
   agreement. Record the command and the result.
2. Run the composite gate and the publish-order gate again. Both must still be green across the
   bump — the ordering property is independent of the version, and confirming that is cheap.
3. Run the workspace format-and-lint composite and the public-API surface check. The surface check
   must pass without regenerating the committed baseline; a reported diff is a stop-and-record
   finding, because this phase adds no public item.
4. Record finding one: the semver allowlist carries no version field of any kind. The draft
   success criterion's phrase about allowlist rows reading the new version corresponds to nothing
   mechanical in that file, so no task was created to edit it, and its prose describing the
   original minor bump stays historically accurate for a patch on top of it. Quote the field names
   an entry actually carries, so the finding is checkable.
5. Record finding two: the migration guide's title was deliberately kept and a dated note appended
   instead, with the reason — the title names the milestone's feature scope, which a patch does not
   change.
6. Record finding three: the non-publishable examples manifest was bumped for tree consistency
   although the pipeline does not require it, closing RESEARCH.md's first open question with the
   decision and its rationale.
7. Carry forward, without fixing, the two documentation-currency findings already on record: the
   stale crate count in the development-setup page, and the previous release section's date being
   earlier than its actual release date. Re-confirm each against the tree before recording it
   again, and name the deferred item that owns each.
8. Add a Local sweep row per command run in steps 1-3.
9. Commit on `release/v0.10.1`.
  </action>
  <acceptance_criteria>
    - `make check-release-consistency RELEASE_TAG=v0.10.1` exits 0
    - `make check-gates` exits 0 and `./scripts/check-publish-order.sh` exits 0
    - `make clean-code` exits 0, `make api-surface` exits 0, and `git status --porcelain .project/current-exports.txt` is empty
    - The Findings section contains three new dated entries: the allowlist having no version field with the actual entry field names quoted, the migration-guide title decision, and the examples-manifest decision closing the open question
    - The Findings section carries forward the stale crate-count and changelog-date findings, each re-confirmed against the tree and each naming its deferred owner
    - The Local sweep table has a row for each of the six commands run in steps 1-3
    - `git status --porcelain` is empty after the commit
  </acceptance_criteria>
  <verify>
    <automated>make check-release-consistency RELEASE_TAG=v0.10.1 >/dev/null && make check-gates >/dev/null && ./scripts/check-publish-order.sh >/dev/null && make api-surface >/dev/null && test -z "$(git status --porcelain .project/current-exports.txt)" && E=.planning/phases/37.1-v0-10-1-patch-release/37.1-CI-EVIDENCE.md && grep -q 'semver-checks-allowlist' "$E" && grep -q 'MIGRATION.md' "$E" && grep -q 'doc-examples' "$E" && grep -q 'development-setup' "$E" && test -z "$(git status --porcelain)"</automated>
  </verify>
  <done>The bump satisfies the release-consistency gate and leaves the other gates green, and the three no-work findings plus the two carried documentation findings are recorded with their reasons.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifests and changelogs → the tag the pipeline reads | The pipeline refuses to publish anything until the tag, every publishable manifest and every changelog agree; a single missed file blocks the release or, worse, is missed by a weaker check |
| manifests → lockfile | A lockfile in a different commit than its manifests is drift any locked resolution step will surface, at the worst possible moment |
| a version-bump sweep → historical prose | A mechanical find-and-replace over every occurrence of the old number rewrites accurate statements about past decisions |

## STRIDE Threat Register (ASVS L1; block on `high`)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37.1-21 | Tampering | A manifest or changelog missed by the bump | high | mitigate | Acceptance criteria assert zero occurrences of the previous number across all fourteen manifests, a per-path positive assertion for each crate manifest, a metadata query requiring every package to read the new version, and the release-consistency gate run for the new tag name. |
| T-37.1-22 | Tampering | Historical prose rewritten by a mechanical sweep | high | mitigate | Prohibitions forbid rewriting the migration guide's title and editing the allowlist; acceptance criteria assert the title is byte-identical to the base commit's and that the allowlist diff against the base commit is empty. |
| T-37.1-23 | Repudiation | A stale lockfile shipping with bumped manifests | medium | mitigate | The lockfile is regenerated by a cargo invocation and asserted non-empty in the same commit's diff; hand-editing it is prohibited. |
| T-37.1-24 | Repudiation | The previous release's changelog section still reading as a completed release | medium | mitigate | A dated line is appended to that section naming the partial publish and the superseding version, in the amend-at-source form the project uses everywhere else. |
| T-37.1-25 | Elevation of Privilege | An unnoticed public-API move requiring a baseline refresh | high | mitigate | The surface check is run and the committed baseline is asserted unmodified afterwards; a reported diff is a stop-and-record finding, never a refresh. |
| T-37.1-SC | Tampering | Package-manager installs | low | accept | No dependency table gains a crate name; only existing version strings change. See RESEARCH.md § Package Legitimacy Audit. |
</threat_model>

<artifacts_this_phase_produces>
See `37.1-01-PLAN.md` → "Artifacts this phase produces". This plan creates no new file. It modifies
fourteen manifests, the lockfile, twelve changelogs and the migration guide. No Rust source file is
touched; the API-surface baseline does not move and is not regenerated.
</artifacts_this_phase_produces>

<verification>
- Every manifest in the workspace reads the patch version, with the exact-equality pin's operator preserved
- The lockfile was regenerated by cargo and committed with the manifests
- Twelve changelogs carry a new patch section above their previous one, in the gate's heading grammar
- The migration guide keeps its title and gains a dated note; the semver allowlist is untouched
- The release-consistency gate passes for the new tag name; the composite, publish-order, lint and surface gates are all green with the surface baseline unmodified
- Three no-work findings and two carried documentation findings are recorded

</verification>

<success_criteria>
- The old version string appears zero times across all fourteen manifests
- Every package reported by `cargo metadata` reads the new version
- `make check-release-consistency RELEASE_TAG=v0.10.1`, `make check-gates`, `./scripts/check-publish-order.sh`, `make clean-code` and `make api-surface` all exit 0
- The migration guide's first line is byte-identical to the base commit's
- The allowlist diff against the base commit is empty
</success_criteria>

<output>
Create `.planning/phases/37.1-v0-10-1-patch-release/37.1-06-SUMMARY.md` on `release/v0.10.1` when
done. Record the date used in the changelog headings and the head SHA this bump landed on — plan
37.1-07's re-seal section names that SHA as the head its gates were measured on, and the release PR
body points at the new changelog section.

**This is the last plan of the build wave.** From plan 37.1-07 onward, D-08 stage 2 applies: any
red gate, locally or in pull-request CI, is recorded with its command, SHA and failing output, and
the agent halts at a blocking checkpoint and fixes nothing. Returning to the build wave happens
only on the maintainer's say-so, and the re-seal then restarts from the top on the new head.
</output>
