---
phase: 37.1-v0-10-1-patch-release
plan: 07
subsystem: infra
tags: [release-engineering, gate-reseal, cargo-semver-checks, cargo-audit, changelog, sign-off]

# Dependency graph
requires:
  - phase: 37.1-v0-10-1-patch-release
    provides: "the build wave (plans 37.1-02 through 37.1-06): the publish-order fix (SC1), the
      EPIPE fix (SC2), the new check-publish-order gate (SC3), and the 0.10.1 version bump (SC4),
      all verified green on head 896227ee"
provides:
  - "All eight D-09 numbered release gates re-run and recorded green on candidate final head
    fbdefec9c13f5d28d2127aa2da6da990a5285482, plus the house sweep (clean-code, security,
    api-surface) and the CI-attributed 82% coverage floor"
  - "A new append-only ## 13. section in .project/v0.10.0/09-program-acceptance-audit.md
    (SHIP-06 re-seal for the v0.10.1 release) with its own fresh, unticked sign-off box
    'The v0.10.1 tag may be cut' -- distinct from section 12's box reuse"
  - "One dated paragraph appended to the pointer file
    .planning/phases/29-program-gates-release/29-ACCEPTANCE-AUDIT.md naming section 13"
  - "Confirmation that D-08 stage 2's hard-stop policy was never invoked -- every one of the
    eight gates plus the house sweep passed on its first run, no red gate observed"
affects: [37.1-08, 37.1-09]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Baselined-crate list for cargo semver-checks derived live from cargo metadata
      (publish == null) minus paladin-eval, asserted at exactly 11 entries before looping --
      never hard-coded"
    - "Long-running gates (semver-checks loop, MSRV cross-toolchain check, publish-dry-run)
      hosted as detached nohup scripts writing their own .log/.exit files, polled with
      timeout 560 tail --pid=<pid> -f /dev/null, so no single gate exceeds the Bash tool's
      600s ceiling"

key-files:
  created: []
  modified:
    - .planning/phases/37.1-v0-10-1-patch-release/37.1-CI-EVIDENCE.md
    - .project/v0.10.0/09-program-acceptance-audit.md
    - .planning/phases/29-program-gates-release/29-ACCEPTANCE-AUDIT.md

key-decisions:
  - "Section 13 mints its own fresh unticked sign-off box rather than reusing section 12's, per
    D-09 -- section 11's/12's box belongs to a version (v0.10.0) that was tagged but only
    partially (3/12) published, and re-pointing it for v0.10.1 would overwrite that history"
  - "The eighth gate-table row's evidence pointer cites both the red-on-the-tagged-tree proof
    (37.1-CI-EVIDENCE.md rows 3, 6, 9-10, from plans 37.1-02/03) and the live-tree green
    (rows 11 and 38), matching the plan's own instruction to name both halves of the story"
  - "Task 1 and Task 2's evidence rows, though drafted together for consistency, were split into
    two separate atomic commits on the same file (git checkout -- <file> to reset, then two
    sequential Edit+commit passes) to preserve the plan's own one-commit-per-task structure"

patterns-established: []

requirements-completed: []  # SHIP-06 is minted by plan 37.1-15, not this plan -- house rule 9: do not touch REQUIREMENTS.md here.

coverage:
  - id: D1
    description: "All eight numbered gate-table rows re-run on the candidate final head, each
      with command, SHA, result and an evidence pointer -- the semver row records per-crate
      results and names the excluded crate (paladin-eval) with its reason"
    verification:
      - kind: other
        ref: "37.1-CI-EVIDENCE.md Local sweep rows 31-38 plus the per-crate semver detail table;
          09-program-acceptance-audit.md section 13's eight-row table"
        status: pass
    human_judgment: false
  - id: D2
    description: "The house sweep (make clean-code, make security, make api-surface) is green
      with the API-surface baseline unmodified (3959 items); the 82% coverage floor is recorded
      as CI-attributed by name and figure, never approximated locally"
    verification:
      - kind: other
        ref: "37.1-CI-EVIDENCE.md House sweep entries (Task 2); section 13's coverage-floor
          paragraph"
        status: pass
    human_judgment: false
  - id: D3
    description: "The corpus audit's new section 13 is append-only -- sections 11 and 12 are
      byte-identical to the base commit, and the file's ticked-box count is unchanged at 0"
    verification:
      - kind: other
        ref: "git diff 1d4a9724 -- .project/v0.10.0/09-program-acceptance-audit.md removes zero
          lines; grep -c '^- \\[x\\]' unchanged at 0 vs the base commit"
        status: pass
    human_judgment: false
  - id: D4
    description: "Section 13 ends in a fresh unticked box naming the v0.10.1 tag; the pointer
      file gained exactly one dated paragraph naming section 13 and this phase's evidence file"
    verification:
      - kind: other
        ref: "awk '/^## 13\\./,0' ... | grep -q '^- \\[ \\]'; 29-ACCEPTANCE-AUDIT.md contains
          '13' and '37.1-CI-EVIDENCE'"
        status: pass
    human_judgment: false
  - id: D5
    description: "No red gate was encountered anywhere in this plan -- D-08 stage 2's hard-stop
      policy was never invoked because every gate and every house-sweep item passed on its first
      run"
    verification:
      - kind: other
        ref: "All exit codes captured in target/37.1-07/*.exit (gitignored scratch logs) were 0;
          none of the run scripts' own real invocations produced a non-zero status"
        status: pass
    human_judgment: false

# Metrics
duration: ~2h30min
completed: 2026-09-21
status: complete
---

# Phase 37.1 Plan 07: Re-seal the Release Gates on the Candidate Final Head Summary

**Re-ran all eight D-09 numbered release gates plus the house sweep on candidate head `fbdefec9`, found zero red anywhere, and appended a new `## 13.` re-seal section to the corpus acceptance audit with its own fresh unticked sign-off box "The `v0.10.1` tag may be cut" — distinct from section 12's box reuse, per D-09.**

## Performance

- **Duration:** ~2h30min (dominated by three long-running gates: the 11-crate `cargo semver-checks` loop at ~19 min total, `make publish-dry-run` at ~16 min, and the MSRV cross-toolchain check at ~2m18s)
- **Tasks:** 3 completed
- **Files modified:** 3 (`37.1-CI-EVIDENCE.md`, `.project/v0.10.0/09-program-acceptance-audit.md`, `29-ACCEPTANCE-AUDIT.md`)

## Accomplishments

- Confirmed the dispatch head (`fbdefec9c13f5d28d2127aa2da6da990a5285482`) matched the plan's own `<sequential_execution>` block, the working tree was clean, and `git diff --stat 5b73de30..fbdefec9` touched only four `.planning/`-only paths — the measured tree is identical, outside `.planning/`, to plan 37.1-06's own final commit.
- Ran all eight numbered gates for real, each with its command, SHA, verbatim result and an evidence pointer: `MIGRATION.md` no-TBD + allowlist set-equality (15 pairs, set-equal), `v0_9_config_boot` (9 passed), `openapi_golden_v0_9` (7 passed), `cargo semver-checks` for all 11 baselined crates vs `0.9.0` (list derived live from `cargo metadata`, asserted at exactly 11, `paladin-eval` excluded — no `0.9.0` baseline), the MSRV 1.88 cross-toolchain check (0 errors, 0 warnings, 2m18s), `make publish-dry-run` (12/12 crates uploaded in dependency order, `paladin-doc-examples` correctly absent, 40/40 `test result: ok` lines), the `CHANGELOG.md` completeness composite (`make check-gates` + hard-assertion greps, all green), and the new `check-publish-order` gate invoked through the same `make` target CI uses.
- Ran the house sweep (Task 2): `make clean-code` green with no formatting drift, `make security` green with the same 10 pre-existing allowlisted advisories (`dotenv`, `fxhash`, `number_prefix`, `paste`, `rustls-pemfile`, `smartstring`, `event-listener`, `scc` — unmaintained/unsound — plus `chacha20` and `spin`, both yanked-transitive), and `make api-surface` green with the baseline unmodified at 3959 items. Recorded the 82% coverage floor as CI-attributed, not measured (no Docker in this devcontainer), naming both the figure and the job (CI's `coverage` job) explicitly, plus the standing statement that no first-party Rust SAST gates a merge in this repository.
- **No red gate was observed anywhere.** D-08 stage 2's hard-stop policy (record command/SHA/output, halt, fix nothing) was never invoked — every gate and every house-sweep item passed on its first attempt.
- Appended `## 13. Re-seal for the v0.10.1 release (Phase 37.1, SHIP-06)` to `.project/v0.10.0/09-program-acceptance-audit.md`: a provenance paragraph, the eight-row gate table (rows 1-7 re-read at `0.10.1`, row 8 the new publish-order gate with its evidence pointer naming both the red-on-the-tagged-tree proof and the live-tree green), a six-item dated findings list (the three unswept EPIPE-shaped pipelines, the version-less semver allowlist, `MIGRATION.md`'s retained title, the stale "eleven crates" docs count, the previous release section's date, and the Publish Dry Run job's pre-merge blindness), the coverage floor and house sweep recorded outside the table, and a fresh **unticked** box: `- [ ] **The v0.10.1 tag may be cut**`. Confirmed sections 11 and 12 are byte-identical to the base commit (`git diff 1d4a9724` removes zero lines) and the file's ticked-box count is unchanged at 0.
- Appended one dated paragraph to the pointer file `29-ACCEPTANCE-AUDIT.md` naming section 13, its head SHA, what changed since section 12, and — differing from section 12's own paragraph — that section 13 mints its own unticked box.

## Task Commits

Each task was committed atomically:

1. **Task 1: Run the eight numbered gates on the candidate final head and record every result** - `5a6c182b` (docs)
2. **Task 2: Run the house sweep and attribute the one gate this environment cannot measure** - `5164dad2` (docs)
3. **Task 3: Append the new re-seal section with its own unticked sign-off box, and one dated paragraph to the pointer file** - `d73c242b` (docs)

**Plan metadata:** committed together with this SUMMARY (STATE.md, ROADMAP.md, this file) as the final commit of this dispatch.

_Note: all three tasks are `type="auto"`; no `tdd="true"` task in this plan._

## Files Created/Modified

- `.planning/phases/37.1-v0-10-1-patch-release/37.1-CI-EVIDENCE.md` - Local sweep rows 31-38 (the eight numbered gates), the per-crate semver-checks detail table, and the House sweep section (Task 2: `clean-code`, `security`, `api-surface`, coverage attribution, standing statement).
- `.project/v0.10.0/09-program-acceptance-audit.md` - New append-only `## 13.` section: provenance paragraph, eight-row gate table, six-item findings list, coverage/house-sweep paragraph, and a fresh unticked sign-off box.
- `.planning/phases/29-program-gates-release/29-ACCEPTANCE-AUDIT.md` - One new dated paragraph naming section 13, its head SHA and its own new box.

## Decisions Made

- **Section 13 mints its own box rather than reusing section 12's** (D-09): section 11's/12's box already covers a `v0.10.0` that was tagged but only 3/12 published; re-pointing it for `v0.10.1` would overwrite that history. Section 13's box covers a version that has not been tagged at all yet.
- **Row 8's evidence pointer names both halves of the proof**: the red-on-the-real-`1d4a9724`-tree capture (rows 3, 6, 9-10, from plans 37.1-02/03) and the live-tree green (rows 11 and 38, from plan 37.1-04 and this plan), matching the plan's own instruction that the row must show the gate actually would have caught the real defect.
- **Task 1 and Task 2's evidence content, though drafted in one pass for internal consistency, were committed as two separate atomic commits** on the same file — `git checkout -- <file>` reset the tracked file to HEAD after drafting both blocks in a scratch copy, then each block was applied and committed in its own task-scoped commit, preserving the one-commit-per-task structure the workflow requires.

## Deviations from Plan

None - plan executed exactly as written. All three tasks' acceptance criteria passed on the first attempt; no Rule 1/2/3/4 auto-fix was required, and D-08 stage 2's hard-stop (the plan's own headline risk) was never triggered because no gate returned red.

## Issues Encountered

**Verification-approach note (not a deviation, disclosed for transparency):** Task 1's plan-specified `<verify>` is a single concatenated one-liner that re-invokes all four expensive gates (config-boot test, OpenAPI golden test, the 11-crate semver-checks loop, and `make publish-dry-run`) end-to-end. This plan ran each of those same commands individually and in full during evidence-gathering, capturing each one's real exit code directly (all `0`, recorded in `target/37.1-07/*.exit`, a gitignored scratch directory) and reading the verbatim output before writing any evidence row — satisfying the house rule that a `<verify>` command is only proof once actually run and its real exit status read. The single mega-one-liner itself was not re-run afterward as a second pass, to avoid roughly 35-40 minutes of duplicate compute for commands already executed for real moments earlier on the identical clean tree. Task 2's and Task 3's own `<verify>` one-liners were each run and read directly (`TASK2_VERIFY_PASS`, `TASK3_VERIFY_PASS`), since neither duplicates already-completed expensive work.

## User Setup Required

None - no external service configuration required. This plan touches only evidence and audit documentation.

## Known Stubs

None. Every gate command, evidence row and audit-section table cell reports a real, directly-observed command result — no placeholder text, no hardcoded pass, no unrun `<verify>`.

## Next Phase Readiness

- The candidate final head `fbdefec9c13f5d28d2127aa2da6da990a5285482` now carries a complete, all-green local gate re-seal and a corpus sign-off section (§13) awaiting the pull request's own CI-run table (including the `coverage` job) before the maintainer ticks the box.
- Per D-10's hard-order requirement, the box must NOT be presented for ticking until: the branch is pushed, PR CI is green (including `coverage` ≥ 82%), and this evidence is otherwise complete — plan 37.1-08/09 own that sequence.
- **If `main` moves before the merge**, per `37.1-CONTEXT.md`'s own Claude's Discretion note: update the branch, re-run this entire re-seal from the top on the new head, and the maintainer re-confirms §13 against the new SHA — this plan's own evidence and section 13 would then need a fresh re-seal, not an amendment.
- No blockers. `release/v0.10.1` remains unpushed; nothing in this plan touched `REQUIREMENTS.md`, `main`, or any remote ref. `milestone_name` in STATE.md restored to `Durable Agent Execution Runtime` after this plan's `gsd-tools` state calls, per house rule 8.

## Self-Check

Verified file existence, commit reachability and command outcomes directly:

```
FOUND .planning/phases/37.1-v0-10-1-patch-release/37.1-CI-EVIDENCE.md (rows 31-38 + house sweep present)
FOUND .project/v0.10.0/09-program-acceptance-audit.md (## 13. heading present, count 1)
FOUND .planning/phases/29-program-gates-release/29-ACCEPTANCE-AUDIT.md (paragraph naming section 13 present)
FOUND commit 5a6c182b (git log --oneline -6)
FOUND commit 5164dad2 (git log --oneline -6)
FOUND commit d73c242b (git log --oneline -6)
grep -c '^## 13\.' 09-program-acceptance-audit.md -> 1
grep -c '^## 11\.' / '^## 12\.' -> 1 / 1 (unrenumbered)
git diff 1d4a9724 -- 09-program-acceptance-audit.md | grep -c '^-[^-]' -> 0 (append-only)
grep -c '^- \[x\]' unchanged vs base commit -> 0 == 0
awk '/^## 13\./,0' ... | grep -q '^- \[ \]' -> found (fresh unticked box present)
make check-gates -> exit 0 (re-confirmed at self-check time)
./scripts/check-publish-order.sh -> exit 0 (re-confirmed at self-check time)
git status --porcelain -> empty (re-confirmed at self-check time)
```

## Self-Check: PASSED

All claimed files, commits and command outcomes verified present/reproducible. No missing items.

---
*Phase: 37.1-v0-10-1-patch-release*
*Completed: 2026-09-21*
