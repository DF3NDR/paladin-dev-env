---
phase: 37.1-v0-10-1-patch-release
plan: 15
subsystem: infra
tags: [release-engineering, requirements, milestones, roadmap-bookkeeping, ship-06]

# Dependency graph
requires:
  - phase: 37.1-v0-10-1-patch-release
    plan: 14
    provides: "The yank disposition (three register rows) and the v0.10.0 release-object banner
      outcome, both quoted into this plan's milestone record"
provides:
  - "The MILESTONES.md v0.10.0 release record telling both tags plainly: v0.10.0 tagged and
    partially published (3/12, two defects, the blind local gate), v0.10.1 the actual release
    (12/12 registry-verified, Trusted Publishing, the yank disposition)"
  - "SHIP-06 minted with six checkable criteria mirroring roadmap Phase 37.1's success criteria,
    ticked because every criterion is true and recorded; SHIP-05 superseded at source with its
    original text intact and a dated amend-at-source note"
  - "Phase 37's missing plan summary (37-08-SUMMARY.md) ending at the D-16 hard stop and the
    maintainer's recovery decision, and dated superseded notes on its three unexecuted post-tag
    plans (37-09, 37-10, 37-11)"
  - "ROADMAP.md corrected: Phase 37's status states what is true (SC1-SC3 met, SC4 superseded),
    its Depends-on sentence corrected from 're-planned' to 'absorbed', and Phase 37.1's own
    checklist/progress row updated"
affects: [37.1-16]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Amend-at-source for a requirement whose literal text never became true: the original
      sentence is retained byte-for-byte, a dated note is appended below it, and a new
      requirement identifier is minted for what actually happened, rather than editing the old
      requirement to describe a different release."
    - "A plan can supersede a prior phase's unexecuted plans by dated note rather than deletion:
      the note lives at the top of the plan file, the body stays byte-identical, and the diff
      against the tagged commit is asserted to add lines only."

key-files:
  created:
    - .planning/phases/37-v0-10-0-crate-release/37-08-SUMMARY.md
    - .planning/phases/37.1-v0-10-1-patch-release/37.1-15-SUMMARY.md
  modified:
    - .planning/MILESTONES.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/phases/37-v0-10-0-crate-release/37-09-PLAN.md
    - .planning/phases/37-v0-10-0-crate-release/37-10-PLAN.md
    - .planning/phases/37-v0-10-0-crate-release/37-11-PLAN.md

key-decisions:
  - "The MILESTONES.md entry was inserted as a new top-of-file '## v0.10.0' heading (newest-first
    ordering, matching how v0.9.0 sits above v0.8.0), titled '(Released: 2026-09-21; milestone
    close pending)' rather than '(Shipped: ...)' — because /gsd-complete-milestone has not run
    and this plan does not run it; the release itself is real and dated, the milestone's formal
    close is a separate, later, maintainer-run act."
  - "SHIP-06 was ticked [x], not left unticked with a 'satisfied by decision' note, because every
    one of its six criteria is independently true and recorded (the maintainer yanked the three
    orphans rather than leaving them, satisfying the disposition criterion by action, not merely
    by a recorded decision)."
  - "ROADMAP.md's Phase 37.1 requirements line already read 'SHIP-06' before this plan ran — it
    was resolved directly during discuss-phase (37.1-CONTEXT.md D-02) rather than left as
    'TBD — assigned at planning.' This plan's own acceptance criteria for that line were already
    satisfied; no edit was needed there, and none was made."
  - "Phase 37's plan checklist marks 37-08 as [x] (its outcome is now fully recorded in
    37-08-SUMMARY.md, including its deviations) and 37-09/37-10/37-11 as superseded ([~]) rather
    than complete or simply left unchecked — distinguishing 'resolved, with a record' from 'never
    executed, absorbed elsewhere.'"

patterns-established: []

requirements-completed: [SHIP-06]

coverage: []  # Bookkeeping plan; every claim is a documented cross-reference into evidence files already on record, not an isolated automated test.

# Metrics
duration: ~45min
completed: 2026-09-22
status: complete
---

# Phase 37.1 Plan 15: The Milestone Record, SHIP-06, and Phase 37's Honest Close Summary

**MILESTONES.md now tells both v0.10.0 (tagged, 3/12 published, two defects) and v0.10.1 (the actual release, 12/12 registry-verified) in one entry; SHIP-06 is minted and ticked, SHIP-05 superseded at source; Phase 37's missing plan-08 summary is written ending at its D-16 hard stop, and its three unexecuted post-tag plans (09-11) carry dated superseded notes pointing at the Phase 37.1 plans that absorbed their wave.**

## Performance

- **Duration:** ~45 min
- **Tasks:** 3 (`type="auto"`, all autonomous)
- **Files modified:** 4 modified in place (`MILESTONES.md`, `REQUIREMENTS.md`, `ROADMAP.md`, plus
  three Phase 37 plan files with additions-only notes), 2 created (`37-08-SUMMARY.md`, this file)

## Accomplishments

- **Task 1 — the milestone record.** Added a new `## v0.10.0 Durable Agent Execution Runtime`
  entry at the top of `MILESTONES.md` (newest-first). States `v0.10.0` plainly: tagged on merge
  commit `1d4a9724`, release run `35404826303`, only 3 of 12 crates published (`paladin-ai-core`,
  `paladin-ports`, `paladin-herald`) before `publish-crates` failed deterministically at
  `paladin-battalion`; both defects named (the `create-or-reuse-release.sh:79` `EPIPE` race, and
  `paladin-battalion`'s two forward-pointing versioned workspace dev-dependencies); why the local
  `make publish-dry-run` gate was green and structurally blind (it resolves siblings from a local
  overlay, never the live index). Then states `v0.10.1` — the release: tag on merge commit
  `f7dae267`, release run `35659477719` (attempt 3, success; attempts 1-2 explained), all 12
  publishable crates (the live `cargo metadata`-derived count, not copied from any document)
  registry-verified with checksums and `yanked: false`, Trusted Publishing with `paladin-eval`'s
  non-null `trustpub_data` as the observable proof, and the yank disposition of the three
  `v0.10.0` orphans with a pointer to the register. Every claim points at
  `37.1-CI-EVIDENCE.md` or `docs/src/appendix/release-recovery.md` rather than restating their
  tables. The diff against the tagged commit adds lines only (verified: 0 deletions).
- **Task 2 — `SHIP-06` minted, `SHIP-05` superseded at source.** `SHIP-05` keeps its original
  sentence byte-identical and stays unticked; a dated (`2026-09-22`) amend-at-source note follows
  it, naming the tag, the 3/12 partial publish, why the pipeline cannot complete forward, and the
  superseding identifier. `SHIP-06` is minted immediately after with six checkable criteria
  mirroring roadmap Phase 37.1's own six success criteria, and is **ticked** — every criterion is
  independently true and recorded (cross-referenced to `37.1-CI-EVIDENCE.md`'s Local sweep,
  corpus audit §13, "Plan 37.1-12 Task 1", "Registry verification", and
  `docs/src/appendix/release-recovery.md` §5). The traceability table gained a `SHIP-06 | Phase
  37.1 | Complete` row and flipped `SHIP-05`'s row to `Superseded`. ROADMAP.md's Phase 37.1
  requirements line already named `SHIP-06` (resolved at discuss-phase, `37.1-CONTEXT.md` D-02)
  — verified already correct; no edit made there.
- **Task 3 — Phase 37's honest close.** Wrote `37-08-SUMMARY.md`, Phase 37's missing plan
  summary: records all three of that plan's tasks against what actually happened (Task 1's §11
  sign-off resolved by deviation — the maintainer merged before any tick commit existed, and
  their in-session sign-off statement is quoted verbatim as the record of provenance; Task 2's
  post-merge read-only re-verification, including the D-17 `paladin-eval` gate passing; Task 3's
  merge-and-tag hand-off superseded by the maintainer's direct acts, followed by both D-16
  read-only diagnoses) — and **ends** at the maintainer's recovery decision ("A"), with no content
  about Phase 37.1's own work. Appended a dated superseded note (additions only, verified 0
  deletions per file) to the top of `37-09-PLAN.md`, `37-10-PLAN.md` and `37-11-PLAN.md`, each
  naming the Phase 37.1 plan that carries the equivalent work. In `ROADMAP.md`: Phase 37's plan
  checklist now ticks `37-08` and marks `37-09`/`37-10`/`37-11` superseded (`[~]`, not `[x]`);
  its progress row and a new status line state what is true — SC1-SC3 met, SC4 superseded by
  `SHIP-06`, SC5 (the milestone close) not yet run; the incorrect "re-planned against what this
  phase ships" sentence in Phase 37.1's own Depends-on line is replaced with "this phase absorbed
  that wave"; and Phase 37.1's own plan checklist (`37.1-14`, `37.1-15` ticked) and progress row
  (`13/16` → `15/16`) were brought current.

## Task Commits

1. **Task 1: Write the milestone release record naming both tags** - `f8f82baa` (`docs(37.1-15)`)
2. **Task 2: Mint the new release requirement and supersede the previous one at source** -
   `60855521` (`docs(37.1-15)`)
3. **Task 3: Mark Phase 37's unexecuted post-tag plans superseded and write its missing summary** -
   `d419bfac` (`docs(37.1-15)`)

_Note: all three tasks are `type="auto"`; no `tdd="true"` task in this plan; no checkpoint task._

## Files Created/Modified

- `.planning/MILESTONES.md` — new top-of-file `## v0.10.0` entry telling both tags.
- `.planning/REQUIREMENTS.md` — `SHIP-05` amend-at-source note, `SHIP-06` minted and ticked,
  traceability table updated.
- `.planning/phases/37-v0-10-0-crate-release/37-08-SUMMARY.md` — new; ends at the D-16 hard stop.
- `.planning/phases/37-v0-10-0-crate-release/37-09-PLAN.md`,
  `37-10-PLAN.md`, `37-11-PLAN.md` — dated superseded notes, additions only.
- `.planning/ROADMAP.md` — Phase 37's plan checklist, progress row and status line; the corrected
  Phase 37.1 Depends-on sentence; Phase 37.1's own checklist and progress row.
- `.planning/phases/37.1-v0-10-1-patch-release/37.1-15-SUMMARY.md` — this file.

## Decisions Made

See `key-decisions` in the frontmatter above.

## Deviations from Plan

None beyond what is already documented as intentional in this plan's own action text (e.g., no
edit to ROADMAP.md's Phase 37.1 requirements line because it already read `SHIP-06`). No Rule 1-3
auto-fix was needed — every task was a bookkeeping write against already-recorded evidence.

## Issues Encountered

None.

## User Setup Required

None. Every edit in this plan is a planning-record change; no external service, credential, or
registry interaction was involved.

## Known Stubs

None.

## Threat Flags

None — every trust boundary this plan's own `<threat_model>` names (a requirement ticked over
untrue text, an unexecuted plan deleted rather than marked, a tooling handler clobbering the
state file's front matter, a stale crate count, bookkeeping landing after verification) was
exercised exactly as designed: `SHIP-05` stays unticked with its byte-identical original text,
the three Phase 37 plans gained notes rather than deletions (diffs add lines only), no
`state.*`/`roadmap.*` handler was invoked in this plan (direct edits only, per plan instruction),
`STATE.md`'s required front-matter keys are all present, the crate count is the live
`cargo metadata`-derived figure, and this plan runs before either phase's verification.

## Next Phase Readiness

- Plan 37.1-16's ready-to-close hand-off and close-out pull request body can cite: the milestone
  record, `SHIP-06`'s tick state, and the full file list this plan touched (listed above).
- Phase 37's own close statement ("SC1-SC3 met; SC4 superseded by `SHIP-06`") is now on record in
  `ROADMAP.md`, ready for a future milestone-audit/close pass to read as ground truth.
- `git status --porcelain` is empty after this plan's three commits.
- No watcher, poller, or run-watching agent was started or left running by this plan.

## Self-Check

```
FOUND .planning/MILESTONES.md — new ## v0.10.0 entry present, 0 deletions vs v0.10.1 tag
FOUND .planning/REQUIREMENTS.md — SHIP-06 count >= 2, SHIP-05 ticked count = 0, 'superseded' present
FOUND .planning/phases/37-v0-10-0-crate-release/37-08-SUMMARY.md
FOUND superseded notes in 37-09/10/11-PLAN.md, 0 deletions each vs v0.10.1 tag
FOUND .planning/ROADMAP.md — 'SHIP-06' present, 're-planned against what this phase ships' absent
git log --oneline --all | grep f8f82baa -> FOUND (Task 1 commit)
git log --oneline --all | grep 60855521 -> FOUND (Task 2 commit)
git log --oneline --all | grep d419bfac -> FOUND (Task 3 commit)
STATE.md front matter keys (gsd_state_version, milestone, current_phase, status) all present
git status --porcelain -> empty
```

## Self-Check: PASSED

Every file this plan claims to have created or modified was independently re-read after writing,
and every automated `<verify>` one-liner from the plan's own three tasks was re-run against the
committed state with the results shown above.

---
*Phase: 37.1-v0-10-1-patch-release*
*Completed: 2026-09-22*
