# Phase 37.1 v0.10.1 Patch Release — CI Evidence Record (plans 37.1-01 onward)

**Phase:** 37.1-v0-10-1-patch-release
**Branch:** `release/v0.10.1` (cut from `origin/main`'s merge commit `1d4a9724`, D-06)
**Written:** 2026-09-19

This record follows the `29-CI-EVIDENCE.md` / `33-CI-EVIDENCE.md` / `37-CI-EVIDENCE.md` house
form: a **Local sweep** table, a **CI-run table**, a **Registry verification (D-00h)** table and
a **Findings carried forward (D-00d)** section. It is this phase's own evidence file (D-11):
`37-CI-EVIDENCE.md` is sealed at the SHAs it verified and receives dated additions only, never an
edit — the dated SHA map and forward pointer appended there by this plan's Task 2 point back to
this file.

---

## Provenance

- **Branch:** `release/v0.10.1`.
- **Base commit (D-06):** `1d4a9724cc219b85856a23012543458d62559e47` — `origin/main`'s tip, the PR
  #55 merge commit tag `v0.10.0` was cut on. Re-confirmed live at this plan's Task 1 dispatch via
  `git fetch origin --tags` then `git rev-parse origin/main` — unchanged from Phase 37's own
  Provenance record.
- **Replayed range, old SHAs (on `feature/phase-33`, never pushed):**
  `1bb9406343b7fb965e0724ac7a689d45e4e5f61c` (exclusive start, the old PR #55 head) ..
  `b6d444162f9c037fe987db853e361c7a3b6c8425` (inclusive end, the tip of `feature/phase-33` at this
  plan's Task 1 dispatch, captured into a variable before the branch switch per the plan's own
  Pitfall guard against `HEAD` moving).
- **Replayed range, new SHAs (on `release/v0.10.1`):** `1d4a9724cc219b85856a23012543458d62559e47`
  (exclusive start, the base commit) .. `8b5df4d8b113972835660f2b1b97a81f79a6ee57` (the tip of
  `release/v0.10.1` immediately after the range cherry-pick, before this evidence file's own
  commit). The full old-to-new SHA map is appended to `37-CI-EVIDENCE.md` by this plan's Task 2,
  not restated here row-by-row.
- **Commits replayed:** 24, cherry-picked in one range invocation (oldest-to-newest, no `-m` flag,
  no conflicts, no merge commit introduced) — recorded strictly as a point-in-time observation of
  what `git rev-list --count 1bb94063..refs/heads/feature/phase-33` read at this plan's Task 1
  dispatch, never as a contract. See the Findings entry below: this count has already moved twice
  since CONTEXT.md was written (14 → 16 → 19 → 24) and is expected to keep moving in Phase 37.1's
  own record-keeping.
- **Toolchain this phase measures on:** rustc/cargo `1.97.1` (devcontainer-installed, matches
  `rust-toolchain.toml`'s pinned `channel = "1.97.1"`); workspace MSRV `1.88`
  (`Cargo.toml:18`); `cargo-deny 0.19.8`; `cargo-semver-checks 0.50.0` — same figures RESEARCH.md
  verified empirically for this phase, carried here as the toolchain baseline later plans in this
  phase measure against.

---

## Local sweep

Rows 1-6 below are plan 37.1-02's own record: the new publish-order gate (SC3), its committed
regression harness, and the live-worktree proof that the gate fails on the real `1d4a9724` tree —
recorded before the `paladin-battalion` defect itself is fixed (plan 37.1-04), per D-08 stage 1's
"build wave, red is expected and is the point."

| # | Command | Result (verbatim/summarized) | Verdict |
|---|---------|-------------------------------|---------|
| 1 | `git worktree add --detach <scratch-dir> 1d4a9724 && (cd <scratch-dir> && cargo metadata --format-version 1 --no-deps) > tests/scripts/fixtures/check-publish-order/metadata-1d4a9724.json && git worktree remove <scratch-dir>` | Captured `cargo metadata` output from a real worktree of commit `1d4a9724cc219b85856a23012543458d62559e47` (the `v0.10.0` tag base, `origin/main`'s tip — same base commit this file's own Provenance block records). 6447-line JSON document, 12 publishable packages enumerated (`.publish == null`). The worktree was removed immediately after the capture — confirmed via `git worktree list` showing only the main `release/v0.10.1` checkout afterward. This is a capture of the real tagged tree, not a hand-written approximation of it. | ✅ CAPTURED |
| 2 | `jq '.packages \|= map(if .name == "paladin-battalion" then .dependencies \|= map(if .kind == "dev" and (.name == "paladin-llm" or .name == "paladin-storage") then .req = "*" else . end) else . end)' metadata-1d4a9724.json > metadata-fixed.json` | Derived `tests/scripts/fixtures/check-publish-order/metadata-fixed.json` from the captured fixture by setting exactly the two offending dependency entries' `req` field — `paladin-battalion`'s `kind: "dev"` entries for `paladin-llm` and `paladin-storage` — to the match-all requirement `"*"`, leaving both entries' `path` fields untouched. Verified: the forward-pointing (`path != null AND req != "*"`) dev-dependency count for `paladin-battalion` drops from 2 to 0, while the path-carrying dev-dependency count stays 2 (unchanged). This is exactly the shape RESEARCH.md's "Battalion Ordering Fix" section observed after applying the path-only fix (dropping `version = "..."` from a workspace dev-dependency) in its own scratch worktree — Cargo strips the version constraint from the published manifest but the path dependency entry itself survives. | ✅ DERIVED (matches RESEARCH.md's own reproduction) |
| 3 | `./scripts/check-publish-order.sh --metadata-json tests/scripts/fixtures/check-publish-order/metadata-1d4a9724.json` | Exit 1. `❌ Publish-order check failed (VIOLATIONS_FOUND)` naming, in one report: `paladin-battalion (CRATES position 4) --dev--> paladin-llm (CRATES position 5): paladin-llm must publish before paladin-battalion but is ordered after it in CRATES` and `paladin-battalion (CRATES position 4) --dev--> paladin-storage (CRATES position 10): paladin-storage must publish before paladin-battalion but is ordered after it in CRATES`. Matches the crates and the edge direction `37-CI-EVIDENCE.md`'s **"D-16 read-only diagnosis #2 — `publish-crates` failed at position 4; PARTIAL PUBLISH 3/12 (2026-09-19, ~00:30Z)"** recorded from the real failed `v0.10.0` release run, byte-for-byte (that diagnosis's own traced cause: `paladin-llm = "^0.10.0"` unresolvable when `paladin-battalion` packaged at CRATES position 4). | ✅ RED as required — this IS the proof (D-08 stage 1) |
| 4 | `./scripts/check-publish-order.sh --metadata-json tests/scripts/fixtures/check-publish-order/metadata-fixed.json` | Exit 0. `✅ OK: 12 crate(s) checked in publish order; every versioned workspace dependency (normal, dev and build) resolves to an earlier CRATES position, and CRATES exactly matches the cargo-metadata publishable set.` | ✅ GREEN |
| 5 | `./tests/scripts/check-publish-order_test.sh` | Exit 0. `✅ 9 assertion(s) passed.` — all six behaviors this plan's Task 1 committed: the `1d4a9724` fixture fires naming `paladin-battalion`/`paladin-llm`/`paladin-storage` (3 assertions); the fixed fixture is silent; a publishable crate absent from `CRATES` fires naming it; a `CRATES`-listed crate absent from the publishable set fires naming it; both forward-pointing edges are named together in a single run's report (not just the first); a non-existent `--metadata-json` path is a hard named failure — plus a no-real-tree-mutation check. | ✅ GREEN |
| 6 | `./scripts/check-publish-order.sh` (no `--metadata-json` flag — live `cargo metadata` against `release/v0.10.1`'s current working tree) | Exit 1. Identical `VIOLATIONS_FOUND` report to row 3, naming `paladin-battalion`, `paladin-llm` and `paladin-storage`. The `crates/paladin-battalion/Cargo.toml` defect is still present in the tree at the end of this plan, by design — fixing it is out of this plan's scope (D-08 stage 1: "the agent fixes anything inside the six success criteria" during the build wave, but this plan's own scope boundary is the gate, not the fix). Plan 37.1-04 fixes the manifest; this exact command (no flag, live tree) is the one later plan is expected to turn green. | ✅ RED as required — the still-unfixed defect, exactly as D-08 intends |

Rows 7-10 below are plan 37.1-03's own record: wiring the gate into `make check-gates` (local) and
into pre-merge CI (the License & Dependency Policy job), and re-confirming the composite gate's
still-expected red.

| # | Command | Result (verbatim/summarized) | Verdict |
|---|---------|-------------------------------|---------|
| 7 | `git ls-files --error-unmatch scripts/check-publish-order.sh tests/scripts/check-publish-order_test.sh` then `make lint-shell` | Both shell files confirmed git-tracked (both paths printed, no error). `make lint-shell` → `✅ shellcheck clean` — a meaningful clean report, since both new files are tracked before this run, not an untracked-file false pass. | ✅ GREEN |
| 8 | `make test-shell-guards` | Exit 0. The `check-publish-order_test.sh` suite ran automatically via the `tests/scripts/*_test.sh` glob (no wiring change needed) — `✅ 9 assertion(s) passed`, interleaved among the other guard suites (`check-workflow-suppressions_test.sh`, `check-workflow-triggers_test.sh`, `check-codeql-dismissals_test.sh`, `create-or-reuse-release_test.sh`, `check-release-consistency_test.sh`, `publish-crates_test.sh`), all green, no mutation of the working tree. | ✅ GREEN |
| 9 | `make check-publish-order` | Exit 1 (make reports `Error 1`). `❌ Publish-order check failed (VIOLATIONS_FOUND)`, naming `paladin-battalion (CRATES position 4) --dev--> paladin-llm (CRATES position 5)` and `paladin-battalion (CRATES position 4) --dev--> paladin-storage (CRATES position 10)` — same as row 6, now reached through the new `make` target rather than the bare script invocation. | ✅ RED as required — this IS the D-08 stage 1 proof, now reachable through `make` |
| 10 | `make check-gates` | Exit 1. Identical `VIOLATIONS_FOUND` report to row 9 is the failing step; each of `make check-changelogs`, `make check-crate-names`, `make check-advisory-register`, `make check-workflow-suppressions`, `make check-workflow-triggers`, `make check-codeql-dismissals` and `make check-migration-allowlist` individually still exits 0 (all seven re-run standalone and confirmed green) — `check-publish-order` is the only failing prerequisite. **This red is expected at this point in the phase, not an unresolved failure**: plan 37.1-04 fixes `crates/paladin-battalion/Cargo.toml` and is expected to turn this exact command (`make check-gates`) green — the composite gate re-seal later in this phase (D-09/D-10) runs this command again on the candidate final head, where it must then be green. | ✅ RED as required, and expected — resolved by plan 37.1-04, not by this plan |

Rows 11-19 below are plan 37.1-04's own record: the SC1 fix itself (dropping `version =` from
`paladin-battalion`'s two workspace dev-dependencies on `paladin-llm`/`paladin-storage`), the gate
and composite gate turning green on the live tree as a direct, immediate result (the red-to-green
transition rows 6/9/10 above predicted and this plan exists to demonstrate), the packager
succeeding with a manifest-level proof that the two entries are genuinely gone from what would be
published, and the four adjacent gates confirmed unmoved. **Rows 9 and 10 above are hereby resolved
— `make check-publish-order` and `make check-gates` are green on `release/v0.10.1` as of the commit
this plan's Task 1 made** (`7fe4b178`), no longer an outstanding red.

| # | Command | Result (verbatim/summarized) | Verdict |
|---|---------|-------------------------------|---------|
| 11 | `./scripts/check-publish-order.sh` (no `--metadata-json` flag — live `cargo metadata` against `release/v0.10.1`'s current working tree, immediately after Task 1's commit `7fe4b178`) | Exit 0. `✅ OK: 12 crate(s) checked in publish order; every versioned workspace dependency (normal, dev and build) resolves to an earlier CRATES position, and CRATES exactly matches the cargo-metadata publishable set.` **This is the row-6/row-9 red turning green, one manifest edit apart, on the exact same command against the exact same live tree** — the SC1 proof this plan exists to produce. | ✅ GREEN — the red-to-green transition (was Exit 1 at row 6) |
| 12 | `make check-gates` | Exit 0. All eight prerequisites (`check-changelogs`, `check-crate-names`, `check-advisory-register`, `check-workflow-suppressions`, `check-workflow-triggers`, `check-codeql-dismissals`, `check-migration-allowlist`, `check-publish-order`) pass; the composite gate's own `check-publish-order` step now emits the same `✅ OK: 12 crate(s) checked...` line as row 11. | ✅ GREEN — the row-10 red resolved |
| 13 | `cargo package -p paladin-battalion --no-verify` | Exit 0. `Packaging paladin-battalion v0.10.0 (/workspace/crates/paladin-battalion)` / `Updating crates.io index` / `Packaged 72 files, 2.4MiB (490.2KiB compressed)`. Matches RESEARCH.md's own scratch-worktree reproduction (72 files, 2.4MiB) byte-for-byte in file count and size class. The meaningful sentence: `--no-verify` skips the isolated build step, not dependency resolution — resolution is exactly what failed on the unfixed manifest (RESEARCH.md § Battalion Ordering Fix item 1), so a clean `cargo package --no-verify` here is direct evidence the resolution-time defect is gone, not merely that the build compiles. | ✅ GREEN |
| 14 | Manifest-level proof: extracted `Cargo.toml` from `target/package/paladin-battalion-0.10.0.crate` (`tar -xOzf ... paladin-battalion-0.10.0/Cargo.toml`) and inspected its `[dev-dependencies.*]` tables directly | The generated manifest's dev-dependency tables are exactly `[dev-dependencies.criterion]` (`version = "0.5"`, `features = ["async_tokio"]`) and `[dev-dependencies.tokio]` (`version = "1"`, `features = ["full", "test-util"]`) — **`paladin-llm` and `paladin-storage` do not appear anywhere in the generated manifest**, confirmed by `grep -n 'paladin-llm\|paladin-storage'` on the extracted file returning zero matches. This is the manifest-level proof the plan's Task 2 requires: not merely that the gate is silent, but that the artifact Cargo would actually publish omits the two entries entirely, matching RESEARCH.md item 3's own documented Cargo behavior ("dev-dependencies without a specified version are omitted from the published crate"), now re-verified on this real edit rather than assumed from the prior scratch-worktree reproduction. | ✅ CONFIRMED — entries fully absent from the packaged manifest |
| 15 | `cargo deny check` | Exit 0. `advisories ok, bans ok, licenses ok, sources ok`. Two pre-existing `warning[yanked]` lines are emitted, both carried findings, neither caused by this plan's edit (confirmed: `git status --short Cargo.lock` / `git diff --stat Cargo.lock` are both empty — this plan touches no lock file, only a manifest version-requirement string on two already-path-resolved workspace members, so the resolved dependency graph and every yanked-crate warning are unchanged by the edit): (1) `spin 0.9.8` via `flume` → `sqlx-sqlite` → `sqlx`/`paladin-storage`/`paladin-memory` — the exact pre-existing warning RESEARCH.md item 4 already recorded on the fixed tree; (2) `chacha20 0.10.0` via `rand v0.10.1` → `rmcp`/`uuid` → the workspace — **not previously named in RESEARCH.md or this file**, recorded here as a newly-observed-but-pre-existing warning (D-00d: findings are recorded, not silently fixed) rather than attributed to this change. | ✅ GREEN, both warnings carried/pre-existing (Cargo.lock unchanged) |
| 16 | `cargo semver-checks check-release --package paladin-battalion --default-features --baseline-version 0.9.0` | Exit 0. `Checking paladin-battalion v0.9.0 -> v0.10.0 (major change)` / `Checked [0.000s] 0 checks: 0 pass, 254 skip` / `Summary no semver update required`. Dev-dependencies are never part of the public rustdoc-JSON surface this tool diffs (RESEARCH.md item 5), confirmed empirically here: the two dropped `version =` keys produced zero semver-relevant findings. | ✅ GREEN |
| 17 | `make api-surface` then `git status --porcelain .project/current-exports.txt` | `make api-surface` exit 0: `✅ API surface extracted to <tmp> (3959 items)` / `✅ API surface unchanged`. The porcelain check on the committed baseline file returns empty — **the baseline was not regenerated**, confirming this plan adds no public item (it touches only a `[dev-dependencies]` table, never `src/` or `crates/*/src/`). | ✅ GREEN, baseline untouched |
| 18 | `cargo fmt --all --check` | Exit 0, no output (nothing to reformat). | ✅ GREEN |
| 19 | `make lint lint-shell check doc-check check-api-examples` | Exit 0. `make check`'s doctest sweep passed across every crate (paladin-ports, paladin-storage, paladin-web, etc., 148 passed / 0 failed / 94 ignored); `doc-check` → `✅ doc-check passed (ADR-0033)`; `check-api-examples` → `All 101 D-05 public API entry points carry a plural '# Examples' heading.`; `lint` (clippy) and `lint-shell` (shellcheck) both silent-clean. This is `make clean-code`'s three checks run in their non-mutating form, per the plan's own note that the mutating `make clean-code` target (its `fmt` prerequisite runs `cargo fmt --all` without `--check`) must not be invoked during verification. | ✅ GREEN |

Rows 20-23 below are plan 37.1-05's own record: the SC2 fix itself (removing the `printf | head
-n1` early-closing-reader race from `_cor_gh_call`'s status-line extraction), the red-to-green pair
observed against the real script, and the sweep of the other release/guard scripts for the same
pipeline shape (findings recorded below, not fixed — D-00d).

| # | Command | Result (verbatim/summarized) | Verdict |
|---|---------|-------------------------------|---------|
| 20 | `./tests/scripts/create-or-reuse-release_test.sh` (Task 1 commit `e9ec96a0`, script still unfixed) | Exit 1. `FAIL: expected zero exit (silent) for: lookup 200 with a response body larger than the pipe buffer (200000+ chars) reuses the release (got 141)` — the signal-driven SIGPIPE/`pipefail` abort (128+13), not an output mismatch. Exactly 1 of 21 assertions failed; every pre-existing case (1-8, missing-tag, unknown-flag, no-mutation) still passed in the same run. `❌ 1/21 assertion(s) failed.` | ✅ RED as required (D-08 stage 1) — the reproduction of the real `v0.10.0` `create-release` failure mode, now committed as a regression case |
| 21 | `./tests/scripts/create-or-reuse-release_test.sh` (Task 2 commit `1ce9d432`, `_cor_gh_call` fixed) | Exit 0. `✅ 21 assertion(s) passed.` — case 9 (the 200 000+ character body) now passes silently alongside all 20 pre-existing assertions, in the same run that failed at row 20. This is the row-20 red turning green, one function-body edit apart. | ✅ GREEN — the red-to-green transition (was Exit 1/141 at row 20) |
| 22 | Two edge cases exercised directly against the real, fixed extraction logic (empty `raw`; single-line `raw` with no trailing newline), each compared against the old `printf \| head -n1 \| tr -d '\r'` form run side-by-side in the same shell session | `raw=""` → both old and new forms yield `status_line=[]` (empty). `raw="HTTP/2.0 200 OK"` (no trailing newline) → both old and new forms yield `status_line=[HTTP/2.0 200 OK]` (the whole string, unchanged). Byte-identical in both cases — the parameter-expansion form is confirmed behaviourally equivalent to `head -n1`'s prior behaviour on both named edge cases, not assumed. | ✅ CONFIRMED — no behavioural drift |
| 23 | `make lint-shell` then `make test-shell-guards` (both run on the fixed tree, after Task 2's commit) | `make lint-shell` → `✅ shellcheck clean` (both files already git-tracked, so this is a meaningful clean report). `make test-shell-guards` → all ten `tests/scripts/*_test.sh` suites green, `create-or-reuse-release_test.sh` contributing 21 of the total assertions (matching row 21) alongside `check-codeql-dismissals_test.sh` (11), `check-migration-allowlist_test.sh` (9), `check-publish-order_test.sh` (9), `check-release-consistency_test.sh` (32), `check-workflow-suppressions_test.sh` (21), `check-workflow-triggers_test.sh` (11), `extract-changelog-section_test.sh` (16), `finalize-crate-changelogs_test.sh` (25), `finalize-release-body_test.sh` (84), `package-release-binaries_test.sh` (17), `publish-crates_test.sh` (54) — no mutation of the working tree in any suite. | ✅ GREEN |

---

## CI-run table

*(No CI runs recorded by this plan — `release/v0.10.1` has not been pushed. Plan 37.1-08 owns the
first push and the first CI-run rows.)*

| Workflow | Run ID | Job | Conclusion | PR head SHA | Notes |
|----------|--------|-----|------------|--------------|-------|

---

## Registry verification (D-00h)

*(No registry reads recorded by this plan — the sparse-index verification against `0.10.1` happens
after the tag is cut, in the post-tag wave this phase absorbs per D-04.)*

| Crate | Version | Checksum | `yanked` | Notes |
|-------|---------|----------|----------|-------|

---

## Findings carried forward (D-00d)

### 2026-09-19 — D-06 commit count is live, not a contract (corrects CONTEXT.md D-06)

CONTEXT.md's own D-06 text states: *"The 14 local-only `.planning/` commits on `feature/phase-33`
(`1bb94063..HEAD`) are cherry-picked onto it in order."* That figure was already stale when
RESEARCH.md re-measured it during this phase's own research pass (**16**, two more having landed
during the discuss-phase session that produced CONTEXT.md itself), and had moved again by planning
time (**19**, per this plan's own `must_haves.truths`). At this plan's Task 1 dispatch, live
re-measurement read **24**:
`git rev-list --count 1bb94063..refs/heads/feature/phase-33` → `24`, matching
`git status -sb`'s `feature/phase-33...origin/feature/phase-33 [ahead 24]` recorded independently
in `37-CI-EVIDENCE.md`'s own Pre-flight measurements section.

**The correction is not a new number — it is that no number is ever asserted as fact.** This
plan's `must_haves.truths` explicitly forbids hard-coding or asserting a commit count anywhere in
this phase's output; the range is resolved live from git at every dispatch and recorded only as
endpoints (`1bb94063` .. the captured tip of `feature/phase-33` at that dispatch). The 24 read
above is the reading Task 1 of this plan actually replayed — recorded here as a point-in-time
observation, not a contract any later plan or reader may rely on staying true.

### 2026-09-19 — `publish-dry-run` is pull-request-blind, not merely "green and blind" (corrects CONTEXT.md D-05)

CONTEXT.md's D-05 (the MILESTONES.md v0.10.0 entry shape) says the entry should tell "the two
defects and why gate row 6 was **green and blind**" — inheriting Phase 37's framing that the
existing `make publish-dry-run` / CI `publish-dry-run` job ran, passed, and was merely blind to
inter-crate publish *order* because `cargo publish --workspace --dry-run` resolves sibling crates
from a local overlay rather than the live registry.

RESEARCH.md's re-inspection of `.github/workflows/ci.yml` found this understates the gap. The
existing `publish-dry-run` CI job is gated
`if: github.event_name == 'push' && github.ref == 'refs/heads/main'` — it has **never run on a
pull request**, only after a merge to `main`. Pre-merge, on any PR (including the one that carried
the `v0.10.0` defect), it did not gate at all — it wasn't blind to ordering while running, it
simply never ran. This is worse than "green and blind" and is recorded here in full so that plan
37.1-03's CI-wiring choice for the new SC3 publish-order gate has a written basis: the new gate
must be wired as its own fast job/step that runs on `pull_request` as well as `push`, never by
loosening the existing `publish-dry-run` job's trigger to cover pull requests (that job is
expensive — `cargo publish --workspace --dry-run` — and broadening it is explicitly ruled out as a
way to satisfy this gap). CONTEXT.md D-05's "green and blind" phrase is retained verbatim
(amend-at-source, D-00d) — this entry is the dated correction sitting alongside it, not a rewrite.

### 2026-09-19 — Spec-less probe fallback, skipped visibly

No `-SPEC.md` exists for Phase 37.1, so both `## Edge Coverage` and `## Prohibitions` probe
sections are absent (`EDGE_ABSENT=1`, `PROHIB_ABSENT=1`). The fallback that derives predicates from
requirement TEXT could not run either: this phase's only requirement ID, `SHIP-06`, does not exist
in `REQUIREMENTS.md` yet — it is minted by this phase (plan 37.1-15, CONTEXT.md D-02). There was
therefore nothing to probe, and **no probe-derived edge or prohibition predicates were generated
for this phase.** Every plan's `must_haves.truths` across this phase is derived instead from the
six ROADMAP success criteria and the locked CONTEXT.md decisions, with maintainer-gated and
post-release truths carried as `verification: backstop` markers. Recorded here as a visible,
measured skip — not an omission discovered later.

### 2026-09-19 — The publish-order gate reproduces Phase 37's real diagnosis, not a synthetic stand-in

Plan 37.1-02's `scripts/check-publish-order.sh` (Local sweep rows 1-6, above) fails on a real
`git worktree` capture of commit `1d4a9724` naming exactly the two crates and the exact edge
direction `37-CI-EVIDENCE.md`'s **"D-16 read-only diagnosis #2 — `publish-crates` failed at
position 4; PARTIAL PUBLISH 3/12 (2026-09-19, ~00:30Z)"** already traced from the real failed
release run (`paladin-battalion` at `CRATES` position 4, forward dev-dependency edges on
`paladin-llm` at position 5 and `paladin-storage` at position 10). That diagnosis's own recorded
finding — "assert, per `CRATES` position, that every versioned workspace dependency INCLUDING
dev-dependencies appears earlier in the array" — is exactly what the new gate implements. This is
recorded here as the closing of that carried finding: the claim "this gate would have caught
`v0.10.0`" rests on a captured worktree of the real tagged tree, not a hand-written fixture
approximating it, and the gate's own regression harness (row 5) proves the red-on-defective /
green-on-fixed pair is stable and repeatable, not a one-off observation.

Also worth recording plainly: the gate still fails against the live `release/v0.10.1` working tree
at the end of this plan (row 6) — the `paladin-battalion` manifest defect itself is untouched by
this plan, by design. This plan's scope is the gate, proven red; plan 37.1-04 owns the fix, proven
by this same command turning green.

### 2026-09-19 — Publish-order gate wired into License & Dependency Policy, not Publish Dry Run (plan 37.1-03)

Plan 37.1-03 wired `scripts/check-publish-order.sh` (plan 37.1-02) into pre-merge CI. The **Publish
Dry Run** job's actual condition, quoted verbatim from `.github/workflows/ci.yml:2004`:

```yaml
if: github.event_name == 'push' && github.ref == 'refs/heads/main'
```

This confirms, at the wiring site itself, the finding recorded above
("`publish-dry-run` is pull-request-blind"): that job **never runs on a pull request**, only after
a merge to `main` — so a step added there would never have gated the PR that actually carried the
`v0.10.0` defect. Consequence stated plainly: pre-merge, that job does not gate at all; it is not
merely blind to publish order while running.

For this reason the new `Check publish order` step was added instead to the **License & Dependency
Policy** job (`.github/workflows/ci.yml`, job key `cargo-deny`, name `License & Dependency Policy`)
— a job with no job-level `if:` condition, running under the workflow's own broader triggers
(`push: branches: ['**']` and `pull_request: branches: [main, 'release/**']`), and already home to
six other offline `check-*.sh` guard-script steps plus `make test-shell-guards`. The gate is pure
`cargo metadata` + shell analysis (seconds, no build, no network), so it costs nothing extra riding
alongside those existing steps.

**Widening the Publish Dry Run job's trigger to cover pull requests was deliberately not done
here** — that job is expensive (`cargo publish --workspace --dry-run`) and its default-branch-only
scope is intentional; broadening it is explicitly out of this phase's scope (this plan's own
`must_haves.prohibitions`). The diff that added the new step touches no `on:` block and no line
containing `github.event_name` or `github.ref` anywhere in the file (verified:
`git diff .github/workflows/ci.yml` shows insertions only), and
`./scripts/check-workflow-triggers.sh` still exits 0 — the recorded trigger-policy table needed no
edit.

### 2026-09-19 — Both `plan:pre` capability detectors ran and neither fired

`api-coverage` reported `detected: false` — this phase integrates no external API or SDK; the
crates.io sparse-index reads this phase performs later are read-only registry probes, not an SDK
integration, so no `COVERAGE.md` is written and none is fabricated. `assumption-delta` reported
`detected: false` — no singular-to-plural, required-to-optional, or derived-to-chosen transition is
in scope anywhere in this phase's plans. Both are recorded here as measured negatives so a later
reader sees that the detectors ran and returned nothing, rather than inferring an omission.

### 2026-09-19 — Remaining versioned workspace dependency edges after the SC1 fix (plan 37.1-04, D-00d)

Live-derived, not quoted from RESEARCH.md or CONTEXT.md — `cargo metadata --format-version 1
--no-deps` was re-run at this plan's Task 3 dispatch (immediately after the Task 1 commit
`7fe4b178` and Task 2's proof) and cross-referenced against `scripts/publish-crates.sh`'s `CRATES`
array position for every publishable package (`.publish == null`, 12 crates). The count the plan's
own acceptance criteria asks for —
`[.packages[] | select(.publish == null) | .dependencies[] | select(.path != null and .req != "*")] | length`
— reads **37** at time of writing (normal + dev + build kinds combined, `paladin-battalion`'s two
former entries no longer among them: they were the only edges this fix removed, dropping the count
from what would have been 39).

**All 37 edges are correctly ordered today** — every one resolves to an earlier `CRATES` position
than its dependent, which is exactly what `./scripts/check-publish-order.sh`'s Exit 0 (row 11,
above) already proves as a single composite assertion; this entry is the per-edge enumeration
behind that one proof, not a new claim. 34 are ordinary (`kind: null`, i.e. "normal") dependency
edges — these are structurally self-ordering: a crate cannot compile without its normal
dependencies, so a valid publish order that lets every crate build at all already orders every
normal edge correctly by construction, and none of them is a candidate for the class of defect
this phase fixed. The **3 remaining *dev*-dependency edges** — the class `paladin-battalion`'s two
former entries belonged to, and the only class this fix's scope concerns — are:

| From (CRATES pos) | Kind | To (CRATES pos) | Ordered? |
|---|---|---|---|
| `paladin-herald` (3) | dev | `paladin-ports` (2) | yes (2 < 3) |
| `paladin-ai` (12, root package) | dev | `paladin-web` (7) | yes (7 < 12) |
| `paladin-ai` (12, root package) | dev | `paladin-eval` (11) | yes (11 < 12) |

This reproduces CONTEXT.md's "Orchestrator-verified tree facts" (`paladin-herald` → `paladin-ports`
and the root package → `paladin-eval`) and RESEARCH.md's Discretion Question 2 paragraph, with one
live correction recorded per D-00f (tree outranks document): RESEARCH.md's own text names the
root-to-`paladin-web` edge as "pos 6" — the live `CRATES` array (`scripts/publish-crates.sh`, sourced
via `PUBLISH_CRATES_LIB_ONLY=1`, same table reproduced in this file's Provenance-adjacent rows) puts
`paladin-web` at **position 7** (`paladin-memory` occupies 6). The edge itself and its correctness
(7 < 12) are unaffected — only the stated position number in RESEARCH.md's prose was off by one, and
it does not change any finding's conclusion. Amend-at-source (D-00d): RESEARCH.md is not edited;
this dated correction stands alongside it.

**None of these three is being touched in this phase.** They are correctly ordered today, exactly
as `paladin-battalion`'s two former entries were *not* — that was the whole defect. The workspace-
wide rule that would make every workspace dev-dependency path-only by policy (not just the two that
broke) is CONTEXT.md's own named deferred item: **"Path-only as a workspace-wide rule for all
workspace dev-dependencies (herald → ports, root → eval), and a lint enforcing it — release-tooling
hardening for v0.11.0"** (`37.1-CONTEXT.md` § Deferred Ideas). A wider manifest diff here, on a
release branch, for edges that are not broken, buys no defect coverage — `check-publish-order`
(SC3, plan 37.1-02/03) is what protects all 37 edges going forward, dev and normal alike, and it
would fail loudly on any future crate that reintroduces this exact shape, at any position.

**Corroborating observation: the publish order itself (`CRATES` in `scripts/publish-crates.sh`) was
not changed anywhere in this phase.** The manifest fix (drop `version =` from two entries) was the
smaller, lower-risk option — a two-line diff confined to one crate's `[dev-dependencies]` table —
versus reordering the twelve-crate array that the real, already-proven-in-production publish loop
walks (`v0.10.0`'s first three crates published successfully through it). RESEARCH.md's own
recommendation reached the same conclusion independently (§ Battalion Ordering Fix, "Primary
recommendation"), and this plan's own prohibition (`must_haves.prohibitions`, "MUST NOT reorder the
`CRATES` array") made it a hard constraint rather than a preference. `git diff --stat
scripts/publish-crates.sh` against this plan's own commits reads empty, confirmed at Task 1 and
unchanged through Task 3.

### 2026-09-20 — Script-fix sweep re-run against the current tree: three further early-closing-reader-under-pipefail instances, none fixed (plan 37.1-05, D-00d)

Re-run live against the current `release/v0.10.1` tree at this plan's Task 3 dispatch (immediately
after the Task 2 fix commit `1ce9d432`), not transcribed from RESEARCH.md's own "Script-fix sweep"
section — every file and line number below was read and confirmed directly against the tree, per
this task's own instruction. The search scope was the same class RESEARCH.md's sweep defined: a
pipeline whose reader (`head`, `grep -q`, or similar) can exit before its writer has finished
writing, under a shell that treats a failing pipeline element as fatal (`set -euo pipefail`).

| File:Line | Shape | Live pipe or here-string? | Risk assessment |
|---|---|---|---|
| `scripts/create-or-reuse-release.sh:79` (pre-fix; now removed by this plan's Task 2) | `printf "%s\n" "$raw" \| head -n1 \| tr -d '\r'` | Live pipe (variable → pipe) | **The SC2 fix target — no longer present in the tree.** `_cor_gh_call`'s status-line extraction is now pure bash parameter expansion (`${raw%%$'\n'*}` / `${status_line//$'\r'/}`); there is no subprocess and no pipe left for this race to act on. |
| `scripts/check-deprecations.sh:32` | `if echo "$OUTPUT" \| grep -q "warning:.*deprecated"; then` (under `set -euo pipefail`, confirmed at line 4) | Live pipe (variable → pipe) | **Same shape, same risk class as the SC2 defect.** `grep -q` exits as soon as it finds the first match; `$OUTPUT` is a full `cargo build --lib` output capture, plausibly large on a workspace this size. If the matching `warning:...deprecated` line appears early in the output, `echo` can receive `SIGPIPE` on its next write. Not touched by this plan — recorded as a finding for the deferred wider release-tooling audit. |
| `scripts/check-doc-examples.sh:142` | `rustfmt --edition 2021 "${tmp_rs}" 2>&1 \| head -10` (under `set -euo pipefail`, confirmed at line 20) | Live pipe (process → pipe) | Same shape. `rustfmt` output is a syntax-error dump — plausibly large for a malformed doc example, though rarely as large as a full compiler cascade. Recorded as a finding, not fixed. |
| `scripts/check-doc-examples.sh:164` | `cargo check --manifest-path "${crate_dir}/Cargo.toml" 2>&1 \| head -20` (same `set -euo pipefail` scope) | Live pipe (process → pipe) | **Same shape, plausibly the highest realistic trigger rate of the three.** This is a diagnostic-only re-invocation of `cargo check` *inside an already-failed branch* — the preceding `if ! cargo check ...; then` on the same block already decided pass/fail via its own exit code; this second invocation exists purely to print the failing diagnostic. `cargo check` compiler-error dumps for generic/trait-bound cascades routinely exceed 64 KB. A `SIGPIPE` here would abort the whole `check-doc-examples.sh` script under `pipefail`, rather than merely truncating the diagnostic output the reader is trying to show — turning a cosmetic "diagnostic got cut off" outcome into a hard script abort that could look like an unrelated CI infrastructure failure. This is the one of the three worth flagging as the priority candidate for the deferred audit, precisely because its trigger condition (a real compile failure with a large diagnostic) is exactly the condition under which the script is already in its unhappy path and the operator most needs the output to actually print. |
| `scripts/check-public-api-examples.sh:136` | `name="$(grep -oE "${name_pattern}" <<< "${rest}" \| head -1)"` (under `set -euo pipefail`, confirmed at line 29) | Partially — `<<<"${rest}"` is a here-string (safe, no live pipe on the input side), but `grep -oE`'s own stdout re-enters a live pipe feeding `head -1` | Same structural shape but low practical risk in this instance — `${rest}` is a single doc-comment/heading line and `grep -oE` extracts a short name substring from it, so the writer-side payload here is always small. Recorded as a finding for completeness, not because it is a realistic trigger. |
| `scripts/check-{codeql-dismissals,advisory-register,crate-names,workflow-triggers,changelogs,workflow-suppressions}.sh`, `scripts/finalize-crate-changelogs.sh`, `scripts/check-release-consistency.sh` — every `STATUS_LINE=$(head -n1 <<<"${REPORT}")`-shaped instance | Here-string into `head -n1` | Here-string (not a live pipe) | **Confirmed structurally safe, not vulnerable.** A here-string is fully materialized (bash writes it to a temp file or an internal buffer) before the reader ever starts reading, so there is no writer process left racing the reader's early exit — the exact mechanism this race class depends on is absent. This confirms CONTEXT.md's narrower literal-form claim was correct for every one of these sites. |

**Verdict on the original claim, stated in both halves, as this plan's `must_haves.truths` requires:**

- **The narrow claim is CONFIRMED.** `scripts/create-or-reuse-release.sh:79` (pre-fix) was the only
  file in `scripts/*.sh` using the exact literal `printf ... | head -n1 | tr -d '\r'` construct — a
  tree-wide `grep` for that literal shape found no second instance, before or after this plan's fix
  removed the one instance that existed.
- **The broader implication is OVERTURNED.** The narrow claim does *not* mean no other instance of
  the underlying early-closing-reader-under-`pipefail` race class exists. **Three further instances
  of that class exist in the current tree**, none of them exercised by this phase's SC2 and none
  fixed by it: `scripts/check-deprecations.sh:32`, `scripts/check-doc-examples.sh:142`,
  `scripts/check-doc-examples.sh:164`, and `scripts/check-public-api-examples.sh:136` (four line
  citations across three files, matching this plan's own `must_haves.truths` wording of "three other
  pipelines" — `check-doc-examples.sh` contributes two of the four citations).

**Deferred owner, and why none of the three is fixed here:** all three sites are recorded findings
assigned to `37.1-CONTEXT.md`'s own named deferred item — **"A wider first-run audit of the release
scripts (every `pipefail` pipeline with an early-closing reader, every path exercised for the first
time on tag day)"** (§ Deferred Ideas) — the same register RESEARCH.md's own sweep assigned them to.
Fixing any of the three here would widen this release branch's diff outside SC2's stated intent (one
function, one assignment, in one file) for a class of defect that has not yet caused an observed
production failure the way `create-or-reuse-release.sh:79` did. `check-doc-examples.sh:164` (the
diagnostic re-invocation inside an already-failed branch, fed by output that routinely exceeds the
buffer for cascading compiler errors) is named above as the one with the highest realistic trigger
rate among the three, and is the natural first item for whoever picks up the deferred audit.

`git diff --stat scripts/check-deprecations.sh scripts/check-doc-examples.sh
scripts/check-public-api-examples.sh` against this plan's own commits reads empty — nothing in any
of the three swept-but-unfixed files was touched.
