# Phase 37.1 v0.10.1 Patch Release — CI Evidence Record (plans 37.1-01 onward)

**Phase:** 37.1-v0-10-1-patch-release
**Branch:** `release/v0.10.1` (cut from `origin/main`'s merge commit `1d4a9724`, D-06)
**Written:** 2026-09-19

This record follows the `29-CI-EVIDENCE.md` / `33-CI-EVIDENCE.md` / `37-CI-EVIDENCE.md` house
form: a **Local sweep** table, a **CI-run table**, a **Registry verification (D-00h)** table and
a **Findings carried forward (D-00d)** section. It is this phase's own evidence file (D-11):
`37-CI-EVIDENCE.md` is sealed at the SHAs it verified and receives dated additions only, never an
edit — the dated SHA map and forward pointer appended there by this plan's Task 2 point back to
this file.

---

## Provenance

- **Branch:** `release/v0.10.1`.
- **Base commit (D-06):** `1d4a9724cc219b85856a23012543458d62559e47` — `origin/main`'s tip, the PR
  #55 merge commit tag `v0.10.0` was cut on. Re-confirmed live at this plan's Task 1 dispatch via
  `git fetch origin --tags` then `git rev-parse origin/main` — unchanged from Phase 37's own
  Provenance record.
- **Replayed range, old SHAs (on `feature/phase-33`, never pushed):**
  `1bb9406343b7fb965e0724ac7a689d45e4e5f61c` (exclusive start, the old PR #55 head) ..
  `b6d444162f9c037fe987db853e361c7a3b6c8425` (inclusive end, the tip of `feature/phase-33` at this
  plan's Task 1 dispatch, captured into a variable before the branch switch per the plan's own
  Pitfall guard against `HEAD` moving).
- **Replayed range, new SHAs (on `release/v0.10.1`):** `1d4a9724cc219b85856a23012543458d62559e47`
  (exclusive start, the base commit) .. `8b5df4d8b113972835660f2b1b97a81f79a6ee57` (the tip of
  `release/v0.10.1` immediately after the range cherry-pick, before this evidence file's own
  commit). The full old-to-new SHA map is appended to `37-CI-EVIDENCE.md` by this plan's Task 2,
  not restated here row-by-row.
- **Commits replayed:** 24, cherry-picked in one range invocation (oldest-to-newest, no `-m` flag,
  no conflicts, no merge commit introduced) — recorded strictly as a point-in-time observation of
  what `git rev-list --count 1bb94063..refs/heads/feature/phase-33` read at this plan's Task 1
  dispatch, never as a contract. See the Findings entry below: this count has already moved twice
  since CONTEXT.md was written (14 → 16 → 19 → 24) and is expected to keep moving in Phase 37.1's
  own record-keeping.
- **Toolchain this phase measures on:** rustc/cargo `1.97.1` (devcontainer-installed, matches
  `rust-toolchain.toml`'s pinned `channel = "1.97.1"`); workspace MSRV `1.88`
  (`Cargo.toml:18`); `cargo-deny 0.19.8`; `cargo-semver-checks 0.50.0` — same figures RESEARCH.md
  verified empirically for this phase, carried here as the toolchain baseline later plans in this
  phase measure against.

---

## Local sweep

Rows 1-6 below are plan 37.1-02's own record: the new publish-order gate (SC3), its committed
regression harness, and the live-worktree proof that the gate fails on the real `1d4a9724` tree —
recorded before the `paladin-battalion` defect itself is fixed (plan 37.1-04), per D-08 stage 1's
"build wave, red is expected and is the point."

| # | Command | Result (verbatim/summarized) | Verdict |
|---|---------|-------------------------------|---------|
| 1 | `git worktree add --detach <scratch-dir> 1d4a9724 && (cd <scratch-dir> && cargo metadata --format-version 1 --no-deps) > tests/scripts/fixtures/check-publish-order/metadata-1d4a9724.json && git worktree remove <scratch-dir>` | Captured `cargo metadata` output from a real worktree of commit `1d4a9724cc219b85856a23012543458d62559e47` (the `v0.10.0` tag base, `origin/main`'s tip — same base commit this file's own Provenance block records). 6447-line JSON document, 12 publishable packages enumerated (`.publish == null`). The worktree was removed immediately after the capture — confirmed via `git worktree list` showing only the main `release/v0.10.1` checkout afterward. This is a capture of the real tagged tree, not a hand-written approximation of it. | ✅ CAPTURED |
| 2 | `jq '.packages \|= map(if .name == "paladin-battalion" then .dependencies \|= map(if .kind == "dev" and (.name == "paladin-llm" or .name == "paladin-storage") then .req = "*" else . end) else . end)' metadata-1d4a9724.json > metadata-fixed.json` | Derived `tests/scripts/fixtures/check-publish-order/metadata-fixed.json` from the captured fixture by setting exactly the two offending dependency entries' `req` field — `paladin-battalion`'s `kind: "dev"` entries for `paladin-llm` and `paladin-storage` — to the match-all requirement `"*"`, leaving both entries' `path` fields untouched. Verified: the forward-pointing (`path != null AND req != "*"`) dev-dependency count for `paladin-battalion` drops from 2 to 0, while the path-carrying dev-dependency count stays 2 (unchanged). This is exactly the shape RESEARCH.md's "Battalion Ordering Fix" section observed after applying the path-only fix (dropping `version = "..."` from a workspace dev-dependency) in its own scratch worktree — Cargo strips the version constraint from the published manifest but the path dependency entry itself survives. | ✅ DERIVED (matches RESEARCH.md's own reproduction) |
| 3 | `./scripts/check-publish-order.sh --metadata-json tests/scripts/fixtures/check-publish-order/metadata-1d4a9724.json` | Exit 1. `❌ Publish-order check failed (VIOLATIONS_FOUND)` naming, in one report: `paladin-battalion (CRATES position 4) --dev--> paladin-llm (CRATES position 5): paladin-llm must publish before paladin-battalion but is ordered after it in CRATES` and `paladin-battalion (CRATES position 4) --dev--> paladin-storage (CRATES position 10): paladin-storage must publish before paladin-battalion but is ordered after it in CRATES`. Matches the crates and the edge direction `37-CI-EVIDENCE.md`'s **"D-16 read-only diagnosis #2 — `publish-crates` failed at position 4; PARTIAL PUBLISH 3/12 (2026-09-19, ~00:30Z)"** recorded from the real failed `v0.10.0` release run, byte-for-byte (that diagnosis's own traced cause: `paladin-llm = "^0.10.0"` unresolvable when `paladin-battalion` packaged at CRATES position 4). | ✅ RED as required — this IS the proof (D-08 stage 1) |
| 4 | `./scripts/check-publish-order.sh --metadata-json tests/scripts/fixtures/check-publish-order/metadata-fixed.json` | Exit 0. `✅ OK: 12 crate(s) checked in publish order; every versioned workspace dependency (normal, dev and build) resolves to an earlier CRATES position, and CRATES exactly matches the cargo-metadata publishable set.` | ✅ GREEN |
| 5 | `./tests/scripts/check-publish-order_test.sh` | Exit 0. `✅ 9 assertion(s) passed.` — all six behaviors this plan's Task 1 committed: the `1d4a9724` fixture fires naming `paladin-battalion`/`paladin-llm`/`paladin-storage` (3 assertions); the fixed fixture is silent; a publishable crate absent from `CRATES` fires naming it; a `CRATES`-listed crate absent from the publishable set fires naming it; both forward-pointing edges are named together in a single run's report (not just the first); a non-existent `--metadata-json` path is a hard named failure — plus a no-real-tree-mutation check. | ✅ GREEN |
| 6 | `./scripts/check-publish-order.sh` (no `--metadata-json` flag — live `cargo metadata` against `release/v0.10.1`'s current working tree) | Exit 1. Identical `VIOLATIONS_FOUND` report to row 3, naming `paladin-battalion`, `paladin-llm` and `paladin-storage`. The `crates/paladin-battalion/Cargo.toml` defect is still present in the tree at the end of this plan, by design — fixing it is out of this plan's scope (D-08 stage 1: "the agent fixes anything inside the six success criteria" during the build wave, but this plan's own scope boundary is the gate, not the fix). Plan 37.1-04 fixes the manifest; this exact command (no flag, live tree) is the one later plan is expected to turn green. | ✅ RED as required — the still-unfixed defect, exactly as D-08 intends |

Rows 7-10 below are plan 37.1-03's own record: wiring the gate into `make check-gates` (local) and
into pre-merge CI (the License & Dependency Policy job), and re-confirming the composite gate's
still-expected red.

| # | Command | Result (verbatim/summarized) | Verdict |
|---|---------|-------------------------------|---------|
| 7 | `git ls-files --error-unmatch scripts/check-publish-order.sh tests/scripts/check-publish-order_test.sh` then `make lint-shell` | Both shell files confirmed git-tracked (both paths printed, no error). `make lint-shell` → `✅ shellcheck clean` — a meaningful clean report, since both new files are tracked before this run, not an untracked-file false pass. | ✅ GREEN |
| 8 | `make test-shell-guards` | Exit 0. The `check-publish-order_test.sh` suite ran automatically via the `tests/scripts/*_test.sh` glob (no wiring change needed) — `✅ 9 assertion(s) passed`, interleaved among the other guard suites (`check-workflow-suppressions_test.sh`, `check-workflow-triggers_test.sh`, `check-codeql-dismissals_test.sh`, `create-or-reuse-release_test.sh`, `check-release-consistency_test.sh`, `publish-crates_test.sh`), all green, no mutation of the working tree. | ✅ GREEN |
| 9 | `make check-publish-order` | Exit 1 (make reports `Error 1`). `❌ Publish-order check failed (VIOLATIONS_FOUND)`, naming `paladin-battalion (CRATES position 4) --dev--> paladin-llm (CRATES position 5)` and `paladin-battalion (CRATES position 4) --dev--> paladin-storage (CRATES position 10)` — same as row 6, now reached through the new `make` target rather than the bare script invocation. | ✅ RED as required — this IS the D-08 stage 1 proof, now reachable through `make` |
| 10 | `make check-gates` | Exit 1. Identical `VIOLATIONS_FOUND` report to row 9 is the failing step; each of `make check-changelogs`, `make check-crate-names`, `make check-advisory-register`, `make check-workflow-suppressions`, `make check-workflow-triggers`, `make check-codeql-dismissals` and `make check-migration-allowlist` individually still exits 0 (all seven re-run standalone and confirmed green) — `check-publish-order` is the only failing prerequisite. **This red is expected at this point in the phase, not an unresolved failure**: plan 37.1-04 fixes `crates/paladin-battalion/Cargo.toml` and is expected to turn this exact command (`make check-gates`) green — the composite gate re-seal later in this phase (D-09/D-10) runs this command again on the candidate final head, where it must then be green. | ✅ RED as required, and expected — resolved by plan 37.1-04, not by this plan |

Rows 11-19 below are plan 37.1-04's own record: the SC1 fix itself (dropping `version =` from
`paladin-battalion`'s two workspace dev-dependencies on `paladin-llm`/`paladin-storage`), the gate
and composite gate turning green on the live tree as a direct, immediate result (the red-to-green
transition rows 6/9/10 above predicted and this plan exists to demonstrate), the packager
succeeding with a manifest-level proof that the two entries are genuinely gone from what would be
published, and the four adjacent gates confirmed unmoved. **Rows 9 and 10 above are hereby resolved
— `make check-publish-order` and `make check-gates` are green on `release/v0.10.1` as of the commit
this plan's Task 1 made** (`7fe4b178`), no longer an outstanding red.

| # | Command | Result (verbatim/summarized) | Verdict |
|---|---------|-------------------------------|---------|
| 11 | `./scripts/check-publish-order.sh` (no `--metadata-json` flag — live `cargo metadata` against `release/v0.10.1`'s current working tree, immediately after Task 1's commit `7fe4b178`) | Exit 0. `✅ OK: 12 crate(s) checked in publish order; every versioned workspace dependency (normal, dev and build) resolves to an earlier CRATES position, and CRATES exactly matches the cargo-metadata publishable set.` **This is the row-6/row-9 red turning green, one manifest edit apart, on the exact same command against the exact same live tree** — the SC1 proof this plan exists to produce. | ✅ GREEN — the red-to-green transition (was Exit 1 at row 6) |
| 12 | `make check-gates` | Exit 0. All eight prerequisites (`check-changelogs`, `check-crate-names`, `check-advisory-register`, `check-workflow-suppressions`, `check-workflow-triggers`, `check-codeql-dismissals`, `check-migration-allowlist`, `check-publish-order`) pass; the composite gate's own `check-publish-order` step now emits the same `✅ OK: 12 crate(s) checked...` line as row 11. | ✅ GREEN — the row-10 red resolved |
| 13 | `cargo package -p paladin-battalion --no-verify` | Exit 0. `Packaging paladin-battalion v0.10.0 (/workspace/crates/paladin-battalion)` / `Updating crates.io index` / `Packaged 72 files, 2.4MiB (490.2KiB compressed)`. Matches RESEARCH.md's own scratch-worktree reproduction (72 files, 2.4MiB) byte-for-byte in file count and size class. The meaningful sentence: `--no-verify` skips the isolated build step, not dependency resolution — resolution is exactly what failed on the unfixed manifest (RESEARCH.md § Battalion Ordering Fix item 1), so a clean `cargo package --no-verify` here is direct evidence the resolution-time defect is gone, not merely that the build compiles. | ✅ GREEN |
| 14 | Manifest-level proof: extracted `Cargo.toml` from `target/package/paladin-battalion-0.10.0.crate` (`tar -xOzf ... paladin-battalion-0.10.0/Cargo.toml`) and inspected its `[dev-dependencies.*]` tables directly | The generated manifest's dev-dependency tables are exactly `[dev-dependencies.criterion]` (`version = "0.5"`, `features = ["async_tokio"]`) and `[dev-dependencies.tokio]` (`version = "1"`, `features = ["full", "test-util"]`) — **`paladin-llm` and `paladin-storage` do not appear anywhere in the generated manifest**, confirmed by `grep -n 'paladin-llm\|paladin-storage'` on the extracted file returning zero matches. This is the manifest-level proof the plan's Task 2 requires: not merely that the gate is silent, but that the artifact Cargo would actually publish omits the two entries entirely, matching RESEARCH.md item 3's own documented Cargo behavior ("dev-dependencies without a specified version are omitted from the published crate"), now re-verified on this real edit rather than assumed from the prior scratch-worktree reproduction. | ✅ CONFIRMED — entries fully absent from the packaged manifest |
| 15 | `cargo deny check` | Exit 0. `advisories ok, bans ok, licenses ok, sources ok`. Two pre-existing `warning[yanked]` lines are emitted, both carried findings, neither caused by this plan's edit (confirmed: `git status --short Cargo.lock` / `git diff --stat Cargo.lock` are both empty — this plan touches no lock file, only a manifest version-requirement string on two already-path-resolved workspace members, so the resolved dependency graph and every yanked-crate warning are unchanged by the edit): (1) `spin 0.9.8` via `flume` → `sqlx-sqlite` → `sqlx`/`paladin-storage`/`paladin-memory` — the exact pre-existing warning RESEARCH.md item 4 already recorded on the fixed tree; (2) `chacha20 0.10.0` via `rand v0.10.1` → `rmcp`/`uuid` → the workspace — **not previously named in RESEARCH.md or this file**, recorded here as a newly-observed-but-pre-existing warning (D-00d: findings are recorded, not silently fixed) rather than attributed to this change. | ✅ GREEN, both warnings carried/pre-existing (Cargo.lock unchanged) |
| 16 | `cargo semver-checks check-release --package paladin-battalion --default-features --baseline-version 0.9.0` | Exit 0. `Checking paladin-battalion v0.9.0 -> v0.10.0 (major change)` / `Checked [0.000s] 0 checks: 0 pass, 254 skip` / `Summary no semver update required`. Dev-dependencies are never part of the public rustdoc-JSON surface this tool diffs (RESEARCH.md item 5), confirmed empirically here: the two dropped `version =` keys produced zero semver-relevant findings. | ✅ GREEN |
| 17 | `make api-surface` then `git status --porcelain .project/current-exports.txt` | `make api-surface` exit 0: `✅ API surface extracted to <tmp> (3959 items)` / `✅ API surface unchanged`. The porcelain check on the committed baseline file returns empty — **the baseline was not regenerated**, confirming this plan adds no public item (it touches only a `[dev-dependencies]` table, never `src/` or `crates/*/src/`). | ✅ GREEN, baseline untouched |
| 18 | `cargo fmt --all --check` | Exit 0, no output (nothing to reformat). | ✅ GREEN |
| 19 | `make lint lint-shell check doc-check check-api-examples` | Exit 0. `make check`'s doctest sweep passed across every crate (paladin-ports, paladin-storage, paladin-web, etc., 148 passed / 0 failed / 94 ignored); `doc-check` → `✅ doc-check passed (ADR-0033)`; `check-api-examples` → `All 101 D-05 public API entry points carry a plural '# Examples' heading.`; `lint` (clippy) and `lint-shell` (shellcheck) both silent-clean. This is `make clean-code`'s three checks run in their non-mutating form, per the plan's own note that the mutating `make clean-code` target (its `fmt` prerequisite runs `cargo fmt --all` without `--check`) must not be invoked during verification. | ✅ GREEN |

Rows 20-23 below are plan 37.1-05's own record: the SC2 fix itself (removing the `printf | head
-n1` early-closing-reader race from `_cor_gh_call`'s status-line extraction), the red-to-green pair
observed against the real script, and the sweep of the other release/guard scripts for the same
pipeline shape (findings recorded below, not fixed — D-00d).

| # | Command | Result (verbatim/summarized) | Verdict |
|---|---------|-------------------------------|---------|
| 20 | `./tests/scripts/create-or-reuse-release_test.sh` (Task 1 commit `e9ec96a0`, script still unfixed) | Exit 1. `FAIL: expected zero exit (silent) for: lookup 200 with a response body larger than the pipe buffer (200000+ chars) reuses the release (got 141)` — the signal-driven SIGPIPE/`pipefail` abort (128+13), not an output mismatch. Exactly 1 of 21 assertions failed; every pre-existing case (1-8, missing-tag, unknown-flag, no-mutation) still passed in the same run. `❌ 1/21 assertion(s) failed.` | ✅ RED as required (D-08 stage 1) — the reproduction of the real `v0.10.0` `create-release` failure mode, now committed as a regression case |
| 21 | `./tests/scripts/create-or-reuse-release_test.sh` (Task 2 commit `1ce9d432`, `_cor_gh_call` fixed) | Exit 0. `✅ 21 assertion(s) passed.` — case 9 (the 200 000+ character body) now passes silently alongside all 20 pre-existing assertions, in the same run that failed at row 20. This is the row-20 red turning green, one function-body edit apart. | ✅ GREEN — the red-to-green transition (was Exit 1/141 at row 20) |
| 22 | Two edge cases exercised directly against the real, fixed extraction logic (empty `raw`; single-line `raw` with no trailing newline), each compared against the old `printf \| head -n1 \| tr -d '\r'` form run side-by-side in the same shell session | `raw=""` → both old and new forms yield `status_line=[]` (empty). `raw="HTTP/2.0 200 OK"` (no trailing newline) → both old and new forms yield `status_line=[HTTP/2.0 200 OK]` (the whole string, unchanged). Byte-identical in both cases — the parameter-expansion form is confirmed behaviourally equivalent to `head -n1`'s prior behaviour on both named edge cases, not assumed. | ✅ CONFIRMED — no behavioural drift |
| 23 | `make lint-shell` then `make test-shell-guards` (both run on the fixed tree, after Task 2's commit) | `make lint-shell` → `✅ shellcheck clean` (both files already git-tracked, so this is a meaningful clean report). `make test-shell-guards` → all ten `tests/scripts/*_test.sh` suites green, `create-or-reuse-release_test.sh` contributing 21 of the total assertions (matching row 21) alongside `check-codeql-dismissals_test.sh` (11), `check-migration-allowlist_test.sh` (9), `check-publish-order_test.sh` (9), `check-release-consistency_test.sh` (32), `check-workflow-suppressions_test.sh` (21), `check-workflow-triggers_test.sh` (11), `extract-changelog-section_test.sh` (16), `finalize-crate-changelogs_test.sh` (25), `finalize-release-body_test.sh` (84), `package-release-binaries_test.sh` (17), `publish-crates_test.sh` (54) — no mutation of the working tree in any suite. | ✅ GREEN |
| 24 | `cargo test -p paladin-web --lib openapi_matches_committed_baseline` (before regenerating the OpenAPI baseline, immediately after the Task 1 manifest bump was staged) | Exit 101. `assertion \`left == right\` failed: OpenAPI spec drifted from /workspace/crates/paladin-web/openapi.json. If the change is intentional, regenerate with: UPDATE_OPENAPI=1 cargo test -p paladin-web openapi_matches_committed_baseline` — `left` (generated) carries `"version": "0.10.1"`, the committed file still carried `"version": "0.10.0"`. `test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 225 filtered out; finished in 0.03s`. This test is not named in this plan's `files_modified` or `<read_first>` — a plan gap identified by the orchestrator's own tree-verified facts before dispatch, not discovered independently. | ✅ RED as required (D-08 stage 1) — proof the baseline needed regeneration |
| 25 | `UPDATE_OPENAPI=1 cargo test -p paladin-web openapi_matches_committed_baseline` then `cargo test -p paladin-web --lib openapi_matches_committed_baseline` (no env var) | First invocation regenerated `crates/paladin-web/openapi.json` and passed (`test result: ok. 1 passed; 0 failed`). `git diff -- crates/paladin-web/openapi.json` is exactly one line: `"version": "0.10.0"` → `"version": "0.10.1"`. Second invocation (no env var, against the regenerated file) also passed: `test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 225 filtered out; finished in 0.02s`. | ✅ GREEN — the row-24 red turning green, one-line baseline diff |
| 26 | `make check-release-consistency RELEASE_TAG=v0.10.1` | Exit 0. `✅ OK: 12 publishable package(s) checked, all match tag version '0.10.1' with a changelog section for it.` Plus the expected local-run note: `NOTE: the CI-conclusion clause (D-10) was not checked -- no --sha was supplied and this is not a GitHub Actions run.` (clauses 1-2 only, as this phase's own gate-re-seal section will supply `--sha` later). | ✅ GREEN |
| 27 | `make check-gates` (bundles `check-changelogs`, `check-crate-names`, `check-advisory-register`, `check-workflow-suppressions`, `check-workflow-triggers`, `check-codeql-dismissals`, `check-migration-allowlist`, `check-publish-order`) | Exit 0. All eight guards report OK: 11 per-crate changelogs present; 12 crate names match the allow-list; 11 advisory-register rows satisfied against 11 `deny.toml` + 5 `.cargo/audit.toml` ignore entries; 0 inline advisory-ignore suppressions across 7 workflow files/166 steps; 7/7 workflow trigger-policy rows pass; 6 CodeQL dismissals pass; the MIGRATION.md §9.2 ↔ semver-allowlist set-equality check reports both 15-row lists identical; `check-publish-order` reports `✅ OK: 12 crate(s) checked in publish order`. Also run standalone: `./scripts/check-publish-order.sh` → same `✅ OK` line, exit 0. | ✅ GREEN |
| 28 | `make clean-code` (fmt --check, clippy -D warnings, lint-shell, check, doc-check, check-api-examples) | Exit 0. `cargo fmt --check` clean; `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean (warmed once before Task 1's commit, since the version bump invalidated every crate's build fingerprint); `lint-shell` shellcheck-clean; `cargo check --workspace --all-targets --all-features` clean; doctests `test result: ok. 148 passed; 0 failed; 94 ignored` (paladin-ports) plus every other crate's doc-test group green, `✅ doc-check passed (ADR-0033)`; `check-api-examples` → `All 101 D-05 public API entry points carry a plural '# Examples' heading.` | ✅ GREEN |
| 29 | `make api-surface` (before and after: `git status --porcelain .project/current-exports.txt`) | Exit 0. `✅ API surface extracted to /tmp/tmp.RlnNhGcNGc (3959 items)` then `✅ API surface unchanged`. `git status --porcelain .project/current-exports.txt` empty both before and after the run — the committed baseline was not regenerated, matching this task's own requirement that a reported diff would be a stop-and-record finding, not a refresh; none occurred. | ✅ GREEN — baseline unmodified |
| 30 | `make test` (`cargo test --workspace --lib --bins`, FACT 4 — a full local sweep beyond this plan's own `<verify>` list, run because the next plan starts the hard-stop gate re-seal) | Exit 0. Fourteen `test result:` lines read directly from the log, all `ok`, 0 failed in every one: 982, 14, 599, 800, 96, 1, 56, 50, 232, 149, 0, 193, 310, 226 passed (3708 total tests passed workspace-wide, 0 failed, 0 measured, across every `--lib`/`--bins` target the command selects). `grep -c 'FAILED\|error\['` against the full log returns `0`. | ✅ GREEN |

---

## CI-run table

### Plan 37.1-08 — push #1 + release PR opened (2026-09-21)

**Push #1 (Task 1 step 1):** `git push origin release/v0.10.1`, hosted detached
(`target/37.1-08/push-1.{log,exit,pid}`). Exit `0`. Full pre-push hook stage ran and every hook
reported `Passed`: `check toml`, `check yaml`, `check for added large files`,
`check for merge conflicts`, `trim trailing whitespace`, `fix end of files`,
`Detect hardcoded secrets`, `shellcheck`, `cargo fmt (check)`,
`cargo clippy (workspace, -D warnings)`, `cargo build (workspace)`,
`cargo test (workspace lib/unit tests)`, `cargo check (doc code examples)`,
`validate doc YAML config snippets`, `check public API surface vs baseline`,
`check rustdoc zero-warning bar (ADR-0033) + doctests`, `check public API '# Examples' headings` —
`--no-verify` was never passed. Remote accepted: `[new branch] release/v0.10.1 -> release/v0.10.1`.

**Post-push verification:** `git fetch origin` then `git rev-parse origin/release/v0.10.1` =
`git rev-parse HEAD` = **`7749f161919f3d5f85f20fa36eba7ad800434f97`** (this is **H1**, the head the
pull request below was opened at). `git ls-remote --tags origin 'v0.10.1'` — empty, no tag pushed.

**Release PR (Task 1 steps 2-4):** `gh pr list --head release/v0.10.1 --base main --state all`
returned `[]` immediately before creation (no duplicate; also independently confirmed at this
plan's dispatch — see Provenance-adjacent orchestrator facts). `gh pr create --base main --head
release/v0.10.1 --title "release: v0.10.1 — v0.10.0 patch (publish-order fix, EPIPE fix, new
gate)" --body-file target/37.1-08/pr-body.md` succeeded:

- **PR #56:** https://github.com/DF3NDR/paladin-dev-env/pull/56
- **Base:** `main` / **Head:** `release/v0.10.1`
- **Head SHA (H1):** `7749f161919f3d5f85f20fa36eba7ad800434f97`
- Live read immediately after creation, `gh pr view 56 --json
  number,url,state,autoMergeRequest,headRefOid,baseRefName,headRefName`:
  `{"autoMergeRequest":null,"baseRefName":"main","headRefName":"release/v0.10.1","headRefOid":
  "7749f161919f3d5f85f20fa36eba7ad800434f97","number":56,"state":"OPEN","url":".../pull/56"}` —
  `state` OPEN, `autoMergeRequest` null, `headRefOid` == H1. **This single live read is the only
  query made against the remote to confirm existence — no poll, no retry, no wait.**
- Body verified (grep, not re-read from memory) to contain: `merge commit`, the phrase
  `37.1-CI-EVIDENCE`, `09-program-acceptance-audit`, the literal `13` (section number),
  `check-publish-order`, `create-or-reuse-release.sh`, `paladin-battalion/Cargo.toml`,
  `35404826303` (the prior failed run), all three published-crate names
  (`paladin-ai-core`/`paladin-ports`/`paladin-herald`), and the final line `🤖 Generated with
  [Claude Code](https://claude.com/claude-code)`.

**Workflow runs queued/in-progress on H1 at this single read** (`gh api
"repos/DF3NDR/paladin-dev-env/actions/runs?head_sha=7749f161919f3d5f85f20fa36eba7ad800434f97"`,
9 rows, none concluded — recorded as a snapshot, not polled to completion, per D-00i):

| Workflow | Run ID | Event | Status at read time |
|---|---|---|---|
| `ci.yml` | [35550912601](https://github.com/DF3NDR/paladin-dev-env/actions/runs/35550912601) | `push` | queued |
| `ci.yml` | [35550981869](https://github.com/DF3NDR/paladin-dev-env/actions/runs/35550981869) | `pull_request` | queued |
| `codeql.yml` | [35550912524](https://github.com/DF3NDR/paladin-dev-env/actions/runs/35550912524) | `push` | in_progress |
| `codeql.yml` | [35550981861](https://github.com/DF3NDR/paladin-dev-env/actions/runs/35550981861) | `pull_request` | queued |
| `pre-commit` | [35550912631](https://github.com/DF3NDR/paladin-dev-env/actions/runs/35550912631) | `push` | queued |
| `pre-commit` | [35550981888](https://github.com/DF3NDR/paladin-dev-env/actions/runs/35550981888) | `pull_request` | queued |
| `feature-flags.yml` | [35550912540](https://github.com/DF3NDR/paladin-dev-env/actions/runs/35550912540) | `push` | queued |
| `feature-flags.yml` | [35550981873](https://github.com/DF3NDR/paladin-dev-env/actions/runs/35550981873) | `pull_request` | queued |
| `Docs` | [35550981874](https://github.com/DF3NDR/paladin-dev-env/actions/runs/35550981874) | `pull_request` | queued |

**These H1 runs are expected to be cancelled, not failed, once push #2 lands** (below): the
workflow's own `concurrency` group (`ci-${{ github.head_ref || github.ref }}`,
`cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}`, `.github/workflows/ci.yml`) cancels
an in-flight run on the same ref when a new commit supersedes it. A cancelled H1 run is **not** a
red gate — plan 37.1-09 reads conclusions on the *final* pushed head, not H1. See ".continue-here.md"
and the dated note below for the full parent-pointer accounting this plan's own defect required.

No merge, no tag, no workflow dispatch, no `gh run rerun`, and no force-push occurred in this
task. No credential-shaped text appears in the PR body or in this record — the credential worked
on the first attempt, so no `credential hand-off` heading applies here.

### Dated note (2026-09-21) — the plan's self-referential-SHA verify defect, and the substitute checks this plan ran instead

Both of this plan's `<verify>` one-liners, as literally written, ask a committed file to contain
the hash of the very commit (or a descendant) that contains it — impossible by construction: a
git commit cannot embed its own hash, and "push again and record the new head" (Task 1 step 6)
would recurse forever if taken literally as the terminal condition. This plan did not fake it (no
uncommitted edit left in the tree to make a `grep` pass, no amend, no force-push, no loop).
Instead, per this plan's own dispatch instructions, it followed a **parent-pointer convention**:

- **Order of work, exactly as followed:** push #1 of H1 (`7749f161919f3d5f85f20fa36eba7ad800434f97`)
  → `gh pr create` (PR #56) → all remaining writing (this evidence section, `.continue-here.md`,
  `37.1-08-SUMMARY.md`, the final STATE.md/ROADMAP.md metadata) done as local commits → **one**
  final push. Nothing is committed after that final push.
- **This evidence entry and `.continue-here.md` both record:** the PR number (56) and URL, `H1`
  (the head the PR was opened at, above), `H_named` (the parent commit of this evidence commit —
  i.e. the last commit that existed at the moment this file was written, named explicitly in the
  commit-hash table `.continue-here.md` carries), and the plain statement that the **final pushed
  head** — the SHA whose checks must actually conclude — is the tip of
  `origin/release/v0.10.1` = the PR's `headRefOid` at that time, and it **cannot** be written
  inside this file because this file is part of that final commit's own tree. It is identified
  instead by three read-only commands, to be run by whoever resumes:
  1. `git fetch origin && git rev-parse origin/release/v0.10.1`
  2. `gh pr view 56 --json headRefOid --jq .headRefOid`
     (must equal the output of command 1)
  3. `git diff --name-only <H_named> origin/release/v0.10.1`
     (must list only `.planning/` paths — proving the final head differs from `H_named` by
     documentation only, not by any code change)
- **Substitute checks, run after the final push, in place of the two impossible greps** (recorded
  under "Final-push substitute checks" in `.continue-here.md`, not restated twice): remote tip ==
  local HEAD; PR `headRefOid` == local HEAD; `git diff --name-only <H_named>..HEAD` lists only
  `.planning/` paths; `git status --porcelain` empty. Every OTHER clause of both plan `<verify>`
  one-liners (the ones that do not depend on the self-referential grep) was run and read for real,
  exactly as written.
- **Why this satisfies the threat the impossible grep was trying to catch (T-37.1-34):** the risk
  named is a resume condition pointing a reader at the wrong head. The three read-only commands
  above make "which head is final" a checkable fact derived from the remote and the PR object —
  not a hash asserted inside a file that cannot contain it — which is a stronger guarantee than
  the literal grep would have provided even if it were constructible.

### Dated finding (2026-09-21) — push #2 rejected by the `Protect release branches` ruleset; the plan's "push again" assumption does not hold for this repository

This plan's own authority section explicitly permits pushing `release/v0.10.1` "more than once if
a later local commit moves the head", and Task 1 step 6 / Task 2 step 5 both instruct committing
further evidence/resume-file content on `release/v0.10.1` and pushing it. **A second direct push
was attempted and rejected — deterministically, not a flake — by a live repository ruleset this
plan's dispatch context did not name.**

Three commits were made locally after push #1 (Task 1's evidence commit `1a07a958`, Task 2's
resume-file commit `3dda866f`, and the final metadata commit `54858c7c` carrying this
`37.1-08-SUMMARY.md`/`STATE.md`/`ROADMAP.md`). The second push, hosted detached exactly as the
first (`target/37.1-08/push-2.{log,exit,pid}`), completed the full pre-push hook stage (every
hook `Passed` or `Skipped (no files to check)` — the `.planning/`-only diff correctly skipped the
API-surface/doctest/examples hooks) and was then rejected by the remote itself, verbatim:

```
remote: error: GH013: Repository rule violations found for refs/heads/release/v0.10.1.
remote: Review all repository rules at https://github.com/DF3NDR/paladin-dev-env/rules?ref=refs%2Fheads%2Frelease%2Fv0.10.1
remote:
remote: - Changes must be made through a pull request.
remote:
To https://github.com/DF3NDR/paladin-dev-env.git
 ! [remote rejected]   release/v0.10.1 -> release/v0.10.1 (push declined due to repository rule violations)
error: failed to push some refs to 'https://github.com/DF3NDR/paladin-dev-env.git'
```

Exit `1`. **This is a measured, deterministic result, not a not-measured DNS/5xx flake — it was
not retried.** The live ruleset was read (read-only, single query) to confirm the mechanism rather
than guess at it: `gh api /repos/DF3NDR/paladin-dev-env/rulesets/20868128` (`"Protect release
branches"`, target `refs/heads/release/*`) carries a `pull_request` rule (`required_approving_review_count:
0`, `allowed_merge_methods: ["merge","squash","rebase"]`) alongside `deletion` and
`non_fast_forward`, with **`"bypass_actors": []`** and **`"current_user_can_bypass": "never"`** —
no actor, including a repository administrator, may push directly to update an existing
`release/*` ref once created; every update must land through a merged pull request. Push #1
succeeded because it was a **ref creation** (the branch did not previously exist on the remote);
`docs/src/appendix/branch-protection.md`'s own words — "`protect-release-branches.json` follows
the trunk ruleset's posture — no bypass — since it also gates a **merge** ..., not a ref
creation" — describe exactly this asymmetry, though neither that document nor this plan's own
dispatch context flagged that the asymmetry would bite a same-branch evidence push mid-plan.

**Consequence, stated plainly and without working around it:** `origin/release/v0.10.1` remains at
**H1** (`7749f161919f3d5f85f20fa36eba7ad800434f97`) — unchanged by the failed push attempt, and
still exactly the head PR #56 was opened at and is still open at
(`gh pr view 56 --json state,headRefOid,autoMergeRequest` re-read after the failure: `state:
"OPEN"`, `headRefOid` still H1, `autoMergeRequest: null` — the PR itself is completely unaffected).
The three commits above (`1a07a958`, `3dda866f`, `54858c7c`) exist **only in this local
checkout**. This plan's own authority does not extend to a workaround: opening a second pull
request (into `release/v0.10.1`, from a new branch) would itself require pushing a new branch —
prohibited ("no push to any other branch") — and even if a PR could be opened, merging it is
prohibited outright, on any branch, under this plan's own prohibitions. No workaround was
attempted. **This is handed to the maintainer as a blocking checkpoint** (see this plan's own
final report) rather than routed around, matching the spirit of this plan's own D-07
credential-hand-off fallback even though the failure mode here is a branch ruleset, not a
credential.

**What this does NOT affect:** PR #56 itself, its body, its live-confirmed `OPEN`/`autoMergeRequest:
null` state, and the CI runs already queued/in-progress against H1 (recorded above) are all
completely unaffected — plan 37.1-09 can read conclusions against **H1** exactly as if this
plan's own Task 2 write had succeeded; the only shortfall is that this evidence file's fuller
account of Task 1/Task 2, the `.continue-here.md` resume file, and this plan's own SUMMARY.md/
STATE.md/ROADMAP.md updates are not yet visible on `origin/release/v0.10.1` — only in this local
checkout, pending the maintainer's decision on how they should land.

---

### Plan 37.1-09 — every workflow run on H1, the required-context tally, the publish-order proof and the coverage figure (2026-09-21)

**Head-SHA re-confirmation (Task 1 step 1), done first, before anything below was recorded.**
`git fetch origin && git rev-parse origin/release/v0.10.1` → `7749f161919f3d5f85f20fa36eba7ad800434f97`.
`gh pr view 56 --json headRefOid --jq .headRefOid` → `7749f161919f3d5f85f20fa36eba7ad800434f97` —
equal. Both equal the resume file's **H1**, exactly as `.continue-here.md`'s "LIVE STATE UPDATE
2026-09-21" section says they would (no push #2 landed, so there is no separate "final head" to
resolve — H1 is the only head, and it is the head every run below was queried against). Every row
below is therefore evidence for **H1**, not a different commit than the one reviewed.

**Every workflow run on H1** (`gh api "repos/DF3NDR/paladin-dev-env/actions/runs?head_sha=7749f161919f3d5f85f20fa36eba7ad800434f97"`,
9 rows, all `status: completed`, 0 in flight, 0 cancelled — the "expected cancellation" scenario
plan 37.1-08 recorded never applied, because no push #2 landed to trigger it):

| Workflow | Run ID (URL) | Event | Conclusion |
|---|---|---|---|
| `ci.yml` | [35550912601](https://github.com/DF3NDR/paladin-dev-env/actions/runs/35550912601) | `push` | `success` |
| `ci.yml` | [35550981869](https://github.com/DF3NDR/paladin-dev-env/actions/runs/35550981869) | `pull_request` | `success` |
| `codeql.yml` | [35550912524](https://github.com/DF3NDR/paladin-dev-env/actions/runs/35550912524) | `push` | `success` |
| `codeql.yml` | [35550981861](https://github.com/DF3NDR/paladin-dev-env/actions/runs/35550981861) | `pull_request` | `success` |
| `pre-commit` | [35550912631](https://github.com/DF3NDR/paladin-dev-env/actions/runs/35550912631) | `push` | `success` |
| `pre-commit` | [35550981888](https://github.com/DF3NDR/paladin-dev-env/actions/runs/35550981888) | `pull_request` | `success` |
| `feature-flags.yml` | [35550912540](https://github.com/DF3NDR/paladin-dev-env/actions/runs/35550912540) | `push` | `success` |
| `feature-flags.yml` | [35550981873](https://github.com/DF3NDR/paladin-dev-env/actions/runs/35550981873) | `pull_request` | `success` |
| `Docs` (`docs.yml`) | [35550981874](https://github.com/DF3NDR/paladin-dev-env/actions/runs/35550981874) | `pull_request` | `success` |

**Per-job conclusions for the runs carrying required checks**, read via `gh pr checks 56 --json
name,state,bucket,workflow` (124 rows total — every required and non-required context named in
`.github/workflows/ci.yml`/`codeql.yml`/`feature-flags.yml`/`docs.yml`/`pre-commit`, each appearing
once per triggering event for the workflows that run on both `push` and `pull_request`; this is a
per-check-run read, not the PR's aggregate `mergeable`/`state` rollup — the aggregate was never
consulted as evidence, per this plan's own prohibition). **124/124 `COMPLETED`; 118 `SUCCESS`, 6
`SKIPPED`, 0 `FAILURE`, 0 in flight:**

| Job (as it appears as a check) | Workflow | `push` bucket | `pull_request` bucket |
|---|---|---|---|
| `Coverage` | `ci.yml` | `pass`/SUCCESS | `pass`/SUCCESS |
| `License & Dependency Policy` | `ci.yml` | `pass`/SUCCESS | `pass`/SUCCESS |
| `CodeQL Analysis (Rust)` | `codeql.yml` | `pass`/SUCCESS | `pass`/SUCCESS |
| `pre-commit run --all-files` | `pre-commit` | `pass`/SUCCESS | `pass`/SUCCESS |
| every `Build & Test (<feature>)` (12), every `Crate Isolation (<crate>)` (9), `Code Quality`, `Security Audit`, `OSV Scanner`, `Workflow Lint`, `API Surface Tracking`, `Benchmark Compile Check`, `Integration Tests`, `Docker Integration Tests`, `Unit Tests (stable\|beta)`, `Example Muster (Feature Matrix)`, `Feature Matrix Summary`, `CLI Snapshot Tests`, `CLI Isolation (library without cli feature)` | `ci.yml`/`feature-flags.yml` | `pass`/SUCCESS | `pass`/SUCCESS |
| `Build MDBook` | `Docs` (`docs.yml`, `pull_request`-only trigger, no `push` row by design) | n/a | `pass`/SUCCESS |
| `End-to-End Tests` (conditional job, this PR's diff does not trigger it) | `ci.yml` | `skipping`/SKIPPED | `skipping`/SKIPPED |
| `Publish Dry Run` (`if: github.event_name == 'push' && github.ref == 'refs/heads/main'` — never runs on a release branch push or a PR, by the trigger condition itself, findings entry "`publish-dry-run` is pull-request-blind" above) | `ci.yml` | `skipping`/SKIPPED | `skipping`/SKIPPED |
| `Benchmark Regression Signal (Non-Blocking)` | `ci.yml` | `pass`/SUCCESS (push) | `skipping`/SKIPPED (pull_request) |
| `Deploy to GitHub Pages` | `Docs` | n/a | `skipping`/SKIPPED |
| `CodeQL` (advisory-only results check, not the `codeql.yml` workflow-run) | — | `pass`/SUCCESS | `pass`/SUCCESS |
| `Socket Security: Project Report` / `Socket Security: Pull Request Alerts` / `osv-scanner` (adjacent, non-required) | — | — | `pass`/SUCCESS |
| `Docker Build` / `Kubernetes Smoke Test` (adjacent, deliberately non-required per `docs/src/appendix/branch-protection.md`) | `ci.yml` | `pass`/SUCCESS | `pass`/SUCCESS |

**Required-context tally, computed from the live `main`-branch ruleset, not asserted.**
`gh api repos/DF3NDR/paladin-dev-env/rulesets/20868126 --jq '.rules[] | select(.type=="required_status_checks") | .parameters.required_status_checks[].context'`
returned **44 unique required-context names**, byte-identical to the 44 names
`docs/src/appendix/branch-protection.md` § "The required-check set" documents (compared
programmatically, `comm -23` between the two sorted lists: **zero divergence** — the live ruleset
and the doc agree exactly, so this tally names no discrepancy to record). Cross-referencing each of
the 44 required names against the 124-row `gh pr checks 56` output (`comm -23` again, live ruleset
list vs. the set of check names present): **zero required contexts are absent from the run list.**
Every one of the 44 appears, and every one reads `pass`/SUCCESS **except `End-to-End Tests`, which
reads `skipping`/SKIPPED** on both its `push` and `pull_request` rows — recorded here as **skipped,
not passed**, per this plan's own instruction, and named as the one required context that did not
execute (a conditional job this PR's diff did not trigger, matching Phase 37's own PR #55 record of
the identical shape). **No required context concluded `fail`.** `Docker Build` and `Kubernetes
Smoke Test` are confirmed, live, not among the 44 required names — consistent with the doc's own
"two jobs are deliberately excluded" statement.

**Publish-order gate proof — the step, by name, inside the License & Dependency Policy job's own
log for run `35550981869` (job `106185557981`, `pull_request` event, the run carrying the required
check).** Log fetched via `curl -sSL -H "Authorization: Bearer $(gh auth token)"
https://api.github.com/repos/DF3NDR/paladin-dev-env/actions/jobs/106185557981/logs` (the `gh api
.../logs` form returns empty for a finished job because it does not follow the redirect; this
`curl` form does). The step itself, read directly from the log:

```
##[group]Run ./scripts/check-publish-order.sh
./scripts/check-publish-order.sh
...
✅ OK: 12 crate(s) checked in publish order; every versioned workspace dependency (normal, dev and build) resolves to an earlier CRATES position, and CRATES exactly matches the cargo-metadata publishable set.
```

Step conclusion: success (part of the job's overall `success` conclusion recorded above). This is
recorded as its own row, separate from the job's conclusion, per this plan's own instruction: a
gate that exists in a file but did not execute on this pull request would reproduce the exact class
of failure this phase exists to fix. **It did execute, pre-merge, on this pull request, by name,
and printed the same `✅ OK: 12 crate(s)...` line the local sweep (`37.1-CI-EVIDENCE.md` Local sweep
rows 11, 27, 38) already recorded on the working tree** — the local and the CI proof describe the
same command, now confirmed to have actually run in the gating context, not merely to exist in a
workflow file. No credential-shaped text appears in the quoted log excerpt or anywhere in this
record; the log's own `GITHUB_TOKEN`/`DEFAULT_GITHUB_TOKEN` lines are pre-redacted by GitHub itself
(`***`), confirmed by inspection before any excerpt was copied here.

**Coverage job's conclusion and printed figure — read from the log, not expected.** Coverage job
(run `35550981869`, job `106185558049`, `pull_request` event): conclusion `success`. Figure read
directly from the job's own "Coverage summary" step output (log fetched the same way as the
License job's log above):

```
Lines:     111770/123587 = 90.44%
```

**90.44% clears the 82% workspace line-coverage floor (ADR-0006,
`.planning/decisions/0006-coverage-gate.md`)** — the single binding figure, by a margin of 8.44
percentage points. This is the one gate this whole plan exists to obtain a real reading of (D-00e):
this devcontainer has no Docker and could not measure it locally at any point in this phase's own
local sweep (§13's own "CI-attributed, not measured" line, and every `37.1-CI-EVIDENCE.md` row
before this one). The figure above is the number the job printed in its own log, not an expected or
carried-forward number.

**No required check concluded failure.** Task 1's conditional halt-and-record branch (D-08 stage 2)
does not apply — every one of the 44 required contexts reads `pass`/SUCCESS except the one
conditional job (`End-to-End Tests`) that reads `skipping`/SKIPPED, which is not a failure. No
workflow run was re-run, re-dispatched or cancelled by this task; every read above was a single
GET, not a poll.

**This commit (evidence only) is a local commit on `release/v0.10.1`, not pushed** — per this
plan's own push constraint (identical mechanism to plan 37.1-08's own push #2 rejection,
`GH013`/"Changes must be made through a pull request", ruleset `20868128`, `bypass_actors: []`,
`current_user_can_bypass: "never""). This commit does **not** move `origin/release/v0.10.1`'s head
and therefore does **not** trigger any new workflow run — plan 37.1-08's own note that "this commit
... will itself trigger a new run" does not hold in this state, because nothing here reaches the
remote. The local head after this task's commit is recorded in this plan's own SUMMARY.md and
final report; `origin/release/v0.10.1` and PR #56's `headRefOid` remain **H1** throughout this
plan, unchanged by anything committed here.

---

## Registry verification (D-00h)

*(No registry reads recorded by this plan — the sparse-index verification against `0.10.1` happens
after the tag is cut, in the post-tag wave this phase absorbs per D-04.)*

| Crate | Version | Checksum | `yanked` | Notes |
|-------|---------|----------|----------|-------|

---

## Findings carried forward (D-00d)

### 2026-09-19 — D-06 commit count is live, not a contract (corrects CONTEXT.md D-06)

CONTEXT.md's own D-06 text states: *"The 14 local-only `.planning/` commits on `feature/phase-33`
(`1bb94063..HEAD`) are cherry-picked onto it in order."* That figure was already stale when
RESEARCH.md re-measured it during this phase's own research pass (**16**, two more having landed
during the discuss-phase session that produced CONTEXT.md itself), and had moved again by planning
time (**19**, per this plan's own `must_haves.truths`). At this plan's Task 1 dispatch, live
re-measurement read **24**:
`git rev-list --count 1bb94063..refs/heads/feature/phase-33` → `24`, matching
`git status -sb`'s `feature/phase-33...origin/feature/phase-33 [ahead 24]` recorded independently
in `37-CI-EVIDENCE.md`'s own Pre-flight measurements section.

**The correction is not a new number — it is that no number is ever asserted as fact.** This
plan's `must_haves.truths` explicitly forbids hard-coding or asserting a commit count anywhere in
this phase's output; the range is resolved live from git at every dispatch and recorded only as
endpoints (`1bb94063` .. the captured tip of `feature/phase-33` at that dispatch). The 24 read
above is the reading Task 1 of this plan actually replayed — recorded here as a point-in-time
observation, not a contract any later plan or reader may rely on staying true.

### 2026-09-19 — `publish-dry-run` is pull-request-blind, not merely "green and blind" (corrects CONTEXT.md D-05)

CONTEXT.md's D-05 (the MILESTONES.md v0.10.0 entry shape) says the entry should tell "the two
defects and why gate row 6 was **green and blind**" — inheriting Phase 37's framing that the
existing `make publish-dry-run` / CI `publish-dry-run` job ran, passed, and was merely blind to
inter-crate publish *order* because `cargo publish --workspace --dry-run` resolves sibling crates
from a local overlay rather than the live registry.

RESEARCH.md's re-inspection of `.github/workflows/ci.yml` found this understates the gap. The
existing `publish-dry-run` CI job is gated
`if: github.event_name == 'push' && github.ref == 'refs/heads/main'` — it has **never run on a
pull request**, only after a merge to `main`. Pre-merge, on any PR (including the one that carried
the `v0.10.0` defect), it did not gate at all — it wasn't blind to ordering while running, it
simply never ran. This is worse than "green and blind" and is recorded here in full so that plan
37.1-03's CI-wiring choice for the new SC3 publish-order gate has a written basis: the new gate
must be wired as its own fast job/step that runs on `pull_request` as well as `push`, never by
loosening the existing `publish-dry-run` job's trigger to cover pull requests (that job is
expensive — `cargo publish --workspace --dry-run` — and broadening it is explicitly ruled out as a
way to satisfy this gap). CONTEXT.md D-05's "green and blind" phrase is retained verbatim
(amend-at-source, D-00d) — this entry is the dated correction sitting alongside it, not a rewrite.

### 2026-09-19 — Spec-less probe fallback, skipped visibly

No `-SPEC.md` exists for Phase 37.1, so both `## Edge Coverage` and `## Prohibitions` probe
sections are absent (`EDGE_ABSENT=1`, `PROHIB_ABSENT=1`). The fallback that derives predicates from
requirement TEXT could not run either: this phase's only requirement ID, `SHIP-06`, does not exist
in `REQUIREMENTS.md` yet — it is minted by this phase (plan 37.1-15, CONTEXT.md D-02). There was
therefore nothing to probe, and **no probe-derived edge or prohibition predicates were generated
for this phase.** Every plan's `must_haves.truths` across this phase is derived instead from the
six ROADMAP success criteria and the locked CONTEXT.md decisions, with maintainer-gated and
post-release truths carried as `verification: backstop` markers. Recorded here as a visible,
measured skip — not an omission discovered later.

### 2026-09-19 — The publish-order gate reproduces Phase 37's real diagnosis, not a synthetic stand-in

Plan 37.1-02's `scripts/check-publish-order.sh` (Local sweep rows 1-6, above) fails on a real
`git worktree` capture of commit `1d4a9724` naming exactly the two crates and the exact edge
direction `37-CI-EVIDENCE.md`'s **"D-16 read-only diagnosis #2 — `publish-crates` failed at
position 4; PARTIAL PUBLISH 3/12 (2026-09-19, ~00:30Z)"** already traced from the real failed
release run (`paladin-battalion` at `CRATES` position 4, forward dev-dependency edges on
`paladin-llm` at position 5 and `paladin-storage` at position 10). That diagnosis's own recorded
finding — "assert, per `CRATES` position, that every versioned workspace dependency INCLUDING
dev-dependencies appears earlier in the array" — is exactly what the new gate implements. This is
recorded here as the closing of that carried finding: the claim "this gate would have caught
`v0.10.0`" rests on a captured worktree of the real tagged tree, not a hand-written fixture
approximating it, and the gate's own regression harness (row 5) proves the red-on-defective /
green-on-fixed pair is stable and repeatable, not a one-off observation.

Also worth recording plainly: the gate still fails against the live `release/v0.10.1` working tree
at the end of this plan (row 6) — the `paladin-battalion` manifest defect itself is untouched by
this plan, by design. This plan's scope is the gate, proven red; plan 37.1-04 owns the fix, proven
by this same command turning green.

### 2026-09-19 — Publish-order gate wired into License & Dependency Policy, not Publish Dry Run (plan 37.1-03)

Plan 37.1-03 wired `scripts/check-publish-order.sh` (plan 37.1-02) into pre-merge CI. The **Publish
Dry Run** job's actual condition, quoted verbatim from `.github/workflows/ci.yml:2004`:

```yaml
if: github.event_name == 'push' && github.ref == 'refs/heads/main'
```

This confirms, at the wiring site itself, the finding recorded above
("`publish-dry-run` is pull-request-blind"): that job **never runs on a pull request**, only after
a merge to `main` — so a step added there would never have gated the PR that actually carried the
`v0.10.0` defect. Consequence stated plainly: pre-merge, that job does not gate at all; it is not
merely blind to publish order while running.

For this reason the new `Check publish order` step was added instead to the **License & Dependency
Policy** job (`.github/workflows/ci.yml`, job key `cargo-deny`, name `License & Dependency Policy`)
— a job with no job-level `if:` condition, running under the workflow's own broader triggers
(`push: branches: ['**']` and `pull_request: branches: [main, 'release/**']`), and already home to
six other offline `check-*.sh` guard-script steps plus `make test-shell-guards`. The gate is pure
`cargo metadata` + shell analysis (seconds, no build, no network), so it costs nothing extra riding
alongside those existing steps.

**Widening the Publish Dry Run job's trigger to cover pull requests was deliberately not done
here** — that job is expensive (`cargo publish --workspace --dry-run`) and its default-branch-only
scope is intentional; broadening it is explicitly out of this phase's scope (this plan's own
`must_haves.prohibitions`). The diff that added the new step touches no `on:` block and no line
containing `github.event_name` or `github.ref` anywhere in the file (verified:
`git diff .github/workflows/ci.yml` shows insertions only), and
`./scripts/check-workflow-triggers.sh` still exits 0 — the recorded trigger-policy table needed no
edit.

### 2026-09-19 — Both `plan:pre` capability detectors ran and neither fired

`api-coverage` reported `detected: false` — this phase integrates no external API or SDK; the
crates.io sparse-index reads this phase performs later are read-only registry probes, not an SDK
integration, so no `COVERAGE.md` is written and none is fabricated. `assumption-delta` reported
`detected: false` — no singular-to-plural, required-to-optional, or derived-to-chosen transition is
in scope anywhere in this phase's plans. Both are recorded here as measured negatives so a later
reader sees that the detectors ran and returned nothing, rather than inferring an omission.

### 2026-09-19 — Remaining versioned workspace dependency edges after the SC1 fix (plan 37.1-04, D-00d)

Live-derived, not quoted from RESEARCH.md or CONTEXT.md — `cargo metadata --format-version 1
--no-deps` was re-run at this plan's Task 3 dispatch (immediately after the Task 1 commit
`7fe4b178` and Task 2's proof) and cross-referenced against `scripts/publish-crates.sh`'s `CRATES`
array position for every publishable package (`.publish == null`, 12 crates). The count the plan's
own acceptance criteria asks for —
`[.packages[] | select(.publish == null) | .dependencies[] | select(.path != null and .req != "*")] | length`
— reads **37** at time of writing (normal + dev + build kinds combined, `paladin-battalion`'s two
former entries no longer among them: they were the only edges this fix removed, dropping the count
from what would have been 39).

**All 37 edges are correctly ordered today** — every one resolves to an earlier `CRATES` position
than its dependent, which is exactly what `./scripts/check-publish-order.sh`'s Exit 0 (row 11,
above) already proves as a single composite assertion; this entry is the per-edge enumeration
behind that one proof, not a new claim. 34 are ordinary (`kind: null`, i.e. "normal") dependency
edges — these are structurally self-ordering: a crate cannot compile without its normal
dependencies, so a valid publish order that lets every crate build at all already orders every
normal edge correctly by construction, and none of them is a candidate for the class of defect
this phase fixed. The **3 remaining *dev*-dependency edges** — the class `paladin-battalion`'s two
former entries belonged to, and the only class this fix's scope concerns — are:

| From (CRATES pos) | Kind | To (CRATES pos) | Ordered? |
|---|---|---|---|
| `paladin-herald` (3) | dev | `paladin-ports` (2) | yes (2 < 3) |
| `paladin-ai` (12, root package) | dev | `paladin-web` (7) | yes (7 < 12) |
| `paladin-ai` (12, root package) | dev | `paladin-eval` (11) | yes (11 < 12) |

This reproduces CONTEXT.md's "Orchestrator-verified tree facts" (`paladin-herald` → `paladin-ports`
and the root package → `paladin-eval`) and RESEARCH.md's Discretion Question 2 paragraph, with one
live correction recorded per D-00f (tree outranks document): RESEARCH.md's own text names the
root-to-`paladin-web` edge as "pos 6" — the live `CRATES` array (`scripts/publish-crates.sh`, sourced
via `PUBLISH_CRATES_LIB_ONLY=1`, same table reproduced in this file's Provenance-adjacent rows) puts
`paladin-web` at **position 7** (`paladin-memory` occupies 6). The edge itself and its correctness
(7 < 12) are unaffected — only the stated position number in RESEARCH.md's prose was off by one, and
it does not change any finding's conclusion. Amend-at-source (D-00d): RESEARCH.md is not edited;
this dated correction stands alongside it.

**None of these three is being touched in this phase.** They are correctly ordered today, exactly
as `paladin-battalion`'s two former entries were *not* — that was the whole defect. The workspace-
wide rule that would make every workspace dev-dependency path-only by policy (not just the two that
broke) is CONTEXT.md's own named deferred item: **"Path-only as a workspace-wide rule for all
workspace dev-dependencies (herald → ports, root → eval), and a lint enforcing it — release-tooling
hardening for v0.11.0"** (`37.1-CONTEXT.md` § Deferred Ideas). A wider manifest diff here, on a
release branch, for edges that are not broken, buys no defect coverage — `check-publish-order`
(SC3, plan 37.1-02/03) is what protects all 37 edges going forward, dev and normal alike, and it
would fail loudly on any future crate that reintroduces this exact shape, at any position.

**Corroborating observation: the publish order itself (`CRATES` in `scripts/publish-crates.sh`) was
not changed anywhere in this phase.** The manifest fix (drop `version =` from two entries) was the
smaller, lower-risk option — a two-line diff confined to one crate's `[dev-dependencies]` table —
versus reordering the twelve-crate array that the real, already-proven-in-production publish loop
walks (`v0.10.0`'s first three crates published successfully through it). RESEARCH.md's own
recommendation reached the same conclusion independently (§ Battalion Ordering Fix, "Primary
recommendation"), and this plan's own prohibition (`must_haves.prohibitions`, "MUST NOT reorder the
`CRATES` array") made it a hard constraint rather than a preference. `git diff --stat
scripts/publish-crates.sh` against this plan's own commits reads empty, confirmed at Task 1 and
unchanged through Task 3.

### 2026-09-20 — Script-fix sweep re-run against the current tree: three further early-closing-reader-under-pipefail instances, none fixed (plan 37.1-05, D-00d)

Re-run live against the current `release/v0.10.1` tree at this plan's Task 3 dispatch (immediately
after the Task 2 fix commit `1ce9d432`), not transcribed from RESEARCH.md's own "Script-fix sweep"
section — every file and line number below was read and confirmed directly against the tree, per
this task's own instruction. The search scope was the same class RESEARCH.md's sweep defined: a
pipeline whose reader (`head`, `grep -q`, or similar) can exit before its writer has finished
writing, under a shell that treats a failing pipeline element as fatal (`set -euo pipefail`).

| File:Line | Shape | Live pipe or here-string? | Risk assessment |
|---|---|---|---|
| `scripts/create-or-reuse-release.sh:79` (pre-fix; now removed by this plan's Task 2) | `printf "%s\n" "$raw" \| head -n1 \| tr -d '\r'` | Live pipe (variable → pipe) | **The SC2 fix target — no longer present in the tree.** `_cor_gh_call`'s status-line extraction is now pure bash parameter expansion (`${raw%%$'\n'*}` / `${status_line//$'\r'/}`); there is no subprocess and no pipe left for this race to act on. |
| `scripts/check-deprecations.sh:32` | `if echo "$OUTPUT" \| grep -q "warning:.*deprecated"; then` (under `set -euo pipefail`, confirmed at line 4) | Live pipe (variable → pipe) | **Same shape, same risk class as the SC2 defect.** `grep -q` exits as soon as it finds the first match; `$OUTPUT` is a full `cargo build --lib` output capture, plausibly large on a workspace this size. If the matching `warning:...deprecated` line appears early in the output, `echo` can receive `SIGPIPE` on its next write. Not touched by this plan — recorded as a finding for the deferred wider release-tooling audit. |
| `scripts/check-doc-examples.sh:142` | `rustfmt --edition 2021 "${tmp_rs}" 2>&1 \| head -10` (under `set -euo pipefail`, confirmed at line 20) | Live pipe (process → pipe) | Same shape. `rustfmt` output is a syntax-error dump — plausibly large for a malformed doc example, though rarely as large as a full compiler cascade. Recorded as a finding, not fixed. |
| `scripts/check-doc-examples.sh:164` | `cargo check --manifest-path "${crate_dir}/Cargo.toml" 2>&1 \| head -20` (same `set -euo pipefail` scope) | Live pipe (process → pipe) | **Same shape, plausibly the highest realistic trigger rate of the three.** This is a diagnostic-only re-invocation of `cargo check` *inside an already-failed branch* — the preceding `if ! cargo check ...; then` on the same block already decided pass/fail via its own exit code; this second invocation exists purely to print the failing diagnostic. `cargo check` compiler-error dumps for generic/trait-bound cascades routinely exceed 64 KB. A `SIGPIPE` here would abort the whole `check-doc-examples.sh` script under `pipefail`, rather than merely truncating the diagnostic output the reader is trying to show — turning a cosmetic "diagnostic got cut off" outcome into a hard script abort that could look like an unrelated CI infrastructure failure. This is the one of the three worth flagging as the priority candidate for the deferred audit, precisely because its trigger condition (a real compile failure with a large diagnostic) is exactly the condition under which the script is already in its unhappy path and the operator most needs the output to actually print. |
| `scripts/check-public-api-examples.sh:136` | `name="$(grep -oE "${name_pattern}" <<< "${rest}" \| head -1)"` (under `set -euo pipefail`, confirmed at line 29) | Partially — `<<<"${rest}"` is a here-string (safe, no live pipe on the input side), but `grep -oE`'s own stdout re-enters a live pipe feeding `head -1` | Same structural shape but low practical risk in this instance — `${rest}` is a single doc-comment/heading line and `grep -oE` extracts a short name substring from it, so the writer-side payload here is always small. Recorded as a finding for completeness, not because it is a realistic trigger. |
| `scripts/check-{codeql-dismissals,advisory-register,crate-names,workflow-triggers,changelogs,workflow-suppressions}.sh`, `scripts/finalize-crate-changelogs.sh`, `scripts/check-release-consistency.sh` — every `STATUS_LINE=$(head -n1 <<<"${REPORT}")`-shaped instance | Here-string into `head -n1` | Here-string (not a live pipe) | **Confirmed structurally safe, not vulnerable.** A here-string is fully materialized (bash writes it to a temp file or an internal buffer) before the reader ever starts reading, so there is no writer process left racing the reader's early exit — the exact mechanism this race class depends on is absent. This confirms CONTEXT.md's narrower literal-form claim was correct for every one of these sites. |

**Verdict on the original claim, stated in both halves, as this plan's `must_haves.truths` requires:**

- **The narrow claim is CONFIRMED.** `scripts/create-or-reuse-release.sh:79` (pre-fix) was the only
  file in `scripts/*.sh` using the exact literal `printf ... | head -n1 | tr -d '\r'` construct — a
  tree-wide `grep` for that literal shape found no second instance, before or after this plan's fix
  removed the one instance that existed.
- **The broader implication is OVERTURNED.** The narrow claim does *not* mean no other instance of
  the underlying early-closing-reader-under-`pipefail` race class exists. **Three further instances
  of that class exist in the current tree**, none of them exercised by this phase's SC2 and none
  fixed by it: `scripts/check-deprecations.sh:32`, `scripts/check-doc-examples.sh:142`,
  `scripts/check-doc-examples.sh:164`, and `scripts/check-public-api-examples.sh:136` (four line
  citations across three files, matching this plan's own `must_haves.truths` wording of "three other
  pipelines" — `check-doc-examples.sh` contributes two of the four citations).

**Deferred owner, and why none of the three is fixed here:** all three sites are recorded findings
assigned to `37.1-CONTEXT.md`'s own named deferred item — **"A wider first-run audit of the release
scripts (every `pipefail` pipeline with an early-closing reader, every path exercised for the first
time on tag day)"** (§ Deferred Ideas) — the same register RESEARCH.md's own sweep assigned them to.
Fixing any of the three here would widen this release branch's diff outside SC2's stated intent (one
function, one assignment, in one file) for a class of defect that has not yet caused an observed
production failure the way `create-or-reuse-release.sh:79` did. `check-doc-examples.sh:164` (the
diagnostic re-invocation inside an already-failed branch, fed by output that routinely exceeds the
buffer for cascading compiler errors) is named above as the one with the highest realistic trigger
rate among the three, and is the natural first item for whoever picks up the deferred audit.

`git diff --stat scripts/check-deprecations.sh scripts/check-doc-examples.sh
scripts/check-public-api-examples.sh` against this plan's own commits reads empty — nothing in any
of the three swept-but-unfixed files was touched.

### 2026-09-20 — Version bump to 0.10.1 (plan 37.1-06, SC4): three no-work findings and two carried documentation findings

Task 1 commit `88d174de`, Task 2 commit `5b73de30`. Every one of the fourteen manifests (root
`Cargo.toml`, the eleven publishable crates, `crates/doc-examples/Cargo.toml`) now reads `0.10.1`
in every version field; `Cargo.lock` was regenerated by a real `cargo check --workspace
--all-targets --all-features` invocation (13 workspace `[[package]]` entries moved, zero
third-party crate moved); twelve changelogs (root + eleven crates) carry a new `## [0.10.1]`
section above their previous `## [0.10.0]` section; `MIGRATION.md`'s title is byte-identical to
the base commit `cb73f26e`'s.

**Finding 1 — the semver allowlist carries no version field of any kind (RESEARCH.md's own
observation, re-confirmed against the live tree at this plan's dispatch, not merely quoted).**
Read `.cargo/semver-checks-allowlist.toml` in full: 18 `[[entry]]` blocks, each carrying exactly
five fields — `crate`, `lint`, `migration_row`, `requirement_id`, `justification` — and nothing
else. No entry has a `version` key, and no entry's value string contains a bare version number.
The file's only literal occurrences of "0.10.0" are inside two comment lines describing the
*original* v0.9.0→v0.10.0 minor-bump rationale ("the v0.10.0 minor bump's one sanctioned use of
semver-breaking permission"), which remain historically accurate for a v0.10.1 patch layered on
top of that minor bump and are therefore not edited. CONTEXT.md's SC4 wording ("allowlist rows
read 0.10.1") corresponds to nothing mechanical in this file; no task in this plan edits it, and
`git diff --stat cb73f26e -- .cargo/semver-checks-allowlist.toml` against the current tree is
empty, confirmed at Task 3.

**Finding 2 — `MIGRATION.md`'s title decision.** The title "Upgrading from v0.9.0 to v0.10.0"
names the milestone's feature scope, which a patch release does not change; rewriting it to say
"v0.10.1" would misrepresent a pipeline-only fix as a new feature milestone, and blindly
find-replacing every "v0.10.0" occurrence in the file's body would also rewrite historically
accurate statements about the v0.10.0 program's own close (Phase 29, `SHIP-01`). Per D-00d /
RESEARCH.md Pitfall 4 / threat T-37.1-22, the title is kept byte-identical (`head -1 MIGRATION.md`
== `git show cb73f26e:MIGRATION.md | head -1`, confirmed at Task 2 and re-confirmed at Task 3) and
a single dated addendum is appended directly under the existing scope note, naming `0.10.1` as the
version that actually shipped through the pipeline and pointing at `CHANGELOG.md`'s new section.

**Finding 3 — the non-publishable `doc-examples` manifest was bumped for tree consistency,
closing RESEARCH.md's Open Question 1.** `crates/doc-examples/Cargo.toml` (`paladin-doc-examples`,
`publish = false`) is excluded from every publishable-set derivation (`cargo metadata`'s
`.publish == null` filter, `check-release-consistency.sh`, `check-crate-names.sh`), so bumping it
carries zero release risk — no gate reads its version, and no registry ever sees it. This plan
bumps it anyway (own version field plus every sibling pin) because leaving it unbumped would make
it the one manifest in the entire tree still reading `0.10.0` after every other manifest read
`0.10.1` — a new, self-inflicted inconsistency this phase would otherwise introduce. Verified:
`grep -c 'version = "0.10.1"' crates/doc-examples/Cargo.toml` reads 9 (its own package version plus
eight sibling pins), and `cargo metadata`'s publishable-set derivation is unaffected (`doc-examples`
never appears in it, before or after).

**Plan-gap finding (not one of the three named above, found via the orchestrator's own
tree-verified facts, not independently) — the committed OpenAPI baseline is version-carrying and
test-guarded, and this plan's `files_modified` list omitted it.** `crates/paladin-web/src/
openapi.rs`'s `ApiDoc` sets `info.version` from `env!("CARGO_PKG_VERSION")` at build time, and the
lib test `openapi::tests::openapi_matches_committed_baseline` does a full-string `assert_eq!`
against the committed `crates/paladin-web/openapi.json`. This is the exact shape of the real
precedent commit `83d219f1` (the `0.10.0` bump), which touched the fourteen manifests, the
lockfile, AND one line of `openapi.json` — a precedent this plan's own text did not cite. Handled
as a deviation Rule 3 (blocking issue caused directly by this plan's own manifest-version change,
inside success criterion 4): observed red (Local sweep row 24) before touching the baseline,
regenerated via the exact command the assertion's own panic message names
(`UPDATE_OPENAPI=1 cargo test -p paladin-web openapi_matches_committed_baseline`), observed the
resulting diff was exactly the one `"version"` line (nothing more — a wider diff would itself have
been a stop-and-record finding, not something to commit), observed green afterward (Local sweep
row 25), and landed it in Task 1's own commit `88d174de` alongside the manifests and the lockfile,
matching `83d219f1`'s precedent shape exactly. See SUMMARY.md's "Deviations from Plan" for the
same account in the plan-execution record.

**Historical-prose finding (also not one of the three named above) — four pre-existing comment
lines in `crates/paladin-battalion/Cargo.toml` describing the real `v0.10.0` release-day defect
were left untouched, not swept.** Plan 37.1-04 added four `#`-prefixed comment lines (lines 61,
66, 82, 87 on the pre-Task-1 tree) narrating the actual `paladin-battalion` publish-order failure
in prose — two read "v0.10.0 release-day defect", one quotes the real `cargo publish` error text
`failed to select a version for the requirement paladin-llm = "^0.10.0"`. All four literally
contain the substring `0.10.0`. This plan's Task 1 acceptance criteria state `grep -c '0\.10\.0'
Cargo.toml crates/*/Cargo.toml` should report 0 for every manifest path, and the literal `<verify>`
automated command sums per-file counts across all fourteen manifests — a sum that is **not** zero
today because of these four lines (confirmed: `grep -c '^#' crates/paladin-battalion/Cargo.toml`
shows all four sit on comment-only lines). Per D-00d / RESEARCH.md Pitfall 4 / threat T-37.1-22 —
the same governing principle Finding 2 above applies to `MIGRATION.md` — these four lines are
historical prose describing a real past event at a specific past version number; rewriting "the
real v0.10.0 release-day defect" to read "v0.10.1" would state something false (the defect
happened publishing `v0.10.0`, not `v0.10.1`), and no acceptance criterion or prohibition in this
plan names these specific comment lines for editing. They are left untouched; the substantive
criteria this task actually needs to satisfy — every VERSION FIELD reads `0.10.1`
(`cargo metadata`'s `[.packages[] | select(.version != "0.10.1")] | length` == 0, confirmed 0 at
Task 1), the exact-equality pin kept its operator, the lockfile was regenerated and committed
alongside the manifests — all pass. See SUMMARY.md's "Deviations from Plan" for the same account.

**Carried finding 1 (re-confirmed against the live tree at this plan's Task 3 dispatch) — the
stale "eleven crates" count in `docs/src/contributing/development-setup.md`.** First recorded in
`37-CI-EVIDENCE.md` (Phase 37, plan 37-02). Line 651 of the live file still reads "the eleven
workspace crates to crates.io in dependency order" — the tree has had twelve publishable crates
since `paladin-eval`'s Phase 28 bootstrap (D-17), confirmed again here via the same
`cargo metadata` publishable-set count this plan's own Task 1 verification used (12). Deferred
owner, unchanged from Phase 37's own record: `37.1-CONTEXT.md` § Deferred Ideas, "Correcting the
stale 'eleven crates' count in `docs/src/contributing/development-setup.md` and elsewhere — docs-
currency fix for v0.11.0." Not fixed here — out of this plan's `files_modified` scope.

**Carried finding 2 (re-confirmed against the live tree at this plan's Task 3 dispatch) — the
previous release section's date predates its actual release.** First recorded in
`37-CI-EVIDENCE.md` (Phase 37, plan 37-02): `CHANGELOG.md`'s `[0.10.0]` heading carries the date
`2026-09-10`, earlier than the tag's actual cut/release date. Re-confirmed here unchanged — this
plan's Task 2 left the `[0.10.0]` heading's date exactly as it was (`## [0.10.0] - 2026-09-10`),
per its own instruction to leave that section's pre-existing text intact and append only a dated
superseded-line beneath the heading, not to correct the date. Neither `check-release-
consistency.sh` clause 2 nor `check-changelogs.sh` reads the date portion of a heading (clause 2's
regex anchors on the bracketed version only), so this remains a documentation-currency finding,
not a gate failure. Deferred owner: same `37.1-CONTEXT.md` § Deferred Ideas docs-currency item as
carried finding 1, v0.11.0.

**Local sweep evidence for this section:** rows 24-30 above (the OpenAPI red/green pair, the
manifest-version regeneration, the six gate commands and the full `make test` run).

---

---

Rows 31-39 below are this plan's (37.1-07) own record: the eight numbered gate-re-seal rows
required by SC4/D-09, measured on the candidate final head `fbdefec9c13f5d28d2127aa2da6da990a5285482`
(the dispatch head named in this plan's own `<sequential_execution>` block, re-confirmed via
`git rev-parse HEAD` immediately before Task 1 began and `git status --porcelain` empty at that
same moment), plus the house-sweep row (Task 2). **D-08 stage 2 is in force from this plan
onward: every gate below ran clean on the first attempt — no red was observed, so no halt was
required.** All eight numbered rows below ran with no Docker dependency, exactly as
`37.1-RESEARCH.md`'s "Phase 29 Gate Re-Seal" section predicted; the ninth row (coverage) is
recorded as CI-attributed per D-00e, not run here.

**Provenance for this row set:** `git diff --stat 5b73de30..fbdefec9` shows only four paths
changed — `.planning/ROADMAP.md`, `.planning/STATE.md`,
`37.1-06-SUMMARY.md` and `37.1-CI-EVIDENCE.md` itself — all under `.planning/`, none read by any
gate. The tree these eight gates measured is therefore identical, outside `.planning/`, to the
tree plan 37.1-06's own final commit (`896227ee`) produced. `git status --porcelain` was empty
before Task 1 began and remained empty after every gate in the table below (confirmed
individually after each run; the workspace publish dry run and `make clean-code` are the two
commands most likely to leave a stray tracked-file diff and both left the tree byte-identical).

| # | Gate | Command | SHA | Result | Evidence |
|---|------|---------|-----|--------|----------|
| 31 | `MIGRATION.md` no-TBD marker + §9.2 register ↔ semver-checks allowlist set-equality | `grep -c TBD MIGRATION.md` then `make check-migration-allowlist` | `fbdefec9` | `COUNT=0`; 15 `crate\|type` pairs in both `MIGRATION.md` §9.2 and `.cargo/semver-checks-allowlist.toml`, set-equal in both directions — unchanged from §11/§12's own 15 (this phase adds no new deliberate-breaking type). **PASS** | `target/37.1-07/cheap-gates.log` (gitignored scratch log; verbatim output quoted in this row) |
| 32 | `v0_9_config_boot` frozen compat target | `cargo test --features web-server --test v0_9_config_boot` | `fbdefec9` | `running 9 tests` / `test result: ok. 9 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out` — same 9-test count as §11/§12, non-zero. **PASS** | `target/37.1-07/configboot.log` |
| 33 | OpenAPI golden diff (path-restricted) | `cargo test -p paladin-web --test openapi_golden_v0_9` | `fbdefec9` | `running 7 tests` / `test result: ok. 7 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out` — same 7-test count as §11/§12, non-zero. **PASS** | `target/37.1-07/openapigolden.log` |
| 34 | `cargo semver-checks` for the eleven baselined crates vs `0.9.0` | `cargo semver-checks check-release --package <pkg> --default-features --baseline-version 0.9.0`, once per crate, list derived live from `cargo metadata --format-version 1 --no-deps \| jq -r '.packages[] \| select(.publish == null) \| .name' \| grep -v '^paladin-eval$'` | `fbdefec9` | List asserted at exactly **11** entries before looping (`paladin-ai`, `paladin-ai-core`, `paladin-battalion`, `paladin-content`, `paladin-herald`, `paladin-llm`, `paladin-memory`, `paladin-notifications`, `paladin-ports`, `paladin-storage`, `paladin-web`) — `paladin-eval` excluded, same reason as every prior re-seal: no published `0.9.0` baseline (it did not exist at `0.9.0`, Phase 28 bootstrap). **Per-crate result, individually, below — 11/11 PASS**, each `Checking <pkg> v0.9.0 -> v0.10.1 (major change)` / `0 checks: 0 pass, 254 skip` / `Summary no semver update required`, identical shape to §11/§12 at the `0.9.0` baseline: | `target/37.1-07/semver-<pkg>.log` per crate (11 files) |
| 35 | MSRV floor, toolchain 1.88 | `env RUSTUP_TOOLCHAIN=1.88 cargo check --workspace --all-features --all-targets` | `fbdefec9` | `Finished \`dev\` profile [unoptimized + debuginfo] target(s) in 2m 18s` — 0 errors, 0 warnings (`grep -c '^error'` / `grep -c '^warning'` both `0`). All fourteen `Checking <crate> v0.10.1` lines present, including `paladin-doc-examples`. **PASS** | `target/37.1-07/msrv.log` |
| 36 | `make publish-dry-run` — twelve crates, dependency order | `make publish-dry-run` | `fbdefec9` | Exit `0`. `release-check` prerequisite: 40/40 `test result:` lines `ok`, 0 failed; `cargo audit` reported the same 10 allowed pre-existing unmaintained/yanked-transitive warnings as the House sweep `make security` entry below (no new advisory); `cargo publish --workspace --dry-run` uploaded all **12** publishable crates in dependency order (`paladin-ai-core`, `paladin-ports`, `paladin-battalion`, `paladin-herald`, `paladin-llm`, `paladin-notifications`, `paladin-storage`, `paladin-web`, `paladin-content`, `paladin-eval`, `paladin-memory`, `paladin-ai`), each ending `warning: aborting upload due to dry run`; `paladin-doc-examples` (`publish = false`) correctly absent from the uploaded set (12 `Uploading` lines, not 13). `git status --porcelain` empty immediately after. **PASS (12/12, dependency order)** | `target/37.1-07/publish-dry-run.log` |
| 37 | `CHANGELOG.md` completeness | `make check-gates` (bundles `check-changelogs` and, since plan 37.1-03, `check-publish-order`) plus three hard-assertion greps | `fbdefec9` | `make check-gates` exit `0`, all eight guards (`check-changelogs`, `check-crate-names`, `check-advisory-register`, `check-workflow-suppressions`, `check-workflow-triggers`, `check-codeql-dismissals`, `check-migration-allowlist`, `check-publish-order`) individually OK. Hard-assertion greps: `## [Unreleased]` count `0`; `## [0.10.1]` count `1` (the released-version section this patch actually adds); `## [0.10.2]` count `0`; `## [0.11.0]` count `0` in `CHANGELOG.md`; `v0[.]11[.]0` count `0` in `MIGRATION.md`. **PASS** | `target/37.1-07/cheap-gates.log` |
| 38 (row 8, new gate) | Publish-order gate (SC3), invoked through the `make` target CI also invokes | `make check-publish-order` | `fbdefec9` | `✅ OK: 12 crate(s) checked in publish order; every versioned workspace dependency (normal, dev and build) resolves to an earlier CRATES position, and CRATES exactly matches the cargo-metadata publishable set.` Exit `0` — the same command `.github/workflows/ci.yml`'s License & Dependency Policy job invokes (plan 37.1-03), so the local and CI evidence describe the same command. **PASS** | `target/37.1-07/cheap-gates.log` |

**This is the full eight-row table (rows 31-38 above map 1:1 to gates 1-8) — all eight PASS on
`fbdefec9`.** The coverage floor is deliberately outside this table (D-00e; see House sweep below).

**Per-crate semver-checks detail (row 34), each run in isolation, wall-clock cold-ish (warm target/ from the MSRV/publish-dry-run runs earlier in this plan) build+check time recorded for completeness, not as a gate condition:**

| Crate | Baseline → Current | Checks | Wall time | Verdict |
|---|---|---|---|---|
| `paladin-ai` | `0.9.0` → `0.10.1` | 0 pass, 254 skip | 388.264s | PASS |
| `paladin-ai-core` | `0.9.0` → `0.10.1` | 0 pass, 254 skip | 57.167s | PASS |
| `paladin-battalion` | `0.9.0` → `0.10.1` | 0 pass, 254 skip | 73.707s | PASS |
| `paladin-content` | `0.9.0` → `0.10.1` | 0 pass, 254 skip | 84.314s | PASS |
| `paladin-herald` | `0.9.0` → `0.10.1` | 0 pass, 254 skip | 57.116s | PASS |
| `paladin-llm` | `0.9.0` → `0.10.1` | 0 pass, 254 skip | 83.830s | PASS |
| `paladin-memory` | `0.9.0` → `0.10.1` | 0 pass, 254 skip | 67.751s | PASS |
| `paladin-notifications` | `0.9.0` → `0.10.1` | 0 pass, 254 skip | 63.183s | PASS |
| `paladin-ports` | `0.9.0` → `0.10.1` | 0 pass, 254 skip | 61.159s | PASS |
| `paladin-storage` | `0.9.0` → `0.10.1` | 0 pass, 254 skip | 68.005s | PASS |
| `paladin-web` | `0.9.0` → `0.10.1` | 0 pass, 254 skip | 125.740s | PASS |
| `paladin-eval` (excluded) | n/a | n/a | n/a | **Excluded — no published `0.9.0` baseline; it did not exist until Phase 28's bootstrap (D-17). Same exclusion reason as §11 and §12.** |

**House sweep, three items that run here plus the one that cannot (Task 2):**

- **`make clean-code` (format-and-lint composite: `fmt`, `clippy -D warnings`, `lint-shell`,
  `check`, `doc-check`, `check-api-examples`)** — Exit `0`. `cargo fmt --all` made no change
  (tree already formatted, `git status --porcelain` empty afterward); `cargo clippy --workspace
  --all-targets --all-features -- -D warnings` clean; `lint-shell` shellcheck-clean; `cargo check
  --workspace --all-targets --all-features` clean; doctests green across every crate (e.g.
  `paladin-ports`: `test result: ok. 148 passed; 0 failed; 94 ignored`); `✅ doc-check passed
  (ADR-0033)`; `check-api-examples` → all 101 D-05 public API entry points still carry a plural
  `# Examples` heading. **PASS.** Evidence: `target/37.1-07/clean-code.log`.
- **`make security` (`cargo audit` + `cargo deny check`)** — Exit `0`. `cargo audit`: **10** allowed
  pre-existing warnings, unchanged in count and membership from §11/§12's own recorded 10/2:
  `dotenv` 0.15.0 (unmaintained, RUSTSEC-2021-0141), `fxhash` 0.2.1 (unmaintained,
  RUSTSEC-2025-0057), `number_prefix` 0.4.0 (unmaintained, RUSTSEC-2025-0119), `paste` 1.0.15
  (unmaintained, RUSTSEC-2024-0436), `rustls-pemfile` 2.2.0 (unmaintained, RUSTSEC-2025-0134),
  `smartstring` 1.0.1 (unmaintained, RUSTSEC-2026-0249), `event-listener` 5.4.1 (unsound,
  RUSTSEC-2026-0221), `scc` 2.4.0 (unsound), `chacha20` 0.10.0 (yanked, via `rand v0.10.1` →
  `rmcp`/`uuid`), `spin` 0.9.8 (yanked, via `flume` → `sqlx-sqlite` → `sqlx`/`paladin-storage`/
  `paladin-memory`) — all 10 satisfied against `.cargo/audit.toml`'s 5 ignore entries and
  `deny.toml`'s 11 ignore entries per `make check-gates`'s own `check-advisory-register` clause
  (row 37 above). `cargo deny check`: `advisories ok, bans ok, licenses ok, sources ok`.
  `git diff --stat -- Cargo.lock` empty — no new dependency-graph edge introduced by this
  docs-only plan. **PASS, all 10 warnings pre-existing and allowlisted.** Evidence:
  `target/37.1-07/security.log` (365.8 KB capture; summary line `advisories ok, bans ok, licenses
  ok, sources ok`).
- **`make api-surface`** — Exit `0`. `✅ API surface extracted to /tmp/tmp.9RmHYUuDKh (3959 items)` / `✅ API surface unchanged`. `git status --porcelain .project/current-exports.txt` empty both before and after — the baseline was **not** regenerated (this phase adds no public item; confirmed unchanged from every prior re-seal's own 3959 count). **PASS**
- **The 82% coverage floor — attributed, not measured.** This devcontainer has no Docker and no reachable Redis/MinIO endpoint, so `make coverage` (`cargo llvm-cov --workspace --features integration-tests,llm-all --lcov --output-path lcov.info --fail-under-lines 82`) cannot complete locally and was not attempted by this plan, exactly as §11/§12 record for the same structural reason (D-00e, ADR-0006). **The floor's figure is 82% (ADR-0006's binding number, single-sourced — see `.planning/decisions/0006-coverage-gate.md`), and the job that measures it is CI's `coverage` job**, read once `release/v0.10.1`'s PR is opened and that job completes (plan 37.1-08/09 own that push and the CI-run table). No local approximation of the coverage figure is recorded here as a gate result.
- **Standing statement on first-party static analysis (recorded here per Task 2 step 4, so a reader of this section does not read the `make security` PASS above as broader assurance than it is):** no static taint analysis of first-party Rust gates a merge in this repository. `cargo-audit` and `cargo-deny` (the `make security` bullet above) scan the dependency graph, not first-party source; `clippy -D warnings` (part of `make clean-code`, re-run clean on `fbdefec9` this plan, no drift) is a lint, not a SAST. `codeql.yml` runs Rust static analysis **advisory-only** — CodeQL was evaluated and disqualified as a required-check-grade Rust SAST at CodeQL `2.26.3` (2026-08-25, `.github/instructions/security.instructions.md`); it does not gate a merge. The manual credential-handling review remains the primary control for that class of code. This phase touches no credential-handling path (its own scope boundary: manifest, shell-script and gate-script changes only, no file under `src/` or `crates/*/src/`), so no additional manual review beyond the standing one was triggered.

**Findings (Task 2, dated):** none new. This plan's own scope (docs, evidence, gate re-seal) introduces no new dependency-graph edge, no new public item, and no new credential-handling surface — every house-sweep item above reproduces §11/§12's own recorded shape at the patch version, with zero drift in warning count or membership.

---

### Plan 37.1-10 — read-only verification that the ticked box is present on the remote at PR #56's head (2026-09-21)

**This section records a read-only verification. No commit was made by this plan (see this plan's
own SUMMARY and `.continue-here.md`); the SHA cited throughout is the tick commit the maintainer
made, not any commit of this plan's own.**

**Remote ref resolved, before anything else was read:**

```
git fetch origin
git rev-parse origin/release/v0.10.1  -> 529e70782168739c003582f3d9db1fbf02fd9614
gh pr view 56 --json headRefOid --jq .headRefOid -> 529e70782168739c003582f3d9db1fbf02fd9614
git rev-parse HEAD -> 529e70782168739c003582f3d9db1fbf02fd9614
```

All three agree: **`529e70782168739c003582f3d9db1fbf02fd9614`** is the remote branch tip, PR #56's
`headRefOid`, and (at this plan's dispatch) the local checkout's own HEAD — the maintainer's tick
was already the tip when this plan started, no push was needed or performed by this plan.

**The audit file's content was read at that remote commit via `git show <sha>:<path>`, not from the
working tree** (the working tree happens to be checked out to the same commit at dispatch, but the
read below used the explicit remote-content form regardless, per this plan's own prohibition
against accepting a working-tree reading as proof):

```
git show 529e70782168739c003582f3d9db1fbf02fd9614:.project/v0.10.0/09-program-acceptance-audit.md \
  | awk '/^## 13\./,0' | grep -n '^\- \['
-> 121:- [x] **The `v0.10.1` tag may be cut** — evidence: this section (§13) in full, plus
```

Full line, read from the remote content (`.project/v0.10.0/09-program-acceptance-audit.md`, line
1747 in the whole-file numbering — the exact location `37.1-07-SUMMARY.md` recorded the box at when
it was minted unticked):

```
- [x] **The `v0.10.1` tag may be cut** — evidence: this section (§13) in full, plus
  `.planning/phases/37.1-v0-10-1-patch-release/37.1-CI-EVIDENCE.md`'s complete Local sweep table
  ...
```

**Ticked-box count, remote content vs. base commit `1d4a9724`:**

```
git show 1d4a9724:.project/v0.10.0/09-program-acceptance-audit.md | grep -c '^- \[x\]' -> 0  (BASE)
git show 529e7078...:.project/v0.10.0/09-program-acceptance-audit.md | grep -c '^- \[x\]' -> 1  (REMOTE)
```

`1 = 0 + 1` — exactly one box became ticked. No other box in the file changed state.

**Tick commit identity, read directly from the remote commit object:**

```
SHA:            529e70782168739c003582f3d9db1fbf02fd9614
Author:         Am0rfu5 <1178902+Am0rfu5@users.noreply.github.com>
Committer:      Am0rfu5 <1178902+Am0rfu5@users.noreply.github.com>
Date:           2026-09-21T11:36:54+00:00
Subject:        docs(37.1-09): maintainer sign-off — the v0.10.1 tag may be cut
Body:           "Section 13 reviewed in full with PR #56's CI evidence: 124 checks, 0 failed;
                coverage 90.44% vs the 82% floor; publish-order gate proven to have executed
                pre-merge. This sign-off is mine alone."
Diffstat:       .project/v0.10.0/09-program-acceptance-audit.md | 2 +-, 1 insertion(+), 1 deletion(-)
Diff:           exactly `- [ ]` -> `- [x]` on the section-13 box line; nothing else on the line,
                and no other line in the file, changed.
```

This is **the maintainer's own commit, not the agent's**: this git host's committer identity is the
same `Am0rfu5` string on every commit in this repository (there is no separate git identity that
distinguishes an agent-authored commit from the maintainer's own, so identity alone cannot make
that distinction) — what does distinguish it is (a) the orchestrator's own dispatch-time account
that the maintainer replied at plan 37.1-09's checkpoint and made this commit themselves, outside
any agent session, and (b) a corroborating timestamp gap: the previous commit on this branch
(`23ba8478`, `2026-09-21T10:13:09Z`, the last of plan 37.1-09's own automated metadata commits) sits
**1h23m before** this tick commit (`11:36:54Z`) — a gap consistent with a separate human session
between plan 37.1-09's checkpoint returning and this plan's dispatch, not a commit produced in the
same automated batch. Recorded honestly rather than asserted as a stronger proof than git metadata
alone provides.

**The tick commit is confirmed to be the tip of the remote release branch and PR #56's head** — the
three-way equality (`origin/release/v0.10.1` = PR #56 `headRefOid` = the tick SHA) shown in the
first block above is exactly that confirmation; nothing on the remote sits ahead of the tick.

**No commit authored during this phase changed a sign-off box's state, other than the tick commit
itself.** Every commit in the range `1d4a9724..529e70782168739c003582f3d9db1fbf02fd9614` was
inspected individually for lines matching `^[+-]- \[[x ]\]` against
`.project/v0.10.0/09-program-acceptance-audit.md`. Exactly two commits touch such a line:

- `d73c242b` (plan 37.1-07, "append corpus audit section 13 with a fresh unticked sign-off box") —
  adds a **new** line `+- [ ] **The `v0.10.1` tag may be cut** ...`. This is the append-only
  creation of a fresh, unticked box (D-09's own instruction — section 13 mints its own box rather
  than reusing section 12's), not a state change of an existing box. No prior line is removed or
  toggled by this commit.
- `529e7078` (this section's subject, the maintainer's tick) — the only commit in the range that
  performs an actual `- [ ]` -> `- [x]` transition, on the line `d73c242b` had created.

No other commit in the 68-commit range touches a `- [ ]`/`- [x]` line in this file at all. This
confirms the mitigation for T-37.1-43 (Repudiation: an agent commit silently changing a box's
state) holds: the only toggle in the entire phase is the maintainer's own commit.

**No tag exists on `origin` for this version** (`git ls-remote --tags origin 'v0.10.1'` — empty).
**No merge, no tag, no `gh run watch`, no polling loop and no watcher process were performed by this
plan** — every read above was a single, read-only query.

**Result: the box is ticked at the remote commit, the tick sits at the tip, exactly one box changed
state, and the tick is the maintainer's own commit. Task 1's stop condition (box not ticked, tick
not at tip, or ticked-box count moved by anything other than exactly one) was NOT triggered — this
plan proceeds to Task 2.**


### 2026-09-21 — RED required check on the tick commit: `API Surface Tracking` (D-08 stage 2: recorded, nothing fixed)

Recorded by the orchestrator after the maintainer reported the failure. Left UNCOMMITTED with plan
37.1-10's other working-tree files so the tick commit `529e7078` stays the branch's last commit.

- **Head:** `529e70782168739c003582f3d9db1fbf02fd9614` (the maintainer's tick commit; PR #56 head).
- **Failing jobs:** `API Surface Tracking`, step `Check API surface changes`, exit 1, in BOTH ci.yml runs on
  that head: pull_request run `35609231646` job `106363909349`, push run `35609226028` job `106363869994`.
  Command: `./scripts/check-api-surface.sh .project/current-exports.txt`.
- **Required:** yes. `API Surface Tracking` is one of `main`'s 44 required contexts (ruleset `20868126`),
  so PR #56 cannot merge while it is red.
- **Same job on the previous head was green:** `7749f161`, run `35550981869`, job `106185557733`,
  `success`, started 2026-09-21T01:32:14Z. `git diff 7749f161..529e7078` touches only `.planning/**` and one
  character of `.project/v0.10.0/09-program-acceptance-audit.md`. No Rust source differs.
- **What changed is the toolchain, not the code.** The job runs `rustup toolchain install nightly`
  (ci.yml line 228, floating). Green job: `1.100.0-nightly (feaadeeac 2026-09-19)`, "latest update on
  2026-09-20". Red job: `1.100.0-nightly (bba531001 2026-09-20)`, "latest update on 2026-09-21".
  `cargo-public-api v0.52.0` in both.
- **Diff shape:** 606 lines removed, 606 added, and all 606 added lines render a return type as `Self`
  where the baseline has the fully qualified path. Verbatim example:
  `-pub fn paladin::...::InProcessArsenal::clone(&self) -> paladin::application::services::arsenal::in_process_arsenal::InProcessArsenal`
  `+pub fn paladin::...::InProcessArsenal::clone(&self) -> Self`
  Zero added lines lack `Self`. No public item was added, removed or changed; this is a rustdoc
  rendering change in the new nightly.
- **Not a flake.** Every run from now on fetches the new nightly, so a re-run reproduces the failure.
  No run was re-run, re-dispatched or cancelled. The local pre-push gate still passes only because the
  local nightly is `5db7f4be8 2026-09-01`.
- **Scope:** not specific to this release. Any pull request against `main` now fails this required check.
- **Status:** phase halted. Nothing fixed. Returning to the build wave is the maintainer's call, and by
  D-08 the re-seal then restarts from the top on the new head.
