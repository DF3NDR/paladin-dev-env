# Phase 37.1: v0.10.1 Patch Release - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-19
**Phase:** 37.1-v0.10.1 Patch Release
**Areas discussed:** Orphans, SHIP ID & the record; Branch, PR & red-gate policy
**Areas offered and not selected:** Battalion ordering fix; Ordering gate & rehearsal

---

## Area selection

| Area offered | Selected |
|--------------|----------|
| Battalion ordering fix — path-only dev-deps, re-order `CRATES`, or both; workspace-wide rule? | |
| Ordering gate & rehearsal — static assertion vs live-index rehearsal; red on `1d4a9724`; wider script audit | |
| Branch, PR & red-gate policy — new branch and the 14 local commits; one PR or two; does D-14 hold for a code phase | ✓ |
| Orphans, SHIP ID & the record — yank or leave; SHIP-05 vs SHIP-06; the `v0.10.0` GitHub Release; Phase 37's open plans | ✓ |

---

## Orphans, SHIP ID & the record

### Q1 — What happens to the three orphaned 0.10.0 versions?

| Option | Description | Selected |
|--------|-------------|----------|
| Yank after 0.10.1 verifies (Recommended) | Maintainer yanks all three only after the sparse index shows all twelve at 0.10.1 — never a window with no resolvable 0.10.x; one yank-register row per crate | ✓ |
| Leave them, record why | No yank; not defective, Cargo prefers 0.10.1 anyway; dated disposition paragraph | |
| Yank before the 0.10.1 tag | Clean the registry first; opens a window with no non-yanked 0.10.x if the 0.10.1 run also goes red | |

**User's choice:** Yank after 0.10.1 verifies (Recommended)
**Notes:** None. → D-01.

### Q2 — How should the requirement reflect that 0.10.1 is what ships?

| Option | Description | Selected |
|--------|-------------|----------|
| Mint SHIP-06, close SHIP-05 honestly (Recommended) | SHIP-06 = "v0.10.1 is released" mapped to 37.1; SHIP-05 keeps its text plus a dated superseded note and is never ticked | ✓ |
| Amend SHIP-05 at source | One requirement; dated addition restates the version; ticked when 0.10.1 verifies | |
| Both: amend SHIP-05 AND mint SHIP-06 | SHIP-05 narrowed to SC1-SC3 and ticked; SHIP-06 carries the registry + record | |

**User's choice:** Mint SHIP-06, close SHIP-05 honestly (Recommended)
**Notes:** None. → D-02.

### Q3 — What should a visitor to the Releases page see for v0.10.0?

| Option | Description | Selected |
|--------|-------------|----------|
| Keep it, banner it, mark pre-release (Recommended) | Dated notice prepended; flipped to pre-release so v0.10.1 is unambiguously Latest; tag untouched | ✓ |
| Keep it, banner only | Same notice, left as a normal release | |
| Delete the Release object, keep the tag | Cleaner page; removes the public explanation of the three 0.10.0 crates | |

**User's choice:** Keep it, banner it, mark pre-release (Recommended)
**Notes:** None. → D-03.

### Q4 — Where does Phase 37's un-run post-tag work end up?

| Option | Description | Selected |
|--------|-------------|----------|
| 37.1 absorbs the post-tag wave (Recommended) | 37.1 does registry verification, MILESTONES.md and the single close PR; 37-09..37-11 marked superseded; 37-08 SUMMARY ends at the D-16 stop | ✓ |
| Re-plan 37-09..37-11 after 37.1 ships | What the ROADMAP draft said; second planning round, two phases open at once | |
| Split: 37.1 verifies registry, 37 writes the record | 37-11 alone re-planned for MILESTONES.md and the close PR | |

**User's choice:** 37.1 absorbs the post-tag wave (Recommended)
**Notes:** This supersedes the ROADMAP Phase 37.1 `**Depends on**` sentence about re-planning Phase 37's plans. → D-04, D-05.

**Continue check:** "Next area".

---

## Branch, PR & red-gate policy

### Q1 — Where does the work live, and how do the 14 local-only commits get there?

| Option | Description | Selected |
|--------|-------------|----------|
| New branch off origin/main, cherry-pick (Recommended) | `release/v0.10.1` from `1d4a9724`; cherry-pick the 14 docs commits; old→new SHA map recorded; `feature/phase-33` retired | ✓ |
| New branch off origin/main, merge the old branch in | Conflict-free merge keeps every SHA reachable; one extra merge commit | |
| Keep working on feature/phase-33 | Merge main in and carry on; branch name keeps lying | |

**User's choice:** New branch off origin/main, cherry-pick (Recommended)
**Notes:** None. → D-06.

### Q2 — How many PRs carry the fixes and the bump?

| Option | Description | Selected |
|--------|-------------|----------|
| One release PR (Recommended) | Fixes, gate, tests, bump, changelogs → one PR → one merge commit → the tag | ✓ |
| Fix PR first, then a bump PR | Gate proven on main at 0.10.0 before a version is spent; two merges, two CI cycles | |

**User's choice:** One release PR (Recommended)
**Notes:** None. → D-07.

### Q3 — How does Phase 37's D-14 "fix nothing" rule apply to a phase that ships fixes?

| Option | Description | Selected |
|--------|-------------|----------|
| Two stages: build freely, then D-14 from the re-seal on (Recommended) | TDD inside the six SCs during the build wave; from the re-seal on, any red is a recorded stop | ✓ |
| D-14 everywhere, scoped exceptions only | Only the files the three defects require; any other red is stop-and-ask | |
| Bounded fix set throughout | Phase 29 D-12 style pre-listed small fixes even during the re-seal | |

**User's choice:** Two stages: build freely, then D-14 from the re-seal on (Recommended)
**Notes:** None. → D-08.

### Q4 — What does the v0.10.1 sign-off look like?

| Option | Description | Selected |
|--------|-------------|----------|
| New dated section + new box, hard merge gate (Recommended) | `## 13.` appended to the corpus audit, eight-row gate table, fresh "The v0.10.1 tag may be cut" box; agent verifies the tick on origin before the merge hand-off | ✓ |
| Same, but sign-off lives in 37.1's own directory | `37.1-SIGNOFF.md`; corpus audit gets a pointer only | |
| PR approval is the sign-off | Approving review after green CI; review ID in evidence | |

**User's choice:** New dated section + new box, hard merge gate (Recommended)
**Notes:** Orchestrator correction recorded after the answer: the question text said the §11 `v0.10.0` box "has been used". Verified against `origin/main`: that box is still **unticked** (Phase 37's sign-off was given in-session and recorded as a process deviation). The decision is unaffected — §11 and §12 are not edited, §13 is appended — and CONTEXT.md states the true box state. → D-09, D-10.

**Continue check:** "I'm ready for context".

---

## Claude's Discretion

Areas not selected, left to discretion with defaults stated to the maintainer at the close of discussion and accepted:

- Battalion ordering fix — default path-only dev-dependencies for the two battalion edges; `CRATES` order left alone unless research shows a reason.
- Whether path-only becomes a workspace-wide rule — default no, this phase; recorded as a finding.
- Ordering gate depth — default a static per-position assertion including dev-dependencies, via the `PUBLISH_CRATES_LIB_ONLY` seam and `cargo metadata`; no live-index rehearsal.
- How the gate is proven red on the `1d4a9724` manifests.
- The form of the line-79 replacement and its >64 KiB regression test; a recorded sweep for sibling `pipefail` hazards.
- CI wiring and script naming for the gate; changelog wording; plan count and wave layout; re-establishing the final commit if `main` moves.

## Deferred Ideas

- Path-only as a workspace-wide dev-dependency rule, plus a lint enforcing it (v0.11.0 release-tooling).
- `release.yml` dry-runnable against a bare SHA before a tag exists (carried from Phase 37).
- A wider first-run audit of the release scripts.
- A consumer smoke test against the published facade (carried from Phase 37).
- Correcting the stale "eleven crates" docs count (v0.11.0 docs currency).
- Carried unchanged from Phase 36.1 / 37: `--all-features` tests in CI; the eight crate-level rustdoc allows; `doc-coverage-report.md`; the Phase 25 / 28 register question.
- Reviewed todos, not folded: local `make coverage` reproduction (Phase 36.1 D-23); RustFS evaluation (Phase 36.1 D-24).
