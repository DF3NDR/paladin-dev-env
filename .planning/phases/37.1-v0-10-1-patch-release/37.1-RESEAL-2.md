# Phase 37.1 — Re-seal #2 (post API-surface-nightly-pin) — Record

**This file is deliberately named so GSD tooling's `*-PLAN.md`/`*-SUMMARY.md` globs do not index
it.** It is a repeat of plan `37.1-07`'s three tasks (see `37.1-07-PLAN.md` and
`37.1-07-SUMMARY.md`), re-run in full on a new candidate head, per the `<reality_delta>` in this
dispatch's own objective.

**Measured head:** `d2f1a81131fa8d504065cceed412909c06143b95` (`release/v0.10.1`'s tip; branch
confirmed, `git status --porcelain` empty, both re-confirmed before the first gate).

**Why this re-seal exists:** re-seal #1 (plan 37.1-07) ran green on `fbdefec9`; PR #56 opened, went
green (124 checks, 0 failed, coverage 90.44%), and the maintainer ticked section 13's box on commit
`529e7078`. The required `API Surface Tracking` check then went **red** on that same tick commit:
CI's toolchain-install step for that job (`rustup toolchain install nightly`, unpinned) picked up
the 2026-09-21 nightly (`bba531001`), which renders derived return types as `-> Self` instead of
the fully-qualified path the committed baseline carries — a 606-line diff (303 real changes) with
**zero** public item actually added, removed or changed. The maintainer's verbatim reply was
**"Pin it."** — authorization under D-08 to return to the build wave. Three build-wave commits
followed the finding: `b13a8553` (recorded the red check), `d4dc0e96` (a failing regression
harness, red 10/15 against the unpinned script), `d2f1a811` (the fix — `PUBLIC_API_TOOLCHAIN:
nightly-2026-09-20` pinned in both `.github/workflows/ci.yml` and `scripts/extract-public-api.sh`,
plus a `CHANGELOG.md` Fixed bullet; harness green 15/15, proven against real toolchains:
`nightly-2026-09-20` → exit 0 unchanged; `nightly-2026-09-21` → exit 1, 303 lines, all `Self`).

Per D-08, the re-seal restarts from the top on this new head rather than resuming from the red row
— this is that full restart.

**What changed since re-seal #1's own measured head, gate-readable files only** (`git diff --stat
fbdefec9..d2f1a811 -- . ':!.planning' ':!.project'`):

```
 .github/workflows/ci.yml                 |  12 ++-
 CHANGELOG.md                             |   8 ++
 scripts/extract-public-api.sh            |  33 +++++-
 tests/scripts/extract-public-api_test.sh | 174 +++++++++++++++++++++++++++++++
 4 files changed, 223 insertions(+), 4 deletions(-)
```

Four files — the CI-workflow pin, its script-side counterpart, the new regression harness, and the
changelog bullet documenting the fix. No public-surface source file (`src/`, `crates/*/src/`) and
no manifest was touched.

---

## What ran

### Task 1 — eight numbered gates (commit `b55b337b`)

All eight gates re-run from the top on `d2f1a811`, matching plan `37.1-07`'s Task 1 exactly:

| # | Gate | Command | Result |
|---|------|---------|--------|
| 1 | MIGRATION.md no-TBD + allowlist set-equality | `grep -c TBD MIGRATION.md` + `make check-migration-allowlist` | PASS — count 0; 15 pairs set-equal |
| 2 | `v0_9_config_boot` | `cargo test --features web-server --test v0_9_config_boot` | PASS — 9 passed, 0 failed |
| 3 | `openapi_golden_v0_9` | `cargo test -p paladin-web --test openapi_golden_v0_9` | PASS — 7 passed, 0 failed |
| 4 | `cargo semver-checks` (11 crates vs 0.9.0) | per-crate, list derived live via `cargo metadata` minus `paladin-eval` | PASS — 11/11, list asserted at exactly 11 before looping |
| 5 | MSRV 1.88 | `env RUSTUP_TOOLCHAIN=1.88 cargo check --workspace --all-features --all-targets` | PASS — 0 errors, 0 warnings, 1m 27s |
| 6 | `make publish-dry-run` | `make publish-dry-run` | PASS — 40/40 test-result lines ok, 12/12 crates uploaded in dependency order |
| 7 | Changelog completeness | `make check-gates` + hard-assertion greps | PASS — Unreleased=0, 0.10.1=1, 0.10.2=0, 0.11.0=0 |
| 8 | Publish-order gate | `make check-publish-order` | PASS — `✅ OK: 12 crate(s) checked in publish order` |

No red gate anywhere. D-08 stage 2's hard-stop was never invoked.

### Task 2 — house sweep (commit `4e8c022c`)

| Item | Result |
|------|--------|
| `make clean-code` | PASS — fmt/clippy/lint-shell/check/doc-check/check-api-examples all clean |
| `make security` | PASS — `cargo audit` 10 allowed pre-existing warnings (unchanged membership: `dotenv`, `fxhash`, `number_prefix`, `paste`, `rustls-pemfile`, `smartstring`, `event-listener`, `scc`, `chacha20`, `spin`); `cargo deny check` — `advisories ok, bans ok, licenses ok, sources ok` (pre-existing non-blocking `warning[duplicate]` multiple-version notices observed, `Cargo.lock` unchanged, not a new finding) |
| `make api-surface` (default local nightly) | PASS — baseline unmodified, 3959 items |
| `PUBLIC_API_TOOLCHAIN=nightly-2026-09-20 ./scripts/check-api-surface.sh .project/current-exports.txt` (new row — the exact CI-pinned toolchain) | PASS — baseline unmodified, 3959 items |
| `make test-shell-guards` (13 suites) | PASS — all 13 green, 325 assertions total, including the new `extract-public-api_test.sh` (15 assertions, was red 10/15 pre-fix) |
| `make test` | PASS — 14 `test result:` lines, all `ok`, 3708 total passed, 0 failed |
| Coverage floor (82%, ADR-0006) | **Attributed, not measured** — no Docker in this devcontainer; the reading comes from CI's `coverage` job on a later push, per D-00e |
| Standing statement | No static taint analysis of first-party Rust gates a merge in this repository (carried forward unchanged) |

### Task 3 — corpus audit section 14 + pointer paragraph (commit `5d6e103e`)

- Appended `## 14. Re-seal after the API-surface nightly pin (Phase 37.1, SHIP-06)` to
  `.project/v0.10.0/09-program-acceptance-audit.md`, immediately after section 13, in section 13's
  own shape: provenance paragraph, eight-row gate table, findings, closing scope paragraph, and a
  **fresh, unticked** sign-off box:
  `- [ ] **The \`v0.10.1\` tag may be cut (re-confirmed on the re-sealed head)**` at line **1844**.
- Verified append-only: `git diff 1d4a9724 -- .project/v0.10.0/09-program-acceptance-audit.md |
  grep -c '^-[^-]'` = `0`. Section headings `## 11.`, `## 12.`, `## 13.` each still appear exactly
  once. Ticked-box count (`grep -c '^- \[x\]'`) is `1` both before and after this commit — the same
  single tick section 13 already carried (the maintainer's own commit `529e7078`); section 14's own
  box was written unticked and stayed unticked.
- Appended one dated paragraph to
  `.planning/phases/29-program-gates-release/29-ACCEPTANCE-AUDIT.md` naming section 14, the head
  SHA, what changed since section 13, and that section 14 mints its own unticked box distinct from
  section 13's already-ticked one.

---

## Deviations from plan (37.1-07 as applied here)

None. All eight gates and the house sweep passed on the first attempt; D-08 stage 2's hard-stop was
never triggered. The only substantive difference from 37.1-07's own execution is the two new
house-sweep rows this reality-delta itself specifies (the pinned-toolchain API-surface check and
`make test-shell-guards`), which are additions the reality delta explicitly requested, not
deviations from it.

## What is deliberately NOT done here

- No push. `git status --porcelain` is empty; `origin/release/v0.10.1` is untouched by this work.
- No tick, untick, or edit of any sign-off box's state anywhere in the file. Section 13's tick
  (the maintainer's own commit `529e7078`) is untouched. Section 14's box is written unticked and
  stays unticked.
- No merge, no tag, no `cargo publish`, no `cargo yank`.
- PR CI conclusions on this new head (including the `Coverage` job's printed figure) are **not**
  filled into section 14's table — that is explicitly deferred to whichever plan pushes this head
  and reads the resulting run, per this section's own "to be filled in before the box is presented"
  language, exactly mirroring how section 13's own coverage cell was filled only after PR #56's run
  concluded.

## Self-Check

```
FOUND commit b55b337b (git log --oneline)
FOUND commit 4e8c022c (git log --oneline)
FOUND commit 5d6e103e (git log --oneline)
git rev-parse HEAD -> 5d6e103e404d47ffd0bf245d6fd2e40bb64eedc7
git status --porcelain -> empty
grep -c '^## 14\.' .project/v0.10.0/09-program-acceptance-audit.md -> 1
grep -c '^## 11\.' / '^## 12\.' / '^## 13\.' -> 1 / 1 / 1 (unrenumbered)
git diff 1d4a9724 -- 09-program-acceptance-audit.md | grep -c '^-[^-]' -> 0 (append-only)
grep -c '^- \[x\]' 09-program-acceptance-audit.md -> 1 (unchanged -- section 13's own tick)
awk '/^## 14\./,0' ... | grep -n '^- \[ \]' -> line 85 within section (absolute file line 1844) -- fresh unticked box present
make check-migration-allowlist / check-gates / check-publish-order -> all exit 0 (re-confirmed at self-check time)
git status --porcelain -- Cargo.lock -> empty
make api-surface / PUBLIC_API_TOOLCHAIN=nightly-2026-09-20 check -> both report baseline unchanged, 3959 items
```

## Self-Check: PASSED

All claimed files, commits and command outcomes verified present/reproducible. No missing items.
Nothing left running; every long gate was hosted detached under `target/37.1-reseal2/` (gitignored)
and waited on to completion before use.

---

## CI evidence for the re-sealed head (2026-09-21)

**Repeat of plan 37.1-09's Tasks 1-2 on the new PR head `d2f1a811`**, per this dispatch's own
`<reality_delta>` — the interim read recorded earlier in this file's own log (see `STATE.md`'s
prior "Resume" note) was partial, with four checks still in progress; this section is the completed
record.

**What was read.** Confirmed the remote head first (`git fetch origin && git rev-parse
origin/release/v0.10.1` and `gh pr view 56 --json headRefOid` both -> `d2f1a811...`, equal). Then,
per run and per job — never the aggregate — all 9 workflow runs on that head via `gh api
.../actions/runs?head_sha=...`; per-job conclusions for the two `ci.yml` runs and a spot-check of
the other 7 via `.../actions/runs/<id>/jobs`; the 44-required-context tally cross-checked against
the live `main` ruleset (`gh api repos/.../rulesets/20868126`) and the 124-row `gh pr checks 56
--json name,state,bucket,workflow` output; three job logs fetched via `curl -sSL -H "Authorization:
Bearer $(gh auth token)" .../actions/jobs/<id>/logs` (API Surface Tracking, License & Dependency
Policy, Coverage) — the `gh api .../logs` form returns empty for a finished job, so the `curl` form
was used throughout, exactly as plan 37.1-09 first established.

**Results.**

- **44/44 required contexts present; 43 `pass`, 1 conditional `skipping`** (`End-to-End Tests`,
  same conditional shape as every prior head); **zero required contexts absent, zero failed.**
- **`API Surface Tracking` = `success`, and its log names the pinned toolchain by name**: `info:
  latest update on 2026-09-20 for version 1.100.0-nightly (feaadeeac 2026-09-19)` /
  `nightly-2026-09-20-x86_64-unknown-linux-gnu installed` / `Extracting public API surface using
  cargo-public-api (toolchain: nightly-2026-09-20)...` / `✅ API surface unchanged` — job
  `106403328151`, PR #56's `pull_request` run `35620879265`. This is the check that was red on the
  maintainer's tick commit `529e7078`; it is green here because the pin resolves to the exact
  `feaadeeac`/2026-09-20 nightly the fix commit `d2f1a811` targets.
- **Publish-order gate proof**: the License & Dependency Policy job (`106403327775`, same run)
  printed `✅ OK: 12 crate(s) checked in publish order...` for the `Check publish order` step,
  identical to the local sweep's own `make check-publish-order` result — proof the gate executed
  pre-merge on this pull request, not merely present in the workflow file.
- **Coverage job's real printed figure**: `Lines: 111771/123587 = 90.44%` (job `106403328509`, same
  run) — clears the 82% ADR-0006 floor by 8.44 percentage points. Read from the log, not expected.
- **One non-required failure, recorded verbatim, nothing fixed, nothing re-run**: `Ollama
  Integration Tests (live server)`, `pull_request` run `35620879265`, job `106403328378`, failed
  step `Start ollama-test and pull the model` — a Docker Hub token-endpoint connection reset (`read:
  connection reset by peer`) while pulling `ollama/ollama:0.3.14`; no test ran. The same job on the
  same head's `push` run (`35620873206`, job `106403553951`) reads `success` — the image exists and
  pulls there; this is a transient network flake, not a retired image. Confirmed, live, not among
  the 44 required contexts — does not block PR #56's merge.
- **`Kubernetes Smoke Test`'s state recorded as read, not waited on**: `push` run `35620873206`, job
  `106444814812`, `status: in_progress` at the single GET issued. Documented in
  `docs/src/appendix/branch-protection.md` as one of the two jobs deliberately excluded from the
  required-context set; its in-progress state gates nothing.
- **No required check concluded failure — D-08 stage 2's halt-and-record branch does not apply.**
  No workflow run was re-run, re-dispatched or cancelled; every read was a single GET.

**Where it landed.** The full per-run/per-job table, the required-context tally, both job-log proof
blocks and the coverage figure are recorded in
`.planning/phases/37.1-v0-10-1-patch-release/37.1-CI-EVIDENCE.md`'s new "CI-run table — re-sealed
head d2f1a811" section (commit `d4fe8edd`). Section 14 of
`.project/v0.10.0/09-program-acceptance-audit.md` was then updated (commit `4f3df7ef`): row 8 gained
the publish-order pre-merge proof, the coverage paragraph was filled with the figure/job/run/head,
and the evidence-basis sentence immediately above the sign-off box was updated to state the basis is
now complete. **The box itself — line 1851, `- [ ] **The \`v0.10.1\` tag may be cut (re-confirmed on
the re-sealed head)** — ...`** — was not touched: verified via `git diff -U0` (all hunks at or after
line 1800, inside section 14) and `grep -c '^- \[x\]'` reading `1` both before and after (unchanged
— section 13's own tick, `529e7078`, is the only tick in the file).

## Deviations

None. Both tasks ran as specified; no required check failed; the one non-required failure (Ollama)
and the one non-required in-progress state (Kubernetes Smoke Test) were recorded as read, not
treated as blockers.

## Commits

- `d4fe8edd` — `docs(37.1): re-seal #2 CI evidence -- full per-run/per-job table on d2f1a811`
  (`37.1-CI-EVIDENCE.md`)
- `4f3df7ef` — `docs(37.1): re-seal #2 -- point section 14's rows at the recorded d2f1a811 CI runs,
  fill coverage entry` (`09-program-acceptance-audit.md`)

Both local-only, on `release/v0.10.1`. **Not pushed**, per this dispatch's own instruction.

## Self-Check

```
FOUND commit d4fe8edd (git log --oneline)
FOUND commit 4f3df7ef (git log --oneline)
git rev-parse HEAD -> 4f3df7ef... (local; origin/release/v0.10.1 unchanged at d2f1a811)
git status --porcelain -> empty
grep -c '^- \[x\]' .project/v0.10.0/09-program-acceptance-audit.md -> 1 (unchanged)
awk '/^## 14\./,0' .project/v0.10.0/09-program-acceptance-audit.md | grep -n '^- \[ \]' -> present, line 92
  within section (absolute file line 1851) -- unticked, byte-identical to before this dispatch
git diff -U0 -- .project/v0.10.0/09-program-acceptance-audit.md -> all hunks at line 1800+
  (section 14 starts at line 1760)
```

## Self-Check: PASSED

All claimed files, commits and command outcomes verified present/reproducible. No missing items.
Nothing left running; no run was re-run, re-dispatched, cancelled or waited on.

---

*Phase: 37.1-v0-10-1-patch-release*
*Written: 2026-09-21*
*This is a re-seal record, not a plan SUMMARY — deliberately named to stay outside the
`*-PLAN.md`/`*-SUMMARY.md` glob GSD tooling indexes, per this dispatch's own instruction.*
