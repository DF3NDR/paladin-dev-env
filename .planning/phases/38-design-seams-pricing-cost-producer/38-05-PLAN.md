---
phase: 38-design-seams-pricing-cost-producer
plan: 05
type: execute
wave: 3
depends_on: ["38-02"]
files_modified:
  - crates/paladin-herald/src/json_herald.rs
  - crates/paladin-herald/src/table_herald.rs
autonomous: true
requirements: [PRICE-03]

must_haves:
  truths:
    - "The JSON herald's finalize_stream keeps emitting the numeric cost_estimate field unchanged and adds a currency field beside it: 0.045 and 'USD' for metadata built from a 45_000_000-nano USD Cost, both null for unpriced metadata (D-04) — existing JSON consumers see no removed or retyped key."
    - "The table herald's finalize_stream renders the metadata it is given for the first time: the model, the real duration, prompt/completion/total tokens plus any reported cache-read, cache-write and reasoning sub-counts, the error count, and a Cost row reading '0.0450 USD' when priced — the placeholder values 3.45s, 950, 'Paladins Executed' and 'Success Rate' are gone (research Pitfall 4, D-11)."
    - "No herald prints a dollar sign for cost: an unpriced run shows no Cost row or a null currency, visibly different from a priced zero ('0.0000 USD'), and never a zero stand-in (D-00c, D-04)."
    - "All three heralds render cost through the one shared ExecutionMetadata::cost_display() formatter from 38-02, so the four-decimal-plus-code format cannot drift between markdown, JSON-adjacent and table output."
  artifacts:
    - path: crates/paladin-herald/src/json_herald.rs
      provides: "finalize_stream currency sibling field"
      contains: "cost_currency()"
    - path: crates/paladin-herald/src/table_herald.rs
      provides: "finalize_stream consuming real ExecutionMetadata incl. Cost row"
      contains: "cost_display()"
  key_links:
    - from: crates/paladin-herald/src/table_herald.rs
      to: crates/paladin-core/src/platform/container/herald.rs
      via: "reads metadata.model_used, duration_ms, token_usage, error_count and cost_display()"
      pattern: "metadata\\.token_usage"
---

<objective>
Finish D-11's herald surface: the JSON herald reports the currency beside `cost_estimate`, and the
table herald's `finalize_stream` — a stub that ignored its argument and printed fixed placeholder
rows — renders the real `ExecutionMetadata`, including a currency-aware Cost row (D-04).

Purpose: the markdown herald was fixed in 38-02; without this plan the JSON herald cannot say
which currency its number is in, and the table herald would print "950 tokens" for every run
regardless of what happened (RESEARCH Pitfall 4 — a separate, larger job than "add a cost row").

Output: two herald files changed, each with tests pinning the new output.

**Tracer:** expansion of plan 38-02's tracer.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-design-seams-pricing-cost-producer/38-CONTEXT.md
@.planning/phases/38-design-seams-pricing-cost-producer/38-PATTERNS.md
@.planning/phases/38-design-seams-pricing-cost-producer/38-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (plan 38-05 slice)

Full phase-wide table: `38-01-PLAN.md`. This plan adds no new public symbol: it adds the
`"currency"` key to `JsonHerald::finalize_stream`'s output object and replaces
`TableHerald::finalize_stream`'s body, plus the tests `finalize_stream_emits_currency_beside_cost_estimate`,
`finalize_stream_unpriced_has_null_currency`, `finalize_stream_uses_real_metadata`,
`finalize_stream_renders_currency_cost_row`, `finalize_stream_omits_cost_row_when_unpriced`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: JSON herald — currency beside cost_estimate</name>
  <files>crates/paladin-herald/src/json_herald.rs</files>
  <read_first>
    - crates/paladin-herald/src/json_herald.rs lines 195-225 (finalize_stream) and its test_finalize_stream (~line 511) and streaming integration test (~line 705)
    - crates/paladin-core/src/platform/container/herald.rs (ExecutionMetadataBuilder::cost, ExecutionMetadata::cost_currency, cost_display — from 38-02)
    - crates/paladin-core/src/platform/container/cost.rs (Cost::new, CurrencyCode::new)
  </read_first>
  <behavior>
    - finalize_stream_emits_currency_beside_cost_estimate: metadata built with .cost(&Cost::new(45_000_000, USD)) → parsed cost_estimate == 0.045 and parsed currency == "USD"
    - finalize_stream_unpriced_has_null_currency: metadata with no cost → cost_estimate is null and currency is null; every pre-existing key (type, execution_id, duration_ms, model_used, usage, total_tokens, timestamp) is still present
  </behavior>
  <action>
Implements **D-04** (JSON herald keeps the numeric field and adds the currency beside it) and **D-11** (heralds print cost). Write the two tests first. In `finalize_stream`'s `json!` object add `"currency": metadata.cost_currency()` immediately after `"cost_estimate"`; change nothing else in the object (no key removed, renamed or retyped).
  </action>
  <verify>
    <automated>cargo test -p paladin-herald --lib json_herald && cargo clippy -p paladin-herald --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `crates/paladin-herald/src/json_herald.rs` contains `"currency": metadata.cost_currency()`
    - `cargo test -p paladin-herald --lib json_herald` passes and lists `finalize_stream_emits_currency_beside_cost_estimate` and `finalize_stream_unpriced_has_null_currency`
    - The pre-existing `test_finalize_stream` passes unmodified
  </acceptance_criteria>
  <done>A JSON consumer can tell which currency cost_estimate is in, and an unpriced run reports null for both.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Table herald — finalize_stream renders real metadata, including cost</name>
  <files>crates/paladin-herald/src/table_herald.rs</files>
  <read_first>
    - crates/paladin-herald/src/table_herald.rs lines 1-80 (create_table, config) and lines 240-300 (the finalize_stream stub) and its test_finalize_stream (~line 589) and the streaming test near line 900
    - crates/paladin-herald/src/markdown_herald.rs lines 238-255 and 412-427 (token_usage_block and finalize_stream — the real-data pattern to imitate, including the optional sub-count rows)
    - crates/paladin-core/src/platform/container/herald.rs (ExecutionMetadata fields and cost_display)
  </read_first>
  <behavior>
    - finalize_stream_uses_real_metadata: model test-model, duration 1000, TokenUsage::new(300, 200).with_cache_read(120) → output contains test-model, 1000ms, 300, 200, 500 and 120; contains none of 3.45s, 950, Paladins Executed, Success Rate
    - finalize_stream_renders_currency_cost_row: metadata with .cost(45_000_000 USD) → output contains Cost and 0.0450 USD and no dollar sign
    - finalize_stream_omits_cost_row_when_unpriced: metadata without cost → output has no Cost row
  </behavior>
  <action>
Implements **D-11** (table herald prints cost) and **D-04**, closing research **Pitfall 4**. Write the tests first (they fail against the stub). Replace the body of `finalize_stream` — rename its parameter from `_metadata` to `metadata` — so the Metric/Value table carries, in order: `Model` (`metadata.model_used`), `Total Duration` (`{ms}ms` when `duration_ms` is `Some`, row omitted otherwise), `Prompt Tokens`, `Completion Tokens`, then `Cache Read Tokens` / `Cache Write Tokens` / `Reasoning Tokens` only when reported (`Some`), `Total Tokens` (kept beside the split, never alone — Phase 31 D-08 coexistence), `Errors` (`error_count`), and `Cost` from `metadata.cost_display()` only when `Some`. Delete the four placeholder rows and their comment. Keep the `--- Execution Metadata ---` header and the existing error mapping. Update the pre-existing `test_finalize_stream` only where it asserted placeholder-only content (its `Total Duration` and `Total Tokens` assertions still hold).
  </action>
  <verify>
    <automated>cargo test -p paladin-herald --features table --lib table_herald && cargo clippy -p paladin-herald --all-targets --features table -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '3\.45s|"950"|Paladins Executed|Success Rate' crates/paladin-herald/src/table_herald.rs` prints `0`
    - `crates/paladin-herald/src/table_herald.rs` contains `metadata: &` in the `finalize_stream` signature (no leading underscore) and `cost_display()`
    - `awk '/fn finalize_stream/,/^    }$/' crates/paladin-herald/src/table_herald.rs | grep -cF '${'` prints `0`
    - `cargo test -p paladin-herald --features table --lib table_herald` passes and lists `finalize_stream_uses_real_metadata`, `finalize_stream_renders_currency_cost_row`, `finalize_stream_omits_cost_row_when_unpriced`
  </acceptance_criteria>
  <done>The table herald's metadata table reflects the actual run — model, duration, token split, errors and a currency-coded cost — instead of fixed placeholder numbers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `ExecutionMetadata` → rendered herald text | herald output is shown to operators and may be logged; it must not misstate spend |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-17 | Tampering (misleading display) | table/JSON herald cost rendering | low | mitigate | single `cost_display()` formatter; currency code, never a hard-coded symbol; unpriced renders no Cost row / null, never `0.0000`; placeholder rows removed (acceptance grep) |
| T-38-18 | Information Disclosure | herald output | low | accept | renders model name, token counts and cost only — no prompt or response content is added by this plan |
</threat_model>

<verification>
- `cargo test -p paladin-herald --lib` and `cargo test -p paladin-herald --features table --lib`
- `cargo clippy -p paladin-herald --all-targets --all-features -- -D warnings`
</verification>

<success_criteria>
All three heralds (markdown from 38-02, JSON and table here) print `ExecutionMetadata.cost_estimate`
with its currency per D-04, and the table herald renders real metadata (D-11, PRICE-03 surface).
</success_criteria>

<output>
Create `.planning/phases/38-design-seams-pricing-cost-producer/38-05-SUMMARY.md` when done
</output>
