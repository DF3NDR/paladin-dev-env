---
phase: 38-design-seams-pricing-cost-producer
plan: 09
type: execute
wave: 6
depends_on: ["38-01", "38-02", "38-03", "38-04", "38-05", "38-06", "38-07", "38-08"]
files_modified:
  - MIGRATION.md
  - .cargo/semver-checks-allowlist.toml
  - crates/paladin-core/Cargo.toml
  - crates/paladin-ports/Cargo.toml
  - crates/paladin-llm/Cargo.toml
  - crates/paladin-battalion/Cargo.toml
  - crates/paladin-herald/Cargo.toml
  - Cargo.toml
  - CHANGELOG.md
  - crates/paladin-core/CHANGELOG.md
  - crates/paladin-ports/CHANGELOG.md
  - crates/paladin-llm/CHANGELOG.md
  - crates/paladin-battalion/CHANGELOG.md
  - crates/paladin-herald/CHANGELOG.md
  - .project/current-exports.txt
autonomous: true
requirements: [PRICE-01, PRICE-02, PRICE-03]

must_haves:
  truths:
    - "cargo semver-checks 0.50.0, run exactly as CI's semver job runs it (every one of the 11 CI packages, --default-features, --baseline-version 0.9.0), passes on the phase's final tree, and every lint it reports for a Phase 38 change is registered as a MIGRATION.md §9.2 row marked Y plus a matching .cargo/semver-checks-allowlist.toml entry plus the crate's Cargo.toml lint allow — derived from the tool's actual output, never guessed (D-00g, Phase 31 D-27 precedent)."
    - "The same tool run against the published 0.10.1 baseline (--release-type minor) for every package Phase 38 changed is captured in the SUMMARY, and each Phase 38 break it reports that the 0.9.0 run cannot see (for example the TraceEvent struct-variant fields) is recorded in MIGRATION.md §9.2 for the v0.10 → v0.11 guide, marked N/A for CI set-equality with the reason stated — no break is left unrecorded (D-00g, X-03)."
    - "The existing PaladinResult and Settings §9.2 rows gain a dated 'Extended, Phase 38 (PRICE-01/PRICE-03)' Change-cell note for the cost and treasurer fields rather than duplicate rows (the set-equality gate deduplicates by crate | type), and ./scripts/check-migration-allowlist.sh and make check-gates exit 0."
    - "CHANGELOG.md gains an [Unreleased] section above [0.10.1] with Added (treasurer price table, per-call pricing decorator, Cost/CurrencyCode/PriceTable/CostTally, ExecutionMetadata production on both run paths, HeraldTraceSink) and Changed (heralds print the currency code instead of a dollar sign; the table herald renders real metadata; the final streamed chunk always carries execution metadata; cost fields added to LlmResponse, PaladinResult, TraceEvent::NodeFinished/RunFinished, StreamingResponse, ChunkMetadata — pointing at MIGRATION.md §9.2) entries, and each changed crate's CHANGELOG [Unreleased] names its own additions."
    - "The public-surface baseline is refreshed with make api-surface-update using the CI-pinned dated nightly, .project/current-exports.txt lists the new cost module, TreasurerConfig and HeraldTraceSink items, and make api-surface exits 0 (D-00j)."
    - "The phase's final tree passes every commit gate CLAUDE.md names: cargo test --workspace, cargo fmt --check, make clean-code (clippy -D warnings on all targets and features, rustdoc zero warnings, public-API examples gate), make security (cargo-audit + cargo-deny), make check-gates; the committed OpenAPI document is unchanged (make openapi produces no diff), confirming no HTTP schema exposes cost; the 82% coverage floor is attributed to CI's coverage job (no Docker locally)."
  artifacts:
    - path: CHANGELOG.md
      provides: "[Unreleased] Added/Changed entries for Phase 38"
      contains: "## [Unreleased]"
    - path: .project/current-exports.txt
      provides: "Refreshed public API baseline including the cost module"
      contains: "cost::Cost"
    - path: MIGRATION.md
      provides: "§9.2 rows/extensions for every Phase 38 break the tool reports"
      contains: "Phase 38"
  key_links:
    - from: MIGRATION.md
      to: .cargo/semver-checks-allowlist.toml
      via: "CI semver job set-equality over crate | type pairs"
      pattern: "PRICE-0[13]"
---

<objective>
Close the phase on the project's commit and release gates (D-00g, D-00j): measure every public-API
change Phase 38 made with `cargo semver-checks` and register what it reports, write the CHANGELOG
entries, refresh the public-surface baseline, and run the full gate suite on the final tree.

Purpose: plans 38-02..38-08 each changed public types; this plan is the single owner of the shared
release-record files (CHANGELOG, MIGRATION §9.2, the allowlist, the API baseline) so no two
parallel plans ever edited them at once, and so the registration is derived empirically from the
tool, as Phase 31 did (D-27), rather than predicted.

Output: MIGRATION/allowlist/lint registrations matching the tool's actual output, CHANGELOG
entries, a refreshed `.project/current-exports.txt`, and a SUMMARY recording every gate's result.

**Tracer:** expansion/closeout of plan 38-02's tracer.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-design-seams-pricing-cost-producer/38-CONTEXT.md
@.planning/phases/38-design-seams-pricing-cost-producer/38-04-SUMMARY.md
@.planning/phases/38-design-seams-pricing-cost-producer/38-08-SUMMARY.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (plan 38-09 slice)

Full phase-wide table: `38-01-PLAN.md`. This plan creates no Rust symbol. It adds the
`## [Unreleased]` section to `CHANGELOG.md`, per-crate `[Unreleased]` bullets, MIGRATION.md §9.2
rows/extensions and allowlist entries for whatever lints the tool reports, and regenerates
`.project/current-exports.txt`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Measure every Phase 38 API change with cargo-semver-checks and register what it reports</name>
  <files>MIGRATION.md, .cargo/semver-checks-allowlist.toml, crates/paladin-core/Cargo.toml, crates/paladin-ports/Cargo.toml, crates/paladin-llm/Cargo.toml, crates/paladin-battalion/Cargo.toml, crates/paladin-herald/Cargo.toml, Cargo.toml</files>
  <read_first>
    - .github/workflows/ci.yml lines 300-470 (the semver job: the 11 packages, flags, baseline 0.9.0, and the set-equality script)
    - .cargo/semver-checks-allowlist.toml (schema header, the Phase 31/32 sections with their empirical-derivation comments, and the Phase 38 entry 38-04 added)
    - MIGRATION.md lines 170-300 (§9.2 columns, the PaladinResult and Settings rows and their "Extended, Phase 31" / Phase 28 extension style, the N/A rows for types absent at the v0.9.0 baseline, the LlmResponse row 38-04 added)
    - The per-crate `[package.metadata.cargo-semver-checks.lints]` tables in crates/paladin-core/Cargo.toml, crates/paladin-ports/Cargo.toml and the root Cargo.toml
    - scripts/check-migration-allowlist.sh
    - .planning/phases/38-design-seams-pricing-cost-producer/38-04-SUMMARY.md (what 38-04 already registered)
  </read_first>
  <action>
Implements **D-00g** (any public-API break is recorded in MIGRATION §9.2 plus the semver-checks allowlist) using the Phase 31 D-27 empirical rule.

Install the CI-pinned tool if absent: `cargo install --locked cargo-semver-checks --version 0.50.0`. Run the CI-parity check for each of the 11 packages CI lists (`paladin-ai paladin-ai-core paladin-ports paladin-battalion paladin-herald paladin-llm paladin-memory paladin-storage paladin-notifications paladin-content paladin-web`): `cargo semver-checks check-release --package <pkg> --default-features --baseline-version 0.9.0`, saving each output. Then run the honest v0.11 baseline for each package Phase 38 changed (`paladin-ai paladin-ai-core paladin-ports paladin-llm paladin-battalion paladin-herald paladin-web`): `cargo semver-checks check-release --package <pkg> --default-features --baseline-version 0.10.1 --release-type minor`, saving each output. Paste the relevant lint lines of every run into the SUMMARY.

Register per result, never more and never less than the tool reports:
- A lint that fires in the 0.9.0 run for a Phase 38 change and is not already suppressed for that crate → add `<lint> = "allow"` to that crate's `[package.metadata.cargo-semver-checks.lints]` table (create the table with the house comment block if the crate has none), a MIGRATION.md §9.2 row marked `Y` (Crate = published package name, the Change/Mitigation/justification in the existing rows' style, Req = the owning PRICE id), and an allowlist `[[entry]]` under the `# --- Phase 38 ...` header 38-04 created.
- A lint that fires for a type that already has a §9.2 row on the same `crate | type` pair (expected: `paladin-ai-core | PaladinResult` for `cost`, `paladin-ai | Settings` for `treasurer`) → append an `**Extended, Phase 38 (PRICE-03, D-10):** …` (or `PRICE-01, D-07` for Settings) sentence to that row's Change cell and name the already-present suppression in its Mitigation cell; add an allowlist entry only if the lint id is not already listed for that pair.
- A break the 0.10.1 run reports that the 0.9.0 run cannot see (expected: the `NodeFinished`/`RunFinished` struct-variant fields on `paladin-ai-core | TraceEvent`, which did not exist at 0.9.0) → a §9.2 row whose Deliberate-breaking cell reads `N/A — TraceEvent did not exist at the v0.9.0 baseline CI diffs against; breaking relative to the published 0.10.1 for downstream full struct-variant literals and exhaustive variant patterns; recorded for the v0.10 → v0.11 migration guide (Phase 46, CURR-23)` (the ResumeAccepted precedent), with no allowlist entry.
- `#[non_exhaustive]` additions (`StreamingResponse`, `ChunkMetadata`) and purely additive items (new modules, methods, types) need no row unless the tool reports one.

Re-run the 0.9.0 check for all 11 packages until every one passes, then run `./scripts/check-migration-allowlist.sh` and `make check-gates`.
  </action>
  <verify>
    <automated>for pkg in paladin-ai paladin-ai-core paladin-ports paladin-battalion paladin-herald paladin-llm paladin-memory paladin-storage paladin-notifications paladin-content paladin-web; do cargo semver-checks check-release --package "$pkg" --default-features --baseline-version 0.9.0 || exit 1; done && ./scripts/check-migration-allowlist.sh && make check-gates</automated>
  </verify>
  <acceptance_criteria>
    - The verify loop exits 0 (all 11 CI packages pass against 0.9.0)
    - `./scripts/check-migration-allowlist.sh` and `make check-gates` exit 0
    - `MIGRATION.md` contains `Phase 38` in the §9.2 section (between `## 9.2` and `## 9.3`) at least once per registered or extended type, and contains `paladin-ports` + `LlmResponse` (from 38-04)
    - The SUMMARY contains the captured lint output of both the 0.9.0 run and the 0.10.1 run for every package listed above
    - Every `[[entry]]` added in this plan has a `requirement_id` that is one of `PRICE-01`, `PRICE-02`, `PRICE-03`
  </acceptance_criteria>
  <done>Every public-API change Phase 38 made is measured by the tool and recorded — CI's semver job and set-equality gate are green, and the v0.10 → v0.11 breaks are on the register for the migration guide.</done>
</task>

<task type="auto">
  <name>Task 2: CHANGELOG entries, refreshed API baseline, and the full gate suite on the final tree</name>
  <files>CHANGELOG.md, crates/paladin-core/CHANGELOG.md, crates/paladin-ports/CHANGELOG.md, crates/paladin-llm/CHANGELOG.md, crates/paladin-battalion/CHANGELOG.md, crates/paladin-herald/CHANGELOG.md, .project/current-exports.txt</files>
  <read_first>
    - CHANGELOG.md lines 1-60 (Keep a Changelog format; the [0.10.1] and [0.10.0] section styles; there is currently no [Unreleased] section)
    - crates/paladin-core/CHANGELOG.md and crates/paladin-llm/CHANGELOG.md lines 1-30 (per-crate [Unreleased] headings and bullet style)
    - Makefile targets api-surface, api-surface-update, clean-code, security, check-gates and openapi; scripts/extract-public-api.sh header (the PUBLIC_API_TOOLCHAIN pin rule)
    - .github/workflows/ci.yml line ~229 (the dated nightly CI pins for the API surface job)
    - Every Phase 38 SUMMARY (38-01 … 38-08) for the exact symbols and behavior changes to describe
  </read_first>
  <action>
Implements **D-00j** (CHANGELOG entry with the refreshed API surface; `make clean-code`, `make security` and the manual credential-handling review gate the commit) and records **D-03/D-04/D-10/D-11/D-12**'s user-visible effects.

Add `## [Unreleased]` above `## [0.10.1] - 2026-09-20` in `CHANGELOG.md` with `### Added` bullets (the `treasurer:` config section with per-1M decimal-string prices in one ISO currency, empty by default, validated at boot; `paladin_llm::pricing::PricingLlmAdapter` / `with_pricing` pricing every LLM call at the port boundary on both run paths, one warning per unpriced model; the `cost` module types `Cost`, `CurrencyCode`, `PriceRow`, `PriceTable`, `cost_of_call`, `CostTally` — i64 nano-units, half-up once per call; `ExecutionMetadata` now produced on the agent loop's streamed completion and, via `HeraldTraceSink` + `RunWorkerPool::with_herald`, on every engine run when a `herald:` section is configured; `ExecutionMetadata::from_run_finished`/`cost`/`cost_currency`/`cost_display`; `PaladinExecutionService::finalize_stream_output`; `FacadeProvisioner::with_treasurer`; ADR-0052/0053) and `### Changed` bullets (heralds print cost as four decimals plus the currency code, never a dollar sign; the table herald's metadata table renders the run's real model/duration/tokens/errors/cost instead of placeholder rows; the final streamed chunk always carries `ChunkMetadata` with the run's `execution` metadata; `cost` fields on `LlmResponse`, `PaladinResult`, `TraceEvent::NodeFinished`/`RunFinished`, `StreamingResponse`, `ChunkMetadata` — full struct literals of the first three need `cost: None`, see `MIGRATION.md` §9.2). Add matching concise bullets under each changed crate's `## [Unreleased]` (`paladin-core`: cost module, trace/result fields, `ExecutionMetadata` methods; `paladin-ports`: `LlmResponse.cost`, `StreamingResponse.cost`, `ChunkMetadata.cost`/`execution`; `paladin-llm`: `pricing` module; `paladin-battalion`: `TraceDispatcher::total_cost`, `RunFinished.cost` populated; `paladin-herald`: currency-aware cost, real table metadata).

Refresh the public-surface baseline with the CI toolchain pin: `rustup toolchain install nightly-2026-09-20` if absent (read the exact name from `ci.yml`'s `PUBLIC_API_TOOLCHAIN`), then `PUBLIC_API_TOOLCHAIN=<that name> make api-surface-update` and `PUBLIC_API_TOOLCHAIN=<that name> make api-surface`.

Run the full gate suite on the final tree and record each result in the SUMMARY: `cargo fmt --check`; `cargo test --workspace`; `make clean-code`; `make security` (install `cargo-audit`/`cargo-deny` with `cargo install --locked` if absent — a new advisory is a blocker unless allowlisted with a reason, per `security.instructions.md`); `make check-gates`; `make openapi` followed by `git diff --exit-code crates/paladin-web/openapi.json` (no HTTP schema gained a cost field). Perform the manual credential-handling review from `.github/instructions/security.instructions.md` over the Phase 38 diff (`git diff <phase-start>..HEAD -- crates src`) and state its result in the SUMMARY: no API key is logged or Debug-formatted, the pricing warning logs only a model name, no new HTTP client was added. State that the 82% coverage floor is attributed to CI's `coverage` job because this devcontainer has no Docker (STATE.md standing concern).
  </action>
  <verify>
    <automated>cargo fmt --check && cargo test --workspace && make clean-code && PUBLIC_API_TOOLCHAIN="$(grep -m1 -oE 'PUBLIC_API_TOOLCHAIN: nightly-[0-9-]+' .github/workflows/ci.yml | cut -d' ' -f2)" make api-surface && make check-gates && grep -q '^## \[Unreleased\]' CHANGELOG.md && grep -q 'cost::Cost' .project/current-exports.txt && grep -q 'TreasurerConfig' .project/current-exports.txt && grep -q 'HeraldTraceSink' .project/current-exports.txt</automated>
  </verify>
  <acceptance_criteria>
    - `CHANGELOG.md` has exactly one `## [Unreleased]` line, placed before `## [0.10.1]`, and its section contains `PricingLlmAdapter`, `treasurer`, `Cost`, `HeraldTraceSink`, `currency`, and `MIGRATION.md`
    - Each of `crates/paladin-core/CHANGELOG.md`, `crates/paladin-ports/CHANGELOG.md`, `crates/paladin-llm/CHANGELOG.md`, `crates/paladin-battalion/CHANGELOG.md`, `crates/paladin-herald/CHANGELOG.md` has at least one new bullet under its `## [Unreleased]` heading mentioning `cost` or `pricing`
    - `.project/current-exports.txt` contains `cost::Cost`, `TreasurerConfig` and `HeraldTraceSink`, and `make api-surface` exits 0
    - `cargo fmt --check`, `cargo test --workspace`, `make clean-code`, `make check-gates` exit 0; `make security` exits 0 or every finding is recorded as pre-existing and allowlisted in `.cargo/audit.toml` with a reason
    - `git diff --exit-code crates/paladin-web/openapi.json` exits 0 after `make openapi`
    - The SUMMARY contains a "Credential-handling review" paragraph and a "Coverage" line naming CI attribution
  </acceptance_criteria>
  <done>The phase's changes are described in the CHANGELOGs, the API baseline matches the built surface, and the final tree passes every gate the project requires before a phase is sealed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this repo → downstream crate consumers | published API changes reach users of the 12 crates; unrecorded breaks surprise them at upgrade |
| dependency graph → build | `make security` guards advisories and licenses |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-30 | Repudiation (unrecorded API break) | MIGRATION §9.2 / allowlist | medium | mitigate | empirical `cargo semver-checks` runs against both 0.9.0 (CI parity) and 0.10.1; every reported lint registered; set-equality script + `make check-gates` |
| T-38-31 | Tampering (public-surface drift) | `.project/current-exports.txt` | medium | mitigate | `make api-surface-update` with the CI-pinned nightly in the same commit as the CHANGELOG entry; `make api-surface` in verify |
| T-38-32 | Information Disclosure | HTTP schema | medium | mitigate | `make openapi` + `git diff --exit-code crates/paladin-web/openapi.json` proves no HTTP response schema gained a cost field this phase |
| T-38-33 | Elevation / Tampering (vulnerable dependency) | dependency graph | low | mitigate | `make security` (cargo-audit + cargo-deny); no new crate was added this phase |
| T-38-34 | Information Disclosure (credential leakage) | Phase 38 diff | low | mitigate | manual credential-handling review per `security.instructions.md`, recorded in the SUMMARY (CodeQL is advisory-only for Rust) |
</threat_model>

<verification>
- 11-package `cargo semver-checks` loop against 0.9.0 exits 0; 0.10.1 outputs captured
- `./scripts/check-migration-allowlist.sh`, `make check-gates`, `make api-surface`, `make clean-code`, `make security`, `cargo test --workspace`, `cargo fmt --check`
- `make openapi` leaves `crates/paladin-web/openapi.json` unchanged
</verification>

<success_criteria>
PRICE-01, PRICE-02 and PRICE-03 are delivered on a tree that passes every project gate, with every
public-API change measured, registered and described (D-00g, D-00j).
</success_criteria>

<output>
Create `.planning/phases/38-design-seams-pricing-cost-producer/38-09-SUMMARY.md` when done
</output>
