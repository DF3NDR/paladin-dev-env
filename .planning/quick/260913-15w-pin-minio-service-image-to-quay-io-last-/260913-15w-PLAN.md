---
phase: quick-260913-15w
plan: 01
type: execute
wave: 1
depends_on: []
quick_id: 260913-15w
autonomous: true
requirements: [QUICK-260913-15w]
files_modified:
  - .github/workflows/ci.yml
  - docker/docker-compose.test.yml
  - docker/docker-compose.yml
  - .devcontainer/docker-compose.yml
  - k8s/minio.yaml
  - .devcontainer/CI-CD.md
  - docs/src/deployment/cicd.md
  - docs/src/deployment/docker.md
  - docs/src/appendix/integration-tests.md
  - docs/src/appendix/minio-file-repository-setup.md
  - docs/src/contributing/testing-guide.md
  - docs/src/contributing/branching-model.md
  - .planning/todos/pending/2026-09-13-evaluate-rustfs-replacement-for-minio.md

must_haves:
  truths:
    - "Every live configuration that starts a MinIO server or client container names a quay.io repository at an explicit pinned release tag, never a floating tag."
    - "The four CI jobs that were failing in 'Initialize containers' (Coverage, Integration Tests, Docker Integration Tests, Kubernetes Smoke Test) pull an image that still exists."
    - "Ports, credentials, health checks, server commands, and entrypoints are byte-identical to before the pin — only the image reference changes."
    - "A reader of ci.yml or docker/docker-compose.test.yml can see, inline, why the tag is pinned and where the long-term replacement is tracked."
    - "User-facing docs quote the same image reference the shipped configuration uses."
    - "The RustFS-replacement evaluation is captured as a pending todo that survives this task."
  artifacts:
    - .github/workflows/ci.yml
    - docker/docker-compose.test.yml
    - docker/docker-compose.yml
    - .devcontainer/docker-compose.yml
    - k8s/minio.yaml
    - .planning/todos/pending/2026-09-13-evaluate-rustfs-replacement-for-minio.md
  key_links:
    - "ci.yml integration-tests + coverage service blocks -> quay.io server image (the two jobs that fail first on a bad reference)"
    - "docker/docker-compose.test.yml minio-test + minio-test-init -> quay.io server + client images (drives Docker Integration Tests, Ollama, Postgres jobs)"
    - "k8s/minio.yaml Deployment container image -> quay.io server image (drives Kubernetes Smoke Test)"
    - "inline pin comments -> .planning/todos/pending/2026-09-13-evaluate-rustfs-replacement-for-minio.md"
---

<objective>
Docker Hub deleted the community MinIO server and client repositories on 2026-09-12 (HTTP 404 on
both the repo and tags endpoints). Every Paladin job that starts an object-storage service now
fails in "Initialize containers" with `pull access denied ... repository does not exist` —
Coverage, Integration Tests, Docker Integration Tests and Kubernetes Smoke Test — with no
first-party code at fault. Confirmed on ci.yml runs 34726321273 and 34724471647 (the latter
carried no code change at all).

Repoint every live configuration, and the docs that quote it, at the last known-good community
release still served by quay.io, pinned to an explicit tag because no newer community tag will
ever be published. Then capture the strategic follow-up: evaluate replacing MinIO in the dev/test
stack with RustFS so CI stops depending on an unmaintained community image.

Pinned references (verified active, multi-arch manifest lists):
- server: `quay.io/minio/minio:RELEASE.2025-09-07T16-13-09Z.hotfix.7aa24e772`
  (digest `sha256:cf3dadcfa1fb0324f43958bad1abba986d53c4ecc04d4d50b46c7dcda28bd3cd`)
- client: `quay.io/minio/mc:RELEASE.2025-08-13T08-35-41Z`

These are the same images the deleted Docker Hub repositories served, so entrypoints, the
`server /data --console-address ":9001"` argument, environment variables and the
`/minio/health/live` endpoint are all unchanged.

Purpose: unblock four red CI jobs and remove a floating-tag supply-chain dependency.
Output: pinned image references across CI, compose, devcontainer, k8s and docs; one new pending
todo.

Scope guard: this task changes image reference strings and adds explanatory comments. It changes
NO ports, NO credentials, NO health checks, NO commands or entrypoints, and NO Rust code.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@/workspace/CLAUDE.md
@.planning/todos/pending/2026-08-13-verify-local-coverage-reproduction.md

Environment facts established before planning — do not re-derive:
- Docker is NOT available in this devcontainer. The pinned images cannot be pulled or run here.
  Verification is static only (grep + YAML parse). Real verification is the CI run after push,
  which the orchestrator handles.
- `python3` with PyYAML is available (confirmed). `actionlint` is NOT installed (confirmed) —
  do not attempt to install it; skip that check and say so in the summary.
- The executor runs in a worktree and commits with `--no-verify`
  (`workflow.worktree_skip_hooks` is true). The orchestrator runs the pre-commit hook on the
  merged tree.
- Out of scope, do NOT edit: anything under `.planning/milestones/` (historical records), and
  `.github/actionlint.yaml` (its comment about the server image requiring an explicit `command`
  argument remains true after the pin).
</context>

<tasks>

<!-- planner-discipline-allow: minio/minio:latest -->
<!-- planner-discipline-allow: minio/mc:latest -->
<!-- planner-discipline-allow: image: minio/ -->
<!-- planner-discipline-allow: quay.io/minio/minio:RELEASE.2025-09-07T16-13-09Z.hotfix.7aa24e772 -->
<!-- planner-discipline-allow: quay.io/minio/mc:RELEASE.2025-08-13T08-35-41Z -->
<!-- The retired references above are named verbatim in <action> bodies on purpose: the task is a
     mechanical find-and-replace and the executor needs the exact strings to match on. The quay
     references are the values being written, checked by a POSITIVE gate (-eq 2), not a negative
     one. New inline comments must still never contain a retired reference verbatim. -->

<task type="auto">
  <name>Task 1: Pin live configuration to the quay.io last known-good release</name>
  <files>.github/workflows/ci.yml, docker/docker-compose.test.yml, docker/docker-compose.yml, .devcontainer/docker-compose.yml, k8s/minio.yaml</files>
  <read_first>
    .github/workflows/ci.yml lines 640-665 (integration-tests service block) and 1332-1360
    (coverage service block); docker/docker-compose.test.yml lines 18-45;
    docker/docker-compose.yml lines 22-50; .devcontainer/docker-compose.yml lines 90-100;
    k8s/minio.yaml lines 20-30.
  </read_first>
  <action>
    Replace the server image reference at exactly these five locations — ci.yml:647
    (integration-tests service), ci.yml:1339 (coverage service), docker/docker-compose.test.yml:19
    (minio-test), docker/docker-compose.yml:23 (minio), .devcontainer/docker-compose.yml:92
    (minio) — with `quay.io/minio/minio:RELEASE.2025-09-07T16-13-09Z.hotfix.7aa24e772`, and at
    k8s/minio.yaml:24 (Deployment container image) with the same reference. Replace the client
    image at docker/docker-compose.test.yml:41 (minio-test-init) and docker/docker-compose.yml:47
    (minio-init) with `quay.io/minio/mc:RELEASE.2025-08-13T08-35-41Z`. Line numbers are a
    locator, not an instruction to edit by offset — match on the image line's own text.

    Touch nothing else in these blocks. The `command: server /data --console-address ":9001"`
    keys, the `args:` list in the k8s Deployment, the `MINIO_ROOT_USER` / `MINIO_ROOT_PASSWORD` /
    `MINIO_CONSOLE_ADDRESS` values, the `9010:9000` / `9011:9001` / `9000:9000` / `9001:9001`
    port maps, the `/minio/health/live` health checks with their intervals and retries, the
    `minio-test-init` shell entrypoint, tmpfs and volume mounts, container names and the existing
    actionlint-suppression comments on the two `command:` keys all stay exactly as they are.

    Add a short YAML comment immediately above three of the new image lines — ci.yml's
    integration-tests service image, ci.yml's coverage service image, and
    docker/docker-compose.test.yml's minio-test image — at the same indentation as the `image:`
    key it precedes. Each comment states: the tag is pinned rather than floating because Docker
    Hub deleted the community MinIO repositories on 2026-09-12 and MinIO no longer publishes
    community-edition images; quay.io still serves this last known-good community release and no
    newer community tag will appear; the server manifest digest is
    `sha256:cf3dadcfa1fb0324f43958bad1abba986d53c4ecc04d4d50b46c7dcda28bd3cd`; and the long-term
    replacement is tracked in
    `.planning/todos/pending/2026-09-13-evaluate-rustfs-replacement-for-minio.md`.

    Comment-text constraint, load-bearing: the verification greps for the retired reference
    strings and expects zero hits repo-wide. Write the comments so they do NOT contain the old
    Docker Hub references verbatim — say "the community MinIO repositories" or "the former Docker
    Hub repositories" in prose. Never write a retired reference (repository name followed by a
    colon and the floating tag name) inside a comment, and never leave a bare `image:` key whose
    value starts with the old unqualified repository namespace.

    Do NOT commit yet — Task 2 shares this commit.
  </action>
  <verify>
    <automated>cd /workspace &amp;&amp; python3 -c "import yaml; [list(yaml.safe_load_all(open(f))) for f in ['.github/workflows/ci.yml','docker/docker-compose.test.yml','docker/docker-compose.yml','.devcontainer/docker-compose.yml','k8s/minio.yaml']]; print('yaml ok')" &amp;&amp; test "$(grep -rnI --exclude-dir=.planning --exclude-dir=.git --exclude-dir=target --exclude-dir=book -e 'minio/minio:latest' -e 'minio/mc:latest' -e 'image: minio/' .github .devcontainer/docker-compose.yml docker k8s | wc -l)" -eq 0 &amp;&amp; test "$(grep -rc 'quay.io/minio/minio:RELEASE.2025-09-07T16-13-09Z.hotfix.7aa24e772' .github/workflows/ci.yml)" -eq 2 &amp;&amp; echo LIVE_CONFIG_CLEAN</automated>
  </verify>
  <done>All six server-image locations and both client-image locations name a quay.io repository at
  the pinned release tag; all five edited YAML files still parse; the three explanatory comments
  are present; `git diff` shows no change to any port, credential, health check, command, args or
  entrypoint line.</done>
</task>

<task type="auto">
  <name>Task 2: Align user-facing docs with the shipped pinned reference</name>
  <files>.devcontainer/CI-CD.md, docs/src/deployment/cicd.md, docs/src/deployment/docker.md, docs/src/appendix/integration-tests.md, docs/src/appendix/minio-file-repository-setup.md, docs/src/contributing/testing-guide.md, docs/src/contributing/branching-model.md</files>
  <action>
    Minimal edits, one token swap per line, so the docs quote what the repo actually ships. Do not
    restructure prose, do not add sections, do not reflow surrounding lines.

    Swap the retired server reference for
    `quay.io/minio/minio:RELEASE.2025-09-07T16-13-09Z.hotfix.7aa24e772` at:
    `.devcontainer/CI-CD.md` lines 86, 232, 327 and 508 (an Actions service block, a GitLab CI
    `services:` list item, a CircleCI `docker:` list item, and a second Actions service block);
    `docs/src/deployment/cicd.md:302`; `docs/src/deployment/docker.md:419`;
    `docs/src/appendix/integration-tests.md:177` (a table cell — keep the surrounding pipes and
    backticks); `docs/src/appendix/minio-file-repository-setup.md` lines 864 and 868.

    At `docs/src/appendix/minio-file-repository-setup.md` lines 52 and 501 the reference is the
    image argument of a `docker run` line that ends `server /data --console-address ":9001"` —
    replace only the unqualified repository token with the fully qualified pinned reference and
    leave the trailing server arguments untouched.

    At `docs/src/contributing/testing-guide.md:269` the reference is a two-argument
    `GenericImage::new` call taking repository and tag as separate string literals — make the
    first argument the quay.io repository path and the second the pinned release tag; keep it a
    two-argument call. At `docs/src/contributing/testing-guide.md:653` the value is a bare,
    untagged `image:` key inside an example Actions service block — give it the fully qualified
    pinned reference.

    At `docs/src/contributing/branching-model.md:49` the retired reference appears inside a table
    cell as an EXAMPLE of a floating service-container tag needing drift detection. Do not delete
    the clause and do not simply swap the token — the sentence's premise no longer holds for
    MinIO. Reword that clause so it reads that the object-storage image is now pinned to an
    explicit quay.io release tag and therefore no longer floats, leaving `redis:7-alpine` as the
    remaining floating tag the revisit condition would cover. Keep it one table cell on one line,
    keep the pipes intact, and keep the rest of the row's meaning unchanged.

    Same comment-text constraint as Task 1: no retired reference string may survive anywhere in
    these files, including inside prose or an explanatory aside.

    Then commit Tasks 1 and 2 together as a single conventional commit:
    `fix(ci): pin MinIO images to quay.io last known-good release`, with body lines explaining the
    Docker Hub community-repository removal on 2026-09-12, the four CI jobs it broke, the two
    pinned references, and `Related to quick task 260913-15w`. Commit with `--no-verify` per the
    worktree convention. Use `-m` flags, one per paragraph.
  </action>
  <verify>
    <automated>cd /workspace &amp;&amp; test "$(grep -rnI --exclude-dir=.planning --exclude-dir=.git --exclude-dir=target --exclude-dir=book -e 'minio/minio:latest' -e 'minio/mc:latest' -e 'image: minio/' -e 'minio/minio server' -e 'GenericImage::new("minio/minio"' . | wc -l)" -eq 0 &amp;&amp; test "$(grep -rn 'quay.io/minio' docs/src .devcontainer/CI-CD.md | wc -l)" -ge 11 &amp;&amp; grep -q 'official `minio/minio`' .github/actionlint.yaml &amp;&amp; echo DOCS_CLEAN</automated>
  </verify>
  <done>Repo-wide (excluding `.planning/`, `.git/`, `target/` and the generated `docs/book/`) there
  are zero retired MinIO references; `.github/actionlint.yaml` is untouched; the branching-model
  row reads correctly as "now pinned" rather than as a floating-tag example; one commit exists
  covering Tasks 1 and 2.</done>
</task>

<task type="auto">
  <name>Task 3: Capture the RustFS replacement evaluation as a pending todo</name>
  <files>.planning/todos/pending/2026-09-13-evaluate-rustfs-replacement-for-minio.md</files>
  <read_first>
    .planning/todos/pending/2026-08-13-verify-local-coverage-reproduction.md — copy its
    frontmatter shape exactly (`created`, `title`, `area`, `severity`, `files`) and its
    `## Problem` / `## Solution` section structure.
  </read_first>
  <action>
    Create the new todo file. Frontmatter: `created` as a `2026-09-13T` ISO-8601 UTC timestamp
    (unquoted, like the reference file); `title` "Evaluate replacing MinIO with RustFS in the
    dev/test stack"; `area` `infrastructure`; `severity` `major`; `files` listing
    `docker/docker-compose.test.yml`, `docker/docker-compose.yml`, `.github/workflows/ci.yml`,
    `k8s/minio.yaml`, and `crates/paladin-storage`.

    `## Problem`: state that Docker Hub deleted the community MinIO server and client
    repositories on 2026-09-12, that this red-lined four CI jobs at the container-initialization
    step with no first-party code at fault, and that quick task 260913-15w restored green by
    pinning to the last known-good quay.io community release. Make the residual risk explicit:
    that pin is terminal, not a maintenance path — no newer community tag will ever be published,
    so the dev/test stack and the Kubernetes smoke test now depend on a frozen, unmaintained
    third-party image that will drift further from upstream and will never receive a security
    fix. Record that quay.io could itself retire the repository, which would reproduce the same
    outage with no remaining fallback.

    `## Solution`: propose evaluating RustFS — an S3-compatible, Rust-native object store — as
    the dev/test object-storage backend. Sketch the shape: a new file-storage adapter in
    `crates/paladin-storage` implementing the existing `FileStoragePort` alongside the MinIO/S3
    adapter, behind its own Cargo feature flag so neither backend is forced on consumers;
    adapter-parity integration tests exercised against a RustFS service container; then swap the
    compose and CI service definitions over once parity holds. Note the hexagonal constraint that
    this is an infrastructure-adapter change only and must not reach into core or ports, and the
    TDD/coverage obligation (82% workspace line floor, ADR-0006) for the new adapter. List the
    open questions to answer during evaluation: RustFS S3 API surface coverage versus what
    `paladin-storage` actually calls, multi-arch container availability and release cadence,
    project maturity and licence acceptability under `cargo-deny` policy, and whether the
    production k8s manifest should follow the dev/test stack or stay on a separately sourced
    S3-compatible service.

    State that this item deliberately carries no `resolves_phase` tag — it is expected to outlive
    the current milestone and must not be silently closed. Owner: repo maintainer.

    Commit separately: `docs(todo): capture RustFS evaluation as MinIO replacement`, body
    referencing quick task 260913-15w and the image pin it follows from. Commit with
    `--no-verify`.
  </action>
  <verify>
    <automated>cd /workspace &amp;&amp; test -f .planning/todos/pending/2026-09-13-evaluate-rustfs-replacement-for-minio.md &amp;&amp; python3 -c "import sys,yaml; t=open('.planning/todos/pending/2026-09-13-evaluate-rustfs-replacement-for-minio.md').read(); assert t.startswith('---'); fm=yaml.safe_load(t.split('---')[1]); assert set(['created','title','area','severity','files']) <= set(fm), fm.keys(); assert '## Problem' in t and '## Solution' in t; assert 'RustFS' in t and 'resolves_phase' in t; print('todo ok')"</automated>
  </verify>
  <done>The todo exists with the reference file's five frontmatter keys, parses as YAML frontmatter,
  contains `## Problem` and `## Solution`, names RustFS and `crates/paladin-storage`, states the
  no-`resolves_phase` intent, and is committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| third-party container registry -> CI runner / dev machine | Image bytes fetched from Docker Hub or quay.io execute with network access to the test workspace and its credentials |
| repository config -> Kubernetes smoke-test cluster | `k8s/minio.yaml` names the image a cluster workload runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-quick-SC | Tampering | service-container image references in ci.yml, docker/*.yml, .devcontainer/docker-compose.yml, k8s/minio.yaml | high | mitigate | Replace the floating community tag with an explicit immutable release tag on a registry that still serves it; record the server manifest digest inline in ci.yml so the exact artifact can be re-verified or promoted to digest pinning later |
| T-quick-02 | Denial of Service | CI jobs Coverage / Integration Tests / Docker Integration Tests / Kubernetes Smoke Test | high | mitigate | The outage is registry-side repository deletion; pinning to a live registry restores availability. Residual risk (quay.io could also retire the repository) is transferred to the tracked RustFS evaluation todo rather than left implicit, and failure mode stays loud — a pull error, never a silent pass |
| T-quick-03 | Elevation of Privilege | pinned image is frozen and will receive no upstream security fixes | medium | accept | Accepted for the dev/test stack only: containers run with test-only credentials on ephemeral tmpfs data, and no production workload depends on this plan's dev/test definitions. Acceptance has a stated exit path — the RustFS replacement todo from Task 3 |
| T-quick-04 | Information Disclosure | MinIO root credentials in compose/CI config | low | accept | Out of scope and unchanged by this task; the existing test-only `testuser`/`testpass123` values are neither read nor rewritten here |

No package-manager installs (npm/pip/cargo) are introduced by this plan, so the package
legitimacy gate does not apply. The only new external artifacts are two container images whose
registry, repository, tag and server digest were verified before planning.
</threat_model>

<verification>
Static only — Docker is unavailable in this environment, so no image can be pulled or started
here. Run from `/workspace`:

1. Zero retired references remain outside the excluded trees:
   `grep -rnI --exclude-dir=.planning --exclude-dir=.git --exclude-dir=target --exclude-dir=book -e 'minio/minio:latest' -e 'minio/mc:latest' -e 'image: minio/' .`
   must print nothing.
2. Every edited YAML file still parses (PyYAML confirmed present):
   `python3 -c "import yaml; [list(yaml.safe_load_all(open(f))) for f in ['.github/workflows/ci.yml','docker/docker-compose.test.yml','docker/docker-compose.yml','.devcontainer/docker-compose.yml','k8s/minio.yaml']]; print('yaml ok')"`
3. `git diff` contains no change to a port mapping, credential, health check, `command:`, `args:`
   or `entrypoint:` line, and no `.rs` file appears in the diff at all.
4. `.github/actionlint.yaml` and everything under `.planning/milestones/` are absent from the
   diff.
5. `actionlint` is not installed here — skip it and record that explicitly in the summary rather
   than reporting it as passed.

Real verification is the post-push CI run (the four previously red jobs going green); the
orchestrator owns that step.
</verification>

<success_criteria>
- Six server-image and two client-image references across CI, compose, devcontainer and k8s name
  `quay.io/minio/minio:RELEASE.2025-09-07T16-13-09Z.hotfix.7aa24e772` and
  `quay.io/minio/mc:RELEASE.2025-08-13T08-35-41Z` respectively.
- Three inline comments explain the pin and point at the RustFS todo.
- Twelve doc references across seven files quote the pinned reference; the branching-model row is
  reworded rather than token-swapped.
- The RustFS todo exists in `.planning/todos/pending/` in the established frontmatter format.
- Two commits total: `fix(ci): pin MinIO images to quay.io last known-good release` and
  `docs(todo): capture RustFS evaluation as MinIO replacement`, both referencing quick task
  260913-15w.
- No Rust code, ports, credentials, health checks or commands changed.
</success_criteria>

<output>
Create `.planning/quick/260913-15w-pin-minio-service-image-to-quay-io-last-/260913-15w-SUMMARY.md`
when done. Record in it: that Docker was unavailable so verification was static, that `actionlint`
was not installed and was skipped (not passed), and the exact grep/YAML-parse results.
</output>
