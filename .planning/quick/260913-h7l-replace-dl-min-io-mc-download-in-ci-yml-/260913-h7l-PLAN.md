---
phase: quick-260913-h7l
plan: 01
type: execute
wave: 1
depends_on: []
quick_id: 260913-h7l
autonomous: true
requirements: [QUICK-260913-h7l]
files_modified:
  - .github/workflows/ci.yml
  - .planning/todos/pending/2026-09-13-evaluate-rustfs-replacement-for-minio.md

must_haves:
  truths:
    - "Both CI jobs that use the MinIO client (Integration Tests, Coverage) install `mc` from a source that still serves it, so the step that currently fails with a retired-download error completes and leaves `mc` on PATH."
    - "The installed `mc` is the same release the pinned quay.io client image carries, and the step fails loudly if the downloaded bytes do not match the published checksum."
    - "Nothing else in the workflow changes: service blocks, ports, credentials, bucket names, and the bucket-setup commands are byte-identical to before."
    - "A reader of ci.yml can see inline why the client binary is fetched this way and where the long-term replacement is tracked."
    - "The RustFS replacement todo records that the client binaries are gone too, not just the server/client images."
  artifacts:
    - .github/workflows/ci.yml
    - .planning/todos/pending/2026-09-13-evaluate-rustfs-replacement-for-minio.md
  key_links:
    - "ci.yml 'Install MinIO Client' (integration-tests job) -> pinned GitHub release asset + its published sha256 (the step that fails first today)"
    - "ci.yml 'Install MinIO Client' (coverage job) -> the identical pinned asset + sha256"
    - "each 'Install MinIO Client' step -> the immediately following 'Setup MinIO buckets' step, which requires `mc` resolvable on PATH at /usr/local/bin/mc"
    - "inline rationale comments -> .planning/todos/pending/2026-09-13-evaluate-rustfs-replacement-for-minio.md"
---

<objective>
Quick task 260913-15w repointed every MinIO **image** reference to quay.io, which turned Docker
Integration Tests and Kubernetes Smoke Test green (run 34730155168, commit `ceca3505`). Coverage and
Integration Tests still fail — one step later, at "Install MinIO Client", which downloads the `mc`
binary from MinIO's former community download host. That host now answers `HTTP 410 Gone` for both
the binary and the archive path: MinIO retired community binary downloads entirely, not just the
Docker Hub repositories.

Replace both "Install MinIO Client" steps with a checksum-verified fetch of the **same** `mc`
release the pinned quay.io client image already carries, sourced from the archived
`github.com/minio/mc` repository's release assets (release assets on archived repositories remain
downloadable). The decision is made and closed — do not re-open the source or the release choice.

This is one vertical slice in a single task: the two CI steps are the only thing standing between a
red job and a green one, and the same edit carries its own static verification. Real end-to-end
verification is the post-push CI run, owned by the orchestrator.

Pinned artifact (downloaded and executed by the orchestrator on 2026-09-13 — HTTP 200,
30,535,864 bytes, `mc version RELEASE.2025-08-13T08-35-41Z
(commit-id=7394ce0dd2a80935aded936b09fa12cbb3cb8096)`):

- Release tag: `RELEASE.2025-08-13T08-35-41Z`
- Asset URL: `https://github.com/minio/mc/releases/download/RELEASE.2025-08-13T08-35-41Z/mc.linux-amd64.RELEASE.2025-08-13T08-35-41Z`
- sha256: `01f866e9c5f9b87c2b09116fa5d7c06695b106242d829a8bb32990c00312e891` (matches the release's
  own published `.sha256sum` asset)

Purpose: unblock the last two red CI jobs without changing a single line of Rust, a port, a
credential, or a bucket name.
Output: two rewritten `Install MinIO Client` steps in `.github/workflows/ci.yml`, plus one appended
sentence in the RustFS replacement todo.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.github/workflows/ci.yml
@.planning/quick/260913-15w-pin-minio-service-image-to-quay-io-last-/260913-15w-SUMMARY.md
@.planning/todos/pending/2026-09-13-evaluate-rustfs-replacement-for-minio.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fetch the pinned mc release from GitHub in both CI jobs, checksum-verified</name>

  <files>.github/workflows/ci.yml, .planning/todos/pending/2026-09-13-evaluate-rustfs-replacement-for-minio.md</files>

  <precondition>`python3 -c "import yaml"` succeeds in this environment (PyYAML was used successfully by quick task 260913-15w's verification); if it fails, install PyYAML or substitute an equivalent YAML parser before running the verify block — do not skip the parse.</precondition>

  <read_first>
    `.github/workflows/ci.yml` around lines 686-697 (integration-tests job) and 1393-1404 (coverage
    job). The two `Install MinIO Client` steps are byte-identical three-line `run:` bodies, so a
    naive single-match edit will fail — target each one with surrounding context, or use
    `replace_all` deliberately. The prior quick task hit exactly this and resolved it mechanically;
    see its SUMMARY "Deviations from Plan".
  </read_first>

  <action>
    Rewrite both `Install MinIO Client` steps in `.github/workflows/ci.yml` — the one in the
    `integration-tests` job and the identical one in the `coverage` job — so they are identical to
    each other afterwards. Change nothing else anywhere in the workflow.

    Above each `- name: Install MinIO Client` line, at the same indentation as that line, add a
    six-line YAML comment block stating, in this order: that MinIO retired its community binary
    downloads and that its former community download host answered `HTTP 410 Gone` for the `mc`
    binary on 2026-09-13; that the binary is now fetched from the archived `github.com/minio/mc`
    repository's release assets (assets on archived repositories remain downloadable), pinned to the
    same release the quay.io client image from quick task 260913-15w already carries; that the
    checksum is the one published as that release's own `.sha256sum` asset; and that the long-term
    replacement is tracked in
    `.planning/todos/pending/2026-09-13-evaluate-rustfs-replacement-for-minio.md`. Do **not** write
    the retired host's bare domain literal into the file — name it descriptively ("MinIO's former
    community download host"), because a completion gate greps the tree for zero occurrences of that
    literal and a comment mentioning it would self-invalidate the gate.

    Replace each step's `run:` body with exactly these seven lines, in this order, as a block
    scalar:
    line 1 `set -euo pipefail`;
    line 2 `MC_RELEASE="RELEASE.2025-08-13T08-35-41Z"`;
    line 3 `MC_SHA256="01f866e9c5f9b87c2b09116fa5d7c06695b106242d829a8bb32990c00312e891"`;
    line 4 a `curl -fsSL -o mc` invocation whose single URL argument is
    `"https://github.com/minio/mc/releases/download/${MC_RELEASE}/mc.linux-amd64.${MC_RELEASE}"`
    (a line continuation before the URL is fine, and keeps the line under 100 columns);
    line 5 `echo "${MC_SHA256}  mc" | sha256sum -c -` — note the TWO spaces between the hash and the
    filename, which is the format `sha256sum -c` requires, and note that this line must fail the
    step on mismatch rather than warn;
    line 6 `chmod +x mc`;
    line 7 `sudo mv mc /usr/local/bin/mc`.

    `-f` is load-bearing: it makes `curl` fail on a non-2xx response so an HTML error page can never
    be made executable and moved onto PATH. `-L` is required because GitHub release assets redirect
    to a CDN host; no credential header is sent on this request, so following the redirect forwards
    nothing sensitive — unlike the LLM adapters' credential-bearing clients, which deliberately
    refuse redirects.

    Leave the following `Setup MinIO buckets` step untouched in both jobs — its alias, endpoint,
    credentials and two bucket-creation commands stay byte-identical. Do not touch either job's
    service block, image references, port mappings, environment variables, health checks, or
    `command:` lines, and do not touch any other workflow, any compose file, `k8s/`, or any Rust
    source.

    Finally, append exactly ONE sentence to the end of the `## Problem` section of
    `.planning/todos/pending/2026-09-13-evaluate-rustfs-replacement-for-minio.md` (before
    `## Solution`), recording that MinIO's community binary downloads are gone as well as its Docker
    Hub images — the former download host now returns `410 Gone` for the client binary — so quick
    task 260913-h7l additionally pinned the CI client install to a checksum-verified release asset on
    the archived `github.com/minio/mc` repository. Change nothing else in that file; do not add a
    `resolves_phase` tag.
  </action>

  <verify>
    <automated>
      Run from the repository root (worktree root is fine):

      `python3 -c "import yaml,sys; list(yaml.safe_load_all(open('.github/workflows/ci.yml'))); print('YAML_OK')"`

      then, capturing counts with `|| true` so a zero count does not abort the shell:
      - `grep -rnIE --exclude-dir=.git --exclude-dir=.planning --exclude-dir=target --exclude-dir=book 'dl\.min\.io' . | wc -l` must be `0`
      - `grep -cF '01f866e9c5f9b87c2b09116fa5d7c06695b106242d829a8bb32990c00312e891' .github/workflows/ci.yml` must be `2`
      - `grep -cF 'github.com/minio/mc/releases/download' .github/workflows/ci.yml` must be `2`
      - `grep -cF 'name: Install MinIO Client' .github/workflows/ci.yml` must be `2`
      - `grep -cF 'mc alias set testminio http://localhost:9010 testuser testpass123' .github/workflows/ci.yml` must be `2`
      - `grep -cF 'mc mb testminio/integration-tests --ignore-existing' .github/workflows/ci.yml` must be `2`
      - `grep -cF 'mc mb testminio/test-bucket --ignore-existing' .github/workflows/ci.yml` must be `2`

      Then extract and syntax-check both step bodies into a `mktemp -d` directory:
      parse `.github/workflows/ci.yml` with PyYAML, walk every job's `steps`, write the `run` body of
      each step named `Install MinIO Client` to a separate file, assert exactly 2 were extracted,
      run `bash -n` on each (must exit 0), and `diff` the two files (must be identical).

      Then scope the diff:
      - `git diff --name-only | sort` must list exactly `.github/workflows/ci.yml` and
        `.planning/todos/pending/2026-09-13-evaluate-rustfs-replacement-for-minio.md`
      - `git diff -U0 -- .github/workflows/ci.yml | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)' | grep -cE 'image:|9010|9011|MINIO_ROOT|testuser|testpass123|mc alias set|mc mb ' || true` must be `0`
        (no changed line touches an image reference, a port, a credential, or a bucket command)

      Print `CI_MC_PINNED_OK` only when every assertion above holds.

      `actionlint` is not installed in this environment — record it as SKIPPED in the SUMMARY, not
      as passed. Docker is unavailable, so no `mc` download or MinIO round-trip is attempted here;
      real verification is the post-push CI run, owned by the orchestrator.
    </automated>
  </verify>

  <done>
    `CI_MC_PINNED_OK` printed. `ci.yml` parses as YAML; zero occurrences of the retired download host
    remain outside `.planning/`; the pinned sha256 literal and the GitHub release-download URL each
    appear exactly twice; both `Install MinIO Client` step bodies are byte-identical to each other
    and pass `bash -n`; both `Setup MinIO buckets` steps are unchanged; the diff touches only
    `.github/workflows/ci.yml` and the RustFS todo, with no changed line carrying an image
    reference, port, credential, or bucket command; and the todo's `## Problem` section gained
    exactly one new sentence.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub release CDN -> CI runner filesystem | A third-party binary is downloaded and made executable on a runner that later holds no secrets for this job but does run the full test suite |
| CI runner -> MinIO service container | Existing, unchanged: fixed test credentials over loopback only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-h7l-01 | Tampering | `Install MinIO Client` step (both jobs) — third-party binary made executable on the runner | high | mitigate | sha256 pinned to the exact digest published as the release's own `.sha256sum` asset and independently reproduced by the orchestrator; `sha256sum -c -` exits non-zero on mismatch, failing the step before `chmod +x`/`sudo mv`, so substituted bytes never reach PATH |
| T-h7l-02 | Spoofing | HTTPS transport for the asset fetch | medium | mitigate | `curl -fsSL` over HTTPS with certificate validation at defaults; `-f` fails the step on any non-2xx so an error page cannot be executed. `-L` is required (release assets redirect to a CDN host) and is safe here because no credential header is attached — the redirect-refusal rule in `security.instructions.md` governs credential-bearing clients, which this is not |
| T-h7l-03 | Denial of Service | Archived `github.com/minio/mc` release assets could be withdrawn | medium | accept | Assets on archived repositories remain downloadable and no second source exists to pin against; recurrence risk is exactly what the RustFS replacement todo tracks, and this plan appends the client-binary half of that problem statement to it |
| T-h7l-04 | Information Disclosure | Test credentials in the unchanged `Setup MinIO buckets` step | low | accept | Pre-existing, out of scope: fixed non-production credentials bound to a loopback service container; this change does not touch them |
| T-h7l-SC | Tampering | Supply chain — package-manager installs | low | mitigate | No npm/pip/cargo package is added by this change, so no Package Legitimacy Audit is triggered; the sole fetched artifact is the `mc` binary covered by T-h7l-01 |
</threat_model>

<verification>
Static only in this environment (no Docker, no `actionlint`):

1. `.github/workflows/ci.yml` parses cleanly with PyYAML.
2. Zero references to the retired MinIO download host remain anywhere outside `.planning/`.
3. The pinned sha256 and the GitHub release-download URL each appear exactly twice in `ci.yml` — one
   per job — and the two extracted step bodies are byte-identical and `bash -n`-clean.
4. The bucket-setup commands, ports, credentials, and service blocks are untouched in the diff.
5. `actionlint`: SKIPPED (not installed) — must be recorded as skipped, never as passed.

Real verification is the CI run after push: the Coverage and Integration Tests jobs must clear
"Install MinIO Client" and "Setup MinIO buckets". The orchestrator owns that run.
</verification>

<success_criteria>
- Both `Install MinIO Client` steps fetch `mc` `RELEASE.2025-08-13T08-35-41Z` from the archived
  `github.com/minio/mc` release assets and verify `01f866e9c5f9b87c2b09116fa5d7c06695b106242d829a8bb32990c00312e891`
  before installing to `/usr/local/bin/mc`.
- A checksum mismatch or a non-2xx response fails the job instead of installing a bad binary.
- `.github/workflows/ci.yml` and the RustFS todo are the only two files changed.
- Exactly one commit: `fix(ci): fetch pinned mc release from GitHub after dl.min.io retired downloads`,
  body referencing quick task 260913-h7l.
</success_criteria>

<output>
Create `.planning/quick/260913-h7l-replace-dl-min-io-mc-download-in-ci-yml-/260913-h7l-SUMMARY.md` when done.
</output>
