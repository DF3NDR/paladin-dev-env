---
phase: quick-260916-h40
plan: 01
type: execute
wave: 1
depends_on: []
quick_id: 260916-h40
files_modified:
  - .project/current-exports.txt
  - CHANGELOG.md
  - .pre-commit-config.yaml
  - Makefile
  - CLAUDE.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "`./scripts/check-api-surface.sh .project/current-exports.txt` exits 0 and prints `✅ API surface unchanged` at HEAD, so CI's `API Surface Tracking` job goes green."
    - "CHANGELOG.md's `[Unreleased] / ### Added` section names the five new `paladin` facade re-exports from Phase 32."
    - "A push that touches Rust sources or a Cargo manifest runs the API-surface check locally before it reaches CI."
    - "A docs-only push does not pay the ~95 s API-surface cost."
    - "`make api-surface` and `make api-surface-update` exist, appear in `make help`, and are the documented way to check/refresh the baseline."
  artifacts:
    - .project/current-exports.txt
    - CHANGELOG.md
    - .pre-commit-config.yaml
    - Makefile
    - CLAUDE.md
  key_links:
    - "`.pre-commit-config.yaml` `check-api-surface` hook -> `./scripts/check-api-surface.sh .project/current-exports.txt` (same command `ci.yml`'s `api-surface` job runs)"
    - "`Makefile` `api-surface-update` -> `./scripts/extract-public-api.sh .project/current-exports.txt` (same command the CI failure message tells you to run)"
    - "`CLAUDE.md` Working agreements checklist -> `make api-surface`"
---

<objective>
Fix the red `API Surface Tracking` CI job (run 35052721223) by regenerating the committed public
API baseline for Phase 32's five additive `paladin` facade re-exports, then stop the same drift
from reaching CI again by gating it at the pre-push hook stage.

Purpose: The baseline was last regenerated in `786a3ba5` (Commissary exports). Phase 32 commit
`52b7447a` added `ResolvedWindow`, `UnknownContextWindow`, `WindowFallbackPolicy`, `WindowSource`
and `resolve_context_window` at `src/lib.rs` ~210-211, moving the count 3944 -> 3949. The drift is
purely ADDITIVE (no removals), so it is not a breaking change — the CHANGELOG entry belongs under
`[Unreleased] / ### Added`, not under a breaking-changes heading. The recurrence is structural:
no close-out plan runs `scripts/check-api-surface.sh`, so the baseline only ever gets caught by CI.

Output: a regenerated baseline + CHANGELOG entry (commit 1), and a pre-push `check-api-surface`
hook plus `make api-surface` / `make api-surface-update` targets and a CLAUDE.md checklist line
(commit 2).
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@scripts/check-api-surface.sh
@scripts/extract-public-api.sh
@.pre-commit-config.yaml

Established facts — do NOT re-investigate:
- The drift is exactly the five additive re-exports listed above. `cargo-public-api 0.52.0` and the
  nightly toolchain are installed; the check takes ~95 s wall-clock locally.
- GSD executors commit with `--no-verify` (`workflow.worktree_skip_hooks=true`), so a pre-COMMIT
  hook would never fire for them. pre-push is the layer that actually catches this before CI.
- No `CONTRIBUTING.md` exists (the pre-commit config's comment references one that isn't there).
  Do NOT create one in this task.
- `.claude/` is vendored upstream GSD tooling — do NOT edit anything under it.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate the public API baseline for the Phase 32 window exports</name>
  <files>.project/current-exports.txt, CHANGELOG.md</files>
  <precondition>`cargo-public-api` is on PATH and a nightly toolchain is installed — assert with `command -v cargo-public-api && rustup toolchain list | grep -q nightly`; halt if either is missing rather than letting `extract-public-api.sh` silently start a `cargo install`.</precondition>
  <action>
Regenerate the committed baseline and record the additive change.

1. Run `./scripts/extract-public-api.sh .project/current-exports.txt` from the repo root (it takes
   ~95 s). The script rewrites the header timestamp, the `cargo-public-api` version line, the
   normalized item list and the `Total public items:` summary. Expect the summary to read 3949.
2. Inspect `git diff .project/current-exports.txt` and confirm the item-level change is exactly the
   five ADDED lines (`pub use paladin::ResolvedWindow`, `pub use paladin::UnknownContextWindow`,
   `pub use paladin::WindowFallbackPolicy`, `pub use paladin::WindowSource`,
   `pub use paladin::resolve_context_window`) plus the regenerated header/summary lines. If the
   diff contains any REMOVED `pub ` line, stop and report it — that would be a breaking change and
   this plan's CHANGELOG placement would be wrong.
3. Add one bullet to the EXISTING `## [Unreleased]` -> `### Added` section of `CHANGELOG.md`,
   directly after the Commissary bullet and modeled on its wording and wrapping. Name the shared
   context-window resolution types as re-exported from the `paladin` facade, list all five items in
   backticks, and cite their source module `paladin_llm::window`. Do not create a new heading, do
   not touch the `## [0.10.0]` section, and do not describe this as breaking.
4. Commit both files together with the message
   `chore(api): regenerate public API baseline for Phase 32 window exports`.
  </action>
  <verify>
    <automated>cd /workspace &amp;&amp; ./scripts/check-api-surface.sh .project/current-exports.txt | tee /tmp/api-check.txt &amp;&amp; grep -q "API surface unchanged" /tmp/api-check.txt &amp;&amp; grep -q "^Total public items: 3949$" .project/current-exports.txt &amp;&amp; for s in ResolvedWindow UnknownContextWindow WindowFallbackPolicy WindowSource resolve_context_window; do grep -q "pub use paladin::$s$" .project/current-exports.txt || { echo "missing baseline entry: $s"; exit 1; }; done &amp;&amp; awk '/^## \[Unreleased\]/{u=NR} /^## \[0\.10\.0\]/{r=NR} /resolve_context_window/{c=NR} END{ if (u&amp;&amp;c&amp;&amp;r&amp;&amp;c&gt;u&amp;&amp;c&lt;r) print "changelog entry in Unreleased"; else exit 1 }' CHANGELOG.md</automated>
  </verify>
  <done>`./scripts/check-api-surface.sh .project/current-exports.txt` exits 0 printing `✅ API surface unchanged`; the baseline summary reads `Total public items: 3949` and contains all five new `pub use paladin::*` window entries; `CHANGELOG.md` has a new `[Unreleased] / ### Added` bullet naming those five items, positioned between the `## [Unreleased]` and `## [0.10.0]` headings; both files committed as `chore(api): regenerate public API baseline for Phase 32 window exports`.</done>
</task>

<task type="auto">
  <name>Task 2: Gate public API surface drift at pre-push (hook + make targets + CLAUDE.md)</name>
  <files>.pre-commit-config.yaml, Makefile, CLAUDE.md</files>
  <action>
Three edits, then one commit.

(a) `.pre-commit-config.yaml` — append a new `local` hook to the existing `repos: - repo: local`
    hooks list, after `check-doc-config`, keeping the file's two-space list indentation and its
    comment style. The hook:
      - `id: check-api-surface`
      - `name: check public API surface vs baseline`
      - `entry: ./scripts/check-api-surface.sh .project/current-exports.txt`
      - `language: system`
      - `stages: [pre-push]`
      - `pass_filenames: false`
      - `files: ^(src|crates)/.*\.rs$|^Cargo\.toml$`
    Use `files:` scoping and deliberately NOT `always_run: true`, so a docs-only push skips the
    hook. Precede it with a short comment (matching the `--- Pre-push gate ---` banner style
    already in the file) stating the two reasons this sits at pre-push rather than pre-commit:
    GSD executor commits bypass commit-stage hooks, and the check costs ~95 s, which is tolerable
    once per push alongside the existing workspace build + lib tests but not on every commit.

(b) `Makefile` — add two targets immediately after the `openapi` target in the `##@ Code Quality`
    section, in that section's existing style (`.PHONY:` line, `##` help text on the target line,
    `@echo "$(CYAN)...$(NC)"` banner, tab-indented `@`-prefixed recipe lines):
      - `api-surface`: runs `./scripts/check-api-surface.sh .project/current-exports.txt`.
      - `api-surface-update`: runs `./scripts/extract-public-api.sh .project/current-exports.txt`,
        then echoes a `$(YELLOW)`-colored reminder to add a `CHANGELOG.md` entry for the changed
        public surface.

(c) `CLAUDE.md` — extend the existing Working-agreements bullet at line 67 that currently reads
    "**Before committing a parent task**: `cargo test` → `cargo fmt --check` → `cargo clippy`, then
    conventional-commit message." so the checklist chain gains a fourth step, `make api-surface`,
    with a parenthetical noting that an intentional public-surface change is refreshed with
    `make api-surface-update` plus a CHANGELOG entry. Keep the existing sentence about stopping
    after each major task, and keep the file's 100-column wrapping.

Do NOT create `CONTRIBUTING.md` and do NOT edit anything under `.claude/`.

Commit all three files with the message `build(hooks): gate public API surface drift at pre-push`.
  </action>
  <verify>
    <automated>cd /workspace &amp;&amp; pre-commit validate-config .pre-commit-config.yaml &amp;&amp; python3 -c "import sys,yaml; h=[x for r in yaml.safe_load(open('.pre-commit-config.yaml'))['repos'] if r['repo']=='local' for x in r['hooks'] if x['id']=='check-api-surface']; assert len(h)==1, h; k=h[0]; assert k['stages']==['pre-push'], k; assert k['pass_filenames'] is False, k; assert k.get('always_run') is not True, k; assert k['files']==r'^(src|crates)/.*\.rs\$|^Cargo\.toml\$', k['files']; assert k['entry']=='./scripts/check-api-surface.sh .project/current-exports.txt', k; print('hook ok')" &amp;&amp; make -n api-surface &amp;&amp; make -n api-surface-update &amp;&amp; make help | grep -q 'api-surface-update' &amp;&amp; grep -q 'make api-surface' CLAUDE.md &amp;&amp; pre-commit run --hook-stage pre-push check-api-surface --all-files</automated>
  </verify>
  <done>`pre-commit validate-config .pre-commit-config.yaml` passes and the parsed `check-api-surface` hook has `stages: [pre-push]`, `pass_filenames: false`, no `always_run`, the `^(src|crates)/.*\.rs$|^Cargo\.toml$` file scope and the exact check-script entry; `make -n api-surface` and `make -n api-surface-update` both resolve and both appear in `make help`; `CLAUDE.md`'s Working-agreements pre-commit checklist bullet names `make api-surface`; `pre-commit run --hook-stage pre-push check-api-surface --all-files` passes; all three files committed as `build(hooks): gate public API surface drift at pre-push`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer push -> CI | The new hook runs a repo-tracked script with repo-tracked arguments on the developer's machine at push time; no network input crosses it. |
| `extract-public-api.sh` -> toolchain | The script will `cargo install cargo-public-api` if the binary is absent, pulling from crates.io. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-h40-01 | Tampering | `.pre-commit-config.yaml` local hook `entry` | low | mitigate | Entry is a fixed, repo-tracked path with a literal baseline argument — identical to the command `ci.yml`'s `api-surface` job already runs. No interpolation of filenames (`pass_filenames: false`), so no push-controlled argument reaches the shell. |
| T-h40-02 | Tampering | implicit `cargo install cargo-public-api` inside `extract-public-api.sh` | low | accept | Pre-existing behavior of an already-shipped script; this task installs no new packages and adds no new dependency. Task 1's `<precondition>` asserts the binary is already present so the install path is not silently exercised. |
| T-h40-03 | Denial of Service | pre-push hook runtime | low | mitigate | Scoped with `files: ^(src\|crates)/.*\.rs$\|^Cargo\.toml$` instead of `always_run`, so docs-only pushes skip the ~95 s cost; `--no-verify` remains the documented emergency override. |
| T-h40-04 | Information Disclosure | `.project/current-exports.txt` | low | accept | Contents are the crate's already-public API signatures plus a generation timestamp — no credentials, paths or host identifiers beyond the tool version. |
</threat_model>

<verification>
1. `./scripts/check-api-surface.sh .project/current-exports.txt` exits 0 at HEAD.
2. `pre-commit validate-config .pre-commit-config.yaml` exits 0.
3. `pre-commit run --hook-stage pre-push check-api-surface --all-files` passes.
4. `git log --oneline -2` shows exactly the two commits named in Tasks 1 and 2, in that order.
5. `git status --porcelain` is clean; nothing under `.claude/` was modified
   (`git diff --name-only HEAD~2 | grep -c '^\.claude/'` reports 0).
</verification>

<success_criteria>
- CI's `API Surface Tracking` job would pass at HEAD (proven locally by the same script it runs).
- The five new facade re-exports are recorded in `[Unreleased] / ### Added`, not as a breaking change.
- The identical drift is now caught at pre-push, before it can reach CI, for any push that touches
  `src/`, `crates/` Rust sources or `Cargo.toml`.
- `make api-surface` / `make api-surface-update` are discoverable via `make help` and referenced
  from CLAUDE.md's pre-commit checklist.
- Two atomic commits; no `CONTRIBUTING.md` created; no edits under `.claude/`.
</success_criteria>

<output>
Create `.planning/quick/260916-h40-regenerate-api-surface-baseline-for-phas/260916-h40-SUMMARY.md` when done
</output>
