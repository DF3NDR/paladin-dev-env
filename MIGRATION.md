# Upgrading from v0.9.0 to v0.10.0

> **Scope note:** this file is the root Markdown deliverable required by `.project/v0.10.0/00-program-overview.md`
> §9. It is a **living document**, created in the first epic of the v0.10.0 program (Phase 22,
> `ENG-08`) and appended to by every later epic that touches an item below. The mdBook "Upgrading"
> page the program's Definition of Done (overview §5.4) also requires is **not** added by this
> phase — that is Phase 29 / `SHIP-01` scope. Every `TBD` below carries the requirement or phase
> that owns closing it; `SHIP-01` (Phase 29) is responsible for clearing every remaining `TBD`
> before the v0.10.0 release, per overview §9's living-document contract.

## 9.1 Behavioral changes (user-visible without code changes)

| ID | Change | Who is affected | Required user action |
|---|---|---|---|
| M-B-01 | **BUG-01 fix.** `EdgeCondition::Custom(name)` no longer evaluates to `true` when no evaluator is registered. Campaign/graph validation now fails with `BattalionError::InvalidGraph` naming each unregistered condition, before any node executes. | Any v0.9 workflow using `EdgeCondition::Custom`. Such workflows were routing *incorrectly* (always following the custom edge); after upgrade they fail loudly at validation. | Register an evaluator for each name via the new registry API (CF-FR-01), or replace the condition with `Contains`/`Regex`/`Always`. |
| M-B-02 | **Graceful shutdown.** On SIGTERM/SIGINT the process now waits up to `shutdown_grace` (default 30 s, env `APP_ENGINE_SHUTDOWN_GRACE_SECS`) for in-flight engine runs to halt before exiting — `paladin-server`'s `shutdown_signal` and `ServiceRunner::wait_for_shutdown` both cancel a shared `ShutdownCoordinator` and wait for registered runs to drain (HITL-04, D-21/D-22). | Operators; container orchestration. | Set `terminationGracePeriodSeconds` ≥ 2 × `shutdown_grace` — `k8s/server/deployment.yaml` and `k8s/deployment.yaml` ship `60` (landed, HITL-FR-15). Set `APP_ENGINE_GRACEFUL_SHUTDOWN=false` to restore the old no-wait behavior for legacy-only deployments (landed, HITL-04). |
| M-B-03 | **No behavioral change.** `tool_error_mode` (RT-FR-24) NAMES a policy the v0.9 loop already implemented, rather than introducing one: a failed Arsenal or handoff tool call was always appended to the accumulated output and the run continued (`paladin_execution_service.rs`'s Arsenal and handoff error-handling arms, unchanged since before this phase). This phase adds `FailRun` as a new opt-in (`PaladinError::ArmamentFailed { tool, reason }`) and sanitizes the fed-back text (redact-then-bound, D-34) before the model ever sees it — the sanitization is the ONLY observable difference for an existing user. | Users of the Arsenal/handoff tool loop. None from the policy itself; a leaked credential previously visible in a tool's raw error text is now redacted before the model sees it. | None required to keep today's behavior — `tool_error_mode` defaults to `FeedToModel`, byte-for-byte matching v0.9 apart from sanitization. Set `tool_error_mode = FailRun` (globally via `AgentRuntimeConfig.tool_errors.mode`, or per tool via `tool_errors.per_tool`) to opt into failing the run on a tool error instead. |
| M-B-04 | **Automatic per-superstep checkpointing.** Any graph executed through the new `WarEngine` (via `WarGraph`/`WarEngine::start`/`WarEngine::resume`) now writes one `Waypoint` — a **full `Battlefield` snapshot**, including whatever a workflow places in shared state (which may include raw LLM prompts and model outputs) — after every superstep, by default (`WaypointDurability::Strict`). The write goes to whichever `WaypointPort` backend the caller wires in: `InMemoryWaypointStore` (ungated, tests/dev) or, once landed, `SqliteWaypointStore`/`PostgresWaypointStore` (ENG-05) for durable deployments. Growth is bounded by `EngineLimits` (`max_supersteps` default 50, `max_node_visits` default 25) and, once configured, by `WaypointRetentionConfig` (`max_age_days`, `max_waypoints_per_thread` — see §9.5). | Any workflow author who adopts the new `WarEngine`/`WarGraph` APIs (ENG-01…ENG-08). | **Legacy `FormationExecutionService`, `PhalanxExecutionService`, `CampaignExecutionService`, and `Commander` execution paths are completely unaffected — they write no Waypoints and their behavior is byte-for-byte unchanged (ENG-FR-20).** A v0.9 workflow gains **no new persistence** unless it is explicitly rebuilt against the new engine. To adopt it: choose a `WaypointPort` backend, review `WaypointDurability` (default `Strict` fails the run on a write error; `BestEffort` downgrades to a logged warning), and configure `WaypointRetentionConfig` if snapshots — which may contain raw prompts/outputs — must not accumulate unbounded. |

**Worked examples** (owed alongside the rows above, per D-08's rule that a pending item is acceptable only in later-epic-owned content):

- M-B-01: **landed (CF-01, Phase 23).** A v0.9 campaign edge like

  ```rust
  // v0.9 — this edge silently fired on EVERY run, regardless of what
  // `analyzer` actually output, because `EdgeCondition::Custom` had no
  // registration mechanism and evaluated as always-true (BUG-01).
  campaign.add_edge(CampaignEdge::new(
      analyzer_id,
      escalate_id,
      EdgeCondition::Custom("is_urgent".to_string()),
  ))?;
  let service = CampaignExecutionService::new(paladin_port);
  ```

  now fails loudly at validation unless an evaluator is registered for
  `"is_urgent"`. The v0.10 fix registers one on the service (the same
  `CampaignEdge`/`EdgeCondition::Custom` construction is unchanged):

  ```rust
  use paladin_battalion::edge_evaluator::{EdgeConditionEvaluator, EdgeContext, EdgeEvaluatorError};

  struct IsUrgent;

  #[async_trait::async_trait]
  impl EdgeConditionEvaluator for IsUrgent {
      async fn evaluate(&self, output: &str, _ctx: &EdgeContext<'_>) -> Result<bool, EdgeEvaluatorError> {
          Ok(output.to_lowercase().contains("urgent"))
      }
  }

  let service = CampaignExecutionService::new(paladin_port)
      .with_evaluator("is_urgent", Arc::new(IsUrgent));
  ```

  The `WarEngine` path registers the same evaluator via
  `WarEngine::with_edge_evaluator("is_urgent", Arc::new(IsUrgent))`.
  A user who does not want an evaluator at all can instead replace the
  condition with `EdgeCondition::Contains("urgent".to_string())`,
  `EdgeCondition::Regex(...)`, or `EdgeCondition::Always` — no registry
  entry needed for any of the three. Skipping both routes produces this
  exact validation error, transcribed verbatim from the shipped code
  (`BattalionError::InvalidGraph`'s `Display`, legacy path; the
  `WarEngine` path's `EngineError::UnregisteredEdgeCondition` names the
  same offending set):

  ```text
  Invalid graph: unregistered custom edge condition(s): is_urgent -- register with CampaignExecutionService::with_evaluator before calling execute
  ```
- M-B-02: **landed (HITL-04, Phase 24).** Before this phase, SIGTERM/SIGINT exited the process
  immediately — any in-flight superstep run was dropped mid-flight with no chance to reach a
  Halted boundary, and neither shipped manifest declared `terminationGracePeriodSeconds`:

  ```yaml
  # v0.9 — k8s/server/deployment.yaml's pod spec (no termination grace declared;
  # the kubelet's own default of 30s applied, with no relationship to any
  # engine-level grace concept because none existed)
  spec:
    template:
      spec:
        containers:
          - name: paladin-server
            # ...
  ```

  After this phase, the process waits up to a configured grace window for
  in-flight runs to drain before exiting, and both shipped manifests declare a
  termination grace derived from — and at least twice — that window:

  ```yaml
  # v0.10 — k8s/server/deployment.yaml's pod spec
  spec:
    template:
      spec:
        # 2x the default APP_ENGINE_SHUTDOWN_GRACE_SECS (30s) so the kubelet's
        # SIGKILL deadline never lands mid-drain.
        terminationGracePeriodSeconds: 60
        containers:
          - name: paladin-server
            # ...
  ```

  Two new env vars (`EngineConfig`, `src/config/engine.rs`) control the process-level wait:

  | Env var | Default | Effect |
  |---|---|---|
  | `APP_ENGINE_SHUTDOWN_GRACE_SECS` | `30` | Seconds `paladin-server`/`ServiceRunner` wait, after SIGTERM/SIGINT, for in-flight superstep runs to finish before giving up on the stragglers. |
  | `APP_ENGINE_GRACEFUL_SHUTDOWN` | `true` | Set `false` to restore the v0.9 no-wait behavior for legacy-only deployments — the process cancels in-flight runs and exits immediately without waiting. |

  An operator upgrading with no config changes gets `shutdown_grace_secs=30`,
  `graceful_shutdown=true` and `terminationGracePeriodSeconds: 60` from the shipped
  manifests — a strictly safer default than v0.9's immediate exit, not a behavior an operator
  must opt into. A run still executing when SIGTERM arrives either finishes inside the grace
  window (Waypoint records completion normally) or is aborted at the deadline (`NodeOutcomeKind::
  Skipped { reason: "shutdown" }`, re-listed in the Halted Waypoint's vanguard for exactly-once
  resume) — no in-flight work silently vanishes either way.
- M-B-03: **landed (RT-07, Phase 26).** No behavioral change — the v0.9 loop already fed a failed
  tool call back into the model's context and continued; this phase names that policy
  (`tool_error_mode`, default `FeedToModel`), adds `FailRun` as a new opt-in
  (`PaladinError::ArmamentFailed { tool, reason }`), and sanitizes the fed-back text. The
  sanitization is the only observable difference:

  ```text
  // v0.9 / v0.10 default (FeedToModel) -- BEFORE this phase, an upstream
  // gateway's raw error text (including a leaked credential) reached the
  // model verbatim:
  🔧 Tool Execution: fetch_report
  Result: FAILED
  Error: upstream gateway rejected the request: Authorization: Bearer sk-live-abcdef0123456789

  // v0.10 -- the identical failure, sanitized (redact-then-bound, D-34)
  // before the model ever sees it:
  🔧 Tool Execution: fetch_report
  Result: FAILED
  Error: upstream gateway rejected the request: Authorization: Bearer [REDACTED]
  You may retry with corrected arguments or proceed without it.
  ```

  Set `tool_error_mode = FailRun` (`AgentRuntimeConfig.tool_errors.mode`, or per tool via
  `tool_errors.per_tool`) to fail the run instead of feeding the error back —
  `PaladinExecutionService::with_tool_error_config`, service-level configuration only
  (`PaladinConfig` is untouched, D-10).

  **Post-plan review fix (26-REVIEW.md CR-01/WR-03/WR-01):** the sanitization above was, at
  first cut, wired into only the `ArsenalError`/handoff `Err` path (`format_error`). A
  code-review fix pass widened it to the `ArmamentResult { success: false, .. }` business-failure
  `Error:` field and the successful `Output:` field too (`format_result`, `ToolResultFormatter`) —
  both previously embedded tool-supplied text raw, and the FAILED-business-result path is
  arguably the more likely one for a tool to surface an upstream response body through, since it
  is the tool author's own designed error-reporting mechanism. The same pass also tightened the
  `key=`/`token=` marker to require a word boundary, so an ordinary word like `monkey=5` embedded
  in a tool's JSON output is no longer misredacted. No further action is required to keep today's
  behavior — this only narrows what gets sanitized (adds sanitization where none existed, and
  removes a false-positive class), it does not change the `tool_error_mode` policy itself.
- M-B-04: no worked example owed — this phase (ENG-08) both introduces the behavior and documents it in full above.

**Note on Phase 25 (Node-Level Fault Tolerance — epic `FT`), D-30: no behavioral change; no row is added above.** Every Aegis capability — per-node retry, per-attempt `run_timeout`/`idle_timeout`, `Route`/`Absorb`/`Custom` error handlers, node result caching — is **opt-in per node** through `WarGraph::set_aegis`/`with_default_aegis` (code, not configuration; §9.5), and a v0.9 graph declares none, so an upgraded deployment's workflows execute identically: one attempt per node, no per-attempt bound, a node failure fails the run with the same `node execution error: {message}` display line as before, and nothing is served from a cache (`node_without_aegis_behaves_exactly_as_before`, `pre_aegis_and_limit_failures_carry_none`, `the_display_line_on_a_failed_waypoint_is_unchanged`). The one run-level knob this phase makes real, `EngineConfig.run_timeout_secs` (§9.5), keeps its `None` default, so no run gains a budget it did not configure. `FallbackLlmAdapter` is a new adapter a caller constructs explicitly — no existing provider adapter's behavior changes, and the shared `map_http_status` mapping (25-05) changes only which *typed* `LlmError` variant a non-2xx status becomes (`ProviderError { status }` instead of `ProcessingError(String)`), never whether an adapter's own retry loop retries it. The legacy `FormationExecutionService`/`PhalanxExecutionService`/`CampaignExecutionService`/`Commander` paths and the legacy Battalion `RetryPolicy`/`ErrorStrategy`/timeout services are untouched (X-03). Two accepted, documented caveats that are *not* behavioral changes for a v0.9 graph but matter once a node opts in: an `idle_timeout` on a node whose `PaladinPort` keeps the default `execute_observed` degrades to a per-attempt wall clock (D-19), and a hanging `EdgeConditionEvaluator` remains outside every timeout (R-23-01, still accepted — per-attempt timeouts wrap node execution, not edge evaluation).

## 9.2 Rust API changes (compile-affecting; the X-10 register)

Columns: **Crate · Type/Trait · Change · Mitigation (`#[non_exhaustive]` / default method / new trait / `#[serde(default)]`) · Deliberate-breaking? (Y/N, justification) · Requirement ID**.

The **Crate** cell of every row marked `Y` is the crate's **published crates.io package name** (`paladin-ai-core` for the `crates/paladin-core` directory, whose library is still imported as `paladin_core`), never the directory name: `.github/workflows/ci.yml`'s `semver` job compares that cell against `.cargo/semver-checks-allowlist.toml`'s `crate = "..."` keys by exact string, in both directions, and the allowlist's own schema requires the package name. (Corrected in Plan 25-14 — the four FT-owned rows below were first written with `paladin-core`, which the set-equality step reads as a different crate from `paladin-ai-core` and fails on.)

| Crate | Type / Trait | Change | Mitigation | Deliberate-breaking? | Req |
|---|---|---|---|---|---|
| `paladin-ai-core` | `StopReason` (defined in `paladin_core::platform::container::execution_result`, re-exported by `paladin_ports::output::paladin_port`) | new variants `CallLimit`, `TokenBudget`; enum marked `#[non_exhaustive]` | `#[non_exhaustive]`, suppressed via `[package.metadata.cargo-semver-checks.lints] enum_marked_non_exhaustive = "allow"` in `crates/paladin-core/Cargo.toml`, mirrored in `.cargo/semver-checks-allowlist.toml` | Y — both new variants are `is_successful() == true` and `is_limit() == true` (a budget stopped the run but the model's last response stands, D-08); the X-10.2 exception overview section 3 names for this exact enum is deliberately declined (D-07); every in-tree exhaustive match (`src/application/cli/formatters/output.rs`, `crates/paladin-web/src/agent_controller.rs`) gained a wildcard arm in the same commit | RT-FR-04, RT-FR-06 |
| `paladin-ai-core` | `BattalionError` | new variant `Node(NodeError)`, path-qualified to the structured `paladin_core::platform::container::node_error::NodeError` (never the legacy `battalion::NodeError` summary); enum marked `#[non_exhaustive]` | `#[non_exhaustive]`, suppressed via `[package.metadata.cargo-semver-checks.lints] enum_marked_non_exhaustive = "allow"` in `crates/paladin-core/Cargo.toml`, mirrored in `.cargo/semver-checks-allowlist.toml` | Y — a future node-execution failure variant must be addable without a semver-major bump (D-04); every in-tree exhaustive match over `BattalionError` gained a wildcard arm in the same commit | FT-FR-02 |
| `paladin-ai-core` | `PaladinError` | new structured variant `LlmFailure { transience, status, provider, message }`; new method `transience()`; enum marked `#[non_exhaustive]`. **Extended, Phase 26 (RT-02/RT-05/RT-07):** three further structured variants — `GuardrailTripped { rule, target }` (D-09, a `Guardrail` `Fail` action), `StructuredOutputInvalid { attempts, last_error, raw_output }` (D-26, the bounded structured-output repair loop's exhaustion), `ArmamentFailed { tool, reason }` (D-33/M-B-03, the `FailRun` tool-error-mode opt-in) — all on the already-`#[non_exhaustive]` enum, so this is a Change-cell extension, not a new row | `#[non_exhaustive]`, suppressed via `[package.metadata.cargo-semver-checks.lints] enum_marked_non_exhaustive = "allow"` in `crates/paladin-core/Cargo.toml`, mirrored in `.cargo/semver-checks-allowlist.toml` | Y — `LlmFailure`'s rendered `Display` is byte-identical to the legacy `LlmError(String)` arm, and `is_retryable()` answers `true` for it exactly as the legacy variant did, so `circuit_breaker.rs` is unaffected (D-02, X-03, X-06); the enum is `#[non_exhaustive]` so a future variant — including the three Phase 26 additions — can be added without a semver-major bump | FT-FR-01, RT-FR-06, RT-FR-19, RT-FR-24 |
| `paladin-ai` (facade) | `ExecutionMiddleware` (new in 0.10; listed for completeness) | New trait: `before_model`/`after_model`/`around_tool` hooks wrapping `PaladinExecutionService`'s reasoning loop (RT-01, D-01…D-06) | N/A — new trait introduced in v0.10, not a pre-existing public API | N/A (new type; X-10 governs only pre-existing types) | RT-FR-01, RT-FR-02, RT-FR-03 |
| `paladin-ports` | `TokenCounterPort` (new in 0.10; listed for completeness) | New port trait: `fn count(&self, text: &str, model: &str) -> u32` — synchronous, infallible (RT-03, D-13) | N/A — new trait introduced in v0.10, not a pre-existing public API | N/A (new type; X-10 governs only pre-existing types) | RT-FR-10 |
| `paladin-ports` | `VaultPort` (new in 0.10; listed for completeness) | New port trait: `put`/`get`/`delete`/`list`/`search` over namespaced key/value memory (RT-04, D-18…D-24) | N/A — new trait introduced in v0.10, not a pre-existing public API | N/A (new type; X-10 governs only pre-existing types) | RT-FR-13, RT-FR-14, RT-FR-15, RT-FR-16 |
| `paladin-ports` | `StructuredExecutorPort` (new in 0.10; listed for completeness) | New port trait: `execute_json_schema`/`execute_json_schema_observed`, object-safe at the JSON level (RT-05, D-26/D-27) — deliberately **not** a `PaladinPort` method | N/A — new trait introduced in v0.10, not a pre-existing public API | N/A (new type; X-10 governs only pre-existing types) | RT-FR-17, RT-FR-18, RT-FR-19 |
| `paladin-ports` | `LlmError` | new variants `ProviderError { provider, status, message }` and `AllProvidersFailed { attempts, last }`; new method `transience()`; enum marked `#[non_exhaustive]` | `#[non_exhaustive]`, suppressed via `[package.metadata.cargo-semver-checks.lints] enum_marked_non_exhaustive = "allow"` in `crates/paladin-ports/Cargo.toml`, mirrored in `.cargo/semver-checks-allowlist.toml` | Y — the status-carrying `ProviderError` and the FT-05 fallback chain's `AllProvidersFailed` extend the provider-error taxonomy without disturbing any existing dedicated variant (D-03); the enum is `#[non_exhaustive]` so it can grow further without a semver-major bump | FT-FR-01, FT-FR-16 |
| `paladin-ports` | `LlmRequest` (the type passed to `LlmPort::generate`) | new additive field `response_format: Option<ResponseFormat>` with `#[serde(default)]`; struct marked `#[non_exhaustive]`; new constructor `LlmRequest::new(model, prompt)` plus chainable `with_attachments`/`with_stream`/`with_metadata`/`with_response_format`, all doc-tested | `#[non_exhaustive]`, suppressed via `[package.metadata.cargo-semver-checks.lints] struct_marked_non_exhaustive = "allow"` in `crates/paladin-ports/Cargo.toml`, mirrored in `.cargo/semver-checks-allowlist.toml`; `LlmRequest::new` + `with_*` builders replace every in-tree full struct literal in the same commit | Y — `LlmRequest` has no `Default`, so there is no functional-update site to preserve; every downstream literal breaks under either X-10.3 option and only marking it `#[non_exhaustive]` (option a) makes the *next* field free — unlike Phase 25 D-26's `PaladinResult`, which does have a `Default` (D-28) | RT-FR-17 |
| `paladin-ai-core` | `GarrisonEntry` (defined in `paladin_core::platform::container::garrison`, re-exported/consumed by `paladin-ports`/`paladin-memory`) | new field `is_summary: bool` with `#[serde(default)]`; struct marked `#[non_exhaustive]`; new constructor `GarrisonEntry::summary(content)` (`ConversationRole::System`, `is_summary: true`) alongside the existing `new`/`with_metadata`/`with_token_count`, all three updated to set `is_summary: false`; the SQLite adapter gains a matching column via `002_add_garrison_is_summary.sql` | `#[serde(default)]` keeps a pre-existing JSON document or SQLite row readable as `is_summary == false`; `#[non_exhaustive]`, suppressed via `[package.metadata.cargo-semver-checks.lints] struct_marked_non_exhaustive = "allow"` in `crates/paladin-core/Cargo.toml`, mirrored in `.cargo/semver-checks-allowlist.toml`; no in-tree struct literal needed migration — the crate's only candidate call site (`crates/paladin-memory/benches/garrison_benchmarks.rs`) already used the `with_token_count` constructor | Y — `GarrisonEntry` has no `Default`, so there is no functional-update site to preserve; marking it `#[non_exhaustive]` (X-10.3 option (a)) makes the *next* field free, the same reasoning as the `LlmRequest` row above | RT-FR-12 |
| `paladin-ai-core` | `PaladinResult` (defined in `paladin_core::platform::container::execution_result`, re-exported by `paladin_ports::output::paladin_port`) | new additive field `served_by: Option<String>` (the `get_provider_name()` of the provider that served the result when the Paladin's `LlmPort` was a `FallbackLlmAdapter`; `None` otherwise). The struct stays **constructible** and is deliberately **not** marked `#[non_exhaustive]` (X-10.3 option (b), D-26) — landed FT-05, Phase 25 (25-08) | `#[serde(default, skip_serializing_if = "Option::is_none")]` — legacy JSON is byte-identical when the field is absent and legacy payloads deserialise with `None`; `Default` and `new()` unchanged; every `..Default::default()` construction site keeps compiling. Suppressed via `[package.metadata.cargo-semver-checks.lints] constructible_struct_adds_field = "allow"` in `crates/paladin-core/Cargo.toml`, mirrored in `.cargo/semver-checks-allowlist.toml` | Y — a public field on a constructible struct breaks full struct literals downstream. Option (a), marking the struct `#[non_exhaustive]` as FT-FR-17's text suggests, was rejected: functional-update syntax (`..Default::default()`) is **disallowed cross-crate on a `#[non_exhaustive]` struct**, so it would break all ~70 in-tree functional-update sites across six crates plus the examples and every downstream caller — contradicting FT-FR-17's own "`Default` still works" intent. **FT-FR-17 assumed functional update survives the `#[non_exhaustive]` marking; it does not.** Option (b) breaks only full struct literals, whose in-tree instances (rustdoc examples, `execution_result.rs`, `paladin_port.rs` tests, two root test files, one real call site in `paladin_execution_service.rs`) were migrated in the same commit as the field | FT-FR-17 |
| `paladin-ai` | `Settings` (the facade's config root, `src/config/settings.rs`) | new additive field `agent_runtime: AgentRuntimeConfig` carrying the X-09 middleware sub-structs (D-10). The struct stays **constructible** and is deliberately **not** marked `#[non_exhaustive]` (X-10.3 option (b), the `PaladinResult` precedent) — landed RT-02, Phase 26 (26-02) | `#[serde(default)]` — an absent `agent_runtime:` key resolves to `AgentRuntimeConfig::default()`, which is inert in every section, so a v0.9 config file boots with identical behavior; `Default` and `new()` unchanged, so every `..Default::default()` construction site keeps compiling. Suppressed via `[package.metadata.cargo-semver-checks.lints] constructible_struct_adds_field = "allow"` in the root `Cargo.toml`, mirrored in `.cargo/semver-checks-allowlist.toml` | Y — a public field on a constructible struct breaks full struct literals downstream. Option (a), marking `Settings` `#[non_exhaustive]`, was rejected for exactly the reason recorded on the `PaladinResult` row above: `Settings` implements `Default`, and functional-update syntax is **disallowed cross-crate on a `#[non_exhaustive]` struct**, so marking it would break every `..Default::default()` site downstream. Option (b) breaks only full struct literals, whose in-tree instances (`src/infrastructure/web/agent_host.rs`, `src/config/user_config.rs`, `tests/unit/settings_config_test.rs`) were migrated with the field | RT-FR-04, RT-FR-05 |
| `paladin-ports` | `PaladinPort` | new trait method `execute_observed(&self, paladin, input, heartbeat: &HeartbeatHandle) -> Result<PaladinResult, PaladinError>` (FT-03, D-19) — the progress channel per-attempt `idle_timeout` reads; the engine always dispatches a Paladin node through it — landed Phase 25 (25-09) | default method — the default body is `self.execute(paladin, input).await`, a *correct* claim of no progress (X-10.4): every existing implementor compiles unchanged and behaves exactly as before; an `idle_timeout` on a node whose port keeps the default degrades to a per-attempt wall clock (stated on `TimeoutPolicy`'s rustdoc and in the fault-tolerance guide). `PaladinExecutionService` reports real progress through inherent `execute_observed`/`execute_stream_observed` methods | N | FT-FR-09 |
| `paladin-ports` | `PaladinPort` | second new trait method `execute_scoped(&self, paladin, input, heartbeat: &HeartbeatHandle, scope: &RunScope) -> Result<PaladinResult, PaladinError>` (RT-04, D-21) — carries a host-issued Vault grant into a run; the engine always dispatches a `NodeSpec::Paladin` node through it (`superstep.rs`'s Paladin arm), replacing its prior `execute_observed` call — landed Phase 26 (26-13) | default method — the default body is `self.execute_observed(paladin, input, heartbeat).await`, discarding `scope`, a *correct* claim of no scoped capability (X-10.4, same pattern as the `execute_observed` row above): every existing implementor compiles unchanged and behaves exactly as before; overriding `execute_scoped` is how an implementor opts INTO acting on a `RunScope`. `PaladinExecutionService` resolves and enforces a real grant through its own inherent `execute_scoped`/`with_vault`/`confined_vault` methods | N | RT-FR-13, RT-FR-14, RT-FR-15, RT-FR-16 |
| `paladin-core` | `EdgeCondition` | *no variant change*, but semantics of `Custom` change (see M-B-01) | none required — no signature or variant change; the semantic change is M-B-01 | N | CF-FR-01 |
| `paladin-battalion` | `Commander` / `CommanderBuilder` | new `StrategySelection` option (additive method; verify no public field added) | additive builder method (`CommanderBuilder::strategy_selection`); the new `StrategySelection` option is stored in a private `Commander` field, defaulting to `Heuristic` -- no public field added, verified by source assertion (`awk`-scanned `pub struct Commander` block) | N | CF-FR-19 |
| `paladin-battalion` | `CampaignExecutionService` | new evaluator-registry builder method (constructor unchanged) | additive builder method (`with_evaluator`); `new(paladin_port)` unchanged | N | CF-FR-01 |
| `paladin-core` | `Waypoint` (new in 0.10; listed for completeness) | `WaypointStatus::AwaitingInput` reshaped to `{ parleys: Vec<ParleyRequest>, responses: Vec<ParleyResponse> }` (HITL-01) plus an additive `#[serde(default)] fork_of: Option<WaypointId>` field (HITL-03) — both landed Phase 24 | N/A — new type introduced in v0.10, not a pre-existing public API. Stub landed in Plan 22-01; payload finalized by HITL-01/HITL-03, Phase 24 (24-01, 24-06). | N/A (new type; X-10 governs only pre-existing types) | HITL-FR-03, HITL-FR-07 |
| `paladin-ports` | `ParleyPort` (new in 0.10; listed for completeness) | New input-side trait: `async fn resume_with(&self, thread: &ThreadId, responses: Vec<ParleyResponse>) -> Result<ResumeAccepted, ParleyError>` — landed HITL-05, Phase 24 (24-10) | N/A — new trait introduced in v0.10, not a pre-existing public API. | N/A (new type; X-10 governs only pre-existing types) | HITL-FR-16 |
| `paladin-ports` | `ResumeAccepted` (new in 0.10, shipped HITL-05 Phase 24; this row records its Phase 27 extension) | new additive field `run_id: Option<RunId>` with accessor `run_id()`; new constructor `with_run_id(self, run_id) -> Self`; struct marked `#[non_exhaustive]` — landed PLAT-03, Phase 27 (27-08) | `#[non_exhaustive]`; construction stays possible only through `new()` + `with_run_id()` (X-10.3). Suppressed via the existing `[package.metadata.cargo-semver-checks.lints] struct_marked_non_exhaustive = "allow"` already present in `crates/paladin-ports/Cargo.toml` (no new entry needed) | N/A — `ResumeAccepted` did not exist at the `v0.9.0` baseline (it shipped in this same v0.10.0 cycle, Phase 24); X-10 governs only a signature change to a type that shipped in a PUBLISHED release, so this is not a deliberate-breaking row and needs no `.cargo/semver-checks-allowlist.toml` entry | PLAT-03, PLAT-FR-06 |
| `paladin-web` | `ResumeAcceptedResponse` (new in 0.10, shipped HITL-05 Phase 24; this row records its Phase 27 extension) | new additive field `run_id: Option<String>`; new constructor `ResumeAcceptedResponse::new(thread_id, state_url, run_id)`; struct marked `#[non_exhaustive]` — landed PLAT-03, Phase 27 (27-08) | `#[non_exhaustive]`, suppressed via a NEW `[package.metadata.cargo-semver-checks.lints] struct_marked_non_exhaustive = "allow"` line added to `crates/paladin-web/Cargo.toml`'s existing lints table (previously carried only `function_requires_different_generic_type_params`); construction stays possible only through the new `new()` constructor (X-10.3) — the sole in-tree call site (`thread_controller.rs`'s `resume_thread` handler) was migrated in the same commit | N/A — `ResumeAcceptedResponse` did not exist at the `v0.9.0` baseline (new-in-0.10, Phase 24); X-10 governs only a signature change to a type that shipped in a PUBLISHED release, so this is not a deliberate-breaking row and needs no `.cargo/semver-checks-allowlist.toml` entry | PLAT-03, PLAT-FR-06 |
| `paladin-web` | `require_authentication` (fn) | Gained a generic type parameter: `pub async fn require_authentication<S>(...)  where S: HasAgentAuth + Clone + Send + Sync + 'static` — was non-generic at v0.9.0. Landed HITL-05, Phase 24 (24-11); surfaced by `cargo semver-checks` during 24-12's gate-evidence pass, not recorded at landing time. | No backward-compatible mitigation exists for a generic-arity change — any external caller invoking the old zero-generic signature via turbofish breaks. Suppressed via `[package.metadata.cargo-semver-checks.lints] function_requires_different_generic_type_params = "allow"` in `crates/paladin-web/Cargo.toml`, mirrored in `.cargo/semver-checks-allowlist.toml`. | Y — genericizing this ONE function is what lets `ThreadApiState`'s routes reuse the exact same authentication middleware `AgentApiState`'s routes already layer, rather than a duplicated ~15-line copy per router state (D-24); accepted as the minimal, intentional cost of that reuse. | HITL-FR-16 |
| `paladin-web` | `ThreadApiState` (new type in `paladin-web`, listed for completeness at HITL-05/Phase 24 per the note below; this row records its Phase 27 extension) | new additive fields `runs: Option<Arc<dyn RunRepositoryPort>>` and `run_submission: Option<Arc<dyn RunSubmissionPort>>`, with `with_runs`/`with_run_submission` builders; struct marked `#[non_exhaustive]` — landed PLAT-06, Phase 27 (27-15, D-45) for `GET /threads`, `GET /threads/{id}`, `POST /threads/{id}/fork` and `DELETE /threads/{id}` | `#[non_exhaustive]`; construction stays possible only through `ThreadApiState::new()` plus the `with_*` builder methods (X-10.3) — no in-tree struct literal exists to migrate, every construction site already used the builder | N/A — `ThreadApiState` did not exist at the `v0.9.0` baseline (it shipped in this same v0.10.0 cycle, Phase 24); X-10 governs only a signature change to a type that shipped in a PUBLISHED release, so this is not a deliberate-breaking row and needs no `.cargo/semver-checks-allowlist.toml` entry | PLAT-06, PLAT-FR-16 |
| `paladin-ports` | `RunSubmissionPort` (new in 0.10, shipped PLAT-01 Phase 27; this row records its Phase 27 extension) | `cancel`'s signature gained a `requested_by: Option<(String, UserRole)>` parameter (D-46 invocation-shaped authorization); a new `fork(&self, request: ForkRun) -> Result<RunAccepted, RunSubmissionError>` method was added — landed PLAT-06, Phase 27 (27-15, D-45/D-46) | No mitigation needed — `RunSubmissionPort` is a new-in-0.10 trait with exactly four implementors, ALL in-tree (`RunSubmissionService`, the port module's own `AlwaysUnwired` test double, `paladin-web::run_controller`'s `MockSubmissionPort` test double, `schedule::tests`' `RecordingSubmission` test double), all four migrated in the same wave | N/A — `RunSubmissionPort` did not exist at the `v0.9.0` baseline; X-10 governs only a signature change to a type/trait that shipped in a PUBLISHED release | PLAT-06, PLAT-FR-16 |

**Note on Plan 22-01 (this phase's tracer plan):** Plan 22-01 added the `battlefield`, `battlefield_error`, and `waypoint` modules to `paladin-core`, `waypoint_port` to `paladin-ports`, the `waypoint` adapter module to `paladin-storage`, and the `engine` module to `paladin-battalion`. All of these are **new** modules/types — none of Plan 22-01's changes touched a *pre-existing* public type's signature (its "modified" files were module-registration edits, e.g. adding a `mod battlefield;` line). **This is a deliberate zero**, not an omission: no register row is added for Plan 22-01.

**Note on Phase 23 (Control Flow — Dynamic Routing, Fan-Out & Subgraphs), D-27:** this phase reshapes several engine and Waypoint types this plan's own read-list names — `StateNode` (`run`'s return type changed from a bare `StateDelta` to `Directive`), `NodeSpec` (a `Battalion` variant and, on `Paladin`, a `directive_parser` field added), `NodeContext` (a `muster: Option<MusterContext>` field added), `EngineLimits` (a `max_muster_tasks` field added), `EngineError` (new variants added to an already `#[non_exhaustive]` enum — `GotoUnknownNode`, `ParleyNotSupported`, `DirectiveParseFailed`, `EmptyMuster`, `DuplicateMusterTaskKey`, `MusterTaskLimitExceeded`, `MusterUnknownWorker`, `MusterWorkerNotATemplate`, `WorkerTemplateIsEntry`, `WorkerTemplateHasStaticIncomingEdge`, `MusterPrefixSchemaField` among them), `WarGraph` (`validate`'s signature changed — it gained an `edge_evaluators: &EdgeEvaluatorRegistry` parameter for CF-01's fail-closed unregistered-condition check — and a new `worker_templates` marker set was added, seeded via the new `add_worker_template` method) and `Waypoint` (two additive `#[serde(default)]` fields carrying Muster progress and `checkpoint_ns`). Every one of these types is **absent at the `v0.9.0` tag** — `StateNode`, `NodeSpec`, `NodeContext`, `EngineLimits` and `EngineError` were all introduced by Plan 22-01 (see the note immediately above); `Waypoint` is new in 0.10 and already carries the "N/A — new type" row above. Because none of them is a *pre-existing* public type, X-10 — which governs only a signature change to a type that shipped in `v0.9.0` — does not apply to any of them, and none gets its own §9.2 row here. **This is a deliberate zero**, not an omission: a reader who diffs this phase's changes against a type list and finds no `StateNode`/`NodeSpec`/`NodeContext`/`EngineLimits`/`EngineError`/`WarGraph`/`Waypoint` row should read that absence as this note, not as an oversight.

**Note on Phase 24 (Pause/Resume, History & Graceful Shutdown — epic `HITL`), D-29:** this phase touches several more engine, Waypoint and web types this plan's own read-list names — `RunOutcome` (`AwaitingInput` reshaped to `{ parleys, waypoint }`; `Halted` unchanged), `EngineError` (new `#[non_exhaustive]` variants: `ParleyInChildUnsupported`, `GateOutputFieldRequired`, `GateOutputFieldMustBeAbsent`, `GateOutputFieldUnknown`, `GateOutputFieldTypeIncompatible`, `GateResumeWithDefaultInvalid`, `ParleyPrefixSchemaField`, `ParleyAlreadyAnswered`, `ResponseShapeInvalid`, `ParleyExpired`, `ThreadAlreadyFailed`, `WaypointNotFound`; `ParleyNotSupported` retained unconstructed per X-03), `NodeSpec` (a `Gate { request: GateRequestTemplate, output_field }` variant added), `NodeContext` (a `parley_response: Option<ParleyResponse>` field added), `NodeOutcomeKind` (a `Parleyed` variant added), `WaypointSummary` (a `fork_of: Option<WaypointId>` field added, additive `#[serde(default)]`), `ThreadApiState` (new type in `paladin-web`, listed for completeness), plus `TraceEvent` recorded as **untouched** by this phase (no `ParleyRaised`/`RunHalted` variant added — Phase 28 owns the authoritative enum, per CONTEXT.md's Claude's-Discretion resolution). Every one of these types is **absent at the `v0.9.0` tag** or is itself new in 0.10 (`ThreadApiState`) — `RunOutcome`, `EngineError`, `NodeSpec`, `NodeContext` and `NodeOutcomeKind` were all introduced by Plan 22-01 (see the note above); `WaypointSummary` is new in 0.10 (`paladin-ports`, Plan 22-01). Because none of them is a *pre-existing* public type at `v0.9.0`, X-10 does not apply to any of them, and none gets its own §9.2 row here. **This is a deliberate zero**, not an omission: a reader who diffs this phase's changes against a type list and finds no `RunOutcome`/`EngineError`/`NodeSpec`/`NodeContext`/`NodeOutcomeKind`/`WaypointSummary`/`ThreadApiState` row should read that absence as this note, not as an oversight. `Waypoint`'s own `AwaitingInput` reshape and the new `fork_of` field are covered by the `Waypoint` row above; `ParleyPort` is recorded above as a new trait.

**Note on Phase 25 (Node-Level Fault Tolerance — epic `FT`), D-30:** beyond the five rows above that this phase resolved (`BattalionError`, `PaladinError`, `LlmError`, `PaladinResult` marked `Y`; `PaladinPort` marked `N`), this phase touches several more **new-in-0.10** types that therefore get no row — `WarGraph` (a per-node `Aegis` sidecar seeded via the new `set_aegis`/`with_default_aegis`/`aegis_for` methods, and `validate`'s signature changed again: its `edge_evaluators: &EdgeEvaluatorRegistry` parameter became one `registries: &EngineRegistries` bundle carrying the edge-evaluator, retry-predicate and error-handler registries together, D-13), `NodeContext` (additive `attempt: u32` and `heartbeat: HeartbeatHandle` fields with `attempt()`/`heartbeat()` accessors, D-18), `NodeExecutionRecord` (additive `#[serde(default)]` `attempts: Vec<AttemptRecord>` and `cache_hit: bool`, D-16), `WaypointStatus::Failed` (an additive `#[serde(default)] node_error: Option<NodeError>` field beside the untouched `error`/`failed_node`, D-08), `EngineError` (new variants on the already-`#[non_exhaustive]` enum: `NodeFailed`, `RunTimeoutExceeded`, `AegisOnUndeclaredNode`, `AegisUnsupportedForNodeKind`, `UnregisteredRetryPredicate`, `UnregisteredErrorHandler`, `RetryPolicyInvalid`, `TimeoutPolicyInvalid`, `RouteTargetUnknown`, `RouteTargetIsWorkerTemplate`, `RouteErrorFieldUndeclared`, `RouteErrorFieldDispatchInvalid`, `AbsorbDeltaSchemaInvalid`, `HandlerNotAllowedOnWorkerTemplate`, `MusterHandlerMustBeDeltaOnly`, `CachePolicyWithoutCacheBackend`, `CachePolicyOnDeniedField`, `CacheKeyFieldUndeclared`), `TraceEvent` (additive `attempt` on `NodeStarted`/`NodeFinished`, `cache_hit` on `NodeFinished`, and a new `FallbackHop { node_id, from_provider, to_provider }` variant), `FieldSpec` (an additive `#[serde(default)] cache: CacheMarker` field, `Allow` by default, D-29), and the engine's bare-string `NodeError(String)` newtype **renamed to `StateNodeError`** (D-06) so the PRD's `NodeError` name is free for the structured `paladin_core::platform::container::node_error::NodeError`. Every one of these types is **absent at the `v0.9.0` tag** — `WarGraph`, `NodeContext`, `EngineError` and the renamed newtype were introduced by Plan 22-01, `NodeExecutionRecord`/`WaypointStatus`/`FieldSpec` are part of the new-in-0.10 Waypoint/Battlefield family, and `TraceEvent` is the Phase 22 trace-sink enum Phase 28 owns. Because none of them is a *pre-existing* public type at `v0.9.0`, X-10 does not apply to any of them, and none gets its own §9.2 row here. **This is a deliberate zero**, not an omission: a reader who diffs this phase's changes against a type list and finds no `WarGraph`/`EngineRegistries`/`NodeContext`/`NodeExecutionRecord`/`WaypointStatus`/`EngineError`/`TraceEvent`/`FieldSpec`/`StateNodeError` row should read that absence as this note, not as an oversight. The legacy v0.9 `battalion::{RetryPolicy, ErrorStrategy, NodeError}` summary types are untouched (X-03) — the new `Aegis` family is a distinct type family under `paladin_core::platform::container::aegis`, and the core prelude re-exports only `Aegis` and `Transience`, never either `RetryPolicy`.

**Note on Phase 26 (Agent Runtime Enhancements — epic `RT`), D-37:** beyond the rows above that
this phase resolved or extended (`StopReason`, `LlmRequest`, `GarrisonEntry` marked `Y`;
`PaladinError`'s Change cell extended; `PaladinPort`'s second `execute_scoped` row marked `N`;
`ExecutionMiddleware`/`TokenCounterPort`/`VaultPort`/`StructuredExecutorPort` listed `N/A, new`),
this phase touches several more **new-in-0.10** types that therefore get no row of their own —
`NodeSpec::Paladin.output_schema: Option<SchemaRef>` (RT-05, D-29 — a new additive field on the
`Paladin` variant of the already new-in-0.10 `NodeSpec` enum, set via the new
`NodeSpec::with_output_schema` builder), `NodeContext.vault: Option<ConfinedVault>` (RT-04, D-21 —
a new additive field on the already new-in-0.10 `NodeContext`, with a `vault()` accessor), the new
`EngineError` variants on the already-`#[non_exhaustive]` enum (`StructuredExecutorMissing`,
`UnregisteredOutputSchema`, `OutputSchemaWithStructuredDirective`, `OutputSchemaFieldNotJson`, RT-05,
D-29), `WarGraph`'s new `with_structured_executor`/`with_output_schema` builder methods plus
`GRAPH_FINGERPRINT_VERSION` bumped `v5:` → `v6:` (RT-05, D-29 — `output_schema` now enters the
hashed byte stream, canonical JSON sorted and length-prefixed; a `v5:`-tagged fingerprint is
recognized as stale on resume, never reinterpreted, the same discipline every prior fingerprint
bump in this document follows), `DirectiveParser`'s internal `StructuredDirective` extraction
refactored to call the new, shared `paladin_core::platform::container::structured::extract_json`
rather than its own private `extract_envelope` (RT-05, D-26 — CF-FR-06's "MUST reuse" rule; no
behavior change, the directive parser's own tests are unchanged), and `MockLlmAdapter` recording
the `response_format` of the most recent request it received via a new
`last_response_format()` accessor (RT-05, D-28 — test-only surface, strictly additive). Every one
of these is either a field/method addition on a type that is itself absent at the `v0.9.0` tag
(`NodeSpec`, `NodeContext`, `EngineError`, `WarGraph` were all introduced by Plan 22-01; the
fingerprint format and `DirectiveParser` are likewise new-in-0.10) or a test-only adapter method.
Because none of them is a change to a *pre-existing* public type at `v0.9.0`, X-10 does not apply
to any of them, and none gets its own §9.2 row here. **This is a deliberate zero**, not an
omission: a reader who diffs this phase's changes against a type list and finds no
`NodeSpec::Paladin.output_schema`/`NodeContext.vault`/`EngineError`/`WarGraph`/`DirectiveParser`/
`MockLlmAdapter` row should read that absence as this note, not as an oversight.

## 9.3 Toolchain & dependencies

- **Final MSRV: 1.88** (Rust, edition 2024) — an X-11.2 stop-and-flag resolution, landed in Plan 22.1-04. The prior "1.85" figure was derived from edition 2024's own minimum and was never measured against this workspace's actual dependency graph; 1.88 is the first MSRV figure in this project's history with a passing measurement behind it (`cargo +1.88 check --workspace --all-features --all-targets --locked`, zero errors). The chain that forces it: the STDIO MCP transport requires `rmcp` at its exact `=2.1.0` pin (D-06, unchanged — no `rmcp` version restores 1.85 with `transport-child-process`), which with the `transport-child-process` feature requires `process-wrap ^9.0`, whose releases require rustc 1.86 or 1.87; separately, `time` >= 0.3.47 (the fix for RUSTSEC-2026-0009) requires rustc 1.88. The higher of the two — 1.88 — is the floor, and it clears the advisory with no new security exception. Two alternatives were considered and rejected: **1.87 plus a new RUSTSEC-2026-0009 exception** (rejected — adds to the exception sprawl ADR-0024 governs, for a floor one release lower than the clean option), and **1.86 with `process-wrap` held at an exact `.0` pin** (rejected — a transitive `.0` pin plus the same exception). The declared floor now lives in exactly one place, `workspace.package.rust-version = "1.88"` in root `Cargo.toml`; all ten crate manifests inherit it via `rust-version.workspace = true`. The lockfile was restored to its pre-`44e13fbd` resolution (reverting the 22-17 hand-downgrade of `time`, `process-wrap`, `darling`, `serde_with`, `tonic`, `idna_adapter`, `home` and others) and re-resolved under the new `[workspace] resolver = "3"` (D-09), which also serves as the recurrence guard against a future `cargo update` silently re-selecting a version above the declared floor. **X-11.4 default-feature-set confirmation:** `cargo tree -e features -p paladin-ai` under default features was compared before (HEAD's pinned-back lockfile) and after (the restored lockfile) this change — the only package-name delta in the full workspace lockfile is `bs58` reappearing (a resolution-only difference restored alongside the rest of the pre-`44e13fbd` graph, per CONTEXT.md's discretion note), and `bs58` does not appear anywhere in `paladin-ai`'s default-feature tree. The default set did not grow.
- **New feature added to an existing dependency — `uuid`'s `v7` feature.** Added to the existing pinned `uuid = 1.8.0` workspace dependency (D-03) to generate time-ordered `WaypointId`s via `Uuid::now_v7()`. No new crate name. Landed in Plan 22-01 (`Cargo.toml`'s `[workspace.dependencies] uuid` feature array now reads `["v4", "v7", "serde"]`).
- **New cargo feature — `postgres` on `paladin-storage`. Landed this plan (22-06), Task 2.** `postgres = ["dep:sqlx", "sqlx/postgres"]` (mirroring the existing `sqlite`/`mysql` feature-gate pattern) enabling `PostgresWaypointStore` (ENG-05/D-01), plus the facade passthrough `storage-postgres = ["paladin-storage/postgres"]` on `paladin-ai` (mirroring the existing `storage-mysql` passthrough) — `storage = ["storage-mysql", "storage-postgres"]` — so a consumer of the facade crate reaches it via `cargo build -p paladin-ai --features storage-postgres` without any `paladin-storage`-level override. Neither `default` nor this addition to `storage`/`full` puts Postgres in the default set.
- **`sqlx`'s `macros` feature added — Task 1 blocking-issue fix.** `sqlx::migrate!` (used by both `sqlite.rs` and `postgres.rs` to embed their versioned migrations at compile time) lives behind sqlx's `macros` feature, not `migrate` alone; added to the workspace-level `sqlx` dependency (`Cargo.toml`'s `[workspace.dependencies] sqlx` feature array now reads `[..., "migrate", "macros"]`). No new crate name — same already-pinned `sqlx 0.8`.
- **Default feature set impact (X-11.4), confirmed at this plan's HEAD:** `cargo tree -e features -p paladin-ai` (default features) contains no `sqlx/postgres`/Postgres-driver entry anywhere in the graph — verified by `cargo tree -e features -p paladin-ai | grep -i postgres` returning no matches. `cargo build -p paladin-ai --features storage-postgres` compiles the adapter; `cargo build -p paladin-storage --features postgres` and `cargo clippy -p paladin-storage --features postgres --all-targets -- -D warnings` are both clean. The default `paladin-ai` build also gains no heavyweight dependency from the earlier `uuid`/`v7` addition — `v7` only pulls in `uuid`'s own `atomic`/`rng` sub-features, not a new crate.
- **Phase 25 (Node-Level Fault Tolerance, D-30): no new dependency.** Verified by direct manifest read at close-out (Plan 25-14): `rand = "0.8"` was **already** a direct dependency of `paladin-battalion` (`crates/paladin-battalion/Cargo.toml`) before this phase — the retry loop's jitter source needed nothing added, which corrects D-30's own "`rand` if not already a battalion dependency" framing; `redis = "0.32.2"` was **already** `paladin-storage`'s optional dependency behind `redis-queue`, and the new cache adapter shares that one dependency line. Two edits to existing lines only: the shared optional `redis` dependency gained the `safe_iterators` feature (25-04 — without it `AsyncIter::next_item` is deprecated and a per-item `scan_match` conversion error would silently truncate `invalidate()`), and `blake3 = "1.8.2"` — the exact pinned version `paladin-core` already resolves for the graph fingerprint — became a direct dependency **edge** of `paladin-battalion` for the node-cache key digest (25-13). `Cargo.lock` gained one dependency-list line and **no new package**; `25-RESEARCH.md`'s Package Legitimacy Audit records zero proposed packages.
- **New cargo feature — `redis-cache` on `paladin-storage`, landed Phase 25 (25-04).** `redis-cache = ["dep:redis"]` (sharing the already-declared optional `redis` dependency with `redis-queue`) enables `RedisNodeCache`/`RedisNodeCacheConfig`, the Redis tier of the node-cache contract suite, and the Docker-gated `redis-cache-integration` CI job that is the only place that tier's evidence is provable; plus the facade passthrough `redis-cache = ["paladin-storage/redis-cache"]` on `paladin-ai`. **Neither enters any default feature set** — not `default`, not `storage`, not `full` — confirmed by inspecting the resolved production dependency graph under default features (no `redis` entry). `InMemoryNodeCache` is ungated and always available.

- **New direct facade dependency — `schemars = "1.2"`, landed Phase 26 (RT-05, D-26, plan 26-12). No new package enters the graph.** Added to `paladin-ai`'s own `Cargo.toml` for `execute_structured<T>`'s schema derivation (`schemars::schema_for!` at the application layer). Pinned to the same minor version already resolved via the existing `rmcp = "2.1.0"` dependency's own `schemars` edge — `cargo tree -i schemars` shows exactly two versions in the whole workspace lockfile both before and after this addition (`0.9.0` via `serde_with`, `1.2.1` via both `rmcp` and this new direct edge), so `Cargo.lock` gains a dependency-list line, not a new package. **MSRV proof:** the workspace floor stays `1.88` (§9.3 above) — `RUSTUP_TOOLCHAIN=1.88 cargo check --workspace --all-features --all-targets` is the `msrv` CI job's exact command (`ci.yml:251`) and is part of this plan's own gate evidence. **No new cargo feature was introduced by this phase's structured-output or context-management work:** the tiktoken token counter rides the pre-existing `content-processing` feature (`TiktokenCounter` already lived in `paladin-memory` behind it; this phase only adds an `impl TokenCounterPort` to the existing type), `SqliteVault` rides the pre-existing `sqlite` feature, and `SemanticVault` is ungated (it holds only trait objects — `Arc<dyn SanctumPort>` + `Arc<dyn EmbeddingPort>` — so it needs no `qdrant-client` of its own, D-24).
- **`schemars 1.2` — second dependency edge, this time from `paladin-battalion`, landed Phase 27 (plan 27-05), registered on that plan's behalf here per this wave's same-wave file-ownership rule.** `paladin-battalion/Cargo.toml` gained its own `schemars = "1.2"` line (a plain version string mirroring `blake3`'s existing precedent in that same manifest, not `{ workspace = true }` — the workspace does not declare `schemars` under `[workspace.dependencies]`) for `WarGraphDoc`'s `schemars(JsonSchema)` derive (D-34). `schemars` was **already** a direct workspace dependency as of Phase 26 (the row immediately above), already resolved at `1.2.1` in `Cargo.lock` via `rmcp`/`paladin-ai`'s own edge — this promotion adds a new CONSUMING edge, not a new package: `cargo tree -i schemars` still shows exactly two versions workspace-wide. `cargo msrv verify`/`RUSTUP_TOOLCHAIN=1.88 cargo check --workspace --all-features --all-targets` (the `msrv` CI job's exact command) passes unchanged at the workspace floor of **1.88** (§9.3 above).
- **`croner = "2.2"` promoted to a direct `[workspace.dependencies]` entry, landed Phase 27 (plan 27-11), D-38.** `croner` was already resolved at `2.2.0` in `Cargo.lock`, pulled transitively via `tokio-cron-scheduler` (`crates/paladin-storage/src/scheduler.rs`'s `TokioCronSchedulerAdapter`, unrelated to this plan and left byte-behaviourally unchanged — X-03). Pinning the SAME `"2.2"` at the workspace level and adding it as a direct `paladin-storage` dependency (for `crates/paladin-storage/src/cron.rs`'s `parse_run_cron`) promotes an existing transitive resolution to a direct edge — `cargo tree -i croner` shows exactly one version (`2.2.0`) both before and after this change, so `Cargo.lock` gains a dependency-list line, not a new package. **MSRV proof:** `RUSTUP_TOOLCHAIN=1.88 cargo check -p paladin-storage --all-features` (the `msrv` CI job's underlying command, scoped to the touched crate) passes clean — verified 2026-09-08.
- **`chrono-tz = "0.10"` — a genuinely NEW direct dependency, landed Phase 27 (plan 27-11), D-38.** Unlike `croner` above, `chrono-tz` was **not** present anywhere in `Cargo.lock` before this plan (0 occurrences) — `crates/paladin-storage/src/cron.rs`'s IANA-timezone support (`RunCron`'s `chrono_tz::Tz` field, `parse_run_cron`'s `Tz::from_str` validation) is the first consumer in this workspace. Added to `[workspace.dependencies]` and as a direct, unconditional `paladin-storage` dependency (`cron.rs` is always compiled, no feature gate — the InMemory `run_schedule` adapter needs cron parsing with no `sqlite`/`postgres` feature enabled). Resolved at `0.10.4`. **MSRV proof:** `RUSTUP_TOOLCHAIN=1.88 cargo check -p paladin-storage --all-features` passes clean — verified 2026-09-08, same run as the `croner` promotion above.
- **`tokio`'s `test-util` feature added to the ROOT `[dev-dependencies]`, landed Phase 27 (plan 27-11), D-52.** A feature addition to the already-`{ workspace = true }`-pinned `tokio` runtime dependency (`[dependencies]`), not a new package — mirrors `crates/paladin-battalion/Cargo.toml`'s identical existing `test-util` dev-dependency entry verbatim. Needed by `src/application/services/run/schedule/tests.rs`'s `spawn_ticks_on_interval_and_stops_on_shutdown` test, which exercises `ScheduleService::spawn`'s real `tokio::time::interval` loop. (The concurrency/restart-safety tests in the same file do NOT rely on `tokio::time::pause` — they inject a fully deterministic `now` closure instead, since `tokio::time::pause` freezes tokio timers but not `chrono::Utc::now()`; see `service.rs`'s own module docs.)
- **`hmac = "0.12"` promoted to a direct facade (`paladin-ai`) dependency, landed Phase 27 (plan 27-13), D-41.** `hmac` was already resolved at `0.12.1` in `Cargo.lock` (a transitive dependency pulled in via an existing crypto dependency elsewhere in the graph) before this plan — `sign_webhook_body` (`src/application/services/run/webhook/signature.rs`) is the first direct consumer, computing the `X-Paladin-Signature: sha256=<hex>` header as `Hmac<Sha256>` over the shared, already-workspace-pinned `sha2 = "0.10.9"` (no new hash crate). Added as a plain, unconditional `[dependencies]` line in the root `Cargo.toml` — mirroring `sha2`'s own placement immediately above it (facade-only, not a `[workspace.dependencies]` entry) — so `Cargo.lock` gains a dependency-list line, not a new package (`cargo tree -i hmac` shows one resolved version, `0.12.1`, both before and after). **MSRV proof:** `RUSTUP_TOOLCHAIN=1.88 cargo check -p paladin-ai --all-features --all-targets` passes clean — verified 2026-09-08 alongside this plan's own `cargo check --workspace --all-targets --all-features` gate evidence. **No `ipnet` dependency was added:** the SSRF guard's address classification (D-42) uses only `std::net::{Ipv4Addr, Ipv6Addr}`'s own stable `is_private`/`is_loopback`/`is_link_local`/`is_unique_local`/`is_unicast_link_local`/`is_unspecified`/`to_ipv4_mapped` methods, all stable at the 1.88 floor — `27-RESEARCH.md`'s own discretion note on this point is confirmed, not merely assumed.

## 9.4 Persistence & schema migrations

- **New table: `waypoints`. Landed this plan (22-06).** Two versioned migration files, one per backend (superseding this section's earlier single-path placeholder from Plan 22-04 — one crate now ships two dialects of the same logical table, hence the per-backend subdirectory split): `crates/paladin-storage/migrations/sqlite/001_create_waypoints_table.sql` (Task 1) and `crates/paladin-storage/migrations/postgres/001_create_waypoints_table.sql` (Task 2; `001_` continues the per-crate numbering convention established by `crates/paladin-memory/migrations/001_create_garrison_tables.sql`). SQLite stores `payload` as `TEXT` holding serialized JSON (D-02, for debuggability and because the `sqlx` `json` feature is already enabled workspace-wide); PostgreSQL stores `payload` as `JSONB`, bound via an explicit `::jsonb` cast on the parameter (sqlx sends bound `String` parameters as text over the wire; the cast is what tells Postgres to store it as JSONB rather than TEXT). Both share the same logical schema: `waypoint_id` (primary key), `thread_id`, `parent_id` (nullable), `superstep`, `status` (its own JSON-serialized column, so `history`/`list_threads` summaries can be built without deserializing the full payload), `payload`, `created_at`, with an index on `(thread_id, created_at DESC)`.
- **Runs automatically, not manually.** Both migrations are embedded at compile time via `sqlx::migrate!` (which required adding `sqlx`'s `macros` feature to the workspace-level `sqlx` dependency — a Task 1 blocking-issue fix, no new crate name, see §9.3) and applied automatically when `SqliteWaypointStore`/`PostgresWaypointStore` is constructed — no manual `sqlx migrate run` step is required of the operator. Idempotent: constructing a store twice against the same database is safe (`CREATE TABLE IF NOT EXISTS`/`CREATE INDEX IF NOT EXISTS` plus `sqlx::migrate::Migrator`'s own applied-version tracking), proven by `constructing_store_twice_against_same_file_is_idempotent` (SQLite) and exercised against a real server by the Postgres Tier 2 suite (`docker/docker-compose.test.yml`'s `postgres-test` service, `make test-integration-docker`).
- **Reversibility:** not reversible without data loss (dropping `waypoints` discards all checkpoint history). No `down` migration is provided, consistent with every existing migration in this workspace (`garrison`, `workflow`, `content`, `user` tables), none of which ship a reverse migration.
- **Storage growth guidance:** unbounded by default — every completed superstep writes one `waypoints` row containing a full `Battlefield` snapshot. Configure `WaypointRetentionConfig` (`max_age_days`, `max_waypoints_per_thread`; see §9.5) to bound growth. Pruning MUST NOT (and, per ENG-FR-18, will not) delete a thread's single latest Waypoint or any Waypoint with status `AwaitingInput`.
- **Memory and storage growth are measured, not asserted (ENG-NFR-01/02, Plan 22-10).** `benches/engine_benchmarks.rs` measures `SqliteWaypointStore::save` latency at three Battlefield payload sizes (1 KiB, 512 KiB, just under 1 MiB) against the ENG-NFR-01 target of under 10 ms p50, and per-superstep engine wall-clock cost at two Vanguard widths; `examples/war_engine_memory_baseline.rs` measures resident-memory growth per superstep for a fixed graph and counts Battlefield clones per superstep (ENG-NFR-02: at most one shared clone per superstep, via the Arc-shared read snapshot, plus one per concurrently executing node view), failing loudly if that bound regresses. Measured figures and the commit they were taken at are recorded in `.planning/phases/22-battlefield-state-superstep-engine/22-10-SUMMARY.md` — treat the per-checkpoint storage-growth guidance above alongside those figures, not in isolation, when sizing a deployment's Waypoint retention policy.
- **Existing state is unaffected.** Existing Citadel JSON/SQLite state files remain readable unchanged — the `waypoints` table is entirely new and additive. `WaypointPort` is deliberately separate from `CitadelPort` (see the rustdoc rationale on `crates/paladin-ports/src/output/waypoint_port.rs`: Waypoints are high-frequency and append-mostly, addressed by thread; Citadel remains for coarse, explicitly-invoked whole-entity snapshots). No Citadel schema or file format is touched by this phase.
- **Persisted `GraphFingerprint` format.** Every Waypoint's payload embeds a `GraphFingerprint` string in the form `{version}:{blake3_hex}` — a versioned tag over the blake3 hex digest of the canonical, deterministically sorted byte stream of node ids (plus Paladin output fields), edge specs, schema field dispatch rules, the declared entry set, `defer_flags`, and `dynamic_targets` (excludes prompts/models, per D-04). This format was decided at Plan 22-01's Task 1 checkpoint (option-b), specifically so a future algorithm change is a detectable new tag rather than a silent invalidation of every stored thread's `resume`. **Bumped `v1:` -> `v2:` in Phase 22.1 (CR-01, D-17):** `v1`'s canonical byte encoding concatenated fields with unescaped ASCII delimiters (`|`, `-`, `:`) that were themselves legal `NodeId`/`FieldName` characters, so two structurally different graphs could be crafted to hash identically (e.g. two nodes `"a"`+`"b"` vs. one node `"a|nf|b"`). `v2` replaces this with a length-prefixed encoding with no delimiter collision. Every `v1:`-tagged fingerprint stored before this bump is recognized as stale (a version-tag mismatch) rather than silently reinterpreted under the new encoding. **Bumped `v4:` → `v5:` in Phase 25 (D-11, 25-03):** each node's own `set_aegis` sidecar entry's `on_error` and `cache` policies (and, separately, the graph-wide `default_aegis`) now enter the hashed byte stream, sorted by node id and length-prefixed, because they change what a run *does*; `retry` and `timeout` are deliberately excluded, exactly like every `EngineLimits` field, so tightening either never makes `resume` fail `GraphMismatch`. A `v4:`-tagged fingerprint is recognized as stale on resume, never reinterpreted.
- **Phase 25 (Node-Level Fault Tolerance, D-30): none — no SQL migration.** The node cache stores Redis keys (`{key_prefix}:{key}`, each a JSON `CachedDelta` carrying its own `schema_version`, expired server-side by `PSETEX`) or process-local in-memory entries, never a table. The Waypoint additions this phase makes — `NodeExecutionRecord.attempts`/`cache_hit`, `WaypointStatus::Failed.node_error`, `FieldSpec.cache` — are additive `#[serde(default)]` fields **inside the existing JSON `payload` column**, not new columns: `BATTLEFIELD_SCHEMA_VERSION` is unchanged (`1.0.0`, pinned by test), a pre-Phase-25 payload deserializes with the defaults, and the three-backend Waypoint contract suite round-trips both shapes. No migration file was added under `crates/paladin-storage/migrations/`.
- **New column: `garrison_entries.is_summary`. Landed Phase 26 (26-07), RT-03/RT-FR-12, D-17.** One new migration file, `crates/paladin-memory/migrations/002_add_garrison_is_summary.sql`: `ALTER TABLE garrison_entries ADD COLUMN is_summary INTEGER NOT NULL DEFAULT 0;`. `001_create_garrison_tables.sql` is byte-untouched — its version number and checksum are unchanged, so a v0.9 database's `_sqlx_migrations` row for `001` still verifies and `002` applies cleanly on top of it. No down migration (additive, out of scope per D-17).
- **Runs automatically at adapter construction, not manually.** `crates/paladin-memory/src/migrations.rs` declares the crate's one shared, compile-time-embedded migrator (`static MIGRATOR`, built from `crates/paladin-memory/migrations/`) — the **embedded migrator replaces the prior CWD-relative runtime path** (`sqlite_garrison.rs`'s old `Migrator::new("./migrations")` call, which depended on the process's current working directory containing a `migrations/` directory — fragile in a container). `SqliteGarrison::initialize()` now calls this one shared static; plan 26-09's `SqliteVault` will call the same one for its own `003_create_vault_tables.sql`, keeping the crate to a single `_sqlx_migrations` numbering sequence. No manual `sqlx migrate run` step is required of the operator. Idempotent: constructing `SqliteGarrison` twice against the same database file is safe, proven by `sqlite_garrison_constructs_twice_idempotently`; a pre-existing v0.9 database (only `001` applied) migrates forward with its rows intact, proven by `existing_v0_9_database_migrates_forward` (the pre-existing row reads back `is_summary == false`, matching the column default and the struct's `#[serde(default)]`).
- **The root `migrations/` mirror is removed in the same change.** The byte-identical copy that lived at the repo root (`migrations/001_create_garrison_tables.sql`) existed only to satisfy the old runtime path's `./migrations`-relative lookup in a container; with the embedded migrator, no runtime reader of that directory remains. `Dockerfile:28` (`COPY migrations ./migrations`) and `Dockerfile:57` (`COPY --from=builder /app/migrations /app/migrations`) are deleted, and `docs/src/deployment/docker.md`'s matching lines are updated to state that migrations are embedded in the binary and require no copy step.
- **Storage growth: negligible.** One `INTEGER` (SQLite stores small integers in as little as 1 byte) per existing `garrison_entries` row.
- **Existing state is unaffected beyond the one additive column.** Existing Citadel JSON/SQLite state files remain readable unchanged — `is_summary` is scoped to the `garrison_entries` table only.
- **New table: `vault_records`. Landed Phase 26 (26-09), RT-04/RT-FR-13…16, D-18/D-23.** One new migration file, `crates/paladin-memory/migrations/003_create_vault_tables.sql`: `CREATE TABLE IF NOT EXISTS vault_records (ns TEXT NOT NULL, key TEXT NOT NULL, value TEXT NOT NULL, created_at TEXT NOT NULL, updated_at TEXT NOT NULL, PRIMARY KEY (ns, key))` plus an index on `(ns, key)` — `SqliteVault`'s persistent backing store for the Vault (RT-04). `ns` is the `Namespace`'s `/`-joined path, unambiguous because a segment can never itself contain `/`. Both `001_create_garrison_tables.sql` and `002_add_garrison_is_summary.sql` are byte-untouched. No down migration (additive, out of scope). **Runs automatically, via the same shared embedded migrator §9.4's `002` entry describes** — `crates/paladin-memory/src/migrations.rs`'s single `static MIGRATOR` now embeds all three files, so `SqliteGarrison` and `SqliteVault` share one `_sqlx_migrations` numbering sequence: a database that only ever constructed a `SqliteGarrison` gains an empty `vault_records` table too, and vice versa (`IF NOT EXISTS` on both statements — documented, harmless, D-23). `SqliteVault::new(path)` constructs twice idempotently (the `SqliteWaypointStore`/`SqliteGarrison` precedent test). **Storage growth:** bounded by the Vault's own `max_value_bytes` cap (default 64 KiB) per value; no automatic pruning — a value persists until explicitly deleted, by design (RT-04's "until explicitly deleted" lifetime). **Existing state is unaffected beyond the one additive table** — `garrison_entries` and every Citadel JSON/SQLite state file remain readable unchanged.
- **New table: `runs`. Landed Phase 27 (27-02), PLAT-01/PLAT-02, D-04/D-05/D-17/D-18.** Two versioned migration files, one per backend, following the `waypoints` precedent's per-backend subdirectory split: `crates/paladin-storage/migrations/sqlite/002_create_runs_table.sql` and `crates/paladin-storage/migrations/postgres/002_create_runs_table.sql`. Columns: `run_id` (primary key), `thread_id`, `assistant_id`/`assistant_version`, `status`, `input`, `submitted_at`/`started_at`/`finished_at`, `attempt`, `cancel_requested`, `error`, `webhook`, `pending_responses`, `fork_from`, `output`, `final_waypoint_id`, `schema_version`. SQLite stores the JSON-shaped columns (`input`, `webhook`, `pending_responses`, `fork_from`, `output`) as `TEXT`; PostgreSQL stores them as `JSONB`, bound via an explicit `::jsonb` cast on write (the `payload` precedent from `001`). Two indexes: `idx_runs_thread_active`, a **partial unique index** on `runs(thread_id) WHERE status IN ('queued','running','awaiting_input')` — this IS the one-active-run-per-thread invariant (D-17), enforced by the database rather than an application check, with `AwaitingInput` deliberately included in the busy set (D-18); and `idx_runs_submitted` on `(submitted_at DESC, run_id DESC)`, serving `RunRepositoryPort::list`'s documented ordering. No `run_status_history` table (D-05): every status transition is a single compare-and-set `UPDATE runs SET status = ?to WHERE run_id = ?id AND status = ?from` (D-04) on this one row; the Waypoint chain (`GET /threads/{id}/history`, Phase 24 D-27) is already the execution audit trail. **Runs automatically, not manually,** applied on construction of `SqliteRunRepository`/`PostgresRunRepository` via `sqlx::migrate!`, exactly the `waypoints` precedent — no manual `sqlx migrate run` step, idempotent across repeated construction. **The InMemory adapter (`InMemoryRunRepository`) needs no migration** — its own write-lock check and CAS-by-equality logic are the in-process twin of the same two invariants (D-17/D-18/D-04), proven identical by the one shared contract suite (`crates/paladin-storage/src/run/contract_tests.rs`) all three adapters pass unchanged. **Reversibility:** not reversible without data loss (dropping `runs` discards all run history); no `down` migration, consistent with every existing migration in this workspace. **Storage growth:** one row per submitted run, bounded by however many runs a deployment retains — no automatic pruning in this phase. **Existing state is unaffected** — `runs` is entirely new and additive; no existing table or Citadel/Waypoint file format is touched.
- **New tables: `assistants` and `assistant_versions`. Landed Phase 27 (27-09), PLAT-04, D-28/D-29/D-30.** Two versioned migration files, one per backend, following the `waypoints`/`runs` precedent's per-backend subdirectory split: `crates/paladin-storage/migrations/sqlite/003_create_assistants_tables.sql` and `crates/paladin-storage/migrations/postgres/003_create_assistants_tables.sql`. `assistants` columns: `assistant_id` (primary key), `latest`, `source`, `created_at`, `deleted_at` (nullable — soft-delete flag), `schema_version`. `assistant_versions` columns: `assistant_id`, `version`, `kind`, `body`, `created_at`, `created_by` (nullable), `note` (nullable), `schema_version`, with **PRIMARY KEY `(assistant_id, version)`** — this IS the D-29 immutability invariant: no adapter in this tree ever executes an `UPDATE` against this table's `kind`/`body` columns (grep-verified: `grep -rc 'UPDATE assistant_versions' crates/paladin-storage/src/assistant/` is `0` in every file), there is no `update_version` method on `AssistantRepositoryPort`, and no `PUT` route is ever registered above it — immutability holds because the code to violate it does not exist. SQLite stores `body` as `TEXT` holding serialized JSON (the `runs.input` precedent from `002`); PostgreSQL stores it as `JSONB`, bound via an explicit `::jsonb` cast on write. An index, `idx_assistant_versions_assistant_version` on `(assistant_id, version)`, documents `AssistantRepositoryPort::list_versions`'s ascending-`version` access pattern alongside the primary key. **`RunRepositoryPort::insert_with_latest` (D-30)** resolves and freezes a run's `assistant_version` from `assistants.latest` inside the SAME `INSERT ... SELECT ... FROM assistants` statement that inserts the `runs` row (`crates/paladin-storage/src/run/{sqlite,postgres}.rs`) — proven under true concurrency by `assistant_version_freeze_at_submit` (20 alternating `append_version`/`insert_with_latest` calls: every resolved version exists and is `<=` the final `latest`). **Runs automatically, not manually,** applied on construction of `SqliteAssistantRepository`/`PostgresAssistantRepository` (and, transitively, `SqliteRunRepository`/`PostgresRunRepository`, since `sqlx::migrate!` embeds every file under its `migrations/` directory) via `sqlx::migrate!`, exactly the `waypoints`/`runs` precedent — no manual `sqlx migrate run` step, idempotent across repeated construction. **The InMemory adapter (`InMemoryAssistantRepository`) needs no migration** — its own write-lock-serialized `create`/`append_version` are the in-process twin of the same PK invariant, proven identical by the one shared contract suite (`crates/paladin-storage/src/assistant/contract_tests.rs`) all three adapters pass unchanged. **Reversibility:** not reversible without data loss (dropping either table discards all assistant version history); no `down` migration, consistent with every existing migration in this workspace. **Storage growth:** one `assistants` row per assistant plus one `assistant_versions` row per published version, unbounded by design (immutability means no version is ever pruned in this phase). **Existing state is unaffected** — both tables are entirely new and additive; no existing table or Citadel/Waypoint/Run file format is touched.
- **New table: `run_schedules`. Landed Phase 27 (27-11), PLAT-05, D-36/D-37/D-38/D-39.** Two versioned migration files, one per backend, following the `waypoints`/`runs`/`assistants` precedent's per-backend subdirectory split: `crates/paladin-storage/migrations/sqlite/004_create_run_schedules_table.sql` and `crates/paladin-storage/migrations/postgres/004_create_run_schedules_table.sql`. Columns: `schedule_id` (primary key), `assistant_id`, `assistant_version` (nullable), `cron`, `timezone`, `input`, `enabled`, `thread_strategy`, `on_missed`, `webhook` (nullable), `last_tick`/`next_tick` (nullable), `skipped_ticks`, `created_at`/`updated_at`, `schema_version`. SQLite stores the JSON-shaped columns (`input`, `thread_strategy`, `webhook`) as `TEXT` (the `runs.input`/`assistant_versions.body` precedent); PostgreSQL stores them as `JSONB`, bound via an explicit `::jsonb` cast on write. `on_missed` is a plain TEXT enum column (`'skip'` | `'run_once'`), not JSON — it carries no payload. One index, `idx_run_schedules_next_tick` on `(enabled, next_tick)`, serves `RunScheduleRepositoryPort::due`'s documented filter/ordering. **`RunScheduleRepositoryPort::claim_tick` (D-37) is the restart/replica-safety mechanism as a single conditional `UPDATE run_schedules SET last_tick = ?, next_tick = ? WHERE schedule_id = ? AND next_tick = ?`** — `rows_affected() == 1` means the caller won the race; `0` means it lost (a stale `expected_next`, or the row does not exist) — proven under true concurrency by `claim_tick_race_admits_exactly_one` (8 concurrent tasks racing one schedule's tick: exactly one succeeds) on all three adapters, and by the facade's own `schedule_restart_exactly_once`/`two_services_one_tick_exactly_one_fire` (`src/application/services/run/schedule/tests.rs`). **Runs automatically, not manually,** applied on construction of `SqliteRunScheduleRepository`/`PostgresRunScheduleRepository` via `sqlx::migrate!`, exactly the `waypoints`/`runs`/`assistants` precedent — no manual `sqlx migrate run` step, idempotent across repeated construction. **The InMemory adapter (`InMemoryRunScheduleRepository`) needs no migration** — its own write-lock-serialized `claim_tick` compare-and-swap is the in-process twin of the same CAS invariant, proven identical by the one shared contract suite (`crates/paladin-storage/src/run_schedule/contract_tests.rs`) all three adapters pass unchanged. **Not `tokio-cron-scheduler` (D-36):** the pre-existing `TokioCronSchedulerAdapter` (`crates/paladin-storage/src/scheduler.rs`) shares no table and is behaviourally untouched except for its own `validate_cron_field_count` now delegating its field-COUNTING primitive (not its six-field acceptance requirement) to the new, always-compiled `crates/paladin-storage/src/cron.rs` (X-03). **Reversibility:** not reversible without data loss (dropping `run_schedules` discards all schedule state, including `skipped_ticks` history); no `down` migration, consistent with every existing migration in this workspace. **Storage growth:** one row per schedule, bounded by however many schedules a deployment retains — no automatic pruning in this phase. **Existing state is unaffected** — `run_schedules` is entirely new and additive; no existing table or Citadel/Waypoint/Run/Assistant file format is touched.

- **New table: `webhook_deliveries`. Landed Phase 27 (27-13), PLAT-FR-14/15, D-40/D-41/D-42/D-43.** Two versioned migration files, one per backend, following the `waypoints`/`runs`/`assistants`/`run_schedules` precedent's per-backend subdirectory split: `crates/paladin-storage/migrations/sqlite/005_create_webhook_deliveries_table.sql` and `crates/paladin-storage/migrations/postgres/005_create_webhook_deliveries_table.sql`. Columns: `delivery_id` (primary key), `run_id`, `thread_id`, `event`, `url`, `payload` (the EXACT JSON string that is signed and sent — plain `TEXT` on BOTH backends, never `JSONB`, since it is a byte buffer to sign, not a value to query by shape, D-41), `attempt`, `max_attempts`, `status`, `next_attempt_at`, `last_response_status` (nullable), `last_error` (nullable), `created_at`/`updated_at`, `schema_version`. **No signing-key column exists on this table** (prohibition P1) — the HMAC signing value lives on the run's own `runs.webhook` column (`002`) and is read from there at send time, never copied onto a delivery row. Two indexes: `idx_webhook_deliveries_due` on `(status, next_attempt_at)`, serving `WebhookDeliveryRepositoryPort::claim_due`'s documented `Pending|Retrying` + due filter; `idx_webhook_deliveries_run` on `(run_id, created_at DESC)`, serving `list_for_run`'s documented ordering. **`WebhookDeliveryRepositoryPort::claim_due` (D-40) is the restart/durability mechanism as a per-row conditional `UPDATE webhook_deliveries SET status = 'in_flight', updated_at = ? WHERE delivery_id = ? AND status IN ('pending', 'retrying') AND next_attempt_at <= ?`** — `rows_affected() == 1` means the caller won that row's claim; `0` means a concurrent caller already took it — proven under true concurrency by `claim_due_race_admits_each_row_once` (8 tasks racing 6 due rows: the union of every claimed batch admits each row exactly once) on all three adapters. **Runs automatically, not manually,** applied on construction of `SqliteWebhookDeliveryRepository`/`PostgresWebhookDeliveryRepository` via `sqlx::migrate!`, exactly the `waypoints`/`runs`/`assistants`/`run_schedules` precedent — no manual `sqlx migrate run` step, idempotent across repeated construction. **The InMemory adapter (`InMemoryWebhookDeliveryRepository`) needs no migration** — its own write-lock-serialized `claim_due` is the in-process twin of the same CAS invariant, proven identical by the one shared 10-clause contract suite (`crates/paladin-storage/src/webhook/contract_tests.rs`) all three adapters pass unchanged. **Reversibility:** not reversible without data loss (dropping `webhook_deliveries` discards all delivery-attempt history); no `down` migration, consistent with every existing migration in this workspace. **Storage growth:** one row per enqueued delivery (one per subscribed lifecycle event per run), bounded by however many runs/webhooks a deployment retains — no automatic pruning in this phase. **Existing state is unaffected** — `webhook_deliveries` is entirely new and additive; no existing table or Citadel/Waypoint/Run/Assistant/RunSchedule file format is touched. **Delivery outcome never affects run status** — every attempt is persisted and queryable on this table independently of the `runs` row's own status column (prohibition P2).

## 9.5 Configuration & environment

Every new runtime behavior this phase introduces is tunable and **disabled or defaulted to today's behavior out of the box** (X-09), so a v0.9 configuration file boots v0.10 with identical behavior. This claim will be backed by an integration test that boots the server with the v0.9 sample config and asserts feature/config resolution to legacy behavior — TBD, owner SHIP-02, Phase 29.

Config structs mirror the existing `CitadelConfig` shape at `src/config/citadel.rs`: `Default` + `validate()` + `EnvOverridable`.

- **`EngineConfig`** (`src/config/engine.rs`) — **landed this plan (23-07, Task 1)**:
  - `max_supersteps: u64` — default `50`. Env: `APP_ENGINE_MAX_SUPERSTEPS`.
  - `max_node_visits: u32` — default `25`. Env: `APP_ENGINE_MAX_NODE_VISITS`.
  - `run_timeout_secs: Option<u64>` — default `None` (no run-level bound). Env: `APP_ENGINE_RUN_TIMEOUT_SECS`. **Landed Phase 25 (FT-03, D-20, Plan 25-09)** — declared as plumbing in 23-07, the semantics now carry: `Some(n)` becomes `EngineLimits.run_timeout = Duration::from_secs(n)` (pinned by `config_seconds_convert_to_duration_exactly`), a wall-clock budget for the **whole** run measured from the start of each `WarEngine::start`/`resume` call (a resume restarts it; a `Battalion` child measures its own budget against its own limits). Exceeding it ends the run with the typed `EngineError::RunTimeoutExceeded { elapsed, limit }` through the same `Failed`-Waypoint path `RecursionLimitExceeded`/`NodeVisitLimitExceeded` take. It nests *outside* every per-node `TimeoutPolicy`: an attempt's effective deadline is `min(its run_timeout, the remaining run budget)`, the fired `TimeoutKind` names whichever was tightest, and an attempt the engine budget cuts records `Timeout(EngineRun)` — which is never retried. Never hashed into the graph fingerprint.
  - `waypoint_durability: WaypointDurability` (`Strict` | `BestEffort`) — default `Strict`. Env: `APP_ENGINE_WAYPOINT_DURABILITY`.
  - `max_muster_tasks: u32` — default `100` (CF-FR-13, D-16). Env: `APP_ENGINE_MAX_MUSTER_TASKS`. `validate()` rejects `0`.
  - `shutdown_grace_secs: u64` — default `30` (HITL-04, D-20). Env: `APP_ENGINE_SHUTDOWN_GRACE_SECS`. Seconds a mid-superstep cancellation races the in-flight batch of spawned node tasks against before aborting stragglers; also the upper bound `paladin-server`/`ServiceRunner` wait on SIGTERM/SIGINT before exiting. `validate()` rejects values above `3600` (1 hour). **Landed this plan (24-08).**
  - `graceful_shutdown: bool` — default `true` (HITL-04, D-20). Env: `APP_ENGINE_GRACEFUL_SHUTDOWN`. Set `false` to skip the shutdown-grace wait entirely and exit immediately on SIGTERM/SIGINT — the M-B-02 disable switch for legacy-only deployments (§9.1). **Landed this plan (24-08).**
  - Converts into `EngineLimits`/`WaypointDurability` via `impl From<EngineConfig> for EngineLimits` (`src/config/engine.rs`); `waypoint_durability` is read directly off the source `EngineConfig` value and passed to `WarEngine::with_durability` separately. `shutdown_grace_secs`/`graceful_shutdown` are deliberately excluded from this conversion — both are runtime-only settings, never hashed into `WarGraph::fingerprint()` (proven by `shutdown_grace_does_not_change_the_graph_fingerprint`).
  - Both new fields default to today's absent behavior (an unconfigured v0.9 deployment gets `shutdown_grace_secs=30`, `graceful_shutdown=true` — the same values a v0.10 deployment that never mentions either env var gets), so upgrading changes nothing about a workflow's own behavior; it only changes what the *process* does on SIGTERM/SIGINT (see M-B-02, §9.1).
- **`WaypointRetentionConfig`** (`src/config/waypoint_retention.rs`) — **landed this plan (22-06, Task 3)**:
  - `enabled: bool` — default `false` (no pruning runs until an operator opts in — the new subsystem is off by default). Env: `APP_WAYPOINT_RETENTION_ENABLED`.
  - `max_age_days: Option<u32>` — default `None`. Env: `APP_WAYPOINT_RETENTION_MAX_AGE_DAYS`. `validate()` rejects `Some(0)`.
  - `max_waypoints_per_thread: Option<u32>` — default `None`. Env: `APP_WAYPOINT_RETENTION_MAX_WAYPOINTS_PER_THREAD`. `validate()` rejects `Some(0)`.
  - This config is driven through `WaypointRetentionService` (`src/application/services/waypoint_retention.rs`, Plan 22-14), which holds the one definition of "protected" — a thread's latest Waypoint plus every `AwaitingInput` Waypoint (ENG-FR-18, T-22-21) — and hands it to `paladin_storage::waypoint::retention::prune` as an argument. The routine itself no longer decides what protected means; it unions whatever it is handed with the configured bounds and removes the rest through `WaypointPort::prune_thread` (Plan 22-13), a keep-set primitive that never touches a protected id — not the delete-then-resave sequence this section originally described. Monotone, crash-safe, and idempotent by construction; runs unchanged over `InMemoryWaypointStore`, `SqliteWaypointStore`, and `PostgresWaypointStore`.

Since every new field either defaults to today's absent behavior (`enabled: false`, no retention pruning) or to a bounded-but-generous limit that only applies to the *new* `WarEngine` path (legacy Formation/Phalanx/Campaign call sites never construct an `EngineConfig`), a v0.9 configuration file — which has no `engine:`/`waypoint_retention:` section at all — resolves to identical runtime behavior: `EngineConfig::default()` converts to exactly `EngineLimits::default()` and `WaypointDurability::Strict`, asserted mechanically by `config::engine::tests::default_engine_config_matches_todays_engine_defaults` rather than left as an unchecked claim.

- **`LlmDecision` and Commander `StrategySelection::Semantic` add no config struct or environment variable (CF-05, D-26).** Both hold `Arc<dyn LlmPort>` trait objects assembled and passed in code — an `LlmDecisionEvaluator` registered by name through `EdgeEvaluatorRegistry`; a `StrategySelection::Semantic { llm, model }` passed to `CommanderBuilder::strategy_selection` — never resolved through an `APP_*` environment variable or a config-file section, so a v0.9 deployment gains no new outbound, paid, non-deterministic model call merely by upgrading (`grep -rn 'APP_LLM_DECISION\|APP_.*SEMANTIC' src/ crates/` returns no matches). `on_ambiguous` (per-`LlmDecisionEvaluator`) and `on_parse_error` (per-node `DirectiveParser`, CF-02) are likewise per-evaluator/per-node enums set in code, not runtime-global tunables — X-09 requires a config struct only for a *tunable*, and neither of these is one.

- **`NodeCacheConfig`** (`src/config/node_cache.rs`) — **landed Phase 25 (FT-06, D-29, Plan 25-04)**, mirroring `WaypointStoreConfig`'s shape (`Default` + `validate()` + `EnvOverridable`). **Off by default**: a v0.9 configuration file, which has no `node_cache:` section, resolves to `enabled: false` and no cache is wired, so no upgraded deployment gains new caching behavior.
  - `enabled: bool` — default `false`. Env: `APP_NODE_CACHE_ENABLED`.
  - `backend: NodeCacheBackend` (`in_memory` | `redis`) — default `in_memory`. Env: `APP_NODE_CACHE_BACKEND`. `redis` additionally requires the `redis-cache` cargo feature (§9.3).
  - `redis_host: String` — default `localhost`. Env: `APP_NODE_CACHE_REDIS_HOST`. `validate()` rejects an empty host when `backend` is `redis`.
  - `redis_port: u16` — default `6379`. Env: `APP_NODE_CACHE_REDIS_PORT`. `validate()` rejects `0`.
  - `redis_password: Option<String>` — default `None`. Env: `APP_NODE_CACHE_REDIS_PASSWORD`. **Never rendered by the struct's hand-written `Debug` impl** (a fixed placeholder is printed instead — T-25-18, `debug_rendering_never_prints_the_password`).
  - `redis_db: u8` — default `0`. Env: `APP_NODE_CACHE_REDIS_DB`.
  - `key_prefix: String` — default `paladin:node_cache`. Env: `APP_NODE_CACHE_KEY_PREFIX`. `validate()` rejects an empty prefix. Every entry the Redis backend writes is namespaced under it.
  - This config selects the cache **backend** only. It resolves into a `RedisNodeCacheConfig` (or an `InMemoryNodeCache`) handed to `WarEngine::with_node_cache`; which nodes are cached, for how long and under which key is a per-node `CachePolicy` in code (below).
- **Aegis policies add no config struct and no environment variable (FT-01…FT-06, D-29; the Phase 23 D-26 rule for per-node enums).** `Aegis { retry, timeout, on_error, cache }` — `RetryPolicy`, `RetryPredicate`, `TimeoutPolicy`, `ErrorHandlerSpec`, `CachePolicy`/`CacheKeySpec` — is attached per node in code through `WarGraph::set_aegis`/`with_default_aegis`, and `RetryPredicate::Custom`/`ErrorHandlerSpec::Custom` names resolve through registries populated by `WarEngine::with_retry_predicate`/`with_error_handler`, exactly like `with_edge_evaluator`. No `APP_AEGIS_*`, `APP_ENGINE_RETRY_*` or `APP_ENGINE_ON_ERROR_*` variable exists (`grep -rn 'APP_AEGIS\|APP_ENGINE_RETRY\|APP_ENGINE_ON_ERROR' src/ crates/` returns no matches). A v0.9 graph declares no `Aegis`, so every node in it keeps one attempt, no bound, no handler and no cache — byte-identical to before (see §9.1).
- **`AgentRuntimeConfig`** (`src/config/agent_runtime.rs`) — **landed Phase 26 (RT-01…RT-07, D-10), one grouped config carrying every built-in middleware's own sub-struct.** Mirrors `NodeCacheConfig`'s shape (`Default` + `validate()` + `EnvOverridable`); every scalar field is overridable through `APP_AGENT_RUNTIME_<SECTION>_<FIELD>`, while regex rules, per-tool maps and model tables are config-file only (no env var can populate a `Vec`/`HashMap`-typed field). **Every one of the twelve sub-structs is inert by default** — proven mechanically by `default_agent_runtime_config_is_inert` (plan 26-02) and, downstream, `build_chain_on_a_default_config_returns_an_empty_chain` (plan 26-20): a v0.9 `config.yml`, which has no `agent_runtime:` section at all, resolves to every section below at its default and `AgentRuntimeConfig::build_chain` returning an empty `Vec`, so an upgraded deployment gains no new middleware behavior merely by upgrading.

  | Sub-struct | Default | Key env vars |
  |---|---|---|
  | `model_call_limit: ModelCallLimitConfig` | `enabled: false`, `max_calls: 10` | `APP_AGENT_RUNTIME_MODEL_CALL_LIMIT_ENABLED`, `_MAX_CALLS` |
  | `token_budget: TokenBudgetConfig` | `enabled: false`, `max_tokens: 100_000` | `APP_AGENT_RUNTIME_TOKEN_BUDGET_ENABLED`, `_MAX_TOKENS` |
  | `tool_call_limit: ToolCallLimitConfig` | `enabled: false`; `per_tool` config-file only | `APP_AGENT_RUNTIME_TOOL_CALL_LIMIT_ENABLED`, `_MAX_CALLS` |
  | `guardrail: GuardrailConfig` | `enabled: false`, `rules: []`, `pattern_size_limit_bytes: 65536` (`1 << 16`); `rules` config-file only | `APP_AGENT_RUNTIME_GUARDRAIL_ENABLED`, `_PATTERN_SIZE_LIMIT_BYTES` |
  | `history_trimmer: HistoryTrimmerConfig` | `enabled: false`; `model_context_limits` config-file only | `APP_AGENT_RUNTIME_HISTORY_TRIMMER_ENABLED`, `_RESERVE_FOR_RESPONSE`, `_DEFAULT_CONTEXT_TOKENS`, `_RECALL_LIMIT` |
  | `summarization: SummarizationConfig` | `enabled: false` | `APP_AGENT_RUNTIME_SUMMARIZATION_ENABLED`, `_THRESHOLD_TOKENS`, `_THRESHOLD_MESSAGES`, `_KEEP_RECENT`, `_SUMMARIZER_MODEL` |
  | `vault_recall: VaultRecallConfig` | `enabled: false` | `APP_AGENT_RUNTIME_VAULT_RECALL_ENABLED`, `_TOP_K`, `_SCORE_FLOOR` |
  | `model_retry: ModelRetryConfig` | `enabled: false`, mirrors `RetryPolicy::default()` | `APP_AGENT_RUNTIME_MODEL_RETRY_ENABLED`, `_MAX_ATTEMPTS`, `_INITIAL_INTERVAL_MS`, `_BACKOFF_FACTOR`, `_MAX_INTERVAL_MS`, `_JITTER`, `_RETRY_ON` |
  | `model_fallback: ModelFallbackConfig` | `enabled: false`; `providers` config-file only, never a credential | `APP_AGENT_RUNTIME_MODEL_FALLBACK_ENABLED` |
  | `tool_errors: ToolErrorConfig` | `mode: FeedToModel` (D-33 — a today's-behavior *policy* default, not `enabled: false`; `per_tool` config-file only) | `APP_AGENT_RUNTIME_TOOL_ERRORS_MODE` |
  | `structured: StructuredOutputConfig` | `max_repair_attempts: 1` | `APP_AGENT_RUNTIME_STRUCTURED_MAX_REPAIR_ATTEMPTS` |
  | `vault_tools: VaultToolsConfig` | `enabled: false` | `APP_AGENT_RUNTIME_VAULT_TOOLS_ENABLED` |

  No field on any of the twelve sub-structs is secret-shaped: `ModelFallbackConfig.providers` names providers by string only, credentials still resolve through the existing `LlmProviderFactory` env/config path. `AgentRuntimeConfig::build_chain(&self, deps: AgentRuntimeDeps) -> Result<Vec<Arc<dyn ExecutionMiddleware>>, ..>` assembles every enabled section in a fixed order (limits → guardrail → trimmer/summarizer → recall → protocol → resilience) and is what an operator or `paladin-server`'s agent provisioning calls to turn this one config block into an installed chain.

- **Platform API config structs (PLAT-01..06, D-50, X-09) — landed Phase 27 (plans 27-06, 27-17).** Seven new structs in `src/config/`, each mirroring `WaypointStoreConfig`'s shape (`Default` + `validate()` + `EnvOverridable`, no `Settings` field — read directly by `src/bin/paladin-server.rs::run()` via `Default::default()` + `apply_env_overrides()`, exactly like `WaypointStoreConfig` itself). **Six of the seven default to OFF or today's-behaviour**; `AssistantsConfig.expose_code_registry` is the one deliberate exception, defaulting `true` (D-32) because it only renders read-only data already discoverable through the pre-existing `AgentRegistry`. A v0.9 `config.yml` (no section for any of these — they are never part of this file's own YAML schema) boots this v0.10 binary with the run server fully disabled: `src/bin/paladin-server.rs::build_run_api` returns an unwired `RunApiState` and spawns no background task, and every `/v1/runs*`, `/v1/assistants*`, `/v1/schedules*` route answers `501 not_implemented` naming the config key to set (D-44).

  | Struct | File | Default | Key env vars |
  |---|---|---|---|
  | `RunStoreConfig`/`RunStoreBackend` | `src/config/run_store.rs` | `backend: disabled` | `APP_RUN_STORE_BACKEND` (`disabled`\|`sqlite`\|`postgres`), `APP_RUN_STORE_PATH` (sqlite), `APP_RUN_STORE_URL_ENV` (postgres — names the env var holding the connection url, never the url itself) |
  | `RunQueueConfig`/`RunQueueBackend` | `src/config/run_queue.rs` | `backend: in_memory` | `APP_RUN_QUEUE_BACKEND` (`in_memory`\|`redis`), `APP_RUN_QUEUE_URL_ENV`, `APP_RUN_QUEUE_KEY_PREFIX` (default `paladin:run_queue`) |
  | `RunWorkerConfig` | `src/config/run_worker.rs` | `concurrency: 4`, `lease_seconds: 60`, `min_probe_interval_ms: 1000` | `APP_RUN_WORKER_CONCURRENCY`, `APP_RUN_WORKER_LEASE_SECONDS`, `APP_RUN_WORKER_MIN_PROBE_INTERVAL_MS`. Deliberately no `heartbeat` field (D-10) — the worker derives `lease_seconds / 4` at runtime; `validate()` enforces `lease_seconds >= 4`. |
  | `RunStreamConfig` | `src/config/run_stream.rs` | `poll_interval_ms: 1000` | `APP_RUN_STREAM_POLL_INTERVAL_MS` — the degraded-mode `GET /runs/{id}/stream` polling interval (D-26). |
  | `AssistantsConfig` | `src/config/assistants.rs` | `expose_code_registry: true` (**the one ON-by-default exception**, D-32) | `APP_ASSISTANTS_EXPOSE_CODE_REGISTRY` |
  | `SchedulesConfig` | `src/config/schedules.rs` | `enabled: false`, `tick_interval_ms: 1000` | `APP_SCHEDULES_ENABLED`, `APP_SCHEDULES_TICK_INTERVAL_MS` |
  | `WebhooksConfig` | `src/config/webhooks.rs` | `allow_private: false`, `max_attempts: 5`, `timeout_secs: 10` | `APP_WEBHOOKS_ALLOW_PRIVATE` (the **only** override of the write-time/send-time SSRF guard, D-42), `APP_WEBHOOKS_MAX_ATTEMPTS`, `APP_WEBHOOKS_TIMEOUT_SECS` |

  `src/bin/paladin-server.rs::build_run_api` (plan 27-17) is the sole production reader: it loads all seven, converts `RunWorkerConfig`/`WebhooksConfig`/`SchedulesConfig` into `RunWorkerOptions`/`WebhookDeliveryOptions`/`ScheduleServiceOptions` (`impl From<&…Config>` kept in `src/infrastructure/web/run_api_wiring.rs`, never in `src/config/`, per this phase's own convention), and fails closed — naming the missing cargo feature — if `run_store` names `postgres`/`run_queue` names `redis` on a binary built without `storage-postgres`/`redis-queue` (§9.3), or if `run_store` is enabled with no `waypoint_store` backend wired (naming `waypoint_store.backend`).

## 9.6 HTTP API

**New engine-backed endpoints — landed this phase (HITL-05, Plan 24-11):** three thread routes in
`paladin-web`, nested under `/v1` (ADR-0037) and authenticated by the same middleware `/v1/agents/*`
already uses. `ThreadApiState` is a new, separate router state (`AgentApiState` is untouched — a
deliberate zero, §9.2). Every route answers `501 not_implemented` (naming
`APP_WAYPOINT_STORE_BACKEND`) when no waypoint backend is configured — the spec still lists the
paths regardless of runtime wiring.

| Method | Path | Status codes |
|---|---|---|
| `GET` | `/v1/threads/{id}/state` | `200`, `400`, `401`, `404`, `501` |
| `POST` | `/v1/threads/{id}/resume` | `202`, `400`, `401`, `403`, `404`, `409` (two distinct envelope `code`s — `thread_not_awaiting_input`, `graph_not_registered` — sharing this one HTTP status), `501` |
| `GET` | `/v1/threads/{id}/history` | `200`, `400`, `401`, `501` |

`POST .../resume` additionally requires an admin-role credential via
`crate::agent_auth::require_admin`, returning `403` for an authenticated non-admin caller. This
narrows D-24's "authenticated callers, any role" default for the one mutating route only (CR-01,
`24-REVIEW.md`, commit `00b1e552`), because neither `ThreadId` nor `Waypoint` carries an
owner/principal to scope against yet. `GET .../state` and `GET .../history` remain
authenticated-any-role. Every credential configured for the resume route must be treated as
admin-equivalent until PLAT-06 (Phase 27) supplies real per-thread ownership scoping;
`crates/paladin-web/src/thread_controller.rs`'s module rustdoc carries the full rationale.

`POST .../resume` returns `202 Accepted { thread_id, state_url }` immediately — the actual engine
continuation runs as a background task registered with the process's `ShutdownCoordinator`
(D-25); a client polls `GET .../state` for the outcome, never holding the connection open.
`GET .../history` paginates with `limit` (≤ 100) and an opaque `cursor` (the last page's own
`waypoint_id`, D-27). `crates/paladin-web/openapi.json` was regenerated with these three paths —
purely additive (553 insertions, 0 deletions) — and every pre-existing `/v1/agents/*` path is
byte-identical, both by raw diff and by a dedicated drift-guard test
(`openapi_pre_existing_agent_paths_are_unchanged`).

**Still owed, not this phase's scope:** the golden `openapi.json` diff proof that every
pre-existing `/v1` path is unchanged across the *whole* v0.10.0 program (not just this phase's own
additions) is **SHIP-02, Phase 29**. The broader platform surface — background run submission,
assistants, schedules, admin/writer scopes on these same thread routes (PLAT-06) — is
**PLAT-01…PLAT-06, Phase 27**.

**`POST /v1/threads/{id}/resume` response body gains `run_id` — landed Phase 27 (PLAT-03, Plan
27-08).** The status-code table two paragraphs above is unchanged verbatim (`202`, `400`, `401`,
`403`, `404`, `409`, `501`, D-20) — only the mechanism behind `ParleyPort::resume_with` changed,
from Phase 24's in-process `tokio::spawn` continuation to a durable re-enqueue of the SAME `run_id`
under the run server's queue (PLAT-FR-06, D-19, D-23; see the `ParleyPortAdapter` row in §9.2
immediately below). The response body is now `202 Accepted { thread_id, state_url, run_id }`:
`run_id` is a nullable string, present whenever the run-server path handled the resume, and `null`
only for a thread suspended before a run-server (`RunRepositoryPort`/`RunQueuePort`) was wired at
all (X-03 — the exact Phase 24 in-process-spawn behaviour is preserved for that one case).
`crates/paladin-web/openapi.json` was regenerated; the only diff is the new nullable `run_id`
property on the `ResumeAcceptedResponse` schema (`git diff --stat` shows 8 insertions, 1 deletion),
verified by `openapi_matches_committed_baseline` and confirmed by direct JSON-path inspection that
every other schema and path is byte-identical.

**Remaining run routes, thread list/get/fork/delete, and pagination everywhere — landed Phase 27
(PLAT-06, Plan 27-15, D-45…D-47).** The last new `/v1/runs*` routes this phase's `RunApiState`
carries, plus four new routes on the existing `ThreadApiState` (D-45), completing PRD 06 §2.1's
run/thread surface and PLAT-FR-16's "pagination, auth and scopes are consistent everywhere" claim.

| Method | Path | Status codes |
|---|---|---|
| `GET` | `/v1/runs` | `200`, `400`, `401`, `501` |
| `POST` | `/v1/runs/{run_id}/cancel` | `202`, `400`, `401`, `403`, `404`, `409`, `501` |
| `GET` | `/v1/runs/{run_id}/webhook-deliveries` | `200`, `400`, `401`, `501` |
| `GET` | `/v1/threads` | `200`, `400`, `401`, `501` |
| `GET` | `/v1/threads/{id}` | `200`, `400`, `401`, `404`, `501` |
| `POST` | `/v1/threads/{id}/fork` | `202`, `400`, `401`, `403`, `404`, `409`, `501` |
| `DELETE` | `/v1/threads/{id}` | `204`, `400`, `401`, `403`, `404`, `409`, `501` |

**Pagination (D-47), applied verbatim on every list route in this table plus the pre-existing
`/v1/assistants*`/`/v1/schedules*`/`/v1/assistants/{id}/versions` routes:** `limit` (an omitted
value defaults to 20; `0` or a value above 100 is `400 bad_request`, message "limit must be
between 1 and 100") and an opaque `cursor` (`400 invalid_cursor` on anything malformed — never a
`500`, never a silent full-table scan, never the cursor's internal encoding in the error message).
`{ items, next_cursor }`, `next_cursor: null` on the last page, `{ items: [], next_cursor: null }`
on an empty result (never a bare array, never `404`). The cursor is a keyset walk — a STABLE
ordering for rows that already existed when the first page was fetched, but NOT a point-in-time
snapshot: a row inserted after the first page was read may be omitted from the walk. Every list
route's own `#[utoipa::path]` description states this explicitly. `crates/paladin-web/src/pagination.rs`
(`resolve_limit`, `encode_cursor`/`decode_cursor`, base64url no-padding over compact JSON) is the
one shared implementation `run_controller.rs`/`thread_controller.rs`/`assistant_controller.rs`/
`schedule_controller.rs` all call — the two latter files' limit handling switched from a locally
duplicated `parse_limit` to this shared helper in the same plan, with no behavior change beyond
the newly typed `limit == 0` rejection (previously silently defaulted to 20 in both files).

**Two-tier authorization scopes (D-46), applied on every mutating route across this whole phase:**
invocation-shaped (`POST /runs`, `POST /runs/{id}/cancel`, `POST /threads/{id}/resume`, `POST
/threads/{id}/fork`) — any authenticated principal, subject to the target assistant's own
`allowed_roles` (empty ⇒ any authenticated caller), authorized entirely inside
`RunSubmissionService` (`paladin-web` never resolves `allowed_roles` itself, ADR-0031); vs.
registry-shaped (`POST`/`DELETE /assistants*`, `POST`/`PATCH`/`DELETE /schedules*`, `DELETE
/threads/{id}`) — `require_admin`. Reads need authentication only. Proven at the router level (not
just per-file) by `run_controller.rs`'s `run_controller_auth` test group, which hits a
merged-in `/v1/assistants` admin route through the SAME `run_router` this table's own routes
mount on.

**Rate limiting (pre-existing `tower-governor` layer, D-44) proven on `/v1/runs*` for the first
time this plan** — `run_controller_auth::rate_limited_request_is_429` applies the SAME
`apply_rate_limit`/`RateLimitConfig` `/v1/agents/*` already used, confirming the layer generalizes
to the new router with no route-specific wiring.

`crates/paladin-web/openapi.json` was regenerated; the diff is exactly these seven new paths and
their schemas, verified by `openapi_matches_committed_baseline` — every pre-existing path and
schema is byte-identical.

**SsrfGuard collapsed onto one shared implementation (D-42), not a new endpoint but recorded here
since the wave_context called it out explicitly.** 27-14 (parallel wave, `src/application/
services/run/schedule/admin.rs`) built its own standalone `SsrfGuard` because 27-13's real guard
was not in that plan's worktree base at the time. This plan deletes that duplicate and routes
`ScheduleService`'s write-time webhook check through `src/application/services/run/webhook::SsrfGuard`
(the same guard 27-13's webhook client already uses at send time) — one guard, two call sites, no
behavior change to either.

## 9.7 Deprecations

Empty — no item is marked `#[deprecated]` by any phase so far, and **Phase 25 (FT) deliberately adds none (X-03)**: the legacy Battalion `RetryPolicy`, `ErrorStrategy` and `NodeError` summary types under `paladin_core::platform::container::battalion`, and the legacy Formation/Phalanx/Campaign timeout handling, remain live and undeprecated alongside the new `Aegis` family; the retained `PaladinError::LlmError(String)` variant, now constructed by no first-party production code (25-06), is likewise left undeprecated. Entries are added here as producing epics ship theirs; this section is finalized (or confirmed empty) at closeout, owner SHIP-01, Phase 29.

## 9.8 Upgrade checklist

TBD — a full ordered, copy-pasteable operator checklist (back up state dirs/DBs → apply migrations → update config → adjust termination grace → register custom evaluators if used → deploy → verify with `paladin-cli` health/graph-validate commands) is written once the subsystems it references exist to check against (migrations land with ENG-05; termination grace **landed with HITL-04, Phase 24** — set `terminationGracePeriodSeconds` to at least `60` (2 × the 30s default `APP_ENGINE_SHUTDOWN_GRACE_SECS`), or at least twice whatever value you configure that env var to, in every Deployment manifest before rolling out this upgrade; custom evaluator registration **landed with CF-01, Phase 23** — `CampaignExecutionService::with_evaluator` on the legacy path, `WarEngine::with_edge_evaluator` on the `WarEngine` path, see M-B-01). Finalized at closeout, owner SHIP-01, Phase 29.
